import { NamedNode } from '@rdfjs/types';

/** Namespace URI of the SCHEMA_NQ vocabulary. */
export const _SCHEMA_NQ = 'https://schema.org/';

export const SCHEMA_NQ = {
	/** A 3D model represents some kind of 3D content, which may have [[encoding]]s in one or more [[MediaObject]]s. Many 3D formats are available (e.g. see [Wikipedia](https://en.wikipedia.org/wiki/Category:3D_graphics_file_formats)); specific encoding formats can be represented using the [[encodingFormat]] property applied to the relevant [[MediaObject]]. For the
case of a single file published after Zip compression, the convention of appending '+zip' to the [[encodingFormat]] can be used. Geospatial, AR/VR, artistic/animation, gaming, engineering and scientific content can all be represented using [[3DModel]]. */
	'_3DModel': 'https://schema.org/3DModel',
	/** A radio channel that uses AM. */
	'AMRadioChannel': 'https://schema.org/AMRadioChannel',
	/** Reference documentation for application programming interfaces (APIs). */
	'APIReference': 'https://schema.org/APIReference',
	/** Abdomen clinical examination. */
	'Abdomen': 'https://schema.org/Abdomen',
	/** Web page type: About page. */
	'AboutPage': 'https://schema.org/AboutPage',
	/** The act of committing to/adopting an object.\n\nRelated actions:\n\n* [[RejectAction]]: The antonym of AcceptAction. */
	'AcceptAction': 'https://schema.org/AcceptAction',
	/** An accommodation is a place that can accommodate human beings, e.g. a hotel room, a camping pitch, or a meeting room. Many accommodations are for overnight stays, but this is not a mandatory requirement.
For more specific types of accommodations not defined in schema.org, one can use [[additionalType]] with external vocabularies.
<br /><br />
See also the <a href="/docs/hotels.html">dedicated document on the use of schema.org for marking up hotels and other forms of accommodations</a>.
 */
	'Accommodation': 'https://schema.org/Accommodation',
	/** Accountancy business.\n\nAs a [[LocalBusiness]] it can be described as a [[provider]] of one or more [[Service]]\(s).
       */
	'AccountingService': 'https://schema.org/AccountingService',
	/** The act of accomplishing something via previous efforts. It is an instantaneous action rather than an ongoing process. */
	'AchieveAction': 'https://schema.org/AchieveAction',
	/** An action performed by a direct agent and indirect participants upon a direct object. Optionally happens at a location with the help of an inanimate instrument. The execution of the action may produce a result. Specific action sub-type documentation specifies the exact expectation of each argument/role.\n\nSee also [blog post](http://blog.schema.org/2014/04/announcing-schemaorg-actions.html) and [Actions overview document](https://schema.org/docs/actions.html). */
	'Action': 'https://schema.org/Action',
	/** A set of requirements that must be fulfilled in order to perform an Action. */
	'ActionAccessSpecification': 'https://schema.org/ActionAccessSpecification',
	/** The status of an Action. */
	'ActionStatusType': 'https://schema.org/ActionStatusType',
	/** The act of starting or activating a device or application (e.g. starting a timer or turning on a flashlight). */
	'ActivateAction': 'https://schema.org/ActivateAction',
	/** Represents the activation fee part of the total price for an offered product, for example a cellphone contract. */
	'ActivationFee': 'https://schema.org/ActivationFee',
	/** An in-progress action (e.g., while watching the movie, or driving to a location). */
	'ActiveActionStatus': 'https://schema.org/ActiveActionStatus',
	/** Active, but not recruiting new participants. */
	'ActiveNotRecruiting': 'https://schema.org/ActiveNotRecruiting',
	/** The act of editing by adding an object to a collection. */
	'AddAction': 'https://schema.org/AddAction',
	/** A geographical region, typically under the jurisdiction of a particular government. */
	'AdministrativeArea': 'https://schema.org/AdministrativeArea',
	/** An adult entertainment establishment. */
	'AdultEntertainment': 'https://schema.org/AdultEntertainment',
	/** Enumeration of considerations that make a product relevant or potentially restricted for adults only. */
	'AdultOrientedEnumeration': 'https://schema.org/AdultOrientedEnumeration',
	/** An [[Article]] that an external entity has paid to place or to produce to its specifications. Includes [advertorials](https://en.wikipedia.org/wiki/Advertorial), sponsored content, native advertising and other paid content. */
	'AdvertiserContentArticle': 'https://schema.org/AdvertiserContentArticle',
	/** Physical activity of relatively low intensity that depends primarily on the aerobic energy-generating process; during activity, the aerobic metabolism uses oxygen to adequately meet energy demands during exercise. */
	'AerobicActivity': 'https://schema.org/AerobicActivity',
	/** When a single product is associated with multiple offers (for example, the same pair of shoes is offered by different merchants), then AggregateOffer can be used.\n\nNote: AggregateOffers are normally expected to associate multiple offers that all share the same defined [[businessFunction]] value, or default to http://purl.org/goodrelations/v1#Sell if businessFunction is not explicitly defined. */
	'AggregateOffer': 'https://schema.org/AggregateOffer',
	/** The average rating based on multiple ratings or reviews. */
	'AggregateRating': 'https://schema.org/AggregateRating',
	/** The act of expressing a consistency of opinion with the object. An agent agrees to/about an object (a proposition, topic or theme) with participants. */
	'AgreeAction': 'https://schema.org/AgreeAction',
	/** An organization that provides flights for passengers. */
	'Airline': 'https://schema.org/Airline',
	/** An airport. */
	'Airport': 'https://schema.org/Airport',
	/** AlbumRelease. */
	'AlbumRelease': 'https://schema.org/AlbumRelease',
	/** Item contains alcohol or promotes alcohol consumption. */
	'AlcoholConsideration': 'https://schema.org/AlcoholConsideration',
	/** An intangible item that describes an alignment between a learning resource and a node in an educational framework.

Should not be used where the nature of the alignment can be described using a simple property, for example to express that a resource [[teaches]] or [[assesses]] a competency. */
	'AlignmentObject': 'https://schema.org/AlignmentObject',
	/** All-wheel Drive is a transmission layout where the engine drives all four wheels. */
	'AllWheelDriveConfiguration': 'https://schema.org/AllWheelDriveConfiguration',
	/** Content about the allergy-related aspects of a health topic. */
	'AllergiesHealthAspect': 'https://schema.org/AllergiesHealthAspect',
	/** The act of organizing tasks/objects/events by associating resources to it. */
	'AllocateAction': 'https://schema.org/AllocateAction',
	/** A creative work with a visual storytelling format intended to be viewed online, particularly on mobile devices. */
	'AmpStory': 'https://schema.org/AmpStory',
	/** An amusement park. */
	'AmusementPark': 'https://schema.org/AmusementPark',
	/** Physical activity that is of high-intensity which utilizes the anaerobic metabolism of the body. */
	'AnaerobicActivity': 'https://schema.org/AnaerobicActivity',
	/** An AnalysisNewsArticle is a [[NewsArticle]] that, while based on factual reporting, incorporates the expertise of the author/producer, offering interpretations and conclusions. */
	'AnalysisNewsArticle': 'https://schema.org/AnalysisNewsArticle',
	/** Any part of the human body, typically a component of an anatomical system. Organs, tissues, and cells are all anatomical structures. */
	'AnatomicalStructure': 'https://schema.org/AnatomicalStructure',
	/** An anatomical system is a group of anatomical structures that work together to perform a certain task. Anatomical systems, such as organ systems, are one organizing principle of anatomy, and can include circulatory, digestive, endocrine, integumentary, immune, lymphatic, muscular, nervous, reproductive, respiratory, skeletal, urinary, vestibular, and other systems. */
	'AnatomicalSystem': 'https://schema.org/AnatomicalSystem',
	/** Represents the broad notion of Android-based operating systems. */
	'AndroidPlatform': 'https://schema.org/AndroidPlatform',
	/** A specific branch of medical science that pertains to study of anesthetics and their application. */
	'Anesthesia': 'https://schema.org/Anesthesia',
	/** Animal shelter. */
	'AnimalShelter': 'https://schema.org/AnimalShelter',
	/** An answer offered to a question; perhaps correct, perhaps opinionated or wrong. */
	'Answer': 'https://schema.org/Answer',
	/** An apartment (in American English) or flat (in British English) is a self-contained housing unit (a type of residential real estate) that occupies only part of a building (source: Wikipedia, the free encyclopedia, see <a href="http://en.wikipedia.org/wiki/Apartment">http://en.wikipedia.org/wiki/Apartment</a>). */
	'Apartment': 'https://schema.org/Apartment',
	/** Residence type: Apartment complex. */
	'ApartmentComplex': 'https://schema.org/ApartmentComplex',
	/** Appearance assessment with clinical examination. */
	'Appearance': 'https://schema.org/Appearance',
	/** The act of inserting at the end if an ordered collection. */
	'AppendAction': 'https://schema.org/AppendAction',
	/** The act of registering to an organization/service without the guarantee to receive it.\n\nRelated actions:\n\n* [[RegisterAction]]: Unlike RegisterAction, ApplyAction has no guarantees that the application will be accepted. */
	'ApplyAction': 'https://schema.org/ApplyAction',
	/** An indication for a medical therapy that has been formally specified or approved by a regulatory body that regulates use of the therapy; for example, the US FDA approves indications for most drugs in the US. */
	'ApprovedIndication': 'https://schema.org/ApprovedIndication',
	/** Aquarium. */
	'Aquarium': 'https://schema.org/Aquarium',
	/** An intangible type to be applied to any archive content, carrying with it a set of properties required to describe archival items and collections. */
	'ArchiveComponent': 'https://schema.org/ArchiveComponent',
	/** An organization with archival holdings. An organization which keeps and preserves archival material and typically makes it accessible to the public. */
	'ArchiveOrganization': 'https://schema.org/ArchiveOrganization',
	/** The act of arriving at a place. An agent arrives at a destination from a fromLocation, optionally with participants. */
	'ArriveAction': 'https://schema.org/ArriveAction',
	/** An art gallery. */
	'ArtGallery': 'https://schema.org/ArtGallery',
	/** A type of blood vessel that specifically carries blood away from the heart. */
	'Artery': 'https://schema.org/Artery',
	/** An article, such as a news article or piece of investigative report. Newspapers and magazines have articles of many different types and this is intended to cover them all.\n\nSee also [blog post](http://blog.schema.org/2014/09/schemaorg-support-for-bibliographic_2.html). */
	'Article': 'https://schema.org/Article',
	/** The act of posing a question / favor to someone.\n\nRelated actions:\n\n* [[ReplyAction]]: Appears generally as a response to AskAction. */
	'AskAction': 'https://schema.org/AskAction',
	/** A [[NewsArticle]] expressing an open call by a [[NewsMediaOrganization]] asking the public for input, insights, clarifications, anecdotes, documentation, etc., on an issue, for reporting purposes. */
	'AskPublicNewsArticle': 'https://schema.org/AskPublicNewsArticle',
	/** The act of forming one's opinion, reaction or sentiment. */
	'AssessAction': 'https://schema.org/AssessAction',
	/** The act of allocating an action/event/task to some destination (someone or something). */
	'AssignAction': 'https://schema.org/AssignAction',
	/** A collection or bound volume of maps, charts, plates or tables, physical or in media form illustrating any subject. */
	'Atlas': 'https://schema.org/Atlas',
	/** Professional service: Attorney. \n\nThis type is deprecated - [[LegalService]] is more inclusive and less ambiguous. */
	'Attorney': 'https://schema.org/Attorney',
	/** Intended audience for an item, i.e. the group for whom the item was created. */
	'Audience': 'https://schema.org/Audience',
	/** An audio file. */
	'AudioObject': 'https://schema.org/AudioObject',
	/** A specific and exact (byte-for-byte) version of an [[AudioObject]]. Two byte-for-byte identical files, for the purposes of this type, considered identical. If they have different embedded metadata the files will differ. Different external facts about the files, e.g. creator or dateCreated that aren't represented in their actual content, do not affect this notion of identity. */
	'AudioObjectSnapshot': 'https://schema.org/AudioObjectSnapshot',
	/** An audiobook. */
	'Audiobook': 'https://schema.org/Audiobook',
	/** Book format: Audiobook. This is an enumerated value for use with the bookFormat property. There is also a type 'Audiobook' in the bib extension which includes Audiobook specific properties. */
	'AudiobookFormat': 'https://schema.org/AudiobookFormat',
	/** Indicates that the publisher gives some special status to the publication of the document. ("The Queens Printer" version of a UK Act of Parliament, or the PDF version of a Directive published by the EU Office of Publications.) Something "Authoritative" is considered to be also [[OfficialLegalValue]]. */
	'AuthoritativeLegalValue': 'https://schema.org/AuthoritativeLegalValue',
	/** The act of granting permission to an object. */
	'AuthorizeAction': 'https://schema.org/AuthorizeAction',
	/** Auto body shop. */
	'AutoBodyShop': 'https://schema.org/AutoBodyShop',
	/** An car dealership. */
	'AutoDealer': 'https://schema.org/AutoDealer',
	/** An auto parts store. */
	'AutoPartsStore': 'https://schema.org/AutoPartsStore',
	/** A car rental business. */
	'AutoRental': 'https://schema.org/AutoRental',
	/** Car repair business. */
	'AutoRepair': 'https://schema.org/AutoRepair',
	/** A car wash business. */
	'AutoWash': 'https://schema.org/AutoWash',
	/** ATM/cash machine. */
	'AutomatedTeller': 'https://schema.org/AutomatedTeller',
	/** Car repair, sales, or parts. */
	'AutomotiveBusiness': 'https://schema.org/AutomotiveBusiness',
	/** A system of medicine that originated in India over thousands of years and that focuses on integrating and balancing the body, mind, and spirit. */
	'Ayurvedic': 'https://schema.org/Ayurvedic',
	/** Indicates that the item is available on back order. */
	'BackOrder': 'https://schema.org/BackOrder',
	/** A [[NewsArticle]] providing historical context, definition and detail on a specific topic (aka "explainer" or "backgrounder"). For example, an in-depth article or frequently-asked-questions ([FAQ](https://en.wikipedia.org/wiki/FAQ)) document on topics such as Climate Change or the European Union. Other kinds of background material from a non-news setting are often described using [[Book]] or [[Article]], in particular [[ScholarlyArticle]]. See also [[NewsArticle]] for related vocabulary from a learning/education perspective. */
	'BackgroundNewsArticle': 'https://schema.org/BackgroundNewsArticle',
	/** Pathogenic bacteria that cause bacterial infection. */
	'Bacteria': 'https://schema.org/Bacteria',
	/** A bakery. */
	'Bakery': 'https://schema.org/Bakery',
	/** Physical activity that is engaged to help maintain posture and balance. */
	'Balance': 'https://schema.org/Balance',
	/** A product or service offered by a bank whereby one may deposit, withdraw or transfer money and in some cases be paid interest. */
	'BankAccount': 'https://schema.org/BankAccount',
	/** Bank or credit union. */
	'BankOrCreditUnion': 'https://schema.org/BankOrCreditUnion',
	/** A bar or pub. */
	'BarOrPub': 'https://schema.org/BarOrPub',
	/** An image of a visual machine-readable code such as a barcode or QR code. */
	'Barcode': 'https://schema.org/Barcode',
	/** BasicIncome: this is a benefit for basic income. */
	'BasicIncome': 'https://schema.org/BasicIncome',
	/** Beach. */
	'Beach': 'https://schema.org/Beach',
	/** Beauty salon. */
	'BeautySalon': 'https://schema.org/BeautySalon',
	/** Bed and breakfast.
<br /><br />
See also the <a href="/docs/hotels.html">dedicated document on the use of schema.org for marking up hotels and other forms of accommodations</a>.
 */
	'BedAndBreakfast': 'https://schema.org/BedAndBreakfast',
	/** An entity holding detailed information about the available bed types, e.g. the quantity of twin beds for a hotel room. For the single case of just one bed of a certain type, you can use bed directly with a text. See also [[BedType]] (under development). */
	'BedDetails': 'https://schema.org/BedDetails',
	/** A type of bed. This is used for indicating the bed or beds available in an accommodation. */
	'BedType': 'https://schema.org/BedType',
	/** The act of forming a personal connection with someone (object) mutually/bidirectionally/symmetrically.\n\nRelated actions:\n\n* [[FollowAction]]: Unlike FollowAction, BefriendAction implies that the connection is reciprocal. */
	'BefriendAction': 'https://schema.org/BefriendAction',
	/** Content about the benefits and advantages of usage or utilization of topic. */
	'BenefitsHealthAspect': 'https://schema.org/BenefitsHealthAspect',
	/** A bike store. */
	'BikeStore': 'https://schema.org/BikeStore',
	/** Any biological, chemical, or biochemical thing. For example: a protein; a gene; a chemical; a synthetic chemical. */
	'BioChemEntity': 'https://schema.org/BioChemEntity',
	/** A [blog](https://en.wikipedia.org/wiki/Blog), sometimes known as a "weblog". Note that the individual posts ([[BlogPosting]]s) in a [[Blog]] are often colloquially referred to by the same term. */
	'Blog': 'https://schema.org/Blog',
	/** A blog post. */
	'BlogPosting': 'https://schema.org/BlogPosting',
	/** A medical test performed on a sample of a patient's blood. */
	'BloodTest': 'https://schema.org/BloodTest',
	/** A type of boarding policy used by an airline. */
	'BoardingPolicyType': 'https://schema.org/BoardingPolicyType',
	/** A reservation for boat travel.

Note: This type is for information about actual reservations, e.g. in confirmation emails or HTML pages with individual confirmations of reservations. For offers of tickets, use [[Offer]]. */
	'BoatReservation': 'https://schema.org/BoatReservation',
	/** A terminal for boats, ships, and other water vessels. */
	'BoatTerminal': 'https://schema.org/BoatTerminal',
	/** A trip on a commercial ferry line. */
	'BoatTrip': 'https://schema.org/BoatTrip',
	/** Arm length (measured between arms/shoulder line intersection and the prominent wrist bone). Used, for example, to fit shirts. */
	'BodyMeasurementArm': 'https://schema.org/BodyMeasurementArm',
	/** Maximum girth of bust. Used, for example, to fit women's suits. */
	'BodyMeasurementBust': 'https://schema.org/BodyMeasurementBust',
	/** Maximum girth of chest. Used, for example, to fit men's suits. */
	'BodyMeasurementChest': 'https://schema.org/BodyMeasurementChest',
	/** Foot length (measured between end of the most prominent toe and the most prominent part of the heel). Used, for example, to measure socks. */
	'BodyMeasurementFoot': 'https://schema.org/BodyMeasurementFoot',
	/** Maximum hand girth (measured over the knuckles of the open right hand excluding thumb, fingers together). Used, for example, to fit gloves. */
	'BodyMeasurementHand': 'https://schema.org/BodyMeasurementHand',
	/** Maximum girth of head above the ears. Used, for example, to fit hats. */
	'BodyMeasurementHead': 'https://schema.org/BodyMeasurementHead',
	/** Body height (measured between crown of head and soles of feet). Used, for example, to fit jackets. */
	'BodyMeasurementHeight': 'https://schema.org/BodyMeasurementHeight',
	/** Girth of hips (measured around the buttocks). Used, for example, to fit skirts. */
	'BodyMeasurementHips': 'https://schema.org/BodyMeasurementHips',
	/** Inside leg (measured between crotch and soles of feet). Used, for example, to fit pants. */
	'BodyMeasurementInsideLeg': 'https://schema.org/BodyMeasurementInsideLeg',
	/** Girth of neck. Used, for example, to fit shirts. */
	'BodyMeasurementNeck': 'https://schema.org/BodyMeasurementNeck',
	/** Enumerates types (or dimensions) of a person's body measurements, for example for fitting of clothes. */
	'BodyMeasurementTypeEnumeration': 'https://schema.org/BodyMeasurementTypeEnumeration',
	/** Girth of body just below the bust. Used, for example, to fit women's swimwear. */
	'BodyMeasurementUnderbust': 'https://schema.org/BodyMeasurementUnderbust',
	/** Girth of natural waistline (between hip bones and lower ribs). Used, for example, to fit pants. */
	'BodyMeasurementWaist': 'https://schema.org/BodyMeasurementWaist',
	/** Body weight. Used, for example, to measure pantyhose. */
	'BodyMeasurementWeight': 'https://schema.org/BodyMeasurementWeight',
	/** A body of water, such as a sea, ocean, or lake. */
	'BodyOfWater': 'https://schema.org/BodyOfWater',
	/** Rigid connective tissue that comprises up the skeletal structure of the human body. */
	'Bone': 'https://schema.org/Bone',
	/** A book. */
	'Book': 'https://schema.org/Book',
	/** The publication format of the book. */
	'BookFormatType': 'https://schema.org/BookFormatType',
	/** A series of books. Included books can be indicated with the hasPart property. */
	'BookSeries': 'https://schema.org/BookSeries',
	/** A bookstore. */
	'BookStore': 'https://schema.org/BookStore',
	/** An agent bookmarks/flags/labels/tags/marks an object. */
	'BookmarkAction': 'https://schema.org/BookmarkAction',
	/** Boolean: True or False. */
	'Boolean': 'https://schema.org/Boolean',
	/** The act of obtaining an object under an agreement to return it at a later date. Reciprocal of LendAction.\n\nRelated actions:\n\n* [[LendAction]]: Reciprocal of BorrowAction. */
	'BorrowAction': 'https://schema.org/BorrowAction',
	/** A bowling alley. */
	'BowlingAlley': 'https://schema.org/BowlingAlley',
	/** Any anatomical structure which pertains to the soft nervous tissue functioning as the coordinating center of sensation and intellectual and nervous activity. */
	'BrainStructure': 'https://schema.org/BrainStructure',
	/** A brand is a name used by an organization or business person for labeling a product, product group, or similar. */
	'Brand': 'https://schema.org/Brand',
	/** A BreadcrumbList is an ItemList consisting of a chain of linked Web pages, typically described using at least their URL and their name, and typically ending with the current page.\n\nThe [[position]] property is used to reconstruct the order of the items in a BreadcrumbList. The convention is that a breadcrumb list has an [[itemListOrder]] of [[ItemListOrderAscending]] (lower values listed first), and that the first items in this list correspond to the "top" or beginning of the breadcrumb trail, e.g. with a site or section homepage. The specific values of 'position' are not assigned meaning for a BreadcrumbList, but they should be integers, e.g. beginning with '1' for the first item in the list.
       */
	'BreadcrumbList': 'https://schema.org/BreadcrumbList',
	/** Brewery. */
	'Brewery': 'https://schema.org/Brewery',
	/** A bridge. */
	'Bridge': 'https://schema.org/Bridge',
	/** A unique instance of a BroadcastService on a CableOrSatelliteService lineup. */
	'BroadcastChannel': 'https://schema.org/BroadcastChannel',
	/** An over the air or online broadcast event. */
	'BroadcastEvent': 'https://schema.org/BroadcastEvent',
	/** The frequency in MHz and the modulation used for a particular BroadcastService. */
	'BroadcastFrequencySpecification': 'https://schema.org/BroadcastFrequencySpecification',
	/** BroadcastRelease. */
	'BroadcastRelease': 'https://schema.org/BroadcastRelease',
	/** A delivery service through which content is provided via broadcast over the air or online. */
	'BroadcastService': 'https://schema.org/BroadcastService',
	/** An account that allows an investor to deposit funds and place investment orders with a licensed broker or brokerage firm. */
	'BrokerageAccount': 'https://schema.org/BrokerageAccount',
	/** A Buddhist temple. */
	'BuddhistTemple': 'https://schema.org/BuddhistTemple',
	/** A bus (also omnibus or autobus) is a road vehicle designed to carry passengers. Coaches are luxury busses, usually in service for long distance travel. */
	'BusOrCoach': 'https://schema.org/BusOrCoach',
	/** A reservation for bus travel. \n\nNote: This type is for information about actual reservations, e.g. in confirmation emails or HTML pages with individual confirmations of reservations. For offers of tickets, use [[Offer]]. */
	'BusReservation': 'https://schema.org/BusReservation',
	/** A bus station. */
	'BusStation': 'https://schema.org/BusStation',
	/** A bus stop. */
	'BusStop': 'https://schema.org/BusStop',
	/** A trip on a commercial bus line. */
	'BusTrip': 'https://schema.org/BusTrip',
	/** A set of characteristics belonging to businesses, e.g. who compose an item's target audience. */
	'BusinessAudience': 'https://schema.org/BusinessAudience',
	/** A business entity type is a conceptual entity representing the legal form, the size, the main line of business, the position in the value chain, or any combination thereof, of an organization or business person.\n\nCommonly used values:\n\n* http://purl.org/goodrelations/v1#Business\n* http://purl.org/goodrelations/v1#Enduser\n* http://purl.org/goodrelations/v1#PublicInstitution\n* http://purl.org/goodrelations/v1#Reseller
	   */
	'BusinessEntityType': 'https://schema.org/BusinessEntityType',
	/** Event type: Business event. */
	'BusinessEvent': 'https://schema.org/BusinessEvent',
	/** The business function specifies the type of activity or access (i.e., the bundle of rights) offered by the organization or business person through the offer. Typical are sell, rental or lease, maintenance or repair, manufacture / produce, recycle / dispose, engineering / construction, or installation. Proprietary specifications of access rights are also instances of this class.\n\nCommonly used values:\n\n* http://purl.org/goodrelations/v1#ConstructionInstallation\n* http://purl.org/goodrelations/v1#Dispose\n* http://purl.org/goodrelations/v1#LeaseOut\n* http://purl.org/goodrelations/v1#Maintain\n* http://purl.org/goodrelations/v1#ProvideService\n* http://purl.org/goodrelations/v1#Repair\n* http://purl.org/goodrelations/v1#Sell\n* http://purl.org/goodrelations/v1#Buy
         */
	'BusinessFunction': 'https://schema.org/BusinessFunction',
	/** BusinessSupport: this is a benefit for supporting businesses. */
	'BusinessSupport': 'https://schema.org/BusinessSupport',
	/** The act of giving money to a seller in exchange for goods or services rendered. An agent buys an object, product, or service from a seller for a price. Reciprocal of SellAction. */
	'BuyAction': 'https://schema.org/BuyAction',
	/** A CDCPMDRecord is a data structure representing a record in a CDC tabular data format
      used for hospital data reporting. See [documentation](/docs/cdc-covid.html) for details, and the linked CDC materials for authoritative
      definitions used as the source here.
       */
	'CDCPMDRecord': 'https://schema.org/CDCPMDRecord',
	/** CDFormat. */
	'CDFormat': 'https://schema.org/CDFormat',
	/** X-ray computed tomography imaging. */
	'CT': 'https://schema.org/CT',
	/** A service which provides access to media programming like TV or radio. Access may be via cable or satellite. */
	'CableOrSatelliteService': 'https://schema.org/CableOrSatelliteService',
	/** A cafe or coffee shop. */
	'CafeOrCoffeeShop': 'https://schema.org/CafeOrCoffeeShop',
	/** A camping site, campsite, or [[Campground]] is a place used for overnight stay in the outdoors, typically containing individual [[CampingPitch]] locations. \n\n
In British English a campsite is an area, usually divided into a number of pitches, where people can camp overnight using tents or camper vans or caravans; this British English use of the word is synonymous with the American English expression campground. In American English the term campsite generally means an area where an individual, family, group, or military unit can pitch a tent or park a camper; a campground may contain many campsites (source: Wikipedia, see [https://en.wikipedia.org/wiki/Campsite](https://en.wikipedia.org/wiki/Campsite)).\n\n

See also the dedicated [document on the use of schema.org for marking up hotels and other forms of accommodations](/docs/hotels.html).
 */
	'Campground': 'https://schema.org/Campground',
	/** A [[CampingPitch]] is an individual place for overnight stay in the outdoors, typically being part of a larger camping site, or [[Campground]].\n\n
In British English a campsite, or campground, is an area, usually divided into a number of pitches, where people can camp overnight using tents or camper vans or caravans; this British English use of the word is synonymous with the American English expression campground. In American English the term campsite generally means an area where an individual, family, group, or military unit can pitch a tent or park a camper; a campground may contain many campsites.
(Source: Wikipedia, see [https://en.wikipedia.org/wiki/Campsite](https://en.wikipedia.org/wiki/Campsite).)\n\n
See also the dedicated [document on the use of schema.org for marking up hotels and other forms of accommodations](/docs/hotels.html).
 */
	'CampingPitch': 'https://schema.org/CampingPitch',
	/** A canal, like the Panama Canal. */
	'Canal': 'https://schema.org/Canal',
	/** The act of asserting that a future event/action is no longer going to happen.\n\nRelated actions:\n\n* [[ConfirmAction]]: The antonym of CancelAction. */
	'CancelAction': 'https://schema.org/CancelAction',
	/** A car is a wheeled, self-powered motor vehicle used for transportation. */
	'Car': 'https://schema.org/Car',
	/** A value indicating a special usage of a car, e.g. commercial rental, driving school, or as a taxi. */
	'CarUsageType': 'https://schema.org/CarUsageType',
	/** A specific branch of medical science that pertains to diagnosis and treatment of disorders of heart and vasculature. */
	'Cardiovascular': 'https://schema.org/Cardiovascular',
	/** Cardiovascular system assessment with clinical examination. */
	'CardiovascularExam': 'https://schema.org/CardiovascularExam',
	/** A case series (also known as a clinical series) is a medical research study that tracks patients with a known exposure given similar treatment or examines their medical records for exposure and outcome. A case series can be retrospective or prospective and usually involves a smaller number of patients than the more powerful case-control studies or randomized controlled trials. Case series may be consecutive or non-consecutive, depending on whether all cases presenting to the reporting authors over a period of time were included, or only a selection. */
	'CaseSeries': 'https://schema.org/CaseSeries',
	/** A casino. */
	'Casino': 'https://schema.org/Casino',
	/** CassetteFormat. */
	'CassetteFormat': 'https://schema.org/CassetteFormat',
	/** A Category Code. */
	'CategoryCode': 'https://schema.org/CategoryCode',
	/** A set of Category Code values. */
	'CategoryCodeSet': 'https://schema.org/CategoryCodeSet',
	/** A Catholic church. */
	'CatholicChurch': 'https://schema.org/CatholicChurch',
	/** Information about the causes and main actions that gave rise to the topic. */
	'CausesHealthAspect': 'https://schema.org/CausesHealthAspect',
	/** A graveyard. */
	'Cemetery': 'https://schema.org/Cemetery',
	/** One of the sections into which a book is divided. A chapter usually has a section number or a name. */
	'Chapter': 'https://schema.org/Chapter',
	/** CharitableIncorporatedOrganization: Non-profit type referring to a Charitable Incorporated Organization (UK). */
	'CharitableIncorporatedOrganization': 'https://schema.org/CharitableIncorporatedOrganization',
	/** An agent inspects, determines, investigates, inquires, or examines an object's accuracy, quality, condition, or state. */
	'CheckAction': 'https://schema.org/CheckAction',
	/** The act of an agent communicating (service provider, social media, etc) their arrival by registering/confirming for a previously reserved service (e.g. flight check-in) or at a place (e.g. hotel), possibly resulting in a result (boarding pass, etc).\n\nRelated actions:\n\n* [[CheckOutAction]]: The antonym of CheckInAction.\n* [[ArriveAction]]: Unlike ArriveAction, CheckInAction implies that the agent is informing/confirming the start of a previously reserved service.\n* [[ConfirmAction]]: Unlike ConfirmAction, CheckInAction implies that the agent is informing/confirming the *start* of a previously reserved service rather than its validity/existence. */
	'CheckInAction': 'https://schema.org/CheckInAction',
	/** The act of an agent communicating (service provider, social media, etc) their departure of a previously reserved service (e.g. flight check-in) or place (e.g. hotel).\n\nRelated actions:\n\n* [[CheckInAction]]: The antonym of CheckOutAction.\n* [[DepartAction]]: Unlike DepartAction, CheckOutAction implies that the agent is informing/confirming the end of a previously reserved service.\n* [[CancelAction]]: Unlike CancelAction, CheckOutAction implies that the agent is informing/confirming the end of a previously reserved service. */
	'CheckOutAction': 'https://schema.org/CheckOutAction',
	/** Web page type: Checkout page. */
	'CheckoutPage': 'https://schema.org/CheckoutPage',
	/** A chemical substance is 'a portion of matter of constant composition, composed of molecular entities of the same type or of different types' (source: [ChEBI:59999](https://www.ebi.ac.uk/chebi/searchId.do?chebiId=59999)). */
	'ChemicalSubstance': 'https://schema.org/ChemicalSubstance',
	/** A Childcare center. */
	'ChildCare': 'https://schema.org/ChildCare',
	/** Event type: Children's event. */
	'ChildrensEvent': 'https://schema.org/ChildrensEvent',
	/** A system of medicine focused on the relationship between the body's structure, mainly the spine, and its functioning. */
	'Chiropractic': 'https://schema.org/Chiropractic',
	/** The act of expressing a preference from a set of options or a large or unbounded set of choices/options. */
	'ChooseAction': 'https://schema.org/ChooseAction',
	/** A church. */
	'Church': 'https://schema.org/Church',
	/** A city or town. */
	'City': 'https://schema.org/City',
	/** A city hall. */
	'CityHall': 'https://schema.org/CityHall',
	/** A public structure, such as a town hall or concert hall. */
	'CivicStructure': 'https://schema.org/CivicStructure',
	/** A [[Claim]] in Schema.org represents a specific, factually-oriented claim that could be the [[itemReviewed]] in a [[ClaimReview]]. The content of a claim can be summarized with the [[text]] property. Variations on well known claims can have their common identity indicated via [[sameAs]] links, and summarized with a [[name]]. Ideally, a [[Claim]] description includes enough contextual information to minimize the risk of ambiguity or inclarity. In practice, many claims are better understood in the context in which they appear or the interpretations provided by claim reviews.

  Beyond [[ClaimReview]], the Claim type can be associated with related creative works - for example a [[ScholarlyArticle]] or [[Question]] might be [[about]] some [[Claim]].

  At this time, Schema.org does not define any types of relationship between claims. This is a natural area for future exploration.
   */
	'Claim': 'https://schema.org/Claim',
	/** A fact-checking review of claims made (or reported) in some creative work (referenced via itemReviewed). */
	'ClaimReview': 'https://schema.org/ClaimReview',
	/** A class, also often called a 'Type'; equivalent to rdfs:Class. */
	'Class': 'https://schema.org/Class',
	/** Represents the cleaning fee part of the total price for an offered product, for example a vacation rental. */
	'CleaningFee': 'https://schema.org/CleaningFee',
	/** Medical clinicians, including practicing physicians and other medical professionals involved in clinical practice. */
	'Clinician': 'https://schema.org/Clinician',
	/** A short TV or radio program or a segment/part of a program. */
	'Clip': 'https://schema.org/Clip',
	/** A clothing store. */
	'ClothingStore': 'https://schema.org/ClothingStore',
	/** Play mode: CoOp. Co-operative games, where you play on the same team with friends. */
	'CoOp': 'https://schema.org/CoOp',
	/** Computer programming source code. Example: Full (compile ready) solutions, code snippet samples, scripts, templates. */
	'Code': 'https://schema.org/Code',
	/** Also known as a panel study. A cohort study is a form of longitudinal study used in medicine and social science. It is one type of study design and should be compared with a cross-sectional study.  A cohort is a group of people who share a common characteristic or experience within a defined period (e.g., are born, leave school, lose their job, are exposed to a drug or a vaccine, etc.). The comparison group may be the general population from which the cohort is drawn, or it may be another cohort of persons thought to have had little or no exposure to the substance under investigation, but otherwise similar. Alternatively, subgroups within the cohort may be compared with each other. */
	'CohortStudy': 'https://schema.org/CohortStudy',
	/** A collection of items, e.g. creative works or products. */
	'Collection': 'https://schema.org/Collection',
	/** Web page type: Collection page. */
	'CollectionPage': 'https://schema.org/CollectionPage',
	/** A college, university, or other third-level educational institution. */
	'CollegeOrUniversity': 'https://schema.org/CollegeOrUniversity',
	/** A comedy club. */
	'ComedyClub': 'https://schema.org/ComedyClub',
	/** Event type: Comedy event. */
	'ComedyEvent': 'https://schema.org/ComedyEvent',
	/** The artwork on the cover of a comic. */
	'ComicCoverArt': 'https://schema.org/ComicCoverArt',
	/** Individual comic issues are serially published as
    	part of a larger series. For the sake of consistency, even one-shot issues
    	belong to a series comprised of a single issue. All comic issues can be
    	uniquely identified by: the combination of the name and volume number of the
    	series to which the issue belongs; the issue number; and the variant
    	description of the issue (if any). */
	'ComicIssue': 'https://schema.org/ComicIssue',
	/** A sequential publication of comic stories under a
    	unifying title, for example "The Amazing Spider-Man" or "Groo the
    	Wanderer". */
	'ComicSeries': 'https://schema.org/ComicSeries',
	/** The term "story" is any indivisible, re-printable
    	unit of a comic, including the interior stories, covers, and backmatter. Most
    	comics have at least two stories: a cover (ComicCoverArt) and an interior story. */
	'ComicStory': 'https://schema.org/ComicStory',
	/** A comment on an item - for example, a comment on a blog post. The comment's content is expressed via the [[text]] property, and its topic via [[about]], properties shared with all CreativeWorks. */
	'Comment': 'https://schema.org/Comment',
	/** The act of generating a comment about a subject. */
	'CommentAction': 'https://schema.org/CommentAction',
	/** Permission to add comments to the document. */
	'CommentPermission': 'https://schema.org/CommentPermission',
	/** The act of conveying information to another person via a communication medium (instrument) such as speech, email, or telephone conversation. */
	'CommunicateAction': 'https://schema.org/CommunicateAction',
	/** A field of public health focusing on improving health characteristics of a defined population in relation with their geographical or environment areas. */
	'CommunityHealth': 'https://schema.org/CommunityHealth',
	/** CompilationAlbum. */
	'CompilationAlbum': 'https://schema.org/CompilationAlbum',
	/** A [[CompleteDataFeed]] is a [[DataFeed]] whose standard representation includes content for every item currently in the feed.

This is the equivalent of Atom's element as defined in Feed Paging and Archiving [RFC 5005](https://tools.ietf.org/html/rfc5005), for example (and as defined for Atom), when using data from a feed that represents a collection of items that varies over time (e.g. "Top Twenty Records") there is no need to have newer entries mixed in alongside older, obsolete entries. By marking this feed as a CompleteDataFeed, old entries can be safely discarded when the feed is refreshed, since we can assume the feed has provided descriptions for all current items. */
	'CompleteDataFeed': 'https://schema.org/CompleteDataFeed',
	/** Completed. */
	'Completed': 'https://schema.org/Completed',
	/** An action that has already taken place. */
	'CompletedActionStatus': 'https://schema.org/CompletedActionStatus',
	/** A compound price specification is one that bundles multiple prices that all apply in combination for different dimensions of consumption. Use the name property of the attached unit price specification for indicating the dimension of a price component (e.g. "electricity" or "final cleaning"). */
	'CompoundPriceSpecification': 'https://schema.org/CompoundPriceSpecification',
	/** This type covers computer programming languages such as Scheme and Lisp, as well as other language-like computer representations. Natural languages are best represented with the [[Language]] type. */
	'ComputerLanguage': 'https://schema.org/ComputerLanguage',
	/** A computer store. */
	'ComputerStore': 'https://schema.org/ComputerStore',
	/** The act of notifying someone that a future event/action is going to happen as expected.\n\nRelated actions:\n\n* [[CancelAction]]: The antonym of ConfirmAction. */
	'ConfirmAction': 'https://schema.org/ConfirmAction',
	/** A Consortium is a membership [[Organization]] whose members are typically Organizations. */
	'Consortium': 'https://schema.org/Consortium',
	/** The ConstraintNode type is provided to support usecases in which a node in a structured data graph is described with properties which appear to describe a single entity, but are being used in a situation where they serve a more abstract purpose. A [[ConstraintNode]] can be described using [[constraintProperty]] and [[numConstraints]]. These constraint properties can serve a 
    variety of purposes, and their values may sometimes be understood to indicate sets of possible values rather than single, exact and specific values. */
	'ConstraintNode': 'https://schema.org/ConstraintNode',
	/** The act of ingesting information/resources/food. */
	'ConsumeAction': 'https://schema.org/ConsumeAction',
	/** Web page type: Contact page. */
	'ContactPage': 'https://schema.org/ContactPage',
	/** A contact point&#x2014;for example, a Customer Complaints department. */
	'ContactPoint': 'https://schema.org/ContactPoint',
	/** Enumerated options related to a ContactPoint. */
	'ContactPointOption': 'https://schema.org/ContactPointOption',
	/** Content about contagion mechanisms and contagiousness information over the topic. */
	'ContagiousnessHealthAspect': 'https://schema.org/ContagiousnessHealthAspect',
	/** One of the continents (for example, Europe or Africa). */
	'Continent': 'https://schema.org/Continent',
	/** An agent controls a device or application. */
	'ControlAction': 'https://schema.org/ControlAction',
	/** A convenience store. */
	'ConvenienceStore': 'https://schema.org/ConvenienceStore',
	/** One or more messages between organizations or people on a particular topic. Individual messages can be linked to the conversation with isPartOf or hasPart properties. */
	'Conversation': 'https://schema.org/Conversation',
	/** The act of producing/preparing food. */
	'CookAction': 'https://schema.org/CookAction',
	/** Organization: A business corporation. */
	'Corporation': 'https://schema.org/Corporation',
	/** A [[comment]] that corrects [[CreativeWork]]. */
	'CorrectionComment': 'https://schema.org/CorrectionComment',
	/** A country. */
	'Country': 'https://schema.org/Country',
	/** A description of an educational course which may be offered as distinct instances which take place at different times or take place at different locations, or be offered through different media or modes of study. An educational course is a sequence of one or more educational events and/or creative works which aims to build knowledge, competence or ability of learners. */
	'Course': 'https://schema.org/Course',
	/** An instance of a [[Course]] which is distinct from other instances because it is offered at a different time or location or through different media or modes of study or to a specific section of students. */
	'CourseInstance': 'https://schema.org/CourseInstance',
	/** A courthouse. */
	'Courthouse': 'https://schema.org/Courthouse',
	/** The artwork on the outer surface of a CreativeWork. */
	'CoverArt': 'https://schema.org/CoverArt',
	/** A CovidTestingFacility is a [[MedicalClinic]] where testing for the COVID-19 Coronavirus
      disease is available. If the facility is being made available from an established [[Pharmacy]], [[Hotel]], or other
      non-medical organization, multiple types can be listed. This makes it easier to re-use existing schema.org information
      about that place, e.g. contact info, address, opening hours. Note that in an emergency, such information may not always be reliable.
       */
	'CovidTestingFacility': 'https://schema.org/CovidTestingFacility',
	/** The act of deliberately creating/producing/generating/building a result out of the agent. */
	'CreateAction': 'https://schema.org/CreateAction',
	/** The most generic kind of creative work, including books, movies, photographs, software programs, etc. */
	'CreativeWork': 'https://schema.org/CreativeWork',
	/** A media season, e.g. TV, radio, video game etc. */
	'CreativeWorkSeason': 'https://schema.org/CreativeWorkSeason',
	/** A CreativeWorkSeries in schema.org is a group of related items, typically but not necessarily of the same kind. CreativeWorkSeries are usually organized into some order, often chronological. Unlike [[ItemList]] which is a general purpose data structure for lists of things, the emphasis with CreativeWorkSeries is on published materials (written e.g. books and periodicals, or media such as TV, radio and games).\n\nSpecific subtypes are available for describing [[TVSeries]], [[RadioSeries]], [[MovieSeries]], [[BookSeries]], [[Periodical]] and [[VideoGameSeries]]. In each case, the [[hasPart]] / [[isPartOf]] properties can be used to relate the CreativeWorkSeries to its parts. The general CreativeWorkSeries type serves largely just to organize these more specific and practical subtypes.\n\nIt is common for properties applicable to an item from the series to be usefully applied to the containing group. Schema.org attempts to anticipate some of these cases, but publishers should be free to apply properties of the series parts to the series as a whole wherever they seem appropriate.
	   */
	'CreativeWorkSeries': 'https://schema.org/CreativeWorkSeries',
	/** A card payment method of a particular brand or name.  Used to mark up a particular payment method and/or the financial product/service that supplies the card account.\n\nCommonly used values:\n\n* http://purl.org/goodrelations/v1#AmericanExpress\n* http://purl.org/goodrelations/v1#DinersClub\n* http://purl.org/goodrelations/v1#Discover\n* http://purl.org/goodrelations/v1#JCB\n* http://purl.org/goodrelations/v1#MasterCard\n* http://purl.org/goodrelations/v1#VISA
        */
	'CreditCard': 'https://schema.org/CreditCard',
	/** A crematorium. */
	'Crematorium': 'https://schema.org/Crematorium',
	/** A [[CriticReview]] is a more specialized form of Review written or published by a source that is recognized for its reviewing activities. These can include online columns, travel and food guides, TV and radio shows, blogs and other independent Web sites. [[CriticReview]]s are typically more in-depth and professionally written. For simpler, casually written user/visitor/viewer/customer reviews, it is more appropriate to use the [[UserReview]] type. Review aggregator sites such as Metacritic already separate out the site's user reviews from selected critic reviews that originate from third-party sources. */
	'CriticReview': 'https://schema.org/CriticReview',
	/** Studies carried out on pre-existing data (usually from 'snapshot' surveys), such as that collected by the Census Bureau. Sometimes called Prevalence Studies. */
	'CrossSectional': 'https://schema.org/CrossSectional',
	/** Text representing a CSS selector. */
	'CssSelectorType': 'https://schema.org/CssSelectorType',
	/** A service to convert funds from one currency to another currency. */
	'CurrencyConversionService': 'https://schema.org/CurrencyConversionService',
	/** An alternative, closely-related condition typically considered later in the differential diagnosis process along with the signs that are used to distinguish it. */
	'DDxElement': 'https://schema.org/DDxElement',
	/** DJMixAlbum. */
	'DJMixAlbum': 'https://schema.org/DJMixAlbum',
	/** DVDFormat. */
	'DVDFormat': 'https://schema.org/DVDFormat',
	/** Indicates that the item is damaged. */
	'DamagedCondition': 'https://schema.org/DamagedCondition',
	/** Event type: A social dance. */
	'DanceEvent': 'https://schema.org/DanceEvent',
	/** A dance group&#x2014;for example, the Alvin Ailey Dance Theater or Riverdance. */
	'DanceGroup': 'https://schema.org/DanceGroup',
	/** The item is dangerous and requires careful handling and/or special training of the user. See also the [UN Model Classification](https://unece.org/DAM/trans/danger/publi/unrec/rev17/English/02EREv17_Part2.pdf) defining the 9 classes of dangerous goods such as explosives, gases, flammables, and more. */
	'DangerousGoodConsideration': 'https://schema.org/DangerousGoodConsideration',
	/** A collection of datasets. */
	'DataCatalog': 'https://schema.org/DataCatalog',
	/** All or part of a [[Dataset]] in downloadable form.  */
	'DataDownload': 'https://schema.org/DataDownload',
	/** A single feed providing structured information about one or more entities or topics. */
	'DataFeed': 'https://schema.org/DataFeed',
	/** A single item within a larger data feed. */
	'DataFeedItem': 'https://schema.org/DataFeedItem',
	/** The basic data types such as Integers, Strings, etc. */
	'DataType': 'https://schema.org/DataType',
	/** A body of structured information describing some topic(s) of interest. */
	'Dataset': 'https://schema.org/Dataset',
	/** A date value in [ISO 8601 date format](http://en.wikipedia.org/wiki/ISO_8601). */
	'Date': 'https://schema.org/Date',
	/** A combination of date and time of day in the form [-]CCYY-MM-DDThh:mm:ss[Z|(+|-)hh:mm] (see Chapter 5.4 of ISO 8601). */
	'DateTime': 'https://schema.org/DateTime',
	/** A DatedMoneySpecification represents monetary values with optional start and end dates. For example, this could represent an employee's salary over a specific period of time. __Note:__ This type has been superseded by [[MonetaryAmount]], use of that type is recommended. */
	'DatedMoneySpecification': 'https://schema.org/DatedMoneySpecification',
	/** The day of the week, e.g. used to specify to which day the opening hours of an OpeningHoursSpecification refer.

Originally, URLs from [GoodRelations](http://purl.org/goodrelations/v1) were used (for [[Monday]], [[Tuesday]], [[Wednesday]], [[Thursday]], [[Friday]], [[Saturday]], [[Sunday]] plus a special entry for [[PublicHolidays]]); these have now been integrated directly into schema.org.
       */
	'DayOfWeek': 'https://schema.org/DayOfWeek',
	/** A day spa. */
	'DaySpa': 'https://schema.org/DaySpa',
	/** The act of stopping or deactivating a device or application (e.g. stopping a timer or turning off a flashlight). */
	'DeactivateAction': 'https://schema.org/DeactivateAction',
	/** Content coded 'missing context' in a [[MediaReview]], considered in the context of how it was published or shared.

For a [[VideoObject]] to be 'missing context': Presenting unaltered video in an inaccurate manner that misrepresents the footage. For example, using incorrect dates or locations, altering the transcript or sharing brief clips from a longer video to mislead viewers. (A video rated 'original' can also be missing context.)

For an [[ImageObject]] to be 'missing context': Presenting unaltered images in an inaccurate manner to misrepresent the image and mislead the viewer. For example, a common tactic is using an unaltered image but saying it came from a different time or place. (An image rated 'original' can also be missing context.)

For an [[ImageObject]] with embedded text to be 'missing context': An unaltered image presented in an inaccurate manner to misrepresent the image and mislead the viewer. For example, a common tactic is using an unaltered image but saying it came from a different time or place. (An 'original' image with inaccurate text would generally fall in this category.)

For an [[AudioObject]] to be 'missing context': Unaltered audio presented in an inaccurate manner that misrepresents it. For example, using incorrect dates or locations, or sharing brief clips from a longer recording to mislead viewers. (Audio rated “original” can also be missing context.)
 */
	'DecontextualizedContent': 'https://schema.org/DecontextualizedContent',
	/** A defence establishment, such as an army or navy base. */
	'DefenceEstablishment': 'https://schema.org/DefenceEstablishment',
	/** A DefinedRegion is a geographic area defined by potentially arbitrary (rather than political, administrative or natural geographical) criteria. Properties are provided for defining a region by reference to sets of postal codes.

Examples: a delivery destination when shopping. Region where regional pricing is configured.

Requirement 1:
Country: US
States: "NY", "CA"

Requirement 2:
Country: US
PostalCode Set: { [94000-94585], [97000, 97999], [13000, 13599]}
{ [12345, 12345], [78945, 78945], }
Region = state, canton, prefecture, autonomous community...
 */
	'DefinedRegion': 'https://schema.org/DefinedRegion',
	/** A word, name, acronym, phrase, etc. with a formal definition. Often used in the context of category or subject classification, glossaries or dictionaries, product or creative work types, etc. Use the name property for the term being defined, use termCode if the term has an alpha-numeric code allocated, use description to provide the definition of the term. */
	'DefinedTerm': 'https://schema.org/DefinedTerm',
	/** A set of defined terms, for example a set of categories or a classification scheme, a glossary, dictionary or enumeration. */
	'DefinedTermSet': 'https://schema.org/DefinedTermSet',
	/** Indicates a document for which the text is conclusively what the law says and is legally binding. (E.g. the digitally signed version of an Official Journal.)
  Something "Definitive" is considered to be also [[AuthoritativeLegalValue]]. */
	'DefinitiveLegalValue': 'https://schema.org/DefinitiveLegalValue',
	/** The act of editing a recipient by removing one of its objects. */
	'DeleteAction': 'https://schema.org/DeleteAction',
	/** The price for the delivery of an offer using a particular delivery method. */
	'DeliveryChargeSpecification': 'https://schema.org/DeliveryChargeSpecification',
	/** An event involving the delivery of an item. */
	'DeliveryEvent': 'https://schema.org/DeliveryEvent',
	/** A delivery method is a standardized procedure for transferring the product or service to the destination of fulfillment chosen by the customer. Delivery methods are characterized by the means of transportation used, and by the organization or group that is the contracting party for the sending organization or person.\n\nCommonly used values:\n\n* http://purl.org/goodrelations/v1#DeliveryModeDirectDownload\n* http://purl.org/goodrelations/v1#DeliveryModeFreight\n* http://purl.org/goodrelations/v1#DeliveryModeMail\n* http://purl.org/goodrelations/v1#DeliveryModeOwnFleet\n* http://purl.org/goodrelations/v1#DeliveryModePickUp\n* http://purl.org/goodrelations/v1#DHL\n* http://purl.org/goodrelations/v1#FederalExpress\n* http://purl.org/goodrelations/v1#UPS
         */
	'DeliveryMethod': 'https://schema.org/DeliveryMethod',
	/** A DeliveryTimeSettings represents re-usable pieces of shipping information, relating to timing. It is designed for publication on an URL that may be referenced via the [[shippingSettingsLink]] property of an [[OfferShippingDetails]]. Several occurrences can be published, distinguished (and identified/referenced) by their different values for [[transitTimeLabel]]. */
	'DeliveryTimeSettings': 'https://schema.org/DeliveryTimeSettings',
	/** A demand entity represents the public, not necessarily binding, not necessarily exclusive, announcement by an organization or person to seek a certain type of goods or services. For describing demand using this type, the very same properties used for Offer apply. */
	'Demand': 'https://schema.org/Demand',
	/** DemoAlbum. */
	'DemoAlbum': 'https://schema.org/DemoAlbum',
	/** Indicates demo game availability, i.e. a somehow limited demonstration of the full game. */
	'DemoGameAvailability': 'https://schema.org/DemoGameAvailability',
	/** A dentist. */
	'Dentist': 'https://schema.org/Dentist',
	/** A branch of medicine that is involved in the dental care. */
	'Dentistry': 'https://schema.org/Dentistry',
	/** The act of  departing from a place. An agent departs from a fromLocation for a destination, optionally with participants. */
	'DepartAction': 'https://schema.org/DepartAction',
	/** A department store. */
	'DepartmentStore': 'https://schema.org/DepartmentStore',
	/** A type of Bank Account with a main purpose of depositing funds to gain interest or other benefits. */
	'DepositAccount': 'https://schema.org/DepositAccount',
	/** Something relating to or practicing dermatology. */
	'Dermatologic': 'https://schema.org/Dermatologic',
	/** A specific branch of medical science that pertains to diagnosis and treatment of disorders of skin. */
	'Dermatology': 'https://schema.org/Dermatology',
	/** Represents the broad notion of 'desktop' browsers as a Web Platform. */
	'DesktopWebPlatform': 'https://schema.org/DesktopWebPlatform',
	/** A diet appropriate for people with diabetes. */
	'DiabeticDiet': 'https://schema.org/DiabeticDiet',
	/** A medical device used for diagnostic purposes. */
	'Diagnostic': 'https://schema.org/Diagnostic',
	/** A medical laboratory that offers on-site or off-site diagnostic services. */
	'DiagnosticLab': 'https://schema.org/DiagnosticLab',
	/** A medical procedure intended primarily for diagnostic, as opposed to therapeutic, purposes. */
	'DiagnosticProcedure': 'https://schema.org/DiagnosticProcedure',
	/** A strategy of regulating the intake of food to achieve or maintain a specific health-related goal. */
	'Diet': 'https://schema.org/Diet',
	/** Dietetics and nutrition as a medical specialty. */
	'DietNutrition': 'https://schema.org/DietNutrition',
	/** A product taken by mouth that contains a dietary ingredient intended to supplement the diet. Dietary ingredients may include vitamins, minerals, herbs or other botanicals, amino acids, and substances such as enzymes, organ tissues, glandulars and metabolites. */
	'DietarySupplement': 'https://schema.org/DietarySupplement',
	/** DigitalAudioTapeFormat. */
	'DigitalAudioTapeFormat': 'https://schema.org/DigitalAudioTapeFormat',
	/** An electronic file or document. */
	'DigitalDocument': 'https://schema.org/DigitalDocument',
	/** A permission for a particular person or group to access a particular file. */
	'DigitalDocumentPermission': 'https://schema.org/DigitalDocumentPermission',
	/** A type of permission which can be granted for accessing a digital document. */
	'DigitalDocumentPermissionType': 'https://schema.org/DigitalDocumentPermissionType',
	/** DigitalFormat. */
	'DigitalFormat': 'https://schema.org/DigitalFormat',
	/** Enumerates some common technology platforms, for use with properties such as [[actionPlatform]]. It is not supposed to be comprehensive - when a suitable code is not enumerated here, textual or URL values can be used instead. These codes are at a fairly high level and do not deal with versioning and other nuance. Additional codes can be suggested [in github](https://github.com/schemaorg/schemaorg/issues/3057).  */
	'DigitalPlatformEnumeration': 'https://schema.org/DigitalPlatformEnumeration',
	/** DisabilitySupport: this is a benefit for disability support. */
	'DisabilitySupport': 'https://schema.org/DisabilitySupport',
	/** The act of expressing a difference of opinion with the object. An agent disagrees to/about an object (a proposition, topic or theme) with participants. */
	'DisagreeAction': 'https://schema.org/DisagreeAction',
	/** Indicates that the item has been discontinued. */
	'Discontinued': 'https://schema.org/Discontinued',
	/** The act of discovering/finding an object. */
	'DiscoverAction': 'https://schema.org/DiscoverAction',
	/** A posting to a discussion forum. */
	'DiscussionForumPosting': 'https://schema.org/DiscussionForumPosting',
	/** The act of expressing a negative sentiment about the object. An agent dislikes an object (a proposition, topic or theme) with participants. */
	'DislikeAction': 'https://schema.org/DislikeAction',
	/** Properties that take Distances as values are of the form '&lt;Number&gt; &lt;Length unit of measure&gt;'. E.g., '7 ft'. */
	'Distance': 'https://schema.org/Distance',
	/** Represents the distance fee (e.g., price per km or mile) part of the total price for an offered product, for example a car rental. */
	'DistanceFee': 'https://schema.org/DistanceFee',
	/** A distillery. */
	'Distillery': 'https://schema.org/Distillery',
	/** The act of providing goods, services, or money without compensation, often for philanthropic reasons. */
	'DonateAction': 'https://schema.org/DonateAction',
	/** A specific dosing schedule for a drug or supplement. */
	'DoseSchedule': 'https://schema.org/DoseSchedule',
	/** A trial design in which neither the researcher nor the patient knows the details of the treatment the patient was randomly assigned to. */
	'DoubleBlindedTrial': 'https://schema.org/DoubleBlindedTrial',
	/** The act of downloading an object. */
	'DownloadAction': 'https://schema.org/DownloadAction',
	/** Represents the downpayment (up-front payment) price component of the total price for an offered product that has additional installment payments. */
	'Downpayment': 'https://schema.org/Downpayment',
	/** The act of producing a visual/graphical representation of an object, typically with a pen/pencil and paper as instruments. */
	'DrawAction': 'https://schema.org/DrawAction',
	/** A picture or diagram made with a pencil, pen, or crayon rather than paint. */
	'Drawing': 'https://schema.org/Drawing',
	/** The act of swallowing liquids. */
	'DrinkAction': 'https://schema.org/DrinkAction',
	/** A value indicating which roadwheels will receive torque. */
	'DriveWheelConfigurationValue': 'https://schema.org/DriveWheelConfigurationValue',
	/** Indicates the usage of the vehicle for driving school. */
	'DrivingSchoolVehicleUsage': 'https://schema.org/DrivingSchoolVehicleUsage',
	/** A chemical or biologic substance, used as a medical therapy, that has a physiological effect on an organism. Here the term drug is used interchangeably with the term medicine although clinical knowledge makes a clear difference between them. */
	'Drug': 'https://schema.org/Drug',
	/** A class of medical drugs, e.g., statins. Classes can represent general pharmacological class, common mechanisms of action, common physiological effects, etc. */
	'DrugClass': 'https://schema.org/DrugClass',
	/** The cost per unit of a medical drug. Note that this type is not meant to represent the price in an offer of a drug for sale; see the Offer type for that. This type will typically be used to tag wholesale or average retail cost of a drug, or maximum reimbursable cost. Costs of medical drugs vary widely depending on how and where they are paid for, so while this type captures some of the variables, costs should be used with caution by consumers of this schema's markup. */
	'DrugCost': 'https://schema.org/DrugCost',
	/** Enumerated categories of medical drug costs. */
	'DrugCostCategory': 'https://schema.org/DrugCostCategory',
	/** The legal availability status of a medical drug. */
	'DrugLegalStatus': 'https://schema.org/DrugLegalStatus',
	/** Categories that represent an assessment of the risk of fetal injury due to a drug or pharmaceutical used as directed by the mother during pregnancy. */
	'DrugPregnancyCategory': 'https://schema.org/DrugPregnancyCategory',
	/** Indicates whether this drug is available by prescription or over-the-counter. */
	'DrugPrescriptionStatus': 'https://schema.org/DrugPrescriptionStatus',
	/** A specific strength in which a medical drug is available in a specific country. */
	'DrugStrength': 'https://schema.org/DrugStrength',
	/** A dry-cleaning business. */
	'DryCleaningOrLaundry': 'https://schema.org/DryCleaningOrLaundry',
	/** Quantity: Duration (use [ISO 8601 duration format](http://en.wikipedia.org/wiki/ISO_8601)). */
	'Duration': 'https://schema.org/Duration',
	/** Book format: Ebook. */
	'EBook': 'https://schema.org/EBook',
	/** EPRelease. */
	'EPRelease': 'https://schema.org/EPRelease',
	/** Represents EU Energy Efficiency Class A as defined in EU energy labeling regulations. */
	'EUEnergyEfficiencyCategoryA': 'https://schema.org/EUEnergyEfficiencyCategoryA',
	/** Represents EU Energy Efficiency Class A+ as defined in EU energy labeling regulations. */
	'EUEnergyEfficiencyCategoryA1Plus': 'https://schema.org/EUEnergyEfficiencyCategoryA1Plus',
	/** Represents EU Energy Efficiency Class A++ as defined in EU energy labeling regulations. */
	'EUEnergyEfficiencyCategoryA2Plus': 'https://schema.org/EUEnergyEfficiencyCategoryA2Plus',
	/** Represents EU Energy Efficiency Class A+++ as defined in EU energy labeling regulations. */
	'EUEnergyEfficiencyCategoryA3Plus': 'https://schema.org/EUEnergyEfficiencyCategoryA3Plus',
	/** Represents EU Energy Efficiency Class B as defined in EU energy labeling regulations. */
	'EUEnergyEfficiencyCategoryB': 'https://schema.org/EUEnergyEfficiencyCategoryB',
	/** Represents EU Energy Efficiency Class C as defined in EU energy labeling regulations. */
	'EUEnergyEfficiencyCategoryC': 'https://schema.org/EUEnergyEfficiencyCategoryC',
	/** Represents EU Energy Efficiency Class D as defined in EU energy labeling regulations. */
	'EUEnergyEfficiencyCategoryD': 'https://schema.org/EUEnergyEfficiencyCategoryD',
	/** Represents EU Energy Efficiency Class E as defined in EU energy labeling regulations. */
	'EUEnergyEfficiencyCategoryE': 'https://schema.org/EUEnergyEfficiencyCategoryE',
	/** Represents EU Energy Efficiency Class F as defined in EU energy labeling regulations. */
	'EUEnergyEfficiencyCategoryF': 'https://schema.org/EUEnergyEfficiencyCategoryF',
	/** Represents EU Energy Efficiency Class G as defined in EU energy labeling regulations. */
	'EUEnergyEfficiencyCategoryG': 'https://schema.org/EUEnergyEfficiencyCategoryG',
	/** Enumerates the EU energy efficiency classes A-G as well as A+, A++, and A+++ as defined in EU directive 2017/1369. */
	'EUEnergyEfficiencyEnumeration': 'https://schema.org/EUEnergyEfficiencyEnumeration',
	/** Ear function assessment with clinical examination. */
	'Ear': 'https://schema.org/Ear',
	/** The act of swallowing solid objects. */
	'EatAction': 'https://schema.org/EatAction',
	/** Content coded 'edited or cropped content' in a [[MediaReview]], considered in the context of how it was published or shared.

For a [[VideoObject]] to be 'edited or cropped content': The video has been edited or rearranged. This category applies to time edits, including editing multiple videos together to alter the story being told or editing out large portions from a video.

For an [[ImageObject]] to be 'edited or cropped content': Presenting a part of an image from a larger whole to mislead the viewer.

For an [[ImageObject]] with embedded text to be 'edited or cropped content': Presenting a part of an image from a larger whole to mislead the viewer.

For an [[AudioObject]] to be 'edited or cropped content': The audio has been edited or rearranged. This category applies to time edits, including editing multiple audio clips together to alter the story being told or editing out large portions from the recording.
 */
	'EditedOrCroppedContent': 'https://schema.org/EditedOrCroppedContent',
	/** Event type: Education event. */
	'EducationEvent': 'https://schema.org/EducationEvent',
	/** An EducationalAudience. */
	'EducationalAudience': 'https://schema.org/EducationalAudience',
	/** An educational or occupational credential. A diploma, academic degree, certification, qualification, badge, etc., that may be awarded to a person or other entity that meets the requirements defined by the credentialer. */
	'EducationalOccupationalCredential': 'https://schema.org/EducationalOccupationalCredential',
	/** A program offered by an institution which determines the learning progress to achieve an outcome, usually a credential like a degree or certificate. This would define a discrete set of opportunities (e.g., job, courses) that together constitute a program with a clear start, end, set of requirements, and transition to a new occupational opportunity (e.g., a job), or sometimes a higher educational opportunity (e.g., an advanced degree). */
	'EducationalOccupationalProgram': 'https://schema.org/EducationalOccupationalProgram',
	/** An educational organization. */
	'EducationalOrganization': 'https://schema.org/EducationalOrganization',
	/** Content about the effectiveness-related aspects of a health topic. */
	'EffectivenessHealthAspect': 'https://schema.org/EffectivenessHealthAspect',
	/** An electrician. */
	'Electrician': 'https://schema.org/Electrician',
	/** An electronics store. */
	'ElectronicsStore': 'https://schema.org/ElectronicsStore',
	/** An elementary school. */
	'ElementarySchool': 'https://schema.org/ElementarySchool',
	/** An email message. */
	'EmailMessage': 'https://schema.org/EmailMessage',
	/** An embassy. */
	'Embassy': 'https://schema.org/Embassy',
	/** A specific branch of medical science that deals with the evaluation and initial treatment of medical conditions caused by trauma or sudden illness. */
	'Emergency': 'https://schema.org/Emergency',
	/** An emergency service, such as a fire station or ER. */
	'EmergencyService': 'https://schema.org/EmergencyService',
	/** A subclass of OrganizationRole used to describe employee relationships. */
	'EmployeeRole': 'https://schema.org/EmployeeRole',
	/** An aggregate rating of an Organization related to its role as an employer. */
	'EmployerAggregateRating': 'https://schema.org/EmployerAggregateRating',
	/** An [[EmployerReview]] is a review of an [[Organization]] regarding its role as an employer, written by a current or former employee of that organization. */
	'EmployerReview': 'https://schema.org/EmployerReview',
	/** An employment agency. */
	'EmploymentAgency': 'https://schema.org/EmploymentAgency',
	/** A specific branch of medical science that pertains to diagnosis and treatment of disorders of endocrine glands and their secretions. */
	'Endocrine': 'https://schema.org/Endocrine',
	/** An agent approves/certifies/likes/supports/sanctions an object. */
	'EndorseAction': 'https://schema.org/EndorseAction',
	/** An EndorsementRating is a rating that expresses some level of endorsement, for example inclusion in a "critic's pick" blog, a
"Like" or "+1" on a social network. It can be considered the [[result]] of an [[EndorseAction]] in which the [[object]] of the action is rated positively by
some [[agent]]. As is common elsewhere in schema.org, it is sometimes more useful to describe the results of such an action without explicitly describing the [[Action]].

An [[EndorsementRating]] may be part of a numeric scale or organized system, but this is not required: having an explicit type for indicating a positive,
endorsement rating is particularly useful in the absence of numeric scales as it helps consumers understand that the rating is broadly positive.
 */
	'EndorsementRating': 'https://schema.org/EndorsementRating',
	/** Properties that take Energy as values are of the form '&lt;Number&gt; &lt;Energy unit of measure&gt;'. */
	'Energy': 'https://schema.org/Energy',
	/** EnergyConsumptionDetails represents information related to the energy efficiency of a product that consumes energy. The information that can be provided is based on international regulations such as for example [EU directive 2017/1369](https://eur-lex.europa.eu/eli/reg/2017/1369/oj) for energy labeling and the [Energy labeling rule](https://www.ftc.gov/enforcement/rules/rulemaking-regulatory-reform-proceedings/energy-water-use-labeling-consumer) under the Energy Policy and Conservation Act (EPCA) in the US. */
	'EnergyConsumptionDetails': 'https://schema.org/EnergyConsumptionDetails',
	/** Enumerates energy efficiency levels (also known as "classes" or "ratings") and certifications that are part of several international energy efficiency standards. */
	'EnergyEfficiencyEnumeration': 'https://schema.org/EnergyEfficiencyEnumeration',
	/** Represents EnergyStar certification. */
	'EnergyStarCertified': 'https://schema.org/EnergyStarCertified',
	/** Used to indicate whether a product is EnergyStar certified. */
	'EnergyStarEnergyEfficiencyEnumeration': 'https://schema.org/EnergyStarEnergyEfficiencyEnumeration',
	/** Information about the engine of the vehicle. A vehicle can have multiple engines represented by multiple engine specification entities. */
	'EngineSpecification': 'https://schema.org/EngineSpecification',
	/** Enrolling participants by invitation only. */
	'EnrollingByInvitation': 'https://schema.org/EnrollingByInvitation',
	/** A business providing entertainment. */
	'EntertainmentBusiness': 'https://schema.org/EntertainmentBusiness',
	/** An entry point, within some Web-based protocol. */
	'EntryPoint': 'https://schema.org/EntryPoint',
	/** Lists or enumerations—for example, a list of cuisines or music genres, etc. */
	'Enumeration': 'https://schema.org/Enumeration',
	/** A media episode (e.g. TV, radio, video game) which can be part of a series or season. */
	'Episode': 'https://schema.org/Episode',
	/** An event happening at a certain time and location, such as a concert, lecture, or festival. Ticketing information may be added via the [[offers]] property. Repeated events may be structured as separate Event objects. */
	'Event': 'https://schema.org/Event',
	/** An EventAttendanceModeEnumeration value is one of potentially several modes of organising an event, relating to whether it is online or offline. */
	'EventAttendanceModeEnumeration': 'https://schema.org/EventAttendanceModeEnumeration',
	/** The event has been cancelled. If the event has multiple startDate values, all are assumed to be cancelled. Either startDate or previousStartDate may be used to specify the event's cancelled date(s). */
	'EventCancelled': 'https://schema.org/EventCancelled',
	/** Indicates that the event was changed to allow online participation. See [[eventAttendanceMode]] for specifics of whether it is now fully or partially online. */
	'EventMovedOnline': 'https://schema.org/EventMovedOnline',
	/** The event has been postponed and no new date has been set. The event's previousStartDate should be set. */
	'EventPostponed': 'https://schema.org/EventPostponed',
	/** The event has been rescheduled. The event's previousStartDate should be set to the old date and the startDate should be set to the event's new date. (If the event has been rescheduled multiple times, the previousStartDate property may be repeated.) */
	'EventRescheduled': 'https://schema.org/EventRescheduled',
	/** A reservation for an event like a concert, sporting event, or lecture.\n\nNote: This type is for information about actual reservations, e.g. in confirmation emails or HTML pages with individual confirmations of reservations. For offers of tickets, use [[Offer]]. */
	'EventReservation': 'https://schema.org/EventReservation',
	/** The event is taking place or has taken place on the startDate as scheduled. Use of this value is optional, as it is assumed by default. */
	'EventScheduled': 'https://schema.org/EventScheduled',
	/** A series of [[Event]]s. Included events can relate with the series using the [[superEvent]] property.

An EventSeries is a collection of events that share some unifying characteristic. For example, "The Olympic Games" is a series, which
is repeated regularly. The "2012 London Olympics" can be presented both as an [[Event]] in the series "Olympic Games", and as an
[[EventSeries]] that included a number of sporting competitions as Events.

The nature of the association between the events in an [[EventSeries]] can vary, but typical examples could
include a thematic event series (e.g. topical meetups or classes), or a series of regular events that share a location, attendee group and/or organizers.

EventSeries has been defined as a kind of Event to make it easy for publishers to use it in an Event context without
worrying about which kinds of series are really event-like enough to call an Event. In general an EventSeries
may seem more Event-like when the period of time is compact and when aspects such as location are fixed, but
it may also sometimes prove useful to describe a longer-term series as an Event.
    */
	'EventSeries': 'https://schema.org/EventSeries',
	/** EventStatusType is an enumeration type whose instances represent several states that an Event may be in. */
	'EventStatusType': 'https://schema.org/EventStatusType',
	/** An event venue. */
	'EventVenue': 'https://schema.org/EventVenue',
	/** Data derived from multiple randomized clinical trials or meta-analyses. */
	'EvidenceLevelA': 'https://schema.org/EvidenceLevelA',
	/** Data derived from a single randomized trial, or nonrandomized studies. */
	'EvidenceLevelB': 'https://schema.org/EvidenceLevelB',
	/** Only consensus opinion of experts, case studies, or standard-of-care. */
	'EvidenceLevelC': 'https://schema.org/EvidenceLevelC',
	/** An example [[MeasurementMethodEnum]] (to remove when real enums are added). */
	'ExampleMeasurementMethodEnum': 'https://schema.org/ExampleMeasurementMethodEnum',
	/** A structured value representing exchange rate. */
	'ExchangeRateSpecification': 'https://schema.org/ExchangeRateSpecification',
	/** Specifies that a refund can be done as an exchange for the same product. */
	'ExchangeRefund': 'https://schema.org/ExchangeRefund',
	/** The act of participating in exertive activity for the purposes of improving health and fitness. */
	'ExerciseAction': 'https://schema.org/ExerciseAction',
	/** A gym. */
	'ExerciseGym': 'https://schema.org/ExerciseGym',
	/** Fitness-related activity designed for a specific health-related purpose, including defined exercise routines as well as activity prescribed by a clinician. */
	'ExercisePlan': 'https://schema.org/ExercisePlan',
	/** Event type: Exhibition event, e.g. at a museum, library, archive, tradeshow, ... */
	'ExhibitionEvent': 'https://schema.org/ExhibitionEvent',
	/** Eye or ophthalmological function assessment with clinical examination. */
	'Eye': 'https://schema.org/Eye',
	/** A [[FAQPage]] is a [[WebPage]] presenting one or more "[Frequently asked questions](https://en.wikipedia.org/wiki/FAQ)" (see also [[QAPage]]). */
	'FAQPage': 'https://schema.org/FAQPage',
	/** A designation by the US FDA signifying that adequate and well-controlled studies have failed to demonstrate a risk to the fetus in the first trimester of pregnancy (and there is no evidence of risk in later trimesters). */
	'FDAcategoryA': 'https://schema.org/FDAcategoryA',
	/** A designation by the US FDA signifying that animal reproduction studies have failed to demonstrate a risk to the fetus and there are no adequate and well-controlled studies in pregnant women. */
	'FDAcategoryB': 'https://schema.org/FDAcategoryB',
	/** A designation by the US FDA signifying that animal reproduction studies have shown an adverse effect on the fetus and there are no adequate and well-controlled studies in humans, but potential benefits may warrant use of the drug in pregnant women despite potential risks. */
	'FDAcategoryC': 'https://schema.org/FDAcategoryC',
	/** A designation by the US FDA signifying that there is positive evidence of human fetal risk based on adverse reaction data from investigational or marketing experience or studies in humans, but potential benefits may warrant use of the drug in pregnant women despite potential risks. */
	'FDAcategoryD': 'https://schema.org/FDAcategoryD',
	/** A designation by the US FDA signifying that studies in animals or humans have demonstrated fetal abnormalities and/or there is positive evidence of human fetal risk based on adverse reaction data from investigational or marketing experience, and the risks involved in use of the drug in pregnant women clearly outweigh potential benefits. */
	'FDAcategoryX': 'https://schema.org/FDAcategoryX',
	/** A designation that the drug in question has not been assigned a pregnancy category designation by the US FDA. */
	'FDAnotEvaluated': 'https://schema.org/FDAnotEvaluated',
	/** A radio channel that uses FM. */
	'FMRadioChannel': 'https://schema.org/FMRadioChannel',
	/** An action that failed to complete. The action's error property and the HTTP return code contain more information about the failure. */
	'FailedActionStatus': 'https://schema.org/FailedActionStatus',
	/** The boolean value false. */
	'False': 'https://schema.org/False',
	/** A fast-food restaurant. */
	'FastFoodRestaurant': 'https://schema.org/FastFoodRestaurant',
	/** The female gender. */
	'Female': 'https://schema.org/Female',
	/** Event type: Festival. */
	'Festival': 'https://schema.org/Festival',
	/** The act of capturing sound and moving images on film, video, or digitally. */
	'FilmAction': 'https://schema.org/FilmAction',
	/** A product provided to consumers and businesses by financial institutions such as banks, insurance companies, brokerage firms, consumer finance companies, and investment companies which comprise the financial services industry. */
	'FinancialProduct': 'https://schema.org/FinancialProduct',
	/** Financial services business. */
	'FinancialService': 'https://schema.org/FinancialService',
	/** The act of finding an object.\n\nRelated actions:\n\n* [[SearchAction]]: FindAction is generally lead by a SearchAction, but not necessarily. */
	'FindAction': 'https://schema.org/FindAction',
	/** A fire station. With firemen. */
	'FireStation': 'https://schema.org/FireStation',
	/** Physical activity that is engaged in to improve joint and muscle flexibility. */
	'Flexibility': 'https://schema.org/Flexibility',
	/** An airline flight. */
	'Flight': 'https://schema.org/Flight',
	/** A reservation for air travel.\n\nNote: This type is for information about actual reservations, e.g. in confirmation emails or HTML pages with individual confirmations of reservations. For offers of tickets, use [[Offer]]. */
	'FlightReservation': 'https://schema.org/FlightReservation',
	/** Data type: Floating number. */
	'Float': 'https://schema.org/Float',
	/** A FloorPlan is an explicit representation of a collection of similar accommodations, allowing the provision of common information (room counts, sizes, layout diagrams) and offers for rental or sale. In typical use, some [[ApartmentComplex]] has an [[accommodationFloorPlan]] which is a [[FloorPlan]].  A FloorPlan is always in the context of a particular place, either a larger [[ApartmentComplex]] or a single [[Apartment]]. The visual/spatial aspects of a floor plan (i.e. room layout, [see wikipedia](https://en.wikipedia.org/wiki/Floor_plan)) can be indicated using [[image]].  */
	'FloorPlan': 'https://schema.org/FloorPlan',
	/** A florist. */
	'Florist': 'https://schema.org/Florist',
	/** The act of forming a personal connection with someone/something (object) unidirectionally/asymmetrically to get updates polled from.\n\nRelated actions:\n\n* [[BefriendAction]]: Unlike BefriendAction, FollowAction implies that the connection is *not* necessarily reciprocal.\n* [[SubscribeAction]]: Unlike SubscribeAction, FollowAction implies that the follower acts as an active agent constantly/actively polling for updates.\n* [[RegisterAction]]: Unlike RegisterAction, FollowAction implies that the agent is interested in continuing receiving updates from the object.\n* [[JoinAction]]: Unlike JoinAction, FollowAction implies that the agent is interested in getting updates from the object.\n* [[TrackAction]]: Unlike TrackAction, FollowAction refers to the polling of updates of all aspects of animate objects rather than the location of inanimate objects (e.g. you track a package, but you don't follow it). */
	'FollowAction': 'https://schema.org/FollowAction',
	/** A food-related business. */
	'FoodEstablishment': 'https://schema.org/FoodEstablishment',
	/** A reservation to dine at a food-related business.\n\nNote: This type is for information about actual reservations, e.g. in confirmation emails or HTML pages with individual confirmations of reservations. */
	'FoodEstablishmentReservation': 'https://schema.org/FoodEstablishmentReservation',
	/** Event type: Food event. */
	'FoodEvent': 'https://schema.org/FoodEvent',
	/** A food service, like breakfast, lunch, or dinner. */
	'FoodService': 'https://schema.org/FoodService',
	/** Four-wheel drive is a transmission layout where the engine primarily drives two wheels with a part-time four-wheel drive capability. */
	'FourWheelDriveConfiguration': 'https://schema.org/FourWheelDriveConfiguration',
	/** Specifies that product returns are free of charge for the customer. */
	'FreeReturn': 'https://schema.org/FreeReturn',
	/** The day of the week between Thursday and Saturday. */
	'Friday': 'https://schema.org/Friday',
	/** Front-wheel drive is a transmission layout where the engine drives the front wheels. */
	'FrontWheelDriveConfiguration': 'https://schema.org/FrontWheelDriveConfiguration',
	/** Indicates full game availability. */
	'FullGameAvailability': 'https://schema.org/FullGameAvailability',
	/** Specifies that a refund can be done in the full amount the customer paid for the product. */
	'FullRefund': 'https://schema.org/FullRefund',
	/** A FundingAgency is an organization that implements one or more [[FundingScheme]]s and manages
    the granting process (via [[Grant]]s, typically [[MonetaryGrant]]s).
    A funding agency is not always required for grant funding, e.g. philanthropic giving, corporate sponsorship etc.
    
Examples of funding agencies include ERC, REA, NIH, Bill and Melinda Gates Foundation, ...
     */
	'FundingAgency': 'https://schema.org/FundingAgency',
	/** A FundingScheme combines organizational, project and policy aspects of grant-based funding
    that sets guidelines, principles and mechanisms to support other kinds of projects and activities.
    Funding is typically organized via [[Grant]] funding. Examples of funding schemes: Swiss Priority Programmes (SPPs); EU Framework 7 (FP7); Horizon 2020; the NIH-R01 Grant Program; Wellcome institutional strategic support fund. For large scale public sector funding, the management and administration of grant awards is often handled by other, dedicated, organizations - [[FundingAgency]]s such as ERC, REA, ... */
	'FundingScheme': 'https://schema.org/FundingScheme',
	/** Pathogenic fungus. */
	'Fungus': 'https://schema.org/Fungus',
	/** A furniture store. */
	'FurnitureStore': 'https://schema.org/FurnitureStore',
	/** The Game type represents things which are games. These are typically rule-governed recreational activities, e.g. role-playing games in which players assume the role of characters in a fictional setting. */
	'Game': 'https://schema.org/Game',
	/** For a [[VideoGame]], such as used with a [[PlayGameAction]], an enumeration of the kind of game availability offered.  */
	'GameAvailabilityEnumeration': 'https://schema.org/GameAvailabilityEnumeration',
	/** Indicates whether this game is multi-player, co-op or single-player. */
	'GamePlayMode': 'https://schema.org/GamePlayMode',
	/** Server that provides game interaction in a multiplayer game. */
	'GameServer': 'https://schema.org/GameServer',
	/** Status of a game server. */
	'GameServerStatus': 'https://schema.org/GameServerStatus',
	/** A garden store. */
	'GardenStore': 'https://schema.org/GardenStore',
	/** A gas station. */
	'GasStation': 'https://schema.org/GasStation',
	/** A specific branch of medical science that pertains to diagnosis and treatment of disorders of digestive system. */
	'Gastroenterologic': 'https://schema.org/Gastroenterologic',
	/** Residence type: Gated community. */
	'GatedResidenceCommunity': 'https://schema.org/GatedResidenceCommunity',
	/** An enumeration of genders. */
	'GenderType': 'https://schema.org/GenderType',
	/** A discrete unit of inheritance which affects one or more biological traits (Source: [https://en.wikipedia.org/wiki/Gene](https://en.wikipedia.org/wiki/Gene)). Examples include FOXP2 (Forkhead box protein P2), SCARNA21 (small Cajal body-specific RNA 21), A- (agouti genotype). */
	'Gene': 'https://schema.org/Gene',
	/** A general contractor. */
	'GeneralContractor': 'https://schema.org/GeneralContractor',
	/** Represents the generic notion of the Web Platform. More specific codes include [[MobileWebPlatform]] and [[DesktopWebPlatform]], as an incomplete list.  */
	'GenericWebPlatform': 'https://schema.org/GenericWebPlatform',
	/** A specific branch of medical science that pertains to hereditary transmission and the variation of inherited characteristics and disorders. */
	'Genetic': 'https://schema.org/Genetic',
	/** Genitourinary system function assessment with clinical examination. */
	'Genitourinary': 'https://schema.org/Genitourinary',
	/** A GeoCircle is a GeoShape representing a circular geographic area. As it is a GeoShape
          it provides the simple textual property 'circle', but also allows the combination of postalCode alongside geoRadius.
          The center of the circle can be indicated via the 'geoMidpoint' property, or more approximately using 'address', 'postalCode'.
        */
	'GeoCircle': 'https://schema.org/GeoCircle',
	/** The geographic coordinates of a place or event. */
	'GeoCoordinates': 'https://schema.org/GeoCoordinates',
	/** The geographic shape of a place. A GeoShape can be described using several properties whose values are based on latitude/longitude pairs. Either whitespace or commas can be used to separate latitude and longitude; whitespace should be used when writing a list of several such points. */
	'GeoShape': 'https://schema.org/GeoShape',
	/** (Eventually to be defined as) a supertype of GeoShape designed to accommodate definitions from Geo-Spatial best practices. */
	'GeospatialGeometry': 'https://schema.org/GeospatialGeometry',
	/** A specific branch of medical science that is concerned with the diagnosis and treatment of diseases, debilities and provision of care to the aged. */
	'Geriatric': 'https://schema.org/Geriatric',
	/** Content that discusses practical and policy aspects for getting access to specific kinds of healthcare (e.g. distribution mechanisms for vaccines). */
	'GettingAccessHealthAspect': 'https://schema.org/GettingAccessHealthAspect',
	/** The act of transferring ownership of an object to a destination. Reciprocal of TakeAction.\n\nRelated actions:\n\n* [[TakeAction]]: Reciprocal of GiveAction.\n* [[SendAction]]: Unlike SendAction, GiveAction implies that ownership is being transferred (e.g. I may send my laptop to you, but that doesn't mean I'm giving it to you). */
	'GiveAction': 'https://schema.org/GiveAction',
	/** A diet exclusive of gluten. */
	'GlutenFreeDiet': 'https://schema.org/GlutenFreeDiet',
	/** A golf course. */
	'GolfCourse': 'https://schema.org/GolfCourse',
	/** GovernmentBenefitsType enumerates several kinds of government benefits to support the COVID-19 situation. Note that this structure may not capture all benefits offered. */
	'GovernmentBenefitsType': 'https://schema.org/GovernmentBenefitsType',
	/** A government building. */
	'GovernmentBuilding': 'https://schema.org/GovernmentBuilding',
	/** A government office&#x2014;for example, an IRS or DMV office. */
	'GovernmentOffice': 'https://schema.org/GovernmentOffice',
	/** A governmental organization or agency. */
	'GovernmentOrganization': 'https://schema.org/GovernmentOrganization',
	/** A permit issued by a government agency. */
	'GovernmentPermit': 'https://schema.org/GovernmentPermit',
	/** A service provided by a government organization, e.g. food stamps, veterans benefits, etc. */
	'GovernmentService': 'https://schema.org/GovernmentService',
	/** A grant, typically financial or otherwise quantifiable, of resources. Typically a [[funder]] sponsors some [[MonetaryAmount]] to an [[Organization]] or [[Person]],
    sometimes not necessarily via a dedicated or long-lived [[Project]], resulting in one or more outputs, or [[fundedItem]]s. For financial sponsorship, indicate the [[funder]] of a [[MonetaryGrant]]. For non-financial support, indicate [[sponsor]] of [[Grant]]s of resources (e.g. office space).

Grants support  activities directed towards some agreed collective goals, often but not always organized as [[Project]]s. Long-lived projects are sometimes sponsored by a variety of grants over time, but it is also common for a project to be associated with a single grant.

The amount of a [[Grant]] is represented using [[amount]] as a [[MonetaryAmount]].
     */
	'Grant': 'https://schema.org/Grant',
	/** Book format: GraphicNovel. May represent a bound collection of ComicIssue instances. */
	'GraphicNovel': 'https://schema.org/GraphicNovel',
	/** A grocery store. */
	'GroceryStore': 'https://schema.org/GroceryStore',
	/** The airline boards by groups based on check-in time, priority, etc. */
	'GroupBoardingPolicy': 'https://schema.org/GroupBoardingPolicy',
	/** [[Guide]] is a page or article that recommends specific products or services, or aspects of a thing for a user to consider. A [[Guide]] may represent a Buying Guide and detail aspects of products or services for a user to consider. A [[Guide]] may represent a Product Guide and recommend specific products or services. A [[Guide]] may represent a Ranked List and recommend specific products or services with ranking. */
	'Guide': 'https://schema.org/Guide',
	/** A specific branch of medical science that pertains to the health care of women, particularly in the diagnosis and treatment of disorders affecting the female reproductive system. */
	'Gynecologic': 'https://schema.org/Gynecologic',
	/** A business that provides Heating, Ventilation and Air Conditioning services. */
	'HVACBusiness': 'https://schema.org/HVACBusiness',
	/** A [hackathon](https://en.wikipedia.org/wiki/Hackathon) event. */
	'Hackathon': 'https://schema.org/Hackathon',
	/** A hair salon. */
	'HairSalon': 'https://schema.org/HairSalon',
	/** A diet conforming to Islamic dietary practices. */
	'HalalDiet': 'https://schema.org/HalalDiet',
	/** Book format: Hardcover. */
	'Hardcover': 'https://schema.org/Hardcover',
	/** A hardware store. */
	'HardwareStore': 'https://schema.org/HardwareStore',
	/** Head assessment with clinical examination. */
	'Head': 'https://schema.org/Head',
	/** Health and beauty. */
	'HealthAndBeautyBusiness': 'https://schema.org/HealthAndBeautyBusiness',
	/** HealthAspectEnumeration enumerates several aspects of health content online, each of which might be described using [[hasHealthAspect]] and [[HealthTopicContent]]. */
	'HealthAspectEnumeration': 'https://schema.org/HealthAspectEnumeration',
	/** HealthCare: this is a benefit for health care. */
	'HealthCare': 'https://schema.org/HealthCare',
	/** A health club. */
	'HealthClub': 'https://schema.org/HealthClub',
	/** A US-style health insurance plan, including PPOs, EPOs, and HMOs.  */
	'HealthInsurancePlan': 'https://schema.org/HealthInsurancePlan',
	/** A description of costs to the patient under a given network or formulary. */
	'HealthPlanCostSharingSpecification': 'https://schema.org/HealthPlanCostSharingSpecification',
	/** For a given health insurance plan, the specification for costs and coverage of prescription drugs.  */
	'HealthPlanFormulary': 'https://schema.org/HealthPlanFormulary',
	/** A US-style health insurance plan network.  */
	'HealthPlanNetwork': 'https://schema.org/HealthPlanNetwork',
	/** [[HealthTopicContent]] is [[WebContent]] that is about some aspect of a health topic, e.g. a condition, its symptoms or treatments. Such content may be comprised of several parts or sections and use different types of media. Multiple instances of [[WebContent]] (and hence [[HealthTopicContent]]) can be related using [[hasPart]] / [[isPartOf]] where there is some kind of content hierarchy, and their content described with [[about]] and [[mentions]] e.g. building upon the existing [[MedicalCondition]] vocabulary.
   */
	'HealthTopicContent': 'https://schema.org/HealthTopicContent',
	/** Item is a pharmaceutical (e.g., a prescription or OTC drug) or a restricted medical device. */
	'HealthcareConsideration': 'https://schema.org/HealthcareConsideration',
	/** Uses devices to support users with hearing impairments. */
	'HearingImpairedSupported': 'https://schema.org/HearingImpairedSupported',
	/** A specific branch of medical science that pertains to diagnosis and treatment of disorders of blood and blood producing organs. */
	'Hematologic': 'https://schema.org/Hematologic',
	/** A high school. */
	'HighSchool': 'https://schema.org/HighSchool',
	/** A diet conforming to Hindu dietary practices, in particular, beef-free. */
	'HinduDiet': 'https://schema.org/HinduDiet',
	/** A Hindu temple. */
	'HinduTemple': 'https://schema.org/HinduTemple',
	/** A store that sells materials useful or necessary for various hobbies. */
	'HobbyShop': 'https://schema.org/HobbyShop',
	/** A construction business.\n\nA HomeAndConstructionBusiness is a [[LocalBusiness]] that provides services around homes and buildings.\n\nAs a [[LocalBusiness]] it can be described as a [[provider]] of one or more [[Service]]\(s). */
	'HomeAndConstructionBusiness': 'https://schema.org/HomeAndConstructionBusiness',
	/** A home goods store. */
	'HomeGoodsStore': 'https://schema.org/HomeGoodsStore',
	/** A system of medicine based on the principle that a disease can be cured by a substance that produces similar symptoms in healthy people. */
	'Homeopathic': 'https://schema.org/Homeopathic',
	/** A hospital. */
	'Hospital': 'https://schema.org/Hospital',
	/** A hostel - cheap accommodation, often in shared dormitories.
<br /><br />
See also the <a href="/docs/hotels.html">dedicated document on the use of schema.org for marking up hotels and other forms of accommodations</a>.
 */
	'Hostel': 'https://schema.org/Hostel',
	/** A hotel is an establishment that provides lodging paid on a short-term basis (source: Wikipedia, the free encyclopedia, see http://en.wikipedia.org/wiki/Hotel).
<br /><br />
See also the <a href="/docs/hotels.html">dedicated document on the use of schema.org for marking up hotels and other forms of accommodations</a>.
 */
	'Hotel': 'https://schema.org/Hotel',
	/** A hotel room is a single room in a hotel.
<br /><br />
See also the <a href="/docs/hotels.html">dedicated document on the use of schema.org for marking up hotels and other forms of accommodations</a>.
 */
	'HotelRoom': 'https://schema.org/HotelRoom',
	/** A house is a building or structure that has the ability to be occupied for habitation by humans or other creatures (source: Wikipedia, the free encyclopedia, see <a href="http://en.wikipedia.org/wiki/House">http://en.wikipedia.org/wiki/House</a>). */
	'House': 'https://schema.org/House',
	/** A house painting service. */
	'HousePainter': 'https://schema.org/HousePainter',
	/** Content that discusses and explains how a particular health-related topic works, e.g. in terms of mechanisms and underlying science. */
	'HowItWorksHealthAspect': 'https://schema.org/HowItWorksHealthAspect',
	/** Information about how or where to find a topic. Also may contain location data that can be used for where to look for help if the topic is observed. */
	'HowOrWhereHealthAspect': 'https://schema.org/HowOrWhereHealthAspect',
	/** Instructions that explain how to achieve a result by performing a sequence of steps. */
	'HowTo': 'https://schema.org/HowTo',
	/** A direction indicating a single action to do in the instructions for how to achieve a result. */
	'HowToDirection': 'https://schema.org/HowToDirection',
	/** An item used as either a tool or supply when performing the instructions for how to achieve a result. */
	'HowToItem': 'https://schema.org/HowToItem',
	/** A sub-grouping of steps in the instructions for how to achieve a result (e.g. steps for making a pie crust within a pie recipe). */
	'HowToSection': 'https://schema.org/HowToSection',
	/** A step in the instructions for how to achieve a result. It is an ordered list with HowToDirection and/or HowToTip items. */
	'HowToStep': 'https://schema.org/HowToStep',
	/** A supply consumed when performing the instructions for how to achieve a result. */
	'HowToSupply': 'https://schema.org/HowToSupply',
	/** An explanation in the instructions for how to achieve a result. It provides supplementary information about a technique, supply, author's preference, etc. It can explain what could be done, or what should not be done, but doesn't specify what should be done (see HowToDirection). */
	'HowToTip': 'https://schema.org/HowToTip',
	/** A tool used (but not consumed) when performing instructions for how to achieve a result. */
	'HowToTool': 'https://schema.org/HowToTool',
	/** A HyperToc represents a hypertext table of contents for complex media objects, such as [[VideoObject]], [[AudioObject]]. Items in the table of contents are indicated using the [[tocEntry]] property, and typed [[HyperTocEntry]]. For cases where the same larger work is split into multiple files, [[associatedMedia]] can be used on individual [[HyperTocEntry]] items. */
	'HyperToc': 'https://schema.org/HyperToc',
	/** A HyperToEntry is an item within a [[HyperToc]], which represents a hypertext table of contents for complex media objects, such as [[VideoObject]], [[AudioObject]]. The media object itself is indicated using [[associatedMedia]]. Each section of interest within that content can be described with a [[HyperTocEntry]], with associated [[startOffset]] and [[endOffset]]. When several entries are all from the same file, [[associatedMedia]] is used on the overarching [[HyperTocEntry]]; if the content has been split into multiple files, they can be referenced using [[associatedMedia]] on each [[HyperTocEntry]]. */
	'HyperTocEntry': 'https://schema.org/HyperTocEntry',
	/** Represents the broad notion of iOS-based operating systems. */
	'IOSPlatform': 'https://schema.org/IOSPlatform',
	/** An ice cream shop. */
	'IceCreamShop': 'https://schema.org/IceCreamShop',
	/** The act of intentionally disregarding the object. An agent ignores an object. */
	'IgnoreAction': 'https://schema.org/IgnoreAction',
	/** Web page type: Image gallery page. */
	'ImageGallery': 'https://schema.org/ImageGallery',
	/** An image file. */
	'ImageObject': 'https://schema.org/ImageObject',
	/** A specific and exact (byte-for-byte) version of an [[ImageObject]]. Two byte-for-byte identical files, for the purposes of this type, considered identical. If they have different embedded metadata (e.g. XMP, EXIF) the files will differ. Different external facts about the files, e.g. creator or dateCreated that aren't represented in their actual content, do not affect this notion of identity. */
	'ImageObjectSnapshot': 'https://schema.org/ImageObjectSnapshot',
	/** Any medical imaging modality typically used for diagnostic purposes. */
	'ImagingTest': 'https://schema.org/ImagingTest',
	/** Indicates that a legislation is in force. */
	'InForce': 'https://schema.org/InForce',
	/** Indicates that the item is in stock. */
	'InStock': 'https://schema.org/InStock',
	/** Indicates that the item is available only at physical locations. */
	'InStoreOnly': 'https://schema.org/InStoreOnly',
	/** A single, identifiable product instance (e.g. a laptop with a particular serial number). */
	'IndividualProduct': 'https://schema.org/IndividualProduct',
	/** Something in medical science that pertains to infectious diseases, i.e. caused by bacterial, viral, fungal or parasitic infections. */
	'Infectious': 'https://schema.org/Infectious',
	/** Classes of agents or pathogens that transmit infectious diseases. Enumerated type. */
	'InfectiousAgentClass': 'https://schema.org/InfectiousAgentClass',
	/** An infectious disease is a clinically evident human disease resulting from the presence of pathogenic microbial agents, like pathogenic viruses, pathogenic bacteria, fungi, protozoa, multicellular parasites, and prions. To be considered an infectious disease, such pathogens are known to be able to cause this disease. */
	'InfectiousDisease': 'https://schema.org/InfectiousDisease',
	/** The act of notifying someone of information pertinent to them, with no expectation of a response. */
	'InformAction': 'https://schema.org/InformAction',
	/** Content discussing ingredients-related aspects of a health topic. */
	'IngredientsHealthAspect': 'https://schema.org/IngredientsHealthAspect',
	/** The act of adding at a specific location in an ordered collection. */
	'InsertAction': 'https://schema.org/InsertAction',
	/** The act of installing an application. */
	'InstallAction': 'https://schema.org/InstallAction',
	/** Represents the installment pricing component of the total price for an offered product. */
	'Installment': 'https://schema.org/Installment',
	/** An Insurance agency. */
	'InsuranceAgency': 'https://schema.org/InsuranceAgency',
	/** A utility class that serves as the umbrella for a number of 'intangible' things such as quantities, structured values, etc. */
	'Intangible': 'https://schema.org/Intangible',
	/** Data type: Integer. */
	'Integer': 'https://schema.org/Integer',
	/** The act of interacting with another person or organization. */
	'InteractAction': 'https://schema.org/InteractAction',
	/** A summary of how users have interacted with this CreativeWork. In most cases, authors will use a subtype to specify the specific type of interaction. */
	'InteractionCounter': 'https://schema.org/InteractionCounter',
	/** An international trial. */
	'InternationalTrial': 'https://schema.org/InternationalTrial',
	/** An internet cafe. */
	'InternetCafe': 'https://schema.org/InternetCafe',
	/** A company or fund that gathers capital from a number of investors to create a pool of money that is then re-invested into stocks, bonds and other assets. */
	'InvestmentFund': 'https://schema.org/InvestmentFund',
	/** A type of financial product that typically requires the client to transfer funds to a financial service in return for potential beneficial financial return. */
	'InvestmentOrDeposit': 'https://schema.org/InvestmentOrDeposit',
	/** The act of asking someone to attend an event. Reciprocal of RsvpAction. */
	'InviteAction': 'https://schema.org/InviteAction',
	/** A statement of the money due for goods or services; a bill. */
	'Invoice': 'https://schema.org/Invoice',
	/** Represents the invoice price of an offered product. */
	'InvoicePrice': 'https://schema.org/InvoicePrice',
	/** A list of possible product availability options. */
	'ItemAvailability': 'https://schema.org/ItemAvailability',
	/** A list of items of any sort&#x2014;for example, Top 10 Movies About Weathermen, or Top 100 Party Songs. Not to be confused with HTML lists, which are often used only for formatting. */
	'ItemList': 'https://schema.org/ItemList',
	/** An ItemList ordered with lower values listed first. */
	'ItemListOrderAscending': 'https://schema.org/ItemListOrderAscending',
	/** An ItemList ordered with higher values listed first. */
	'ItemListOrderDescending': 'https://schema.org/ItemListOrderDescending',
	/** Enumerated for values for itemListOrder for indicating how an ordered ItemList is organized. */
	'ItemListOrderType': 'https://schema.org/ItemListOrderType',
	/** An ItemList ordered with no explicit order. */
	'ItemListUnordered': 'https://schema.org/ItemListUnordered',
	/** A page devoted to a single item, such as a particular product or hotel. */
	'ItemPage': 'https://schema.org/ItemPage',
	/** A jewelry store. */
	'JewelryStore': 'https://schema.org/JewelryStore',
	/** A listing that describes a job opening in a certain organization. */
	'JobPosting': 'https://schema.org/JobPosting',
	/** An agent joins an event/group with participants/friends at a location.\n\nRelated actions:\n\n* [[RegisterAction]]: Unlike RegisterAction, JoinAction refers to joining a group/team of people.\n* [[SubscribeAction]]: Unlike SubscribeAction, JoinAction does not imply that you'll be receiving updates.\n* [[FollowAction]]: Unlike FollowAction, JoinAction does not imply that you'll be polling for updates. */
	'JoinAction': 'https://schema.org/JoinAction',
	/** The anatomical location at which two or more bones make contact. */
	'Joint': 'https://schema.org/Joint',
	/** A diet conforming to Jewish dietary practices. */
	'KosherDiet': 'https://schema.org/KosherDiet',
	/** A medical science pertaining to chemical, hematological, immunologic, microscopic, or bacteriological diagnostic analyses or research. */
	'LaboratoryScience': 'https://schema.org/LaboratoryScience',
	/** A lake (for example, Lake Pontrachain). */
	'LakeBodyOfWater': 'https://schema.org/LakeBodyOfWater',
	/** A landform or physical feature.  Landform elements include mountains, plains, lakes, rivers, seascape and oceanic waterbody interface features such as bays, peninsulas, seas and so forth, including sub-aqueous terrain features such as submersed mountain ranges, volcanoes, and the great ocean basins. */
	'Landform': 'https://schema.org/Landform',
	/** An historical landmark or building. */
	'LandmarksOrHistoricalBuildings': 'https://schema.org/LandmarksOrHistoricalBuildings',
	/** Natural languages such as Spanish, Tamil, Hindi, English, etc. Formal language code tags expressed in [BCP 47](https://en.wikipedia.org/wiki/IETF_language_tag) can be used via the [[alternateName]] property. The Language type previously also covered programming languages such as Scheme and Lisp, which are now best represented using [[ComputerLanguage]]. */
	'Language': 'https://schema.org/Language',
	/** LaserDiscFormat. */
	'LaserDiscFormat': 'https://schema.org/LaserDiscFormat',
	/** The LearningResource type can be used to indicate [[CreativeWork]]s (whether physical or digital) that have a particular and explicit orientation towards learning, education, skill acquisition, and other educational purposes.

[[LearningResource]] is expected to be used as an addition to a primary type such as [[Book]], [[VideoObject]], [[Product]] etc.

[[EducationEvent]] serves a similar purpose for event-like things (e.g. a [[Trip]]). A [[LearningResource]] may be created as a result of an [[EducationEvent]], for example by recording one. */
	'LearningResource': 'https://schema.org/LearningResource',
	/** An agent leaves an event / group with participants/friends at a location.\n\nRelated actions:\n\n* [[JoinAction]]: The antonym of LeaveAction.\n* [[UnRegisterAction]]: Unlike UnRegisterAction, LeaveAction implies leaving a group/team of people rather than a service. */
	'LeaveAction': 'https://schema.org/LeaveAction',
	/** The steering position is on the left side of the vehicle (viewed from the main direction of driving). */
	'LeftHandDriving': 'https://schema.org/LeftHandDriving',
	/** A list of possible statuses for the legal force of a legislation. */
	'LegalForceStatus': 'https://schema.org/LegalForceStatus',
	/** A LegalService is a business that provides legally-oriented services, advice and representation, e.g. law firms.\n\nAs a [[LocalBusiness]] it can be described as a [[provider]] of one or more [[Service]]\(s). */
	'LegalService': 'https://schema.org/LegalService',
	/** A list of possible levels for the legal validity of a legislation. */
	'LegalValueLevel': 'https://schema.org/LegalValueLevel',
	/** A legal document such as an act, decree, bill, etc. (enforceable or not) or a component of a legal act (like an article). */
	'Legislation': 'https://schema.org/Legislation',
	/** A specific object or file containing a Legislation. Note that the same Legislation can be published in multiple files. For example, a digitally signed PDF, a plain PDF and an HTML version. */
	'LegislationObject': 'https://schema.org/LegislationObject',
	/** A legislative building&#x2014;for example, the state capitol. */
	'LegislativeBuilding': 'https://schema.org/LegislativeBuilding',
	/** Any physical activity engaged in for recreational purposes. Examples may include ballroom dancing, roller skating, canoeing, fishing, etc. */
	'LeisureTimeActivity': 'https://schema.org/LeisureTimeActivity',
	/** The act of providing an object under an agreement that it will be returned at a later date. Reciprocal of BorrowAction.\n\nRelated actions:\n\n* [[BorrowAction]]: Reciprocal of LendAction. */
	'LendAction': 'https://schema.org/LendAction',
	/** A library. */
	'Library': 'https://schema.org/Library',
	/** A [[LibrarySystem]] is a collaborative system amongst several libraries. */
	'LibrarySystem': 'https://schema.org/LibrarySystem',
	/** A process of care involving exercise, changes to diet, fitness routines, and other lifestyle changes aimed at improving a health condition. */
	'LifestyleModification': 'https://schema.org/LifestyleModification',
	/** A short band of tough, flexible, fibrous connective tissue that functions to connect multiple bones, cartilages, and structurally support joints. */
	'Ligament': 'https://schema.org/Ligament',
	/** The act of expressing a positive sentiment about the object. An agent likes an object (a proposition, topic or theme) with participants. */
	'LikeAction': 'https://schema.org/LikeAction',
	/** Indicates that the item has limited availability. */
	'LimitedAvailability': 'https://schema.org/LimitedAvailability',
	/** LimitedByGuaranteeCharity: Non-profit type referring to a charitable company that is limited by guarantee (UK). */
	'LimitedByGuaranteeCharity': 'https://schema.org/LimitedByGuaranteeCharity',
	/** A Role that represents a Web link, e.g. as expressed via the 'url' property. Its linkRelationship property can indicate URL-based and plain textual link types, e.g. those in IANA link registry or others such as 'amphtml'. This structure provides a placeholder where details from HTML's link element can be represented outside of HTML, e.g. in JSON-LD feeds. */
	'LinkRole': 'https://schema.org/LinkRole',
	/** A shop that sells alcoholic drinks such as wine, beer, whisky and other spirits. */
	'LiquorStore': 'https://schema.org/LiquorStore',
	/** An list item, e.g. a step in a checklist or how-to description. */
	'ListItem': 'https://schema.org/ListItem',
	/** Represents the list price (the price a product is actually advertised for) of an offered product. */
	'ListPrice': 'https://schema.org/ListPrice',
	/** The act of consuming audio content. */
	'ListenAction': 'https://schema.org/ListenAction',
	/** Event type: Literary event. */
	'LiteraryEvent': 'https://schema.org/LiteraryEvent',
	/** LiveAlbum. */
	'LiveAlbum': 'https://schema.org/LiveAlbum',
	/** A [[LiveBlogPosting]] is a [[BlogPosting]] intended to provide a rolling textual coverage of an ongoing event through continuous updates. */
	'LiveBlogPosting': 'https://schema.org/LiveBlogPosting',
	/** Information about coping or life related to the topic. */
	'LivingWithHealthAspect': 'https://schema.org/LivingWithHealthAspect',
	/** A financial product for the loaning of an amount of money, or line of credit, under agreed terms and charges. */
	'LoanOrCredit': 'https://schema.org/LoanOrCredit',
	/** A particular physical business or branch of an organization. Examples of LocalBusiness include a restaurant, a particular branch of a restaurant chain, a branch of a bank, a medical practice, a club, a bowling alley, etc. */
	'LocalBusiness': 'https://schema.org/LocalBusiness',
	/** Specifies a location feature by providing a structured value representing a feature of an accommodation as a property-value pair of varying degrees of formality. */
	'LocationFeatureSpecification': 'https://schema.org/LocationFeatureSpecification',
	/** A DeliveryMethod in which an item is made available via locker. */
	'LockerDelivery': 'https://schema.org/LockerDelivery',
	/** A locksmith. */
	'Locksmith': 'https://schema.org/Locksmith',
	/** A lodging business, such as a motel, hotel, or inn. */
	'LodgingBusiness': 'https://schema.org/LodgingBusiness',
	/** A reservation for lodging at a hotel, motel, inn, etc.\n\nNote: This type is for information about actual reservations, e.g. in confirmation emails or HTML pages with individual confirmations of reservations. */
	'LodgingReservation': 'https://schema.org/LodgingReservation',
	/** Unlike cross-sectional studies, longitudinal studies track the same people, and therefore the differences observed in those people are less likely to be the result of cultural differences across generations. Longitudinal studies are also used in medicine to uncover predictors of certain diseases. */
	'Longitudinal': 'https://schema.org/Longitudinal',
	/** The act of being defeated in a competitive activity. */
	'LoseAction': 'https://schema.org/LoseAction',
	/** A diet focused on reduced calorie intake. */
	'LowCalorieDiet': 'https://schema.org/LowCalorieDiet',
	/** A diet focused on reduced fat and cholesterol intake. */
	'LowFatDiet': 'https://schema.org/LowFatDiet',
	/** A diet appropriate for people with lactose intolerance. */
	'LowLactoseDiet': 'https://schema.org/LowLactoseDiet',
	/** A diet focused on reduced sodium intake. */
	'LowSaltDiet': 'https://schema.org/LowSaltDiet',
	/** Lung and respiratory system clinical examination. */
	'Lung': 'https://schema.org/Lung',
	/** A type of blood vessel that specifically carries lymph fluid unidirectionally toward the heart. */
	'LymphaticVessel': 'https://schema.org/LymphaticVessel',
	/** Magnetic resonance imaging. */
	'MRI': 'https://schema.org/MRI',
	/** Represents the manufacturer suggested retail price ("MSRP") of an offered product. */
	'MSRP': 'https://schema.org/MSRP',
	/** The male gender. */
	'Male': 'https://schema.org/Male',
	/** A book, document, or piece of music written by hand rather than typed or printed. */
	'Manuscript': 'https://schema.org/Manuscript',
	/** A map. */
	'Map': 'https://schema.org/Map',
	/** An enumeration of several kinds of Map. */
	'MapCategoryType': 'https://schema.org/MapCategoryType',
	/** The act of marrying a person. */
	'MarryAction': 'https://schema.org/MarryAction',
	/** Properties that take Mass as values are of the form '&lt;Number&gt; &lt;Mass unit of measure&gt;'. E.g., '7 kg'. */
	'Mass': 'https://schema.org/Mass',
	/** A math solver which is capable of solving a subset of mathematical problems. */
	'MathSolver': 'https://schema.org/MathSolver',
	/** The maximum dosing schedule considered safe for a drug or supplement as recommended by an authority or by the drug/supplement's manufacturer. Capture the recommending authority in the recognizingAuthority property of MedicalEntity. */
	'MaximumDoseSchedule': 'https://schema.org/MaximumDoseSchedule',
	/** Related topics may be treated by a Topic. */
	'MayTreatHealthAspect': 'https://schema.org/MayTreatHealthAspect',
	/** Enumeration(s) for use with [[measurementMethod]]. */
	'MeasurementMethodEnum': 'https://schema.org/MeasurementMethodEnum',
	/** Enumeration of common measurement types (or dimensions), for example "chest" for a person, "inseam" for pants, "gauge" for screws, or "wheel" for bicycles. */
	'MeasurementTypeEnumeration': 'https://schema.org/MeasurementTypeEnumeration',
	/** Web page type: Media gallery page. A mixed-media page that can contain media such as images, videos, and other multimedia. */
	'MediaGallery': 'https://schema.org/MediaGallery',
	/**  Codes for use with the [[mediaAuthenticityCategory]] property, indicating the authenticity of a media object (in the context of how it was published or shared). In general these codes are not mutually exclusive, although some combinations (such as 'original' versus 'transformed', 'edited' and 'staged') would be contradictory if applied in the same [[MediaReview]]. Note that the application of these codes is with regard to a piece of media shared or published in a particular context. */
	'MediaManipulationRatingEnumeration': 'https://schema.org/MediaManipulationRatingEnumeration',
	/** A media object, such as an image, video, audio, or text object embedded in a web page or a downloadable dataset i.e. DataDownload. Note that a creative work may have many media objects associated with it on the same web page. For example, a page about a single song (MusicRecording) may have a music video (VideoObject), and a high and low bandwidth audio stream (2 AudioObject's). */
	'MediaObject': 'https://schema.org/MediaObject',
	/** A [[MediaReview]] is a more specialized form of Review dedicated to the evaluation of media content online, typically in the context of fact-checking and misinformation.
    For more general reviews of media in the broader sense, use [[UserReview]], [[CriticReview]] or other [[Review]] types. This definition is
    a work in progress. While the [[MediaManipulationRatingEnumeration]] list reflects significant community review amongst fact-checkers and others working
    to combat misinformation, the specific structures for representing media objects, their versions and publication context, are still evolving. Similarly, best practices for the relationship between [[MediaReview]] and [[ClaimReview]] markup have not yet been finalized. */
	'MediaReview': 'https://schema.org/MediaReview',
	/** Represents an item or group of closely related items treated as a unit for the sake of evaluation in a [[MediaReview]]. Authorship etc. apply to the items rather than to the curation/grouping or reviewing party. */
	'MediaReviewItem': 'https://schema.org/MediaReviewItem',
	/** A subscription which allows a user to access media including audio, video, books, etc. */
	'MediaSubscription': 'https://schema.org/MediaSubscription',
	/** Target audiences for medical web pages. */
	'MedicalAudience': 'https://schema.org/MedicalAudience',
	/** Target audiences types for medical web pages. Enumerated type. */
	'MedicalAudienceType': 'https://schema.org/MedicalAudienceType',
	/** A particular physical or virtual business of an organization for medical purposes. Examples of MedicalBusiness include different businesses run by health professionals. */
	'MedicalBusiness': 'https://schema.org/MedicalBusiness',
	/** The causative agent(s) that are responsible for the pathophysiologic process that eventually results in a medical condition, symptom or sign. In this schema, unless otherwise specified this is meant to be the proximate cause of the medical condition, symptom or sign. The proximate cause is defined as the causative agent that most directly results in the medical condition, symptom or sign. For example, the HIV virus could be considered a cause of AIDS. Or in a diagnostic context, if a patient fell and sustained a hip fracture and two days later sustained a pulmonary embolism which eventuated in a cardiac arrest, the cause of the cardiac arrest (the proximate cause) would be the pulmonary embolism and not the fall. Medical causes can include cardiovascular, chemical, dermatologic, endocrine, environmental, gastroenterologic, genetic, hematologic, gynecologic, iatrogenic, infectious, musculoskeletal, neurologic, nutritional, obstetric, oncologic, otolaryngologic, pharmacologic, psychiatric, pulmonary, renal, rheumatologic, toxic, traumatic, or urologic causes; medical conditions can be causes as well. */
	'MedicalCause': 'https://schema.org/MedicalCause',
	/** A facility, often associated with a hospital or medical school, that is devoted to the specific diagnosis and/or healthcare. Previously limited to outpatients but with evolution it may be open to inpatients as well. */
	'MedicalClinic': 'https://schema.org/MedicalClinic',
	/** A code for a medical entity. */
	'MedicalCode': 'https://schema.org/MedicalCode',
	/** Any condition of the human body that affects the normal functioning of a person, whether physically or mentally. Includes diseases, injuries, disabilities, disorders, syndromes, etc. */
	'MedicalCondition': 'https://schema.org/MedicalCondition',
	/** A stage of a medical condition, such as 'Stage IIIa'. */
	'MedicalConditionStage': 'https://schema.org/MedicalConditionStage',
	/** A condition or factor that serves as a reason to withhold a certain medical therapy. Contraindications can be absolute (there are no reasonable circumstances for undertaking a course of action) or relative (the patient is at higher risk of complications, but these risks may be outweighed by other considerations or mitigated by other measures). */
	'MedicalContraindication': 'https://schema.org/MedicalContraindication',
	/** Any object used in a medical capacity, such as to diagnose or treat a patient. */
	'MedicalDevice': 'https://schema.org/MedicalDevice',
	/** Categories of medical devices, organized by the purpose or intended use of the device. */
	'MedicalDevicePurpose': 'https://schema.org/MedicalDevicePurpose',
	/** The most generic type of entity related to health and the practice of medicine. */
	'MedicalEntity': 'https://schema.org/MedicalEntity',
	/** Enumerations related to health and the practice of medicine: A concept that is used to attribute a quality to another concept, as a qualifier, a collection of items or a listing of all of the elements of a set in medicine practice. */
	'MedicalEnumeration': 'https://schema.org/MedicalEnumeration',
	/** Level of evidence for a medical guideline. Enumerated type. */
	'MedicalEvidenceLevel': 'https://schema.org/MedicalEvidenceLevel',
	/** Any recommendation made by a standard society (e.g. ACC/AHA) or consensus statement that denotes how to diagnose and treat a particular condition. Note: this type should be used to tag the actual guideline recommendation; if the guideline recommendation occurs in a larger scholarly article, use MedicalScholarlyArticle to tag the overall article, not this type. Note also: the organization making the recommendation should be captured in the recognizingAuthority base property of MedicalEntity. */
	'MedicalGuideline': 'https://schema.org/MedicalGuideline',
	/** A guideline contraindication that designates a process as harmful and where quality of the data supporting the contraindication is sound. */
	'MedicalGuidelineContraindication': 'https://schema.org/MedicalGuidelineContraindication',
	/** A guideline recommendation that is regarded as efficacious and where quality of the data supporting the recommendation is sound. */
	'MedicalGuidelineRecommendation': 'https://schema.org/MedicalGuidelineRecommendation',
	/** Any medical imaging modality typically used for diagnostic purposes. Enumerated type. */
	'MedicalImagingTechnique': 'https://schema.org/MedicalImagingTechnique',
	/** A condition or factor that indicates use of a medical therapy, including signs, symptoms, risk factors, anatomical states, etc. */
	'MedicalIndication': 'https://schema.org/MedicalIndication',
	/** A utility class that serves as the umbrella for a number of 'intangible' things in the medical space. */
	'MedicalIntangible': 'https://schema.org/MedicalIntangible',
	/** An observational study is a type of medical study that attempts to infer the possible effect of a treatment through observation of a cohort of subjects over a period of time. In an observational study, the assignment of subjects into treatment groups versus control groups is outside the control of the investigator. This is in contrast with controlled studies, such as the randomized controlled trials represented by MedicalTrial, where each subject is randomly assigned to a treatment group or a control group before the start of the treatment. */
	'MedicalObservationalStudy': 'https://schema.org/MedicalObservationalStudy',
	/** Design models for observational medical studies. Enumerated type. */
	'MedicalObservationalStudyDesign': 'https://schema.org/MedicalObservationalStudyDesign',
	/** A medical organization (physical or not), such as hospital, institution or clinic. */
	'MedicalOrganization': 'https://schema.org/MedicalOrganization',
	/** A process of care used in either a diagnostic, therapeutic, preventive or palliative capacity that relies on invasive (surgical), non-invasive, or other techniques. */
	'MedicalProcedure': 'https://schema.org/MedicalProcedure',
	/** An enumeration that describes different types of medical procedures. */
	'MedicalProcedureType': 'https://schema.org/MedicalProcedureType',
	/** Medical researchers. */
	'MedicalResearcher': 'https://schema.org/MedicalResearcher',
	/** A complex mathematical calculation requiring an online calculator, used to assess prognosis. Note: use the url property of Thing to record any URLs for online calculators. */
	'MedicalRiskCalculator': 'https://schema.org/MedicalRiskCalculator',
	/** Any rule set or interactive tool for estimating the risk of developing a complication or condition. */
	'MedicalRiskEstimator': 'https://schema.org/MedicalRiskEstimator',
	/** A risk factor is anything that increases a person's likelihood of developing or contracting a disease, medical condition, or complication. */
	'MedicalRiskFactor': 'https://schema.org/MedicalRiskFactor',
	/** A simple system that adds up the number of risk factors to yield a score that is associated with prognosis, e.g. CHAD score, TIMI risk score. */
	'MedicalRiskScore': 'https://schema.org/MedicalRiskScore',
	/** A scholarly article in the medical domain. */
	'MedicalScholarlyArticle': 'https://schema.org/MedicalScholarlyArticle',
	/** Any physical manifestation of a person's medical condition discoverable by objective diagnostic tests or physical examination. */
	'MedicalSign': 'https://schema.org/MedicalSign',
	/** Any feature associated or not with a medical condition. In medicine a symptom is generally subjective while a sign is objective. */
	'MedicalSignOrSymptom': 'https://schema.org/MedicalSignOrSymptom',
	/** Any specific branch of medical science or practice. Medical specialities include clinical specialties that pertain to particular organ systems and their respective disease states, as well as allied health specialties. Enumerated type. */
	'MedicalSpecialty': 'https://schema.org/MedicalSpecialty',
	/** A medical study is an umbrella type covering all kinds of research studies relating to human medicine or health, including observational studies and interventional trials and registries, randomized, controlled or not. When the specific type of study is known, use one of the extensions of this type, such as MedicalTrial or MedicalObservationalStudy. Also, note that this type should be used to mark up data that describes the study itself; to tag an article that publishes the results of a study, use MedicalScholarlyArticle. Note: use the code property of MedicalEntity to store study IDs, e.g. clinicaltrials.gov ID. */
	'MedicalStudy': 'https://schema.org/MedicalStudy',
	/** The status of a medical study. Enumerated type. */
	'MedicalStudyStatus': 'https://schema.org/MedicalStudyStatus',
	/** Any complaint sensed and expressed by the patient (therefore defined as subjective)  like stomachache, lower-back pain, or fatigue. */
	'MedicalSymptom': 'https://schema.org/MedicalSymptom',
	/** Any medical test, typically performed for diagnostic purposes. */
	'MedicalTest': 'https://schema.org/MedicalTest',
	/** Any collection of tests commonly ordered together. */
	'MedicalTestPanel': 'https://schema.org/MedicalTestPanel',
	/** Any medical intervention designed to prevent, treat, and cure human diseases and medical conditions, including both curative and palliative therapies. Medical therapies are typically processes of care relying upon pharmacotherapy, behavioral therapy, supportive therapy (with fluid or nutrition for example), or detoxification (e.g. hemodialysis) aimed at improving or preventing a health condition. */
	'MedicalTherapy': 'https://schema.org/MedicalTherapy',
	/** A medical trial is a type of medical study that uses a scientific process to compare the safety and efficacy of medical therapies or medical procedures. In general, medical trials are controlled and subjects are allocated at random to the different treatment and/or control groups. */
	'MedicalTrial': 'https://schema.org/MedicalTrial',
	/** Design models for medical trials. Enumerated type. */
	'MedicalTrialDesign': 'https://schema.org/MedicalTrialDesign',
	/** A web page that provides medical information. */
	'MedicalWebPage': 'https://schema.org/MedicalWebPage',
	/** Systems of medical practice. */
	'MedicineSystem': 'https://schema.org/MedicineSystem',
	/** A meeting room, conference room, or conference hall is a room provided for singular events such as business conferences and meetings (source: Wikipedia, the free encyclopedia, see <a href="http://en.wikipedia.org/wiki/Conference_hall">http://en.wikipedia.org/wiki/Conference_hall</a>).
<br /><br />
See also the <a href="/docs/hotels.html">dedicated document on the use of schema.org for marking up hotels and other forms of accommodations</a>.
 */
	'MeetingRoom': 'https://schema.org/MeetingRoom',
	/** A men's clothing store. */
	'MensClothingStore': 'https://schema.org/MensClothingStore',
	/** A structured representation of food or drink items available from a FoodEstablishment. */
	'Menu': 'https://schema.org/Menu',
	/** A food or drink item listed in a menu or menu section. */
	'MenuItem': 'https://schema.org/MenuItem',
	/** A sub-grouping of food or drink items in a menu. E.g. courses (such as 'Dinner', 'Breakfast', etc.), specific type of dishes (such as 'Meat', 'Vegan', 'Drinks', etc.), or some other classification made by the menu provider. */
	'MenuSection': 'https://schema.org/MenuSection',
	/** Enumerates several kinds of product return policies. */
	'MerchantReturnEnumeration': 'https://schema.org/MerchantReturnEnumeration',
	/** Specifies that there is a finite window for product returns. */
	'MerchantReturnFiniteReturnWindow': 'https://schema.org/MerchantReturnFiniteReturnWindow',
	/** Specifies that product returns are not permitted. */
	'MerchantReturnNotPermitted': 'https://schema.org/MerchantReturnNotPermitted',
	/** A MerchantReturnPolicy provides information about product return policies associated with an [[Organization]], [[Product]], or [[Offer]]. */
	'MerchantReturnPolicy': 'https://schema.org/MerchantReturnPolicy',
	/** A seasonal override of a return policy, for example used for holidays. */
	'MerchantReturnPolicySeasonalOverride': 'https://schema.org/MerchantReturnPolicySeasonalOverride',
	/** Specifies that there is an unlimited window for product returns. */
	'MerchantReturnUnlimitedWindow': 'https://schema.org/MerchantReturnUnlimitedWindow',
	/** Specifies that a product return policy is not provided. */
	'MerchantReturnUnspecified': 'https://schema.org/MerchantReturnUnspecified',
	/** A single message from a sender to one or more organizations or people. */
	'Message': 'https://schema.org/Message',
	/** A middle school (typically for children aged around 11-14, although this varies somewhat). */
	'MiddleSchool': 'https://schema.org/MiddleSchool',
	/** A nurse-like health profession that deals with pregnancy, childbirth, and the postpartum period (including care of the newborn), besides sexual and reproductive health of women throughout their lives. */
	'Midwifery': 'https://schema.org/Midwifery',
	/** Represents the minimum advertised price ("MAP") (as dictated by the manufacturer) of an offered product. */
	'MinimumAdvertisedPrice': 'https://schema.org/MinimumAdvertisedPrice',
	/** Content about common misconceptions and myths that are related to a topic. */
	'MisconceptionsHealthAspect': 'https://schema.org/MisconceptionsHealthAspect',
	/** MixedEventAttendanceMode - an event that is conducted as a combination of both offline and online modes. */
	'MixedEventAttendanceMode': 'https://schema.org/MixedEventAttendanceMode',
	/** MixtapeAlbum. */
	'MixtapeAlbum': 'https://schema.org/MixtapeAlbum',
	/** A software application designed specifically to work well on a mobile device such as a telephone. */
	'MobileApplication': 'https://schema.org/MobileApplication',
	/** A store that sells mobile phones and related accessories. */
	'MobilePhoneStore': 'https://schema.org/MobilePhoneStore',
	/** Represents the broad notion of 'mobile' browsers as a Web Platform. */
	'MobileWebPlatform': 'https://schema.org/MobileWebPlatform',
	/** Any constitutionally or isotopically distinct atom, molecule, ion, ion pair, radical, radical ion, complex, conformer etc., identifiable as a separately distinguishable entity. */
	'MolecularEntity': 'https://schema.org/MolecularEntity',
	/** The day of the week between Sunday and Tuesday. */
	'Monday': 'https://schema.org/Monday',
	/** A monetary value or range. This type can be used to describe an amount of money such as $50 USD, or a range as in describing a bank account being suitable for a balance between £1,000 and £1,000,000 GBP, or the value of a salary, etc. It is recommended to use [[PriceSpecification]] Types to describe the price of an Offer, Invoice, etc. */
	'MonetaryAmount': 'https://schema.org/MonetaryAmount',
	/** A statistical distribution of monetary amounts. */
	'MonetaryAmountDistribution': 'https://schema.org/MonetaryAmountDistribution',
	/** A monetary grant. */
	'MonetaryGrant': 'https://schema.org/MonetaryGrant',
	/** The act of transferring money from one place to another place. This may occur electronically or physically. */
	'MoneyTransfer': 'https://schema.org/MoneyTransfer',
	/** A loan in which property or real estate is used as collateral. (A loan securitized against some real estate.) */
	'MortgageLoan': 'https://schema.org/MortgageLoan',
	/** A mosque. */
	'Mosque': 'https://schema.org/Mosque',
	/** A motel.
<br /><br />
See also the <a href="/docs/hotels.html">dedicated document on the use of schema.org for marking up hotels and other forms of accommodations</a>.
 */
	'Motel': 'https://schema.org/Motel',
	/** A motorcycle or motorbike is a single-track, two-wheeled motor vehicle. */
	'Motorcycle': 'https://schema.org/Motorcycle',
	/** A motorcycle dealer. */
	'MotorcycleDealer': 'https://schema.org/MotorcycleDealer',
	/** A motorcycle repair shop. */
	'MotorcycleRepair': 'https://schema.org/MotorcycleRepair',
	/** A motorized bicycle is a bicycle with an attached motor used to power the vehicle, or to assist with pedaling. */
	'MotorizedBicycle': 'https://schema.org/MotorizedBicycle',
	/** A mountain, like Mount Whitney or Mount Everest. */
	'Mountain': 'https://schema.org/Mountain',
	/** The act of an agent relocating to a place.\n\nRelated actions:\n\n* [[TransferAction]]: Unlike TransferAction, the subject of the move is a living Person or Organization rather than an inanimate object. */
	'MoveAction': 'https://schema.org/MoveAction',
	/** A movie. */
	'Movie': 'https://schema.org/Movie',
	/** A short segment/part of a movie. */
	'MovieClip': 'https://schema.org/MovieClip',
	/** A movie rental store. */
	'MovieRentalStore': 'https://schema.org/MovieRentalStore',
	/** A series of movies. Included movies can be indicated with the hasPart property. */
	'MovieSeries': 'https://schema.org/MovieSeries',
	/** A movie theater. */
	'MovieTheater': 'https://schema.org/MovieTheater',
	/** A moving company. */
	'MovingCompany': 'https://schema.org/MovingCompany',
	/** A trial that takes place at multiple centers. */
	'MultiCenterTrial': 'https://schema.org/MultiCenterTrial',
	/** Play mode: MultiPlayer. Requiring or allowing multiple human players to play simultaneously. */
	'MultiPlayer': 'https://schema.org/MultiPlayer',
	/** Multicellular parasite that causes an infection. */
	'MulticellularParasite': 'https://schema.org/MulticellularParasite',
	/** A muscle is an anatomical structure consisting of a contractile form of tissue that animals use to effect movement. */
	'Muscle': 'https://schema.org/Muscle',
	/** A specific branch of medical science that pertains to diagnosis and treatment of disorders of muscles, ligaments and skeletal system. */
	'Musculoskeletal': 'https://schema.org/Musculoskeletal',
	/** Musculoskeletal system clinical examination. */
	'MusculoskeletalExam': 'https://schema.org/MusculoskeletalExam',
	/** A museum. */
	'Museum': 'https://schema.org/Museum',
	/** A collection of music tracks. */
	'MusicAlbum': 'https://schema.org/MusicAlbum',
	/** Classification of the album by its type of content: soundtrack, live album, studio album, etc. */
	'MusicAlbumProductionType': 'https://schema.org/MusicAlbumProductionType',
	/** The kind of release which this album is: single, EP or album. */
	'MusicAlbumReleaseType': 'https://schema.org/MusicAlbumReleaseType',
	/** A musical composition. */
	'MusicComposition': 'https://schema.org/MusicComposition',
	/** Event type: Music event. */
	'MusicEvent': 'https://schema.org/MusicEvent',
	/** A musical group, such as a band, an orchestra, or a choir. Can also be a solo musician. */
	'MusicGroup': 'https://schema.org/MusicGroup',
	/** A collection of music tracks in playlist form. */
	'MusicPlaylist': 'https://schema.org/MusicPlaylist',
	/** A music recording (track), usually a single song. */
	'MusicRecording': 'https://schema.org/MusicRecording',
	/** A MusicRelease is a specific release of a music album. */
	'MusicRelease': 'https://schema.org/MusicRelease',
	/** Format of this release (the type of recording media used, i.e. compact disc, digital media, LP, etc.). */
	'MusicReleaseFormatType': 'https://schema.org/MusicReleaseFormatType',
	/** A music store. */
	'MusicStore': 'https://schema.org/MusicStore',
	/** A music venue. */
	'MusicVenue': 'https://schema.org/MusicVenue',
	/** A music video file. */
	'MusicVideoObject': 'https://schema.org/MusicVideoObject',
	/** Organization: Non-governmental Organization. */
	'NGO': 'https://schema.org/NGO',
	/** NLNonprofitType: Non-profit organization type originating from the Netherlands. */
	'NLNonprofitType': 'https://schema.org/NLNonprofitType',
	/** A nail salon. */
	'NailSalon': 'https://schema.org/NailSalon',
	/** Item is a narcotic as defined by the [1961 UN convention](https://www.incb.org/incb/en/narcotic-drugs/Yellowlist/yellow-list.html), for example marijuana or heroin. */
	'NarcoticConsideration': 'https://schema.org/NarcoticConsideration',
	/** Neck assessment with clinical examination. */
	'Neck': 'https://schema.org/Neck',
	/** A common pathway for the electrochemical nerve impulses that are transmitted along each of the axons. */
	'Nerve': 'https://schema.org/Nerve',
	/** Neurological system clinical examination. */
	'Neuro': 'https://schema.org/Neuro',
	/** A specific branch of medical science that studies the nerves and nervous system and its respective disease states. */
	'Neurologic': 'https://schema.org/Neurologic',
	/** Indicates that the item is new. */
	'NewCondition': 'https://schema.org/NewCondition',
	/** A NewsArticle is an article whose content reports news, or provides background context and supporting materials for understanding the news.

A more detailed overview of [schema.org News markup](/docs/news.html) is also available.
 */
	'NewsArticle': 'https://schema.org/NewsArticle',
	/** A News/Media organization such as a newspaper or TV station. */
	'NewsMediaOrganization': 'https://schema.org/NewsMediaOrganization',
	/** A publication containing information about varied topics that are pertinent to general information, a geographic area, or a specific subject matter (i.e. business, culture, education). Often published daily. */
	'Newspaper': 'https://schema.org/Newspaper',
	/** A nightclub or discotheque. */
	'NightClub': 'https://schema.org/NightClub',
	/** A type of medical procedure that involves noninvasive techniques. */
	'NoninvasiveProcedure': 'https://schema.org/NoninvasiveProcedure',
	/** Nonprofit501a: Non-profit type referring to Farmers’ Cooperative Associations. */
	'Nonprofit501a': 'https://schema.org/Nonprofit501a',
	/** Nonprofit501c1: Non-profit type referring to Corporations Organized Under Act of Congress, including Federal Credit Unions and National Farm Loan Associations. */
	'Nonprofit501c1': 'https://schema.org/Nonprofit501c1',
	/** Nonprofit501c10: Non-profit type referring to Domestic Fraternal Societies and Associations. */
	'Nonprofit501c10': 'https://schema.org/Nonprofit501c10',
	/** Nonprofit501c11: Non-profit type referring to Teachers' Retirement Fund Associations. */
	'Nonprofit501c11': 'https://schema.org/Nonprofit501c11',
	/** Nonprofit501c12: Non-profit type referring to Benevolent Life Insurance Associations, Mutual Ditch or Irrigation Companies, Mutual or Cooperative Telephone Companies. */
	'Nonprofit501c12': 'https://schema.org/Nonprofit501c12',
	/** Nonprofit501c13: Non-profit type referring to Cemetery Companies. */
	'Nonprofit501c13': 'https://schema.org/Nonprofit501c13',
	/** Nonprofit501c14: Non-profit type referring to State-Chartered Credit Unions, Mutual Reserve Funds. */
	'Nonprofit501c14': 'https://schema.org/Nonprofit501c14',
	/** Nonprofit501c15: Non-profit type referring to Mutual Insurance Companies or Associations. */
	'Nonprofit501c15': 'https://schema.org/Nonprofit501c15',
	/** Nonprofit501c16: Non-profit type referring to Cooperative Organizations to Finance Crop Operations. */
	'Nonprofit501c16': 'https://schema.org/Nonprofit501c16',
	/** Nonprofit501c17: Non-profit type referring to Supplemental Unemployment Benefit Trusts. */
	'Nonprofit501c17': 'https://schema.org/Nonprofit501c17',
	/** Nonprofit501c18: Non-profit type referring to Employee Funded Pension Trust (created before 25 June 1959). */
	'Nonprofit501c18': 'https://schema.org/Nonprofit501c18',
	/** Nonprofit501c19: Non-profit type referring to Post or Organization of Past or Present Members of the Armed Forces. */
	'Nonprofit501c19': 'https://schema.org/Nonprofit501c19',
	/** Nonprofit501c2: Non-profit type referring to Title-holding Corporations for Exempt Organizations. */
	'Nonprofit501c2': 'https://schema.org/Nonprofit501c2',
	/** Nonprofit501c20: Non-profit type referring to Group Legal Services Plan Organizations. */
	'Nonprofit501c20': 'https://schema.org/Nonprofit501c20',
	/** Nonprofit501c21: Non-profit type referring to Black Lung Benefit Trusts. */
	'Nonprofit501c21': 'https://schema.org/Nonprofit501c21',
	/** Nonprofit501c22: Non-profit type referring to Withdrawal Liability Payment Funds. */
	'Nonprofit501c22': 'https://schema.org/Nonprofit501c22',
	/** Nonprofit501c23: Non-profit type referring to Veterans Organizations. */
	'Nonprofit501c23': 'https://schema.org/Nonprofit501c23',
	/** Nonprofit501c24: Non-profit type referring to Section 4049 ERISA Trusts. */
	'Nonprofit501c24': 'https://schema.org/Nonprofit501c24',
	/** Nonprofit501c25: Non-profit type referring to Real Property Title-Holding Corporations or Trusts with Multiple Parents. */
	'Nonprofit501c25': 'https://schema.org/Nonprofit501c25',
	/** Nonprofit501c26: Non-profit type referring to State-Sponsored Organizations Providing Health Coverage for High-Risk Individuals. */
	'Nonprofit501c26': 'https://schema.org/Nonprofit501c26',
	/** Nonprofit501c27: Non-profit type referring to State-Sponsored Workers' Compensation Reinsurance Organizations. */
	'Nonprofit501c27': 'https://schema.org/Nonprofit501c27',
	/** Nonprofit501c28: Non-profit type referring to National Railroad Retirement Investment Trusts. */
	'Nonprofit501c28': 'https://schema.org/Nonprofit501c28',
	/** Nonprofit501c3: Non-profit type referring to Religious, Educational, Charitable, Scientific, Literary, Testing for Public Safety, Fostering National or International Amateur Sports Competition, or Prevention of Cruelty to Children or Animals Organizations. */
	'Nonprofit501c3': 'https://schema.org/Nonprofit501c3',
	/** Nonprofit501c4: Non-profit type referring to Civic Leagues, Social Welfare Organizations, and Local Associations of Employees. */
	'Nonprofit501c4': 'https://schema.org/Nonprofit501c4',
	/** Nonprofit501c5: Non-profit type referring to Labor, Agricultural and Horticultural Organizations. */
	'Nonprofit501c5': 'https://schema.org/Nonprofit501c5',
	/** Nonprofit501c6: Non-profit type referring to Business Leagues, Chambers of Commerce, Real Estate Boards. */
	'Nonprofit501c6': 'https://schema.org/Nonprofit501c6',
	/** Nonprofit501c7: Non-profit type referring to Social and Recreational Clubs. */
	'Nonprofit501c7': 'https://schema.org/Nonprofit501c7',
	/** Nonprofit501c8: Non-profit type referring to Fraternal Beneficiary Societies and Associations. */
	'Nonprofit501c8': 'https://schema.org/Nonprofit501c8',
	/** Nonprofit501c9: Non-profit type referring to Voluntary Employee Beneficiary Associations. */
	'Nonprofit501c9': 'https://schema.org/Nonprofit501c9',
	/** Nonprofit501d: Non-profit type referring to Religious and Apostolic Associations. */
	'Nonprofit501d': 'https://schema.org/Nonprofit501d',
	/** Nonprofit501e: Non-profit type referring to Cooperative Hospital Service Organizations. */
	'Nonprofit501e': 'https://schema.org/Nonprofit501e',
	/** Nonprofit501f: Non-profit type referring to Cooperative Service Organizations. */
	'Nonprofit501f': 'https://schema.org/Nonprofit501f',
	/** Nonprofit501k: Non-profit type referring to Child Care Organizations. */
	'Nonprofit501k': 'https://schema.org/Nonprofit501k',
	/** Nonprofit501n: Non-profit type referring to Charitable Risk Pools. */
	'Nonprofit501n': 'https://schema.org/Nonprofit501n',
	/** Nonprofit501q: Non-profit type referring to Credit Counseling Organizations. */
	'Nonprofit501q': 'https://schema.org/Nonprofit501q',
	/** Nonprofit527: Non-profit type referring to political organizations. */
	'Nonprofit527': 'https://schema.org/Nonprofit527',
	/** NonprofitANBI: Non-profit type referring to a Public Benefit Organization (NL). */
	'NonprofitANBI': 'https://schema.org/NonprofitANBI',
	/** NonprofitSBBI: Non-profit type referring to a Social Interest Promoting Institution (NL). */
	'NonprofitSBBI': 'https://schema.org/NonprofitSBBI',
	/** NonprofitType enumerates several kinds of official non-profit types of which a non-profit organization can be. */
	'NonprofitType': 'https://schema.org/NonprofitType',
	/** Nose function assessment with clinical examination. */
	'Nose': 'https://schema.org/Nose',
	/** Indicates that a legislation is currently not in force. */
	'NotInForce': 'https://schema.org/NotInForce',
	/** Not yet recruiting. */
	'NotYetRecruiting': 'https://schema.org/NotYetRecruiting',
	/** A notary. */
	'Notary': 'https://schema.org/Notary',
	/** A file containing a note, primarily for the author. */
	'NoteDigitalDocument': 'https://schema.org/NoteDigitalDocument',
	/** Data type: Number.\n\nUsage guidelines:\n\n* Use values from 0123456789 (Unicode 'DIGIT ZERO' (U+0030) to 'DIGIT NINE' (U+0039)) rather than superficially similar Unicode symbols.\n* Use '.' (Unicode 'FULL STOP' (U+002E)) rather than ',' to indicate a decimal point. Avoid using these symbols as a readability separator. */
	'Number': 'https://schema.org/Number',
	/** A health profession of a person formally educated and trained in the care of the sick or infirm person. */
	'Nursing': 'https://schema.org/Nursing',
	/** Nutritional information about the recipe. */
	'NutritionInformation': 'https://schema.org/NutritionInformation',
	/** The character of a medical substance, typically a medicine, of being available over the counter or not. */
	'OTC': 'https://schema.org/OTC',
	/** Instances of the class [[Observation]] are used to specify observations about an entity at a particular time. The principal properties of an [[Observation]] are [[observationAbout]], [[measuredProperty]], [[statType]], [[value] and [[observationDate]]  and [[measuredProperty]]. Some but not all Observations represent a [[QuantitativeValue]]. Quantitative observations can be about a [[StatisticalVariable]], which is an abstract specification about which we can make observations that are grounded at a particular location and time. 
    
Observations can also encode a subset of simple RDF-like statements (its observationAbout, a StatisticalVariable, defining the measuredPoperty; its observationAbout property indicating the entity the statement is about, and [[value]] )

In the context of a quantitative knowledge graph, typical properties could include [[measuredProperty]], [[observationAbout]], [[observationDate]], [[value]], [[unitCode]], [[unitText]], [[measurementMethod]].
     */
	'Observation': 'https://schema.org/Observation',
	/** An observational study design. */
	'Observational': 'https://schema.org/Observational',
	/** A specific branch of medical science that specializes in the care of women during the prenatal and postnatal care and with the delivery of the child. */
	'Obstetric': 'https://schema.org/Obstetric',
	/** A profession, may involve prolonged training and/or a formal qualification. */
	'Occupation': 'https://schema.org/Occupation',
	/** Any physical activity engaged in for job-related purposes. Examples may include waiting tables, maid service, carrying a mailbag, picking fruits or vegetables, construction work, etc. */
	'OccupationalActivity': 'https://schema.org/OccupationalActivity',
	/** Indicates employment-related experience requirements, e.g. [[monthsOfExperience]]. */
	'OccupationalExperienceRequirements': 'https://schema.org/OccupationalExperienceRequirements',
	/** A treatment of people with physical, emotional, or social problems, using purposeful activity to help them overcome or learn to deal with their problems. */
	'OccupationalTherapy': 'https://schema.org/OccupationalTherapy',
	/** An ocean (for example, the Pacific). */
	'OceanBodyOfWater': 'https://schema.org/OceanBodyOfWater',
	/** An offer to transfer some rights to an item or to provide a service — for example, an offer to sell tickets to an event, to rent the DVD of a movie, to stream a TV show over the internet, to repair a motorcycle, or to loan a book.\n\nNote: As the [[businessFunction]] property, which identifies the form of offer (e.g. sell, lease, repair, dispose), defaults to http://purl.org/goodrelations/v1#Sell; an Offer without a defined businessFunction value can be assumed to be an offer to sell.\n\nFor [GTIN](http://www.gs1.org/barcodes/technical/idkeys/gtin)-related fields, see [Check Digit calculator](http://www.gs1.org/barcodes/support/check_digit_calculator) and [validation guide](http://www.gs1us.org/resources/standards/gtin-validation-guide) from [GS1](http://www.gs1.org/). */
	'Offer': 'https://schema.org/Offer',
	/** An OfferCatalog is an ItemList that contains related Offers and/or further OfferCatalogs that are offeredBy the same provider. */
	'OfferCatalog': 'https://schema.org/OfferCatalog',
	/** An [[OfferForLease]] in Schema.org represents an [[Offer]] to lease out something, i.e. an [[Offer]] whose
  [[businessFunction]] is [lease out](http://purl.org/goodrelations/v1#LeaseOut.). See [Good Relations](https://en.wikipedia.org/wiki/GoodRelations) for
  background on the underlying concepts.
   */
	'OfferForLease': 'https://schema.org/OfferForLease',
	/** An [[OfferForPurchase]] in Schema.org represents an [[Offer]] to sell something, i.e. an [[Offer]] whose
  [[businessFunction]] is [sell](http://purl.org/goodrelations/v1#Sell.). See [Good Relations](https://en.wikipedia.org/wiki/GoodRelations) for
  background on the underlying concepts.
   */
	'OfferForPurchase': 'https://schema.org/OfferForPurchase',
	/** A list of possible conditions for the item. */
	'OfferItemCondition': 'https://schema.org/OfferItemCondition',
	/** OfferShippingDetails represents information about shipping destinations.

Multiple of these entities can be used to represent different shipping rates for different destinations:

One entity for Alaska/Hawaii. A different one for continental US. A different one for all France.

Multiple of these entities can be used to represent different shipping costs and delivery times.

Two entities that are identical but differ in rate and time:

E.g. Cheaper and slower: $5 in 5-7 days
or Fast and expensive: $15 in 1-2 days. */
	'OfferShippingDetails': 'https://schema.org/OfferShippingDetails',
	/** An office equipment store. */
	'OfficeEquipmentStore': 'https://schema.org/OfficeEquipmentStore',
	/** All the documents published by an official publisher should have at least the legal value level "OfficialLegalValue". This indicates that the document was published by an organisation with the public task of making it available (e.g. a consolidated version of an EU directive published by the EU Office of Publications). */
	'OfficialLegalValue': 'https://schema.org/OfficialLegalValue',
	/** OfflineEventAttendanceMode - an event that is primarily conducted offline.  */
	'OfflineEventAttendanceMode': 'https://schema.org/OfflineEventAttendanceMode',
	/** Game server status: OfflinePermanently. Server is offline and not available. */
	'OfflinePermanently': 'https://schema.org/OfflinePermanently',
	/** Game server status: OfflineTemporarily. Server is offline now but it can be online soon. */
	'OfflineTemporarily': 'https://schema.org/OfflineTemporarily',
	/** A publication event, e.g. catch-up TV or radio podcast, during which a program is available on-demand. */
	'OnDemandEvent': 'https://schema.org/OnDemandEvent',
	/** A DeliveryMethod in which an item is collected on site, e.g. in a store or at a box office. */
	'OnSitePickup': 'https://schema.org/OnSitePickup',
	/** A specific branch of medical science that deals with benign and malignant tumors, including the study of their development, diagnosis, treatment and prevention. */
	'Oncologic': 'https://schema.org/Oncologic',
	/** OneTimePayments: this is a benefit for one-time payments for individuals. */
	'OneTimePayments': 'https://schema.org/OneTimePayments',
	/** Game server status: Online. Server is available. */
	'Online': 'https://schema.org/Online',
	/** A particular online business, either standalone or the online part of a broader organization. Examples include an eCommerce site, an online travel booking site, an online learning site, an online logistics and shipping provider, an online (virtual) doctor, etc. */
	'OnlineBusiness': 'https://schema.org/OnlineBusiness',
	/** OnlineEventAttendanceMode - an event that is primarily conducted online.  */
	'OnlineEventAttendanceMode': 'https://schema.org/OnlineEventAttendanceMode',
	/** Game server status: OnlineFull. Server is online but unavailable. The maximum number of players has reached. */
	'OnlineFull': 'https://schema.org/OnlineFull',
	/** Indicates that the item is available only online. */
	'OnlineOnly': 'https://schema.org/OnlineOnly',
	/** An eCommerce site. */
	'OnlineStore': 'https://schema.org/OnlineStore',
	/** A trial design in which the researcher knows the full details of the treatment, and so does the patient. */
	'OpenTrial': 'https://schema.org/OpenTrial',
	/** A structured value providing information about the opening hours of a place or a certain service inside a place.\n\n
The place is __open__ if the [[opens]] property is specified, and __closed__ otherwise.\n\nIf the value for the [[closes]] property is less than the value for the [[opens]] property then the hour range is assumed to span over the next day.
       */
	'OpeningHoursSpecification': 'https://schema.org/OpeningHoursSpecification',
	/** An [[OpinionNewsArticle]] is a [[NewsArticle]] that primarily expresses opinions rather than journalistic reporting of news and events. For example, a [[NewsArticle]] consisting of a column or [[Blog]]/[[BlogPosting]] entry in the Opinions section of a news publication.  */
	'OpinionNewsArticle': 'https://schema.org/OpinionNewsArticle',
	/** A store that sells reading glasses and similar devices for improving vision. */
	'Optician': 'https://schema.org/Optician',
	/** The science or practice of testing visual acuity and prescribing corrective lenses. */
	'Optometric': 'https://schema.org/Optometric',
	/** An order is a confirmation of a transaction (a receipt), which can contain multiple line items, each represented by an Offer that has been accepted by the customer. */
	'Order': 'https://schema.org/Order',
	/** An agent orders an object/product/service to be delivered/sent. */
	'OrderAction': 'https://schema.org/OrderAction',
	/** OrderStatus representing cancellation of an order. */
	'OrderCancelled': 'https://schema.org/OrderCancelled',
	/** OrderStatus representing successful delivery of an order. */
	'OrderDelivered': 'https://schema.org/OrderDelivered',
	/** OrderStatus representing that an order is in transit. */
	'OrderInTransit': 'https://schema.org/OrderInTransit',
	/** An order item is a line of an order. It includes the quantity and shipping details of a bought offer. */
	'OrderItem': 'https://schema.org/OrderItem',
	/** OrderStatus representing that payment is due on an order. */
	'OrderPaymentDue': 'https://schema.org/OrderPaymentDue',
	/** OrderStatus representing availability of an order for pickup. */
	'OrderPickupAvailable': 'https://schema.org/OrderPickupAvailable',
	/** OrderStatus representing that there is a problem with the order. */
	'OrderProblem': 'https://schema.org/OrderProblem',
	/** OrderStatus representing that an order is being processed. */
	'OrderProcessing': 'https://schema.org/OrderProcessing',
	/** OrderStatus representing that an order has been returned. */
	'OrderReturned': 'https://schema.org/OrderReturned',
	/** Enumerated status values for Order. */
	'OrderStatus': 'https://schema.org/OrderStatus',
	/** An organization such as a school, NGO, corporation, club, etc. */
	'Organization': 'https://schema.org/Organization',
	/** A subclass of Role used to describe roles within organizations. */
	'OrganizationRole': 'https://schema.org/OrganizationRole',
	/** The act of manipulating/administering/supervising/controlling one or more objects. */
	'OrganizeAction': 'https://schema.org/OrganizeAction',
	/** Content coded 'as original media content' in a [[MediaReview]], considered in the context of how it was published or shared.

For a [[VideoObject]] to be 'original': No evidence the footage has been misleadingly altered or manipulated, though it may contain false or misleading claims.

For an [[ImageObject]] to be 'original': No evidence the image has been misleadingly altered or manipulated, though it may still contain false or misleading claims.

For an [[ImageObject]] with embedded text to be 'original': No evidence the image has been misleadingly altered or manipulated, though it may still contain false or misleading claims.

For an [[AudioObject]] to be 'original': No evidence the audio has been misleadingly altered or manipulated, though it may contain false or misleading claims.
 */
	'OriginalMediaContent': 'https://schema.org/OriginalMediaContent',
	/** Specifies that the customer must pay the original shipping costs when returning a product. */
	'OriginalShippingFees': 'https://schema.org/OriginalShippingFees',
	/** A system of medicine focused on promoting the body's innate ability to heal itself. */
	'Osteopathic': 'https://schema.org/Osteopathic',
	/** A specific branch of medical science that is concerned with the ear, nose and throat and their respective disease states. */
	'Otolaryngologic': 'https://schema.org/Otolaryngologic',
	/** Indicates that the item is out of stock. */
	'OutOfStock': 'https://schema.org/OutOfStock',
	/** An outlet store. */
	'OutletStore': 'https://schema.org/OutletStore',
	/** Overview of the content. Contains a summarized view of the topic with the most relevant information for an introduction. */
	'OverviewHealthAspect': 'https://schema.org/OverviewHealthAspect',
	/** A structured value providing information about when a certain organization or person owned a certain product. */
	'OwnershipInfo': 'https://schema.org/OwnershipInfo',
	/** Positron emission tomography imaging. */
	'PET': 'https://schema.org/PET',
	/** PaidLeave: this is a benefit for paid leave. */
	'PaidLeave': 'https://schema.org/PaidLeave',
	/** The act of producing a painting, typically with paint and canvas as instruments. */
	'PaintAction': 'https://schema.org/PaintAction',
	/** A painting. */
	'Painting': 'https://schema.org/Painting',
	/** A medical procedure intended primarily for palliative purposes, aimed at relieving the symptoms of an underlying health condition. */
	'PalliativeProcedure': 'https://schema.org/PalliativeProcedure',
	/** Book format: Paperback. */
	'Paperback': 'https://schema.org/Paperback',
	/** The delivery of a parcel either via the postal service or a commercial service. */
	'ParcelDelivery': 'https://schema.org/ParcelDelivery',
	/** A private parcel service as the delivery mode available for a certain offer.\n\nCommonly used values:\n\n* http://purl.org/goodrelations/v1#DHL\n* http://purl.org/goodrelations/v1#FederalExpress\n* http://purl.org/goodrelations/v1#UPS
       */
	'ParcelService': 'https://schema.org/ParcelService',
	/** A set of characteristics describing parents, who can be interested in viewing some content. */
	'ParentAudience': 'https://schema.org/ParentAudience',
	/** ParentalSupport: this is a benefit for parental support. */
	'ParentalSupport': 'https://schema.org/ParentalSupport',
	/** A park. */
	'Park': 'https://schema.org/Park',
	/** A parking lot or other parking facility. */
	'ParkingFacility': 'https://schema.org/ParkingFacility',
	/** A parking map. */
	'ParkingMap': 'https://schema.org/ParkingMap',
	/** Indicates that parts of the legislation are in force, and parts are not. */
	'PartiallyInForce': 'https://schema.org/PartiallyInForce',
	/** A specific branch of medical science that is concerned with the study of the cause, origin and nature of a disease state, including its consequences as a result of manifestation of the disease. In clinical care, the term is used to designate a branch of medicine using laboratory tests to diagnose and determine the prognostic significance of illness. */
	'Pathology': 'https://schema.org/Pathology',
	/** A medical test performed by a laboratory that typically involves examination of a tissue sample by a pathologist. */
	'PathologyTest': 'https://schema.org/PathologyTest',
	/** A patient is any person recipient of health care services. */
	'Patient': 'https://schema.org/Patient',
	/** Content about the real life experience of patients or people that have lived a similar experience about the topic. May be forums, topics, Q-and-A and related material. */
	'PatientExperienceHealthAspect': 'https://schema.org/PatientExperienceHealthAspect',
	/** A shop that will buy, or lend money against the security of, personal possessions. */
	'PawnShop': 'https://schema.org/PawnShop',
	/** An agent pays a price to a participant. */
	'PayAction': 'https://schema.org/PayAction',
	/** An automatic payment system is in place and will be used. */
	'PaymentAutomaticallyApplied': 'https://schema.org/PaymentAutomaticallyApplied',
	/** A payment method using a credit, debit, store or other card to associate the payment with an account. */
	'PaymentCard': 'https://schema.org/PaymentCard',
	/** The costs of settling the payment using a particular payment method. */
	'PaymentChargeSpecification': 'https://schema.org/PaymentChargeSpecification',
	/** The payment has been received and processed. */
	'PaymentComplete': 'https://schema.org/PaymentComplete',
	/** The payee received the payment, but it was declined for some reason. */
	'PaymentDeclined': 'https://schema.org/PaymentDeclined',
	/** The payment is due, but still within an acceptable time to be received. */
	'PaymentDue': 'https://schema.org/PaymentDue',
	/** A payment method is a standardized procedure for transferring the monetary amount for a purchase. Payment methods are characterized by the legal and technical structures used, and by the organization or group carrying out the transaction.\n\nCommonly used values:\n\n* http://purl.org/goodrelations/v1#ByBankTransferInAdvance\n* http://purl.org/goodrelations/v1#ByInvoice\n* http://purl.org/goodrelations/v1#Cash\n* http://purl.org/goodrelations/v1#CheckInAdvance\n* http://purl.org/goodrelations/v1#COD\n* http://purl.org/goodrelations/v1#DirectDebit\n* http://purl.org/goodrelations/v1#GoogleCheckout\n* http://purl.org/goodrelations/v1#PayPal\n* http://purl.org/goodrelations/v1#PaySwarm
         */
	'PaymentMethod': 'https://schema.org/PaymentMethod',
	/** The payment is due and considered late. */
	'PaymentPastDue': 'https://schema.org/PaymentPastDue',
	/** A Service to transfer funds from a person or organization to a beneficiary person or organization. */
	'PaymentService': 'https://schema.org/PaymentService',
	/** A specific payment status. For example, PaymentDue, PaymentComplete, etc. */
	'PaymentStatusType': 'https://schema.org/PaymentStatusType',
	/** A specific branch of medical science that specializes in the care of infants, children and adolescents. */
	'Pediatric': 'https://schema.org/Pediatric',
	/** A set of characteristics belonging to people, e.g. who compose an item's target audience. */
	'PeopleAudience': 'https://schema.org/PeopleAudience',
	/** A type of medical procedure that involves percutaneous techniques, where access to organs or tissue is achieved via needle-puncture of the skin. For example, catheter-based procedures like stent delivery. */
	'PercutaneousProcedure': 'https://schema.org/PercutaneousProcedure',
	/** The act of participating in performance arts. */
	'PerformAction': 'https://schema.org/PerformAction',
	/** A PerformanceRole is a Role that some entity places with regard to a theatrical performance, e.g. in a Movie, TVSeries etc. */
	'PerformanceRole': 'https://schema.org/PerformanceRole',
	/** A theater or other performing art center. */
	'PerformingArtsTheater': 'https://schema.org/PerformingArtsTheater',
	/** A performance group, such as a band, an orchestra, or a circus. */
	'PerformingGroup': 'https://schema.org/PerformingGroup',
	/** A publication in any medium issued in successive parts bearing numerical or chronological designations and intended to continue indefinitely, such as a magazine, scholarly journal, or newspaper.\n\nSee also [blog post](http://blog.schema.org/2014/09/schemaorg-support-for-bibliographic_2.html). */
	'Periodical': 'https://schema.org/Periodical',
	/** A permit issued by an organization, e.g. a parking pass. */
	'Permit': 'https://schema.org/Permit',
	/** A person (alive, dead, undead, or fictional). */
	'Person': 'https://schema.org/Person',
	/** A pet store. */
	'PetStore': 'https://schema.org/PetStore',
	/** A pharmacy or drugstore. */
	'Pharmacy': 'https://schema.org/Pharmacy',
	/** The practice or art and science of preparing and dispensing drugs and medicines. */
	'PharmacySpecialty': 'https://schema.org/PharmacySpecialty',
	/** A photograph. */
	'Photograph': 'https://schema.org/Photograph',
	/** The act of capturing still images of objects using a camera. */
	'PhotographAction': 'https://schema.org/PhotographAction',
	/** Any bodily activity that enhances or maintains physical fitness and overall health and wellness. Includes activity that is part of daily living and routine, structured exercise, and exercise prescribed as part of a medical treatment or recovery plan. */
	'PhysicalActivity': 'https://schema.org/PhysicalActivity',
	/** Categories of physical activity, organized by physiologic classification. */
	'PhysicalActivityCategory': 'https://schema.org/PhysicalActivityCategory',
	/** A type of physical examination of a patient performed by a physician.  */
	'PhysicalExam': 'https://schema.org/PhysicalExam',
	/** A process of progressive physical care and rehabilitation aimed at improving a health condition. */
	'PhysicalTherapy': 'https://schema.org/PhysicalTherapy',
	/** A doctor's office. */
	'Physician': 'https://schema.org/Physician',
	/** The practice of treatment of disease, injury, or deformity by physical methods such as massage, heat treatment, and exercise rather than by drugs or surgery. */
	'Physiotherapy': 'https://schema.org/Physiotherapy',
	/** Entities that have a somewhat fixed, physical extension. */
	'Place': 'https://schema.org/Place',
	/** Place of worship, such as a church, synagogue, or mosque. */
	'PlaceOfWorship': 'https://schema.org/PlaceOfWorship',
	/** A placebo-controlled trial design. */
	'PlaceboControlledTrial': 'https://schema.org/PlaceboControlledTrial',
	/** The act of planning the execution of an event/task/action/reservation/plan to a future date. */
	'PlanAction': 'https://schema.org/PlanAction',
	/** A specific branch of medical science that pertains to therapeutic or cosmetic repair or re-formation of missing, injured or malformed tissues or body parts by manual and instrumental means. */
	'PlasticSurgery': 'https://schema.org/PlasticSurgery',
	/** A play is a form of literature, usually consisting of dialogue between characters, intended for theatrical performance rather than just reading. Note: A performance of a Play would be a [[TheaterEvent]] or [[BroadcastEvent]] - the *Play* being the [[workPerformed]]. */
	'Play': 'https://schema.org/Play',
	/** The act of playing/exercising/training/performing for enjoyment, leisure, recreation, competition or exercise.\n\nRelated actions:\n\n* [[ListenAction]]: Unlike ListenAction (which is under ConsumeAction), PlayAction refers to performing for an audience or at an event, rather than consuming music.\n* [[WatchAction]]: Unlike WatchAction (which is under ConsumeAction), PlayAction refers to showing/displaying for an audience or at an event, rather than consuming visual content. */
	'PlayAction': 'https://schema.org/PlayAction',
	/** The act of playing a video game. */
	'PlayGameAction': 'https://schema.org/PlayGameAction',
	/** A playground. */
	'Playground': 'https://schema.org/Playground',
	/** A plumbing service. */
	'Plumber': 'https://schema.org/Plumber',
	/** A single episode of a podcast series. */
	'PodcastEpisode': 'https://schema.org/PodcastEpisode',
	/** A single season of a podcast. Many podcasts do not break down into separate seasons. In that case, PodcastSeries should be used. */
	'PodcastSeason': 'https://schema.org/PodcastSeason',
	/** A podcast is an episodic series of digital audio or video files which a user can download and listen to. */
	'PodcastSeries': 'https://schema.org/PodcastSeries',
	/** Podiatry is the care of the human foot, especially the diagnosis and treatment of foot disorders. */
	'Podiatric': 'https://schema.org/Podiatric',
	/** A police station. */
	'PoliceStation': 'https://schema.org/PoliceStation',
	/** Organization: Political Party. */
	'PoliticalParty': 'https://schema.org/PoliticalParty',
	/** A pond. */
	'Pond': 'https://schema.org/Pond',
	/** A post office. */
	'PostOffice': 'https://schema.org/PostOffice',
	/** The mailing address. */
	'PostalAddress': 'https://schema.org/PostalAddress',
	/** Indicates a range of postal codes, usually defined as the set of valid codes between [[postalCodeBegin]] and [[postalCodeEnd]], inclusively. */
	'PostalCodeRangeSpecification': 'https://schema.org/PostalCodeRangeSpecification',
	/** A large, usually printed placard, bill, or announcement, often illustrated, that is posted to advertise or publicize something. */
	'Poster': 'https://schema.org/Poster',
	/** A description of an action that is supported. */
	'PotentialActionStatus': 'https://schema.org/PotentialActionStatus',
	/** Indicates that the item is available for pre-order. */
	'PreOrder': 'https://schema.org/PreOrder',
	/** An agent orders a (not yet released) object/product/service to be delivered/sent. */
	'PreOrderAction': 'https://schema.org/PreOrderAction',
	/** Indicates that the item is available for ordering and delivery before general availability. */
	'PreSale': 'https://schema.org/PreSale',
	/** Content discussing pregnancy-related aspects of a health topic. */
	'PregnancyHealthAspect': 'https://schema.org/PregnancyHealthAspect',
	/** The act of inserting at the beginning if an ordered collection. */
	'PrependAction': 'https://schema.org/PrependAction',
	/** A preschool. */
	'Preschool': 'https://schema.org/Preschool',
	/** Available by prescription only. */
	'PrescriptionOnly': 'https://schema.org/PrescriptionOnly',
	/** A file containing slides or used for a presentation. */
	'PresentationDigitalDocument': 'https://schema.org/PresentationDigitalDocument',
	/** Information about actions or measures that can be taken to avoid getting the topic or reaching a critical situation related to the topic. */
	'PreventionHealthAspect': 'https://schema.org/PreventionHealthAspect',
	/** An indication for preventing an underlying condition, symptom, etc. */
	'PreventionIndication': 'https://schema.org/PreventionIndication',
	/** Enumerates different price components that together make up the total price for an offered product. */
	'PriceComponentTypeEnumeration': 'https://schema.org/PriceComponentTypeEnumeration',
	/** A structured value representing a price or price range. Typically, only the subclasses of this type are used for markup. It is recommended to use [[MonetaryAmount]] to describe independent amounts of money such as a salary, credit card limits, etc. */
	'PriceSpecification': 'https://schema.org/PriceSpecification',
	/** Enumerates different price types, for example list price, invoice price, and sale price. */
	'PriceTypeEnumeration': 'https://schema.org/PriceTypeEnumeration',
	/** The medical care by a physician, or other health-care professional, who is the patient's first contact with the health-care system and who may recommend a specialist if necessary. */
	'PrimaryCare': 'https://schema.org/PrimaryCare',
	/** A prion is an infectious agent composed of protein in a misfolded form. */
	'Prion': 'https://schema.org/Prion',
	/** Any offered product or service. For example: a pair of shoes; a concert ticket; the rental of a car; a haircut; or an episode of a TV show streamed online. */
	'Product': 'https://schema.org/Product',
	/** A set of products (either [[ProductGroup]]s or specific variants) that are listed together e.g. in an [[Offer]]. */
	'ProductCollection': 'https://schema.org/ProductCollection',
	/** A ProductGroup represents a group of [[Product]]s that vary only in certain well-described ways, such as by [[size]], [[color]], [[material]] etc.

While a ProductGroup itself is not directly offered for sale, the various varying products that it represents can be. The ProductGroup serves as a prototype or template, standing in for all of the products who have an [[isVariantOf]] relationship to it. As such, properties (including additional types) can be applied to the ProductGroup to represent characteristics shared by each of the (possibly very many) variants. Properties that reference a ProductGroup are not included in this mechanism; neither are the following specific properties [[variesBy]], [[hasVariant]], [[url]].  */
	'ProductGroup': 'https://schema.org/ProductGroup',
	/** A datasheet or vendor specification of a product (in the sense of a prototypical description). */
	'ProductModel': 'https://schema.org/ProductModel',
	/** Original definition: "provider of professional services."\n\nThe general [[ProfessionalService]] type for local businesses was deprecated due to confusion with [[Service]]. For reference, the types that it included were: [[Dentist]],
        [[AccountingService]], [[Attorney]], [[Notary]], as well as types for several kinds of [[HomeAndConstructionBusiness]]: [[Electrician]], [[GeneralContractor]],
        [[HousePainter]], [[Locksmith]], [[Plumber]], [[RoofingContractor]]. [[LegalService]] was introduced as a more inclusive supertype of [[Attorney]]. */
	'ProfessionalService': 'https://schema.org/ProfessionalService',
	/** Web page type: Profile page. */
	'ProfilePage': 'https://schema.org/ProfilePage',
	/** Typical progression and happenings of life course of the topic. */
	'PrognosisHealthAspect': 'https://schema.org/PrognosisHealthAspect',
	/** Used to describe membership in a loyalty programs (e.g. "StarAliance"), traveler clubs (e.g. "AAA"), purchase clubs ("Safeway Club"), etc. */
	'ProgramMembership': 'https://schema.org/ProgramMembership',
	/** An enterprise (potentially individual but typically collaborative), planned to achieve a particular aim.
Use properties from [[Organization]], [[subOrganization]]/[[parentOrganization]] to indicate project sub-structures. 
    */
	'Project': 'https://schema.org/Project',
	/** Data type: PronounceableText. */
	'PronounceableText': 'https://schema.org/PronounceableText',
	/** A property, used to indicate attributes and relationships of some Thing; equivalent to rdf:Property. */
	'Property': 'https://schema.org/Property',
	/** A property-value pair, e.g. representing a feature of a product or place. Use the 'name' property for the name of the property. If there is an additional human-readable version of the value, put that into the 'description' property.\n\n Always use specific schema.org properties when a) they exist and b) you can populate them. Using PropertyValue as a substitute will typically not trigger the same effect as using the original, specific property.
     */
	'PropertyValue': 'https://schema.org/PropertyValue',
	/** A Property value specification. */
	'PropertyValueSpecification': 'https://schema.org/PropertyValueSpecification',
	/** Protein is here used in its widest possible definition, as classes of amino acid based molecules. Amyloid-beta Protein in human (UniProt P05067), eukaryota (e.g. an OrthoDB group) or even a single molecule that one can point to are all of type :Protein. A protein can thus be a subclass of another protein, e.g. :Protein as a UniProt record can have multiple isoforms inside it which would also be :Protein. They can be imagined, synthetic, hypothetical or naturally occurring. */
	'Protein': 'https://schema.org/Protein',
	/** Single-celled organism that causes an infection. */
	'Protozoa': 'https://schema.org/Protozoa',
	/** A specific branch of medical science that is concerned with the study, treatment, and prevention of mental illness, using both medical and psychological therapies. */
	'Psychiatric': 'https://schema.org/Psychiatric',
	/** A process of care relying upon counseling, dialogue and communication  aimed at improving a mental health condition without use of drugs. */
	'PsychologicalTreatment': 'https://schema.org/PsychologicalTreatment',
	/** Branch of medicine that pertains to the health services to improve and protect community health, especially epidemiology, sanitation, immunization, and preventive medicine. */
	'PublicHealth': 'https://schema.org/PublicHealth',
	/** This stands for any day that is a public holiday; it is a placeholder for all official public holidays in some particular location. While not technically a "day of the week", it can be used with [[OpeningHoursSpecification]]. In the context of an opening hours specification it can be used to indicate opening hours on public holidays, overriding general opening hours for the day of the week on which a public holiday occurs. */
	'PublicHolidays': 'https://schema.org/PublicHolidays',
	/** A public swimming pool. */
	'PublicSwimmingPool': 'https://schema.org/PublicSwimmingPool',
	/** A public toilet is a room or small building containing one or more toilets (and possibly also urinals) which is available for use by the general public, or by customers or employees of certain businesses. */
	'PublicToilet': 'https://schema.org/PublicToilet',
	/** A PublicationEvent corresponds indifferently to the event of publication for a CreativeWork of any type, e.g. a broadcast event, an on-demand event, a book/journal publication via a variety of delivery media. */
	'PublicationEvent': 'https://schema.org/PublicationEvent',
	/** A part of a successively published publication such as a periodical or publication volume, often numbered, usually containing a grouping of works such as articles.\n\nSee also [blog post](http://blog.schema.org/2014/09/schemaorg-support-for-bibliographic_2.html). */
	'PublicationIssue': 'https://schema.org/PublicationIssue',
	/** A part of a successively published publication such as a periodical or multi-volume work, often numbered. It may represent a time span, such as a year.\n\nSee also [blog post](http://blog.schema.org/2014/09/schemaorg-support-for-bibliographic_2.html). */
	'PublicationVolume': 'https://schema.org/PublicationVolume',
	/** A specific branch of medical science that pertains to the study of the respiratory system and its respective disease states. */
	'Pulmonary': 'https://schema.org/Pulmonary',
	/** A QAPage is a WebPage focussed on a specific Question and its Answer(s), e.g. in a question answering site or documenting Frequently Asked Questions (FAQs). */
	'QAPage': 'https://schema.org/QAPage',
	/** A predefined value for a product characteristic, e.g. the power cord plug type 'US' or the garment sizes 'S', 'M', 'L', and 'XL'. */
	'QualitativeValue': 'https://schema.org/QualitativeValue',
	/**  A point value or interval for product characteristics and other purposes. */
	'QuantitativeValue': 'https://schema.org/QuantitativeValue',
	/** A statistical distribution of values. */
	'QuantitativeValueDistribution': 'https://schema.org/QuantitativeValueDistribution',
	/** Quantities such as distance, time, mass, weight, etc. Particular instances of say Mass are entities like '3 kg' or '4 milligrams'. */
	'Quantity': 'https://schema.org/Quantity',
	/** A specific question - e.g. from a user seeking answers online, or collected in a Frequently Asked Questions (FAQ) document. */
	'Question': 'https://schema.org/Question',
	/** Quiz: A test of knowledge, skills and abilities. */
	'Quiz': 'https://schema.org/Quiz',
	/** A quotation. Often but not necessarily from some written work, attributable to a real world author and - if associated with a fictional character - to any fictional Person. Use [[isBasedOn]] to link to source/origin. The [[recordedIn]] property can be used to reference a Quotation from an [[Event]]. */
	'Quotation': 'https://schema.org/Quotation',
	/** An agent quotes/estimates/appraises an object/product/service with a price at a location/store. */
	'QuoteAction': 'https://schema.org/QuoteAction',
	/** A place offering space for "Recreational Vehicles", Caravans, mobile homes and the like. */
	'RVPark': 'https://schema.org/RVPark',
	/** A process of care using radiation aimed at improving a health condition. */
	'RadiationTherapy': 'https://schema.org/RadiationTherapy',
	/** A delivery service through which radio content is provided via broadcast over the air or online. */
	'RadioBroadcastService': 'https://schema.org/RadioBroadcastService',
	/** A unique instance of a radio BroadcastService on a CableOrSatelliteService lineup. */
	'RadioChannel': 'https://schema.org/RadioChannel',
	/** A short radio program or a segment/part of a radio program. */
	'RadioClip': 'https://schema.org/RadioClip',
	/** A radio episode which can be part of a series or season. */
	'RadioEpisode': 'https://schema.org/RadioEpisode',
	/** Season dedicated to radio broadcast and associated online delivery. */
	'RadioSeason': 'https://schema.org/RadioSeason',
	/** CreativeWorkSeries dedicated to radio broadcast and associated online delivery. */
	'RadioSeries': 'https://schema.org/RadioSeries',
	/** A radio station. */
	'RadioStation': 'https://schema.org/RadioStation',
	/** Radiography is an imaging technique that uses electromagnetic radiation other than visible light, especially X-rays, to view the internal structure of a non-uniformly composed and opaque object such as the human body. */
	'Radiography': 'https://schema.org/Radiography',
	/** A randomized trial design. */
	'RandomizedTrial': 'https://schema.org/RandomizedTrial',
	/** A rating is an evaluation on a numeric scale, such as 1 to 5 stars. */
	'Rating': 'https://schema.org/Rating',
	/** The act of responding instinctively and emotionally to an object, expressing a sentiment. */
	'ReactAction': 'https://schema.org/ReactAction',
	/** The act of consuming written content. */
	'ReadAction': 'https://schema.org/ReadAction',
	/** Permission to read or view the document. */
	'ReadPermission': 'https://schema.org/ReadPermission',
	/** A real-estate agent. */
	'RealEstateAgent': 'https://schema.org/RealEstateAgent',
	/** A [[RealEstateListing]] is a listing that describes one or more real-estate [[Offer]]s (whose [[businessFunction]] is typically to lease out, or to sell).
  The [[RealEstateListing]] type itself represents the overall listing, as manifested in some [[WebPage]].
   */
	'RealEstateListing': 'https://schema.org/RealEstateListing',
	/** Real-wheel drive is a transmission layout where the engine drives the rear wheels. */
	'RearWheelDriveConfiguration': 'https://schema.org/RearWheelDriveConfiguration',
	/** The act of physically/electronically taking delivery of an object that has been transferred from an origin to a destination. Reciprocal of SendAction.\n\nRelated actions:\n\n* [[SendAction]]: The reciprocal of ReceiveAction.\n* [[TakeAction]]: Unlike TakeAction, ReceiveAction does not imply that the ownership has been transferred (e.g. I can receive a package, but it does not mean the package is now mine). */
	'ReceiveAction': 'https://schema.org/ReceiveAction',
	/** A recipe. For dietary restrictions covered by the recipe, a few common restrictions are enumerated via [[suitableForDiet]]. The [[keywords]] property can also be used to add more detail. */
	'Recipe': 'https://schema.org/Recipe',
	/** [[Recommendation]] is a type of [[Review]] that suggests or proposes something as the best option or best course of action. Recommendations may be for products or services, or other concrete things, as in the case of a ranked list or product guide. A [[Guide]] may list multiple recommendations for different categories. For example, in a [[Guide]] about which TVs to buy, the author may have several [[Recommendation]]s. */
	'Recommendation': 'https://schema.org/Recommendation',
	/** A recommended dosing schedule for a drug or supplement as prescribed or recommended by an authority or by the drug/supplement's manufacturer. Capture the recommending authority in the recognizingAuthority property of MedicalEntity. */
	'RecommendedDoseSchedule': 'https://schema.org/RecommendedDoseSchedule',
	/** Recruiting participants. */
	'Recruiting': 'https://schema.org/Recruiting',
	/** A recycling center. */
	'RecyclingCenter': 'https://schema.org/RecyclingCenter',
	/** A general code for cases where relevance to children is reduced, e.g. adult education, mortgages, retirement-related products, etc. */
	'ReducedRelevanceForChildrenConsideration': 'https://schema.org/ReducedRelevanceForChildrenConsideration',
	/** Enumerates several kinds of product return refund types. */
	'RefundTypeEnumeration': 'https://schema.org/RefundTypeEnumeration',
	/** Indicates that the item is refurbished. */
	'RefurbishedCondition': 'https://schema.org/RefurbishedCondition',
	/** The act of registering to be a user of a service, product or web page.\n\nRelated actions:\n\n* [[JoinAction]]: Unlike JoinAction, RegisterAction implies you are registering to be a user of a service, *not* a group/team of people.\n* [[FollowAction]]: Unlike FollowAction, RegisterAction doesn't imply that the agent is expecting to poll for updates from the object.\n* [[SubscribeAction]]: Unlike SubscribeAction, RegisterAction doesn't imply that the agent is expecting updates from the object. */
	'RegisterAction': 'https://schema.org/RegisterAction',
	/** A registry-based study design. */
	'Registry': 'https://schema.org/Registry',
	/** The drug's cost represents the maximum reimbursement paid by an insurer for the drug. */
	'ReimbursementCap': 'https://schema.org/ReimbursementCap',
	/** The act of rejecting to/adopting an object.\n\nRelated actions:\n\n* [[AcceptAction]]: The antonym of RejectAction. */
	'RejectAction': 'https://schema.org/RejectAction',
	/** Other prominent or relevant topics tied to the main topic. */
	'RelatedTopicsHealthAspect': 'https://schema.org/RelatedTopicsHealthAspect',
	/** RemixAlbum. */
	'RemixAlbum': 'https://schema.org/RemixAlbum',
	/** A specific branch of medical science that pertains to the study of the kidneys and its respective disease states. */
	'Renal': 'https://schema.org/Renal',
	/** The act of giving money in return for temporary use, but not ownership, of an object such as a vehicle or property. For example, an agent rents a property from a landlord in exchange for a periodic payment. */
	'RentAction': 'https://schema.org/RentAction',
	/** A reservation for a rental car.\n\nNote: This type is for information about actual reservations, e.g. in confirmation emails or HTML pages with individual confirmations of reservations. */
	'RentalCarReservation': 'https://schema.org/RentalCarReservation',
	/** Indicates the usage of the vehicle as a rental car. */
	'RentalVehicleUsage': 'https://schema.org/RentalVehicleUsage',
	/** A structured value representing repayment. */
	'RepaymentSpecification': 'https://schema.org/RepaymentSpecification',
	/** The act of editing a recipient by replacing an old object with a new object. */
	'ReplaceAction': 'https://schema.org/ReplaceAction',
	/** The act of responding to a question/message asked/sent by the object. Related to [[AskAction]].\n\nRelated actions:\n\n* [[AskAction]]: Appears generally as an origin of a ReplyAction. */
	'ReplyAction': 'https://schema.org/ReplyAction',
	/** A Report generated by governmental or non-governmental organization. */
	'Report': 'https://schema.org/Report',
	/** The [[ReportageNewsArticle]] type is a subtype of [[NewsArticle]] representing
 news articles which are the result of journalistic news reporting conventions.

In practice many news publishers produce a wide variety of article types, many of which might be considered a [[NewsArticle]] but not a [[ReportageNewsArticle]]. For example, opinion pieces, reviews, analysis, sponsored or satirical articles, or articles that combine several of these elements.

The [[ReportageNewsArticle]] type is based on a stricter ideal for "news" as a work of journalism, with articles based on factual information either observed or verified by the author, or reported and verified from knowledgeable sources.  This often includes perspectives from multiple viewpoints on a particular issue (distinguishing news reports from public relations or propaganda).  News reports in the [[ReportageNewsArticle]] sense de-emphasize the opinion of the author, with commentary and value judgements typically expressed elsewhere.

A [[ReportageNewsArticle]] which goes deeper into analysis can also be marked with an additional type of [[AnalysisNewsArticle]].
 */
	'ReportageNewsArticle': 'https://schema.org/ReportageNewsArticle',
	/** A patient-reported or observed dosing schedule for a drug or supplement. */
	'ReportedDoseSchedule': 'https://schema.org/ReportedDoseSchedule',
	/** A Research Organization (e.g. scientific institute, research company). */
	'ResearchOrganization': 'https://schema.org/ResearchOrganization',
	/** A Research project. */
	'ResearchProject': 'https://schema.org/ResearchProject',
	/** Researchers. */
	'Researcher': 'https://schema.org/Researcher',
	/** Describes a reservation for travel, dining or an event. Some reservations require tickets. \n\nNote: This type is for information about actual reservations, e.g. in confirmation emails or HTML pages with individual confirmations of reservations. For offers of tickets, restaurant reservations, flights, or rental cars, use [[Offer]]. */
	'Reservation': 'https://schema.org/Reservation',
	/** The status for a previously confirmed reservation that is now cancelled. */
	'ReservationCancelled': 'https://schema.org/ReservationCancelled',
	/** The status of a confirmed reservation. */
	'ReservationConfirmed': 'https://schema.org/ReservationConfirmed',
	/** The status of a reservation on hold pending an update like credit card number or flight changes. */
	'ReservationHold': 'https://schema.org/ReservationHold',
	/** A group of multiple reservations with common values for all sub-reservations. */
	'ReservationPackage': 'https://schema.org/ReservationPackage',
	/** The status of a reservation when a request has been sent, but not confirmed. */
	'ReservationPending': 'https://schema.org/ReservationPending',
	/** Enumerated status values for Reservation. */
	'ReservationStatusType': 'https://schema.org/ReservationStatusType',
	/** Reserving a concrete object.\n\nRelated actions:\n\n* [[ScheduleAction]]: Unlike ScheduleAction, ReserveAction reserves concrete objects (e.g. a table, a hotel) towards a time slot / spatial allocation. */
	'ReserveAction': 'https://schema.org/ReserveAction',
	/** A reservoir of water, typically an artificially created lake, like the Lake Kariba reservoir. */
	'Reservoir': 'https://schema.org/Reservoir',
	/** The place where a person lives. */
	'Residence': 'https://schema.org/Residence',
	/** A resort is a place used for relaxation or recreation, attracting visitors for holidays or vacations. Resorts are places, towns or sometimes commercial establishments operated by a single company (source: Wikipedia, the free encyclopedia, see <a href="http://en.wikipedia.org/wiki/Resort">http://en.wikipedia.org/wiki/Resort</a>).
<br /><br />
See also the <a href="/docs/hotels.html">dedicated document on the use of schema.org for marking up hotels and other forms of accommodations</a>.
     */
	'Resort': 'https://schema.org/Resort',
	/** The therapy that is concerned with the maintenance or improvement of respiratory function (as in patients with pulmonary disease). */
	'RespiratoryTherapy': 'https://schema.org/RespiratoryTherapy',
	/** A restaurant. */
	'Restaurant': 'https://schema.org/Restaurant',
	/** Specifies that the customer must pay a restocking fee when returning a product. */
	'RestockingFees': 'https://schema.org/RestockingFees',
	/** A diet restricted to certain foods or preparations for cultural, religious, health or lifestyle reasons.  */
	'RestrictedDiet': 'https://schema.org/RestrictedDiet',
	/** Results are available. */
	'ResultsAvailable': 'https://schema.org/ResultsAvailable',
	/** Results are not available. */
	'ResultsNotAvailable': 'https://schema.org/ResultsNotAvailable',
	/** The act of resuming a device or application which was formerly paused (e.g. resume music playback or resume a timer). */
	'ResumeAction': 'https://schema.org/ResumeAction',
	/** The drug's cost represents the retail cost of the drug. */
	'Retail': 'https://schema.org/Retail',
	/** The act of returning to the origin that which was previously received (concrete objects) or taken (ownership). */
	'ReturnAction': 'https://schema.org/ReturnAction',
	/** Specifies that product returns must be made at a kiosk. */
	'ReturnAtKiosk': 'https://schema.org/ReturnAtKiosk',
	/** Specifies that product returns must be done by mail. */
	'ReturnByMail': 'https://schema.org/ReturnByMail',
	/** Specifies that product returns must be paid for, and are the responsibility of, the customer. */
	'ReturnFeesCustomerResponsibility': 'https://schema.org/ReturnFeesCustomerResponsibility',
	/** Enumerates several kinds of policies for product return fees. */
	'ReturnFeesEnumeration': 'https://schema.org/ReturnFeesEnumeration',
	/** Specifies that product returns must be made in a store. */
	'ReturnInStore': 'https://schema.org/ReturnInStore',
	/** Indicated that creating a return label is the responsibility of the customer. */
	'ReturnLabelCustomerResponsibility': 'https://schema.org/ReturnLabelCustomerResponsibility',
	/** Indicated that a return label must be downloaded and printed by the customer. */
	'ReturnLabelDownloadAndPrint': 'https://schema.org/ReturnLabelDownloadAndPrint',
	/** Specifies that a return label will be provided by the seller in the shipping box. */
	'ReturnLabelInBox': 'https://schema.org/ReturnLabelInBox',
	/** Enumerates several types of return labels for product returns. */
	'ReturnLabelSourceEnumeration': 'https://schema.org/ReturnLabelSourceEnumeration',
	/** Enumerates several types of product return methods. */
	'ReturnMethodEnumeration': 'https://schema.org/ReturnMethodEnumeration',
	/** Specifies that the customer must pay the return shipping costs when returning a product. */
	'ReturnShippingFees': 'https://schema.org/ReturnShippingFees',
	/** A review of an item - for example, of a restaurant, movie, or store. */
	'Review': 'https://schema.org/Review',
	/** The act of producing a balanced opinion about the object for an audience. An agent reviews an object with participants resulting in a review. */
	'ReviewAction': 'https://schema.org/ReviewAction',
	/** A [[NewsArticle]] and [[CriticReview]] providing a professional critic's assessment of a service, product, performance, or artistic or literary work. */
	'ReviewNewsArticle': 'https://schema.org/ReviewNewsArticle',
	/** A specific branch of medical science that deals with the study and treatment of rheumatic, autoimmune or joint diseases. */
	'Rheumatologic': 'https://schema.org/Rheumatologic',
	/** The steering position is on the right side of the vehicle (viewed from the main direction of driving). */
	'RightHandDriving': 'https://schema.org/RightHandDriving',
	/** Information about the risk factors and possible complications that may follow a topic. */
	'RisksOrComplicationsHealthAspect': 'https://schema.org/RisksOrComplicationsHealthAspect',
	/** A river (for example, the broad majestic Shannon). */
	'RiverBodyOfWater': 'https://schema.org/RiverBodyOfWater',
	/** Represents additional information about a relationship or property. For example a Role can be used to say that a 'member' role linking some SportsTeam to a player occurred during a particular time period. Or that a Person's 'actor' role in a Movie was for some particular characterName. Such properties can be attached to a Role entity, which is then associated with the main entities using ordinary properties like 'member' or 'actor'.\n\nSee also [blog post](http://blog.schema.org/2014/06/introducing-role.html). */
	'Role': 'https://schema.org/Role',
	/** A roofing contractor. */
	'RoofingContractor': 'https://schema.org/RoofingContractor',
	/** A room is a distinguishable space within a structure, usually separated from other spaces by interior walls (source: Wikipedia, the free encyclopedia, see <a href="http://en.wikipedia.org/wiki/Room">http://en.wikipedia.org/wiki/Room</a>).
<br /><br />
See also the <a href="/docs/hotels.html">dedicated document on the use of schema.org for marking up hotels and other forms of accommodations</a>.
 */
	'Room': 'https://schema.org/Room',
	/** The act of notifying an event organizer as to whether you expect to attend the event. */
	'RsvpAction': 'https://schema.org/RsvpAction',
	/** The invitee may or may not attend. */
	'RsvpResponseMaybe': 'https://schema.org/RsvpResponseMaybe',
	/** The invitee will not attend. */
	'RsvpResponseNo': 'https://schema.org/RsvpResponseNo',
	/** RsvpResponseType is an enumeration type whose instances represent responding to an RSVP request. */
	'RsvpResponseType': 'https://schema.org/RsvpResponseType',
	/** The invitee will attend. */
	'RsvpResponseYes': 'https://schema.org/RsvpResponseYes',
	/** Represents the suggested retail price ("SRP") of an offered product. */
	'SRP': 'https://schema.org/SRP',
	/** Content about the safety-related aspects of a health topic. */
	'SafetyHealthAspect': 'https://schema.org/SafetyHealthAspect',
	/** Event type: Sales event. */
	'SaleEvent': 'https://schema.org/SaleEvent',
	/** Represents a sale price (usually active for a limited period) of an offered product. */
	'SalePrice': 'https://schema.org/SalePrice',
	/** Content coded 'satire or parody content' in a [[MediaReview]], considered in the context of how it was published or shared.

For a [[VideoObject]] to be 'satire or parody content': A video that was created as political or humorous commentary and is presented in that context. (Reshares of satire/parody content that do not include relevant context are more likely to fall under the “missing context” rating.)

For an [[ImageObject]] to be 'satire or parody content': An image that was created as political or humorous commentary and is presented in that context. (Reshares of satire/parody content that do not include relevant context are more likely to fall under the “missing context” rating.)

For an [[ImageObject]] with embedded text to be 'satire or parody content': An image that was created as political or humorous commentary and is presented in that context. (Reshares of satire/parody content that do not include relevant context are more likely to fall under the “missing context” rating.)

For an [[AudioObject]] to be 'satire or parody content': Audio that was created as political or humorous commentary and is presented in that context. (Reshares of satire/parody content that do not include relevant context are more likely to fall under the “missing context” rating.)
 */
	'SatireOrParodyContent': 'https://schema.org/SatireOrParodyContent',
	/** An [[Article]] whose content is primarily [[satirical]](https://en.wikipedia.org/wiki/Satire) in nature, i.e. unlikely to be literally true. A satirical article is sometimes but not necessarily also a [[NewsArticle]]. [[ScholarlyArticle]]s are also sometimes satirized. */
	'SatiricalArticle': 'https://schema.org/SatiricalArticle',
	/** The day of the week between Friday and Sunday. */
	'Saturday': 'https://schema.org/Saturday',
	/** A schedule defines a repeating time period used to describe a regularly occurring [[Event]]. At a minimum a schedule will specify [[repeatFrequency]] which describes the interval between occurrences of the event. Additional information can be provided to specify the schedule more precisely.
      This includes identifying the day(s) of the week or month when the recurring event will take place, in addition to its start and end time. Schedules may also
      have start and end dates to indicate when they are active, e.g. to define a limited calendar of events. */
	'Schedule': 'https://schema.org/Schedule',
	/** Scheduling future actions, events, or tasks.\n\nRelated actions:\n\n* [[ReserveAction]]: Unlike ReserveAction, ScheduleAction allocates future actions (e.g. an event, a task, etc) towards a time slot / spatial allocation. */
	'ScheduleAction': 'https://schema.org/ScheduleAction',
	/** A scholarly article. */
	'ScholarlyArticle': 'https://schema.org/ScholarlyArticle',
	/** A school. */
	'School': 'https://schema.org/School',
	/** A School District is an administrative area for the administration of schools. */
	'SchoolDistrict': 'https://schema.org/SchoolDistrict',
	/** A screening of a movie or other video. */
	'ScreeningEvent': 'https://schema.org/ScreeningEvent',
	/** Content about how to screen or further filter a topic. */
	'ScreeningHealthAspect': 'https://schema.org/ScreeningHealthAspect',
	/** A piece of sculpture. */
	'Sculpture': 'https://schema.org/Sculpture',
	/** A sea (for example, the Caspian sea). */
	'SeaBodyOfWater': 'https://schema.org/SeaBodyOfWater',
	/** The act of searching for an object.\n\nRelated actions:\n\n* [[FindAction]]: SearchAction generally leads to a FindAction, but not necessarily. */
	'SearchAction': 'https://schema.org/SearchAction',
	/** A Search and Rescue organization of some kind. */
	'SearchRescueOrganization': 'https://schema.org/SearchRescueOrganization',
	/** Web page type: Search results page. */
	'SearchResultsPage': 'https://schema.org/SearchResultsPage',
	/** A media season, e.g. TV, radio, video game etc. */
	'Season': 'https://schema.org/Season',
	/** Used to describe a seat, such as a reserved seat in an event reservation. */
	'Seat': 'https://schema.org/Seat',
	/** A seating map. */
	'SeatingMap': 'https://schema.org/SeatingMap',
	/** Information about questions that may be asked, when to see a professional, measures before seeing a doctor or content about the first consultation. */
	'SeeDoctorHealthAspect': 'https://schema.org/SeeDoctorHealthAspect',
	/** This is the [[Action]] of navigating to a specific [[startOffset]] timestamp within a [[VideoObject]], typically represented with a URL template structure. */
	'SeekToAction': 'https://schema.org/SeekToAction',
	/** Self care actions or measures that can be taken to sooth, health or avoid a topic. This may be carried at home and can be carried/managed by the person itself. */
	'SelfCareHealthAspect': 'https://schema.org/SelfCareHealthAspect',
	/** A self-storage facility. */
	'SelfStorage': 'https://schema.org/SelfStorage',
	/** The act of taking money from a buyer in exchange for goods or services rendered. An agent sells an object, product, or service to a buyer for a price. Reciprocal of BuyAction. */
	'SellAction': 'https://schema.org/SellAction',
	/** The act of physically/electronically dispatching an object for transfer from an origin to a destination. Related actions:\n\n* [[ReceiveAction]]: The reciprocal of SendAction.\n* [[GiveAction]]: Unlike GiveAction, SendAction does not imply the transfer of ownership (e.g. I can send you my laptop, but I'm not necessarily giving it to you). */
	'SendAction': 'https://schema.org/SendAction',
	/** A Series in schema.org is a group of related items, typically but not necessarily of the same kind. See also [[CreativeWorkSeries]], [[EventSeries]]. */
	'Series': 'https://schema.org/Series',
	/** A service provided by an organization, e.g. delivery service, print services, etc. */
	'Service': 'https://schema.org/Service',
	/** A means for accessing a service, e.g. a government office location, web site, or phone number. */
	'ServiceChannel': 'https://schema.org/ServiceChannel',
	/** The item contains sexually oriented content such as nudity, suggestive or explicit material, or related online services, or is intended to enhance sexual activity. Examples: Erotic videos or magazine, sexual enhancement devices, sex toys. */
	'SexualContentConsideration': 'https://schema.org/SexualContentConsideration',
	/** The act of distributing content to people for their amusement or edification. */
	'ShareAction': 'https://schema.org/ShareAction',
	/** Printed music, as opposed to performed or recorded music. */
	'SheetMusic': 'https://schema.org/SheetMusic',
	/** ShippingDeliveryTime provides various pieces of information about delivery times for shipping. */
	'ShippingDeliveryTime': 'https://schema.org/ShippingDeliveryTime',
	/** A ShippingRateSettings represents re-usable pieces of shipping information. It is designed for publication on an URL that may be referenced via the [[shippingSettingsLink]] property of an [[OfferShippingDetails]]. Several occurrences can be published, distinguished and matched (i.e. identified/referenced) by their different values for [[shippingLabel]]. */
	'ShippingRateSettings': 'https://schema.org/ShippingRateSettings',
	/** A shoe store. */
	'ShoeStore': 'https://schema.org/ShoeStore',
	/** A shopping center or mall. */
	'ShoppingCenter': 'https://schema.org/ShoppingCenter',
	/** Short story or tale. A brief work of literature, usually written in narrative prose. */
	'ShortStory': 'https://schema.org/ShortStory',
	/** Side effects that can be observed from the usage of the topic. */
	'SideEffectsHealthAspect': 'https://schema.org/SideEffectsHealthAspect',
	/** A trial design in which the researcher knows which treatment the patient was randomly assigned to but the patient does not. */
	'SingleBlindedTrial': 'https://schema.org/SingleBlindedTrial',
	/** A trial that takes place at a single center. */
	'SingleCenterTrial': 'https://schema.org/SingleCenterTrial',
	/** Residence type: Single-family home. */
	'SingleFamilyResidence': 'https://schema.org/SingleFamilyResidence',
	/** Play mode: SinglePlayer. Which is played by a lone player. */
	'SinglePlayer': 'https://schema.org/SinglePlayer',
	/** SingleRelease. */
	'SingleRelease': 'https://schema.org/SingleRelease',
	/** A navigation element of the page. */
	'SiteNavigationElement': 'https://schema.org/SiteNavigationElement',
	/** Enumerates common size groups for various product categories. */
	'SizeGroupEnumeration': 'https://schema.org/SizeGroupEnumeration',
	/** Size related properties of a product, typically a size code ([[name]]) and optionally a [[sizeSystem]], [[sizeGroup]], and product measurements ([[hasMeasurement]]). In addition, the intended audience can be defined through [[suggestedAge]], [[suggestedGender]], and suggested body measurements ([[suggestedMeasurement]]). */
	'SizeSpecification': 'https://schema.org/SizeSpecification',
	/** Enumerates common size systems for different categories of products, for example "EN-13402" or "UK" for wearables or "Imperial" for screws. */
	'SizeSystemEnumeration': 'https://schema.org/SizeSystemEnumeration',
	/** Imperial size system. */
	'SizeSystemImperial': 'https://schema.org/SizeSystemImperial',
	/** Metric size system. */
	'SizeSystemMetric': 'https://schema.org/SizeSystemMetric',
	/** A ski resort. */
	'SkiResort': 'https://schema.org/SkiResort',
	/** Skin assessment with clinical examination. */
	'Skin': 'https://schema.org/Skin',
	/** Event type: Social event. */
	'SocialEvent': 'https://schema.org/SocialEvent',
	/** A post to a social media platform, including blog posts, tweets, Facebook posts, etc. */
	'SocialMediaPosting': 'https://schema.org/SocialMediaPosting',
	/** A software application. */
	'SoftwareApplication': 'https://schema.org/SoftwareApplication',
	/** Computer programming source code. Example: Full (compile ready) solutions, code snippet samples, scripts, templates. */
	'SoftwareSourceCode': 'https://schema.org/SoftwareSourceCode',
	/** Indicates that the item has sold out. */
	'SoldOut': 'https://schema.org/SoldOut',
	/** The action that takes in a math expression and directs users to a page potentially capable of solving/simplifying that expression. */
	'SolveMathAction': 'https://schema.org/SolveMathAction',
	/** A placeholder for multiple similar products of the same kind. */
	'SomeProducts': 'https://schema.org/SomeProducts',
	/** SoundtrackAlbum. */
	'SoundtrackAlbum': 'https://schema.org/SoundtrackAlbum',
	/** A SpeakableSpecification indicates (typically via [[xpath]] or [[cssSelector]]) sections of a document that are highlighted as particularly [[speakable]]. Instances of this type are expected to be used primarily as values of the [[speakable]] property. */
	'SpeakableSpecification': 'https://schema.org/SpeakableSpecification',
	/** A SpecialAnnouncement combines a simple date-stamped textual information update
      with contextualized Web links and other structured data.  It represents an information update made by a
      locally-oriented organization, for example schools, pharmacies, healthcare providers,  community groups, police,
      local government.

For work in progress guidelines on Coronavirus-related markup see [this doc](https://docs.google.com/document/d/14ikaGCKxo50rRM7nvKSlbUpjyIk2WMQd3IkB1lItlrM/edit#).

The motivating scenario for SpecialAnnouncement is the [Coronavirus pandemic](https://en.wikipedia.org/wiki/2019%E2%80%9320_coronavirus_pandemic), and the initial vocabulary is oriented to this urgent situation. Schema.org
expect to improve the markup iteratively as it is deployed and as feedback emerges from use. In addition to our
usual [Github entry](https://github.com/schemaorg/schemaorg/issues/2490), feedback comments can also be provided in [this document](https://docs.google.com/document/d/1fpdFFxk8s87CWwACs53SGkYv3aafSxz_DTtOQxMrBJQ/edit#).


While this schema is designed to communicate urgent crisis-related information, it is not the same as an emergency warning technology like [CAP](https://en.wikipedia.org/wiki/Common_Alerting_Protocol), although there may be overlaps. The intent is to cover
the kinds of everyday practical information being posted to existing websites during an emergency situation.

Several kinds of information can be provided:

We encourage the provision of "name", "text", "datePosted", "expires" (if appropriate), "category" and
"url" as a simple baseline. It is important to provide a value for "category" where possible, most ideally as a well known
URL from Wikipedia or Wikidata. In the case of the 2019-2020 Coronavirus pandemic, this should be "https://en.wikipedia.org/w/index.php?title=2019-20\_coronavirus\_pandemic" or "https://www.wikidata.org/wiki/Q81068910".

For many of the possible properties, values can either be simple links or an inline description, depending on whether a summary is available. For a link, provide just the URL of the appropriate page as the property's value. For an inline description, use a [[WebContent]] type, and provide the url as a property of that, alongside at least a simple "[[text]]" summary of the page. It is
unlikely that a single SpecialAnnouncement will need all of the possible properties simultaneously.

We expect that in many cases the page referenced might contain more specialized structured data, e.g. contact info, [[openingHours]], [[Event]], [[FAQPage]] etc. By linking to those pages from a [[SpecialAnnouncement]] you can help make it clearer that the events are related to the situation (e.g. Coronavirus) indicated by the [[category]] property of the [[SpecialAnnouncement]].

Many [[SpecialAnnouncement]]s will relate to particular regions and to identifiable local organizations. Use [[spatialCoverage]] for the region, and [[announcementLocation]] to indicate specific [[LocalBusiness]]es and [[CivicStructure]]s. If the announcement affects both a particular region and a specific location (for example, a library closure that serves an entire region), use both [[spatialCoverage]] and [[announcementLocation]].

The [[about]] property can be used to indicate entities that are the focus of the announcement. We now recommend using [[about]] only
for representing non-location entities (e.g. a [[Course]] or a [[RadioStation]]). For places, use [[announcementLocation]] and [[spatialCoverage]]. Consumers of this markup should be aware that the initial design encouraged the use of [[about]] for locations too.

The basic content of [[SpecialAnnouncement]] is similar to that of an [RSS](https://en.wikipedia.org/wiki/RSS) or [Atom](https://en.wikipedia.org/wiki/Atom_(Web_standard)) feed. For publishers without such feeds, basic feed-like information can be shared by posting
[[SpecialAnnouncement]] updates in a page, e.g. using JSON-LD. For sites with Atom/RSS functionality, you can point to a feed
with the [[webFeed]] property. This can be a simple URL, or an inline [[DataFeed]] object, with [[encodingFormat]] providing
media type information, e.g. "application/rss+xml" or "application/atom+xml".
 */
	'SpecialAnnouncement': 'https://schema.org/SpecialAnnouncement',
	/** Any branch of a field in which people typically develop specific expertise, usually after significant study, time, and effort. */
	'Specialty': 'https://schema.org/Specialty',
	/** The scientific study and treatment of defects, disorders, and malfunctions of speech and voice, as stuttering, lisping, or lalling, and of language disturbances, as aphasia or delayed language acquisition. */
	'SpeechPathology': 'https://schema.org/SpeechPathology',
	/** SpokenWordAlbum. */
	'SpokenWordAlbum': 'https://schema.org/SpokenWordAlbum',
	/** A sporting goods store. */
	'SportingGoodsStore': 'https://schema.org/SportingGoodsStore',
	/** A sports location, such as a playing field. */
	'SportsActivityLocation': 'https://schema.org/SportsActivityLocation',
	/** A sports club. */
	'SportsClub': 'https://schema.org/SportsClub',
	/** Event type: Sports event. */
	'SportsEvent': 'https://schema.org/SportsEvent',
	/** Represents the collection of all sports organizations, including sports teams, governing bodies, and sports associations. */
	'SportsOrganization': 'https://schema.org/SportsOrganization',
	/** Organization: Sports team. */
	'SportsTeam': 'https://schema.org/SportsTeam',
	/** A spreadsheet file. */
	'SpreadsheetDigitalDocument': 'https://schema.org/SpreadsheetDigitalDocument',
	/** A stadium. */
	'StadiumOrArena': 'https://schema.org/StadiumOrArena',
	/** Content coded 'staged content' in a [[MediaReview]], considered in the context of how it was published or shared.

For a [[VideoObject]] to be 'staged content': A video that has been created using actors or similarly contrived.

For an [[ImageObject]] to be 'staged content': An image that was created using actors or similarly contrived, such as a screenshot of a fake tweet.

For an [[ImageObject]] with embedded text to be 'staged content': An image that was created using actors or similarly contrived, such as a screenshot of a fake tweet.

For an [[AudioObject]] to be 'staged content': Audio that has been created using actors or similarly contrived.
 */
	'StagedContent': 'https://schema.org/StagedContent',
	/** Stages that can be observed from a topic. */
	'StagesHealthAspect': 'https://schema.org/StagesHealthAspect',
	/** A state or province of a country. */
	'State': 'https://schema.org/State',
	/** A statement about something, for example a fun or interesting fact. If known, the main entity this statement is about can be indicated using mainEntity. For more formal claims (e.g. in Fact Checking), consider using [[Claim]] instead. Use the [[text]] property to capture the text of the statement. */
	'Statement': 'https://schema.org/Statement',
	/** A StatisticalPopulation is a set of instances of a certain given type that satisfy some set of constraints. The property [[populationType]] is used to specify the type. Any property that can be used on instances of that type can appear on the statistical population. For example, a [[StatisticalPopulation]] representing all [[Person]]s with a [[homeLocation]] of East Podunk California would be described by applying the appropriate [[homeLocation]] and [[populationType]] properties to a [[StatisticalPopulation]] item that stands for that set of people.
The properties [[numConstraints]] and [[constraintProperty]] are used to specify which of the populations properties are used to specify the population. Note that the sense of "population" used here is the general sense of a statistical
population, and does not imply that the population consists of people. For example, a [[populationType]] of [[Event]] or [[NewsArticle]] could be used. See also [[Observation]], where a [[populationType]] such as [[Person]] or [[Event]] can be indicated directly. In most cases it may be better to use [[StatisticalVariable]] instead of [[StatisticalPopulation]]. */
	'StatisticalPopulation': 'https://schema.org/StatisticalPopulation',
	/** [[StatisticalVariable]] represents any type of statistical metric that can be measured at a place and time. The usage pattern for [[StatisticalVariable]] is typically expressed using [[Observation]] with an explicit [[populationType]], which is a type, typically drawn from Schema.org. Each [[StatisticalVariable]] is marked as a [[ConstraintNode]], meaning that some properties (those listed using [[constraintProperty]]) serve in this setting solely to define the statistical variable rather than literally describe a specific person, place or thing. For example, a [[StatisticalVariable]] Median_Height_Person_Female representing the median height of women, could be written as follows: the population type is [[Person]]; the measuredProperty [[height]]; the [[statType]] [[median]]; the [[gender]] [[Female]]. It is important to note that there are many kinds of scientific quantitative observation which are not fully, perfectly or unambiguously described following this pattern, or with solely Schema.org terminology. The approach taken here is designed to allow partial, incremental or minimal description of [[StatisticalVariable]]s, and the use of detailed sets of entity and property IDs from external repositories. The [[measurementMethod]], [[unitCode]] and [[unitText]] properties can also be used to clarify the specific nature and notation of an observed measurement.  */
	'StatisticalVariable': 'https://schema.org/StatisticalVariable',
	/** Lists or enumerations dealing with status types. */
	'StatusEnumeration': 'https://schema.org/StatusEnumeration',
	/** A value indicating a steering position. */
	'SteeringPositionValue': 'https://schema.org/SteeringPositionValue',
	/** A retail good store. */
	'Store': 'https://schema.org/Store',
	/** Specifies that the customer receives a store credit as refund when returning a product. */
	'StoreCreditRefund': 'https://schema.org/StoreCreditRefund',
	/** Physical activity that is engaged in to improve muscle and bone strength. Also referred to as resistance training. */
	'StrengthTraining': 'https://schema.org/StrengthTraining',
	/** Structured values are used when the value of a property has a more complex structure than simply being a textual value or a reference to another thing. */
	'StructuredValue': 'https://schema.org/StructuredValue',
	/** StudioAlbum. */
	'StudioAlbum': 'https://schema.org/StudioAlbum',
	/** The act of forming a personal connection with someone/something (object) unidirectionally/asymmetrically to get updates pushed to.\n\nRelated actions:\n\n* [[FollowAction]]: Unlike FollowAction, SubscribeAction implies that the subscriber acts as a passive agent being constantly/actively pushed for updates.\n* [[RegisterAction]]: Unlike RegisterAction, SubscribeAction implies that the agent is interested in continuing receiving updates from the object.\n* [[JoinAction]]: Unlike JoinAction, SubscribeAction implies that the agent is interested in continuing receiving updates from the object. */
	'SubscribeAction': 'https://schema.org/SubscribeAction',
	/** Represents the subscription pricing component of the total price for an offered product. */
	'Subscription': 'https://schema.org/Subscription',
	/** Any matter of defined composition that has discrete existence, whose origin may be biological, mineral or chemical. */
	'Substance': 'https://schema.org/Substance',
	/** A subway station. */
	'SubwayStation': 'https://schema.org/SubwayStation',
	/** A suite in a hotel or other public accommodation, denotes a class of luxury accommodations, the key feature of which is multiple rooms (source: Wikipedia, the free encyclopedia, see <a href="http://en.wikipedia.org/wiki/Suite_(hotel)">http://en.wikipedia.org/wiki/Suite_(hotel)</a>).
<br /><br />
See also the <a href="/docs/hotels.html">dedicated document on the use of schema.org for marking up hotels and other forms of accommodations</a>.
 */
	'Suite': 'https://schema.org/Suite',
	/** The day of the week between Saturday and Monday. */
	'Sunday': 'https://schema.org/Sunday',
	/** Anatomical features that can be observed by sight (without dissection), including the form and proportions of the human body as well as surface landmarks that correspond to deeper subcutaneous structures. Superficial anatomy plays an important role in sports medicine, phlebotomy, and other medical specialties as underlying anatomical structures can be identified through surface palpation. For example, during back surgery, superficial anatomy can be used to palpate and count vertebrae to find the site of incision. Or in phlebotomy, superficial anatomy can be used to locate an underlying vein; for example, the median cubital vein can be located by palpating the borders of the cubital fossa (such as the epicondyles of the humerus) and then looking for the superficial signs of the vein, such as size, prominence, ability to refill after depression, and feel of surrounding tissue support. As another example, in a subluxation (dislocation) of the glenohumeral joint, the bony structure becomes pronounced with the deltoid muscle failing to cover the glenohumeral joint allowing the edges of the scapula to be superficially visible. Here, the superficial anatomy is the visible edges of the scapula, implying the underlying dislocation of the joint (the related anatomical structure). */
	'SuperficialAnatomy': 'https://schema.org/SuperficialAnatomy',
	/** A specific branch of medical science that pertains to treating diseases, injuries and deformities by manual and instrumental means. */
	'Surgical': 'https://schema.org/Surgical',
	/** A medical procedure involving an incision with instruments; performed for diagnose, or therapeutic purposes. */
	'SurgicalProcedure': 'https://schema.org/SurgicalProcedure',
	/** The act of momentarily pausing a device or application (e.g. pause music playback or pause a timer). */
	'SuspendAction': 'https://schema.org/SuspendAction',
	/** Suspended. */
	'Suspended': 'https://schema.org/Suspended',
	/** A syllabus that describes the material covered in a course, often with several such sections per [[Course]] so that a distinct [[timeRequired]] can be provided for that section of the [[Course]]. */
	'Syllabus': 'https://schema.org/Syllabus',
	/** Symptoms or related symptoms of a Topic. */
	'SymptomsHealthAspect': 'https://schema.org/SymptomsHealthAspect',
	/** A synagogue. */
	'Synagogue': 'https://schema.org/Synagogue',
	/** A short TV program or a segment/part of a TV program. */
	'TVClip': 'https://schema.org/TVClip',
	/** A TV episode which can be part of a series or season. */
	'TVEpisode': 'https://schema.org/TVEpisode',
	/** Season dedicated to TV broadcast and associated online delivery. */
	'TVSeason': 'https://schema.org/TVSeason',
	/** CreativeWorkSeries dedicated to TV broadcast and associated online delivery. */
	'TVSeries': 'https://schema.org/TVSeries',
	/** A table on a Web page. */
	'Table': 'https://schema.org/Table',
	/** The act of gaining ownership of an object from an origin. Reciprocal of GiveAction.\n\nRelated actions:\n\n* [[GiveAction]]: The reciprocal of TakeAction.\n* [[ReceiveAction]]: Unlike ReceiveAction, TakeAction implies that ownership has been transferred. */
	'TakeAction': 'https://schema.org/TakeAction',
	/** A tattoo parlor. */
	'TattooParlor': 'https://schema.org/TattooParlor',
	/** A taxi. */
	'Taxi': 'https://schema.org/Taxi',
	/** A reservation for a taxi.\n\nNote: This type is for information about actual reservations, e.g. in confirmation emails or HTML pages with individual confirmations of reservations. For offers of tickets, use [[Offer]]. */
	'TaxiReservation': 'https://schema.org/TaxiReservation',
	/** A service for a vehicle for hire with a driver for local travel. Fares are usually calculated based on distance traveled. */
	'TaxiService': 'https://schema.org/TaxiService',
	/** A taxi stand. */
	'TaxiStand': 'https://schema.org/TaxiStand',
	/** Indicates the usage of the car as a taxi. */
	'TaxiVehicleUsage': 'https://schema.org/TaxiVehicleUsage',
	/** A set of organisms asserted to represent a natural cohesive biological unit. */
	'Taxon': 'https://schema.org/Taxon',
	/** A technical article - Example: How-to (task) topics, step-by-step, procedural troubleshooting, specifications, etc. */
	'TechArticle': 'https://schema.org/TechArticle',
	/** A unique instance of a television BroadcastService on a CableOrSatelliteService lineup. */
	'TelevisionChannel': 'https://schema.org/TelevisionChannel',
	/** A television station. */
	'TelevisionStation': 'https://schema.org/TelevisionStation',
	/** A tennis complex. */
	'TennisComplex': 'https://schema.org/TennisComplex',
	/** Terminated. */
	'Terminated': 'https://schema.org/Terminated',
	/** Data type: Text. */
	'Text': 'https://schema.org/Text',
	/** A file composed primarily of text. */
	'TextDigitalDocument': 'https://schema.org/TextDigitalDocument',
	/** A text file. The text can be unformatted or contain markup, html, etc. */
	'TextObject': 'https://schema.org/TextObject',
	/** Event type: Theater performance. */
	'TheaterEvent': 'https://schema.org/TheaterEvent',
	/** A theater group or company, for example, the Royal Shakespeare Company or Druid Theatre. */
	'TheaterGroup': 'https://schema.org/TheaterGroup',
	/** A medical device used for therapeutic purposes. */
	'Therapeutic': 'https://schema.org/Therapeutic',
	/** A medical procedure intended primarily for therapeutic purposes, aimed at improving a health condition. */
	'TherapeuticProcedure': 'https://schema.org/TherapeuticProcedure',
	/** A thesis or dissertation document submitted in support of candidature for an academic degree or professional qualification. */
	'Thesis': 'https://schema.org/Thesis',
	/** The most generic type of item. */
	'Thing': 'https://schema.org/Thing',
	/** Throat assessment with  clinical examination. */
	'Throat': 'https://schema.org/Throat',
	/** The day of the week between Wednesday and Friday. */
	'Thursday': 'https://schema.org/Thursday',
	/** Used to describe a ticket to an event, a flight, a bus ride, etc. */
	'Ticket': 'https://schema.org/Ticket',
	/** The act of reaching a draw in a competitive activity. */
	'TieAction': 'https://schema.org/TieAction',
	/** A point in time recurring on multiple days in the form hh:mm:ss[Z|(+|-)hh:mm] (see [XML schema for details](http://www.w3.org/TR/xmlschema-2/#time)). */
	'Time': 'https://schema.org/Time',
	/** The act of giving money voluntarily to a beneficiary in recognition of services rendered. */
	'TipAction': 'https://schema.org/TipAction',
	/** A tire shop. */
	'TireShop': 'https://schema.org/TireShop',
	/** Item contains tobacco and/or nicotine, for example cigars, cigarettes, chewing tobacco, e-cigarettes, or hookahs. */
	'TobaccoNicotineConsideration': 'https://schema.org/TobaccoNicotineConsideration',
	/** The associated telephone number is toll free. */
	'TollFree': 'https://schema.org/TollFree',
	/** A tourist attraction.  In principle any Thing can be a [[TouristAttraction]], from a [[Mountain]] and [[LandmarksOrHistoricalBuildings]] to a [[LocalBusiness]].  This Type can be used on its own to describe a general [[TouristAttraction]], or be used as an [[additionalType]] to add tourist attraction properties to any other type.  (See examples below) */
	'TouristAttraction': 'https://schema.org/TouristAttraction',
	/** A tourist destination. In principle any [[Place]] can be a [[TouristDestination]] from a [[City]], Region or [[Country]] to an [[AmusementPark]] or [[Hotel]]. This Type can be used on its own to describe a general [[TouristDestination]], or be used as an [[additionalType]] to add tourist relevant properties to any other [[Place]].  A [[TouristDestination]] is defined as a [[Place]] that contains, or is colocated with, one or more [[TouristAttraction]]s, often linked by a similar theme or interest to a particular [[touristType]]. The [UNWTO](http://www2.unwto.org/) defines Destination (main destination of a tourism trip) as the place visited that is central to the decision to take the trip.
  (See examples below.) */
	'TouristDestination': 'https://schema.org/TouristDestination',
	/** A tourist information center. */
	'TouristInformationCenter': 'https://schema.org/TouristInformationCenter',
	/** A tourist trip. A created itinerary of visits to one or more places of interest ([[TouristAttraction]]/[[TouristDestination]]) often linked by a similar theme, geographic area, or interest to a particular [[touristType]]. The [UNWTO](http://www2.unwto.org/) defines tourism trip as the Trip taken by visitors.
  (See examples below.) */
	'TouristTrip': 'https://schema.org/TouristTrip',
	/** A specific branch of medical science that is concerned with poisons, their nature, effects and detection and involved in the treatment of poisoning. */
	'Toxicologic': 'https://schema.org/Toxicologic',
	/** A toy store. */
	'ToyStore': 'https://schema.org/ToyStore',
	/** An agent tracks an object for updates.\n\nRelated actions:\n\n* [[FollowAction]]: Unlike FollowAction, TrackAction refers to the interest on the location of innanimates objects.\n* [[SubscribeAction]]: Unlike SubscribeAction, TrackAction refers to  the interest on the location of innanimate objects. */
	'TrackAction': 'https://schema.org/TrackAction',
	/** The act of participating in an exchange of goods and services for monetary compensation. An agent trades an object, product or service with a participant in exchange for a one time or periodic payment. */
	'TradeAction': 'https://schema.org/TradeAction',
	/** A system of medicine based on common theoretical concepts that originated in China and evolved over thousands of years, that uses herbs, acupuncture, exercise, massage, dietary therapy, and other methods to treat a wide range of conditions. */
	'TraditionalChinese': 'https://schema.org/TraditionalChinese',
	/** A reservation for train travel.\n\nNote: This type is for information about actual reservations, e.g. in confirmation emails or HTML pages with individual confirmations of reservations. For offers of tickets, use [[Offer]]. */
	'TrainReservation': 'https://schema.org/TrainReservation',
	/** A train station. */
	'TrainStation': 'https://schema.org/TrainStation',
	/** A trip on a commercial train line. */
	'TrainTrip': 'https://schema.org/TrainTrip',
	/** The act of transferring/moving (abstract or concrete) animate or inanimate objects from one place to another. */
	'TransferAction': 'https://schema.org/TransferAction',
	/** Content coded 'transformed content' in a [[MediaReview]], considered in the context of how it was published or shared.

For a [[VideoObject]] to be 'transformed content':  or all of the video has been manipulated to transform the footage itself. This category includes using tools like the Adobe Suite to change the speed of the video, add or remove visual elements or dub audio. Deepfakes are also a subset of transformation.

For an [[ImageObject]] to be 'transformed content': Adding or deleting visual elements to give the image a different meaning with the intention to mislead.

For an [[ImageObject]] with embedded text to be 'transformed content': Adding or deleting visual elements to give the image a different meaning with the intention to mislead.

For an [[AudioObject]] to be 'transformed content': Part or all of the audio has been manipulated to alter the words or sounds, or the audio has been synthetically generated, such as to create a sound-alike voice.
 */
	'TransformedContent': 'https://schema.org/TransformedContent',
	/** A transit map. */
	'TransitMap': 'https://schema.org/TransitMap',
	/** The act of traveling from a fromLocation to a destination by a specified mode of transport, optionally with participants. */
	'TravelAction': 'https://schema.org/TravelAction',
	/** A travel agency. */
	'TravelAgency': 'https://schema.org/TravelAgency',
	/** An indication for treating an underlying condition, symptom, etc. */
	'TreatmentIndication': 'https://schema.org/TreatmentIndication',
	/** Treatments or related therapies for a Topic. */
	'TreatmentsHealthAspect': 'https://schema.org/TreatmentsHealthAspect',
	/** A trip or journey. An itinerary of visits to one or more places. */
	'Trip': 'https://schema.org/Trip',
	/** A trial design in which neither the researcher, the person administering the therapy nor the patient knows the details of the treatment the patient was randomly assigned to. */
	'TripleBlindedTrial': 'https://schema.org/TripleBlindedTrial',
	/** The boolean value true. */
	'True': 'https://schema.org/True',
	/** The day of the week between Monday and Wednesday. */
	'Tuesday': 'https://schema.org/Tuesday',
	/** A structured value indicating the quantity, unit of measurement, and business function of goods included in a bundle offer. */
	'TypeAndQuantityNode': 'https://schema.org/TypeAndQuantityNode',
	/** Categorization and other types related to a topic. */
	'TypesHealthAspect': 'https://schema.org/TypesHealthAspect',
	/** UKNonprofitType: Non-profit organization type originating from the United Kingdom. */
	'UKNonprofitType': 'https://schema.org/UKNonprofitType',
	/** UKTrust: Non-profit type referring to a UK trust. */
	'UKTrust': 'https://schema.org/UKTrust',
	/** Data type: URL. */
	'URL': 'https://schema.org/URL',
	/** USNonprofitType: Non-profit organization type originating from the United States. */
	'USNonprofitType': 'https://schema.org/USNonprofitType',
	/** Ultrasound imaging. */
	'Ultrasound': 'https://schema.org/Ultrasound',
	/** The act of un-registering from a service.\n\nRelated actions:\n\n* [[RegisterAction]]: antonym of UnRegisterAction.\n* [[LeaveAction]]: Unlike LeaveAction, UnRegisterAction implies that you are unregistering from a service you were previously registered, rather than leaving a team/group of people. */
	'UnRegisterAction': 'https://schema.org/UnRegisterAction',
	/** The item is suitable only for adults, without indicating why. Due to widespread use of "adult" as a euphemism for "sexual", many such items are likely suited also for the SexualContentConsideration code. */
	'UnclassifiedAdultConsideration': 'https://schema.org/UnclassifiedAdultConsideration',
	/** UnemploymentSupport: this is a benefit for unemployment support. */
	'UnemploymentSupport': 'https://schema.org/UnemploymentSupport',
	/** UnincorporatedAssociationCharity: Non-profit type referring to a charitable company that is not incorporated (UK). */
	'UnincorporatedAssociationCharity': 'https://schema.org/UnincorporatedAssociationCharity',
	/** The price asked for a given offer by the respective organization or person. */
	'UnitPriceSpecification': 'https://schema.org/UnitPriceSpecification',
	/** Indicates that a document has no particular or special standing (e.g. a republication of a law by a private publisher). */
	'UnofficialLegalValue': 'https://schema.org/UnofficialLegalValue',
	/** The act of managing by changing/editing the state of the object. */
	'UpdateAction': 'https://schema.org/UpdateAction',
	/** A specific branch of medical science that is concerned with the diagnosis and treatment of diseases pertaining to the urinary tract and the urogenital system. */
	'Urologic': 'https://schema.org/Urologic',
	/** Content about how, when, frequency and dosage of a topic. */
	'UsageOrScheduleHealthAspect': 'https://schema.org/UsageOrScheduleHealthAspect',
	/** The act of applying an object to its intended purpose. */
	'UseAction': 'https://schema.org/UseAction',
	/** Indicates that the item is used. */
	'UsedCondition': 'https://schema.org/UsedCondition',
	/** UserInteraction and its subtypes is an old way of talking about users interacting with pages. It is generally better to use [[Action]]-based vocabulary, alongside types such as [[Comment]]. */
	'UserBlocks': 'https://schema.org/UserBlocks',
	/** UserInteraction and its subtypes is an old way of talking about users interacting with pages. It is generally better to use [[Action]]-based vocabulary, alongside types such as [[Comment]]. */
	'UserCheckins': 'https://schema.org/UserCheckins',
	/** UserInteraction and its subtypes is an old way of talking about users interacting with pages. It is generally better to use [[Action]]-based vocabulary, alongside types such as [[Comment]]. */
	'UserComments': 'https://schema.org/UserComments',
	/** UserInteraction and its subtypes is an old way of talking about users interacting with pages. It is generally better to use [[Action]]-based vocabulary, alongside types such as [[Comment]]. */
	'UserDownloads': 'https://schema.org/UserDownloads',
	/** UserInteraction and its subtypes is an old way of talking about users interacting with pages. It is generally better to use [[Action]]-based vocabulary, alongside types such as [[Comment]]. */
	'UserInteraction': 'https://schema.org/UserInteraction',
	/** UserInteraction and its subtypes is an old way of talking about users interacting with pages. It is generally better to use [[Action]]-based vocabulary, alongside types such as [[Comment]]. */
	'UserLikes': 'https://schema.org/UserLikes',
	/** UserInteraction and its subtypes is an old way of talking about users interacting with pages. It is generally better to use [[Action]]-based vocabulary, alongside types such as [[Comment]]. */
	'UserPageVisits': 'https://schema.org/UserPageVisits',
	/** UserInteraction and its subtypes is an old way of talking about users interacting with pages. It is generally better to use [[Action]]-based vocabulary, alongside types such as [[Comment]]. */
	'UserPlays': 'https://schema.org/UserPlays',
	/** UserInteraction and its subtypes is an old way of talking about users interacting with pages. It is generally better to use [[Action]]-based vocabulary, alongside types such as [[Comment]]. */
	'UserPlusOnes': 'https://schema.org/UserPlusOnes',
	/** A review created by an end-user (e.g. consumer, purchaser, attendee etc.), in contrast with [[CriticReview]]. */
	'UserReview': 'https://schema.org/UserReview',
	/** UserInteraction and its subtypes is an old way of talking about users interacting with pages. It is generally better to use [[Action]]-based vocabulary, alongside types such as [[Comment]]. */
	'UserTweets': 'https://schema.org/UserTweets',
	/** A kind of lodging business that focuses on renting single properties for limited time. */
	'VacationRental': 'https://schema.org/VacationRental',
	/** A diet exclusive of all animal products. */
	'VeganDiet': 'https://schema.org/VeganDiet',
	/** A diet exclusive of animal meat. */
	'VegetarianDiet': 'https://schema.org/VegetarianDiet',
	/** A vehicle is a device that is designed or used to transport people or cargo over land, water, air, or through space. */
	'Vehicle': 'https://schema.org/Vehicle',
	/** A type of blood vessel that specifically carries blood to the heart. */
	'Vein': 'https://schema.org/Vein',
	/** A venue map (e.g. for malls, auditoriums, museums, etc.). */
	'VenueMap': 'https://schema.org/VenueMap',
	/** A component of the human body circulatory system comprised of an intricate network of hollow tubes that transport blood throughout the entire body. */
	'Vessel': 'https://schema.org/Vessel',
	/** A vet's office. */
	'VeterinaryCare': 'https://schema.org/VeterinaryCare',
	/** Web page type: Video gallery page. */
	'VideoGallery': 'https://schema.org/VideoGallery',
	/** A video game is an electronic game that involves human interaction with a user interface to generate visual feedback on a video device. */
	'VideoGame': 'https://schema.org/VideoGame',
	/** A short segment/part of a video game. */
	'VideoGameClip': 'https://schema.org/VideoGameClip',
	/** A video game series. */
	'VideoGameSeries': 'https://schema.org/VideoGameSeries',
	/** A video file. */
	'VideoObject': 'https://schema.org/VideoObject',
	/** A specific and exact (byte-for-byte) version of a [[VideoObject]]. Two byte-for-byte identical files, for the purposes of this type, considered identical. If they have different embedded metadata the files will differ. Different external facts about the files, e.g. creator or dateCreated that aren't represented in their actual content, do not affect this notion of identity. */
	'VideoObjectSnapshot': 'https://schema.org/VideoObjectSnapshot',
	/** The act of consuming static visual content. */
	'ViewAction': 'https://schema.org/ViewAction',
	/** VinylFormat. */
	'VinylFormat': 'https://schema.org/VinylFormat',
	/** Item shows or promotes violence. */
	'ViolenceConsideration': 'https://schema.org/ViolenceConsideration',
	/** An online or virtual location for attending events. For example, one may attend an online seminar or educational event. While a virtual location may be used as the location of an event, virtual locations should not be confused with physical locations in the real world. */
	'VirtualLocation': 'https://schema.org/VirtualLocation',
	/** Pathogenic virus that causes viral infection. */
	'Virus': 'https://schema.org/Virus',
	/** Event type: Visual arts event. */
	'VisualArtsEvent': 'https://schema.org/VisualArtsEvent',
	/** A work of art that is primarily visual in character. */
	'VisualArtwork': 'https://schema.org/VisualArtwork',
	/** Vital signs are measures of various physiological functions in order to assess the most basic body functions. */
	'VitalSign': 'https://schema.org/VitalSign',
	/** A volcano, like Fujisan. */
	'Volcano': 'https://schema.org/Volcano',
	/** The act of expressing a preference from a fixed/finite/structured set of choices/options. */
	'VoteAction': 'https://schema.org/VoteAction',
	/** An advertising section of the page. */
	'WPAdBlock': 'https://schema.org/WPAdBlock',
	/** The footer section of the page. */
	'WPFooter': 'https://schema.org/WPFooter',
	/** The header section of the page. */
	'WPHeader': 'https://schema.org/WPHeader',
	/** A sidebar section of the page. */
	'WPSideBar': 'https://schema.org/WPSideBar',
	/** The act of expressing a desire about the object. An agent wants an object. */
	'WantAction': 'https://schema.org/WantAction',
	/** A structured value representing the duration and scope of services that will be provided to a customer free of charge in case of a defect or malfunction of a product. */
	'WarrantyPromise': 'https://schema.org/WarrantyPromise',
	/** A range of services that will be provided to a customer free of charge in case of a defect or malfunction of a product.\n\nCommonly used values:\n\n* http://purl.org/goodrelations/v1#Labor-BringIn\n* http://purl.org/goodrelations/v1#PartsAndLabor-BringIn\n* http://purl.org/goodrelations/v1#PartsAndLabor-PickUp
       */
	'WarrantyScope': 'https://schema.org/WarrantyScope',
	/** The act of consuming dynamic/moving visual content. */
	'WatchAction': 'https://schema.org/WatchAction',
	/** A waterfall, like Niagara. */
	'Waterfall': 'https://schema.org/Waterfall',
	/** The item is intended to induce bodily harm, for example guns, mace, combat knives, brass knuckles, nail or other bombs, and spears. */
	'WeaponConsideration': 'https://schema.org/WeaponConsideration',
	/** The act of dressing oneself in clothing. */
	'WearAction': 'https://schema.org/WearAction',
	/** Measurement of the back section, for example of a jacket */
	'WearableMeasurementBack': 'https://schema.org/WearableMeasurementBack',
	/** Measurement of the chest/bust section, for example of a suit */
	'WearableMeasurementChestOrBust': 'https://schema.org/WearableMeasurementChestOrBust',
	/** Measurement of the collar, for example of a shirt */
	'WearableMeasurementCollar': 'https://schema.org/WearableMeasurementCollar',
	/** Measurement of the cup, for example of a bra */
	'WearableMeasurementCup': 'https://schema.org/WearableMeasurementCup',
	/** Measurement of the height, for example the heel height of a shoe */
	'WearableMeasurementHeight': 'https://schema.org/WearableMeasurementHeight',
	/** Measurement of the hip section, for example of a skirt */
	'WearableMeasurementHips': 'https://schema.org/WearableMeasurementHips',
	/** Measurement of the inseam, for example of pants */
	'WearableMeasurementInseam': 'https://schema.org/WearableMeasurementInseam',
	/** Represents the length, for example of a dress */
	'WearableMeasurementLength': 'https://schema.org/WearableMeasurementLength',
	/** Measurement of the outside leg, for example of pants */
	'WearableMeasurementOutsideLeg': 'https://schema.org/WearableMeasurementOutsideLeg',
	/** Measurement of the sleeve length, for example of a shirt */
	'WearableMeasurementSleeve': 'https://schema.org/WearableMeasurementSleeve',
	/** Enumerates common types of measurement for wearables products. */
	'WearableMeasurementTypeEnumeration': 'https://schema.org/WearableMeasurementTypeEnumeration',
	/** Measurement of the waist section, for example of pants */
	'WearableMeasurementWaist': 'https://schema.org/WearableMeasurementWaist',
	/** Measurement of the width, for example of shoes */
	'WearableMeasurementWidth': 'https://schema.org/WearableMeasurementWidth',
	/** Size group "Big" for wearables. */
	'WearableSizeGroupBig': 'https://schema.org/WearableSizeGroupBig',
	/** Size group "Boys" for wearables. */
	'WearableSizeGroupBoys': 'https://schema.org/WearableSizeGroupBoys',
	/** Enumerates common size groups (also known as "size types") for wearable products. */
	'WearableSizeGroupEnumeration': 'https://schema.org/WearableSizeGroupEnumeration',
	/** Size group "Extra Short" for wearables. */
	'WearableSizeGroupExtraShort': 'https://schema.org/WearableSizeGroupExtraShort',
	/** Size group "Extra Tall" for wearables. */
	'WearableSizeGroupExtraTall': 'https://schema.org/WearableSizeGroupExtraTall',
	/** Size group "Girls" for wearables. */
	'WearableSizeGroupGirls': 'https://schema.org/WearableSizeGroupGirls',
	/** Size group "Husky" (or "Stocky") for wearables. */
	'WearableSizeGroupHusky': 'https://schema.org/WearableSizeGroupHusky',
	/** Size group "Infants" for wearables. */
	'WearableSizeGroupInfants': 'https://schema.org/WearableSizeGroupInfants',
	/** Size group "Juniors" for wearables. */
	'WearableSizeGroupJuniors': 'https://schema.org/WearableSizeGroupJuniors',
	/** Size group "Maternity" for wearables. */
	'WearableSizeGroupMaternity': 'https://schema.org/WearableSizeGroupMaternity',
	/** Size group "Mens" for wearables. */
	'WearableSizeGroupMens': 'https://schema.org/WearableSizeGroupMens',
	/** Size group "Misses" (also known as "Missy") for wearables. */
	'WearableSizeGroupMisses': 'https://schema.org/WearableSizeGroupMisses',
	/** Size group "Petite" for wearables. */
	'WearableSizeGroupPetite': 'https://schema.org/WearableSizeGroupPetite',
	/** Size group "Plus" for wearables. */
	'WearableSizeGroupPlus': 'https://schema.org/WearableSizeGroupPlus',
	/** Size group "Regular" for wearables. */
	'WearableSizeGroupRegular': 'https://schema.org/WearableSizeGroupRegular',
	/** Size group "Short" for wearables. */
	'WearableSizeGroupShort': 'https://schema.org/WearableSizeGroupShort',
	/** Size group "Tall" for wearables. */
	'WearableSizeGroupTall': 'https://schema.org/WearableSizeGroupTall',
	/** Size group "Womens" for wearables. */
	'WearableSizeGroupWomens': 'https://schema.org/WearableSizeGroupWomens',
	/** Australian size system for wearables. */
	'WearableSizeSystemAU': 'https://schema.org/WearableSizeSystemAU',
	/** Brazilian size system for wearables. */
	'WearableSizeSystemBR': 'https://schema.org/WearableSizeSystemBR',
	/** Chinese size system for wearables. */
	'WearableSizeSystemCN': 'https://schema.org/WearableSizeSystemCN',
	/** Continental size system for wearables. */
	'WearableSizeSystemContinental': 'https://schema.org/WearableSizeSystemContinental',
	/** German size system for wearables. */
	'WearableSizeSystemDE': 'https://schema.org/WearableSizeSystemDE',
	/** EN 13402 (joint European standard for size labelling of clothes). */
	'WearableSizeSystemEN13402': 'https://schema.org/WearableSizeSystemEN13402',
	/** Enumerates common size systems specific for wearable products */
	'WearableSizeSystemEnumeration': 'https://schema.org/WearableSizeSystemEnumeration',
	/** European size system for wearables. */
	'WearableSizeSystemEurope': 'https://schema.org/WearableSizeSystemEurope',
	/** French size system for wearables. */
	'WearableSizeSystemFR': 'https://schema.org/WearableSizeSystemFR',
	/** GS1 (formerly NRF) size system for wearables. */
	'WearableSizeSystemGS1': 'https://schema.org/WearableSizeSystemGS1',
	/** Italian size system for wearables. */
	'WearableSizeSystemIT': 'https://schema.org/WearableSizeSystemIT',
	/** Japanese size system for wearables. */
	'WearableSizeSystemJP': 'https://schema.org/WearableSizeSystemJP',
	/** Mexican size system for wearables. */
	'WearableSizeSystemMX': 'https://schema.org/WearableSizeSystemMX',
	/** United Kingdom size system for wearables. */
	'WearableSizeSystemUK': 'https://schema.org/WearableSizeSystemUK',
	/** United States size system for wearables. */
	'WearableSizeSystemUS': 'https://schema.org/WearableSizeSystemUS',
	/** An application programming interface accessible over Web/Internet technologies. */
	'WebAPI': 'https://schema.org/WebAPI',
	/** Web applications. */
	'WebApplication': 'https://schema.org/WebApplication',
	/** WebContent is a type representing all [[WebPage]], [[WebSite]] and [[WebPageElement]] content. It is sometimes the case that detailed distinctions between Web pages, sites and their parts are not always important or obvious. The  [[WebContent]] type makes it easier to describe Web-addressable content without requiring such distinctions to always be stated. (The intent is that the existing types [[WebPage]], [[WebSite]] and [[WebPageElement]] will eventually be declared as subtypes of [[WebContent]].) */
	'WebContent': 'https://schema.org/WebContent',
	/** A web page. Every web page is implicitly assumed to be declared to be of type WebPage, so the various properties about that webpage, such as <code>breadcrumb</code> may be used. We recommend explicit declaration if these properties are specified, but if they are found outside of an itemscope, they will be assumed to be about the page. */
	'WebPage': 'https://schema.org/WebPage',
	/** A web page element, like a table or an image. */
	'WebPageElement': 'https://schema.org/WebPageElement',
	/** A WebSite is a set of related web pages and other items typically served from a single web domain and accessible via URLs. */
	'WebSite': 'https://schema.org/WebSite',
	/** The day of the week between Tuesday and Thursday. */
	'Wednesday': 'https://schema.org/Wednesday',
	/** The conventional Western system of medicine, that aims to apply the best available evidence gained from the scientific method to clinical decision making. Also known as conventional or Western medicine. */
	'WesternConventional': 'https://schema.org/WesternConventional',
	/** The drug's cost represents the wholesale acquisition cost of the drug. */
	'Wholesale': 'https://schema.org/Wholesale',
	/** A wholesale store. */
	'WholesaleStore': 'https://schema.org/WholesaleStore',
	/** The act of achieving victory in a competitive activity. */
	'WinAction': 'https://schema.org/WinAction',
	/** A winery. */
	'Winery': 'https://schema.org/Winery',
	/** Withdrawn. */
	'Withdrawn': 'https://schema.org/Withdrawn',
	/** A program with both an educational and employment component. Typically based at a workplace and structured around work-based learning, with the aim of instilling competencies related to an occupation. WorkBasedProgram is used to distinguish programs such as apprenticeships from school, college or other classroom based educational programs. */
	'WorkBasedProgram': 'https://schema.org/WorkBasedProgram',
	/** A Workers Union (also known as a Labor Union, Labour Union, or Trade Union) is an organization that promotes the interests of its worker members by collectively bargaining with management, organizing, and political lobbying. */
	'WorkersUnion': 'https://schema.org/WorkersUnion',
	/** The act of authoring written creative content. */
	'WriteAction': 'https://schema.org/WriteAction',
	/** Permission to write or edit the document. */
	'WritePermission': 'https://schema.org/WritePermission',
	/** Text representing an XPath (typically but not necessarily version 1.0). */
	'XPathType': 'https://schema.org/XPathType',
	/** X-ray imaging. */
	'XRay': 'https://schema.org/XRay',
	/** The airline boards by zones of the plane. */
	'ZoneBoardingPolicy': 'https://schema.org/ZoneBoardingPolicy',
	/** A zoo. */
	'Zoo': 'https://schema.org/Zoo',
	/** The subject matter of the content. */
	'about': 'https://schema.org/about',
	/** Indicates whether the book is an abridged edition. */
	'abridged': 'https://schema.org/abridged',
	/** An abstract is a short description that summarizes a [[CreativeWork]]. */
	'abstract': 'https://schema.org/abstract',
	/** The time needed to accelerate the vehicle from a given start velocity to a given target velocity.\n\nTypical unit code(s): SEC for seconds\n\n* Note: There are unfortunately no standard unit codes for seconds/0..100 km/h or seconds/0..60 mph. Simply use "SEC" for seconds and indicate the velocities in the [[name]] of the [[QuantitativeValue]], or use [[valueReference]] with a [[QuantitativeValue]] of 0..60 mph or 0..100 km/h to specify the reference speeds. */
	'accelerationTime': 'https://schema.org/accelerationTime',
	/** The answer(s) that has been accepted as best, typically on a Question/Answer site. Sites vary in their selection mechanisms, e.g. drawing on community opinion and/or the view of the Question author. */
	'acceptedAnswer': 'https://schema.org/acceptedAnswer',
	/** The offer(s) -- e.g., product, quantity and price combinations -- included in the order. */
	'acceptedOffer': 'https://schema.org/acceptedOffer',
	/** The payment method(s) accepted by seller for this offer. */
	'acceptedPaymentMethod': 'https://schema.org/acceptedPaymentMethod',
	/** Indicates whether a FoodEstablishment accepts reservations. Values can be Boolean, an URL at which reservations can be made or (for backwards compatibility) the strings ```Yes``` or ```No```. */
	'acceptsReservations': 'https://schema.org/acceptsReservations',
	/** Password, PIN, or access code needed for delivery (e.g. from a locker). */
	'accessCode': 'https://schema.org/accessCode',
	/** The human sensory perceptual system or cognitive faculty through which a person may process or perceive information. Values should be drawn from the [approved vocabulary](https://www.w3.org/2021/a11y-discov-vocab/latest/#accessMode-vocabulary). */
	'accessMode': 'https://schema.org/accessMode',
	/** A list of single or combined accessModes that are sufficient to understand all the intellectual content of a resource. Values should be drawn from the [approved vocabulary](https://www.w3.org/2021/a11y-discov-vocab/latest/#accessModeSufficient-vocabulary). */
	'accessModeSufficient': 'https://schema.org/accessModeSufficient',
	/** Indicates that the resource is compatible with the referenced accessibility API. Values should be drawn from the [approved vocabulary](https://www.w3.org/2021/a11y-discov-vocab/latest/#accessibilityAPI-vocabulary). */
	'accessibilityAPI': 'https://schema.org/accessibilityAPI',
	/** Identifies input methods that are sufficient to fully control the described resource. Values should be drawn from the [approved vocabulary](https://www.w3.org/2021/a11y-discov-vocab/latest/#accessibilityControl-vocabulary). */
	'accessibilityControl': 'https://schema.org/accessibilityControl',
	/** Content features of the resource, such as accessible media, alternatives and supported enhancements for accessibility. Values should be drawn from the [approved vocabulary](https://www.w3.org/2021/a11y-discov-vocab/latest/#accessibilityFeature-vocabulary). */
	'accessibilityFeature': 'https://schema.org/accessibilityFeature',
	/** A characteristic of the described resource that is physiologically dangerous to some users. Related to WCAG 2.0 guideline 2.3. Values should be drawn from the [approved vocabulary](https://www.w3.org/2021/a11y-discov-vocab/latest/#accessibilityHazard-vocabulary). */
	'accessibilityHazard': 'https://schema.org/accessibilityHazard',
	/** A human-readable summary of specific accessibility features or deficiencies, consistent with the other accessibility metadata but expressing subtleties such as "short descriptions are present but long descriptions will be needed for non-visual users" or "short descriptions are present and no long descriptions are needed." */
	'accessibilitySummary': 'https://schema.org/accessibilitySummary',
	/** Category of an [[Accommodation]], following real estate conventions, e.g. RESO (see [PropertySubType](https://ddwiki.reso.org/display/DDW17/PropertySubType+Field), and [PropertyType](https://ddwiki.reso.org/display/DDW17/PropertyType+Field) fields  for suggested values). */
	'accommodationCategory': 'https://schema.org/accommodationCategory',
	/** A floorplan of some [[Accommodation]]. */
	'accommodationFloorPlan': 'https://schema.org/accommodationFloorPlan',
	/** The identifier for the account the payment will be applied to. */
	'accountId': 'https://schema.org/accountId',
	/** A minimum amount that has to be paid in every month. */
	'accountMinimumInflow': 'https://schema.org/accountMinimumInflow',
	/** An overdraft is an extension of credit from a lending institution when an account reaches zero. An overdraft allows the individual to continue withdrawing money even if the account has no funds in it. Basically the bank allows people to borrow a set amount of money. */
	'accountOverdraftLimit': 'https://schema.org/accountOverdraftLimit',
	/** Specifies the Person that is legally accountable for the CreativeWork. */
	'accountablePerson': 'https://schema.org/accountablePerson',
	/** Indicates a page documenting how licenses can be purchased or otherwise acquired, for the current item. */
	'acquireLicensePage': 'https://schema.org/acquireLicensePage',
	/** The organization or person from which the product was acquired. */
	'acquiredFrom': 'https://schema.org/acquiredFrom',
	/** The ACRISS Car Classification Code is a code used by many car rental companies, for classifying vehicles. ACRISS stands for Association of Car Rental Industry Systems and Standards. */
	'acrissCode': 'https://schema.org/acrissCode',
	/** A set of requirements that must be fulfilled in order to perform an Action. If more than one value is specified, fulfilling one set of requirements will allow the Action to be performed. */
	'actionAccessibilityRequirement': 'https://schema.org/actionAccessibilityRequirement',
	/** An application that can complete the request. */
	'actionApplication': 'https://schema.org/actionApplication',
	/** A sub property of object. The options subject to this action. */
	'actionOption': 'https://schema.org/actionOption',
	/** The high level platform(s) where the Action can be performed for the given URL. To specify a specific application or operating system instance, use actionApplication. */
	'actionPlatform': 'https://schema.org/actionPlatform',
	/** Indicates the current disposition of the Action. */
	'actionStatus': 'https://schema.org/actionStatus',
	/** For a [[NewsMediaOrganization]] or other news-related [[Organization]], a statement about public engagement activities (for news media, the newsroom’s), including involving the public - digitally or otherwise -- in coverage decisions, reporting and activities after publication. */
	'actionableFeedbackPolicy': 'https://schema.org/actionableFeedbackPolicy',
	/** An active ingredient, typically chemical compounds and/or biologic substances. */
	'activeIngredient': 'https://schema.org/activeIngredient',
	/** Length of time to engage in the activity. */
	'activityDuration': 'https://schema.org/activityDuration',
	/** How often one should engage in the activity. */
	'activityFrequency': 'https://schema.org/activityFrequency',
	/** An actor, e.g. in TV, radio, movie, video games etc., or in an event. Actors can be associated with individual items or with a series, episode, clip. */
	'actor': 'https://schema.org/actor',
	/** An actor, e.g. in TV, radio, movie, video games etc. Actors can be associated with individual items or with a series, episode, clip. */
	'actors': 'https://schema.org/actors',
	/** An additional offer that can only be obtained in combination with the first base offer (e.g. supplements and extensions that are available for a surcharge). */
	'addOn': 'https://schema.org/addOn',
	/** An additional name for a Person, can be used for a middle name. */
	'additionalName': 'https://schema.org/additionalName',
	/** If responding yes, the number of guests who will attend in addition to the invitee. */
	'additionalNumberOfGuests': 'https://schema.org/additionalNumberOfGuests',
	/** A property-value pair representing an additional characteristic of the entity, e.g. a product feature or another characteristic for which there is no matching property in schema.org.\n\nNote: Publishers should be aware that applications designed to use specific schema.org properties (e.g. https://schema.org/width, https://schema.org/color, https://schema.org/gtin13, ...) will typically expect such data to be provided using those properties, rather than using the generic property/value mechanism.
 */
	'additionalProperty': 'https://schema.org/additionalProperty',
	/** An additional type for the item, typically used for adding more specific types from external vocabularies in microdata syntax. This is a relationship between something and a class that the thing is in. Typically the value is a URI-identified RDF class, and in this case corresponds to the 
    use of rdf:type in RDF. Text values can be used sparingly, for cases where useful information can be added without their being an appropriate schema to reference. In the case of text values, the class label should follow the schema.org <a href="https://schema.org/docs/styleguide.html">style guide</a> */
	'additionalType': 'https://schema.org/additionalType',
	/** Any additional component of the exercise prescription that may need to be articulated to the patient. This may include the order of exercises, the number of repetitions of movement, quantitative distance, progressions over time, etc. */
	'additionalVariable': 'https://schema.org/additionalVariable',
	/** Physical address of the item. */
	'address': 'https://schema.org/address',
	/** The country. For example, USA. You can also provide the two-letter [ISO 3166-1 alpha-2 country code](http://en.wikipedia.org/wiki/ISO_3166-1). */
	'addressCountry': 'https://schema.org/addressCountry',
	/** The locality in which the street address is, and which is in the region. For example, Mountain View. */
	'addressLocality': 'https://schema.org/addressLocality',
	/** The region in which the locality is, and which is in the country. For example, California or another appropriate first-level [Administrative division](https://en.wikipedia.org/wiki/List_of_administrative_divisions_by_country). */
	'addressRegion': 'https://schema.org/addressRegion',
	/** A route by which this drug may be administered, e.g. 'oral'. */
	'administrationRoute': 'https://schema.org/administrationRoute',
	/** The amount of time that is required between accepting the offer and the actual usage of the resource or service. */
	'advanceBookingRequirement': 'https://schema.org/advanceBookingRequirement',
	/** A possible complication and/or side effect of this therapy. If it is known that an adverse outcome is serious (resulting in death, disability, or permanent damage; requiring hospitalization; or otherwise life-threatening or requiring immediate medical attention), tag it as a seriousAdverseOutcome instead. */
	'adverseOutcome': 'https://schema.org/adverseOutcome',
	/** Drugs that affect the test's results. */
	'affectedBy': 'https://schema.org/affectedBy',
	/** An organization that this person is affiliated with. For example, a school/university, a club, or a team. */
	'affiliation': 'https://schema.org/affiliation',
	/** A media object representing the circumstances after performing this direction. */
	'afterMedia': 'https://schema.org/afterMedia',
	/** The direct performer or driver of the action (animate or inanimate). E.g. *John* wrote a book. */
	'agent': 'https://schema.org/agent',
	/** The overall rating, based on a collection of reviews or ratings, of the item. */
	'aggregateRating': 'https://schema.org/aggregateRating',
	/** The kind of aircraft (e.g., "Boeing 747"). */
	'aircraft': 'https://schema.org/aircraft',
	/** A music album. */
	'album': 'https://schema.org/album',
	/** Classification of the album by its type of content: soundtrack, live album, studio album, etc. */
	'albumProductionType': 'https://schema.org/albumProductionType',
	/** A release of this album. */
	'albumRelease': 'https://schema.org/albumRelease',
	/** The kind of release which this album is: single, EP or album. */
	'albumReleaseType': 'https://schema.org/albumReleaseType',
	/** A collection of music albums. */
	'albums': 'https://schema.org/albums',
	/** Any precaution, guidance, contraindication, etc. related to consumption of alcohol while taking this drug. */
	'alcoholWarning': 'https://schema.org/alcoholWarning',
	/** The algorithm or rules to follow to compute the score. */
	'algorithm': 'https://schema.org/algorithm',
	/** A category of alignment between the learning resource and the framework node. Recommended values include: 'requires', 'textComplexity', 'readingLevel', and 'educationalSubject'. */
	'alignmentType': 'https://schema.org/alignmentType',
	/** An alias for the item. */
	'alternateName': 'https://schema.org/alternateName',
	/** A secondary title of the CreativeWork. */
	'alternativeHeadline': 'https://schema.org/alternativeHeadline',
	/** Another gene which is a variation of this one. */
	'alternativeOf': 'https://schema.org/alternativeOf',
	/** Alumni of an organization. */
	'alumni': 'https://schema.org/alumni',
	/** An organization that the person is an alumni of. */
	'alumniOf': 'https://schema.org/alumniOf',
	/** An amenity feature (e.g. a characteristic or service) of the Accommodation. This generic property does not make a statement about whether the feature is included in an offer for the main accommodation or available at extra costs. */
	'amenityFeature': 'https://schema.org/amenityFeature',
	/** The amount of money. */
	'amount': 'https://schema.org/amount',
	/** The quantity of the goods included in the offer. */
	'amountOfThisGood': 'https://schema.org/amountOfThisGood',
	/** Indicates a specific [[CivicStructure]] or [[LocalBusiness]] associated with the SpecialAnnouncement. For example, a specific testing facility or business with special opening hours. For a larger geographic region like a quarantine of an entire region, use [[spatialCoverage]]. */
	'announcementLocation': 'https://schema.org/announcementLocation',
	/** The annual rate that is charged for borrowing (or made by investing), expressed as a single percentage number that represents the actual yearly cost of funds over the term of a loan. This includes any fees or additional costs associated with the transaction. */
	'annualPercentageRate': 'https://schema.org/annualPercentageRate',
	/** The number of answers this question has received. */
	'answerCount': 'https://schema.org/answerCount',
	/** A step-by-step or full explanation about Answer. Can outline how this Answer was achieved or contain more broad clarification or statement about it.  */
	'answerExplanation': 'https://schema.org/answerExplanation',
	/** The muscle whose action counteracts the specified muscle. */
	'antagonist': 'https://schema.org/antagonist',
	/** Indicates an occurrence of a [[Claim]] in some [[CreativeWork]]. */
	'appearance': 'https://schema.org/appearance',
	/** A country where a particular merchant return policy applies to, for example the two-letter ISO 3166-1 alpha-2 country code. */
	'applicableCountry': 'https://schema.org/applicableCountry',
	/** The location in which the status applies. */
	'applicableLocation': 'https://schema.org/applicableLocation',
	/** The location(s) applicants can apply from. This is usually used for telecommuting jobs where the applicant does not need to be in a physical office. Note: This should not be used for citizenship or work visa requirements. */
	'applicantLocationRequirements': 'https://schema.org/applicantLocationRequirements',
	/** An application that can complete the request. */
	'application': 'https://schema.org/application',
	/** Type of software application, e.g. 'Game, Multimedia'. */
	'applicationCategory': 'https://schema.org/applicationCategory',
	/** Contact details for further information relevant to this job posting. */
	'applicationContact': 'https://schema.org/applicationContact',
	/** The date at which the program stops collecting applications for the next enrollment cycle. */
	'applicationDeadline': 'https://schema.org/applicationDeadline',
	/** The date at which the program begins collecting applications for the next enrollment cycle. */
	'applicationStartDate': 'https://schema.org/applicationStartDate',
	/** Subcategory of the application, e.g. 'Arcade Game'. */
	'applicationSubCategory': 'https://schema.org/applicationSubCategory',
	/** The name of the application suite to which the application belongs (e.g. Excel belongs to Office). */
	'applicationSuite': 'https://schema.org/applicationSuite',
	/** The delivery method(s) to which the delivery charge or payment charge specification applies. */
	'appliesToDeliveryMethod': 'https://schema.org/appliesToDeliveryMethod',
	/** The payment method(s) to which the payment charge specification applies. */
	'appliesToPaymentMethod': 'https://schema.org/appliesToPaymentMethod',
	/** Collection, [fonds](https://en.wikipedia.org/wiki/Fonds), or item held, kept or maintained by an [[ArchiveOrganization]]. */
	'archiveHeld': 'https://schema.org/archiveHeld',
	/** Indicates a page or other link involved in archival of a [[CreativeWork]]. In the case of [[MediaReview]], the items in a [[MediaReviewItem]] may often become inaccessible, but be archived by archival, journalistic, activist, or law enforcement organizations. In such cases, the referenced page may not directly publish the content. */
	'archivedAt': 'https://schema.org/archivedAt',
	/** The area within which users can expect to reach the broadcast service. */
	'area': 'https://schema.org/area',
	/** The geographic area where a service or offered item is provided. */
	'areaServed': 'https://schema.org/areaServed',
	/** The airport where the flight terminates. */
	'arrivalAirport': 'https://schema.org/arrivalAirport',
	/** The terminal or port from which the boat arrives. */
	'arrivalBoatTerminal': 'https://schema.org/arrivalBoatTerminal',
	/** The stop or station from which the bus arrives. */
	'arrivalBusStop': 'https://schema.org/arrivalBusStop',
	/** Identifier of the flight's arrival gate. */
	'arrivalGate': 'https://schema.org/arrivalGate',
	/** The platform where the train arrives. */
	'arrivalPlatform': 'https://schema.org/arrivalPlatform',
	/** The station where the train trip ends. */
	'arrivalStation': 'https://schema.org/arrivalStation',
	/** Identifier of the flight's arrival terminal. */
	'arrivalTerminal': 'https://schema.org/arrivalTerminal',
	/** The expected arrival time. */
	'arrivalTime': 'https://schema.org/arrivalTime',
	/** The number of copies when multiple copies of a piece of artwork are produced - e.g. for a limited edition of 20 prints, 'artEdition' refers to the total number of copies (in this example "20"). */
	'artEdition': 'https://schema.org/artEdition',
	/** The material used. (E.g. Oil, Watercolour, Acrylic, Linoprint, Marble, Cyanotype, Digital, Lithograph, DryPoint, Intaglio, Pastel, Woodcut, Pencil, Mixed Media, etc.) */
	'artMedium': 'https://schema.org/artMedium',
	/** The branches that comprise the arterial structure. */
	'arterialBranch': 'https://schema.org/arterialBranch',
	/** e.g. Painting, Drawing, Sculpture, Print, Photograph, Assemblage, Collage, etc. */
	'artform': 'https://schema.org/artform',
	/** The actual body of the article. */
	'articleBody': 'https://schema.org/articleBody',
	/** Articles may belong to one or more 'sections' in a magazine or newspaper, such as Sports, Lifestyle, etc. */
	'articleSection': 'https://schema.org/articleSection',
	/** The primary artist for a work
    	in a medium other than pencils or digital line art--for example, if the
    	primary artwork is done in watercolors or digital paints. */
	'artist': 'https://schema.org/artist',
	/** The supporting materials for the artwork, e.g. Canvas, Paper, Wood, Board, etc. */
	'artworkSurface': 'https://schema.org/artworkSurface',
	/** An Amazon Standard Identification Number (ASIN) is a 10-character alphanumeric unique identifier assigned by Amazon.com and its partners for product identification within the Amazon organization (summary from [Wikipedia](https://en.wikipedia.org/wiki/Amazon_Standard_Identification_Number)'s article).

Note also that this is a definition for how to include ASINs in Schema.org data, and not a definition of ASINs in general - see documentation from Amazon for authoritative details.
ASINs are most commonly encoded as text strings, but the [asin] property supports URL/URI as potential values too. */
	'asin': 'https://schema.org/asin',
	/** An aspect of medical practice that is considered on the page, such as 'diagnosis', 'treatment', 'causes', 'prognosis', 'etiology', 'epidemiology', etc. */
	'aspect': 'https://schema.org/aspect',
	/** Library file name, e.g., mscorlib.dll, system.web.dll. */
	'assembly': 'https://schema.org/assembly',
	/** Associated product/technology version. E.g., .NET Framework 4.5. */
	'assemblyVersion': 'https://schema.org/assemblyVersion',
	/** The item being described is intended to assess the competency or learning outcome defined by the referenced term. */
	'assesses': 'https://schema.org/assesses',
	/** The anatomy of the underlying organ system or structures associated with this entity. */
	'associatedAnatomy': 'https://schema.org/associatedAnatomy',
	/** A NewsArticle associated with the Media Object. */
	'associatedArticle': 'https://schema.org/associatedArticle',
	/** An associated [[ClaimReview]], related by specific common content, topic or claim. The expectation is that this property would be most typically used in cases where a single activity is conducting both claim reviews and media reviews, in which case [[relatedMediaReview]] would commonly be used on a [[ClaimReview]], while [[relatedClaimReview]] would be used on [[MediaReview]]. */
	'associatedClaimReview': 'https://schema.org/associatedClaimReview',
	/** Disease associated to this BioChemEntity. Such disease can be a MedicalCondition or a URL. If you want to add an evidence supporting the association, please use PropertyValue. */
	'associatedDisease': 'https://schema.org/associatedDisease',
	/** A media object that encodes this CreativeWork. This property is a synonym for encoding. */
	'associatedMedia': 'https://schema.org/associatedMedia',
	/** An associated [[MediaReview]], related by specific common content, topic or claim. The expectation is that this property would be most typically used in cases where a single activity is conducting both claim reviews and media reviews, in which case [[relatedMediaReview]] would commonly be used on a [[ClaimReview]], while [[relatedClaimReview]] would be used on [[MediaReview]]. */
	'associatedMediaReview': 'https://schema.org/associatedMediaReview',
	/** If applicable, a description of the pathophysiology associated with the anatomical system, including potential abnormal changes in the mechanical, physical, and biochemical functions of the system. */
	'associatedPathophysiology': 'https://schema.org/associatedPathophysiology',
	/** An associated [[Review]]. */
	'associatedReview': 'https://schema.org/associatedReview',
	/** A person that acts as performing member of a sports team; a player as opposed to a coach. */
	'athlete': 'https://schema.org/athlete',
	/** A person or organization attending the event. */
	'attendee': 'https://schema.org/attendee',
	/** A person attending the event. */
	'attendees': 'https://schema.org/attendees',
	/** An intended audience, i.e. a group for whom something was created. */
	'audience': 'https://schema.org/audience',
	/** The target group associated with a given audience (e.g. veterans, car owners, musicians, etc.). */
	'audienceType': 'https://schema.org/audienceType',
	/** An embedded audio object. */
	'audio': 'https://schema.org/audio',
	/** The Organization responsible for authenticating the user's subscription. For example, many media apps require a cable/satellite provider to authenticate your subscription before playing media. */
	'authenticator': 'https://schema.org/authenticator',
	/** The author of this content or rating. Please note that author is special in that HTML 5 provides a special mechanism for indicating authorship via the rel tag. That is equivalent to this and may be used interchangeably. */
	'author': 'https://schema.org/author',
	/** The availability of this item&#x2014;for example In stock, Out of stock, Pre-order, etc. */
	'availability': 'https://schema.org/availability',
	/** The end of the availability of the product or service included in the offer. */
	'availabilityEnds': 'https://schema.org/availabilityEnds',
	/** The beginning of the availability of the product or service included in the offer. */
	'availabilityStarts': 'https://schema.org/availabilityStarts',
	/** The place(s) from which the offer can be obtained (e.g. store locations). */
	'availableAtOrFrom': 'https://schema.org/availableAtOrFrom',
	/** A means of accessing the service (e.g. a phone bank, a web site, a location, etc.). */
	'availableChannel': 'https://schema.org/availableChannel',
	/** The delivery method(s) available for this offer. */
	'availableDeliveryMethod': 'https://schema.org/availableDeliveryMethod',
	/** When the item is available for pickup from the store, locker, etc. */
	'availableFrom': 'https://schema.org/availableFrom',
	/** The location in which the strength is available. */
	'availableIn': 'https://schema.org/availableIn',
	/** A language someone may use with or at the item, service or place. Please use one of the language codes from the [IETF BCP 47 standard](http://tools.ietf.org/html/bcp47). See also [[inLanguage]]. */
	'availableLanguage': 'https://schema.org/availableLanguage',
	/** Device required to run the application. Used in cases where a specific make/model is required to run the application. */
	'availableOnDevice': 'https://schema.org/availableOnDevice',
	/** A medical service available from this provider. */
	'availableService': 'https://schema.org/availableService',
	/** An available dosage strength for the drug. */
	'availableStrength': 'https://schema.org/availableStrength',
	/** A diagnostic test or procedure offered by this lab. */
	'availableTest': 'https://schema.org/availableTest',
	/** After this date, the item will no longer be available for pickup. */
	'availableThrough': 'https://schema.org/availableThrough',
	/** An award won by or for this item. */
	'award': 'https://schema.org/award',
	/** Awards won by or for this item. */
	'awards': 'https://schema.org/awards',
	/** The away team in a sports event. */
	'awayTeam': 'https://schema.org/awayTeam',
	/** For an [[Article]], typically a [[NewsArticle]], the backstory property provides a textual summary giving a brief explanation of why and how an article was created. In a journalistic setting this could include information about reporting process, methods, interviews, data sources, etc. */
	'backstory': 'https://schema.org/backstory',
	/** The type of a bank account. */
	'bankAccountType': 'https://schema.org/bankAccountType',
	/** The base salary of the job or of an employee in an EmployeeRole. */
	'baseSalary': 'https://schema.org/baseSalary',
	/** A sub property of recipient. The recipient blind copied on a message. */
	'bccRecipient': 'https://schema.org/bccRecipient',
	/** The type of bed or beds included in the accommodation. For the single case of just one bed of a certain type, you use bed directly with a text.
      If you want to indicate the quantity of a certain kind of bed, use an instance of BedDetails. For more detailed information, use the amenityFeature property. */
	'bed': 'https://schema.org/bed',
	/** A media object representing the circumstances before performing this direction. */
	'beforeMedia': 'https://schema.org/beforeMedia',
	/** A bank or bank’s branch, financial institution or international financial institution operating the beneficiary’s bank account or releasing funds for the beneficiary. */
	'beneficiaryBank': 'https://schema.org/beneficiaryBank',
	/** Description of benefits associated with the job. */
	'benefits': 'https://schema.org/benefits',
	/** The URL that goes directly to the summary of benefits and coverage for the specific standard plan or plan variation. */
	'benefitsSummaryUrl': 'https://schema.org/benefitsSummaryUrl',
	/** The highest value allowed in this rating system. If bestRating is omitted, 5 is assumed. */
	'bestRating': 'https://schema.org/bestRating',
	/** The billing address for the order. */
	'billingAddress': 'https://schema.org/billingAddress',
	/** Specifies for how long this price (or price component) will be billed. Can be used, for example, to model the contractual duration of a subscription or payment plan. Type can be either a Duration or a Number (in which case the unit of measurement, for example month, is specified by the unitCode property). */
	'billingDuration': 'https://schema.org/billingDuration',
	/** This property specifies the minimal quantity and rounding increment that will be the basis for the billing. The unit of measurement is specified by the unitCode property. */
	'billingIncrement': 'https://schema.org/billingIncrement',
	/** The time interval used to compute the invoice. */
	'billingPeriod': 'https://schema.org/billingPeriod',
	/** Specifies after how much time this price (or price component) becomes valid and billing starts. Can be used, for example, to model a price increase after the first year of a subscription. The unit of measurement is specified by the unitCode property. */
	'billingStart': 'https://schema.org/billingStart',
	/** A BioChemEntity that is known to interact with this item. */
	'bioChemInteraction': 'https://schema.org/bioChemInteraction',
	/** A similar BioChemEntity, e.g., obtained by fingerprint similarity algorithms. */
	'bioChemSimilarity': 'https://schema.org/bioChemSimilarity',
	/** A role played by the BioChemEntity within a biological context. */
	'biologicalRole': 'https://schema.org/biologicalRole',
	/** The biomechanical properties of the bone. */
	'biomechnicalClass': 'https://schema.org/biomechnicalClass',
	/** Date of birth. */
	'birthDate': 'https://schema.org/birthDate',
	/** The place where the person was born. */
	'birthPlace': 'https://schema.org/birthPlace',
	/** The bitrate of the media object. */
	'bitrate': 'https://schema.org/bitrate',
	/** A posting that is part of this blog. */
	'blogPost': 'https://schema.org/blogPost',
	/** Indicates a post that is part of a [[Blog]]. Note that historically, what we term a "Blog" was once known as a "weblog", and that what we term a "BlogPosting" is now often colloquially referred to as a "blog". */
	'blogPosts': 'https://schema.org/blogPosts',
	/** The blood vessel that carries blood from the heart to the muscle. */
	'bloodSupply': 'https://schema.org/bloodSupply',
	/** The airline-specific indicator of boarding order / preference. */
	'boardingGroup': 'https://schema.org/boardingGroup',
	/** The type of boarding policy used by the airline (e.g. zone-based or group-based). */
	'boardingPolicy': 'https://schema.org/boardingPolicy',
	/** Location in the body of the anatomical structure. */
	'bodyLocation': 'https://schema.org/bodyLocation',
	/** Indicates the design and body style of the vehicle (e.g. station wagon, hatchback, etc.). */
	'bodyType': 'https://schema.org/bodyType',
	/** The edition of the book. */
	'bookEdition': 'https://schema.org/bookEdition',
	/** The format of the book. */
	'bookFormat': 'https://schema.org/bookFormat',
	/** 'bookingAgent' is an out-dated term indicating a 'broker' that serves as a booking agent. */
	'bookingAgent': 'https://schema.org/bookingAgent',
	/** The date and time the reservation was booked. */
	'bookingTime': 'https://schema.org/bookingTime',
	/** A sub property of participant. The person that borrows the object being lent. */
	'borrower': 'https://schema.org/borrower',
	/** A box is the area enclosed by the rectangle formed by two points. The first point is the lower corner, the second point is the upper corner. A box is expressed as two points separated by a space character. */
	'box': 'https://schema.org/box',
	/** The branches that delineate from the nerve bundle. Not to be confused with [[branchOf]]. */
	'branch': 'https://schema.org/branch',
	/** A short textual code (also called "store code") that uniquely identifies a place of business. The code is typically assigned by the parentOrganization and used in structured URLs.\n\nFor example, in the URL http://www.starbucks.co.uk/store-locator/etc/detail/3047 the code "3047" is a branchCode for a particular branch.
       */
	'branchCode': 'https://schema.org/branchCode',
	/** The larger organization that this local business is a branch of, if any. Not to be confused with (anatomical) [[branch]]. */
	'branchOf': 'https://schema.org/branchOf',
	/** The brand(s) associated with a product or service, or the brand(s) maintained by an organization or business person. */
	'brand': 'https://schema.org/brand',
	/** A set of links that can help a user understand and navigate a website hierarchy. */
	'breadcrumb': 'https://schema.org/breadcrumb',
	/** Any precaution, guidance, contraindication, etc. related to this drug's use by breastfeeding mothers. */
	'breastfeedingWarning': 'https://schema.org/breastfeedingWarning',
	/** The media network(s) whose content is broadcast on this station. */
	'broadcastAffiliateOf': 'https://schema.org/broadcastAffiliateOf',
	/** The unique address by which the BroadcastService can be identified in a provider lineup. In US, this is typically a number. */
	'broadcastChannelId': 'https://schema.org/broadcastChannelId',
	/** The name displayed in the channel guide. For many US affiliates, it is the network name. */
	'broadcastDisplayName': 'https://schema.org/broadcastDisplayName',
	/** The frequency used for over-the-air broadcasts. Numeric values or simple ranges, e.g. 87-99. In addition a shortcut idiom is supported for frequences of AM and FM radio channels, e.g. "87 FM". */
	'broadcastFrequency': 'https://schema.org/broadcastFrequency',
	/** The frequency in MHz for a particular broadcast. */
	'broadcastFrequencyValue': 'https://schema.org/broadcastFrequencyValue',
	/** The event being broadcast such as a sporting event or awards ceremony. */
	'broadcastOfEvent': 'https://schema.org/broadcastOfEvent',
	/** The type of service required to have access to the channel (e.g. Standard or Premium). */
	'broadcastServiceTier': 'https://schema.org/broadcastServiceTier',
	/** The modulation (e.g. FM, AM, etc) used by a particular broadcast service. */
	'broadcastSignalModulation': 'https://schema.org/broadcastSignalModulation',
	/** The subchannel used for the broadcast. */
	'broadcastSubChannel': 'https://schema.org/broadcastSubChannel',
	/** The timezone in [ISO 8601 format](http://en.wikipedia.org/wiki/ISO_8601) for which the service bases its broadcasts. */
	'broadcastTimezone': 'https://schema.org/broadcastTimezone',
	/** The organization owning or operating the broadcast service. */
	'broadcaster': 'https://schema.org/broadcaster',
	/** An entity that arranges for an exchange between a buyer and a seller.  In most cases a broker never acquires or releases ownership of a product or service involved in an exchange.  If it is not clear whether an entity is a broker, seller, or buyer, the latter two terms are preferred. */
	'broker': 'https://schema.org/broker',
	/** Specifies browser requirements in human-readable text. For example, 'requires HTML5 support'. */
	'browserRequirements': 'https://schema.org/browserRequirements',
	/** The name of the bus (e.g. Bolt Express). */
	'busName': 'https://schema.org/busName',
	/** The unique identifier for the bus. */
	'busNumber': 'https://schema.org/busNumber',
	/** Days of the week when the merchant typically operates, indicated via opening hours markup. */
	'businessDays': 'https://schema.org/businessDays',
	/** The business function (e.g. sell, lease, repair, dispose) of the offer or component of a bundle (TypeAndQuantityNode). The default is http://purl.org/goodrelations/v1#Sell. */
	'businessFunction': 'https://schema.org/businessFunction',
	/** A sub property of participant. The participant/person/organization that bought the object. */
	'buyer': 'https://schema.org/buyer',
	/** The artist that performed this album or recording. */
	'byArtist': 'https://schema.org/byArtist',
	/** Defines the day(s) of the week on which a recurring [[Event]] takes place. May be specified using either [[DayOfWeek]], or alternatively [[Text]] conforming to iCal's syntax for byDay recurrence rules. */
	'byDay': 'https://schema.org/byDay',
	/** Defines the month(s) of the year on which a recurring [[Event]] takes place. Specified as an [[Integer]] between 1-12. January is 1. */
	'byMonth': 'https://schema.org/byMonth',
	/** Defines the day(s) of the month on which a recurring [[Event]] takes place. Specified as an [[Integer]] between 1-31. */
	'byMonthDay': 'https://schema.org/byMonthDay',
	/** Defines the week(s) of the month on which a recurring Event takes place. Specified as an Integer between 1-5. For clarity, byMonthWeek is best used in conjunction with byDay to indicate concepts like the first and third Mondays of a month. */
	'byMonthWeek': 'https://schema.org/byMonthWeek',
	/** A [callsign](https://en.wikipedia.org/wiki/Call_sign), as used in broadcasting and radio communications to identify people, radio and TV stations, or vehicles. */
	'callSign': 'https://schema.org/callSign',
	/** The number of calories. */
	'calories': 'https://schema.org/calories',
	/** A sub property of object. The candidate subject of this action. */
	'candidate': 'https://schema.org/candidate',
	/** The caption for this object. For downloadable machine formats (closed caption, subtitles etc.) use MediaObject and indicate the [[encodingFormat]]. */
	'caption': 'https://schema.org/caption',
	/** The number of grams of carbohydrates. */
	'carbohydrateContent': 'https://schema.org/carbohydrateContent',
	/** The available volume for cargo or luggage. For automobiles, this is usually the trunk volume.\n\nTypical unit code(s): LTR for liters, FTQ for cubic foot/feet\n\nNote: You can use [[minValue]] and [[maxValue]] to indicate ranges. */
	'cargoVolume': 'https://schema.org/cargoVolume',
	/** 'carrier' is an out-dated term indicating the 'provider' for parcel delivery and flights. */
	'carrier': 'https://schema.org/carrier',
	/** Specifies specific carrier(s) requirements for the application (e.g. an application may only work on a specific carrier network). */
	'carrierRequirements': 'https://schema.org/carrierRequirements',
	/** A cardholder benefit that pays the cardholder a small percentage of their net expenditures. */
	'cashBack': 'https://schema.org/cashBack',
	/** A data catalog which contains this dataset. */
	'catalog': 'https://schema.org/catalog',
	/** The catalog number for the release. */
	'catalogNumber': 'https://schema.org/catalogNumber',
	/** A category for the item. Greater signs or slashes can be used to informally indicate a category hierarchy. */
	'category': 'https://schema.org/category',
	/** The condition, complication, symptom, sign, etc. caused. */
	'causeOf': 'https://schema.org/causeOf',
	/** A sub property of recipient. The recipient copied on a message. */
	'ccRecipient': 'https://schema.org/ccRecipient',
	/** Fictional person connected with a creative work. */
	'character': 'https://schema.org/character',
	/** A piece of data that represents a particular aspect of a fictional character (skill, power, character points, advantage, disadvantage). */
	'characterAttribute': 'https://schema.org/characterAttribute',
	/** The name of a character played in some acting or performing role, i.e. in a PerformanceRole. */
	'characterName': 'https://schema.org/characterName',
	/** Cheat codes to the game. */
	'cheatCode': 'https://schema.org/cheatCode',
	/** The earliest someone may check into a lodging establishment. */
	'checkinTime': 'https://schema.org/checkinTime',
	/** A URL template (RFC 6570) for a checkout page for an offer. This approach allows merchants to specify a URL for online checkout of the offered product, by interpolating parameters such as the logged in user ID, product ID, quantity, discount code etc. Parameter naming and standardization are not specified here. */
	'checkoutPageURLTemplate': 'https://schema.org/checkoutPageURLTemplate',
	/** The latest someone may check out of a lodging establishment. */
	'checkoutTime': 'https://schema.org/checkoutTime',
	/** The chemical composition describes the identity and relative ratio of the chemical elements that make up the substance. */
	'chemicalComposition': 'https://schema.org/chemicalComposition',
	/** A role played by the BioChemEntity within a chemical context. */
	'chemicalRole': 'https://schema.org/chemicalRole',
	/** Maximal age of the child. */
	'childMaxAge': 'https://schema.org/childMaxAge',
	/** Minimal age of the child. */
	'childMinAge': 'https://schema.org/childMinAge',
	/** Closest child taxa of the taxon in question. */
	'childTaxon': 'https://schema.org/childTaxon',
	/** A child of the person. */
	'children': 'https://schema.org/children',
	/** The number of milligrams of cholesterol. */
	'cholesterolContent': 'https://schema.org/cholesterolContent',
	/** A circle is the circular region of a specified radius centered at a specified latitude and longitude. A circle is expressed as a pair followed by a radius in meters. */
	'circle': 'https://schema.org/circle',
	/** A citation or reference to another creative work, such as another publication, web page, scholarly article, etc. */
	'citation': 'https://schema.org/citation',
	/** For a [[Claim]] interpreted from [[MediaObject]] content
    sed to indicate a claim contained, implied or refined from the content of a [[MediaObject]]. */
	'claimInterpreter': 'https://schema.org/claimInterpreter',
	/** A short summary of the specific claims reviewed in a ClaimReview. */
	'claimReviewed': 'https://schema.org/claimReviewed',
	/** Description of the absorption and elimination of drugs, including their concentration (pharmacokinetics, pK) and biological effects (pharmacodynamics, pD). */
	'clincalPharmacology': 'https://schema.org/clincalPharmacology',
	/** Description of the absorption and elimination of drugs, including their concentration (pharmacokinetics, pK) and biological effects (pharmacodynamics, pD). */
	'clinicalPharmacology': 'https://schema.org/clinicalPharmacology',
	/** Position of the clip within an ordered group of clips. */
	'clipNumber': 'https://schema.org/clipNumber',
	/** The closing hour of the place or service on the given day(s) of the week. */
	'closes': 'https://schema.org/closes',
	/** A person that acts in a coaching role for a sports team. */
	'coach': 'https://schema.org/coach',
	/** A medical code for the entity, taken from a controlled vocabulary or ontology such as ICD-9, DiseasesDB, MeSH, SNOMED-CT, RxNorm, etc. */
	'code': 'https://schema.org/code',
	/** Link to the repository where the un-compiled, human readable code and related code is located (SVN, GitHub, CodePlex). */
	'codeRepository': 'https://schema.org/codeRepository',
	/** What type of code sample: full (compile ready) solution, code snippet, inline code, scripts, template. */
	'codeSampleType': 'https://schema.org/codeSampleType',
	/** A short textual code that uniquely identifies the value. */
	'codeValue': 'https://schema.org/codeValue',
	/** The coding system, e.g. 'ICD-10'. */
	'codingSystem': 'https://schema.org/codingSystem',
	/** A colleague of the person. */
	'colleague': 'https://schema.org/colleague',
	/** A colleague of the person. */
	'colleagues': 'https://schema.org/colleagues',
	/** A sub property of object. The collection target of the action. */
	'collection': 'https://schema.org/collection',
	/** The number of items in the [[Collection]]. */
	'collectionSize': 'https://schema.org/collectionSize',
	/** The color of the product. */
	'color': 'https://schema.org/color',
	/** The individual who adds color to inked drawings. */
	'colorist': 'https://schema.org/colorist',
	/** Comments, typically from users. */
	'comment': 'https://schema.org/comment',
	/** The number of comments this CreativeWork (e.g. Article, Question or Answer) has received. This is most applicable to works published in Web sites with commenting system; additional comments may exist elsewhere. */
	'commentCount': 'https://schema.org/commentCount',
	/** The text of the UserComment. */
	'commentText': 'https://schema.org/commentText',
	/** The time at which the UserComment was made. */
	'commentTime': 'https://schema.org/commentTime',
	/** Knowledge, skill, ability or personal attribute that must be demonstrated by a person or other entity in order to do something such as earn an Educational Occupational Credential or understand a LearningResource. */
	'competencyRequired': 'https://schema.org/competencyRequired',
	/** A competitor in a sports event. */
	'competitor': 'https://schema.org/competitor',
	/** The person or organization who wrote a composition, or who is the composer of a work performed at some event. */
	'composer': 'https://schema.org/composer',
	/** Specifying something physically contained by something else. Typically used here for the underlying anatomical structures, such as organs, that comprise the anatomical system. */
	'comprisedOf': 'https://schema.org/comprisedOf',
	/** Conditions that affect the availability of, or method(s) of access to, an item. Typically used for real world items such as an [[ArchiveComponent]] held by an [[ArchiveOrganization]]. This property is not suitable for use as a general Web access control mechanism. It is expressed only in natural language.\n\nFor example "Available by appointment from the Reading Room" or "Accessible only from logged-in accounts ".  */
	'conditionsOfAccess': 'https://schema.org/conditionsOfAccess',
	/** A number that confirms the given order or payment has been received. */
	'confirmationNumber': 'https://schema.org/confirmationNumber',
	/** Other anatomical structures to which this structure is connected. */
	'connectedTo': 'https://schema.org/connectedTo',
	/** Indicates a property used as a constraint. For example, in the definition of a [[StatisticalVariable]]. The value is a property, either from within Schema.org or from other compatible (e.g. RDF) systems such as DataCommons.org or Wikidata.org.  */
	'constraintProperty': 'https://schema.org/constraintProperty',
	/** An option available on this contact point (e.g. a toll-free number or support for hearing-impaired callers). */
	'contactOption': 'https://schema.org/contactOption',
	/** A contact point for a person or organization. */
	'contactPoint': 'https://schema.org/contactPoint',
	/** A contact point for a person or organization. */
	'contactPoints': 'https://schema.org/contactPoints',
	/** A person or organization can have different contact points, for different purposes. For example, a sales contact point, a PR contact point and so on. This property is used to specify the kind of contact point. */
	'contactType': 'https://schema.org/contactType',
	/** A secure method for consumers to purchase products or services via debit, credit or smartcards by using RFID or NFC technology. */
	'contactlessPayment': 'https://schema.org/contactlessPayment',
	/** The basic containment relation between a place and one that contains it. */
	'containedIn': 'https://schema.org/containedIn',
	/** The basic containment relation between a place and one that contains it. */
	'containedInPlace': 'https://schema.org/containedInPlace',
	/** The basic containment relation between a place and another that it contains. */
	'containsPlace': 'https://schema.org/containsPlace',
	/** A season that is part of the media series. */
	'containsSeason': 'https://schema.org/containsSeason',
	/** The location depicted or described in the content. For example, the location in a photograph or painting. */
	'contentLocation': 'https://schema.org/contentLocation',
	/** Official rating of a piece of content&#x2014;for example, 'MPAA PG-13'. */
	'contentRating': 'https://schema.org/contentRating',
	/** The specific time described by a creative work, for works (e.g. articles, video objects etc.) that emphasise a particular moment within an Event. */
	'contentReferenceTime': 'https://schema.org/contentReferenceTime',
	/** File size in (mega/kilo)bytes. */
	'contentSize': 'https://schema.org/contentSize',
	/** The supported content type(s) for an EntryPoint response. */
	'contentType': 'https://schema.org/contentType',
	/** Actual bytes of the media object, for example the image file or video file. */
	'contentUrl': 'https://schema.org/contentUrl',
	/** A contraindication for this therapy. */
	'contraindication': 'https://schema.org/contraindication',
	/** A secondary contributor to the CreativeWork or Event. */
	'contributor': 'https://schema.org/contributor',
	/** The time it takes to actually cook the dish, in [ISO 8601 duration format](http://en.wikipedia.org/wiki/ISO_8601). */
	'cookTime': 'https://schema.org/cookTime',
	/** The method of cooking, such as Frying, Steaming, ... */
	'cookingMethod': 'https://schema.org/cookingMethod',
	/** The party holding the legal copyright to the CreativeWork. */
	'copyrightHolder': 'https://schema.org/copyrightHolder',
	/** Text of a notice appropriate for describing the copyright aspects of this Creative Work, ideally indicating the owner of the copyright for the Work. */
	'copyrightNotice': 'https://schema.org/copyrightNotice',
	/** The year during which the claimed copyright for the CreativeWork was first asserted. */
	'copyrightYear': 'https://schema.org/copyrightYear',
	/** Indicates a correction to a [[CreativeWork]], either via a [[CorrectionComment]], textually or in another document. */
	'correction': 'https://schema.org/correction',
	/** For an [[Organization]] (e.g. [[NewsMediaOrganization]]), a statement describing (in news media, the newsroom’s) disclosure and correction policy for errors. */
	'correctionsPolicy': 'https://schema.org/correctionsPolicy',
	/** The category of cost, such as wholesale, retail, reimbursement cap, etc. */
	'costCategory': 'https://schema.org/costCategory',
	/** The currency (in 3-letter) of the drug cost. See: http://en.wikipedia.org/wiki/ISO_4217.  */
	'costCurrency': 'https://schema.org/costCurrency',
	/** Additional details to capture the origin of the cost data. For example, 'Medicare Part B'. */
	'costOrigin': 'https://schema.org/costOrigin',
	/** The cost per unit of the drug. */
	'costPerUnit': 'https://schema.org/costPerUnit',
	/** Countries for which the application is not supported. You can also provide the two-letter ISO 3166-1 alpha-2 country code. */
	'countriesNotSupported': 'https://schema.org/countriesNotSupported',
	/** Countries for which the application is supported. You can also provide the two-letter ISO 3166-1 alpha-2 country code. */
	'countriesSupported': 'https://schema.org/countriesSupported',
	/** The place where the product was assembled. */
	'countryOfAssembly': 'https://schema.org/countryOfAssembly',
	/** The place where the item (typically [[Product]]) was last processed and tested before importation. */
	'countryOfLastProcessing': 'https://schema.org/countryOfLastProcessing',
	/** The country of origin of something, including products as well as creative  works such as movie and TV content.

In the case of TV and movie, this would be the country of the principle offices of the production company or individual responsible for the movie. For other kinds of [[CreativeWork]] it is difficult to provide fully general guidance, and properties such as [[contentLocation]] and [[locationCreated]] may be more applicable.

In the case of products, the country of origin of the product. The exact interpretation of this may vary by context and product type, and cannot be fully enumerated here. */
	'countryOfOrigin': 'https://schema.org/countryOfOrigin',
	/** A sub property of location. The course where this action was taken. */
	'course': 'https://schema.org/course',
	/** The identifier for the [[Course]] used by the course [[provider]] (e.g. CS101 or 6.001). */
	'courseCode': 'https://schema.org/courseCode',
	/** The medium or means of delivery of the course instance or the mode of study, either as a text label (e.g. "online", "onsite" or "blended"; "synchronous" or "asynchronous"; "full-time" or "part-time") or as a URL reference to a term from a controlled vocabulary (e.g. https://ceds.ed.gov/element/001311#Asynchronous). */
	'courseMode': 'https://schema.org/courseMode',
	/** Requirements for taking the Course. May be completion of another [[Course]] or a textual description like "permission of instructor". Requirements may be a pre-requisite competency, referenced using [[AlignmentObject]]. */
	'coursePrerequisites': 'https://schema.org/coursePrerequisites',
	/** Represents the length and pace of a course, expressed as a [[Schedule]]. */
	'courseSchedule': 'https://schema.org/courseSchedule',
	/** The amount of work expected of students taking the course, often provided as a figure per week or per month, and may be broken down by type. For example, "2 hours of lectures, 1 hour of lab work and 3 hours of independent study per week". */
	'courseWorkload': 'https://schema.org/courseWorkload',
	/** The time when the live blog will stop covering the Event. Note that coverage may continue after the Event concludes. */
	'coverageEndTime': 'https://schema.org/coverageEndTime',
	/** The time when the live blog will begin covering the Event. Note that coverage may begin before the Event's start time. The LiveBlogPosting may also be created before coverage begins. */
	'coverageStartTime': 'https://schema.org/coverageStartTime',
	/** The status of a creative work in terms of its stage in a lifecycle. Example terms include Incomplete, Draft, Published, Obsolete. Some organizations define a set of terms for the stages of their publication lifecycle. */
	'creativeWorkStatus': 'https://schema.org/creativeWorkStatus',
	/** The creator/author of this CreativeWork. This is the same as the Author property for CreativeWork. */
	'creator': 'https://schema.org/creator',
	/** The category or type of credential being described, for example "degree”, “certificate”, “badge”, or more specific term. */
	'credentialCategory': 'https://schema.org/credentialCategory',
	/** Text that can be used to credit person(s) and/or organization(s) associated with a published Creative Work. */
	'creditText': 'https://schema.org/creditText',
	/** The group the release is credited to if different than the byArtist. For example, Red and Blue is credited to "Stefani Germanotta Band", but by Lady Gaga. */
	'creditedTo': 'https://schema.org/creditedTo',
	/** A CSS selector, e.g. of a [[SpeakableSpecification]] or [[WebPageElement]]. In the latter case, multiple matches within a page can constitute a single conceptual "Web page element". */
	'cssSelector': 'https://schema.org/cssSelector',
	/** The currency accepted.\n\nUse standard formats: [ISO 4217 currency format](http://en.wikipedia.org/wiki/ISO_4217), e.g. "USD"; [Ticker symbol](https://en.wikipedia.org/wiki/List_of_cryptocurrencies) for cryptocurrencies, e.g. "BTC"; well known names for [Local Exchange Trading Systems](https://en.wikipedia.org/wiki/Local_exchange_trading_system) (LETS) and other currency types, e.g. "Ithaca HOUR". */
	'currenciesAccepted': 'https://schema.org/currenciesAccepted',
	/** The currency in which the monetary amount is expressed.\n\nUse standard formats: [ISO 4217 currency format](http://en.wikipedia.org/wiki/ISO_4217), e.g. "USD"; [Ticker symbol](https://en.wikipedia.org/wiki/List_of_cryptocurrencies) for cryptocurrencies, e.g. "BTC"; well known names for [Local Exchange Trading Systems](https://en.wikipedia.org/wiki/Local_exchange_trading_system) (LETS) and other currency types, e.g. "Ithaca HOUR". */
	'currency': 'https://schema.org/currency',
	/** The current price of a currency. */
	'currentExchangeRate': 'https://schema.org/currentExchangeRate',
	/** Party placing the order or paying the invoice. */
	'customer': 'https://schema.org/customer',
	/** The type of return fees if the product is returned due to customer remorse. */
	'customerRemorseReturnFees': 'https://schema.org/customerRemorseReturnFees',
	/** The method (from an enumeration) by which the customer obtains a return shipping label for a product returned due to customer remorse. */
	'customerRemorseReturnLabelSource': 'https://schema.org/customerRemorseReturnLabelSource',
	/** The amount of shipping costs if a product is returned due to customer remorse. Applicable when property [[customerRemorseReturnFees]] equals [[ReturnShippingFees]]. */
	'customerRemorseReturnShippingFeesAmount': 'https://schema.org/customerRemorseReturnShippingFeesAmount',
	/** Order cutoff time allows merchants to describe the time after which they will no longer process orders received on that day. For orders processed after cutoff time, one day gets added to the delivery time estimate. This property is expected to be most typically used via the [[ShippingRateSettings]] publication pattern. The time is indicated using the ISO-8601 Time format, e.g. "23:30:00-05:00" would represent 6:30 pm Eastern Standard Time (EST) which is 5 hours behind Coordinated Universal Time (UTC). */
	'cutoffTime': 'https://schema.org/cutoffTime',
	/** collectiondate - Date for which patient counts are reported. */
	'cvdCollectionDate': 'https://schema.org/cvdCollectionDate',
	/** Name of the County of the NHSN facility that this data record applies to. Use [[cvdFacilityId]] to identify the facility. To provide other details, [[healthcareReportingData]] can be used on a [[Hospital]] entry. */
	'cvdFacilityCounty': 'https://schema.org/cvdFacilityCounty',
	/** Identifier of the NHSN facility that this data record applies to. Use [[cvdFacilityCounty]] to indicate the county. To provide other details, [[healthcareReportingData]] can be used on a [[Hospital]] entry. */
	'cvdFacilityId': 'https://schema.org/cvdFacilityId',
	/** numbeds - HOSPITAL INPATIENT BEDS: Inpatient beds, including all staffed, licensed, and overflow (surge) beds used for inpatients. */
	'cvdNumBeds': 'https://schema.org/cvdNumBeds',
	/** numbedsocc - HOSPITAL INPATIENT BED OCCUPANCY: Total number of staffed inpatient beds that are occupied. */
	'cvdNumBedsOcc': 'https://schema.org/cvdNumBedsOcc',
	/** numc19died - DEATHS: Patients with suspected or confirmed COVID-19 who died in the hospital, ED, or any overflow location. */
	'cvdNumC19Died': 'https://schema.org/cvdNumC19Died',
	/** numc19hopats - HOSPITAL ONSET: Patients hospitalized in an NHSN inpatient care location with onset of suspected or confirmed COVID-19 14 or more days after hospitalization. */
	'cvdNumC19HOPats': 'https://schema.org/cvdNumC19HOPats',
	/** numc19hosppats - HOSPITALIZED: Patients currently hospitalized in an inpatient care location who have suspected or confirmed COVID-19. */
	'cvdNumC19HospPats': 'https://schema.org/cvdNumC19HospPats',
	/** numc19mechventpats - HOSPITALIZED and VENTILATED: Patients hospitalized in an NHSN inpatient care location who have suspected or confirmed COVID-19 and are on a mechanical ventilator. */
	'cvdNumC19MechVentPats': 'https://schema.org/cvdNumC19MechVentPats',
	/** numc19ofmechventpats - ED/OVERFLOW and VENTILATED: Patients with suspected or confirmed COVID-19 who are in the ED or any overflow location awaiting an inpatient bed and on a mechanical ventilator. */
	'cvdNumC19OFMechVentPats': 'https://schema.org/cvdNumC19OFMechVentPats',
	/** numc19overflowpats - ED/OVERFLOW: Patients with suspected or confirmed COVID-19 who are in the ED or any overflow location awaiting an inpatient bed. */
	'cvdNumC19OverflowPats': 'https://schema.org/cvdNumC19OverflowPats',
	/** numicubeds - ICU BEDS: Total number of staffed inpatient intensive care unit (ICU) beds. */
	'cvdNumICUBeds': 'https://schema.org/cvdNumICUBeds',
	/** numicubedsocc - ICU BED OCCUPANCY: Total number of staffed inpatient ICU beds that are occupied. */
	'cvdNumICUBedsOcc': 'https://schema.org/cvdNumICUBedsOcc',
	/** numtotbeds - ALL HOSPITAL BEDS: Total number of all inpatient and outpatient beds, including all staffed, ICU, licensed, and overflow (surge) beds used for inpatients or outpatients. */
	'cvdNumTotBeds': 'https://schema.org/cvdNumTotBeds',
	/** numvent - MECHANICAL VENTILATORS: Total number of ventilators available. */
	'cvdNumVent': 'https://schema.org/cvdNumVent',
	/** numventuse - MECHANICAL VENTILATORS IN USE: Total number of ventilators in use. */
	'cvdNumVentUse': 'https://schema.org/cvdNumVentUse',
	/** An item within a data feed. Data feeds may have many elements. */
	'dataFeedElement': 'https://schema.org/dataFeedElement',
	/** A dataset contained in this catalog. */
	'dataset': 'https://schema.org/dataset',
	/** The range of temporal applicability of a dataset, e.g. for a 2011 census dataset, the year 2011 (in ISO 8601 time interval format). */
	'datasetTimeInterval': 'https://schema.org/datasetTimeInterval',
	/** The date on which the CreativeWork was created or the item was added to a DataFeed. */
	'dateCreated': 'https://schema.org/dateCreated',
	/** The datetime the item was removed from the DataFeed. */
	'dateDeleted': 'https://schema.org/dateDeleted',
	/** The date the ticket was issued. */
	'dateIssued': 'https://schema.org/dateIssued',
	/** The date on which the CreativeWork was most recently modified or when the item's entry was modified within a DataFeed. */
	'dateModified': 'https://schema.org/dateModified',
	/** Publication date of an online listing. */
	'datePosted': 'https://schema.org/datePosted',
	/** Date of first broadcast/publication. */
	'datePublished': 'https://schema.org/datePublished',
	/** The date/time at which the message has been read by the recipient if a single recipient exists. */
	'dateRead': 'https://schema.org/dateRead',
	/** The date/time the message was received if a single recipient exists. */
	'dateReceived': 'https://schema.org/dateReceived',
	/** The date/time at which the message was sent. */
	'dateSent': 'https://schema.org/dateSent',
	/** The date of the first registration of the vehicle with the respective public authorities. */
	'dateVehicleFirstRegistered': 'https://schema.org/dateVehicleFirstRegistered',
	/** A [dateline](https://en.wikipedia.org/wiki/Dateline) is a brief piece of text included in news articles that describes where and when the story was written or filed though the date is often omitted. Sometimes only a placename is provided.

Structured representations of dateline-related information can also be expressed more explicitly using [[locationCreated]] (which represents where a work was created, e.g. where a news report was written).  For location depicted or described in the content, use [[contentLocation]].

Dateline summaries are oriented more towards human readers than towards automated processing, and can vary substantially. Some examples: "BEIRUT, Lebanon, June 2.", "Paris, France", "December 19, 2017 11:43AM Reporting from Washington", "Beijing/Moscow", "QUEZON CITY, Philippines".
       */
	'dateline': 'https://schema.org/dateline',
	/** The day of the week for which these opening hours are valid. */
	'dayOfWeek': 'https://schema.org/dayOfWeek',
	/** Date of death. */
	'deathDate': 'https://schema.org/deathDate',
	/** The place where the person died. */
	'deathPlace': 'https://schema.org/deathPlace',
	/** The default value of the input.  For properties that expect a literal, the default is a literal value, for properties that expect an object, it's an ID reference to one of the current values. */
	'defaultValue': 'https://schema.org/defaultValue',
	/** Destination address. */
	'deliveryAddress': 'https://schema.org/deliveryAddress',
	/** The typical delay between the receipt of the order and the goods either leaving the warehouse or being prepared for pickup, in case the delivery method is on site pickup. */
	'deliveryLeadTime': 'https://schema.org/deliveryLeadTime',
	/** A sub property of instrument. The method of delivery. */
	'deliveryMethod': 'https://schema.org/deliveryMethod',
	/** New entry added as the package passes through each leg of its journey (from shipment to final delivery). */
	'deliveryStatus': 'https://schema.org/deliveryStatus',
	/** The total delay between the receipt of the order and the goods reaching the final customer. */
	'deliveryTime': 'https://schema.org/deliveryTime',
	/** A relationship between an organization and a department of that organization, also described as an organization (allowing different urls, logos, opening hours). For example: a store with a pharmacy, or a bakery with a cafe. */
	'department': 'https://schema.org/department',
	/** The airport where the flight originates. */
	'departureAirport': 'https://schema.org/departureAirport',
	/** The terminal or port from which the boat departs. */
	'departureBoatTerminal': 'https://schema.org/departureBoatTerminal',
	/** The stop or station from which the bus departs. */
	'departureBusStop': 'https://schema.org/departureBusStop',
	/** Identifier of the flight's departure gate. */
	'departureGate': 'https://schema.org/departureGate',
	/** The platform from which the train departs. */
	'departurePlatform': 'https://schema.org/departurePlatform',
	/** The station from which the train departs. */
	'departureStation': 'https://schema.org/departureStation',
	/** Identifier of the flight's departure terminal. */
	'departureTerminal': 'https://schema.org/departureTerminal',
	/** The expected departure time. */
	'departureTime': 'https://schema.org/departureTime',
	/** Prerequisites needed to fulfill steps in article. */
	'dependencies': 'https://schema.org/dependencies',
	/** The depth of the item. */
	'depth': 'https://schema.org/depth',
	/** A description of the item. */
	'description': 'https://schema.org/description',
	/** Device required to run the application. Used in cases where a specific make/model is required to run the application. */
	'device': 'https://schema.org/device',
	/** One or more alternative conditions considered in the differential diagnosis process as output of a diagnosis process. */
	'diagnosis': 'https://schema.org/diagnosis',
	/** An image containing a diagram that illustrates the structure and/or its component substructures and/or connections with other structures. */
	'diagram': 'https://schema.org/diagram',
	/** A sub property of instrument. The diet used in this action. */
	'diet': 'https://schema.org/diet',
	/** Nutritional information specific to the dietary plan. May include dietary recommendations on what foods to avoid, what foods to consume, and specific alterations/deviations from the USDA or other regulatory body's approved dietary guidelines. */
	'dietFeatures': 'https://schema.org/dietFeatures',
	/** One of a set of differential diagnoses for the condition. Specifically, a closely-related or competing diagnosis typically considered later in the cognitive process whereby this medical condition is distinguished from others most likely responsible for a similar collection of signs and symptoms to reach the most parsimonious diagnosis or diagnoses in a patient. */
	'differentialDiagnosis': 'https://schema.org/differentialDiagnosis',
	/** Indicates whether an [[url]] that is associated with a [[JobPosting]] enables direct application for the job, via the posting website. A job posting is considered to have directApply of [[True]] if an application process for the specified job can be directly initiated via the url(s) given (noting that e.g. multiple internet domains might nevertheless be involved at an implementation level). A value of [[False]] is appropriate if there is no clear path to applying directly online for the specified job, navigating directly from the JobPosting url(s) supplied. */
	'directApply': 'https://schema.org/directApply',
	/** A director of e.g. TV, radio, movie, video gaming etc. content, or of an event. Directors can be associated with individual items or with a series, episode, clip. */
	'director': 'https://schema.org/director',
	/** A director of e.g. TV, radio, movie, video games etc. content. Directors can be associated with individual items or with a series, episode, clip. */
	'directors': 'https://schema.org/directors',
	/** A sub property of description. A short description of the item used to disambiguate from other, similar items. Information from other properties (in particular, name) may be necessary for the description to be useful for disambiguation. */
	'disambiguatingDescription': 'https://schema.org/disambiguatingDescription',
	/** Any discount applied (to an Order). */
	'discount': 'https://schema.org/discount',
	/** Code used to redeem a discount. */
	'discountCode': 'https://schema.org/discountCode',
	/** The currency of the discount.\n\nUse standard formats: [ISO 4217 currency format](http://en.wikipedia.org/wiki/ISO_4217), e.g. "USD"; [Ticker symbol](https://en.wikipedia.org/wiki/List_of_cryptocurrencies) for cryptocurrencies, e.g. "BTC"; well known names for [Local Exchange Trading Systems](https://en.wikipedia.org/wiki/Local_exchange_trading_system) (LETS) and other currency types, e.g. "Ithaca HOUR". */
	'discountCurrency': 'https://schema.org/discountCurrency',
	/** Specifies the CreativeWork associated with the UserComment. */
	'discusses': 'https://schema.org/discusses',
	/** A link to the page containing the comments of the CreativeWork. */
	'discussionUrl': 'https://schema.org/discussionUrl',
	/** Information about disease prevention. */
	'diseasePreventionInfo': 'https://schema.org/diseasePreventionInfo',
	/** Statistical information about the spread of a disease, either as [[WebContent]], or
  described directly as a [[Dataset]], or the specific [[Observation]]s in the dataset. When a [[WebContent]] URL is
  provided, the page indicated might also contain more such markup. */
	'diseaseSpreadStatistics': 'https://schema.org/diseaseSpreadStatistics',
	/** The date that this organization was dissolved. */
	'dissolutionDate': 'https://schema.org/dissolutionDate',
	/** The distance travelled, e.g. exercising or travelling. */
	'distance': 'https://schema.org/distance',
	/** One of a set of signs and symptoms that can be used to distinguish this diagnosis from others in the differential diagnosis. */
	'distinguishingSign': 'https://schema.org/distinguishingSign',
	/** A downloadable form of this dataset, at a specific location, in a specific format. This property can be repeated if different variations are available. There is no expectation that different downloadable distributions must contain exactly equivalent information (see also [DCAT](https://www.w3.org/TR/vocab-dcat-3/#Class:Distribution) on this point). Different distributions might include or exclude different subsets of the entire dataset, for example. */
	'distribution': 'https://schema.org/distribution',
	/** Statement on diversity policy by an [[Organization]] e.g. a [[NewsMediaOrganization]]. For a [[NewsMediaOrganization]], a statement describing the newsroom’s diversity policy on both staffing and sources, typically providing staffing data. */
	'diversityPolicy': 'https://schema.org/diversityPolicy',
	/** For an [[Organization]] (often but not necessarily a [[NewsMediaOrganization]]), a report on staffing diversity issues. In a news context this might be for example ASNE or RTDNA (US) reports, or self-reported. */
	'diversityStaffingReport': 'https://schema.org/diversityStaffingReport',
	/** Further documentation describing the Web API in more detail. */
	'documentation': 'https://schema.org/documentation',
	/** Indicates when shipping to a particular [[shippingDestination]] is not available. */
	'doesNotShip': 'https://schema.org/doesNotShip',
	/** Relates a property to a class that is (one of) the type(s) the property is expected to be used on. */
	'domainIncludes': 'https://schema.org/domainIncludes',
	/** Whether borrower is a resident of the jurisdiction where the property is located. */
	'domiciledMortgage': 'https://schema.org/domiciledMortgage',
	/** The time admission will commence. */
	'doorTime': 'https://schema.org/doorTime',
	/** A dosage form in which this drug/supplement is available, e.g. 'tablet', 'suspension', 'injection'. */
	'dosageForm': 'https://schema.org/dosageForm',
	/** A dosing schedule for the drug for a given population, either observed, recommended, or maximum dose based on the type used. */
	'doseSchedule': 'https://schema.org/doseSchedule',
	/** The unit of the dose, e.g. 'mg'. */
	'doseUnit': 'https://schema.org/doseUnit',
	/** The value of the dose, e.g. 500. */
	'doseValue': 'https://schema.org/doseValue',
	/** a type of payment made in cash during the onset of the purchase of an expensive good/service. The payment typically represents only a percentage of the full purchase price. */
	'downPayment': 'https://schema.org/downPayment',
	/** If the file can be downloaded, URL to download the binary. */
	'downloadUrl': 'https://schema.org/downloadUrl',
	/** The number of downvotes this question, answer or comment has received from the community. */
	'downvoteCount': 'https://schema.org/downvoteCount',
	/** The vasculature that the vein drains into. */
	'drainsTo': 'https://schema.org/drainsTo',
	/** The drive wheel configuration, i.e. which roadwheels will receive torque from the vehicle's engine via the drivetrain. */
	'driveWheelConfiguration': 'https://schema.org/driveWheelConfiguration',
	/** Where a rental car can be dropped off. */
	'dropoffLocation': 'https://schema.org/dropoffLocation',
	/** When a rental car can be dropped off. */
	'dropoffTime': 'https://schema.org/dropoffTime',
	/** Specifying a drug or medicine used in a medication procedure. */
	'drug': 'https://schema.org/drug',
	/** The class of drug this belongs to (e.g., statins). */
	'drugClass': 'https://schema.org/drugClass',
	/** The unit in which the drug is measured, e.g. '5 mg tablet'. */
	'drugUnit': 'https://schema.org/drugUnit',
	/** The Dun & Bradstreet DUNS number for identifying an organization or business person. */
	'duns': 'https://schema.org/duns',
	/** A therapy that duplicates or overlaps this one. */
	'duplicateTherapy': 'https://schema.org/duplicateTherapy',
	/** The duration of the item (movie, audio recording, event, etc.) in [ISO 8601 date format](http://en.wikipedia.org/wiki/ISO_8601). */
	'duration': 'https://schema.org/duration',
	/** The duration of the warranty promise. Common unitCode values are ANN for year, MON for months, or DAY for days. */
	'durationOfWarranty': 'https://schema.org/durationOfWarranty',
	/** A media object representing the circumstances while performing this direction. */
	'duringMedia': 'https://schema.org/duringMedia',
	/** The amount to be paid as a penalty in the event of early payment of the loan. */
	'earlyPrepaymentPenalty': 'https://schema.org/earlyPrepaymentPenalty',
	/** An [EIDR](https://eidr.org/) (Entertainment Identifier Registry) [[identifier]] representing a specific edit / edition for a work of film or television.

For example, the motion picture known as "Ghostbusters" whose [[titleEIDR]] is "10.5240/7EC7-228A-510A-053E-CBB8-J" has several edits, e.g. "10.5240/1F2A-E1C5-680A-14C6-E76B-I" and "10.5240/8A35-3BEE-6497-5D12-9E4F-3".

Since schema.org types like [[Movie]] and [[TVEpisode]] can be used for both works and their multiple expressions, it is possible to use [[titleEIDR]] alone (for a general description), or alongside [[editEIDR]] for a more edit-specific description.
 */
	'editEIDR': 'https://schema.org/editEIDR',
	/** Specifies the Person who edited the CreativeWork. */
	'editor': 'https://schema.org/editor',
	/** For questions that are part of learning resources (e.g. Quiz), eduQuestionType indicates the format of question being given. Example: "Multiple choice", "Open ended", "Flashcard". */
	'eduQuestionType': 'https://schema.org/eduQuestionType',
	/** Educational background needed for the position or Occupation. */
	'educationRequirements': 'https://schema.org/educationRequirements',
	/** An alignment to an established educational framework.

This property should not be used where the nature of the alignment can be described using a simple property, for example to express that a resource [[teaches]] or [[assesses]] a competency. */
	'educationalAlignment': 'https://schema.org/educationalAlignment',
	/** A description of the qualification, award, certificate, diploma or other educational credential awarded as a consequence of successful completion of this course or program. */
	'educationalCredentialAwarded': 'https://schema.org/educationalCredentialAwarded',
	/** The framework to which the resource being described is aligned. */
	'educationalFramework': 'https://schema.org/educationalFramework',
	/** The level in terms of progression through an educational or training context. Examples of educational levels include 'beginner', 'intermediate' or 'advanced', and formal sets of level indicators. */
	'educationalLevel': 'https://schema.org/educationalLevel',
	/** Similar to courseMode, the medium or means of delivery of the program as a whole. The value may either be a text label (e.g. "online", "onsite" or "blended"; "synchronous" or "asynchronous"; "full-time" or "part-time") or a URL reference to a term from a controlled vocabulary (e.g. https://ceds.ed.gov/element/001311#Asynchronous ). */
	'educationalProgramMode': 'https://schema.org/educationalProgramMode',
	/** An educationalRole of an EducationalAudience. */
	'educationalRole': 'https://schema.org/educationalRole',
	/** The purpose of a work in the context of education; for example, 'assignment', 'group work'. */
	'educationalUse': 'https://schema.org/educationalUse',
	/** The elevation of a location ([WGS 84](https://en.wikipedia.org/wiki/World_Geodetic_System)). Values may be of the form 'NUMBER UNIT\_OF\_MEASUREMENT' (e.g., '1,000 m', '3,200 ft') while numbers alone should be assumed to be a value in meters. */
	'elevation': 'https://schema.org/elevation',
	/** The legal requirements such as citizenship, visa and other documentation required for an applicant to this job. */
	'eligibilityToWorkRequirement': 'https://schema.org/eligibilityToWorkRequirement',
	/** The type(s) of customers for which the given offer is valid. */
	'eligibleCustomerType': 'https://schema.org/eligibleCustomerType',
	/** The duration for which the given offer is valid. */
	'eligibleDuration': 'https://schema.org/eligibleDuration',
	/** The interval and unit of measurement of ordering quantities for which the offer or price specification is valid. This allows e.g. specifying that a certain freight charge is valid only for a certain quantity. */
	'eligibleQuantity': 'https://schema.org/eligibleQuantity',
	/** The ISO 3166-1 (ISO 3166-1 alpha-2) or ISO 3166-2 code, the place, or the GeoShape for the geo-political region(s) for which the offer or delivery charge specification is valid.\n\nSee also [[ineligibleRegion]].
     */
	'eligibleRegion': 'https://schema.org/eligibleRegion',
	/** The transaction volume, in a monetary unit, for which the offer or price specification is valid, e.g. for indicating a minimal purchasing volume, to express free shipping above a certain order volume, or to limit the acceptance of credit cards to purchases to a certain minimal amount. */
	'eligibleTransactionVolume': 'https://schema.org/eligibleTransactionVolume',
	/** Email address. */
	'email': 'https://schema.org/email',
	/** A URL pointing to a player for a specific video. In general, this is the information in the ```src``` element of an ```embed``` tag and should not be the same as the content of the ```loc``` tag. */
	'embedUrl': 'https://schema.org/embedUrl',
	/** Represents textual captioning from a [[MediaObject]], e.g. text of a 'meme'. */
	'embeddedTextCaption': 'https://schema.org/embeddedTextCaption',
	/** The CO2 emissions in g/km. When used in combination with a QuantitativeValue, put "g/km" into the unitText property of that value, since there is no UN/CEFACT Common Code for "g/km". */
	'emissionsCO2': 'https://schema.org/emissionsCO2',
	/** Someone working for this organization. */
	'employee': 'https://schema.org/employee',
	/** People working for this organization. */
	'employees': 'https://schema.org/employees',
	/** A description of the employer, career opportunities and work environment for this position. */
	'employerOverview': 'https://schema.org/employerOverview',
	/** Type of employment (e.g. full-time, part-time, contract, temporary, seasonal, internship). */
	'employmentType': 'https://schema.org/employmentType',
	/** Indicates the department, unit and/or facility where the employee reports and/or in which the job is to be performed. */
	'employmentUnit': 'https://schema.org/employmentUnit',
	/** Another BioChemEntity encoded by this one.  */
	'encodesBioChemEntity': 'https://schema.org/encodesBioChemEntity',
	/** The CreativeWork encoded by this media object. */
	'encodesCreativeWork': 'https://schema.org/encodesCreativeWork',
	/** A media object that encodes this CreativeWork. This property is a synonym for associatedMedia. */
	'encoding': 'https://schema.org/encoding',
	/** Media type typically expressed using a MIME format (see [IANA site](http://www.iana.org/assignments/media-types/media-types.xhtml) and [MDN reference](https://developer.mozilla.org/en-US/docs/Web/HTTP/Basics_of_HTTP/MIME_types)), e.g. application/zip for a SoftwareApplication binary, audio/mpeg for .mp3 etc.

In cases where a [[CreativeWork]] has several media type representations, [[encoding]] can be used to indicate each [[MediaObject]] alongside particular [[encodingFormat]] information.

Unregistered or niche encoding and file formats can be indicated instead via the most appropriate URL, e.g. defining Web page or a Wikipedia/Wikidata entry. */
	'encodingFormat': 'https://schema.org/encodingFormat',
	/** The supported encoding type(s) for an EntryPoint request. */
	'encodingType': 'https://schema.org/encodingType',
	/** A media object that encodes this CreativeWork. */
	'encodings': 'https://schema.org/encodings',
	/** The end date and time of the item (in [ISO 8601 date format](http://en.wikipedia.org/wiki/ISO_8601)). */
	'endDate': 'https://schema.org/endDate',
	/** The end time of the clip expressed as the number of seconds from the beginning of the work. */
	'endOffset': 'https://schema.org/endOffset',
	/** The endTime of something. For a reserved event or service (e.g. FoodEstablishmentReservation), the time that it is expected to end. For actions that span a period of time, when the action was performed. E.g. John wrote a book from January to *December*. For media, including audio and video, it's the time offset of the end of a clip within a larger file.\n\nNote that Event uses startDate/endDate instead of startTime/endTime, even when describing dates with times. This situation may be clarified in future revisions. */
	'endTime': 'https://schema.org/endTime',
	/** A sub property of participant. The person/organization being supported. */
	'endorsee': 'https://schema.org/endorsee',
	/** People or organizations that endorse the plan. */
	'endorsers': 'https://schema.org/endorsers',
	/** Specifies the most energy efficient class on the regulated EU energy consumption scale for the product category a product belongs to. For example, energy consumption for televisions placed on the market after January 1, 2020 is scaled from D to A+++. */
	'energyEfficiencyScaleMax': 'https://schema.org/energyEfficiencyScaleMax',
	/** Specifies the least energy efficient class on the regulated EU energy consumption scale for the product category a product belongs to. For example, energy consumption for televisions placed on the market after January 1, 2020 is scaled from D to A+++. */
	'energyEfficiencyScaleMin': 'https://schema.org/energyEfficiencyScaleMin',
	/** The volume swept by all of the pistons inside the cylinders of an internal combustion engine in a single movement. \n\nTypical unit code(s): CMQ for cubic centimeter, LTR for liters, INQ for cubic inches\n* Note 1: You can link to information about how the given value has been determined using the [[valueReference]] property.\n* Note 2: You can use [[minValue]] and [[maxValue]] to indicate ranges. */
	'engineDisplacement': 'https://schema.org/engineDisplacement',
	/** The power of the vehicle's engine.
    Typical unit code(s): KWT for kilowatt, BHP for brake horsepower, N12 for metric horsepower (PS, with 1 PS = 735,49875 W)\n\n* Note 1: There are many different ways of measuring an engine's power. For an overview, see  [http://en.wikipedia.org/wiki/Horsepower#Engine\_power\_test\_codes](http://en.wikipedia.org/wiki/Horsepower#Engine_power_test_codes).\n* Note 2: You can link to information about how the given value has been determined using the [[valueReference]] property.\n* Note 3: You can use [[minValue]] and [[maxValue]] to indicate ranges. */
	'enginePower': 'https://schema.org/enginePower',
	/** The type of engine or engines powering the vehicle. */
	'engineType': 'https://schema.org/engineType',
	/** A sub property of location. The entertainment business where the action occurred. */
	'entertainmentBusiness': 'https://schema.org/entertainmentBusiness',
	/** The characteristics of associated patients, such as age, gender, race etc. */
	'epidemiology': 'https://schema.org/epidemiology',
	/** An episode of a TV, radio or game media within a series or season. */
	'episode': 'https://schema.org/episode',
	/** Position of the episode within an ordered group of episodes. */
	'episodeNumber': 'https://schema.org/episodeNumber',
	/** An episode of a TV/radio series or season. */
	'episodes': 'https://schema.org/episodes',
	/** This ordering relation for qualitative values indicates that the subject is equal to the object. */
	'equal': 'https://schema.org/equal',
	/** For failed actions, more information on the cause of the failure. */
	'error': 'https://schema.org/error',
	/** The estimated cost of the supply or supplies consumed when performing instructions. */
	'estimatedCost': 'https://schema.org/estimatedCost',
	/** The estimated time the flight will take. */
	'estimatedFlightDuration': 'https://schema.org/estimatedFlightDuration',
	/** An estimated salary for a job posting or occupation, based on a variety of variables including, but not limited to industry, job title, and location. Estimated salaries  are often computed by outside organizations rather than the hiring organization, who may not have committed to the estimated value. */
	'estimatedSalary': 'https://schema.org/estimatedSalary',
	/** The condition, complication, or symptom whose risk is being estimated. */
	'estimatesRiskOf': 'https://schema.org/estimatesRiskOf',
	/** Statement about ethics policy, e.g. of a [[NewsMediaOrganization]] regarding journalistic and publishing practices, or of a [[Restaurant]], a page describing food source policies. In the case of a [[NewsMediaOrganization]], an ethicsPolicy is typically a statement describing the personal, organizational, and corporate standards of behavior expected by the organization. */
	'ethicsPolicy': 'https://schema.org/ethicsPolicy',
	/** Upcoming or past event associated with this place, organization, or action. */
	'event': 'https://schema.org/event',
	/** The eventAttendanceMode of an event indicates whether it occurs online, offline, or a mix. */
	'eventAttendanceMode': 'https://schema.org/eventAttendanceMode',
	/** Associates an [[Event]] with a [[Schedule]]. There are circumstances where it is preferable to share a schedule for a series of
      repeating events rather than data on the individual events themselves. For example, a website or application might prefer to publish a schedule for a weekly
      gym class rather than provide data on every event. A schedule could be processed by applications to add forthcoming events to a calendar. An [[Event]] that
      is associated with a [[Schedule]] using this property should not have [[startDate]] or [[endDate]] properties. These are instead defined within the associated
      [[Schedule]], this avoids any ambiguity for clients using the data. The property might have repeated values to specify different schedules, e.g. for different months
      or seasons. */
	'eventSchedule': 'https://schema.org/eventSchedule',
	/** An eventStatus of an event represents its status; particularly useful when an event is cancelled or rescheduled. */
	'eventStatus': 'https://schema.org/eventStatus',
	/** Upcoming or past events associated with this place or organization. */
	'events': 'https://schema.org/events',
	/** Strength of evidence of the data used to formulate the guideline (enumerated). */
	'evidenceLevel': 'https://schema.org/evidenceLevel',
	/** Source of the data used to formulate the guidance, e.g. RCT, consensus opinion, etc. */
	'evidenceOrigin': 'https://schema.org/evidenceOrigin',
	/** A creative work that this work is an example/instance/realization/derivation of. */
	'exampleOfWork': 'https://schema.org/exampleOfWork',
	/** Defines a [[Date]] or [[DateTime]] during which a scheduled [[Event]] will not take place. The property allows exceptions to
      a [[Schedule]] to be specified. If an exception is specified as a [[DateTime]] then only the event that would have started at that specific date and time
      should be excluded from the schedule. If an exception is specified as a [[Date]] then any event that is scheduled for that 24 hour period should be
      excluded from the schedule. This allows a whole day to be excluded from the schedule without having to itemise every scheduled event. */
	'exceptDate': 'https://schema.org/exceptDate',
	/** The difference between the price at which a broker or other intermediary buys and sells foreign currency. */
	'exchangeRateSpread': 'https://schema.org/exchangeRateSpread',
	/** Library file name, e.g., mscorlib.dll, system.web.dll. */
	'executableLibraryName': 'https://schema.org/executableLibraryName',
	/** A sub property of location. The course where this action was taken. */
	'exerciseCourse': 'https://schema.org/exerciseCourse',
	/** A sub property of instrument. The exercise plan used on this action. */
	'exercisePlan': 'https://schema.org/exercisePlan',
	/** A sub property of instrument. The diet used in this action. */
	'exerciseRelatedDiet': 'https://schema.org/exerciseRelatedDiet',
	/** Type(s) of exercise or activity, such as strength training, flexibility training, aerobics, cardiac rehabilitation, etc. */
	'exerciseType': 'https://schema.org/exerciseType',
	/** exif data for this object. */
	'exifData': 'https://schema.org/exifData',
	/** The earliest date the package may arrive. */
	'expectedArrivalFrom': 'https://schema.org/expectedArrivalFrom',
	/** The latest date the package may arrive. */
	'expectedArrivalUntil': 'https://schema.org/expectedArrivalUntil',
	/** The likely outcome in either the short term or long term of the medical condition. */
	'expectedPrognosis': 'https://schema.org/expectedPrognosis',
	/** An Offer which must be accepted before the user can perform the Action. For example, the user may need to buy a movie before being able to watch it. */
	'expectsAcceptanceOf': 'https://schema.org/expectsAcceptanceOf',
	/** Indicates whether a [[JobPosting]] will accept experience (as indicated by [[OccupationalExperienceRequirements]]) in place of its formal educational qualifications (as indicated by [[educationRequirements]]). If true, indicates that satisfying one of these requirements is sufficient. */
	'experienceInPlaceOfEducation': 'https://schema.org/experienceInPlaceOfEducation',
	/** Description of skills and experience needed for the position or Occupation. */
	'experienceRequirements': 'https://schema.org/experienceRequirements',
	/** Medical expert advice related to the plan. */
	'expertConsiderations': 'https://schema.org/expertConsiderations',
	/** Date the content expires and is no longer useful or available. For example a [[VideoObject]] or [[NewsArticle]] whose availability or relevance is time-limited, or a [[ClaimReview]] fact check whose publisher wants to indicate that it may no longer be relevant (or helpful to highlight) after some date. */
	'expires': 'https://schema.org/expires',
	/** Tissue, organ, biological sample, etc in which activity of this gene has been observed experimentally. For example brain, digestive system. */
	'expressedIn': 'https://schema.org/expressedIn',
	/** Family name. In the U.S., the last name of a Person. */
	'familyName': 'https://schema.org/familyName',
	/** The number of grams of fat. */
	'fatContent': 'https://schema.org/fatContent',
	/** The fax number. */
	'faxNumber': 'https://schema.org/faxNumber',
	/** Features or modules provided by this application (and possibly required by other applications). */
	'featureList': 'https://schema.org/featureList',
	/** Description of fees, commissions, and other terms applied either to a class of financial product, or by a financial service organization. */
	'feesAndCommissionsSpecification': 'https://schema.org/feesAndCommissionsSpecification',
	/** The number of grams of fiber. */
	'fiberContent': 'https://schema.org/fiberContent',
	/** Media type, typically MIME format (see [IANA site](http://www.iana.org/assignments/media-types/media-types.xhtml)) of the content, e.g. application/zip of a SoftwareApplication binary. In cases where a CreativeWork has several media type representations, 'encoding' can be used to indicate each MediaObject alongside particular fileFormat information. Unregistered or niche file formats can be indicated instead via the most appropriate URL, e.g. defining Web page or a Wikipedia entry. */
	'fileFormat': 'https://schema.org/fileFormat',
	/** Size of the application / package (e.g. 18MB). In the absence of a unit (MB, KB etc.), KB will be assumed. */
	'fileSize': 'https://schema.org/fileSize',
	/** A financial aid type or program which students may use to pay for tuition or fees associated with the program. */
	'financialAidEligible': 'https://schema.org/financialAidEligible',
	/** Indicates the first known occurrence of a [[Claim]] in some [[CreativeWork]]. */
	'firstAppearance': 'https://schema.org/firstAppearance',
	/** The date and place the work was first performed. */
	'firstPerformance': 'https://schema.org/firstPerformance',
	/** The distance of the flight. */
	'flightDistance': 'https://schema.org/flightDistance',
	/** The unique identifier for a flight including the airline IATA code. For example, if describing United flight 110, where the IATA code for United is 'UA', the flightNumber is 'UA110'. */
	'flightNumber': 'https://schema.org/flightNumber',
	/** The floor level for an [[Accommodation]] in a multi-storey building. Since counting
  systems [vary internationally](https://en.wikipedia.org/wiki/Storey#Consecutive_number_floor_designations), the local system should be used where possible. */
	'floorLevel': 'https://schema.org/floorLevel',
	/** A floor limit is the amount of money above which credit card transactions must be authorized. */
	'floorLimit': 'https://schema.org/floorLimit',
	/** The size of the accommodation, e.g. in square meter or squarefoot.
Typical unit code(s): MTK for square meter, FTK for square foot, or YDK for square yard  */
	'floorSize': 'https://schema.org/floorSize',
	/** A sub property of object. The person or organization being followed. */
	'followee': 'https://schema.org/followee',
	/** The most generic uni-directional social relation. */
	'follows': 'https://schema.org/follows',
	/** Typical or recommended followup care after the procedure is performed. */
	'followup': 'https://schema.org/followup',
	/** A sub property of location. The specific food establishment where the action occurred. */
	'foodEstablishment': 'https://schema.org/foodEstablishment',
	/** A sub property of location. The specific food event where the action occurred. */
	'foodEvent': 'https://schema.org/foodEvent',
	/** Any precaution, guidance, contraindication, etc. related to consumption of specific foods while taking this drug. */
	'foodWarning': 'https://schema.org/foodWarning',
	/** A person who founded this organization. */
	'founder': 'https://schema.org/founder',
	/** A person who founded this organization. */
	'founders': 'https://schema.org/founders',
	/** The date that this organization was founded. */
	'foundingDate': 'https://schema.org/foundingDate',
	/** The place where the Organization was founded. */
	'foundingLocation': 'https://schema.org/foundingLocation',
	/** A flag to signal that the item, event, or place is accessible for free. */
	'free': 'https://schema.org/free',
	/** A monetary value above (or at) which the shipping rate becomes free. Intended to be used via an [[OfferShippingDetails]] with [[shippingSettingsLink]] matching this [[ShippingRateSettings]]. */
	'freeShippingThreshold': 'https://schema.org/freeShippingThreshold',
	/** How often the dose is taken, e.g. 'daily'. */
	'frequency': 'https://schema.org/frequency',
	/** A sub property of location. The original location of the object or the agent before the action. */
	'fromLocation': 'https://schema.org/fromLocation',
	/** The capacity of the fuel tank or in the case of electric cars, the battery. If there are multiple components for storage, this should indicate the total of all storage of the same type.\n\nTypical unit code(s): LTR for liters, GLL of US gallons, GLI for UK / imperial gallons, AMH for ampere-hours (for electrical vehicles). */
	'fuelCapacity': 'https://schema.org/fuelCapacity',
	/** The amount of fuel consumed for traveling a particular distance or temporal duration with the given vehicle (e.g. liters per 100 km).\n\n* Note 1: There are unfortunately no standard unit codes for liters per 100 km.  Use [[unitText]] to indicate the unit of measurement, e.g. L/100 km.\n* Note 2: There are two ways of indicating the fuel consumption, [[fuelConsumption]] (e.g. 8 liters per 100 km) and [[fuelEfficiency]] (e.g. 30 miles per gallon). They are reciprocal.\n* Note 3: Often, the absolute value is useful only when related to driving speed ("at 80 km/h") or usage pattern ("city traffic"). You can use [[valueReference]] to link the value for the fuel consumption to another value. */
	'fuelConsumption': 'https://schema.org/fuelConsumption',
	/** The distance traveled per unit of fuel used; most commonly miles per gallon (mpg) or kilometers per liter (km/L).\n\n* Note 1: There are unfortunately no standard unit codes for miles per gallon or kilometers per liter. Use [[unitText]] to indicate the unit of measurement, e.g. mpg or km/L.\n* Note 2: There are two ways of indicating the fuel consumption, [[fuelConsumption]] (e.g. 8 liters per 100 km) and [[fuelEfficiency]] (e.g. 30 miles per gallon). They are reciprocal.\n* Note 3: Often, the absolute value is useful only when related to driving speed ("at 80 km/h") or usage pattern ("city traffic"). You can use [[valueReference]] to link the value for the fuel economy to another value. */
	'fuelEfficiency': 'https://schema.org/fuelEfficiency',
	/** The type of fuel suitable for the engine or engines of the vehicle. If the vehicle has only one engine, this property can be attached directly to the vehicle. */
	'fuelType': 'https://schema.org/fuelType',
	/** The degree of mobility the joint allows. */
	'functionalClass': 'https://schema.org/functionalClass',
	/** Indicates something directly or indirectly funded or sponsored through a [[Grant]]. See also [[ownershipFundingInfo]]. */
	'fundedItem': 'https://schema.org/fundedItem',
	/** A person or organization that supports (sponsors) something through some kind of financial contribution. */
	'funder': 'https://schema.org/funder',
	/** A [[Grant]] that directly or indirectly provide funding or sponsorship for this item. See also [[ownershipFundingInfo]]. */
	'funding': 'https://schema.org/funding',
	/** Video game which is played on this server. */
	'game': 'https://schema.org/game',
	/** Indicates the availability type of the game content associated with this action, such as whether it is a full version or a demo. */
	'gameAvailabilityType': 'https://schema.org/gameAvailabilityType',
	/** The edition of a video game. */
	'gameEdition': 'https://schema.org/gameEdition',
	/** An item is an object within the game world that can be collected by a player or, occasionally, a non-player character. */
	'gameItem': 'https://schema.org/gameItem',
	/** Real or fictional location of the game (or part of game). */
	'gameLocation': 'https://schema.org/gameLocation',
	/** The electronic systems used to play <a href="http://en.wikipedia.org/wiki/Category:Video_game_platforms">video games</a>. */
	'gamePlatform': 'https://schema.org/gamePlatform',
	/** The server on which  it is possible to play the game. */
	'gameServer': 'https://schema.org/gameServer',
	/** Links to tips, tactics, etc. */
	'gameTip': 'https://schema.org/gameTip',
	/** Gender of something, typically a [[Person]], but possibly also fictional characters, animals, etc. While https://schema.org/Male and https://schema.org/Female may be used, text strings are also acceptable for people who do not identify as a binary gender. The [[gender]] property can also be used in an extended sense to cover e.g. the gender of sports teams. As with the gender of individuals, we do not try to enumerate all possibilities. A mixed-gender [[SportsTeam]] can be indicated with a text value of "Mixed". */
	'gender': 'https://schema.org/gender',
	/** Genre of the creative work, broadcast channel or group. */
	'genre': 'https://schema.org/genre',
	/** The geo coordinates of the place. */
	'geo': 'https://schema.org/geo',
	/** Represents a relationship between two geometries (or the places they represent), relating a containing geometry to a contained geometry. "a contains b iff no points of b lie in the exterior of a, and at least one point of the interior of b lies in the interior of a". As defined in [DE-9IM](https://en.wikipedia.org/wiki/DE-9IM). */
	'geoContains': 'https://schema.org/geoContains',
	/** Represents a relationship between two geometries (or the places they represent), relating a geometry to another that covers it. As defined in [DE-9IM](https://en.wikipedia.org/wiki/DE-9IM). */
	'geoCoveredBy': 'https://schema.org/geoCoveredBy',
	/** Represents a relationship between two geometries (or the places they represent), relating a covering geometry to a covered geometry. "Every point of b is a point of (the interior or boundary of) a". As defined in [DE-9IM](https://en.wikipedia.org/wiki/DE-9IM). */
	'geoCovers': 'https://schema.org/geoCovers',
	/** Represents a relationship between two geometries (or the places they represent), relating a geometry to another that crosses it: "a crosses b: they have some but not all interior points in common, and the dimension of the intersection is less than that of at least one of them". As defined in [DE-9IM](https://en.wikipedia.org/wiki/DE-9IM). */
	'geoCrosses': 'https://schema.org/geoCrosses',
	/** Represents spatial relations in which two geometries (or the places they represent) are topologically disjoint: "they have no point in common. They form a set of disconnected geometries." (A symmetric relationship, as defined in [DE-9IM](https://en.wikipedia.org/wiki/DE-9IM).) */
	'geoDisjoint': 'https://schema.org/geoDisjoint',
	/** Represents spatial relations in which two geometries (or the places they represent) are topologically equal, as defined in [DE-9IM](https://en.wikipedia.org/wiki/DE-9IM). "Two geometries are topologically equal if their interiors intersect and no part of the interior or boundary of one geometry intersects the exterior of the other" (a symmetric relationship). */
	'geoEquals': 'https://schema.org/geoEquals',
	/** Represents spatial relations in which two geometries (or the places they represent) have at least one point in common. As defined in [DE-9IM](https://en.wikipedia.org/wiki/DE-9IM). */
	'geoIntersects': 'https://schema.org/geoIntersects',
	/** Indicates the GeoCoordinates at the centre of a GeoShape, e.g. GeoCircle. */
	'geoMidpoint': 'https://schema.org/geoMidpoint',
	/** Represents a relationship between two geometries (or the places they represent), relating a geometry to another that geospatially overlaps it, i.e. they have some but not all points in common. As defined in [DE-9IM](https://en.wikipedia.org/wiki/DE-9IM). */
	'geoOverlaps': 'https://schema.org/geoOverlaps',
	/** Indicates the approximate radius of a GeoCircle (metres unless indicated otherwise via Distance notation). */
	'geoRadius': 'https://schema.org/geoRadius',
	/** Represents spatial relations in which two geometries (or the places they represent) touch: "they have at least one boundary point in common, but no interior points." (A symmetric relationship, as defined in [DE-9IM](https://en.wikipedia.org/wiki/DE-9IM).) */
	'geoTouches': 'https://schema.org/geoTouches',
	/** Represents a relationship between two geometries (or the places they represent), relating a geometry to one that contains it, i.e. it is inside (i.e. within) its interior. As defined in [DE-9IM](https://en.wikipedia.org/wiki/DE-9IM). */
	'geoWithin': 'https://schema.org/geoWithin',
	/** The geographic area associated with the audience. */
	'geographicArea': 'https://schema.org/geographicArea',
	/** Information about getting tested (for a [[MedicalCondition]]), e.g. in the context of a pandemic. */
	'gettingTestedInfo': 'https://schema.org/gettingTestedInfo',
	/** Given name. In the U.S., the first name of a Person. */
	'givenName': 'https://schema.org/givenName',
	/** The [Global Location Number](http://www.gs1.org/gln) (GLN, sometimes also referred to as International Location Number or ILN) of the respective organization, person, or place. The GLN is a 13-digit number used to identify parties and physical locations. */
	'globalLocationNumber': 'https://schema.org/globalLocationNumber',
	/** governmentBenefitsInfo provides information about government benefits associated with a SpecialAnnouncement. */
	'governmentBenefitsInfo': 'https://schema.org/governmentBenefitsInfo',
	/** The period of time after any due date that the borrower has to fulfil its obligations before a default (failure to pay) is deemed to have occurred. */
	'gracePeriod': 'https://schema.org/gracePeriod',
	/** The person, organization, contact point, or audience that has been granted this permission. */
	'grantee': 'https://schema.org/grantee',
	/** This ordering relation for qualitative values indicates that the subject is greater than the object. */
	'greater': 'https://schema.org/greater',
	/** This ordering relation for qualitative values indicates that the subject is greater than or equal to the object. */
	'greaterOrEqual': 'https://schema.org/greaterOrEqual',
	/** A Global Trade Item Number ([GTIN](https://www.gs1.org/standards/id-keys/gtin)). GTINs identify trade items, including products and services, using numeric identification codes.

The GS1 [digital link specifications](https://www.gs1.org/standards/Digital-Link/) express GTINs as URLs (URIs, IRIs, etc.). Details including regular expression examples can be found in, Section 6 of the GS1 URI Syntax specification; see also [schema.org tracking issue](https://github.com/schemaorg/schemaorg/issues/3156#issuecomment-1209522809) for schema.org-specific discussion. A correct [[gtin]] value should be a valid GTIN, which means that it should be an all-numeric string of either 8, 12, 13 or 14 digits, or a "GS1 Digital Link" URL based on such a string. The numeric component should also have a [valid GS1 check digit](https://www.gs1.org/services/check-digit-calculator) and meet the other rules for valid GTINs. See also [GS1's GTIN Summary](http://www.gs1.org/barcodes/technical/idkeys/gtin) and [Wikipedia](https://en.wikipedia.org/wiki/Global_Trade_Item_Number) for more details. Left-padding of the gtin values is not required or encouraged. The [[gtin]] property generalizes the earlier [[gtin8]], [[gtin12]], [[gtin13]], and [[gtin14]] properties.

Note also that this is a definition for how to include GTINs in Schema.org data, and not a definition of GTINs in general - see the GS1 documentation for authoritative details. */
	'gtin': 'https://schema.org/gtin',
	/** The GTIN-12 code of the product, or the product to which the offer refers. The GTIN-12 is the 12-digit GS1 Identification Key composed of a U.P.C. Company Prefix, Item Reference, and Check Digit used to identify trade items. See [GS1 GTIN Summary](http://www.gs1.org/barcodes/technical/idkeys/gtin) for more details. */
	'gtin12': 'https://schema.org/gtin12',
	/** The GTIN-13 code of the product, or the product to which the offer refers. This is equivalent to 13-digit ISBN codes and EAN UCC-13. Former 12-digit UPC codes can be converted into a GTIN-13 code by simply adding a preceding zero. See [GS1 GTIN Summary](http://www.gs1.org/barcodes/technical/idkeys/gtin) for more details. */
	'gtin13': 'https://schema.org/gtin13',
	/** The GTIN-14 code of the product, or the product to which the offer refers. See [GS1 GTIN Summary](http://www.gs1.org/barcodes/technical/idkeys/gtin) for more details. */
	'gtin14': 'https://schema.org/gtin14',
	/** The GTIN-8 code of the product, or the product to which the offer refers. This code is also known as EAN/UCC-8 or 8-digit EAN. See [GS1 GTIN Summary](http://www.gs1.org/barcodes/technical/idkeys/gtin) for more details. */
	'gtin8': 'https://schema.org/gtin8',
	/** A medical guideline related to this entity. */
	'guideline': 'https://schema.org/guideline',
	/** Date on which this guideline's recommendation was made. */
	'guidelineDate': 'https://schema.org/guidelineDate',
	/** The medical conditions, treatments, etc. that are the subject of the guideline. */
	'guidelineSubject': 'https://schema.org/guidelineSubject',
	/** The typical delay between the receipt of the order and the goods either leaving the warehouse or being prepared for pickup, in case the delivery method is on site pickup. Typical properties: minValue, maxValue, unitCode (d for DAY).  This is by common convention assumed to mean business days (if a unitCode is used, coded as "d"), i.e. only counting days when the business normally operates. */
	'handlingTime': 'https://schema.org/handlingTime',
	/** Used to tag an item to be intended or suitable for consumption or use by adults only. */
	'hasAdultConsideration': 'https://schema.org/hasAdultConsideration',
	/** Indicates a BioChemEntity that (in some sense) has this BioChemEntity as a part.  */
	'hasBioChemEntityPart': 'https://schema.org/hasBioChemEntityPart',
	/** A symbolic representation of a BioChemEntity. For example, a nucleotide sequence of a Gene or an amino acid sequence of a Protein. */
	'hasBioPolymerSequence': 'https://schema.org/hasBioPolymerSequence',
	/** A broadcast channel of a broadcast service. */
	'hasBroadcastChannel': 'https://schema.org/hasBroadcastChannel',
	/** A Category code contained in this code set. */
	'hasCategoryCode': 'https://schema.org/hasCategoryCode',
	/** A course or class that is one of the learning opportunities that constitute an educational / occupational program. No information is implied about whether the course is mandatory or optional; no guarantee is implied about whether the course will be available to everyone on the program. */
	'hasCourse': 'https://schema.org/hasCourse',
	/** An offering of the course at a specific time and place or through specific media or mode of study or to a specific section of students. */
	'hasCourseInstance': 'https://schema.org/hasCourseInstance',
	/** A credential awarded to the Person or Organization. */
	'hasCredential': 'https://schema.org/hasCredential',
	/** A Defined Term contained in this term set. */
	'hasDefinedTerm': 'https://schema.org/hasDefinedTerm',
	/** Method used for delivery or shipping. */
	'hasDeliveryMethod': 'https://schema.org/hasDeliveryMethod',
	/** A permission related to the access to this document (e.g. permission to read or write an electronic document). For a public document, specify a grantee with an Audience with audienceType equal to "public". */
	'hasDigitalDocumentPermission': 'https://schema.org/hasDigitalDocumentPermission',
	/** Indicates whether some facility (e.g. [[FoodEstablishment]], [[CovidTestingFacility]]) offers a service that can be used by driving through in a car. In the case of [[CovidTestingFacility]] such facilities could potentially help with social distancing from other potentially-infected users. */
	'hasDriveThroughService': 'https://schema.org/hasDriveThroughService',
	/** Defines the energy efficiency Category (also known as "class" or "rating") for a product according to an international energy efficiency standard. */
	'hasEnergyConsumptionDetails': 'https://schema.org/hasEnergyConsumptionDetails',
	/** Defines the energy efficiency Category (which could be either a rating out of range of values or a yes/no certification) for a product according to an international energy efficiency standard. */
	'hasEnergyEfficiencyCategory': 'https://schema.org/hasEnergyEfficiencyCategory',
	/** Indicates the aspect or aspects specifically addressed in some [[HealthTopicContent]]. For example, that the content is an overview, or that it talks about treatment, self-care, treatments or their side-effects. */
	'hasHealthAspect': 'https://schema.org/hasHealthAspect',
	/** A URL to a map of the place. */
	'hasMap': 'https://schema.org/hasMap',
	/** A product measurement, for example the inseam of pants, the wheel size of a bicycle, or the gauge of a screw. Usually an exact measurement, but can also be a range of measurements for adjustable products, for example belts and ski bindings. */
	'hasMeasurement': 'https://schema.org/hasMeasurement',
	/** Either the actual menu as a structured representation, as text, or a URL of the menu. */
	'hasMenu': 'https://schema.org/hasMenu',
	/** A food or drink item contained in a menu or menu section. */
	'hasMenuItem': 'https://schema.org/hasMenuItem',
	/** A subgrouping of the menu (by dishes, course, serving time period, etc.). */
	'hasMenuSection': 'https://schema.org/hasMenuSection',
	/** Specifies a MerchantReturnPolicy that may be applicable. */
	'hasMerchantReturnPolicy': 'https://schema.org/hasMerchantReturnPolicy',
	/** Molecular function performed by this BioChemEntity; please use PropertyValue if you want to include any evidence. */
	'hasMolecularFunction': 'https://schema.org/hasMolecularFunction',
	/** The Person's occupation. For past professions, use Role for expressing dates. */
	'hasOccupation': 'https://schema.org/hasOccupation',
	/** Indicates an OfferCatalog listing for this Organization, Person, or Service. */
	'hasOfferCatalog': 'https://schema.org/hasOfferCatalog',
	/** Points-of-Sales operated by the organization or person. */
	'hasPOS': 'https://schema.org/hasPOS',
	/** Indicates an item or CreativeWork that is part of this item, or CreativeWork (in some sense). */
	'hasPart': 'https://schema.org/hasPart',
	/** A common representation such as a protein sequence or chemical structure for this entity. For images use schema.org/image. */
	'hasRepresentation': 'https://schema.org/hasRepresentation',
	/** Indicates a [[Product]] that is a member of this [[ProductGroup]] (or [[ProductModel]]). */
	'hasVariant': 'https://schema.org/hasVariant',
	/** Headline of the article. */
	'headline': 'https://schema.org/headline',
	/** Specifying the health condition(s) of a patient, medical study, or other target audience. */
	'healthCondition': 'https://schema.org/healthCondition',
	/** Whether the coinsurance applies before or after deductible, etc. TODO: Is this a closed set? */
	'healthPlanCoinsuranceOption': 'https://schema.org/healthPlanCoinsuranceOption',
	/** The rate of coinsurance expressed as a number between 0.0 and 1.0. */
	'healthPlanCoinsuranceRate': 'https://schema.org/healthPlanCoinsuranceRate',
	/** The copay amount. */
	'healthPlanCopay': 'https://schema.org/healthPlanCopay',
	/** Whether the copay is before or after deductible, etc. TODO: Is this a closed set? */
	'healthPlanCopayOption': 'https://schema.org/healthPlanCopayOption',
	/** The costs to the patient for services under this network or formulary. */
	'healthPlanCostSharing': 'https://schema.org/healthPlanCostSharing',
	/** TODO. */
	'healthPlanDrugOption': 'https://schema.org/healthPlanDrugOption',
	/** The tier(s) of drugs offered by this formulary or insurance plan. */
	'healthPlanDrugTier': 'https://schema.org/healthPlanDrugTier',
	/** The 14-character, HIOS-generated Plan ID number. (Plan IDs must be unique, even across different markets.) */
	'healthPlanId': 'https://schema.org/healthPlanId',
	/** The URL that goes directly to the plan brochure for the specific standard plan or plan variation. */
	'healthPlanMarketingUrl': 'https://schema.org/healthPlanMarketingUrl',
	/** Name or unique ID of network. (Networks are often reused across different insurance plans.) */
	'healthPlanNetworkId': 'https://schema.org/healthPlanNetworkId',
	/** The tier(s) for this network. */
	'healthPlanNetworkTier': 'https://schema.org/healthPlanNetworkTier',
	/** The category or type of pharmacy associated with this cost sharing. */
	'healthPlanPharmacyCategory': 'https://schema.org/healthPlanPharmacyCategory',
	/** Indicates data describing a hospital, e.g. a CDC [[CDCPMDRecord]] or as some kind of [[Dataset]]. */
	'healthcareReportingData': 'https://schema.org/healthcareReportingData',
	/** The height of the item. */
	'height': 'https://schema.org/height',
	/** The highest price of all offers available.\n\nUsage guidelines:\n\n* Use values from 0123456789 (Unicode 'DIGIT ZERO' (U+0030) to 'DIGIT NINE' (U+0039)) rather than superficially similar Unicode symbols.\n* Use '.' (Unicode 'FULL STOP' (U+002E)) rather than ',' to indicate a decimal point. Avoid using these symbols as a readability separator. */
	'highPrice': 'https://schema.org/highPrice',
	/** Organization or Person offering the job position. */
	'hiringOrganization': 'https://schema.org/hiringOrganization',
	/** [[ArchiveOrganization]] that holds, keeps or maintains the [[ArchiveComponent]]. */
	'holdingArchive': 'https://schema.org/holdingArchive',
	/** A contact location for a person's residence. */
	'homeLocation': 'https://schema.org/homeLocation',
	/** The home team in a sports event. */
	'homeTeam': 'https://schema.org/homeTeam',
	/** An honorific prefix preceding a Person's name such as Dr/Mrs/Mr. */
	'honorificPrefix': 'https://schema.org/honorificPrefix',
	/** An honorific suffix following a Person's name such as M.D./PhD/MSCSW. */
	'honorificSuffix': 'https://schema.org/honorificSuffix',
	/** A hospital with which the physician or office is affiliated. */
	'hospitalAffiliation': 'https://schema.org/hospitalAffiliation',
	/** The organization (airline, travelers' club, etc.) the membership is made with. */
	'hostingOrganization': 'https://schema.org/hostingOrganization',
	/** The hours during which this service or contact is available. */
	'hoursAvailable': 'https://schema.org/hoursAvailable',
	/** How the procedure is performed. */
	'howPerformed': 'https://schema.org/howPerformed',
	/** An HTTP method that specifies the appropriate HTTP method for a request to an HTTP EntryPoint. Values are capitalized strings as used in HTTP. */
	'httpMethod': 'https://schema.org/httpMethod',
	/** IATA identifier for an airline or airport. */
	'iataCode': 'https://schema.org/iataCode',
	/** ICAO identifier for an airport. */
	'icaoCode': 'https://schema.org/icaoCode',
	/** The identifier property represents any kind of identifier for any kind of [[Thing]], such as ISBNs, GTIN codes, UUIDs etc. Schema.org provides dedicated properties for representing many of these, either as textual strings or as URL (URI) links. See [background notes](/docs/datamodel.html#identifierBg) for more details.
         */
	'identifier': 'https://schema.org/identifier',
	/** A physical examination that can identify this sign. */
	'identifyingExam': 'https://schema.org/identifyingExam',
	/** A diagnostic test that can identify this sign. */
	'identifyingTest': 'https://schema.org/identifyingTest',
	/** The illustrator of the book. */
	'illustrator': 'https://schema.org/illustrator',
	/** An image of the item. This can be a [[URL]] or a fully described [[ImageObject]]. */
	'image': 'https://schema.org/image',
	/** Imaging technique used. */
	'imagingTechnique': 'https://schema.org/imagingTechnique',
	/** The album to which this recording belongs. */
	'inAlbum': 'https://schema.org/inAlbum',
	/** The CableOrSatelliteService offering the channel. */
	'inBroadcastLineup': 'https://schema.org/inBroadcastLineup',
	/** Non-proprietary identifier for molecular entity that can be used in printed and electronic data sources thus enabling easier linking of diverse data compilations. */
	'inChI': 'https://schema.org/inChI',
	/** InChIKey is a hashed version of the full InChI (using the SHA-256 algorithm). */
	'inChIKey': 'https://schema.org/inChIKey',
	/** A [[CategoryCodeSet]] that contains this category code. */
	'inCodeSet': 'https://schema.org/inCodeSet',
	/** A [[DefinedTermSet]] that contains this term. */
	'inDefinedTermSet': 'https://schema.org/inDefinedTermSet',
	/** The language of the content or performance or used in an action. Please use one of the language codes from the [IETF BCP 47 standard](http://tools.ietf.org/html/bcp47). See also [[availableLanguage]]. */
	'inLanguage': 'https://schema.org/inLanguage',
	/** The playlist to which this recording belongs. */
	'inPlaylist': 'https://schema.org/inPlaylist',
	/** Indicates the [[productGroupID]] for a [[ProductGroup]] that this product [[isVariantOf]].  */
	'inProductGroupWithID': 'https://schema.org/inProductGroupWithID',
	/** Are in-store returns offered? (For more advanced return methods use the [[returnMethod]] property.) */
	'inStoreReturnsOffered': 'https://schema.org/inStoreReturnsOffered',
	/** Qualification, candidature, degree, application that Thesis supports. */
	'inSupportOf': 'https://schema.org/inSupportOf',
	/** Description of bonus and commission compensation aspects of the job. */
	'incentiveCompensation': 'https://schema.org/incentiveCompensation',
	/** Description of bonus and commission compensation aspects of the job. */
	'incentives': 'https://schema.org/incentives',
	/** Smaller compositions included in this work (e.g. a movement in a symphony). */
	'includedComposition': 'https://schema.org/includedComposition',
	/** A data catalog which contains this dataset (this property was previously 'catalog', preferred name is now 'includedInDataCatalog'). */
	'includedDataCatalog': 'https://schema.org/includedDataCatalog',
	/** A data catalog which contains this dataset. */
	'includedInDataCatalog': 'https://schema.org/includedInDataCatalog',
	/** The insurance plans that cover this drug. */
	'includedInHealthInsurancePlan': 'https://schema.org/includedInHealthInsurancePlan',
	/** A modifiable or non-modifiable risk factor included in the calculation, e.g. age, coexisting condition. */
	'includedRiskFactor': 'https://schema.org/includedRiskFactor',
	/** Attraction located at destination. */
	'includesAttraction': 'https://schema.org/includesAttraction',
	/** Formularies covered by this plan. */
	'includesHealthPlanFormulary': 'https://schema.org/includesHealthPlanFormulary',
	/** Networks covered by this plan. */
	'includesHealthPlanNetwork': 'https://schema.org/includesHealthPlanNetwork',
	/** This links to a node or nodes indicating the exact quantity of the products included in  an [[Offer]] or [[ProductCollection]]. */
	'includesObject': 'https://schema.org/includesObject',
	/** The condition, complication, etc. influenced by this factor. */
	'increasesRiskOf': 'https://schema.org/increasesRiskOf',
	/** The industry associated with the job position. */
	'industry': 'https://schema.org/industry',
	/** The ISO 3166-1 (ISO 3166-1 alpha-2) or ISO 3166-2 code, the place, or the GeoShape for the geo-political region(s) for which the offer or delivery charge specification is not valid, e.g. a region where the transaction is not allowed.\n\nSee also [[eligibleRegion]].
       */
	'ineligibleRegion': 'https://schema.org/ineligibleRegion',
	/** The actual infectious agent, such as a specific bacterium. */
	'infectiousAgent': 'https://schema.org/infectiousAgent',
	/** The class of infectious agent (bacteria, prion, etc.) that causes the disease. */
	'infectiousAgentClass': 'https://schema.org/infectiousAgentClass',
	/** A single ingredient used in the recipe, e.g. sugar, flour or garlic. */
	'ingredients': 'https://schema.org/ingredients',
	/** The individual who traces over the pencil drawings in ink after pencils are complete. */
	'inker': 'https://schema.org/inker',
	/** The place of attachment of a muscle, or what the muscle moves. */
	'insertion': 'https://schema.org/insertion',
	/** URL at which the app may be installed, if different from the URL of the item. */
	'installUrl': 'https://schema.org/installUrl',
	/** A person assigned to instruct or provide instructional assistance for the [[CourseInstance]]. */
	'instructor': 'https://schema.org/instructor',
	/** The object that helped the agent perform the action. E.g. John wrote a book with *a pen*. */
	'instrument': 'https://schema.org/instrument',
	/** Quantitative measure gauging the degree of force involved in the exercise, for example, heartbeats per minute. May include the velocity of the movement. */
	'intensity': 'https://schema.org/intensity',
	/** Another drug that is known to interact with this drug in a way that impacts the effect of this drug or causes a risk to the patient. Note: disease interactions are typically captured as contraindications. */
	'interactingDrug': 'https://schema.org/interactingDrug',
	/** This property is deprecated, alongside the UserInteraction types on which it depended. */
	'interactionCount': 'https://schema.org/interactionCount',
	/** The WebSite or SoftwareApplication where the interactions took place. */
	'interactionService': 'https://schema.org/interactionService',
	/** The number of interactions for the CreativeWork using the WebSite or SoftwareApplication. The most specific child type of InteractionCounter should be used. */
	'interactionStatistic': 'https://schema.org/interactionStatistic',
	/** The Action representing the type of interaction. For up votes, +1s, etc. use [[LikeAction]]. For down votes use [[DislikeAction]]. Otherwise, use the most specific Action. */
	'interactionType': 'https://schema.org/interactionType',
	/** The predominant mode of learning supported by the learning resource. Acceptable values are 'active', 'expositive', or 'mixed'. */
	'interactivityType': 'https://schema.org/interactivityType',
	/** The interest rate, charged or paid, applicable to the financial product. Note: This is different from the calculated annualPercentageRate. */
	'interestRate': 'https://schema.org/interestRate',
	/** Used to indicate a specific claim contained, implied, translated or refined from the content of a [[MediaObject]] or other [[CreativeWork]]. The interpreting party can be indicated using [[claimInterpreter]]. */
	'interpretedAsClaim': 'https://schema.org/interpretedAsClaim',
	/** The current approximate inventory level for the item or items. */
	'inventoryLevel': 'https://schema.org/inventoryLevel',
	/** Relates a property to a property that is its inverse. Inverse properties relate the same pairs of items to each other, but in reversed direction. For example, the 'alumni' and 'alumniOf' properties are inverseOf each other. Some properties don't have explicit inverses; in these situations RDFa and JSON-LD syntax for reverse properties can be used. */
	'inverseOf': 'https://schema.org/inverseOf',
	/** Whether the provider is accepting new patients. */
	'isAcceptingNewPatients': 'https://schema.org/isAcceptingNewPatients',
	/** A flag to signal that the item, event, or place is accessible for free. */
	'isAccessibleForFree': 'https://schema.org/isAccessibleForFree',
	/** A pointer to another product (or multiple products) for which this product is an accessory or spare part. */
	'isAccessoryOrSparePartFor': 'https://schema.org/isAccessoryOrSparePartFor',
	/** True if the drug is available in a generic form (regardless of name). */
	'isAvailableGenerically': 'https://schema.org/isAvailableGenerically',
	/** A resource from which this work is derived or from which it is a modification or adaptation. */
	'isBasedOn': 'https://schema.org/isBasedOn',
	/** A resource that was used in the creation of this resource. This term can be repeated for multiple sources. For example, http://example.com/great-multiplication-intro.html. */
	'isBasedOnUrl': 'https://schema.org/isBasedOnUrl',
	/** A pointer to another product (or multiple products) for which this product is a consumable. */
	'isConsumableFor': 'https://schema.org/isConsumableFor',
	/** Another BioChemEntity encoding by this one. */
	'isEncodedByBioChemEntity': 'https://schema.org/isEncodedByBioChemEntity',
	/** Indicates whether this content is family friendly. */
	'isFamilyFriendly': 'https://schema.org/isFamilyFriendly',
	/** Indicates whether the offer was accepted as a gift for someone other than the buyer. */
	'isGift': 'https://schema.org/isGift',
	/** Biological process this BioChemEntity is involved in; please use PropertyValue if you want to include any evidence. */
	'isInvolvedInBiologicalProcess': 'https://schema.org/isInvolvedInBiologicalProcess',
	/** True if the broadcast is of a live event. */
	'isLiveBroadcast': 'https://schema.org/isLiveBroadcast',
	/** Subcellular location where this BioChemEntity is located; please use PropertyValue if you want to include any evidence. */
	'isLocatedInSubcellularLocation': 'https://schema.org/isLocatedInSubcellularLocation',
	/** Indicates an item or CreativeWork that this item, or CreativeWork (in some sense), is part of. */
	'isPartOf': 'https://schema.org/isPartOf',
	/** Indicates a BioChemEntity that is (in some sense) a part of this BioChemEntity.  */
	'isPartOfBioChemEntity': 'https://schema.org/isPartOfBioChemEntity',
	/** Indicates some accommodation that this floor plan describes. */
	'isPlanForApartment': 'https://schema.org/isPlanForApartment',
	/** True if this item's name is a proprietary/brand name (vs. generic name). */
	'isProprietary': 'https://schema.org/isProprietary',
	/** A pointer to another, somehow related product (or multiple products). */
	'isRelatedTo': 'https://schema.org/isRelatedTo',
	/** Whether the 3DModel allows resizing. For example, room layout applications often do not allow 3DModel elements to be resized to reflect reality. */
	'isResizable': 'https://schema.org/isResizable',
	/** A pointer to another, functionally similar product (or multiple products). */
	'isSimilarTo': 'https://schema.org/isSimilarTo',
	/** This can be marked 'true' to indicate that some published [[DeliveryTimeSettings]] or [[ShippingRateSettings]] are intended to apply to all [[OfferShippingDetails]] published by the same merchant, when referenced by a [[shippingSettingsLink]] in those settings. It is not meaningful to use a 'true' value for this property alongside a transitTimeLabel (for [[DeliveryTimeSettings]]) or shippingLabel (for [[ShippingRateSettings]]), since this property is for use with unlabelled settings. */
	'isUnlabelledFallback': 'https://schema.org/isUnlabelledFallback',
	/** Indicates the kind of product that this is a variant of. In the case of [[ProductModel]], this is a pointer (from a ProductModel) to a base product from which this product is a variant. It is safe to infer that the variant inherits all product features from the base model, unless defined locally. This is not transitive. In the case of a [[ProductGroup]], the group description also serves as a template, representing a set of Products that vary on explicitly defined, specific dimensions only (so it defines both a set of variants, as well as which values distinguish amongst those variants). When used with [[ProductGroup]], this property can apply to any [[Product]] included in the group. */
	'isVariantOf': 'https://schema.org/isVariantOf',
	/** The ISBN of the book. */
	'isbn': 'https://schema.org/isbn',
	/** The International Standard of Industrial Classification of All Economic Activities (ISIC), Revision 4 code for a particular organization, business person, or place. */
	'isicV4': 'https://schema.org/isicV4',
	/** An organization identifier as defined in ISO 6523(-1). Note that many existing organization identifiers such as [leiCode](https://schema.org/leiCode), [duns](https://schema.org/duns) and [vatID](https://schema.org/vatID) can be expressed as an ISO 6523 identifier by setting the ICD part of the ISO 6523 identifier accordingly.  */
	'iso6523Code': 'https://schema.org/iso6523Code',
	/** The International Standard Recording Code for the recording. */
	'isrcCode': 'https://schema.org/isrcCode',
	/** The International Standard Serial Number (ISSN) that identifies this serial publication. You can repeat this property to identify different formats of, or the linking ISSN (ISSN-L) for, this serial publication. */
	'issn': 'https://schema.org/issn',
	/** Identifies the issue of publication; for example, "iii" or "2". */
	'issueNumber': 'https://schema.org/issueNumber',
	/** The organization issuing the ticket or permit. */
	'issuedBy': 'https://schema.org/issuedBy',
	/** The service through which the permit was granted. */
	'issuedThrough': 'https://schema.org/issuedThrough',
	/** The International Standard Musical Work Code for the composition. */
	'iswcCode': 'https://schema.org/iswcCode',
	/** An entity represented by an entry in a list or data feed (e.g. an 'artist' in a list of 'artists'). */
	'item': 'https://schema.org/item',
	/** A predefined value from OfferItemCondition specifying the condition of the product or service, or the products or services included in the offer. Also used for product return policies to specify the condition of products accepted for returns. */
	'itemCondition': 'https://schema.org/itemCondition',
	/** The type of return fees for returns of defect products. */
	'itemDefectReturnFees': 'https://schema.org/itemDefectReturnFees',
	/** The method (from an enumeration) by which the customer obtains a return shipping label for a defect product. */
	'itemDefectReturnLabelSource': 'https://schema.org/itemDefectReturnLabelSource',
	/** Amount of shipping costs for defect product returns. Applicable when property [[itemDefectReturnFees]] equals [[ReturnShippingFees]]. */
	'itemDefectReturnShippingFeesAmount': 'https://schema.org/itemDefectReturnShippingFeesAmount',
	/** For itemListElement values, you can use simple strings (e.g. "Peter", "Paul", "Mary"), existing entities, or use ListItem.\n\nText values are best if the elements in the list are plain strings. Existing entities are best for a simple, unordered list of existing things in your data. ListItem is used with ordered lists when you want to provide additional context about the element in that list or when the same item might be in different places in different lists.\n\nNote: The order of elements in your mark-up is not sufficient for indicating the order or elements.  Use ListItem with a 'position' property in such cases. */
	'itemListElement': 'https://schema.org/itemListElement',
	/** Type of ordering (e.g. Ascending, Descending, Unordered). */
	'itemListOrder': 'https://schema.org/itemListOrder',
	/** Current location of the item. */
	'itemLocation': 'https://schema.org/itemLocation',
	/** An item being offered (or demanded). The transactional nature of the offer or demand is documented using [[businessFunction]], e.g. sell, lease etc. While several common expected types are listed explicitly in this definition, others can be used. Using a second type, such as Product or a subtype of Product, can clarify the nature of the offer. */
	'itemOffered': 'https://schema.org/itemOffered',
	/** The item that is being reviewed/rated. */
	'itemReviewed': 'https://schema.org/itemReviewed',
	/** Item(s) being shipped. */
	'itemShipped': 'https://schema.org/itemShipped',
	/** Destination(s) ( [[Place]] ) that make up a trip. For a trip where destination order is important use [[ItemList]] to specify that order (see examples). */
	'itinerary': 'https://schema.org/itinerary',
	/** Systematic method of naming chemical compounds as recommended by the International Union of Pure and Applied Chemistry (IUPAC). */
	'iupacName': 'https://schema.org/iupacName',
	/** Description of benefits associated with the job. */
	'jobBenefits': 'https://schema.org/jobBenefits',
	/** An indicator as to whether a position is available for an immediate start. */
	'jobImmediateStart': 'https://schema.org/jobImmediateStart',
	/** A (typically single) geographic location associated with the job position. */
	'jobLocation': 'https://schema.org/jobLocation',
	/** A description of the job location (e.g. TELECOMMUTE for telecommute jobs). */
	'jobLocationType': 'https://schema.org/jobLocationType',
	/** The date on which a successful applicant for this job would be expected to start work. Choose a specific date in the future or use the jobImmediateStart property to indicate the position is to be filled as soon as possible. */
	'jobStartDate': 'https://schema.org/jobStartDate',
	/** The job title of the person (for example, Financial Manager). */
	'jobTitle': 'https://schema.org/jobTitle',
	/** Indicates a legal jurisdiction, e.g. of some legislation, or where some government service is based. */
	'jurisdiction': 'https://schema.org/jurisdiction',
	/** Keywords or tags used to describe some item. Multiple textual entries in a keywords list are typically delimited by commas, or by repeating the property. */
	'keywords': 'https://schema.org/keywords',
	/** A textual description of known damages, both repaired and unrepaired. */
	'knownVehicleDamages': 'https://schema.org/knownVehicleDamages',
	/** The most generic bi-directional social/work relation. */
	'knows': 'https://schema.org/knows',
	/** Of a [[Person]], and less typically of an [[Organization]], to indicate a topic that is known about - suggesting possible expertise but not implying it. We do not distinguish skill levels here, or relate this to educational content, events, objectives or [[JobPosting]] descriptions. */
	'knowsAbout': 'https://schema.org/knowsAbout',
	/** Of a [[Person]], and less typically of an [[Organization]], to indicate a known language. We do not distinguish skill levels or reading/writing/speaking/signing here. Use language codes from the [IETF BCP 47 standard](http://tools.ietf.org/html/bcp47). */
	'knowsLanguage': 'https://schema.org/knowsLanguage',
	/** Link to the drug's label details. */
	'labelDetails': 'https://schema.org/labelDetails',
	/** A sub property of participant. The owner of the real estate property. */
	'landlord': 'https://schema.org/landlord',
	/** A sub property of instrument. The language used on this action. */
	'language': 'https://schema.org/language',
	/** Date on which the content on this web page was last reviewed for accuracy and/or completeness. */
	'lastReviewed': 'https://schema.org/lastReviewed',
	/** The latitude of a location. For example ```37.42242``` ([WGS 84](https://en.wikipedia.org/wiki/World_Geodetic_System)). */
	'latitude': 'https://schema.org/latitude',
	/** A schematic image showing the floorplan layout. */
	'layoutImage': 'https://schema.org/layoutImage',
	/** The predominant type or kind characterizing the learning resource. For example, 'presentation', 'handout'. */
	'learningResourceType': 'https://schema.org/learningResourceType',
	/** Length of the lease for some [[Accommodation]], either particular to some [[Offer]] or in some cases intrinsic to the property. */
	'leaseLength': 'https://schema.org/leaseLength',
	/** The official name of the organization, e.g. the registered company name. */
	'legalName': 'https://schema.org/legalName',
	/** The drug or supplement's legal status, including any controlled substance schedules that apply. */
	'legalStatus': 'https://schema.org/legalStatus',
	/** Indicates that this legislation (or part of a legislation) somehow transfers another legislation in a different legislative context. This is an informative link, and it has no legal value. For legally-binding links of transposition, use the <a href="/legislationTransposes">legislationTransposes</a> property. For example an informative consolidated law of a European Union's member state "applies" the consolidated version of the European Directive implemented in it. */
	'legislationApplies': 'https://schema.org/legislationApplies',
	/** Another legislation that this legislation changes. This encompasses the notions of amendment, replacement, correction, repeal, or other types of change. This may be a direct change (textual or non-textual amendment) or a consequential or indirect change. The property is to be used to express the existence of a change relationship between two acts rather than the existence of a consolidated version of the text that shows the result of the change. For consolidation relationships, use the <a href="/legislationConsolidates">legislationConsolidates</a> property. */
	'legislationChanges': 'https://schema.org/legislationChanges',
	/** Indicates another legislation taken into account in this consolidated legislation (which is usually the product of an editorial process that revises the legislation). This property should be used multiple times to refer to both the original version or the previous consolidated version, and to the legislations making the change. */
	'legislationConsolidates': 'https://schema.org/legislationConsolidates',
	/** The date of adoption or signature of the legislation. This is the date at which the text is officially aknowledged to be a legislation, even though it might not even be published or in force. */
	'legislationDate': 'https://schema.org/legislationDate',
	/** The point-in-time at which the provided description of the legislation is valid (e.g.: when looking at the law on the 2016-04-07 (= dateVersion), I get the consolidation of 2015-04-12 of the "National Insurance Contributions Act 2015") */
	'legislationDateVersion': 'https://schema.org/legislationDateVersion',
	/** An identifier for the legislation. This can be either a string-based identifier, like the CELEX at EU level or the NOR in France, or a web-based, URL/URI identifier, like an ELI (European Legislation Identifier) or an URN-Lex. */
	'legislationIdentifier': 'https://schema.org/legislationIdentifier',
	/** The jurisdiction from which the legislation originates. */
	'legislationJurisdiction': 'https://schema.org/legislationJurisdiction',
	/** Whether the legislation is currently in force, not in force, or partially in force. */
	'legislationLegalForce': 'https://schema.org/legislationLegalForce',
	/** The legal value of this legislation file. The same legislation can be written in multiple files with different legal values. Typically a digitally signed PDF have a "stronger" legal value than the HTML file of the same act. */
	'legislationLegalValue': 'https://schema.org/legislationLegalValue',
	/** The person or organization that originally passed or made the law: typically parliament (for primary legislation) or government (for secondary legislation). This indicates the "legal author" of the law, as opposed to its physical author. */
	'legislationPassedBy': 'https://schema.org/legislationPassedBy',
	/** An individual or organization that has some kind of responsibility for the legislation. Typically the ministry who is/was in charge of elaborating the legislation, or the adressee for potential questions about the legislation once it is published. */
	'legislationResponsible': 'https://schema.org/legislationResponsible',
	/** Indicates that this legislation (or part of legislation) fulfills the objectives set by another legislation, by passing appropriate implementation measures. Typically, some legislations of European Union's member states or regions transpose European Directives. This indicates a legally binding link between the 2 legislations. */
	'legislationTransposes': 'https://schema.org/legislationTransposes',
	/** The type of the legislation. Examples of values are "law", "act", "directive", "decree", "regulation", "statutory instrument", "loi organique", "règlement grand-ducal", etc., depending on the country. */
	'legislationType': 'https://schema.org/legislationType',
	/** An organization identifier that uniquely identifies a legal entity as defined in ISO 17442. */
	'leiCode': 'https://schema.org/leiCode',
	/** A sub property of participant. The person that lends the object being borrowed. */
	'lender': 'https://schema.org/lender',
	/** This ordering relation for qualitative values indicates that the subject is lesser than the object. */
	'lesser': 'https://schema.org/lesser',
	/** This ordering relation for qualitative values indicates that the subject is lesser than or equal to the object. */
	'lesserOrEqual': 'https://schema.org/lesserOrEqual',
	/** The individual who adds lettering, including speech balloons and sound effects, to artwork. */
	'letterer': 'https://schema.org/letterer',
	/** A license document that applies to this content, typically indicated by URL. */
	'license': 'https://schema.org/license',
	/** A line is a point-to-point path consisting of two or more points. A line is expressed as a series of two or more point objects separated by space. */
	'line': 'https://schema.org/line',
	/** Indicates the relationship type of a Web link.  */
	'linkRelationship': 'https://schema.org/linkRelationship',
	/** An update to the LiveBlog. */
	'liveBlogUpdate': 'https://schema.org/liveBlogUpdate',
	/** Amount of mortgage mandate that can be converted into a proper mortgage at a later stage. */
	'loanMortgageMandateAmount': 'https://schema.org/loanMortgageMandateAmount',
	/** The amount of money to pay in a single payment. */
	'loanPaymentAmount': 'https://schema.org/loanPaymentAmount',
	/** Frequency of payments due, i.e. number of months between payments. This is defined as a frequency, i.e. the reciprocal of a period of time. */
	'loanPaymentFrequency': 'https://schema.org/loanPaymentFrequency',
	/** A form of paying back money previously borrowed from a lender. Repayment usually takes the form of periodic payments that normally include part principal plus interest in each payment. */
	'loanRepaymentForm': 'https://schema.org/loanRepaymentForm',
	/** The duration of the loan or credit agreement. */
	'loanTerm': 'https://schema.org/loanTerm',
	/** The type of a loan or credit. */
	'loanType': 'https://schema.org/loanType',
	/** The location of, for example, where an event is happening, where an organization is located, or where an action takes place. */
	'location': 'https://schema.org/location',
	/** The location where the CreativeWork was created, which may not be the same as the location depicted in the CreativeWork. */
	'locationCreated': 'https://schema.org/locationCreated',
	/** A full description of the lodging unit. */
	'lodgingUnitDescription': 'https://schema.org/lodgingUnitDescription',
	/** Textual description of the unit type (including suite vs. room, size of bed, etc.). */
	'lodgingUnitType': 'https://schema.org/lodgingUnitType',
	/** An associated logo. */
	'logo': 'https://schema.org/logo',
	/** The longitude of a location. For example ```-122.08585``` ([WGS 84](https://en.wikipedia.org/wiki/World_Geodetic_System)). */
	'longitude': 'https://schema.org/longitude',
	/** A sub property of participant. The loser of the action. */
	'loser': 'https://schema.org/loser',
	/** The lowest price of all offers available.\n\nUsage guidelines:\n\n* Use values from 0123456789 (Unicode 'DIGIT ZERO' (U+0030) to 'DIGIT NINE' (U+0039)) rather than superficially similar Unicode symbols.\n* Use '.' (Unicode 'FULL STOP' (U+002E)) rather than ',' to indicate a decimal point. Avoid using these symbols as a readability separator. */
	'lowPrice': 'https://schema.org/lowPrice',
	/** The person who wrote the words. */
	'lyricist': 'https://schema.org/lyricist',
	/** The words in the song. */
	'lyrics': 'https://schema.org/lyrics',
	/** Indicates if this web page element is the main subject of the page. */
	'mainContentOfPage': 'https://schema.org/mainContentOfPage',
	/** Indicates the primary entity described in some page or other CreativeWork. */
	'mainEntity': 'https://schema.org/mainEntity',
	/** Indicates a page (or other CreativeWork) for which this thing is the main entity being described. See [background notes](/docs/datamodel.html#mainEntityBackground) for details. */
	'mainEntityOfPage': 'https://schema.org/mainEntityOfPage',
	/** A maintainer of a [[Dataset]], software package ([[SoftwareApplication]]), or other [[Project]]. A maintainer is a [[Person]] or [[Organization]] that manages contributions to, and/or publication of, some (typically complex) artifact. It is common for distributions of software and data to be based on "upstream" sources. When [[maintainer]] is applied to a specific version of something e.g. a particular version or packaging of a [[Dataset]], it is always  possible that the upstream source has a different maintainer. The [[isBasedOn]] property can be used to indicate such relationships between datasets to make the different maintenance roles clear. Similarly in the case of software, a package may have dedicated maintainers working on integration into software distributions such as Ubuntu, as well as upstream maintainers of the underlying work.
       */
	'maintainer': 'https://schema.org/maintainer',
	/** A pointer to products or services offered by the organization or person. */
	'makesOffer': 'https://schema.org/makesOffer',
	/** The manufacturer of the product. */
	'manufacturer': 'https://schema.org/manufacturer',
	/** A URL to a map of the place. */
	'map': 'https://schema.org/map',
	/** Indicates the kind of Map, from the MapCategoryType Enumeration. */
	'mapType': 'https://schema.org/mapType',
	/** A URL to a map of the place. */
	'maps': 'https://schema.org/maps',
	/** A [[marginOfError]] for an [[Observation]]. */
	'marginOfError': 'https://schema.org/marginOfError',
	/** For a [[NewsMediaOrganization]], a link to the masthead page or a page listing top editorial management. */
	'masthead': 'https://schema.org/masthead',
	/** A material that something is made from, e.g. leather, wool, cotton, paper. */
	'material': 'https://schema.org/material',
	/** The quantity of the materials being described or an expression of the physical space they occupy. */
	'materialExtent': 'https://schema.org/materialExtent',
	/** A mathematical expression (e.g. 'x^2-3x=0') that may be solved for a specific variable, simplified, or transformed. This can take many formats, e.g. LaTeX, Ascii-Math, or math as you would write with a keyboard. */
	'mathExpression': 'https://schema.org/mathExpression',
	/** The highest price if the price is a range. */
	'maxPrice': 'https://schema.org/maxPrice',
	/** The upper value of some characteristic or property. */
	'maxValue': 'https://schema.org/maxValue',
	/** The total number of individuals that may attend an event or venue. */
	'maximumAttendeeCapacity': 'https://schema.org/maximumAttendeeCapacity',
	/** The maximum number of students who may be enrolled in the program. */
	'maximumEnrollment': 'https://schema.org/maximumEnrollment',
	/** Recommended intake of this supplement for a given population as defined by a specific recommending authority. */
	'maximumIntake': 'https://schema.org/maximumIntake',
	/** The maximum physical attendee capacity of an [[Event]] whose [[eventAttendanceMode]] is [[OfflineEventAttendanceMode]] (or the offline aspects, in the case of a [[MixedEventAttendanceMode]]).  */
	'maximumPhysicalAttendeeCapacity': 'https://schema.org/maximumPhysicalAttendeeCapacity',
	/** The maximum virtual attendee capacity of an [[Event]] whose [[eventAttendanceMode]] is [[OnlineEventAttendanceMode]] (or the online aspects, in the case of a [[MixedEventAttendanceMode]]).  */
	'maximumVirtualAttendeeCapacity': 'https://schema.org/maximumVirtualAttendeeCapacity',
	/** Description of the meals that will be provided or available for purchase. */
	'mealService': 'https://schema.org/mealService',
	/** The measuredProperty of an [[Observation]], typically via its [[StatisticalVariable]]. There are various kinds of applicable [[Property]]: a schema.org property, a property from other RDF-compatible systems, e.g. W3C RDF Data Cube, Data Commons, Wikidata, or schema.org extensions such as [GS1's](https://www.gs1.org/voc/?show=properties). */
	'measuredProperty': 'https://schema.org/measuredProperty',
	/** Identifies the denominator variable when an observation represents a ratio or percentage. */
	'measurementDenominator': 'https://schema.org/measurementDenominator',
	/** A subproperty of [[measurementTechnique]] that can be used for specifying specific methods, in particular via [[MeasurementMethodEnum]]. */
	'measurementMethod': 'https://schema.org/measurementMethod',
	/** Provides additional qualification to an observation. For example, a GDP observation measures the Nominal value. */
	'measurementQualifier': 'https://schema.org/measurementQualifier',
	/** A technique, method or technology used in an [[Observation]], [[StatisticalVariable]] or [[Dataset]] (or [[DataDownload]], [[DataCatalog]]), corresponding to the method used for measuring the corresponding variable(s) (for datasets, described using [[variableMeasured]]; for [[Observation]], a [[StatisticalVariable]]). Often but not necessarily each [[variableMeasured]] will have an explicit representation as (or mapping to) an property such as those defined in Schema.org, or other RDF vocabularies and "knowledge graphs". In that case the subproperty of [[variableMeasured]] called [[measuredProperty]] is applicable.
    
The [[measurementTechnique]] property helps when extra clarification is needed about how a [[measuredProperty]] was measured. This is oriented towards scientific and scholarly dataset publication but may have broader applicability; it is not intended as a full representation of measurement, but can often serve as a high level summary for dataset discovery. 

For example, if [[variableMeasured]] is: molecule concentration, [[measurementTechnique]] could be: "mass spectrometry" or "nmr spectroscopy" or "colorimetry" or "immunofluorescence". If the [[variableMeasured]] is "depression rating", the [[measurementTechnique]] could be "Zung Scale" or "HAM-D" or "Beck Depression Inventory". 

If there are several [[variableMeasured]] properties recorded for some given data object, use a [[PropertyValue]] for each [[variableMeasured]] and attach the corresponding [[measurementTechnique]]. The value can also be from an enumeration, organized as a [[MeasurementMetholdEnumeration]]. */
	'measurementTechnique': 'https://schema.org/measurementTechnique',
	/** The specific biochemical interaction through which this drug or supplement produces its pharmacological effect. */
	'mechanismOfAction': 'https://schema.org/mechanismOfAction',
	/** Indicates a MediaManipulationRatingEnumeration classification of a media object (in the context of how it was published or shared). */
	'mediaAuthenticityCategory': 'https://schema.org/mediaAuthenticityCategory',
	/** In the context of a [[MediaReview]], indicates specific media item(s) that are grouped using a [[MediaReviewItem]]. */
	'mediaItemAppearance': 'https://schema.org/mediaItemAppearance',
	/** The median value. */
	'median': 'https://schema.org/median',
	/** Medical audience for page. */
	'medicalAudience': 'https://schema.org/medicalAudience',
	/** A medical specialty of the provider. */
	'medicalSpecialty': 'https://schema.org/medicalSpecialty',
	/** The system of medicine that includes this MedicalEntity, for example 'evidence-based', 'homeopathic', 'chiropractic', etc. */
	'medicineSystem': 'https://schema.org/medicineSystem',
	/** Indicates that the vehicle meets the respective emission standard. */
	'meetsEmissionStandard': 'https://schema.org/meetsEmissionStandard',
	/** A member of an Organization or a ProgramMembership. Organizations can be members of organizations; ProgramMembership is typically for individuals. */
	'member': 'https://schema.org/member',
	/** An Organization (or ProgramMembership) to which this Person or Organization belongs. */
	'memberOf': 'https://schema.org/memberOf',
	/** A member of this organization. */
	'members': 'https://schema.org/members',
	/** A unique identifier for the membership. */
	'membershipNumber': 'https://schema.org/membershipNumber',
	/** The number of membership points earned by the member. If necessary, the unitText can be used to express the units the points are issued in. (E.g. stars, miles, etc.) */
	'membershipPointsEarned': 'https://schema.org/membershipPointsEarned',
	/** Minimum memory requirements. */
	'memoryRequirements': 'https://schema.org/memoryRequirements',
	/** Indicates that the CreativeWork contains a reference to, but is not necessarily about a concept. */
	'mentions': 'https://schema.org/mentions',
	/** Either the actual menu as a structured representation, as text, or a URL of the menu. */
	'menu': 'https://schema.org/menu',
	/** Additional menu item(s) such as a side dish of salad or side order of fries that can be added to this menu item. Additionally it can be a menu section containing allowed add-on menu items for this menu item. */
	'menuAddOn': 'https://schema.org/menuAddOn',
	/** 'merchant' is an out-dated term for 'seller'. */
	'merchant': 'https://schema.org/merchant',
	/** Specifies either a fixed return date or the number of days (from the delivery date) that a product can be returned. Used when the [[returnPolicyCategory]] property is specified as [[MerchantReturnFiniteReturnWindow]]. */
	'merchantReturnDays': 'https://schema.org/merchantReturnDays',
	/** Specifies a Web page or service by URL, for product returns. */
	'merchantReturnLink': 'https://schema.org/merchantReturnLink',
	/** A CreativeWork attached to the message. */
	'messageAttachment': 'https://schema.org/messageAttachment',
	/** The total distance travelled by the particular vehicle since its initial production, as read from its odometer.\n\nTypical unit code(s): KMT for kilometers, SMI for statute miles */
	'mileageFromOdometer': 'https://schema.org/mileageFromOdometer',
	/** The lowest price if the price is a range. */
	'minPrice': 'https://schema.org/minPrice',
	/** The lower value of some characteristic or property. */
	'minValue': 'https://schema.org/minValue',
	/** The minimum payment required at this time. */
	'minimumPaymentDue': 'https://schema.org/minimumPaymentDue',
	/** For a [[NewsMediaOrganization]], a statement on coverage priorities, including any public agenda or stance on issues. */
	'missionCoveragePrioritiesPolicy': 'https://schema.org/missionCoveragePrioritiesPolicy',
	/** The [[mobileUrl]] property is provided for specific situations in which data consumers need to determine whether one of several provided URLs is a dedicated 'mobile site'.

To discourage over-use, and reflecting intial usecases, the property is expected only on [[Product]] and [[Offer]], rather than [[Thing]]. The general trend in web technology is towards [responsive design](https://en.wikipedia.org/wiki/Responsive_web_design) in which content can be flexibly adapted to a wide range of browsing environments. Pages and sites referenced with the long-established [[url]] property should ideally also be usable on a wide variety of devices, including mobile phones. In most cases, it would be pointless and counter productive to attempt to update all [[url]] markup to use [[mobileUrl]] for more mobile-oriented pages. The property is intended for the case when items (primarily [[Product]] and [[Offer]]) have extra URLs hosted on an additional "mobile site" alongside the main one. It should not be taken as an endorsement of this publication style.
     */
	'mobileUrl': 'https://schema.org/mobileUrl',
	/** The model of the product. Use with the URL of a ProductModel or a textual representation of the model identifier. The URL of the ProductModel can be from an external source. It is recommended to additionally provide strong product identifiers via the gtin8/gtin13/gtin14 and mpn properties. */
	'model': 'https://schema.org/model',
	/** The release date of a vehicle model (often used to differentiate versions of the same make and model). */
	'modelDate': 'https://schema.org/modelDate',
	/** The date and time the reservation was modified. */
	'modifiedTime': 'https://schema.org/modifiedTime',
	/** The empirical formula is the simplest whole number ratio of all the atoms in a molecule. */
	'molecularFormula': 'https://schema.org/molecularFormula',
	/** This is the molecular weight of the entity being described, not of the parent. Units should be included in the form '&lt;Number&gt; &lt;unit&gt;', for example '12 amu' or as '&lt;QuantitativeValue&gt;. */
	'molecularWeight': 'https://schema.org/molecularWeight',
	/** The monoisotopic mass is the sum of the masses of the atoms in a molecule using the unbound, ground-state, rest mass of the principal (most abundant) isotope for each element instead of the isotopic average mass. Please include the units in the form '&lt;Number&gt; &lt;unit&gt;', for example '770.230488 g/mol' or as '&lt;QuantitativeValue&gt;. */
	'monoisotopicMolecularWeight': 'https://schema.org/monoisotopicMolecularWeight',
	/** The minimum payment is the lowest amount of money that one is required to pay on a credit card statement each month. */
	'monthlyMinimumRepaymentAmount': 'https://schema.org/monthlyMinimumRepaymentAmount',
	/** Indicates the minimal number of months of experience required for a position. */
	'monthsOfExperience': 'https://schema.org/monthsOfExperience',
	/** The Manufacturer Part Number (MPN) of the product, or the product to which the offer refers. */
	'mpn': 'https://schema.org/mpn',
	/** Whether multiple values are allowed for the property.  Default is false. */
	'multipleValues': 'https://schema.org/multipleValues',
	/** The movement the muscle generates. */
	'muscleAction': 'https://schema.org/muscleAction',
	/** An arrangement derived from the composition. */
	'musicArrangement': 'https://schema.org/musicArrangement',
	/** The composer of the soundtrack. */
	'musicBy': 'https://schema.org/musicBy',
	/** The type of composition (e.g. overture, sonata, symphony, etc.). */
	'musicCompositionForm': 'https://schema.org/musicCompositionForm',
	/** A member of a music group&#x2014;for example, John, Paul, George, or Ringo. */
	'musicGroupMember': 'https://schema.org/musicGroupMember',
	/** Format of this release (the type of recording media used, i.e. compact disc, digital media, LP, etc.). */
	'musicReleaseFormat': 'https://schema.org/musicReleaseFormat',
	/** The key, mode, or scale this composition uses. */
	'musicalKey': 'https://schema.org/musicalKey',
	/** The North American Industry Classification System (NAICS) code for a particular organization or business person. */
	'naics': 'https://schema.org/naics',
	/** The name of the item. */
	'name': 'https://schema.org/name',
	/** A position played, performed or filled by a person or organization, as part of an organization. For example, an athlete in a SportsTeam might play in the position named 'Quarterback'. */
	'namedPosition': 'https://schema.org/namedPosition',
	/** Nationality of the person. */
	'nationality': 'https://schema.org/nationality',
	/** The expected progression of the condition if it is not treated and allowed to progress naturally. */
	'naturalProgression': 'https://schema.org/naturalProgression',
	/** Provides negative considerations regarding something, most typically in pro/con lists for reviews (alongside [[positiveNotes]]). For symmetry 

In the case of a [[Review]], the property describes the [[itemReviewed]] from the perspective of the review; in the case of a [[Product]], the product itself is being described. Since product descriptions 
tend to emphasise positive claims, it may be relatively unusual to find [[negativeNotes]] used in this way. Nevertheless for the sake of symmetry, [[negativeNotes]] can be used on [[Product]].

The property values can be expressed either as unstructured text (repeated as necessary), or if ordered, as a list (in which case the most negative is at the beginning of the list). */
	'negativeNotes': 'https://schema.org/negativeNotes',
	/** The underlying innervation associated with the muscle. */
	'nerve': 'https://schema.org/nerve',
	/** The neurological pathway extension that involves muscle control. */
	'nerveMotor': 'https://schema.org/nerveMotor',
	/** The total financial value of the person as calculated by subtracting assets from liabilities. */
	'netWorth': 'https://schema.org/netWorth',
	/** Indicates a page with news updates and guidelines. This could often be (but is not required to be) the main page containing [[SpecialAnnouncement]] markup on a site. */
	'newsUpdatesAndGuidelines': 'https://schema.org/newsUpdatesAndGuidelines',
	/** A link to the ListItem that follows the current one. */
	'nextItem': 'https://schema.org/nextItem',
	/** For a [[NewsMediaOrganization]] or other news-related [[Organization]], a statement explaining when authors of articles are not named in bylines. */
	'noBylinesPolicy': 'https://schema.org/noBylinesPolicy',
	/** This ordering relation for qualitative values indicates that the subject is not equal to the object. */
	'nonEqual': 'https://schema.org/nonEqual',
	/** The generic name of this drug or supplement. */
	'nonProprietaryName': 'https://schema.org/nonProprietaryName',
	/** nonprofitStatus indicates the legal status of a non-profit organization in its primary place of business. */
	'nonprofitStatus': 'https://schema.org/nonprofitStatus',
	/** Range of acceptable values for a typical patient, when applicable. */
	'normalRange': 'https://schema.org/normalRange',
	/** Indicates the [NATO stock number](https://en.wikipedia.org/wiki/NATO_Stock_Number) (nsn) of a [[Product]].  */
	'nsn': 'https://schema.org/nsn',
	/** The number of adults staying in the unit. */
	'numAdults': 'https://schema.org/numAdults',
	/** The number of children staying in the unit. */
	'numChildren': 'https://schema.org/numChildren',
	/** Indicates the number of constraints property values defined for a particular [[ConstraintNode]] such as [[StatisticalVariable]]. This helps applications understand if they have access to a sufficiently complete description of a [[StatisticalVariable]] or other construct that is defined using properties on template-style nodes. */
	'numConstraints': 'https://schema.org/numConstraints',
	/** The number of tracks in this album or playlist. */
	'numTracks': 'https://schema.org/numTracks',
	/** Indicates the total (available plus unavailable) number of accommodation units in an [[ApartmentComplex]], or the number of accommodation units for a specific [[FloorPlan]] (within its specific [[ApartmentComplex]]). See also [[numberOfAvailableAccommodationUnits]]. */
	'numberOfAccommodationUnits': 'https://schema.org/numberOfAccommodationUnits',
	/** The number or type of airbags in the vehicle. */
	'numberOfAirbags': 'https://schema.org/numberOfAirbags',
	/** Indicates the number of available accommodation units in an [[ApartmentComplex]], or the number of accommodation units for a specific [[FloorPlan]] (within its specific [[ApartmentComplex]]). See also [[numberOfAccommodationUnits]]. */
	'numberOfAvailableAccommodationUnits': 'https://schema.org/numberOfAvailableAccommodationUnits',
	/** The number of axles.\n\nTypical unit code(s): C62 */
	'numberOfAxles': 'https://schema.org/numberOfAxles',
	/** The total integer number of bathrooms in some [[Accommodation]], following real estate conventions as [documented in RESO](https://ddwiki.reso.org/display/DDW17/BathroomsTotalInteger+Field): "The simple sum of the number of bathrooms. For example for a property with two Full Bathrooms and one Half Bathroom, the Bathrooms Total Integer will be 3.". See also [[numberOfRooms]]. */
	'numberOfBathroomsTotal': 'https://schema.org/numberOfBathroomsTotal',
	/** The total integer number of bedrooms in a some [[Accommodation]], [[ApartmentComplex]] or [[FloorPlan]]. */
	'numberOfBedrooms': 'https://schema.org/numberOfBedrooms',
	/** The quantity of the given bed type available in the HotelRoom, Suite, House, or Apartment. */
	'numberOfBeds': 'https://schema.org/numberOfBeds',
	/** The number of credits or units awarded by a Course or required to complete an EducationalOccupationalProgram. */
	'numberOfCredits': 'https://schema.org/numberOfCredits',
	/** The number of doors.\n\nTypical unit code(s): C62 */
	'numberOfDoors': 'https://schema.org/numberOfDoors',
	/** The number of employees in an organization, e.g. business. */
	'numberOfEmployees': 'https://schema.org/numberOfEmployees',
	/** The number of episodes in this season or series. */
	'numberOfEpisodes': 'https://schema.org/numberOfEpisodes',
	/** The total number of forward gears available for the transmission system of the vehicle.\n\nTypical unit code(s): C62 */
	'numberOfForwardGears': 'https://schema.org/numberOfForwardGears',
	/** Number of full bathrooms - The total number of full and ¾ bathrooms in an [[Accommodation]]. This corresponds to the [BathroomsFull field in RESO](https://ddwiki.reso.org/display/DDW17/BathroomsFull+Field). */
	'numberOfFullBathrooms': 'https://schema.org/numberOfFullBathrooms',
	/** The number of items in an ItemList. Note that some descriptions might not fully describe all items in a list (e.g., multi-page pagination); in such cases, the numberOfItems would be for the entire list. */
	'numberOfItems': 'https://schema.org/numberOfItems',
	/** The number of payments contractually required at origination to repay the loan. For monthly paying loans this is the number of months from the contractual first payment date to the maturity date. */
	'numberOfLoanPayments': 'https://schema.org/numberOfLoanPayments',
	/** The number of pages in the book. */
	'numberOfPages': 'https://schema.org/numberOfPages',
	/** Number of partial bathrooms - The total number of half and ¼ bathrooms in an [[Accommodation]]. This corresponds to the [BathroomsPartial field in RESO](https://ddwiki.reso.org/display/DDW17/BathroomsPartial+Field).  */
	'numberOfPartialBathrooms': 'https://schema.org/numberOfPartialBathrooms',
	/** Indicate how many people can play this game (minimum, maximum, or range). */
	'numberOfPlayers': 'https://schema.org/numberOfPlayers',
	/** The number of owners of the vehicle, including the current one.\n\nTypical unit code(s): C62 */
	'numberOfPreviousOwners': 'https://schema.org/numberOfPreviousOwners',
	/** The number of rooms (excluding bathrooms and closets) of the accommodation or lodging business.
Typical unit code(s): ROM for room or C62 for no unit. The type of room can be put in the unitText property of the QuantitativeValue. */
	'numberOfRooms': 'https://schema.org/numberOfRooms',
	/** The number of seasons in this series. */
	'numberOfSeasons': 'https://schema.org/numberOfSeasons',
	/** A number associated with a role in an organization, for example, the number on an athlete's jersey. */
	'numberedPosition': 'https://schema.org/numberedPosition',
	/** Nutrition information about the recipe or menu item. */
	'nutrition': 'https://schema.org/nutrition',
	/** The object upon which the action is carried out, whose state is kept intact or changed. Also known as the semantic roles patient, affected or undergoer (which change their state) or theme (which doesn't). E.g. John read *a book*. */
	'object': 'https://schema.org/object',
	/** The [[observationAbout]] property identifies an entity, often a [[Place]], associated with an [[Observation]]. */
	'observationAbout': 'https://schema.org/observationAbout',
	/** The observationDate of an [[Observation]]. */
	'observationDate': 'https://schema.org/observationDate',
	/** The length of time an Observation took place over. The format follows `P[0-9]*[Y|M|D|h|m|s]`. For example, P1Y is Period 1 Year, P3M is Period 3 Months, P3h is Period 3 hours. */
	'observationPeriod': 'https://schema.org/observationPeriod',
	/** The allowed total occupancy for the accommodation in persons (including infants etc). For individual accommodations, this is not necessarily the legal maximum but defines the permitted usage as per the contractual agreement (e.g. a double room used by a single person).
Typical unit code(s): C62 for person */
	'occupancy': 'https://schema.org/occupancy',
	/**  The region/country for which this occupational description is appropriate. Note that educational requirements and qualifications can vary between jurisdictions. */
	'occupationLocation': 'https://schema.org/occupationLocation',
	/** A category describing the job, preferably using a term from a taxonomy such as [BLS O*NET-SOC](http://www.onetcenter.org/taxonomy.html), [ISCO-08](https://www.ilo.org/public/english/bureau/stat/isco/isco08/) or similar, with the property repeated for each applicable value. Ideally the taxonomy should be identified, and both the textual label and formal code for the category should be provided.\n
Note: for historical reasons, any textual label and formal code provided as a literal may be assumed to be from O*NET-SOC. */
	'occupationalCategory': 'https://schema.org/occupationalCategory',
	/** A description of the qualification, award, certificate, diploma or other occupational credential awarded as a consequence of successful completion of this course or program. */
	'occupationalCredentialAwarded': 'https://schema.org/occupationalCredentialAwarded',
	/** The number of offers for the product. */
	'offerCount': 'https://schema.org/offerCount',
	/** A pointer to the organization or person making the offer. */
	'offeredBy': 'https://schema.org/offeredBy',
	/** An offer to provide this item&#x2014;for example, an offer to sell a product, rent the DVD of a movie, perform a service, or give away tickets to an event. Use [[businessFunction]] to indicate the kind of transaction offered, i.e. sell, lease, etc. This property can also be used to describe a [[Demand]]. While this property is listed as expected on a number of common types, it can be used in others. In that case, using a second type, such as Product or a subtype of Product, can clarify the nature of the offer.
       */
	'offers': 'https://schema.org/offers',
	/** Whether prescriptions can be delivered by mail. */
	'offersPrescriptionByMail': 'https://schema.org/offersPrescriptionByMail',
	/** The general opening hours for a business. Opening hours can be specified as a weekly time range, starting with days, then times per day. Multiple days can be listed with commas ',' separating each day. Day or time ranges are specified using a hyphen '-'.\n\n* Days are specified using the following two-letter combinations: ```Mo```, ```Tu```, ```We```, ```Th```, ```Fr```, ```Sa```, ```Su```.\n* Times are specified using 24:00 format. For example, 3pm is specified as ```15:00```, 10am as ```10:00```. \n* Here is an example: <code>&lt;time itemprop="openingHours" datetime=&quot;Tu,Th 16:00-20:00&quot;&gt;Tuesdays and Thursdays 4-8pm&lt;/time&gt;</code>.\n* If a business is open 7 days a week, then it can be specified as <code>&lt;time itemprop=&quot;openingHours&quot; datetime=&quot;Mo-Su&quot;&gt;Monday through Sunday, all day&lt;/time&gt;</code>. */
	'openingHours': 'https://schema.org/openingHours',
	/** The opening hours of a certain place. */
	'openingHoursSpecification': 'https://schema.org/openingHoursSpecification',
	/** The opening hour of the place or service on the given day(s) of the week. */
	'opens': 'https://schema.org/opens',
	/** Operating systems supported (Windows 7, OS X 10.6, Android 1.6). */
	'operatingSystem': 'https://schema.org/operatingSystem',
	/** A sub property of participant. The opponent on this action. */
	'opponent': 'https://schema.org/opponent',
	/** A sub property of object. The options subject to this action. */
	'option': 'https://schema.org/option',
	/** Date order was placed. */
	'orderDate': 'https://schema.org/orderDate',
	/** The delivery of the parcel related to this order or order item. */
	'orderDelivery': 'https://schema.org/orderDelivery',
	/** The identifier of the order item. */
	'orderItemNumber': 'https://schema.org/orderItemNumber',
	/** The current status of the order item. */
	'orderItemStatus': 'https://schema.org/orderItemStatus',
	/** The identifier of the transaction. */
	'orderNumber': 'https://schema.org/orderNumber',
	/** The number of the item ordered. If the property is not set, assume the quantity is one. */
	'orderQuantity': 'https://schema.org/orderQuantity',
	/** The current status of the order. */
	'orderStatus': 'https://schema.org/orderStatus',
	/** The item ordered. */
	'orderedItem': 'https://schema.org/orderedItem',
	/** An organizer of an Event. */
	'organizer': 'https://schema.org/organizer',
	/** Shipper's address. */
	'originAddress': 'https://schema.org/originAddress',
	/** Describes, in a [[MediaReview]] when dealing with [[DecontextualizedContent]], background information that can contribute to better interpretation of the [[MediaObject]]. */
	'originalMediaContextDescription': 'https://schema.org/originalMediaContextDescription',
	/** Link to the page containing an original version of the content, or directly to an online copy of the original [[MediaObject]] content, e.g. video file. */
	'originalMediaLink': 'https://schema.org/originalMediaLink',
	/** The vasculature the lymphatic structure originates, or afferents, from. */
	'originatesFrom': 'https://schema.org/originatesFrom',
	/** Any information related to overdose on a drug, including signs or symptoms, treatments, contact information for emergency response. */
	'overdosage': 'https://schema.org/overdosage',
	/** The date and time of obtaining the product. */
	'ownedFrom': 'https://schema.org/ownedFrom',
	/** The date and time of giving up ownership on the product. */
	'ownedThrough': 'https://schema.org/ownedThrough',
	/** For an [[Organization]] (often but not necessarily a [[NewsMediaOrganization]]), a description of organizational ownership structure; funding and grants. In a news/media setting, this is with particular reference to editorial independence.   Note that the [[funder]] is also available and can be used to make basic funder information machine-readable. */
	'ownershipFundingInfo': 'https://schema.org/ownershipFundingInfo',
	/** Products owned by the organization or person. */
	'owns': 'https://schema.org/owns',
	/** The page on which the work ends; for example "138" or "xvi". */
	'pageEnd': 'https://schema.org/pageEnd',
	/** The page on which the work starts; for example "135" or "xiii". */
	'pageStart': 'https://schema.org/pageStart',
	/** Any description of pages that is not separated into pageStart and pageEnd; for example, "1-6, 9, 55" or "10-12, 46-49". */
	'pagination': 'https://schema.org/pagination',
	/** A parent of this person. */
	'parent': 'https://schema.org/parent',
	/** The parent of a question, answer or item in general. */
	'parentItem': 'https://schema.org/parentItem',
	/** The larger organization that this organization is a [[subOrganization]] of, if any. */
	'parentOrganization': 'https://schema.org/parentOrganization',
	/** A broadcast service to which the broadcast service may belong to such as regional variations of a national channel. */
	'parentService': 'https://schema.org/parentService',
	/** Closest parent taxon of the taxon in question. */
	'parentTaxon': 'https://schema.org/parentTaxon',
	/** A parents of the person. */
	'parents': 'https://schema.org/parents',
	/** The episode to which this clip belongs. */
	'partOfEpisode': 'https://schema.org/partOfEpisode',
	/** The order is being paid as part of the referenced Invoice. */
	'partOfInvoice': 'https://schema.org/partOfInvoice',
	/** The overall order the items in this delivery were included in. */
	'partOfOrder': 'https://schema.org/partOfOrder',
	/** The season to which this episode belongs. */
	'partOfSeason': 'https://schema.org/partOfSeason',
	/** The series to which this episode or season belongs. */
	'partOfSeries': 'https://schema.org/partOfSeries',
	/** The anatomical or organ system that this structure is part of. */
	'partOfSystem': 'https://schema.org/partOfSystem',
	/** The TV series to which this episode or season belongs. */
	'partOfTVSeries': 'https://schema.org/partOfTVSeries',
	/** Identifies that this [[Trip]] is a subTrip of another Trip.  For example Day 1, Day 2, etc. of a multi-day trip. */
	'partOfTrip': 'https://schema.org/partOfTrip',
	/** Other co-agents that participated in the action indirectly. E.g. John wrote a book with *Steve*. */
	'participant': 'https://schema.org/participant',
	/** Number of people the reservation should accommodate. */
	'partySize': 'https://schema.org/partySize',
	/** The priority status assigned to a passenger for security or boarding (e.g. FastTrack or Priority). */
	'passengerPriorityStatus': 'https://schema.org/passengerPriorityStatus',
	/** The passenger's sequence number as assigned by the airline. */
	'passengerSequenceNumber': 'https://schema.org/passengerSequenceNumber',
	/** Changes in the normal mechanical, physical, and biochemical functions that are associated with this activity or condition. */
	'pathophysiology': 'https://schema.org/pathophysiology',
	/** A pattern that something has, for example 'polka dot', 'striped', 'Canadian flag'. Values are typically expressed as text, although links to controlled value schemes are also supported. */
	'pattern': 'https://schema.org/pattern',
	/** The permitted weight of passengers and cargo, EXCLUDING the weight of the empty vehicle.\n\nTypical unit code(s): KGM for kilogram, LBR for pound\n\n* Note 1: Many databases specify the permitted TOTAL weight instead, which is the sum of [[weight]] and [[payload]]\n* Note 2: You can indicate additional information in the [[name]] of the [[QuantitativeValue]] node.\n* Note 3: You may also link to a [[QualitativeValue]] node that provides additional information using [[valueReference]].\n* Note 4: Note that you can use [[minValue]] and [[maxValue]] to indicate ranges. */
	'payload': 'https://schema.org/payload',
	/** Cash, Credit Card, Cryptocurrency, Local Exchange Tradings System, etc. */
	'paymentAccepted': 'https://schema.org/paymentAccepted',
	/** The date that payment is due. */
	'paymentDue': 'https://schema.org/paymentDue',
	/** The date that payment is due. */
	'paymentDueDate': 'https://schema.org/paymentDueDate',
	/** The name of the credit card or other method of payment for the order. */
	'paymentMethod': 'https://schema.org/paymentMethod',
	/** An identifier for the method of payment used (e.g. the last 4 digits of the credit card). */
	'paymentMethodId': 'https://schema.org/paymentMethodId',
	/** The status of payment; whether the invoice has been paid or not. */
	'paymentStatus': 'https://schema.org/paymentStatus',
	/** The URL for sending a payment. */
	'paymentUrl': 'https://schema.org/paymentUrl',
	/** The individual who draws the primary narrative artwork. */
	'penciler': 'https://schema.org/penciler',
	/** The 10th percentile value. */
	'percentile10': 'https://schema.org/percentile10',
	/** The 25th percentile value. */
	'percentile25': 'https://schema.org/percentile25',
	/** The 75th percentile value. */
	'percentile75': 'https://schema.org/percentile75',
	/** The 90th percentile value. */
	'percentile90': 'https://schema.org/percentile90',
	/** The length of time it takes to perform instructions or a direction (not including time to prepare the supplies), in [ISO 8601 duration format](http://en.wikipedia.org/wiki/ISO_8601). */
	'performTime': 'https://schema.org/performTime',
	/** A performer at the event&#x2014;for example, a presenter, musician, musical group or actor. */
	'performer': 'https://schema.org/performer',
	/** Event that this person is a performer or participant in. */
	'performerIn': 'https://schema.org/performerIn',
	/** The main performer or performers of the event&#x2014;for example, a presenter, musician, or actor. */
	'performers': 'https://schema.org/performers',
	/** The type of permission granted the person, organization, or audience. */
	'permissionType': 'https://schema.org/permissionType',
	/** Permission(s) required to run the app (for example, a mobile app may require full internet access or may run only on wifi). */
	'permissions': 'https://schema.org/permissions',
	/** The target audience for this permit. */
	'permitAudience': 'https://schema.org/permitAudience',
	/** Indications regarding the permitted usage of the accommodation. */
	'permittedUsage': 'https://schema.org/permittedUsage',
	/** Indicates whether pets are allowed to enter the accommodation or lodging business. More detailed information can be put in a text value. */
	'petsAllowed': 'https://schema.org/petsAllowed',
	/** Representation of a text [[textValue]] using the specified [[speechToTextMarkup]]. For example the city name of Houston in IPA: /ˈhjuːstən/. */
	'phoneticText': 'https://schema.org/phoneticText',
	/** A photograph of this place. */
	'photo': 'https://schema.org/photo',
	/** Photographs of this place. */
	'photos': 'https://schema.org/photos',
	/** A description of the types of physical activity associated with the job. Defined terms such as those in O*net may be used, but note that there is no way to specify the level of ability as well as its nature when using a defined term. */
	'physicalRequirement': 'https://schema.org/physicalRequirement',
	/** Specific physiologic benefits associated to the plan. */
	'physiologicalBenefits': 'https://schema.org/physiologicalBenefits',
	/** Where a taxi will pick up a passenger or a rental car can be picked up. */
	'pickupLocation': 'https://schema.org/pickupLocation',
	/** When a taxi will pick up a passenger or a rental car can be picked up. */
	'pickupTime': 'https://schema.org/pickupTime',
	/** Indicates whether this game is multi-player, co-op or single-player.  The game can be marked as multi-player, co-op and single-player at the same time. */
	'playMode': 'https://schema.org/playMode',
	/** Player type required&#x2014;for example, Flash or Silverlight. */
	'playerType': 'https://schema.org/playerType',
	/** Number of players on the server. */
	'playersOnline': 'https://schema.org/playersOnline',
	/** A polygon is the area enclosed by a point-to-point path for which the starting and ending points are the same. A polygon is expressed as a series of four or more space delimited points where the first and final points are identical. */
	'polygon': 'https://schema.org/polygon',
	/** Indicates the populationType common to all members of a [[StatisticalPopulation]] or all cases within the scope of a [[StatisticalVariable]]. */
	'populationType': 'https://schema.org/populationType',
	/** The position of an item in a series or sequence of items. */
	'position': 'https://schema.org/position',
	/** Provides positive considerations regarding something, for example product highlights or (alongside [[negativeNotes]]) pro/con lists for reviews.

In the case of a [[Review]], the property describes the [[itemReviewed]] from the perspective of the review; in the case of a [[Product]], the product itself is being described.

The property values can be expressed either as unstructured text (repeated as necessary), or if ordered, as a list (in which case the most positive is at the beginning of the list). */
	'positiveNotes': 'https://schema.org/positiveNotes',
	/** A possible unexpected and unfavorable evolution of a medical condition. Complications may include worsening of the signs or symptoms of the disease, extension of the condition to other organ systems, etc. */
	'possibleComplication': 'https://schema.org/possibleComplication',
	/** A possible treatment to address this condition, sign or symptom. */
	'possibleTreatment': 'https://schema.org/possibleTreatment',
	/** The post office box number for PO box addresses. */
	'postOfficeBoxNumber': 'https://schema.org/postOfficeBoxNumber',
	/** A description of the postoperative procedures, care, and/or followups for this device. */
	'postOp': 'https://schema.org/postOp',
	/** The postal code. For example, 94043. */
	'postalCode': 'https://schema.org/postalCode',
	/** First postal code in a range (included). */
	'postalCodeBegin': 'https://schema.org/postalCodeBegin',
	/** Last postal code in the range (included). Needs to be after [[postalCodeBegin]]. */
	'postalCodeEnd': 'https://schema.org/postalCodeEnd',
	/** A defined range of postal codes indicated by a common textual prefix. Used for non-numeric systems such as UK. */
	'postalCodePrefix': 'https://schema.org/postalCodePrefix',
	/** A defined range of postal codes. */
	'postalCodeRange': 'https://schema.org/postalCodeRange',
	/** Indicates a potential Action, which describes an idealized action in which this thing would play an 'object' role. */
	'potentialAction': 'https://schema.org/potentialAction',
	/** Intended use of the BioChemEntity by humans. */
	'potentialUse': 'https://schema.org/potentialUse',
	/** A description of the workup, testing, and other preparations required before implanting this device. */
	'preOp': 'https://schema.org/preOp',
	/** A pointer from a previous, often discontinued variant of the product to its newer variant. */
	'predecessorOf': 'https://schema.org/predecessorOf',
	/** Pregnancy category of this drug. */
	'pregnancyCategory': 'https://schema.org/pregnancyCategory',
	/** Any precaution, guidance, contraindication, etc. related to this drug's use during pregnancy. */
	'pregnancyWarning': 'https://schema.org/pregnancyWarning',
	/** The length of time it takes to prepare the items to be used in instructions or a direction, in [ISO 8601 duration format](http://en.wikipedia.org/wiki/ISO_8601). */
	'prepTime': 'https://schema.org/prepTime',
	/** Typical preparation that a patient must undergo before having the procedure performed. */
	'preparation': 'https://schema.org/preparation',
	/** Link to prescribing information for the drug. */
	'prescribingInfo': 'https://schema.org/prescribingInfo',
	/** Indicates the status of drug prescription, e.g. local catalogs classifications or whether the drug is available by prescription or over-the-counter, etc. */
	'prescriptionStatus': 'https://schema.org/prescriptionStatus',
	/** A link to the ListItem that precedes the current one. */
	'previousItem': 'https://schema.org/previousItem',
	/** Used in conjunction with eventStatus for rescheduled or cancelled events. This property contains the previously scheduled start date. For rescheduled events, the startDate property should be used for the newly scheduled start date. In the (rare) case of an event that has been postponed and rescheduled multiple times, this field may be repeated. */
	'previousStartDate': 'https://schema.org/previousStartDate',
	/** The offer price of a product, or of a price component when attached to PriceSpecification and its subtypes.\n\nUsage guidelines:\n\n* Use the [[priceCurrency]] property (with standard formats: [ISO 4217 currency format](http://en.wikipedia.org/wiki/ISO_4217), e.g. "USD"; [Ticker symbol](https://en.wikipedia.org/wiki/List_of_cryptocurrencies) for cryptocurrencies, e.g. "BTC"; well known names for [Local Exchange Trading Systems](https://en.wikipedia.org/wiki/Local_exchange_trading_system) (LETS) and other currency types, e.g. "Ithaca HOUR") instead of including [ambiguous symbols](http://en.wikipedia.org/wiki/Dollar_sign#Currencies_that_use_the_dollar_or_peso_sign) such as '$' in the value.\n* Use '.' (Unicode 'FULL STOP' (U+002E)) rather than ',' to indicate a decimal point. Avoid using these symbols as a readability separator.\n* Note that both [RDFa](http://www.w3.org/TR/xhtml-rdfa-primer/#using-the-content-attribute) and Microdata syntax allow the use of a "content=" attribute for publishing simple machine-readable values alongside more human-friendly formatting.\n* Use values from 0123456789 (Unicode 'DIGIT ZERO' (U+0030) to 'DIGIT NINE' (U+0039)) rather than superficially similar Unicode symbols.
       */
	'price': 'https://schema.org/price',
	/** This property links to all [[UnitPriceSpecification]] nodes that apply in parallel for the [[CompoundPriceSpecification]] node. */
	'priceComponent': 'https://schema.org/priceComponent',
	/** Identifies a price component (for example, a line item on an invoice), part of the total price for an offer. */
	'priceComponentType': 'https://schema.org/priceComponentType',
	/** The currency of the price, or a price component when attached to [[PriceSpecification]] and its subtypes.\n\nUse standard formats: [ISO 4217 currency format](http://en.wikipedia.org/wiki/ISO_4217), e.g. "USD"; [Ticker symbol](https://en.wikipedia.org/wiki/List_of_cryptocurrencies) for cryptocurrencies, e.g. "BTC"; well known names for [Local Exchange Trading Systems](https://en.wikipedia.org/wiki/Local_exchange_trading_system) (LETS) and other currency types, e.g. "Ithaca HOUR". */
	'priceCurrency': 'https://schema.org/priceCurrency',
	/** The price range of the business, for example ```$$$```. */
	'priceRange': 'https://schema.org/priceRange',
	/** One or more detailed price specifications, indicating the unit price and delivery or payment charges. */
	'priceSpecification': 'https://schema.org/priceSpecification',
	/** Defines the type of a price specified for an offered product, for example a list price, a (temporary) sale price or a manufacturer suggested retail price. If multiple prices are specified for an offer the [[priceType]] property can be used to identify the type of each such specified price. The value of priceType can be specified as a value from enumeration PriceTypeEnumeration or as a free form text string for price types that are not already predefined in PriceTypeEnumeration. */
	'priceType': 'https://schema.org/priceType',
	/** The date after which the price is no longer available. */
	'priceValidUntil': 'https://schema.org/priceValidUntil',
	/** Indicates the main image on the page. */
	'primaryImageOfPage': 'https://schema.org/primaryImageOfPage',
	/** A preventative therapy used to prevent an initial occurrence of the medical condition, such as vaccination. */
	'primaryPrevention': 'https://schema.org/primaryPrevention',
	/** The number of the column in which the NewsArticle appears in the print edition. */
	'printColumn': 'https://schema.org/printColumn',
	/** The edition of the print product in which the NewsArticle appears. */
	'printEdition': 'https://schema.org/printEdition',
	/** If this NewsArticle appears in print, this field indicates the name of the page on which the article is found. Please note that this field is intended for the exact page name (e.g. A5, B18). */
	'printPage': 'https://schema.org/printPage',
	/** If this NewsArticle appears in print, this field indicates the print section in which the article appeared. */
	'printSection': 'https://schema.org/printSection',
	/** A description of the procedure involved in setting up, using, and/or installing the device. */
	'procedure': 'https://schema.org/procedure',
	/** The type of procedure, for example Surgical, Noninvasive, or Percutaneous. */
	'procedureType': 'https://schema.org/procedureType',
	/** Estimated processing time for the service using this channel. */
	'processingTime': 'https://schema.org/processingTime',
	/** Processor architecture required to run the application (e.g. IA64). */
	'processorRequirements': 'https://schema.org/processorRequirements',
	/** The person or organization who produced the work (e.g. music album, movie, TV/radio series etc.). */
	'producer': 'https://schema.org/producer',
	/** The tangible thing generated by the service, e.g. a passport, permit, etc. */
	'produces': 'https://schema.org/produces',
	/** Indicates a textual identifier for a ProductGroup. */
	'productGroupID': 'https://schema.org/productGroupID',
	/** The product identifier, such as ISBN. For example: ``` meta itemprop="productID" content="isbn:123-456-789" ```. */
	'productID': 'https://schema.org/productID',
	/** The product or service this support contact point is related to (such as product support for a particular product line). This can be a specific product or product line (e.g. "iPhone") or a general category of products or services (e.g. "smartphones"). */
	'productSupported': 'https://schema.org/productSupported',
	/** The production company or studio responsible for the item, e.g. series, video game, episode etc. */
	'productionCompany': 'https://schema.org/productionCompany',
	/** The date of production of the item, e.g. vehicle. */
	'productionDate': 'https://schema.org/productionDate',
	/** Proficiency needed for this content; expected values: 'Beginner', 'Expert'. */
	'proficiencyLevel': 'https://schema.org/proficiencyLevel',
	/** Any membership in a frequent flyer, hotel loyalty program, etc. being applied to the reservation. */
	'programMembershipUsed': 'https://schema.org/programMembershipUsed',
	/** The program providing the membership. */
	'programName': 'https://schema.org/programName',
	/** Prerequisites for enrolling in the program. */
	'programPrerequisites': 'https://schema.org/programPrerequisites',
	/** The type of educational or occupational program. For example, classroom, internship, alternance, etc. */
	'programType': 'https://schema.org/programType',
	/** The computer programming language. */
	'programmingLanguage': 'https://schema.org/programmingLanguage',
	/** Indicates whether API is managed or unmanaged. */
	'programmingModel': 'https://schema.org/programmingModel',
	/** A commonly used identifier for the characteristic represented by the property, e.g. a manufacturer or a standard code for a property. propertyID can be
(1) a prefixed string, mainly meant to be used with standards for product properties; (2) a site-specific, non-prefixed string (e.g. the primary key of the property or the vendor-specific ID of the property), or (3)
a URL indicating the type of the property, either pointing to an external vocabulary, or a Web resource that describes the property (e.g. a glossary entry).
Standards bodies should promote a standard prefix for the identifiers of properties from their standards. */
	'propertyID': 'https://schema.org/propertyID',
	/** Proprietary name given to the diet plan, typically by its originator or creator. */
	'proprietaryName': 'https://schema.org/proprietaryName',
	/** The number of grams of protein. */
	'proteinContent': 'https://schema.org/proteinContent',
	/** The service provider, service operator, or service performer; the goods producer. Another party (a seller) may offer those services or goods on behalf of the provider. A provider may also serve as the seller. */
	'provider': 'https://schema.org/provider',
	/** Indicates the mobility of a provided service (e.g. 'static', 'dynamic'). */
	'providerMobility': 'https://schema.org/providerMobility',
	/** The BroadcastService offered on this channel. */
	'providesBroadcastService': 'https://schema.org/providesBroadcastService',
	/** The service provided by this channel. */
	'providesService': 'https://schema.org/providesService',
	/** A flag to signal that the [[Place]] is open to public visitors.  If this property is omitted there is no assumed default boolean value */
	'publicAccess': 'https://schema.org/publicAccess',
	/** Information about public transport closures. */
	'publicTransportClosuresInfo': 'https://schema.org/publicTransportClosuresInfo',
	/** A publication event associated with the item. */
	'publication': 'https://schema.org/publication',
	/** The type of the medical article, taken from the US NLM MeSH publication type catalog. See also [MeSH documentation](http://www.nlm.nih.gov/mesh/pubtypes.html). */
	'publicationType': 'https://schema.org/publicationType',
	/** An agent associated with the publication event. */
	'publishedBy': 'https://schema.org/publishedBy',
	/** A broadcast service associated with the publication event. */
	'publishedOn': 'https://schema.org/publishedOn',
	/** The publisher of the creative work. */
	'publisher': 'https://schema.org/publisher',
	/** The publishing division which published the comic. */
	'publisherImprint': 'https://schema.org/publisherImprint',
	/** The publishingPrinciples property indicates (typically via [[URL]]) a document describing the editorial principles of an [[Organization]] (or individual, e.g. a [[Person]] writing a blog) that relate to their activities as a publisher, e.g. ethics or diversity policies. When applied to a [[CreativeWork]] (e.g. [[NewsArticle]]) the principles are those of the party primarily responsible for the creation of the [[CreativeWork]].

While such policies are most typically expressed in natural language, sometimes related information (e.g. indicating a [[funder]]) can be expressed using schema.org terminology.
 */
	'publishingPrinciples': 'https://schema.org/publishingPrinciples',
	/** The date the item, e.g. vehicle, was purchased by the current owner. */
	'purchaseDate': 'https://schema.org/purchaseDate',
	/** Specific qualifications required for this role or Occupation. */
	'qualifications': 'https://schema.org/qualifications',
	/** Guidelines about quarantine rules, e.g. in the context of a pandemic. */
	'quarantineGuidelines': 'https://schema.org/quarantineGuidelines',
	/** A sub property of instrument. The query used on this action. */
	'query': 'https://schema.org/query',
	/** The task that a player-controlled character, or group of characters may complete in order to gain a reward. */
	'quest': 'https://schema.org/quest',
	/** A sub property of object. A question. */
	'question': 'https://schema.org/question',
	/** Relates a property to a class that constitutes (one of) the expected type(s) for values of the property. */
	'rangeIncludes': 'https://schema.org/rangeIncludes',
	/** The count of total number of ratings. */
	'ratingCount': 'https://schema.org/ratingCount',
	/** A short explanation (e.g. one to two sentences) providing background context and other information that led to the conclusion expressed in the rating. This is particularly applicable to ratings associated with "fact check" markup using [[ClaimReview]]. */
	'ratingExplanation': 'https://schema.org/ratingExplanation',
	/** The rating for the content.\n\nUsage guidelines:\n\n* Use values from 0123456789 (Unicode 'DIGIT ZERO' (U+0030) to 'DIGIT NINE' (U+0039)) rather than superficially similar Unicode symbols.\n* Use '.' (Unicode 'FULL STOP' (U+002E)) rather than ',' to indicate a decimal point. Avoid using these symbols as a readability separator. */
	'ratingValue': 'https://schema.org/ratingValue',
	/** A person who reads (performs) the audiobook. */
	'readBy': 'https://schema.org/readBy',
	/** Whether or not a property is mutable.  Default is false. Specifying this for a property that also has a value makes it act similar to a "hidden" input in an HTML form. */
	'readonlyValue': 'https://schema.org/readonlyValue',
	/** A sub property of participant. The real estate agent involved in the action. */
	'realEstateAgent': 'https://schema.org/realEstateAgent',
	/** A sub property of instrument. The recipe/instructions used to perform the action. */
	'recipe': 'https://schema.org/recipe',
	/** The category of the recipe—for example, appetizer, entree, etc. */
	'recipeCategory': 'https://schema.org/recipeCategory',
	/** The cuisine of the recipe (for example, French or Ethiopian). */
	'recipeCuisine': 'https://schema.org/recipeCuisine',
	/** A single ingredient used in the recipe, e.g. sugar, flour or garlic. */
	'recipeIngredient': 'https://schema.org/recipeIngredient',
	/** A step in making the recipe, in the form of a single item (document, video, etc.) or an ordered list with HowToStep and/or HowToSection items. */
	'recipeInstructions': 'https://schema.org/recipeInstructions',
	/** The quantity produced by the recipe (for example, number of people served, number of servings, etc). */
	'recipeYield': 'https://schema.org/recipeYield',
	/** A sub property of participant. The participant who is at the receiving end of the action. */
	'recipient': 'https://schema.org/recipient',
	/** An organization that acknowledges the validity, value or utility of a credential. Note: recognition may include a process of quality assurance or accreditation. */
	'recognizedBy': 'https://schema.org/recognizedBy',
	/** If applicable, the organization that officially recognizes this entity as part of its endorsed system of medicine. */
	'recognizingAuthority': 'https://schema.org/recognizingAuthority',
	/** Strength of the guideline's recommendation (e.g. 'class I'). */
	'recommendationStrength': 'https://schema.org/recommendationStrength',
	/** Recommended intake of this supplement for a given population as defined by a specific recommending authority. */
	'recommendedIntake': 'https://schema.org/recommendedIntake',
	/** The label that issued the release. */
	'recordLabel': 'https://schema.org/recordLabel',
	/** An audio recording of the work. */
	'recordedAs': 'https://schema.org/recordedAs',
	/** The Event where the CreativeWork was recorded. The CreativeWork may capture all or part of the event. */
	'recordedAt': 'https://schema.org/recordedAt',
	/** The CreativeWork that captured all or part of this Event. */
	'recordedIn': 'https://schema.org/recordedIn',
	/** The composition this track is a recording of. */
	'recordingOf': 'https://schema.org/recordingOf',
	/** The only way you get the money back in the event of default is the security. Recourse is where you still have the opportunity to go back to the borrower for the rest of the money. */
	'recourseLoan': 'https://schema.org/recourseLoan',
	/** The reference quantity for which a certain price applies, e.g. 1 EUR per 4 kWh of electricity. This property is a replacement for unitOfMeasurement for the advanced cases where the price does not relate to a standard unit. */
	'referenceQuantity': 'https://schema.org/referenceQuantity',
	/** The Order(s) related to this Invoice. One or more Orders may be combined into a single Invoice. */
	'referencesOrder': 'https://schema.org/referencesOrder',
	/** A refund type, from an enumerated list. */
	'refundType': 'https://schema.org/refundType',
	/** The anatomical or organ system drained by this vessel; generally refers to a specific part of an organ. */
	'regionDrained': 'https://schema.org/regionDrained',
	/** The regions where the media is allowed. If not specified, then it's assumed to be allowed everywhere. Specify the countries in [ISO 3166 format](http://en.wikipedia.org/wiki/ISO_3166). */
	'regionsAllowed': 'https://schema.org/regionsAllowed',
	/** Anatomical systems or structures that relate to the superficial anatomy. */
	'relatedAnatomy': 'https://schema.org/relatedAnatomy',
	/** A medical condition associated with this anatomy. */
	'relatedCondition': 'https://schema.org/relatedCondition',
	/** Any other drug related to this one, for example commonly-prescribed alternatives. */
	'relatedDrug': 'https://schema.org/relatedDrug',
	/** A link related to this web page, for example to other related web pages. */
	'relatedLink': 'https://schema.org/relatedLink',
	/** Related anatomical structure(s) that are not part of the system but relate or connect to it, such as vascular bundles associated with an organ system. */
	'relatedStructure': 'https://schema.org/relatedStructure',
	/** A medical therapy related to this anatomy. */
	'relatedTherapy': 'https://schema.org/relatedTherapy',
	/** The most generic familial relation. */
	'relatedTo': 'https://schema.org/relatedTo',
	/** The release date of a product or product model. This can be used to distinguish the exact variant of a product. */
	'releaseDate': 'https://schema.org/releaseDate',
	/** Description of what changed in this version. */
	'releaseNotes': 'https://schema.org/releaseNotes',
	/** The album this is a release of. */
	'releaseOf': 'https://schema.org/releaseOf',
	/** The place and time the release was issued, expressed as a PublicationEvent. */
	'releasedEvent': 'https://schema.org/releasedEvent',
	/** The Occupation for the JobPosting. */
	'relevantOccupation': 'https://schema.org/relevantOccupation',
	/** If applicable, a medical specialty in which this entity is relevant. */
	'relevantSpecialty': 'https://schema.org/relevantSpecialty',
	/** The number of attendee places for an event that remain unallocated. */
	'remainingAttendeeCapacity': 'https://schema.org/remainingAttendeeCapacity',
	/** Whether the terms for payment of interest can be renegotiated during the life of the loan. */
	'renegotiableLoan': 'https://schema.org/renegotiableLoan',
	/** Defines the number of times a recurring [[Event]] will take place. */
	'repeatCount': 'https://schema.org/repeatCount',
	/** Defines the frequency at which [[Event]]s will occur according to a schedule [[Schedule]]. The intervals between
      events should be defined as a [[Duration]] of time. */
	'repeatFrequency': 'https://schema.org/repeatFrequency',
	/** Number of times one should repeat the activity. */
	'repetitions': 'https://schema.org/repetitions',
	/** A sub property of object. The object that is being replaced. */
	'replacee': 'https://schema.org/replacee',
	/** A sub property of object. The object that replaces. */
	'replacer': 'https://schema.org/replacer',
	/** The URL at which a reply may be posted to the specified UserComment. */
	'replyToUrl': 'https://schema.org/replyToUrl',
	/** The number or other unique designator assigned to a Report by the publishing organization. */
	'reportNumber': 'https://schema.org/reportNumber',
	/** Indicates whether this image is representative of the content of the page. */
	'representativeOfPage': 'https://schema.org/representativeOfPage',
	/** Assets required to secure loan or credit repayments. It may take form of third party pledge, goods, financial instruments (cash, securities, etc.) */
	'requiredCollateral': 'https://schema.org/requiredCollateral',
	/** Audiences defined by a person's gender. */
	'requiredGender': 'https://schema.org/requiredGender',
	/** Audiences defined by a person's maximum age. */
	'requiredMaxAge': 'https://schema.org/requiredMaxAge',
	/** Audiences defined by a person's minimum age. */
	'requiredMinAge': 'https://schema.org/requiredMinAge',
	/** The required quantity of the item(s). */
	'requiredQuantity': 'https://schema.org/requiredQuantity',
	/** Component dependency requirements for application. This includes runtime environments and shared libraries that are not included in the application distribution package, but required to run the application (examples: DirectX, Java or .NET runtime). */
	'requirements': 'https://schema.org/requirements',
	/** Indicates if use of the media require a subscription  (either paid or free). Allowed values are ```true``` or ```false``` (note that an earlier version had 'yes', 'no'). */
	'requiresSubscription': 'https://schema.org/requiresSubscription',
	/** The thing -- flight, event, restaurant, etc. being reserved. */
	'reservationFor': 'https://schema.org/reservationFor',
	/** A unique identifier for the reservation. */
	'reservationId': 'https://schema.org/reservationId',
	/** The current status of the reservation. */
	'reservationStatus': 'https://schema.org/reservationStatus',
	/** A ticket associated with the reservation. */
	'reservedTicket': 'https://schema.org/reservedTicket',
	/** Responsibilities associated with this role or Occupation. */
	'responsibilities': 'https://schema.org/responsibilities',
	/** How often one should break from the activity. */
	'restPeriods': 'https://schema.org/restPeriods',
	/** Use [[MonetaryAmount]] to specify a fixed restocking fee for product returns, or use [[Number]] to specify a percentage of the product price paid by the customer. */
	'restockingFee': 'https://schema.org/restockingFee',
	/** The result produced in the action. E.g. John wrote *a book*. */
	'result': 'https://schema.org/result',
	/** A sub property of result. The Comment created or sent as a result of this action. */
	'resultComment': 'https://schema.org/resultComment',
	/** A sub property of result. The review that resulted in the performing of the action. */
	'resultReview': 'https://schema.org/resultReview',
	/** The type of return fees for purchased products (for any return reason). */
	'returnFees': 'https://schema.org/returnFees',
	/** The method (from an enumeration) by which the customer obtains a return shipping label for a product returned for any reason. */
	'returnLabelSource': 'https://schema.org/returnLabelSource',
	/** The type of return method offered, specified from an enumeration. */
	'returnMethod': 'https://schema.org/returnMethod',
	/** Specifies an applicable return policy (from an enumeration). */
	'returnPolicyCategory': 'https://schema.org/returnPolicyCategory',
	/** The country where the product has to be sent to for returns, for example "Ireland" using the [[name]] property of [[Country]]. You can also provide the two-letter [ISO 3166-1 alpha-2 country code](http://en.wikipedia.org/wiki/ISO_3166-1). Note that this can be different from the country where the product was originally shipped from or sent to. */
	'returnPolicyCountry': 'https://schema.org/returnPolicyCountry',
	/** Seasonal override of a return policy. */
	'returnPolicySeasonalOverride': 'https://schema.org/returnPolicySeasonalOverride',
	/** Amount of shipping costs for product returns (for any reason). Applicable when property [[returnFees]] equals [[ReturnShippingFees]]. */
	'returnShippingFeesAmount': 'https://schema.org/returnShippingFeesAmount',
	/** A review of the item. */
	'review': 'https://schema.org/review',
	/** This Review or Rating is relevant to this part or facet of the itemReviewed. */
	'reviewAspect': 'https://schema.org/reviewAspect',
	/** The actual body of the review. */
	'reviewBody': 'https://schema.org/reviewBody',
	/** The count of total number of reviews. */
	'reviewCount': 'https://schema.org/reviewCount',
	/** The rating given in this review. Note that reviews can themselves be rated. The ```reviewRating``` applies to rating given by the review. The [[aggregateRating]] property applies to the review itself, as a creative work. */
	'reviewRating': 'https://schema.org/reviewRating',
	/** People or organizations that have reviewed the content on this web page for accuracy and/or completeness. */
	'reviewedBy': 'https://schema.org/reviewedBy',
	/** Review of the item. */
	'reviews': 'https://schema.org/reviews',
	/** A modifiable or non-modifiable factor that increases the risk of a patient contracting this condition, e.g. age,  coexisting condition. */
	'riskFactor': 'https://schema.org/riskFactor',
	/** Specific physiologic risks associated to the diet plan. */
	'risks': 'https://schema.org/risks',
	/** A role played, performed or filled by a person or organization. For example, the team of creators for a comic book might fill the roles named 'inker', 'penciller', and 'letterer'; or an athlete in a SportsTeam might play in the position named 'Quarterback'. */
	'roleName': 'https://schema.org/roleName',
	/** The permitted total weight of cargo and installations (e.g. a roof rack) on top of the vehicle.\n\nTypical unit code(s): KGM for kilogram, LBR for pound\n\n* Note 1: You can indicate additional information in the [[name]] of the [[QuantitativeValue]] node.\n* Note 2: You may also link to a [[QualitativeValue]] node that provides additional information using [[valueReference]]\n* Note 3: Note that you can use [[minValue]] and [[maxValue]] to indicate ranges. */
	'roofLoad': 'https://schema.org/roofLoad',
	/** The response (yes, no, maybe) to the RSVP. */
	'rsvpResponse': 'https://schema.org/rsvpResponse',
	/** The vasculature the lymphatic structure runs, or efferents, to. */
	'runsTo': 'https://schema.org/runsTo',
	/** Runtime platform or script interpreter dependencies (example: Java v1, Python 2.3, .NET Framework 3.0). */
	'runtime': 'https://schema.org/runtime',
	/** Runtime platform or script interpreter dependencies (example: Java v1, Python 2.3, .NET Framework 3.0). */
	'runtimePlatform': 'https://schema.org/runtimePlatform',
	/** The RxCUI drug identifier from RXNORM. */
	'rxcui': 'https://schema.org/rxcui',
	/** Any potential safety concern associated with the supplement. May include interactions with other drugs and foods, pregnancy, breastfeeding, known adverse reactions, and documented efficacy of the supplement. */
	'safetyConsideration': 'https://schema.org/safetyConsideration',
	/** The currency (coded using [ISO 4217](http://en.wikipedia.org/wiki/ISO_4217)) used for the main salary information in this job posting or for this employee. */
	'salaryCurrency': 'https://schema.org/salaryCurrency',
	/** The expected salary upon completing the training. */
	'salaryUponCompletion': 'https://schema.org/salaryUponCompletion',
	/** URL of a reference Web page that unambiguously indicates the item's identity. E.g. the URL of the item's Wikipedia page, Wikidata entry, or official website. */
	'sameAs': 'https://schema.org/sameAs',
	/** What type of code sample: full (compile ready) solution, code snippet, inline code, scripts, template. */
	'sampleType': 'https://schema.org/sampleType',
	/** The number of grams of saturated fat. */
	'saturatedFatContent': 'https://schema.org/saturatedFatContent',
	/** Indicates the timezone for which the time(s) indicated in the [[Schedule]] are given. The value provided should be among those listed in the IANA Time Zone Database. */
	'scheduleTimezone': 'https://schema.org/scheduleTimezone',
	/** The date the invoice is scheduled to be paid. */
	'scheduledPaymentDate': 'https://schema.org/scheduledPaymentDate',
	/** The time the object is scheduled to. */
	'scheduledTime': 'https://schema.org/scheduledTime',
	/** Indicates (by URL or string) a particular version of a schema used in some CreativeWork. This property was created primarily to
    indicate the use of a specific schema.org release, e.g. ```10.0``` as a simple string, or more explicitly via URL, ```https://schema.org/docs/releases.html#v10.0```. There may be situations in which other schemas might usefully be referenced this way, e.g. ```http://dublincore.org/specifications/dublin-core/dces/1999-07-02/``` but this has not been carefully explored in the community. */
	'schemaVersion': 'https://schema.org/schemaVersion',
	/** Information about school closures. */
	'schoolClosuresInfo': 'https://schema.org/schoolClosuresInfo',
	/** The number of screens in the movie theater. */
	'screenCount': 'https://schema.org/screenCount',
	/** A link to a screenshot image of the app. */
	'screenshot': 'https://schema.org/screenshot',
	/** Indicates the date on which the current structured data was generated / published. Typically used alongside [[sdPublisher]] */
	'sdDatePublished': 'https://schema.org/sdDatePublished',
	/** A license document that applies to this structured data, typically indicated by URL. */
	'sdLicense': 'https://schema.org/sdLicense',
	/** Indicates the party responsible for generating and publishing the current structured data markup, typically in cases where the structured data is derived automatically from existing published content but published on a different site. For example, student projects and open data initiatives often re-publish existing content with more explicitly structured metadata. The
[[sdPublisher]] property helps make such practices more explicit. */
	'sdPublisher': 'https://schema.org/sdPublisher',
	/** A season in a media series. */
	'season': 'https://schema.org/season',
	/** Position of the season within an ordered group of seasons. */
	'seasonNumber': 'https://schema.org/seasonNumber',
	/** A season in a media series. */
	'seasons': 'https://schema.org/seasons',
	/** The location of the reserved seat (e.g., 27). */
	'seatNumber': 'https://schema.org/seatNumber',
	/** The row location of the reserved seat (e.g., B). */
	'seatRow': 'https://schema.org/seatRow',
	/** The section location of the reserved seat (e.g. Orchestra). */
	'seatSection': 'https://schema.org/seatSection',
	/** The number of persons that can be seated (e.g. in a vehicle), both in terms of the physical space available, and in terms of limitations set by law.\n\nTypical unit code(s): C62 for persons  */
	'seatingCapacity': 'https://schema.org/seatingCapacity',
	/** The type/class of the seat. */
	'seatingType': 'https://schema.org/seatingType',
	/** A preventative therapy used to prevent reoccurrence of the medical condition after an initial episode of the condition. */
	'secondaryPrevention': 'https://schema.org/secondaryPrevention',
	/** A description of any security clearance requirements of the job. */
	'securityClearanceRequirement': 'https://schema.org/securityClearanceRequirement',
	/** The type of security screening the passenger is subject to. */
	'securityScreening': 'https://schema.org/securityScreening',
	/** A pointer to products or services sought by the organization or person (demand). */
	'seeks': 'https://schema.org/seeks',
	/** An entity which offers (sells / leases / lends / loans) the services / goods.  A seller may also be a provider. */
	'seller': 'https://schema.org/seller',
	/** A sub property of participant. The participant who is at the sending end of the action. */
	'sender': 'https://schema.org/sender',
	/** A description of any sensory requirements and levels necessary to function on the job, including hearing and vision. Defined terms such as those in O*net may be used, but note that there is no way to specify the level of ability as well as its nature when using a defined term. */
	'sensoryRequirement': 'https://schema.org/sensoryRequirement',
	/** The neurological pathway extension that inputs and sends information to the brain or spinal cord. */
	'sensoryUnit': 'https://schema.org/sensoryUnit',
	/** The serial number or any alphanumeric identifier of a particular product. When attached to an offer, it is a shortcut for the serial number of the product included in the offer. */
	'serialNumber': 'https://schema.org/serialNumber',
	/** A possible serious complication and/or serious side effect of this therapy. Serious adverse outcomes include those that are life-threatening; result in death, disability, or permanent damage; require hospitalization or prolong existing hospitalization; cause congenital anomalies or birth defects; or jeopardize the patient and may require medical or surgical intervention to prevent one of the outcomes in this definition. */
	'seriousAdverseOutcome': 'https://schema.org/seriousAdverseOutcome',
	/** Status of a game server. */
	'serverStatus': 'https://schema.org/serverStatus',
	/** The cuisine of the restaurant. */
	'servesCuisine': 'https://schema.org/servesCuisine',
	/** The geographic area where the service is provided. */
	'serviceArea': 'https://schema.org/serviceArea',
	/** The audience eligible for this service. */
	'serviceAudience': 'https://schema.org/serviceAudience',
	/** The location (e.g. civic structure, local business, etc.) where a person can go to access the service. */
	'serviceLocation': 'https://schema.org/serviceLocation',
	/** The operating organization, if different from the provider.  This enables the representation of services that are provided by an organization, but operated by another organization like a subcontractor. */
	'serviceOperator': 'https://schema.org/serviceOperator',
	/** The tangible thing generated by the service, e.g. a passport, permit, etc. */
	'serviceOutput': 'https://schema.org/serviceOutput',
	/** The phone number to use to access the service. */
	'servicePhone': 'https://schema.org/servicePhone',
	/** The address for accessing the service by mail. */
	'servicePostalAddress': 'https://schema.org/servicePostalAddress',
	/** The number to access the service by text message. */
	'serviceSmsNumber': 'https://schema.org/serviceSmsNumber',
	/** The type of service being offered, e.g. veterans' benefits, emergency relief, etc. */
	'serviceType': 'https://schema.org/serviceType',
	/** The website to access the service. */
	'serviceUrl': 'https://schema.org/serviceUrl',
	/** The serving size, in terms of the number of volume or mass. */
	'servingSize': 'https://schema.org/servingSize',
	/** The [SHA-2](https://en.wikipedia.org/wiki/SHA-2) SHA256 hash of the content of the item. For example, a zero-length input has value 'e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855' */
	'sha256': 'https://schema.org/sha256',
	/** A CreativeWork such as an image, video, or audio clip shared as part of this posting. */
	'sharedContent': 'https://schema.org/sharedContent',
	/** indicates (possibly multiple) shipping destinations. These can be defined in several ways, e.g. postalCode ranges. */
	'shippingDestination': 'https://schema.org/shippingDestination',
	/** Indicates information about the shipping policies and options associated with an [[Offer]]. */
	'shippingDetails': 'https://schema.org/shippingDetails',
	/** Label to match an [[OfferShippingDetails]] with a [[ShippingRateSettings]] (within the context of a [[shippingSettingsLink]] cross-reference). */
	'shippingLabel': 'https://schema.org/shippingLabel',
	/** Indicates the origin of a shipment, i.e. where it should be coming from. */
	'shippingOrigin': 'https://schema.org/shippingOrigin',
	/** The shipping rate is the cost of shipping to the specified destination. Typically, the maxValue and currency values (of the [[MonetaryAmount]]) are most appropriate. */
	'shippingRate': 'https://schema.org/shippingRate',
	/** Link to a page containing [[ShippingRateSettings]] and [[DeliveryTimeSettings]] details. */
	'shippingSettingsLink': 'https://schema.org/shippingSettingsLink',
	/** A sibling of the person. */
	'sibling': 'https://schema.org/sibling',
	/** A sibling of the person. */
	'siblings': 'https://schema.org/siblings',
	/** A sign detected by the test. */
	'signDetected': 'https://schema.org/signDetected',
	/** A sign or symptom of this condition. Signs are objective or physically observable manifestations of the medical condition while symptoms are the subjective experience of the medical condition. */
	'signOrSymptom': 'https://schema.org/signOrSymptom',
	/** The significance associated with the superficial anatomy; as an example, how characteristics of the superficial anatomy can suggest underlying medical conditions or courses of treatment. */
	'significance': 'https://schema.org/significance',
	/** One of the more significant URLs on the page. Typically, these are the non-navigation links that are clicked on the most. */
	'significantLink': 'https://schema.org/significantLink',
	/** The most significant URLs on the page. Typically, these are the non-navigation links that are clicked on the most. */
	'significantLinks': 'https://schema.org/significantLinks',
	/** A standardized size of a product or creative work, specified either through a simple textual string (for example 'XL', '32Wx34L'), a  QuantitativeValue with a unitCode, or a comprehensive and structured [[SizeSpecification]]; in other cases, the [[width]], [[height]], [[depth]] and [[weight]] properties may be more applicable.  */
	'size': 'https://schema.org/size',
	/** The size group (also known as "size type") for a product's size. Size groups are common in the fashion industry to define size segments and suggested audiences for wearable products. Multiple values can be combined, for example "men's big and tall", "petite maternity" or "regular" */
	'sizeGroup': 'https://schema.org/sizeGroup',
	/** The size system used to identify a product's size. Typically either a standard (for example, "GS1" or "ISO-EN13402"), country code (for example "US" or "JP"), or a measuring system (for example "Metric" or "Imperial"). */
	'sizeSystem': 'https://schema.org/sizeSystem',
	/** A statement of knowledge, skill, ability, task or any other assertion expressing a competency that is desired or required to fulfill this role or to work in this occupation. */
	'skills': 'https://schema.org/skills',
	/** The Stock Keeping Unit (SKU), i.e. a merchant-specific identifier for a product or service, or the product to which the offer refers. */
	'sku': 'https://schema.org/sku',
	/** A slogan or motto associated with the item. */
	'slogan': 'https://schema.org/slogan',
	/** A specification in form of a line notation for describing the structure of chemical species using short ASCII strings.  Double bond stereochemistry \ indicators may need to be escaped in the string in formats where the backslash is an escape character. */
	'smiles': 'https://schema.org/smiles',
	/** Indicates whether it is allowed to smoke in the place, e.g. in the restaurant, hotel or hotel room. */
	'smokingAllowed': 'https://schema.org/smokingAllowed',
	/** The number of milligrams of sodium. */
	'sodiumContent': 'https://schema.org/sodiumContent',
	/** Additional content for a software application. */
	'softwareAddOn': 'https://schema.org/softwareAddOn',
	/** Software application help. */
	'softwareHelp': 'https://schema.org/softwareHelp',
	/** Component dependency requirements for application. This includes runtime environments and shared libraries that are not included in the application distribution package, but required to run the application (examples: DirectX, Java or .NET runtime). */
	'softwareRequirements': 'https://schema.org/softwareRequirements',
	/** Version of the software instance. */
	'softwareVersion': 'https://schema.org/softwareVersion',
	/** The Organization on whose behalf the creator was working. */
	'sourceOrganization': 'https://schema.org/sourceOrganization',
	/** The neurological pathway that originates the neurons. */
	'sourcedFrom': 'https://schema.org/sourcedFrom',
	/** The "spatial" property can be used in cases when more specific properties
(e.g. [[locationCreated]], [[spatialCoverage]], [[contentLocation]]) are not known to be appropriate. */
	'spatial': 'https://schema.org/spatial',
	/** The spatialCoverage of a CreativeWork indicates the place(s) which are the focus of the content. It is a subproperty of
      contentLocation intended primarily for more technical and detailed materials. For example with a Dataset, it indicates
      areas that the dataset describes: a dataset of New York weather would have spatialCoverage which was the place: the state of New York. */
	'spatialCoverage': 'https://schema.org/spatialCoverage',
	/** Indicates sections of a Web page that are particularly 'speakable' in the sense of being highlighted as being especially appropriate for text-to-speech conversion. Other sections of a page may also be usefully spoken in particular circumstances; the 'speakable' property serves to indicate the parts most likely to be generally useful for speech.

The *speakable* property can be repeated an arbitrary number of times, with three kinds of possible 'content-locator' values:

1.) *id-value* URL references - uses *id-value* of an element in the page being annotated. The simplest use of *speakable* has (potentially relative) URL values, referencing identified sections of the document concerned.

2.) CSS Selectors - addresses content in the annotated page, e.g. via class attribute. Use the [[cssSelector]] property.

3.)  XPaths - addresses content via XPaths (assuming an XML view of the content). Use the [[xpath]] property.


For more sophisticated markup of speakable sections beyond simple ID references, either CSS selectors or XPath expressions to pick out document section(s) as speakable. For this
we define a supporting type, [[SpeakableSpecification]]  which is defined to be a possible value of the *speakable* property.
          */
	'speakable': 'https://schema.org/speakable',
	/** Any special commitments associated with this job posting. Valid entries include VeteranCommit, MilitarySpouseCommit, etc. */
	'specialCommitments': 'https://schema.org/specialCommitments',
	/** The special opening hours of a certain place.\n\nUse this to explicitly override general opening hours brought in scope by [[openingHoursSpecification]] or [[openingHours]].
       */
	'specialOpeningHoursSpecification': 'https://schema.org/specialOpeningHoursSpecification',
	/** One of the domain specialities to which this web page's content applies. */
	'specialty': 'https://schema.org/specialty',
	/** Form of markup used. eg. [SSML](https://www.w3.org/TR/speech-synthesis11) or [IPA](https://www.wikidata.org/wiki/Property:P898). */
	'speechToTextMarkup': 'https://schema.org/speechToTextMarkup',
	/** The speed range of the vehicle. If the vehicle is powered by an engine, the upper limit of the speed range (indicated by [[maxValue]]) should be the maximum speed achievable under regular conditions.\n\nTypical unit code(s): KMH for km/h, HM for mile per hour (0.447 04 m/s), KNT for knot\n\n*Note 1: Use [[minValue]] and [[maxValue]] to indicate the range. Typically, the minimal value is zero.\n* Note 2: There are many different ways of measuring the speed range. You can link to information about how the given value has been determined using the [[valueReference]] property. */
	'speed': 'https://schema.org/speed',
	/** The (e.g. fictional) character, Person or Organization to whom the quotation is attributed within the containing CreativeWork. */
	'spokenByCharacter': 'https://schema.org/spokenByCharacter',
	/** A person or organization that supports a thing through a pledge, promise, or financial contribution. E.g. a sponsor of a Medical Study or a corporate sponsor of an event. */
	'sponsor': 'https://schema.org/sponsor',
	/** A type of sport (e.g. Baseball). */
	'sport': 'https://schema.org/sport',
	/** A sub property of location. The sports activity location where this action occurred. */
	'sportsActivityLocation': 'https://schema.org/sportsActivityLocation',
	/** A sub property of location. The sports event where this action occurred. */
	'sportsEvent': 'https://schema.org/sportsEvent',
	/** A sub property of participant. The sports team that participated on this action. */
	'sportsTeam': 'https://schema.org/sportsTeam',
	/** The person's spouse. */
	'spouse': 'https://schema.org/spouse',
	/** The stage of the condition, if applicable. */
	'stage': 'https://schema.org/stage',
	/** The stage represented as a number, e.g. 3. */
	'stageAsNumber': 'https://schema.org/stageAsNumber',
	/** An official rating for a lodging business or food establishment, e.g. from national associations or standards bodies. Use the author property to indicate the rating organization, e.g. as an Organization with name such as (e.g. HOTREC, DEHOGA, WHR, or Hotelstars). */
	'starRating': 'https://schema.org/starRating',
	/** The start date and time of the item (in [ISO 8601 date format](http://en.wikipedia.org/wiki/ISO_8601)). */
	'startDate': 'https://schema.org/startDate',
	/** The start time of the clip expressed as the number of seconds from the beginning of the work. */
	'startOffset': 'https://schema.org/startOffset',
	/** The startTime of something. For a reserved event or service (e.g. FoodEstablishmentReservation), the time that it is expected to start. For actions that span a period of time, when the action was performed. E.g. John wrote a book from *January* to December. For media, including audio and video, it's the time offset of the start of a clip within a larger file.\n\nNote that Event uses startDate/endDate instead of startTime/endTime, even when describing dates with times. This situation may be clarified in future revisions. */
	'startTime': 'https://schema.org/startTime',
	/** Indicates the kind of statistic represented by a [[StatisticalVariable]], e.g. mean, count etc. The value of statType is a property, either from within Schema.org (e.g. [[count]], [[median]], [[marginOfError]], [[maxValue]], [[minValue]]) or from other compatible (e.g. RDF) systems such as DataCommons.org or Wikidata.org.  */
	'statType': 'https://schema.org/statType',
	/** The status of the study (enumerated). */
	'status': 'https://schema.org/status',
	/** The position of the steering wheel or similar device (mostly for cars). */
	'steeringPosition': 'https://schema.org/steeringPosition',
	/** A single step item (as HowToStep, text, document, video, etc.) or a HowToSection. */
	'step': 'https://schema.org/step',
	/** The stepValue attribute indicates the granularity that is expected (and required) of the value in a PropertyValueSpecification. */
	'stepValue': 'https://schema.org/stepValue',
	/** A single step item (as HowToStep, text, document, video, etc.) or a HowToSection (originally misnamed 'steps'; 'step' is preferred). */
	'steps': 'https://schema.org/steps',
	/** Storage requirements (free space required). */
	'storageRequirements': 'https://schema.org/storageRequirements',
	/** The street address. For example, 1600 Amphitheatre Pkwy. */
	'streetAddress': 'https://schema.org/streetAddress',
	/** The units of an active ingredient's strength, e.g. mg. */
	'strengthUnit': 'https://schema.org/strengthUnit',
	/** The value of an active ingredient's strength, e.g. 325. */
	'strengthValue': 'https://schema.org/strengthValue',
	/** The name given to how bone physically connects to each other. */
	'structuralClass': 'https://schema.org/structuralClass',
	/** A medical study or trial related to this entity. */
	'study': 'https://schema.org/study',
	/** Specifics about the observational study design (enumerated). */
	'studyDesign': 'https://schema.org/studyDesign',
	/** The location in which the study is taking/took place. */
	'studyLocation': 'https://schema.org/studyLocation',
	/** A subject of the study, i.e. one of the medical conditions, therapies, devices, drugs, etc. investigated by the study. */
	'studySubject': 'https://schema.org/studySubject',
	/** An Event that is part of this event. For example, a conference event includes many presentations, each of which is a subEvent of the conference. */
	'subEvent': 'https://schema.org/subEvent',
	/** Events that are a part of this event. For example, a conference event includes many presentations, each subEvents of the conference. */
	'subEvents': 'https://schema.org/subEvents',
	/** A relationship between two organizations where the first includes the second, e.g., as a subsidiary. See also: the more specific 'department' property. */
	'subOrganization': 'https://schema.org/subOrganization',
	/** The individual reservations included in the package. Typically a repeated property. */
	'subReservation': 'https://schema.org/subReservation',
	/** The substage, e.g. 'a' for Stage IIIa. */
	'subStageSuffix': 'https://schema.org/subStageSuffix',
	/** Component (sub-)structure(s) that comprise this anatomical structure. */
	'subStructure': 'https://schema.org/subStructure',
	/** A component test of the panel. */
	'subTest': 'https://schema.org/subTest',
	/** Identifies a [[Trip]] that is a subTrip of this Trip.  For example Day 1, Day 2, etc. of a multi-day trip. */
	'subTrip': 'https://schema.org/subTrip',
	/** A CreativeWork or Event about this Thing. */
	'subjectOf': 'https://schema.org/subjectOf',
	/** Languages in which subtitles/captions are available, in [IETF BCP 47 standard format](http://tools.ietf.org/html/bcp47). */
	'subtitleLanguage': 'https://schema.org/subtitleLanguage',
	/** A pointer from a newer variant of a product  to its previous, often discontinued predecessor. */
	'successorOf': 'https://schema.org/successorOf',
	/** The number of grams of sugar. */
	'sugarContent': 'https://schema.org/sugarContent',
	/** The age or age range for the intended audience or person, for example 3-12 months for infants, 1-5 years for toddlers. */
	'suggestedAge': 'https://schema.org/suggestedAge',
	/** An answer (possibly one of several, possibly incorrect) to a Question, e.g. on a Question/Answer site. */
	'suggestedAnswer': 'https://schema.org/suggestedAnswer',
	/** The suggested gender of the intended person or audience, for example "male", "female", or "unisex". */
	'suggestedGender': 'https://schema.org/suggestedGender',
	/** Maximum recommended age in years for the audience or user. */
	'suggestedMaxAge': 'https://schema.org/suggestedMaxAge',
	/** A suggested range of body measurements for the intended audience or person, for example inseam between 32 and 34 inches or height between 170 and 190 cm. Typically found on a size chart for wearable products. */
	'suggestedMeasurement': 'https://schema.org/suggestedMeasurement',
	/** Minimum recommended age in years for the audience or user. */
	'suggestedMinAge': 'https://schema.org/suggestedMinAge',
	/** Indicates a dietary restriction or guideline for which this recipe or menu item is suitable, e.g. diabetic, halal etc. */
	'suitableForDiet': 'https://schema.org/suitableForDiet',
	/** An event that this event is a part of. For example, a collection of individual music performances might each have a music festival as their superEvent. */
	'superEvent': 'https://schema.org/superEvent',
	/** Relates a term (i.e. a property, class or enumeration) to one that supersedes it. */
	'supersededBy': 'https://schema.org/supersededBy',
	/** A sub-property of instrument. A supply consumed when performing instructions or a direction. */
	'supply': 'https://schema.org/supply',
	/** The area to which the artery supplies blood. */
	'supplyTo': 'https://schema.org/supplyTo',
	/** Supporting data for a SoftwareApplication. */
	'supportingData': 'https://schema.org/supportingData',
	/** A material used as a surface in some artwork, e.g. Canvas, Paper, Wood, Board, etc. */
	'surface': 'https://schema.org/surface',
	/** Indicates (typically several) Syllabus entities that lay out what each section of the overall course will cover. */
	'syllabusSections': 'https://schema.org/syllabusSections',
	/** Indicates a target EntryPoint, or url, for an Action. */
	'target': 'https://schema.org/target',
	/** A sub property of object. The collection target of the action. */
	'targetCollection': 'https://schema.org/targetCollection',
	/** The description of a node in an established educational framework. */
	'targetDescription': 'https://schema.org/targetDescription',
	/** The name of a node in an established educational framework. */
	'targetName': 'https://schema.org/targetName',
	/** Type of app development: phone, Metro style, desktop, XBox, etc. */
	'targetPlatform': 'https://schema.org/targetPlatform',
	/** Characteristics of the population for which this is intended, or which typically uses it, e.g. 'adults'. */
	'targetPopulation': 'https://schema.org/targetPopulation',
	/** Target Operating System / Product to which the code applies.  If applies to several versions, just the product name can be used. */
	'targetProduct': 'https://schema.org/targetProduct',
	/** The URL of a node in an established educational framework. */
	'targetUrl': 'https://schema.org/targetUrl',
	/** The Tax / Fiscal ID of the organization or person, e.g. the TIN in the US or the CIF/NIF in Spain. */
	'taxID': 'https://schema.org/taxID',
	/** The taxonomic rank of this taxon given preferably as a URI from a controlled vocabulary – typically the ranks from TDWG TaxonRank ontology or equivalent Wikidata URIs. */
	'taxonRank': 'https://schema.org/taxonRank',
	/** The taxonomic grouping of the organism that expresses, encodes, or in some way related to the BioChemEntity. */
	'taxonomicRange': 'https://schema.org/taxonomicRange',
	/** The item being described is intended to help a person learn the competency or learning outcome defined by the referenced term. */
	'teaches': 'https://schema.org/teaches',
	/** The telephone number. */
	'telephone': 'https://schema.org/telephone',
	/** The "temporal" property can be used in cases where more specific properties
(e.g. [[temporalCoverage]], [[dateCreated]], [[dateModified]], [[datePublished]]) are not known to be appropriate. */
	'temporal': 'https://schema.org/temporal',
	/** The temporalCoverage of a CreativeWork indicates the period that the content applies to, i.e. that it describes, either as a DateTime or as a textual string indicating a time period in [ISO 8601 time interval format](https://en.wikipedia.org/wiki/ISO_8601#Time_intervals). In
      the case of a Dataset it will typically indicate the relevant time period in a precise notation (e.g. for a 2011 census dataset, the year 2011 would be written "2011/2012"). Other forms of content, e.g. ScholarlyArticle, Book, TVSeries or TVEpisode, may indicate their temporalCoverage in broader terms - textually or via well-known URL.
      Written works such as books may sometimes have precise temporal coverage too, e.g. a work set in 1939 - 1945 can be indicated in ISO 8601 interval format format via "1939/1945".

Open-ended date ranges can be written with ".." in place of the end date. For example, "2015-11/.." indicates a range beginning in November 2015 and with no specified final date. This is tentative and might be updated in future when ISO 8601 is officially updated. */
	'temporalCoverage': 'https://schema.org/temporalCoverage',
	/** A code that identifies this [[DefinedTerm]] within a [[DefinedTermSet]] */
	'termCode': 'https://schema.org/termCode',
	/** The amount of time in a term as defined by the institution. A term is a length of time where students take one or more classes. Semesters and quarters are common units for term. */
	'termDuration': 'https://schema.org/termDuration',
	/** Human-readable terms of service documentation. */
	'termsOfService': 'https://schema.org/termsOfService',
	/** The number of times terms of study are offered per year. Semesters and quarters are common units for term. For example, if the student can only take 2 semesters for the program in one year, then termsPerYear should be 2. */
	'termsPerYear': 'https://schema.org/termsPerYear',
	/** The textual content of this CreativeWork. */
	'text': 'https://schema.org/text',
	/** Text value being annotated. */
	'textValue': 'https://schema.org/textValue',
	/** Thumbnail image for an image or video. */
	'thumbnail': 'https://schema.org/thumbnail',
	/** A thumbnail image relevant to the Thing. */
	'thumbnailUrl': 'https://schema.org/thumbnailUrl',
	/** The exchange traded instrument associated with a Corporation object. The tickerSymbol is expressed as an exchange and an instrument name separated by a space character. For the exchange component of the tickerSymbol attribute, we recommend using the controlled vocabulary of Market Identifier Codes (MIC) specified in ISO 15022. */
	'tickerSymbol': 'https://schema.org/tickerSymbol',
	/** The unique identifier for the ticket. */
	'ticketNumber': 'https://schema.org/ticketNumber',
	/** Reference to an asset (e.g., Barcode, QR code image or PDF) usable for entrance. */
	'ticketToken': 'https://schema.org/ticketToken',
	/** The seat associated with the ticket. */
	'ticketedSeat': 'https://schema.org/ticketedSeat',
	/** The time of day the program normally runs. For example, "evenings". */
	'timeOfDay': 'https://schema.org/timeOfDay',
	/** Approximate or typical time it usually takes to work with or through the content of this work for the typical or target audience. */
	'timeRequired': 'https://schema.org/timeRequired',
	/** The expected length of time to complete the program if attending full-time. */
	'timeToComplete': 'https://schema.org/timeToComplete',
	/** The type of tissue sample required for the test. */
	'tissueSample': 'https://schema.org/tissueSample',
	/** The title of the job. */
	'title': 'https://schema.org/title',
	/** An [EIDR](https://eidr.org/) (Entertainment Identifier Registry) [[identifier]] representing at the most general/abstract level, a work of film or television.

For example, the motion picture known as "Ghostbusters" has a titleEIDR of  "10.5240/7EC7-228A-510A-053E-CBB8-J". This title (or work) may have several variants, which EIDR calls "edits". See [[editEIDR]].

Since schema.org types like [[Movie]], [[TVEpisode]], [[TVSeason]], and [[TVSeries]] can be used for both works and their multiple expressions, it is possible to use [[titleEIDR]] alone (for a general description), or alongside [[editEIDR]] for a more edit-specific description.
 */
	'titleEIDR': 'https://schema.org/titleEIDR',
	/** A sub property of location. The final location of the object or the agent after the action. */
	'toLocation': 'https://schema.org/toLocation',
	/** A sub property of recipient. The recipient who was directly sent the message. */
	'toRecipient': 'https://schema.org/toRecipient',
	/** A [[HyperTocEntry]] can have a [[tocContinuation]] indicated, which is another [[HyperTocEntry]] that would be the default next item to play or render. */
	'tocContinuation': 'https://schema.org/tocContinuation',
	/** Indicates a [[HyperTocEntry]] in a [[HyperToc]]. */
	'tocEntry': 'https://schema.org/tocEntry',
	/** The permitted vertical load (TWR) of a trailer attached to the vehicle. Also referred to as Tongue Load Rating (TLR) or Vertical Load Rating (VLR).\n\nTypical unit code(s): KGM for kilogram, LBR for pound\n\n* Note 1: You can indicate additional information in the [[name]] of the [[QuantitativeValue]] node.\n* Note 2: You may also link to a [[QualitativeValue]] node that provides additional information using [[valueReference]].\n* Note 3: Note that you can use [[minValue]] and [[maxValue]] to indicate ranges. */
	'tongueWeight': 'https://schema.org/tongueWeight',
	/** A sub property of instrument. An object used (but not consumed) when performing instructions or a direction. */
	'tool': 'https://schema.org/tool',
	/** The torque (turning force) of the vehicle's engine.\n\nTypical unit code(s): NU for newton metre (N m), F17 for pound-force per foot, or F48 for pound-force per inch\n\n* Note 1: You can link to information about how the given value has been determined (e.g. reference RPM) using the [[valueReference]] property.\n* Note 2: You can use [[minValue]] and [[maxValue]] to indicate ranges. */
	'torque': 'https://schema.org/torque',
	/** The total number of students that have enrolled in the history of the course. */
	'totalHistoricalEnrollment': 'https://schema.org/totalHistoricalEnrollment',
	/** The number of positions open for this job posting. Use a positive integer. Do not use if the number of positions is unclear or not known. */
	'totalJobOpenings': 'https://schema.org/totalJobOpenings',
	/** The total amount due. */
	'totalPaymentDue': 'https://schema.org/totalPaymentDue',
	/** The total price for the reservation or ticket, including applicable taxes, shipping, etc.\n\nUsage guidelines:\n\n* Use values from 0123456789 (Unicode 'DIGIT ZERO' (U+0030) to 'DIGIT NINE' (U+0039)) rather than superficially similar Unicode symbols.\n* Use '.' (Unicode 'FULL STOP' (U+002E)) rather than ',' to indicate a decimal point. Avoid using these symbols as a readability separator. */
	'totalPrice': 'https://schema.org/totalPrice',
	/** The total time required to perform instructions or a direction (including time to prepare the supplies), in [ISO 8601 duration format](http://en.wikipedia.org/wiki/ISO_8601). */
	'totalTime': 'https://schema.org/totalTime',
	/** A page providing information on how to book a tour of some [[Place]], such as an [[Accommodation]] or [[ApartmentComplex]] in a real estate setting, as well as other kinds of tours as appropriate. */
	'tourBookingPage': 'https://schema.org/tourBookingPage',
	/** Attraction suitable for type(s) of tourist. E.g. children, visitors from a particular country, etc.  */
	'touristType': 'https://schema.org/touristType',
	/** A music recording (track)&#x2014;usually a single song. If an ItemList is given, the list should contain items of type MusicRecording. */
	'track': 'https://schema.org/track',
	/** Shipper tracking number. */
	'trackingNumber': 'https://schema.org/trackingNumber',
	/** Tracking url for the parcel delivery. */
	'trackingUrl': 'https://schema.org/trackingUrl',
	/** A music recording (track)&#x2014;usually a single song. */
	'tracks': 'https://schema.org/tracks',
	/** The trailer of a movie or TV/radio series, season, episode, etc. */
	'trailer': 'https://schema.org/trailer',
	/** The permitted weight of a trailer attached to the vehicle.\n\nTypical unit code(s): KGM for kilogram, LBR for pound\n* Note 1: You can indicate additional information in the [[name]] of the [[QuantitativeValue]] node.\n* Note 2: You may also link to a [[QualitativeValue]] node that provides additional information using [[valueReference]].\n* Note 3: Note that you can use [[minValue]] and [[maxValue]] to indicate ranges. */
	'trailerWeight': 'https://schema.org/trailerWeight',
	/** The name of the train (e.g. The Orient Express). */
	'trainName': 'https://schema.org/trainName',
	/** The unique identifier for the train. */
	'trainNumber': 'https://schema.org/trainNumber',
	/** The estimated salary earned while in the program. */
	'trainingSalary': 'https://schema.org/trainingSalary',
	/** The number of grams of trans fat. */
	'transFatContent': 'https://schema.org/transFatContent',
	/** If this MediaObject is an AudioObject or VideoObject, the transcript of that object. */
	'transcript': 'https://schema.org/transcript',
	/** The typical delay the order has been sent for delivery and the goods reach the final customer. Typical properties: minValue, maxValue, unitCode (d for DAY). */
	'transitTime': 'https://schema.org/transitTime',
	/** Label to match an [[OfferShippingDetails]] with a [[DeliveryTimeSettings]] (within the context of a [[shippingSettingsLink]] cross-reference). */
	'transitTimeLabel': 'https://schema.org/transitTimeLabel',
	/** The work that this work has been translated from. E.g. 物种起源 is a translationOf “On the Origin of Species”. */
	'translationOfWork': 'https://schema.org/translationOfWork',
	/** Organization or person who adapts a creative work to different languages, regional differences and technical requirements of a target market, or that translates during some event. */
	'translator': 'https://schema.org/translator',
	/** How the disease spreads, either as a route or vector, for example 'direct contact', 'Aedes aegypti', etc. */
	'transmissionMethod': 'https://schema.org/transmissionMethod',
	/** Information about travel bans, e.g. in the context of a pandemic. */
	'travelBans': 'https://schema.org/travelBans',
	/** Specifics about the trial design (enumerated). */
	'trialDesign': 'https://schema.org/trialDesign',
	/** The anatomical or organ system that the vein flows into; a larger structure that the vein connects to. */
	'tributary': 'https://schema.org/tributary',
	/** The location of origin of the trip, prior to any destination(s). */
	'tripOrigin': 'https://schema.org/tripOrigin',
	/** The type of bed to which the BedDetail refers, i.e. the type of bed available in the quantity indicated by quantity. */
	'typeOfBed': 'https://schema.org/typeOfBed',
	/** The product that this structured value is referring to. */
	'typeOfGood': 'https://schema.org/typeOfGood',
	/** The typical expected age range, e.g. '7-9', '11-'. */
	'typicalAgeRange': 'https://schema.org/typicalAgeRange',
	/** The number of credits or units a full-time student would be expected to take in 1 term however 'term' is defined by the institution. */
	'typicalCreditsPerTerm': 'https://schema.org/typicalCreditsPerTerm',
	/** A medical test typically performed given this condition. */
	'typicalTest': 'https://schema.org/typicalTest',
	/** The person or organization the reservation or ticket is for. */
	'underName': 'https://schema.org/underName',
	/** The unit of measurement given using the UN/CEFACT Common Code (3 characters) or a URL. Other codes than the UN/CEFACT Common Code may be used with a prefix followed by a colon. */
	'unitCode': 'https://schema.org/unitCode',
	/** A string or text indicating the unit of measurement. Useful if you cannot provide a standard unit code for
<a href='unitCode'>unitCode</a>. */
	'unitText': 'https://schema.org/unitText',
	/** For an [[Organization]] (typically a [[NewsMediaOrganization]]), a statement about policy on use of unnamed sources and the decision process required. */
	'unnamedSourcesPolicy': 'https://schema.org/unnamedSourcesPolicy',
	/** The number of grams of unsaturated fat. */
	'unsaturatedFatContent': 'https://schema.org/unsaturatedFatContent',
	/** Date when this media object was uploaded to this site. */
	'uploadDate': 'https://schema.org/uploadDate',
	/** The number of upvotes this question, answer or comment has received from the community. */
	'upvoteCount': 'https://schema.org/upvoteCount',
	/** URL of the item. */
	'url': 'https://schema.org/url',
	/** An url template (RFC6570) that will be used to construct the target of the execution of the action. */
	'urlTemplate': 'https://schema.org/urlTemplate',
	/** The schema.org [[usageInfo]] property indicates further information about a [[CreativeWork]]. This property is applicable both to works that are freely available and to those that require payment or other transactions. It can reference additional information, e.g. community expectations on preferred linking and citation conventions, as well as purchasing details. For something that can be commercially licensed, usageInfo can provide detailed, resource-specific information about licensing options.

This property can be used alongside the license property which indicates license(s) applicable to some piece of content. The usageInfo property can provide information about other licensing options, e.g. acquiring commercial usage rights for an image that is also available under non-commercial creative commons licenses. */
	'usageInfo': 'https://schema.org/usageInfo',
	/** A condition the test is used to diagnose. */
	'usedToDiagnose': 'https://schema.org/usedToDiagnose',
	/** The number of interactions for the CreativeWork using the WebSite or SoftwareApplication. */
	'userInteractionCount': 'https://schema.org/userInteractionCount',
	/** Device used to perform the test. */
	'usesDevice': 'https://schema.org/usesDevice',
	/** The standard for interpreting the Plan ID. The preferred is "HIOS". See the Centers for Medicare & Medicaid Services for more details. */
	'usesHealthPlanIdStandard': 'https://schema.org/usesHealthPlanIdStandard',
	/** Text of an utterances (spoken words, lyrics etc.) that occurs at a certain section of a media object, represented as a [[HyperTocEntry]]. */
	'utterances': 'https://schema.org/utterances',
	/** The duration of validity of a permit or similar thing. */
	'validFor': 'https://schema.org/validFor',
	/** The date when the item becomes valid. */
	'validFrom': 'https://schema.org/validFrom',
	/** The geographic area where a permit or similar thing is valid. */
	'validIn': 'https://schema.org/validIn',
	/** The date after when the item is not valid. For example the end of an offer, salary period, or a period of opening hours. */
	'validThrough': 'https://schema.org/validThrough',
	/** The date when the item is no longer valid. */
	'validUntil': 'https://schema.org/validUntil',
	/** The value of a [[QuantitativeValue]] (including [[Observation]]) or property value node.\n\n* For [[QuantitativeValue]] and [[MonetaryAmount]], the recommended type for values is 'Number'.\n* For [[PropertyValue]], it can be 'Text', 'Number', 'Boolean', or 'StructuredValue'.\n* Use values from 0123456789 (Unicode 'DIGIT ZERO' (U+0030) to 'DIGIT NINE' (U+0039)) rather than superficially similar Unicode symbols.\n* Use '.' (Unicode 'FULL STOP' (U+002E)) rather than ',' to indicate a decimal point. Avoid using these symbols as a readability separator. */
	'value': 'https://schema.org/value',
	/** Specifies whether the applicable value-added tax (VAT) is included in the price specification or not. */
	'valueAddedTaxIncluded': 'https://schema.org/valueAddedTaxIncluded',
	/** Specifies the allowed range for number of characters in a literal value. */
	'valueMaxLength': 'https://schema.org/valueMaxLength',
	/** Specifies the minimum allowed range for number of characters in a literal value. */
	'valueMinLength': 'https://schema.org/valueMinLength',
	/** Indicates the name of the PropertyValueSpecification to be used in URL templates and form encoding in a manner analogous to HTML's input@name. */
	'valueName': 'https://schema.org/valueName',
	/** Specifies a regular expression for testing literal values according to the HTML spec. */
	'valuePattern': 'https://schema.org/valuePattern',
	/** A secondary value that provides additional information on the original value, e.g. a reference temperature or a type of measurement. */
	'valueReference': 'https://schema.org/valueReference',
	/** Whether the property must be filled in to complete the action.  Default is false. */
	'valueRequired': 'https://schema.org/valueRequired',
	/** The variableMeasured property can indicate (repeated as necessary) the  variables that are measured in some dataset, either described as text or as pairs of identifier and description using PropertyValue, or more explicitly as a [[StatisticalVariable]]. */
	'variableMeasured': 'https://schema.org/variableMeasured',
	/** A description of the variant cover
    	for the issue, if the issue is a variant printing. For example, "Bryan Hitch
    	Variant Cover" or "2nd Printing Variant". */
	'variantCover': 'https://schema.org/variantCover',
	/** Indicates the property or properties by which the variants in a [[ProductGroup]] vary, e.g. their size, color etc. Schema.org properties can be referenced by their short name e.g. "color"; terms defined elsewhere can be referenced with their URIs. */
	'variesBy': 'https://schema.org/variesBy',
	/** The Value-added Tax ID of the organization or person. */
	'vatID': 'https://schema.org/vatID',
	/** A short text indicating the configuration of the vehicle, e.g. '5dr hatchback ST 2.5 MT 225 hp' or 'limited edition'. */
	'vehicleConfiguration': 'https://schema.org/vehicleConfiguration',
	/** Information about the engine or engines of the vehicle. */
	'vehicleEngine': 'https://schema.org/vehicleEngine',
	/** The Vehicle Identification Number (VIN) is a unique serial number used by the automotive industry to identify individual motor vehicles. */
	'vehicleIdentificationNumber': 'https://schema.org/vehicleIdentificationNumber',
	/** The color or color combination of the interior of the vehicle. */
	'vehicleInteriorColor': 'https://schema.org/vehicleInteriorColor',
	/** The type or material of the interior of the vehicle (e.g. synthetic fabric, leather, wood, etc.). While most interior types are characterized by the material used, an interior type can also be based on vehicle usage or target audience. */
	'vehicleInteriorType': 'https://schema.org/vehicleInteriorType',
	/** The release date of a vehicle model (often used to differentiate versions of the same make and model). */
	'vehicleModelDate': 'https://schema.org/vehicleModelDate',
	/** The number of passengers that can be seated in the vehicle, both in terms of the physical space available, and in terms of limitations set by law.\n\nTypical unit code(s): C62 for persons. */
	'vehicleSeatingCapacity': 'https://schema.org/vehicleSeatingCapacity',
	/** Indicates whether the vehicle has been used for special purposes, like commercial rental, driving school, or as a taxi. The legislation in many countries requires this information to be revealed when offering a car for sale. */
	'vehicleSpecialUsage': 'https://schema.org/vehicleSpecialUsage',
	/** The type of component used for transmitting the power from a rotating power source to the wheels or other relevant component(s) ("gearbox" for cars). */
	'vehicleTransmission': 'https://schema.org/vehicleTransmission',
	/** 'vendor' is an earlier term for 'seller'. */
	'vendor': 'https://schema.org/vendor',
	/** Disclosure about verification and fact-checking processes for a [[NewsMediaOrganization]] or other fact-checking [[Organization]]. */
	'verificationFactCheckingPolicy': 'https://schema.org/verificationFactCheckingPolicy',
	/** The version of the CreativeWork embodied by a specified resource. */
	'version': 'https://schema.org/version',
	/** An embedded video object. */
	'video': 'https://schema.org/video',
	/** The type of screening or video broadcast used (e.g. IMAX, 3D, SD, HD, etc.). */
	'videoFormat': 'https://schema.org/videoFormat',
	/** The frame size of the video. */
	'videoFrameSize': 'https://schema.org/videoFrameSize',
	/** The quality of the video. */
	'videoQuality': 'https://schema.org/videoQuality',
	/** Identifies the volume of publication or multi-part work; for example, "iii" or "2". */
	'volumeNumber': 'https://schema.org/volumeNumber',
	/** Any FDA or other warnings about the drug (text or URL). */
	'warning': 'https://schema.org/warning',
	/** The warranty promise(s) included in the offer. */
	'warranty': 'https://schema.org/warranty',
	/** The warranty promise(s) included in the offer. */
	'warrantyPromise': 'https://schema.org/warrantyPromise',
	/** The scope of the warranty promise. */
	'warrantyScope': 'https://schema.org/warrantyScope',
	/** The time when a passenger can check into the flight online. */
	'webCheckinTime': 'https://schema.org/webCheckinTime',
	/** The URL for a feed, e.g. associated with a podcast series, blog, or series of date-stamped updates. This is usually RSS or Atom. */
	'webFeed': 'https://schema.org/webFeed',
	/** The weight of the product or person. */
	'weight': 'https://schema.org/weight',
	/** The permitted total weight of the loaded vehicle, including passengers and cargo and the weight of the empty vehicle.\n\nTypical unit code(s): KGM for kilogram, LBR for pound\n\n* Note 1: You can indicate additional information in the [[name]] of the [[QuantitativeValue]] node.\n* Note 2: You may also link to a [[QualitativeValue]] node that provides additional information using [[valueReference]].\n* Note 3: Note that you can use [[minValue]] and [[maxValue]] to indicate ranges. */
	'weightTotal': 'https://schema.org/weightTotal',
	/** The distance between the centers of the front and rear wheels.\n\nTypical unit code(s): CMT for centimeters, MTR for meters, INH for inches, FOT for foot/feet */
	'wheelbase': 'https://schema.org/wheelbase',
	/** The width of the item. */
	'width': 'https://schema.org/width',
	/** A sub property of participant. The winner of the action. */
	'winner': 'https://schema.org/winner',
	/** The number of words in the text of the Article. */
	'wordCount': 'https://schema.org/wordCount',
	/** Example/instance/realization/derivation of the concept of this creative work. E.g. the paperback edition, first edition, or e-book. */
	'workExample': 'https://schema.org/workExample',
	/** A work featured in some event, e.g. exhibited in an ExhibitionEvent.
       Specific subproperties are available for workPerformed (e.g. a play), or a workPresented (a Movie at a ScreeningEvent). */
	'workFeatured': 'https://schema.org/workFeatured',
	/** The typical working hours for this job (e.g. 1st shift, night shift, 8am-5pm). */
	'workHours': 'https://schema.org/workHours',
	/** A contact location for a person's place of work. */
	'workLocation': 'https://schema.org/workLocation',
	/** A work performed in some event, for example a play performed in a TheaterEvent. */
	'workPerformed': 'https://schema.org/workPerformed',
	/** The movie presented during this event. */
	'workPresented': 'https://schema.org/workPresented',
	/** A work that is a translation of the content of this work. E.g. 西遊記 has an English workTranslation “Journey to the West”, a German workTranslation “Monkeys Pilgerfahrt” and a Vietnamese  translation Tây du ký bình khảo. */
	'workTranslation': 'https://schema.org/workTranslation',
	/** Quantitative measure of the physiologic output of the exercise; also referred to as energy expenditure. */
	'workload': 'https://schema.org/workload',
	/** Organizations that the person works for. */
	'worksFor': 'https://schema.org/worksFor',
	/** The lowest value allowed in this rating system. If worstRating is omitted, 1 is assumed. */
	'worstRating': 'https://schema.org/worstRating',
	/** An XPath, e.g. of a [[SpeakableSpecification]] or [[WebPageElement]]. In the latter case, multiple matches within a page can constitute a single conceptual "Web page element". */
	'xpath': 'https://schema.org/xpath',
	/** The year an [[Accommodation]] was constructed. This corresponds to the [YearBuilt field in RESO](https://ddwiki.reso.org/display/DDW17/YearBuilt+Field).  */
	'yearBuilt': 'https://schema.org/yearBuilt',
	/** The size of the business in annual revenue. */
	'yearlyRevenue': 'https://schema.org/yearlyRevenue',
	/** The age of the business. */
	'yearsInOperation': 'https://schema.org/yearsInOperation',
	/** The quantity that results by performing instructions. For example, a paper airplane, 10 personalized candles. */
	'yield': 'https://schema.org/yield',
}

/** Namespace URI of the schema_nq vocabulary. */
export const _schema_nq = { termType: 'NamedNode', value: 'https://schema.org/', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/' } as NamedNode;

export const schema_nq = {
	/** A 3D model represents some kind of 3D content, which may have [[encoding]]s in one or more [[MediaObject]]s. Many 3D formats are available (e.g. see [Wikipedia](https://en.wikipedia.org/wiki/Category:3D_graphics_file_formats)); specific encoding formats can be represented using the [[encodingFormat]] property applied to the relevant [[MediaObject]]. For the
case of a single file published after Zip compression, the convention of appending '+zip' to the [[encodingFormat]] can be used. Geospatial, AR/VR, artistic/animation, gaming, engineering and scientific content can all be represented using [[3DModel]]. */
	'_3DModel': { termType: 'NamedNode', value: 'https://schema.org/3DModel', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/3DModel' } as NamedNode,
	/** A radio channel that uses AM. */
	'AMRadioChannel': { termType: 'NamedNode', value: 'https://schema.org/AMRadioChannel', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/AMRadioChannel' } as NamedNode,
	/** Reference documentation for application programming interfaces (APIs). */
	'APIReference': { termType: 'NamedNode', value: 'https://schema.org/APIReference', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/APIReference' } as NamedNode,
	/** Abdomen clinical examination. */
	'Abdomen': { termType: 'NamedNode', value: 'https://schema.org/Abdomen', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Abdomen' } as NamedNode,
	/** Web page type: About page. */
	'AboutPage': { termType: 'NamedNode', value: 'https://schema.org/AboutPage', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/AboutPage' } as NamedNode,
	/** The act of committing to/adopting an object.\n\nRelated actions:\n\n* [[RejectAction]]: The antonym of AcceptAction. */
	'AcceptAction': { termType: 'NamedNode', value: 'https://schema.org/AcceptAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/AcceptAction' } as NamedNode,
	/** An accommodation is a place that can accommodate human beings, e.g. a hotel room, a camping pitch, or a meeting room. Many accommodations are for overnight stays, but this is not a mandatory requirement.
For more specific types of accommodations not defined in schema.org, one can use [[additionalType]] with external vocabularies.
<br /><br />
See also the <a href="/docs/hotels.html">dedicated document on the use of schema.org for marking up hotels and other forms of accommodations</a>.
 */
	'Accommodation': { termType: 'NamedNode', value: 'https://schema.org/Accommodation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Accommodation' } as NamedNode,
	/** Accountancy business.\n\nAs a [[LocalBusiness]] it can be described as a [[provider]] of one or more [[Service]]\(s).
       */
	'AccountingService': { termType: 'NamedNode', value: 'https://schema.org/AccountingService', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/AccountingService' } as NamedNode,
	/** The act of accomplishing something via previous efforts. It is an instantaneous action rather than an ongoing process. */
	'AchieveAction': { termType: 'NamedNode', value: 'https://schema.org/AchieveAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/AchieveAction' } as NamedNode,
	/** An action performed by a direct agent and indirect participants upon a direct object. Optionally happens at a location with the help of an inanimate instrument. The execution of the action may produce a result. Specific action sub-type documentation specifies the exact expectation of each argument/role.\n\nSee also [blog post](http://blog.schema.org/2014/04/announcing-schemaorg-actions.html) and [Actions overview document](https://schema.org/docs/actions.html). */
	'Action': { termType: 'NamedNode', value: 'https://schema.org/Action', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Action' } as NamedNode,
	/** A set of requirements that must be fulfilled in order to perform an Action. */
	'ActionAccessSpecification': { termType: 'NamedNode', value: 'https://schema.org/ActionAccessSpecification', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ActionAccessSpecification' } as NamedNode,
	/** The status of an Action. */
	'ActionStatusType': { termType: 'NamedNode', value: 'https://schema.org/ActionStatusType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ActionStatusType' } as NamedNode,
	/** The act of starting or activating a device or application (e.g. starting a timer or turning on a flashlight). */
	'ActivateAction': { termType: 'NamedNode', value: 'https://schema.org/ActivateAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ActivateAction' } as NamedNode,
	/** Represents the activation fee part of the total price for an offered product, for example a cellphone contract. */
	'ActivationFee': { termType: 'NamedNode', value: 'https://schema.org/ActivationFee', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ActivationFee' } as NamedNode,
	/** An in-progress action (e.g., while watching the movie, or driving to a location). */
	'ActiveActionStatus': { termType: 'NamedNode', value: 'https://schema.org/ActiveActionStatus', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ActiveActionStatus' } as NamedNode,
	/** Active, but not recruiting new participants. */
	'ActiveNotRecruiting': { termType: 'NamedNode', value: 'https://schema.org/ActiveNotRecruiting', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ActiveNotRecruiting' } as NamedNode,
	/** The act of editing by adding an object to a collection. */
	'AddAction': { termType: 'NamedNode', value: 'https://schema.org/AddAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/AddAction' } as NamedNode,
	/** A geographical region, typically under the jurisdiction of a particular government. */
	'AdministrativeArea': { termType: 'NamedNode', value: 'https://schema.org/AdministrativeArea', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/AdministrativeArea' } as NamedNode,
	/** An adult entertainment establishment. */
	'AdultEntertainment': { termType: 'NamedNode', value: 'https://schema.org/AdultEntertainment', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/AdultEntertainment' } as NamedNode,
	/** Enumeration of considerations that make a product relevant or potentially restricted for adults only. */
	'AdultOrientedEnumeration': { termType: 'NamedNode', value: 'https://schema.org/AdultOrientedEnumeration', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/AdultOrientedEnumeration' } as NamedNode,
	/** An [[Article]] that an external entity has paid to place or to produce to its specifications. Includes [advertorials](https://en.wikipedia.org/wiki/Advertorial), sponsored content, native advertising and other paid content. */
	'AdvertiserContentArticle': { termType: 'NamedNode', value: 'https://schema.org/AdvertiserContentArticle', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/AdvertiserContentArticle' } as NamedNode,
	/** Physical activity of relatively low intensity that depends primarily on the aerobic energy-generating process; during activity, the aerobic metabolism uses oxygen to adequately meet energy demands during exercise. */
	'AerobicActivity': { termType: 'NamedNode', value: 'https://schema.org/AerobicActivity', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/AerobicActivity' } as NamedNode,
	/** When a single product is associated with multiple offers (for example, the same pair of shoes is offered by different merchants), then AggregateOffer can be used.\n\nNote: AggregateOffers are normally expected to associate multiple offers that all share the same defined [[businessFunction]] value, or default to http://purl.org/goodrelations/v1#Sell if businessFunction is not explicitly defined. */
	'AggregateOffer': { termType: 'NamedNode', value: 'https://schema.org/AggregateOffer', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/AggregateOffer' } as NamedNode,
	/** The average rating based on multiple ratings or reviews. */
	'AggregateRating': { termType: 'NamedNode', value: 'https://schema.org/AggregateRating', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/AggregateRating' } as NamedNode,
	/** The act of expressing a consistency of opinion with the object. An agent agrees to/about an object (a proposition, topic or theme) with participants. */
	'AgreeAction': { termType: 'NamedNode', value: 'https://schema.org/AgreeAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/AgreeAction' } as NamedNode,
	/** An organization that provides flights for passengers. */
	'Airline': { termType: 'NamedNode', value: 'https://schema.org/Airline', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Airline' } as NamedNode,
	/** An airport. */
	'Airport': { termType: 'NamedNode', value: 'https://schema.org/Airport', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Airport' } as NamedNode,
	/** AlbumRelease. */
	'AlbumRelease': { termType: 'NamedNode', value: 'https://schema.org/AlbumRelease', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/AlbumRelease' } as NamedNode,
	/** Item contains alcohol or promotes alcohol consumption. */
	'AlcoholConsideration': { termType: 'NamedNode', value: 'https://schema.org/AlcoholConsideration', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/AlcoholConsideration' } as NamedNode,
	/** An intangible item that describes an alignment between a learning resource and a node in an educational framework.

Should not be used where the nature of the alignment can be described using a simple property, for example to express that a resource [[teaches]] or [[assesses]] a competency. */
	'AlignmentObject': { termType: 'NamedNode', value: 'https://schema.org/AlignmentObject', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/AlignmentObject' } as NamedNode,
	/** All-wheel Drive is a transmission layout where the engine drives all four wheels. */
	'AllWheelDriveConfiguration': { termType: 'NamedNode', value: 'https://schema.org/AllWheelDriveConfiguration', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/AllWheelDriveConfiguration' } as NamedNode,
	/** Content about the allergy-related aspects of a health topic. */
	'AllergiesHealthAspect': { termType: 'NamedNode', value: 'https://schema.org/AllergiesHealthAspect', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/AllergiesHealthAspect' } as NamedNode,
	/** The act of organizing tasks/objects/events by associating resources to it. */
	'AllocateAction': { termType: 'NamedNode', value: 'https://schema.org/AllocateAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/AllocateAction' } as NamedNode,
	/** A creative work with a visual storytelling format intended to be viewed online, particularly on mobile devices. */
	'AmpStory': { termType: 'NamedNode', value: 'https://schema.org/AmpStory', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/AmpStory' } as NamedNode,
	/** An amusement park. */
	'AmusementPark': { termType: 'NamedNode', value: 'https://schema.org/AmusementPark', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/AmusementPark' } as NamedNode,
	/** Physical activity that is of high-intensity which utilizes the anaerobic metabolism of the body. */
	'AnaerobicActivity': { termType: 'NamedNode', value: 'https://schema.org/AnaerobicActivity', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/AnaerobicActivity' } as NamedNode,
	/** An AnalysisNewsArticle is a [[NewsArticle]] that, while based on factual reporting, incorporates the expertise of the author/producer, offering interpretations and conclusions. */
	'AnalysisNewsArticle': { termType: 'NamedNode', value: 'https://schema.org/AnalysisNewsArticle', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/AnalysisNewsArticle' } as NamedNode,
	/** Any part of the human body, typically a component of an anatomical system. Organs, tissues, and cells are all anatomical structures. */
	'AnatomicalStructure': { termType: 'NamedNode', value: 'https://schema.org/AnatomicalStructure', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/AnatomicalStructure' } as NamedNode,
	/** An anatomical system is a group of anatomical structures that work together to perform a certain task. Anatomical systems, such as organ systems, are one organizing principle of anatomy, and can include circulatory, digestive, endocrine, integumentary, immune, lymphatic, muscular, nervous, reproductive, respiratory, skeletal, urinary, vestibular, and other systems. */
	'AnatomicalSystem': { termType: 'NamedNode', value: 'https://schema.org/AnatomicalSystem', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/AnatomicalSystem' } as NamedNode,
	/** Represents the broad notion of Android-based operating systems. */
	'AndroidPlatform': { termType: 'NamedNode', value: 'https://schema.org/AndroidPlatform', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/AndroidPlatform' } as NamedNode,
	/** A specific branch of medical science that pertains to study of anesthetics and their application. */
	'Anesthesia': { termType: 'NamedNode', value: 'https://schema.org/Anesthesia', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Anesthesia' } as NamedNode,
	/** Animal shelter. */
	'AnimalShelter': { termType: 'NamedNode', value: 'https://schema.org/AnimalShelter', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/AnimalShelter' } as NamedNode,
	/** An answer offered to a question; perhaps correct, perhaps opinionated or wrong. */
	'Answer': { termType: 'NamedNode', value: 'https://schema.org/Answer', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Answer' } as NamedNode,
	/** An apartment (in American English) or flat (in British English) is a self-contained housing unit (a type of residential real estate) that occupies only part of a building (source: Wikipedia, the free encyclopedia, see <a href="http://en.wikipedia.org/wiki/Apartment">http://en.wikipedia.org/wiki/Apartment</a>). */
	'Apartment': { termType: 'NamedNode', value: 'https://schema.org/Apartment', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Apartment' } as NamedNode,
	/** Residence type: Apartment complex. */
	'ApartmentComplex': { termType: 'NamedNode', value: 'https://schema.org/ApartmentComplex', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ApartmentComplex' } as NamedNode,
	/** Appearance assessment with clinical examination. */
	'Appearance': { termType: 'NamedNode', value: 'https://schema.org/Appearance', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Appearance' } as NamedNode,
	/** The act of inserting at the end if an ordered collection. */
	'AppendAction': { termType: 'NamedNode', value: 'https://schema.org/AppendAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/AppendAction' } as NamedNode,
	/** The act of registering to an organization/service without the guarantee to receive it.\n\nRelated actions:\n\n* [[RegisterAction]]: Unlike RegisterAction, ApplyAction has no guarantees that the application will be accepted. */
	'ApplyAction': { termType: 'NamedNode', value: 'https://schema.org/ApplyAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ApplyAction' } as NamedNode,
	/** An indication for a medical therapy that has been formally specified or approved by a regulatory body that regulates use of the therapy; for example, the US FDA approves indications for most drugs in the US. */
	'ApprovedIndication': { termType: 'NamedNode', value: 'https://schema.org/ApprovedIndication', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ApprovedIndication' } as NamedNode,
	/** Aquarium. */
	'Aquarium': { termType: 'NamedNode', value: 'https://schema.org/Aquarium', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Aquarium' } as NamedNode,
	/** An intangible type to be applied to any archive content, carrying with it a set of properties required to describe archival items and collections. */
	'ArchiveComponent': { termType: 'NamedNode', value: 'https://schema.org/ArchiveComponent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ArchiveComponent' } as NamedNode,
	/** An organization with archival holdings. An organization which keeps and preserves archival material and typically makes it accessible to the public. */
	'ArchiveOrganization': { termType: 'NamedNode', value: 'https://schema.org/ArchiveOrganization', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ArchiveOrganization' } as NamedNode,
	/** The act of arriving at a place. An agent arrives at a destination from a fromLocation, optionally with participants. */
	'ArriveAction': { termType: 'NamedNode', value: 'https://schema.org/ArriveAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ArriveAction' } as NamedNode,
	/** An art gallery. */
	'ArtGallery': { termType: 'NamedNode', value: 'https://schema.org/ArtGallery', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ArtGallery' } as NamedNode,
	/** A type of blood vessel that specifically carries blood away from the heart. */
	'Artery': { termType: 'NamedNode', value: 'https://schema.org/Artery', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Artery' } as NamedNode,
	/** An article, such as a news article or piece of investigative report. Newspapers and magazines have articles of many different types and this is intended to cover them all.\n\nSee also [blog post](http://blog.schema.org/2014/09/schemaorg-support-for-bibliographic_2.html). */
	'Article': { termType: 'NamedNode', value: 'https://schema.org/Article', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Article' } as NamedNode,
	/** The act of posing a question / favor to someone.\n\nRelated actions:\n\n* [[ReplyAction]]: Appears generally as a response to AskAction. */
	'AskAction': { termType: 'NamedNode', value: 'https://schema.org/AskAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/AskAction' } as NamedNode,
	/** A [[NewsArticle]] expressing an open call by a [[NewsMediaOrganization]] asking the public for input, insights, clarifications, anecdotes, documentation, etc., on an issue, for reporting purposes. */
	'AskPublicNewsArticle': { termType: 'NamedNode', value: 'https://schema.org/AskPublicNewsArticle', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/AskPublicNewsArticle' } as NamedNode,
	/** The act of forming one's opinion, reaction or sentiment. */
	'AssessAction': { termType: 'NamedNode', value: 'https://schema.org/AssessAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/AssessAction' } as NamedNode,
	/** The act of allocating an action/event/task to some destination (someone or something). */
	'AssignAction': { termType: 'NamedNode', value: 'https://schema.org/AssignAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/AssignAction' } as NamedNode,
	/** A collection or bound volume of maps, charts, plates or tables, physical or in media form illustrating any subject. */
	'Atlas': { termType: 'NamedNode', value: 'https://schema.org/Atlas', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Atlas' } as NamedNode,
	/** Professional service: Attorney. \n\nThis type is deprecated - [[LegalService]] is more inclusive and less ambiguous. */
	'Attorney': { termType: 'NamedNode', value: 'https://schema.org/Attorney', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Attorney' } as NamedNode,
	/** Intended audience for an item, i.e. the group for whom the item was created. */
	'Audience': { termType: 'NamedNode', value: 'https://schema.org/Audience', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Audience' } as NamedNode,
	/** An audio file. */
	'AudioObject': { termType: 'NamedNode', value: 'https://schema.org/AudioObject', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/AudioObject' } as NamedNode,
	/** A specific and exact (byte-for-byte) version of an [[AudioObject]]. Two byte-for-byte identical files, for the purposes of this type, considered identical. If they have different embedded metadata the files will differ. Different external facts about the files, e.g. creator or dateCreated that aren't represented in their actual content, do not affect this notion of identity. */
	'AudioObjectSnapshot': { termType: 'NamedNode', value: 'https://schema.org/AudioObjectSnapshot', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/AudioObjectSnapshot' } as NamedNode,
	/** An audiobook. */
	'Audiobook': { termType: 'NamedNode', value: 'https://schema.org/Audiobook', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Audiobook' } as NamedNode,
	/** Book format: Audiobook. This is an enumerated value for use with the bookFormat property. There is also a type 'Audiobook' in the bib extension which includes Audiobook specific properties. */
	'AudiobookFormat': { termType: 'NamedNode', value: 'https://schema.org/AudiobookFormat', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/AudiobookFormat' } as NamedNode,
	/** Indicates that the publisher gives some special status to the publication of the document. ("The Queens Printer" version of a UK Act of Parliament, or the PDF version of a Directive published by the EU Office of Publications.) Something "Authoritative" is considered to be also [[OfficialLegalValue]]. */
	'AuthoritativeLegalValue': { termType: 'NamedNode', value: 'https://schema.org/AuthoritativeLegalValue', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/AuthoritativeLegalValue' } as NamedNode,
	/** The act of granting permission to an object. */
	'AuthorizeAction': { termType: 'NamedNode', value: 'https://schema.org/AuthorizeAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/AuthorizeAction' } as NamedNode,
	/** Auto body shop. */
	'AutoBodyShop': { termType: 'NamedNode', value: 'https://schema.org/AutoBodyShop', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/AutoBodyShop' } as NamedNode,
	/** An car dealership. */
	'AutoDealer': { termType: 'NamedNode', value: 'https://schema.org/AutoDealer', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/AutoDealer' } as NamedNode,
	/** An auto parts store. */
	'AutoPartsStore': { termType: 'NamedNode', value: 'https://schema.org/AutoPartsStore', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/AutoPartsStore' } as NamedNode,
	/** A car rental business. */
	'AutoRental': { termType: 'NamedNode', value: 'https://schema.org/AutoRental', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/AutoRental' } as NamedNode,
	/** Car repair business. */
	'AutoRepair': { termType: 'NamedNode', value: 'https://schema.org/AutoRepair', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/AutoRepair' } as NamedNode,
	/** A car wash business. */
	'AutoWash': { termType: 'NamedNode', value: 'https://schema.org/AutoWash', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/AutoWash' } as NamedNode,
	/** ATM/cash machine. */
	'AutomatedTeller': { termType: 'NamedNode', value: 'https://schema.org/AutomatedTeller', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/AutomatedTeller' } as NamedNode,
	/** Car repair, sales, or parts. */
	'AutomotiveBusiness': { termType: 'NamedNode', value: 'https://schema.org/AutomotiveBusiness', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/AutomotiveBusiness' } as NamedNode,
	/** A system of medicine that originated in India over thousands of years and that focuses on integrating and balancing the body, mind, and spirit. */
	'Ayurvedic': { termType: 'NamedNode', value: 'https://schema.org/Ayurvedic', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Ayurvedic' } as NamedNode,
	/** Indicates that the item is available on back order. */
	'BackOrder': { termType: 'NamedNode', value: 'https://schema.org/BackOrder', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BackOrder' } as NamedNode,
	/** A [[NewsArticle]] providing historical context, definition and detail on a specific topic (aka "explainer" or "backgrounder"). For example, an in-depth article or frequently-asked-questions ([FAQ](https://en.wikipedia.org/wiki/FAQ)) document on topics such as Climate Change or the European Union. Other kinds of background material from a non-news setting are often described using [[Book]] or [[Article]], in particular [[ScholarlyArticle]]. See also [[NewsArticle]] for related vocabulary from a learning/education perspective. */
	'BackgroundNewsArticle': { termType: 'NamedNode', value: 'https://schema.org/BackgroundNewsArticle', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BackgroundNewsArticle' } as NamedNode,
	/** Pathogenic bacteria that cause bacterial infection. */
	'Bacteria': { termType: 'NamedNode', value: 'https://schema.org/Bacteria', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Bacteria' } as NamedNode,
	/** A bakery. */
	'Bakery': { termType: 'NamedNode', value: 'https://schema.org/Bakery', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Bakery' } as NamedNode,
	/** Physical activity that is engaged to help maintain posture and balance. */
	'Balance': { termType: 'NamedNode', value: 'https://schema.org/Balance', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Balance' } as NamedNode,
	/** A product or service offered by a bank whereby one may deposit, withdraw or transfer money and in some cases be paid interest. */
	'BankAccount': { termType: 'NamedNode', value: 'https://schema.org/BankAccount', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BankAccount' } as NamedNode,
	/** Bank or credit union. */
	'BankOrCreditUnion': { termType: 'NamedNode', value: 'https://schema.org/BankOrCreditUnion', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BankOrCreditUnion' } as NamedNode,
	/** A bar or pub. */
	'BarOrPub': { termType: 'NamedNode', value: 'https://schema.org/BarOrPub', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BarOrPub' } as NamedNode,
	/** An image of a visual machine-readable code such as a barcode or QR code. */
	'Barcode': { termType: 'NamedNode', value: 'https://schema.org/Barcode', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Barcode' } as NamedNode,
	/** BasicIncome: this is a benefit for basic income. */
	'BasicIncome': { termType: 'NamedNode', value: 'https://schema.org/BasicIncome', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BasicIncome' } as NamedNode,
	/** Beach. */
	'Beach': { termType: 'NamedNode', value: 'https://schema.org/Beach', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Beach' } as NamedNode,
	/** Beauty salon. */
	'BeautySalon': { termType: 'NamedNode', value: 'https://schema.org/BeautySalon', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BeautySalon' } as NamedNode,
	/** Bed and breakfast.
<br /><br />
See also the <a href="/docs/hotels.html">dedicated document on the use of schema.org for marking up hotels and other forms of accommodations</a>.
 */
	'BedAndBreakfast': { termType: 'NamedNode', value: 'https://schema.org/BedAndBreakfast', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BedAndBreakfast' } as NamedNode,
	/** An entity holding detailed information about the available bed types, e.g. the quantity of twin beds for a hotel room. For the single case of just one bed of a certain type, you can use bed directly with a text. See also [[BedType]] (under development). */
	'BedDetails': { termType: 'NamedNode', value: 'https://schema.org/BedDetails', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BedDetails' } as NamedNode,
	/** A type of bed. This is used for indicating the bed or beds available in an accommodation. */
	'BedType': { termType: 'NamedNode', value: 'https://schema.org/BedType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BedType' } as NamedNode,
	/** The act of forming a personal connection with someone (object) mutually/bidirectionally/symmetrically.\n\nRelated actions:\n\n* [[FollowAction]]: Unlike FollowAction, BefriendAction implies that the connection is reciprocal. */
	'BefriendAction': { termType: 'NamedNode', value: 'https://schema.org/BefriendAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BefriendAction' } as NamedNode,
	/** Content about the benefits and advantages of usage or utilization of topic. */
	'BenefitsHealthAspect': { termType: 'NamedNode', value: 'https://schema.org/BenefitsHealthAspect', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BenefitsHealthAspect' } as NamedNode,
	/** A bike store. */
	'BikeStore': { termType: 'NamedNode', value: 'https://schema.org/BikeStore', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BikeStore' } as NamedNode,
	/** Any biological, chemical, or biochemical thing. For example: a protein; a gene; a chemical; a synthetic chemical. */
	'BioChemEntity': { termType: 'NamedNode', value: 'https://schema.org/BioChemEntity', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BioChemEntity' } as NamedNode,
	/** A [blog](https://en.wikipedia.org/wiki/Blog), sometimes known as a "weblog". Note that the individual posts ([[BlogPosting]]s) in a [[Blog]] are often colloquially referred to by the same term. */
	'Blog': { termType: 'NamedNode', value: 'https://schema.org/Blog', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Blog' } as NamedNode,
	/** A blog post. */
	'BlogPosting': { termType: 'NamedNode', value: 'https://schema.org/BlogPosting', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BlogPosting' } as NamedNode,
	/** A medical test performed on a sample of a patient's blood. */
	'BloodTest': { termType: 'NamedNode', value: 'https://schema.org/BloodTest', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BloodTest' } as NamedNode,
	/** A type of boarding policy used by an airline. */
	'BoardingPolicyType': { termType: 'NamedNode', value: 'https://schema.org/BoardingPolicyType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BoardingPolicyType' } as NamedNode,
	/** A reservation for boat travel.

Note: This type is for information about actual reservations, e.g. in confirmation emails or HTML pages with individual confirmations of reservations. For offers of tickets, use [[Offer]]. */
	'BoatReservation': { termType: 'NamedNode', value: 'https://schema.org/BoatReservation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BoatReservation' } as NamedNode,
	/** A terminal for boats, ships, and other water vessels. */
	'BoatTerminal': { termType: 'NamedNode', value: 'https://schema.org/BoatTerminal', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BoatTerminal' } as NamedNode,
	/** A trip on a commercial ferry line. */
	'BoatTrip': { termType: 'NamedNode', value: 'https://schema.org/BoatTrip', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BoatTrip' } as NamedNode,
	/** Arm length (measured between arms/shoulder line intersection and the prominent wrist bone). Used, for example, to fit shirts. */
	'BodyMeasurementArm': { termType: 'NamedNode', value: 'https://schema.org/BodyMeasurementArm', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BodyMeasurementArm' } as NamedNode,
	/** Maximum girth of bust. Used, for example, to fit women's suits. */
	'BodyMeasurementBust': { termType: 'NamedNode', value: 'https://schema.org/BodyMeasurementBust', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BodyMeasurementBust' } as NamedNode,
	/** Maximum girth of chest. Used, for example, to fit men's suits. */
	'BodyMeasurementChest': { termType: 'NamedNode', value: 'https://schema.org/BodyMeasurementChest', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BodyMeasurementChest' } as NamedNode,
	/** Foot length (measured between end of the most prominent toe and the most prominent part of the heel). Used, for example, to measure socks. */
	'BodyMeasurementFoot': { termType: 'NamedNode', value: 'https://schema.org/BodyMeasurementFoot', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BodyMeasurementFoot' } as NamedNode,
	/** Maximum hand girth (measured over the knuckles of the open right hand excluding thumb, fingers together). Used, for example, to fit gloves. */
	'BodyMeasurementHand': { termType: 'NamedNode', value: 'https://schema.org/BodyMeasurementHand', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BodyMeasurementHand' } as NamedNode,
	/** Maximum girth of head above the ears. Used, for example, to fit hats. */
	'BodyMeasurementHead': { termType: 'NamedNode', value: 'https://schema.org/BodyMeasurementHead', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BodyMeasurementHead' } as NamedNode,
	/** Body height (measured between crown of head and soles of feet). Used, for example, to fit jackets. */
	'BodyMeasurementHeight': { termType: 'NamedNode', value: 'https://schema.org/BodyMeasurementHeight', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BodyMeasurementHeight' } as NamedNode,
	/** Girth of hips (measured around the buttocks). Used, for example, to fit skirts. */
	'BodyMeasurementHips': { termType: 'NamedNode', value: 'https://schema.org/BodyMeasurementHips', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BodyMeasurementHips' } as NamedNode,
	/** Inside leg (measured between crotch and soles of feet). Used, for example, to fit pants. */
	'BodyMeasurementInsideLeg': { termType: 'NamedNode', value: 'https://schema.org/BodyMeasurementInsideLeg', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BodyMeasurementInsideLeg' } as NamedNode,
	/** Girth of neck. Used, for example, to fit shirts. */
	'BodyMeasurementNeck': { termType: 'NamedNode', value: 'https://schema.org/BodyMeasurementNeck', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BodyMeasurementNeck' } as NamedNode,
	/** Enumerates types (or dimensions) of a person's body measurements, for example for fitting of clothes. */
	'BodyMeasurementTypeEnumeration': { termType: 'NamedNode', value: 'https://schema.org/BodyMeasurementTypeEnumeration', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BodyMeasurementTypeEnumeration' } as NamedNode,
	/** Girth of body just below the bust. Used, for example, to fit women's swimwear. */
	'BodyMeasurementUnderbust': { termType: 'NamedNode', value: 'https://schema.org/BodyMeasurementUnderbust', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BodyMeasurementUnderbust' } as NamedNode,
	/** Girth of natural waistline (between hip bones and lower ribs). Used, for example, to fit pants. */
	'BodyMeasurementWaist': { termType: 'NamedNode', value: 'https://schema.org/BodyMeasurementWaist', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BodyMeasurementWaist' } as NamedNode,
	/** Body weight. Used, for example, to measure pantyhose. */
	'BodyMeasurementWeight': { termType: 'NamedNode', value: 'https://schema.org/BodyMeasurementWeight', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BodyMeasurementWeight' } as NamedNode,
	/** A body of water, such as a sea, ocean, or lake. */
	'BodyOfWater': { termType: 'NamedNode', value: 'https://schema.org/BodyOfWater', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BodyOfWater' } as NamedNode,
	/** Rigid connective tissue that comprises up the skeletal structure of the human body. */
	'Bone': { termType: 'NamedNode', value: 'https://schema.org/Bone', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Bone' } as NamedNode,
	/** A book. */
	'Book': { termType: 'NamedNode', value: 'https://schema.org/Book', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Book' } as NamedNode,
	/** The publication format of the book. */
	'BookFormatType': { termType: 'NamedNode', value: 'https://schema.org/BookFormatType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BookFormatType' } as NamedNode,
	/** A series of books. Included books can be indicated with the hasPart property. */
	'BookSeries': { termType: 'NamedNode', value: 'https://schema.org/BookSeries', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BookSeries' } as NamedNode,
	/** A bookstore. */
	'BookStore': { termType: 'NamedNode', value: 'https://schema.org/BookStore', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BookStore' } as NamedNode,
	/** An agent bookmarks/flags/labels/tags/marks an object. */
	'BookmarkAction': { termType: 'NamedNode', value: 'https://schema.org/BookmarkAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BookmarkAction' } as NamedNode,
	/** Boolean: True or False. */
	'Boolean': { termType: 'NamedNode', value: 'https://schema.org/Boolean', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Boolean' } as NamedNode,
	/** The act of obtaining an object under an agreement to return it at a later date. Reciprocal of LendAction.\n\nRelated actions:\n\n* [[LendAction]]: Reciprocal of BorrowAction. */
	'BorrowAction': { termType: 'NamedNode', value: 'https://schema.org/BorrowAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BorrowAction' } as NamedNode,
	/** A bowling alley. */
	'BowlingAlley': { termType: 'NamedNode', value: 'https://schema.org/BowlingAlley', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BowlingAlley' } as NamedNode,
	/** Any anatomical structure which pertains to the soft nervous tissue functioning as the coordinating center of sensation and intellectual and nervous activity. */
	'BrainStructure': { termType: 'NamedNode', value: 'https://schema.org/BrainStructure', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BrainStructure' } as NamedNode,
	/** A brand is a name used by an organization or business person for labeling a product, product group, or similar. */
	'Brand': { termType: 'NamedNode', value: 'https://schema.org/Brand', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Brand' } as NamedNode,
	/** A BreadcrumbList is an ItemList consisting of a chain of linked Web pages, typically described using at least their URL and their name, and typically ending with the current page.\n\nThe [[position]] property is used to reconstruct the order of the items in a BreadcrumbList. The convention is that a breadcrumb list has an [[itemListOrder]] of [[ItemListOrderAscending]] (lower values listed first), and that the first items in this list correspond to the "top" or beginning of the breadcrumb trail, e.g. with a site or section homepage. The specific values of 'position' are not assigned meaning for a BreadcrumbList, but they should be integers, e.g. beginning with '1' for the first item in the list.
       */
	'BreadcrumbList': { termType: 'NamedNode', value: 'https://schema.org/BreadcrumbList', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BreadcrumbList' } as NamedNode,
	/** Brewery. */
	'Brewery': { termType: 'NamedNode', value: 'https://schema.org/Brewery', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Brewery' } as NamedNode,
	/** A bridge. */
	'Bridge': { termType: 'NamedNode', value: 'https://schema.org/Bridge', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Bridge' } as NamedNode,
	/** A unique instance of a BroadcastService on a CableOrSatelliteService lineup. */
	'BroadcastChannel': { termType: 'NamedNode', value: 'https://schema.org/BroadcastChannel', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BroadcastChannel' } as NamedNode,
	/** An over the air or online broadcast event. */
	'BroadcastEvent': { termType: 'NamedNode', value: 'https://schema.org/BroadcastEvent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BroadcastEvent' } as NamedNode,
	/** The frequency in MHz and the modulation used for a particular BroadcastService. */
	'BroadcastFrequencySpecification': { termType: 'NamedNode', value: 'https://schema.org/BroadcastFrequencySpecification', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BroadcastFrequencySpecification' } as NamedNode,
	/** BroadcastRelease. */
	'BroadcastRelease': { termType: 'NamedNode', value: 'https://schema.org/BroadcastRelease', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BroadcastRelease' } as NamedNode,
	/** A delivery service through which content is provided via broadcast over the air or online. */
	'BroadcastService': { termType: 'NamedNode', value: 'https://schema.org/BroadcastService', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BroadcastService' } as NamedNode,
	/** An account that allows an investor to deposit funds and place investment orders with a licensed broker or brokerage firm. */
	'BrokerageAccount': { termType: 'NamedNode', value: 'https://schema.org/BrokerageAccount', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BrokerageAccount' } as NamedNode,
	/** A Buddhist temple. */
	'BuddhistTemple': { termType: 'NamedNode', value: 'https://schema.org/BuddhistTemple', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BuddhistTemple' } as NamedNode,
	/** A bus (also omnibus or autobus) is a road vehicle designed to carry passengers. Coaches are luxury busses, usually in service for long distance travel. */
	'BusOrCoach': { termType: 'NamedNode', value: 'https://schema.org/BusOrCoach', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BusOrCoach' } as NamedNode,
	/** A reservation for bus travel. \n\nNote: This type is for information about actual reservations, e.g. in confirmation emails or HTML pages with individual confirmations of reservations. For offers of tickets, use [[Offer]]. */
	'BusReservation': { termType: 'NamedNode', value: 'https://schema.org/BusReservation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BusReservation' } as NamedNode,
	/** A bus station. */
	'BusStation': { termType: 'NamedNode', value: 'https://schema.org/BusStation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BusStation' } as NamedNode,
	/** A bus stop. */
	'BusStop': { termType: 'NamedNode', value: 'https://schema.org/BusStop', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BusStop' } as NamedNode,
	/** A trip on a commercial bus line. */
	'BusTrip': { termType: 'NamedNode', value: 'https://schema.org/BusTrip', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BusTrip' } as NamedNode,
	/** A set of characteristics belonging to businesses, e.g. who compose an item's target audience. */
	'BusinessAudience': { termType: 'NamedNode', value: 'https://schema.org/BusinessAudience', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BusinessAudience' } as NamedNode,
	/** A business entity type is a conceptual entity representing the legal form, the size, the main line of business, the position in the value chain, or any combination thereof, of an organization or business person.\n\nCommonly used values:\n\n* http://purl.org/goodrelations/v1#Business\n* http://purl.org/goodrelations/v1#Enduser\n* http://purl.org/goodrelations/v1#PublicInstitution\n* http://purl.org/goodrelations/v1#Reseller
	   */
	'BusinessEntityType': { termType: 'NamedNode', value: 'https://schema.org/BusinessEntityType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BusinessEntityType' } as NamedNode,
	/** Event type: Business event. */
	'BusinessEvent': { termType: 'NamedNode', value: 'https://schema.org/BusinessEvent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BusinessEvent' } as NamedNode,
	/** The business function specifies the type of activity or access (i.e., the bundle of rights) offered by the organization or business person through the offer. Typical are sell, rental or lease, maintenance or repair, manufacture / produce, recycle / dispose, engineering / construction, or installation. Proprietary specifications of access rights are also instances of this class.\n\nCommonly used values:\n\n* http://purl.org/goodrelations/v1#ConstructionInstallation\n* http://purl.org/goodrelations/v1#Dispose\n* http://purl.org/goodrelations/v1#LeaseOut\n* http://purl.org/goodrelations/v1#Maintain\n* http://purl.org/goodrelations/v1#ProvideService\n* http://purl.org/goodrelations/v1#Repair\n* http://purl.org/goodrelations/v1#Sell\n* http://purl.org/goodrelations/v1#Buy
         */
	'BusinessFunction': { termType: 'NamedNode', value: 'https://schema.org/BusinessFunction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BusinessFunction' } as NamedNode,
	/** BusinessSupport: this is a benefit for supporting businesses. */
	'BusinessSupport': { termType: 'NamedNode', value: 'https://schema.org/BusinessSupport', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BusinessSupport' } as NamedNode,
	/** The act of giving money to a seller in exchange for goods or services rendered. An agent buys an object, product, or service from a seller for a price. Reciprocal of SellAction. */
	'BuyAction': { termType: 'NamedNode', value: 'https://schema.org/BuyAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/BuyAction' } as NamedNode,
	/** A CDCPMDRecord is a data structure representing a record in a CDC tabular data format
      used for hospital data reporting. See [documentation](/docs/cdc-covid.html) for details, and the linked CDC materials for authoritative
      definitions used as the source here.
       */
	'CDCPMDRecord': { termType: 'NamedNode', value: 'https://schema.org/CDCPMDRecord', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/CDCPMDRecord' } as NamedNode,
	/** CDFormat. */
	'CDFormat': { termType: 'NamedNode', value: 'https://schema.org/CDFormat', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/CDFormat' } as NamedNode,
	/** X-ray computed tomography imaging. */
	'CT': { termType: 'NamedNode', value: 'https://schema.org/CT', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/CT' } as NamedNode,
	/** A service which provides access to media programming like TV or radio. Access may be via cable or satellite. */
	'CableOrSatelliteService': { termType: 'NamedNode', value: 'https://schema.org/CableOrSatelliteService', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/CableOrSatelliteService' } as NamedNode,
	/** A cafe or coffee shop. */
	'CafeOrCoffeeShop': { termType: 'NamedNode', value: 'https://schema.org/CafeOrCoffeeShop', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/CafeOrCoffeeShop' } as NamedNode,
	/** A camping site, campsite, or [[Campground]] is a place used for overnight stay in the outdoors, typically containing individual [[CampingPitch]] locations. \n\n
In British English a campsite is an area, usually divided into a number of pitches, where people can camp overnight using tents or camper vans or caravans; this British English use of the word is synonymous with the American English expression campground. In American English the term campsite generally means an area where an individual, family, group, or military unit can pitch a tent or park a camper; a campground may contain many campsites (source: Wikipedia, see [https://en.wikipedia.org/wiki/Campsite](https://en.wikipedia.org/wiki/Campsite)).\n\n

See also the dedicated [document on the use of schema.org for marking up hotels and other forms of accommodations](/docs/hotels.html).
 */
	'Campground': { termType: 'NamedNode', value: 'https://schema.org/Campground', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Campground' } as NamedNode,
	/** A [[CampingPitch]] is an individual place for overnight stay in the outdoors, typically being part of a larger camping site, or [[Campground]].\n\n
In British English a campsite, or campground, is an area, usually divided into a number of pitches, where people can camp overnight using tents or camper vans or caravans; this British English use of the word is synonymous with the American English expression campground. In American English the term campsite generally means an area where an individual, family, group, or military unit can pitch a tent or park a camper; a campground may contain many campsites.
(Source: Wikipedia, see [https://en.wikipedia.org/wiki/Campsite](https://en.wikipedia.org/wiki/Campsite).)\n\n
See also the dedicated [document on the use of schema.org for marking up hotels and other forms of accommodations](/docs/hotels.html).
 */
	'CampingPitch': { termType: 'NamedNode', value: 'https://schema.org/CampingPitch', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/CampingPitch' } as NamedNode,
	/** A canal, like the Panama Canal. */
	'Canal': { termType: 'NamedNode', value: 'https://schema.org/Canal', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Canal' } as NamedNode,
	/** The act of asserting that a future event/action is no longer going to happen.\n\nRelated actions:\n\n* [[ConfirmAction]]: The antonym of CancelAction. */
	'CancelAction': { termType: 'NamedNode', value: 'https://schema.org/CancelAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/CancelAction' } as NamedNode,
	/** A car is a wheeled, self-powered motor vehicle used for transportation. */
	'Car': { termType: 'NamedNode', value: 'https://schema.org/Car', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Car' } as NamedNode,
	/** A value indicating a special usage of a car, e.g. commercial rental, driving school, or as a taxi. */
	'CarUsageType': { termType: 'NamedNode', value: 'https://schema.org/CarUsageType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/CarUsageType' } as NamedNode,
	/** A specific branch of medical science that pertains to diagnosis and treatment of disorders of heart and vasculature. */
	'Cardiovascular': { termType: 'NamedNode', value: 'https://schema.org/Cardiovascular', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Cardiovascular' } as NamedNode,
	/** Cardiovascular system assessment with clinical examination. */
	'CardiovascularExam': { termType: 'NamedNode', value: 'https://schema.org/CardiovascularExam', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/CardiovascularExam' } as NamedNode,
	/** A case series (also known as a clinical series) is a medical research study that tracks patients with a known exposure given similar treatment or examines their medical records for exposure and outcome. A case series can be retrospective or prospective and usually involves a smaller number of patients than the more powerful case-control studies or randomized controlled trials. Case series may be consecutive or non-consecutive, depending on whether all cases presenting to the reporting authors over a period of time were included, or only a selection. */
	'CaseSeries': { termType: 'NamedNode', value: 'https://schema.org/CaseSeries', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/CaseSeries' } as NamedNode,
	/** A casino. */
	'Casino': { termType: 'NamedNode', value: 'https://schema.org/Casino', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Casino' } as NamedNode,
	/** CassetteFormat. */
	'CassetteFormat': { termType: 'NamedNode', value: 'https://schema.org/CassetteFormat', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/CassetteFormat' } as NamedNode,
	/** A Category Code. */
	'CategoryCode': { termType: 'NamedNode', value: 'https://schema.org/CategoryCode', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/CategoryCode' } as NamedNode,
	/** A set of Category Code values. */
	'CategoryCodeSet': { termType: 'NamedNode', value: 'https://schema.org/CategoryCodeSet', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/CategoryCodeSet' } as NamedNode,
	/** A Catholic church. */
	'CatholicChurch': { termType: 'NamedNode', value: 'https://schema.org/CatholicChurch', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/CatholicChurch' } as NamedNode,
	/** Information about the causes and main actions that gave rise to the topic. */
	'CausesHealthAspect': { termType: 'NamedNode', value: 'https://schema.org/CausesHealthAspect', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/CausesHealthAspect' } as NamedNode,
	/** A graveyard. */
	'Cemetery': { termType: 'NamedNode', value: 'https://schema.org/Cemetery', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Cemetery' } as NamedNode,
	/** One of the sections into which a book is divided. A chapter usually has a section number or a name. */
	'Chapter': { termType: 'NamedNode', value: 'https://schema.org/Chapter', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Chapter' } as NamedNode,
	/** CharitableIncorporatedOrganization: Non-profit type referring to a Charitable Incorporated Organization (UK). */
	'CharitableIncorporatedOrganization': { termType: 'NamedNode', value: 'https://schema.org/CharitableIncorporatedOrganization', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/CharitableIncorporatedOrganization' } as NamedNode,
	/** An agent inspects, determines, investigates, inquires, or examines an object's accuracy, quality, condition, or state. */
	'CheckAction': { termType: 'NamedNode', value: 'https://schema.org/CheckAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/CheckAction' } as NamedNode,
	/** The act of an agent communicating (service provider, social media, etc) their arrival by registering/confirming for a previously reserved service (e.g. flight check-in) or at a place (e.g. hotel), possibly resulting in a result (boarding pass, etc).\n\nRelated actions:\n\n* [[CheckOutAction]]: The antonym of CheckInAction.\n* [[ArriveAction]]: Unlike ArriveAction, CheckInAction implies that the agent is informing/confirming the start of a previously reserved service.\n* [[ConfirmAction]]: Unlike ConfirmAction, CheckInAction implies that the agent is informing/confirming the *start* of a previously reserved service rather than its validity/existence. */
	'CheckInAction': { termType: 'NamedNode', value: 'https://schema.org/CheckInAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/CheckInAction' } as NamedNode,
	/** The act of an agent communicating (service provider, social media, etc) their departure of a previously reserved service (e.g. flight check-in) or place (e.g. hotel).\n\nRelated actions:\n\n* [[CheckInAction]]: The antonym of CheckOutAction.\n* [[DepartAction]]: Unlike DepartAction, CheckOutAction implies that the agent is informing/confirming the end of a previously reserved service.\n* [[CancelAction]]: Unlike CancelAction, CheckOutAction implies that the agent is informing/confirming the end of a previously reserved service. */
	'CheckOutAction': { termType: 'NamedNode', value: 'https://schema.org/CheckOutAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/CheckOutAction' } as NamedNode,
	/** Web page type: Checkout page. */
	'CheckoutPage': { termType: 'NamedNode', value: 'https://schema.org/CheckoutPage', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/CheckoutPage' } as NamedNode,
	/** A chemical substance is 'a portion of matter of constant composition, composed of molecular entities of the same type or of different types' (source: [ChEBI:59999](https://www.ebi.ac.uk/chebi/searchId.do?chebiId=59999)). */
	'ChemicalSubstance': { termType: 'NamedNode', value: 'https://schema.org/ChemicalSubstance', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ChemicalSubstance' } as NamedNode,
	/** A Childcare center. */
	'ChildCare': { termType: 'NamedNode', value: 'https://schema.org/ChildCare', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ChildCare' } as NamedNode,
	/** Event type: Children's event. */
	'ChildrensEvent': { termType: 'NamedNode', value: 'https://schema.org/ChildrensEvent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ChildrensEvent' } as NamedNode,
	/** A system of medicine focused on the relationship between the body's structure, mainly the spine, and its functioning. */
	'Chiropractic': { termType: 'NamedNode', value: 'https://schema.org/Chiropractic', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Chiropractic' } as NamedNode,
	/** The act of expressing a preference from a set of options or a large or unbounded set of choices/options. */
	'ChooseAction': { termType: 'NamedNode', value: 'https://schema.org/ChooseAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ChooseAction' } as NamedNode,
	/** A church. */
	'Church': { termType: 'NamedNode', value: 'https://schema.org/Church', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Church' } as NamedNode,
	/** A city or town. */
	'City': { termType: 'NamedNode', value: 'https://schema.org/City', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/City' } as NamedNode,
	/** A city hall. */
	'CityHall': { termType: 'NamedNode', value: 'https://schema.org/CityHall', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/CityHall' } as NamedNode,
	/** A public structure, such as a town hall or concert hall. */
	'CivicStructure': { termType: 'NamedNode', value: 'https://schema.org/CivicStructure', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/CivicStructure' } as NamedNode,
	/** A [[Claim]] in Schema.org represents a specific, factually-oriented claim that could be the [[itemReviewed]] in a [[ClaimReview]]. The content of a claim can be summarized with the [[text]] property. Variations on well known claims can have their common identity indicated via [[sameAs]] links, and summarized with a [[name]]. Ideally, a [[Claim]] description includes enough contextual information to minimize the risk of ambiguity or inclarity. In practice, many claims are better understood in the context in which they appear or the interpretations provided by claim reviews.

  Beyond [[ClaimReview]], the Claim type can be associated with related creative works - for example a [[ScholarlyArticle]] or [[Question]] might be [[about]] some [[Claim]].

  At this time, Schema.org does not define any types of relationship between claims. This is a natural area for future exploration.
   */
	'Claim': { termType: 'NamedNode', value: 'https://schema.org/Claim', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Claim' } as NamedNode,
	/** A fact-checking review of claims made (or reported) in some creative work (referenced via itemReviewed). */
	'ClaimReview': { termType: 'NamedNode', value: 'https://schema.org/ClaimReview', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ClaimReview' } as NamedNode,
	/** A class, also often called a 'Type'; equivalent to rdfs:Class. */
	'Class': { termType: 'NamedNode', value: 'https://schema.org/Class', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Class' } as NamedNode,
	/** Represents the cleaning fee part of the total price for an offered product, for example a vacation rental. */
	'CleaningFee': { termType: 'NamedNode', value: 'https://schema.org/CleaningFee', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/CleaningFee' } as NamedNode,
	/** Medical clinicians, including practicing physicians and other medical professionals involved in clinical practice. */
	'Clinician': { termType: 'NamedNode', value: 'https://schema.org/Clinician', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Clinician' } as NamedNode,
	/** A short TV or radio program or a segment/part of a program. */
	'Clip': { termType: 'NamedNode', value: 'https://schema.org/Clip', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Clip' } as NamedNode,
	/** A clothing store. */
	'ClothingStore': { termType: 'NamedNode', value: 'https://schema.org/ClothingStore', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ClothingStore' } as NamedNode,
	/** Play mode: CoOp. Co-operative games, where you play on the same team with friends. */
	'CoOp': { termType: 'NamedNode', value: 'https://schema.org/CoOp', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/CoOp' } as NamedNode,
	/** Computer programming source code. Example: Full (compile ready) solutions, code snippet samples, scripts, templates. */
	'Code': { termType: 'NamedNode', value: 'https://schema.org/Code', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Code' } as NamedNode,
	/** Also known as a panel study. A cohort study is a form of longitudinal study used in medicine and social science. It is one type of study design and should be compared with a cross-sectional study.  A cohort is a group of people who share a common characteristic or experience within a defined period (e.g., are born, leave school, lose their job, are exposed to a drug or a vaccine, etc.). The comparison group may be the general population from which the cohort is drawn, or it may be another cohort of persons thought to have had little or no exposure to the substance under investigation, but otherwise similar. Alternatively, subgroups within the cohort may be compared with each other. */
	'CohortStudy': { termType: 'NamedNode', value: 'https://schema.org/CohortStudy', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/CohortStudy' } as NamedNode,
	/** A collection of items, e.g. creative works or products. */
	'Collection': { termType: 'NamedNode', value: 'https://schema.org/Collection', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Collection' } as NamedNode,
	/** Web page type: Collection page. */
	'CollectionPage': { termType: 'NamedNode', value: 'https://schema.org/CollectionPage', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/CollectionPage' } as NamedNode,
	/** A college, university, or other third-level educational institution. */
	'CollegeOrUniversity': { termType: 'NamedNode', value: 'https://schema.org/CollegeOrUniversity', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/CollegeOrUniversity' } as NamedNode,
	/** A comedy club. */
	'ComedyClub': { termType: 'NamedNode', value: 'https://schema.org/ComedyClub', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ComedyClub' } as NamedNode,
	/** Event type: Comedy event. */
	'ComedyEvent': { termType: 'NamedNode', value: 'https://schema.org/ComedyEvent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ComedyEvent' } as NamedNode,
	/** The artwork on the cover of a comic. */
	'ComicCoverArt': { termType: 'NamedNode', value: 'https://schema.org/ComicCoverArt', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ComicCoverArt' } as NamedNode,
	/** Individual comic issues are serially published as
    	part of a larger series. For the sake of consistency, even one-shot issues
    	belong to a series comprised of a single issue. All comic issues can be
    	uniquely identified by: the combination of the name and volume number of the
    	series to which the issue belongs; the issue number; and the variant
    	description of the issue (if any). */
	'ComicIssue': { termType: 'NamedNode', value: 'https://schema.org/ComicIssue', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ComicIssue' } as NamedNode,
	/** A sequential publication of comic stories under a
    	unifying title, for example "The Amazing Spider-Man" or "Groo the
    	Wanderer". */
	'ComicSeries': { termType: 'NamedNode', value: 'https://schema.org/ComicSeries', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ComicSeries' } as NamedNode,
	/** The term "story" is any indivisible, re-printable
    	unit of a comic, including the interior stories, covers, and backmatter. Most
    	comics have at least two stories: a cover (ComicCoverArt) and an interior story. */
	'ComicStory': { termType: 'NamedNode', value: 'https://schema.org/ComicStory', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ComicStory' } as NamedNode,
	/** A comment on an item - for example, a comment on a blog post. The comment's content is expressed via the [[text]] property, and its topic via [[about]], properties shared with all CreativeWorks. */
	'Comment': { termType: 'NamedNode', value: 'https://schema.org/Comment', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Comment' } as NamedNode,
	/** The act of generating a comment about a subject. */
	'CommentAction': { termType: 'NamedNode', value: 'https://schema.org/CommentAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/CommentAction' } as NamedNode,
	/** Permission to add comments to the document. */
	'CommentPermission': { termType: 'NamedNode', value: 'https://schema.org/CommentPermission', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/CommentPermission' } as NamedNode,
	/** The act of conveying information to another person via a communication medium (instrument) such as speech, email, or telephone conversation. */
	'CommunicateAction': { termType: 'NamedNode', value: 'https://schema.org/CommunicateAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/CommunicateAction' } as NamedNode,
	/** A field of public health focusing on improving health characteristics of a defined population in relation with their geographical or environment areas. */
	'CommunityHealth': { termType: 'NamedNode', value: 'https://schema.org/CommunityHealth', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/CommunityHealth' } as NamedNode,
	/** CompilationAlbum. */
	'CompilationAlbum': { termType: 'NamedNode', value: 'https://schema.org/CompilationAlbum', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/CompilationAlbum' } as NamedNode,
	/** A [[CompleteDataFeed]] is a [[DataFeed]] whose standard representation includes content for every item currently in the feed.

This is the equivalent of Atom's element as defined in Feed Paging and Archiving [RFC 5005](https://tools.ietf.org/html/rfc5005), for example (and as defined for Atom), when using data from a feed that represents a collection of items that varies over time (e.g. "Top Twenty Records") there is no need to have newer entries mixed in alongside older, obsolete entries. By marking this feed as a CompleteDataFeed, old entries can be safely discarded when the feed is refreshed, since we can assume the feed has provided descriptions for all current items. */
	'CompleteDataFeed': { termType: 'NamedNode', value: 'https://schema.org/CompleteDataFeed', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/CompleteDataFeed' } as NamedNode,
	/** Completed. */
	'Completed': { termType: 'NamedNode', value: 'https://schema.org/Completed', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Completed' } as NamedNode,
	/** An action that has already taken place. */
	'CompletedActionStatus': { termType: 'NamedNode', value: 'https://schema.org/CompletedActionStatus', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/CompletedActionStatus' } as NamedNode,
	/** A compound price specification is one that bundles multiple prices that all apply in combination for different dimensions of consumption. Use the name property of the attached unit price specification for indicating the dimension of a price component (e.g. "electricity" or "final cleaning"). */
	'CompoundPriceSpecification': { termType: 'NamedNode', value: 'https://schema.org/CompoundPriceSpecification', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/CompoundPriceSpecification' } as NamedNode,
	/** This type covers computer programming languages such as Scheme and Lisp, as well as other language-like computer representations. Natural languages are best represented with the [[Language]] type. */
	'ComputerLanguage': { termType: 'NamedNode', value: 'https://schema.org/ComputerLanguage', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ComputerLanguage' } as NamedNode,
	/** A computer store. */
	'ComputerStore': { termType: 'NamedNode', value: 'https://schema.org/ComputerStore', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ComputerStore' } as NamedNode,
	/** The act of notifying someone that a future event/action is going to happen as expected.\n\nRelated actions:\n\n* [[CancelAction]]: The antonym of ConfirmAction. */
	'ConfirmAction': { termType: 'NamedNode', value: 'https://schema.org/ConfirmAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ConfirmAction' } as NamedNode,
	/** A Consortium is a membership [[Organization]] whose members are typically Organizations. */
	'Consortium': { termType: 'NamedNode', value: 'https://schema.org/Consortium', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Consortium' } as NamedNode,
	/** The ConstraintNode type is provided to support usecases in which a node in a structured data graph is described with properties which appear to describe a single entity, but are being used in a situation where they serve a more abstract purpose. A [[ConstraintNode]] can be described using [[constraintProperty]] and [[numConstraints]]. These constraint properties can serve a 
    variety of purposes, and their values may sometimes be understood to indicate sets of possible values rather than single, exact and specific values. */
	'ConstraintNode': { termType: 'NamedNode', value: 'https://schema.org/ConstraintNode', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ConstraintNode' } as NamedNode,
	/** The act of ingesting information/resources/food. */
	'ConsumeAction': { termType: 'NamedNode', value: 'https://schema.org/ConsumeAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ConsumeAction' } as NamedNode,
	/** Web page type: Contact page. */
	'ContactPage': { termType: 'NamedNode', value: 'https://schema.org/ContactPage', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ContactPage' } as NamedNode,
	/** A contact point&#x2014;for example, a Customer Complaints department. */
	'ContactPoint': { termType: 'NamedNode', value: 'https://schema.org/ContactPoint', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ContactPoint' } as NamedNode,
	/** Enumerated options related to a ContactPoint. */
	'ContactPointOption': { termType: 'NamedNode', value: 'https://schema.org/ContactPointOption', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ContactPointOption' } as NamedNode,
	/** Content about contagion mechanisms and contagiousness information over the topic. */
	'ContagiousnessHealthAspect': { termType: 'NamedNode', value: 'https://schema.org/ContagiousnessHealthAspect', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ContagiousnessHealthAspect' } as NamedNode,
	/** One of the continents (for example, Europe or Africa). */
	'Continent': { termType: 'NamedNode', value: 'https://schema.org/Continent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Continent' } as NamedNode,
	/** An agent controls a device or application. */
	'ControlAction': { termType: 'NamedNode', value: 'https://schema.org/ControlAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ControlAction' } as NamedNode,
	/** A convenience store. */
	'ConvenienceStore': { termType: 'NamedNode', value: 'https://schema.org/ConvenienceStore', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ConvenienceStore' } as NamedNode,
	/** One or more messages between organizations or people on a particular topic. Individual messages can be linked to the conversation with isPartOf or hasPart properties. */
	'Conversation': { termType: 'NamedNode', value: 'https://schema.org/Conversation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Conversation' } as NamedNode,
	/** The act of producing/preparing food. */
	'CookAction': { termType: 'NamedNode', value: 'https://schema.org/CookAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/CookAction' } as NamedNode,
	/** Organization: A business corporation. */
	'Corporation': { termType: 'NamedNode', value: 'https://schema.org/Corporation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Corporation' } as NamedNode,
	/** A [[comment]] that corrects [[CreativeWork]]. */
	'CorrectionComment': { termType: 'NamedNode', value: 'https://schema.org/CorrectionComment', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/CorrectionComment' } as NamedNode,
	/** A country. */
	'Country': { termType: 'NamedNode', value: 'https://schema.org/Country', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Country' } as NamedNode,
	/** A description of an educational course which may be offered as distinct instances which take place at different times or take place at different locations, or be offered through different media or modes of study. An educational course is a sequence of one or more educational events and/or creative works which aims to build knowledge, competence or ability of learners. */
	'Course': { termType: 'NamedNode', value: 'https://schema.org/Course', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Course' } as NamedNode,
	/** An instance of a [[Course]] which is distinct from other instances because it is offered at a different time or location or through different media or modes of study or to a specific section of students. */
	'CourseInstance': { termType: 'NamedNode', value: 'https://schema.org/CourseInstance', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/CourseInstance' } as NamedNode,
	/** A courthouse. */
	'Courthouse': { termType: 'NamedNode', value: 'https://schema.org/Courthouse', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Courthouse' } as NamedNode,
	/** The artwork on the outer surface of a CreativeWork. */
	'CoverArt': { termType: 'NamedNode', value: 'https://schema.org/CoverArt', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/CoverArt' } as NamedNode,
	/** A CovidTestingFacility is a [[MedicalClinic]] where testing for the COVID-19 Coronavirus
      disease is available. If the facility is being made available from an established [[Pharmacy]], [[Hotel]], or other
      non-medical organization, multiple types can be listed. This makes it easier to re-use existing schema.org information
      about that place, e.g. contact info, address, opening hours. Note that in an emergency, such information may not always be reliable.
       */
	'CovidTestingFacility': { termType: 'NamedNode', value: 'https://schema.org/CovidTestingFacility', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/CovidTestingFacility' } as NamedNode,
	/** The act of deliberately creating/producing/generating/building a result out of the agent. */
	'CreateAction': { termType: 'NamedNode', value: 'https://schema.org/CreateAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/CreateAction' } as NamedNode,
	/** The most generic kind of creative work, including books, movies, photographs, software programs, etc. */
	'CreativeWork': { termType: 'NamedNode', value: 'https://schema.org/CreativeWork', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/CreativeWork' } as NamedNode,
	/** A media season, e.g. TV, radio, video game etc. */
	'CreativeWorkSeason': { termType: 'NamedNode', value: 'https://schema.org/CreativeWorkSeason', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/CreativeWorkSeason' } as NamedNode,
	/** A CreativeWorkSeries in schema.org is a group of related items, typically but not necessarily of the same kind. CreativeWorkSeries are usually organized into some order, often chronological. Unlike [[ItemList]] which is a general purpose data structure for lists of things, the emphasis with CreativeWorkSeries is on published materials (written e.g. books and periodicals, or media such as TV, radio and games).\n\nSpecific subtypes are available for describing [[TVSeries]], [[RadioSeries]], [[MovieSeries]], [[BookSeries]], [[Periodical]] and [[VideoGameSeries]]. In each case, the [[hasPart]] / [[isPartOf]] properties can be used to relate the CreativeWorkSeries to its parts. The general CreativeWorkSeries type serves largely just to organize these more specific and practical subtypes.\n\nIt is common for properties applicable to an item from the series to be usefully applied to the containing group. Schema.org attempts to anticipate some of these cases, but publishers should be free to apply properties of the series parts to the series as a whole wherever they seem appropriate.
	   */
	'CreativeWorkSeries': { termType: 'NamedNode', value: 'https://schema.org/CreativeWorkSeries', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/CreativeWorkSeries' } as NamedNode,
	/** A card payment method of a particular brand or name.  Used to mark up a particular payment method and/or the financial product/service that supplies the card account.\n\nCommonly used values:\n\n* http://purl.org/goodrelations/v1#AmericanExpress\n* http://purl.org/goodrelations/v1#DinersClub\n* http://purl.org/goodrelations/v1#Discover\n* http://purl.org/goodrelations/v1#JCB\n* http://purl.org/goodrelations/v1#MasterCard\n* http://purl.org/goodrelations/v1#VISA
        */
	'CreditCard': { termType: 'NamedNode', value: 'https://schema.org/CreditCard', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/CreditCard' } as NamedNode,
	/** A crematorium. */
	'Crematorium': { termType: 'NamedNode', value: 'https://schema.org/Crematorium', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Crematorium' } as NamedNode,
	/** A [[CriticReview]] is a more specialized form of Review written or published by a source that is recognized for its reviewing activities. These can include online columns, travel and food guides, TV and radio shows, blogs and other independent Web sites. [[CriticReview]]s are typically more in-depth and professionally written. For simpler, casually written user/visitor/viewer/customer reviews, it is more appropriate to use the [[UserReview]] type. Review aggregator sites such as Metacritic already separate out the site's user reviews from selected critic reviews that originate from third-party sources. */
	'CriticReview': { termType: 'NamedNode', value: 'https://schema.org/CriticReview', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/CriticReview' } as NamedNode,
	/** Studies carried out on pre-existing data (usually from 'snapshot' surveys), such as that collected by the Census Bureau. Sometimes called Prevalence Studies. */
	'CrossSectional': { termType: 'NamedNode', value: 'https://schema.org/CrossSectional', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/CrossSectional' } as NamedNode,
	/** Text representing a CSS selector. */
	'CssSelectorType': { termType: 'NamedNode', value: 'https://schema.org/CssSelectorType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/CssSelectorType' } as NamedNode,
	/** A service to convert funds from one currency to another currency. */
	'CurrencyConversionService': { termType: 'NamedNode', value: 'https://schema.org/CurrencyConversionService', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/CurrencyConversionService' } as NamedNode,
	/** An alternative, closely-related condition typically considered later in the differential diagnosis process along with the signs that are used to distinguish it. */
	'DDxElement': { termType: 'NamedNode', value: 'https://schema.org/DDxElement', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DDxElement' } as NamedNode,
	/** DJMixAlbum. */
	'DJMixAlbum': { termType: 'NamedNode', value: 'https://schema.org/DJMixAlbum', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DJMixAlbum' } as NamedNode,
	/** DVDFormat. */
	'DVDFormat': { termType: 'NamedNode', value: 'https://schema.org/DVDFormat', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DVDFormat' } as NamedNode,
	/** Indicates that the item is damaged. */
	'DamagedCondition': { termType: 'NamedNode', value: 'https://schema.org/DamagedCondition', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DamagedCondition' } as NamedNode,
	/** Event type: A social dance. */
	'DanceEvent': { termType: 'NamedNode', value: 'https://schema.org/DanceEvent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DanceEvent' } as NamedNode,
	/** A dance group&#x2014;for example, the Alvin Ailey Dance Theater or Riverdance. */
	'DanceGroup': { termType: 'NamedNode', value: 'https://schema.org/DanceGroup', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DanceGroup' } as NamedNode,
	/** The item is dangerous and requires careful handling and/or special training of the user. See also the [UN Model Classification](https://unece.org/DAM/trans/danger/publi/unrec/rev17/English/02EREv17_Part2.pdf) defining the 9 classes of dangerous goods such as explosives, gases, flammables, and more. */
	'DangerousGoodConsideration': { termType: 'NamedNode', value: 'https://schema.org/DangerousGoodConsideration', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DangerousGoodConsideration' } as NamedNode,
	/** A collection of datasets. */
	'DataCatalog': { termType: 'NamedNode', value: 'https://schema.org/DataCatalog', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DataCatalog' } as NamedNode,
	/** All or part of a [[Dataset]] in downloadable form.  */
	'DataDownload': { termType: 'NamedNode', value: 'https://schema.org/DataDownload', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DataDownload' } as NamedNode,
	/** A single feed providing structured information about one or more entities or topics. */
	'DataFeed': { termType: 'NamedNode', value: 'https://schema.org/DataFeed', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DataFeed' } as NamedNode,
	/** A single item within a larger data feed. */
	'DataFeedItem': { termType: 'NamedNode', value: 'https://schema.org/DataFeedItem', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DataFeedItem' } as NamedNode,
	/** The basic data types such as Integers, Strings, etc. */
	'DataType': { termType: 'NamedNode', value: 'https://schema.org/DataType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DataType' } as NamedNode,
	/** A body of structured information describing some topic(s) of interest. */
	'Dataset': { termType: 'NamedNode', value: 'https://schema.org/Dataset', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Dataset' } as NamedNode,
	/** A date value in [ISO 8601 date format](http://en.wikipedia.org/wiki/ISO_8601). */
	'Date': { termType: 'NamedNode', value: 'https://schema.org/Date', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Date' } as NamedNode,
	/** A combination of date and time of day in the form [-]CCYY-MM-DDThh:mm:ss[Z|(+|-)hh:mm] (see Chapter 5.4 of ISO 8601). */
	'DateTime': { termType: 'NamedNode', value: 'https://schema.org/DateTime', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DateTime' } as NamedNode,
	/** A DatedMoneySpecification represents monetary values with optional start and end dates. For example, this could represent an employee's salary over a specific period of time. __Note:__ This type has been superseded by [[MonetaryAmount]], use of that type is recommended. */
	'DatedMoneySpecification': { termType: 'NamedNode', value: 'https://schema.org/DatedMoneySpecification', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DatedMoneySpecification' } as NamedNode,
	/** The day of the week, e.g. used to specify to which day the opening hours of an OpeningHoursSpecification refer.

Originally, URLs from [GoodRelations](http://purl.org/goodrelations/v1) were used (for [[Monday]], [[Tuesday]], [[Wednesday]], [[Thursday]], [[Friday]], [[Saturday]], [[Sunday]] plus a special entry for [[PublicHolidays]]); these have now been integrated directly into schema.org.
       */
	'DayOfWeek': { termType: 'NamedNode', value: 'https://schema.org/DayOfWeek', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DayOfWeek' } as NamedNode,
	/** A day spa. */
	'DaySpa': { termType: 'NamedNode', value: 'https://schema.org/DaySpa', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DaySpa' } as NamedNode,
	/** The act of stopping or deactivating a device or application (e.g. stopping a timer or turning off a flashlight). */
	'DeactivateAction': { termType: 'NamedNode', value: 'https://schema.org/DeactivateAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DeactivateAction' } as NamedNode,
	/** Content coded 'missing context' in a [[MediaReview]], considered in the context of how it was published or shared.

For a [[VideoObject]] to be 'missing context': Presenting unaltered video in an inaccurate manner that misrepresents the footage. For example, using incorrect dates or locations, altering the transcript or sharing brief clips from a longer video to mislead viewers. (A video rated 'original' can also be missing context.)

For an [[ImageObject]] to be 'missing context': Presenting unaltered images in an inaccurate manner to misrepresent the image and mislead the viewer. For example, a common tactic is using an unaltered image but saying it came from a different time or place. (An image rated 'original' can also be missing context.)

For an [[ImageObject]] with embedded text to be 'missing context': An unaltered image presented in an inaccurate manner to misrepresent the image and mislead the viewer. For example, a common tactic is using an unaltered image but saying it came from a different time or place. (An 'original' image with inaccurate text would generally fall in this category.)

For an [[AudioObject]] to be 'missing context': Unaltered audio presented in an inaccurate manner that misrepresents it. For example, using incorrect dates or locations, or sharing brief clips from a longer recording to mislead viewers. (Audio rated “original” can also be missing context.)
 */
	'DecontextualizedContent': { termType: 'NamedNode', value: 'https://schema.org/DecontextualizedContent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DecontextualizedContent' } as NamedNode,
	/** A defence establishment, such as an army or navy base. */
	'DefenceEstablishment': { termType: 'NamedNode', value: 'https://schema.org/DefenceEstablishment', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DefenceEstablishment' } as NamedNode,
	/** A DefinedRegion is a geographic area defined by potentially arbitrary (rather than political, administrative or natural geographical) criteria. Properties are provided for defining a region by reference to sets of postal codes.

Examples: a delivery destination when shopping. Region where regional pricing is configured.

Requirement 1:
Country: US
States: "NY", "CA"

Requirement 2:
Country: US
PostalCode Set: { [94000-94585], [97000, 97999], [13000, 13599]}
{ [12345, 12345], [78945, 78945], }
Region = state, canton, prefecture, autonomous community...
 */
	'DefinedRegion': { termType: 'NamedNode', value: 'https://schema.org/DefinedRegion', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DefinedRegion' } as NamedNode,
	/** A word, name, acronym, phrase, etc. with a formal definition. Often used in the context of category or subject classification, glossaries or dictionaries, product or creative work types, etc. Use the name property for the term being defined, use termCode if the term has an alpha-numeric code allocated, use description to provide the definition of the term. */
	'DefinedTerm': { termType: 'NamedNode', value: 'https://schema.org/DefinedTerm', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DefinedTerm' } as NamedNode,
	/** A set of defined terms, for example a set of categories or a classification scheme, a glossary, dictionary or enumeration. */
	'DefinedTermSet': { termType: 'NamedNode', value: 'https://schema.org/DefinedTermSet', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DefinedTermSet' } as NamedNode,
	/** Indicates a document for which the text is conclusively what the law says and is legally binding. (E.g. the digitally signed version of an Official Journal.)
  Something "Definitive" is considered to be also [[AuthoritativeLegalValue]]. */
	'DefinitiveLegalValue': { termType: 'NamedNode', value: 'https://schema.org/DefinitiveLegalValue', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DefinitiveLegalValue' } as NamedNode,
	/** The act of editing a recipient by removing one of its objects. */
	'DeleteAction': { termType: 'NamedNode', value: 'https://schema.org/DeleteAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DeleteAction' } as NamedNode,
	/** The price for the delivery of an offer using a particular delivery method. */
	'DeliveryChargeSpecification': { termType: 'NamedNode', value: 'https://schema.org/DeliveryChargeSpecification', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DeliveryChargeSpecification' } as NamedNode,
	/** An event involving the delivery of an item. */
	'DeliveryEvent': { termType: 'NamedNode', value: 'https://schema.org/DeliveryEvent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DeliveryEvent' } as NamedNode,
	/** A delivery method is a standardized procedure for transferring the product or service to the destination of fulfillment chosen by the customer. Delivery methods are characterized by the means of transportation used, and by the organization or group that is the contracting party for the sending organization or person.\n\nCommonly used values:\n\n* http://purl.org/goodrelations/v1#DeliveryModeDirectDownload\n* http://purl.org/goodrelations/v1#DeliveryModeFreight\n* http://purl.org/goodrelations/v1#DeliveryModeMail\n* http://purl.org/goodrelations/v1#DeliveryModeOwnFleet\n* http://purl.org/goodrelations/v1#DeliveryModePickUp\n* http://purl.org/goodrelations/v1#DHL\n* http://purl.org/goodrelations/v1#FederalExpress\n* http://purl.org/goodrelations/v1#UPS
         */
	'DeliveryMethod': { termType: 'NamedNode', value: 'https://schema.org/DeliveryMethod', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DeliveryMethod' } as NamedNode,
	/** A DeliveryTimeSettings represents re-usable pieces of shipping information, relating to timing. It is designed for publication on an URL that may be referenced via the [[shippingSettingsLink]] property of an [[OfferShippingDetails]]. Several occurrences can be published, distinguished (and identified/referenced) by their different values for [[transitTimeLabel]]. */
	'DeliveryTimeSettings': { termType: 'NamedNode', value: 'https://schema.org/DeliveryTimeSettings', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DeliveryTimeSettings' } as NamedNode,
	/** A demand entity represents the public, not necessarily binding, not necessarily exclusive, announcement by an organization or person to seek a certain type of goods or services. For describing demand using this type, the very same properties used for Offer apply. */
	'Demand': { termType: 'NamedNode', value: 'https://schema.org/Demand', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Demand' } as NamedNode,
	/** DemoAlbum. */
	'DemoAlbum': { termType: 'NamedNode', value: 'https://schema.org/DemoAlbum', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DemoAlbum' } as NamedNode,
	/** Indicates demo game availability, i.e. a somehow limited demonstration of the full game. */
	'DemoGameAvailability': { termType: 'NamedNode', value: 'https://schema.org/DemoGameAvailability', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DemoGameAvailability' } as NamedNode,
	/** A dentist. */
	'Dentist': { termType: 'NamedNode', value: 'https://schema.org/Dentist', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Dentist' } as NamedNode,
	/** A branch of medicine that is involved in the dental care. */
	'Dentistry': { termType: 'NamedNode', value: 'https://schema.org/Dentistry', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Dentistry' } as NamedNode,
	/** The act of  departing from a place. An agent departs from a fromLocation for a destination, optionally with participants. */
	'DepartAction': { termType: 'NamedNode', value: 'https://schema.org/DepartAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DepartAction' } as NamedNode,
	/** A department store. */
	'DepartmentStore': { termType: 'NamedNode', value: 'https://schema.org/DepartmentStore', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DepartmentStore' } as NamedNode,
	/** A type of Bank Account with a main purpose of depositing funds to gain interest or other benefits. */
	'DepositAccount': { termType: 'NamedNode', value: 'https://schema.org/DepositAccount', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DepositAccount' } as NamedNode,
	/** Something relating to or practicing dermatology. */
	'Dermatologic': { termType: 'NamedNode', value: 'https://schema.org/Dermatologic', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Dermatologic' } as NamedNode,
	/** A specific branch of medical science that pertains to diagnosis and treatment of disorders of skin. */
	'Dermatology': { termType: 'NamedNode', value: 'https://schema.org/Dermatology', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Dermatology' } as NamedNode,
	/** Represents the broad notion of 'desktop' browsers as a Web Platform. */
	'DesktopWebPlatform': { termType: 'NamedNode', value: 'https://schema.org/DesktopWebPlatform', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DesktopWebPlatform' } as NamedNode,
	/** A diet appropriate for people with diabetes. */
	'DiabeticDiet': { termType: 'NamedNode', value: 'https://schema.org/DiabeticDiet', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DiabeticDiet' } as NamedNode,
	/** A medical device used for diagnostic purposes. */
	'Diagnostic': { termType: 'NamedNode', value: 'https://schema.org/Diagnostic', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Diagnostic' } as NamedNode,
	/** A medical laboratory that offers on-site or off-site diagnostic services. */
	'DiagnosticLab': { termType: 'NamedNode', value: 'https://schema.org/DiagnosticLab', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DiagnosticLab' } as NamedNode,
	/** A medical procedure intended primarily for diagnostic, as opposed to therapeutic, purposes. */
	'DiagnosticProcedure': { termType: 'NamedNode', value: 'https://schema.org/DiagnosticProcedure', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DiagnosticProcedure' } as NamedNode,
	/** A strategy of regulating the intake of food to achieve or maintain a specific health-related goal. */
	'Diet': { termType: 'NamedNode', value: 'https://schema.org/Diet', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Diet' } as NamedNode,
	/** Dietetics and nutrition as a medical specialty. */
	'DietNutrition': { termType: 'NamedNode', value: 'https://schema.org/DietNutrition', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DietNutrition' } as NamedNode,
	/** A product taken by mouth that contains a dietary ingredient intended to supplement the diet. Dietary ingredients may include vitamins, minerals, herbs or other botanicals, amino acids, and substances such as enzymes, organ tissues, glandulars and metabolites. */
	'DietarySupplement': { termType: 'NamedNode', value: 'https://schema.org/DietarySupplement', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DietarySupplement' } as NamedNode,
	/** DigitalAudioTapeFormat. */
	'DigitalAudioTapeFormat': { termType: 'NamedNode', value: 'https://schema.org/DigitalAudioTapeFormat', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DigitalAudioTapeFormat' } as NamedNode,
	/** An electronic file or document. */
	'DigitalDocument': { termType: 'NamedNode', value: 'https://schema.org/DigitalDocument', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DigitalDocument' } as NamedNode,
	/** A permission for a particular person or group to access a particular file. */
	'DigitalDocumentPermission': { termType: 'NamedNode', value: 'https://schema.org/DigitalDocumentPermission', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DigitalDocumentPermission' } as NamedNode,
	/** A type of permission which can be granted for accessing a digital document. */
	'DigitalDocumentPermissionType': { termType: 'NamedNode', value: 'https://schema.org/DigitalDocumentPermissionType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DigitalDocumentPermissionType' } as NamedNode,
	/** DigitalFormat. */
	'DigitalFormat': { termType: 'NamedNode', value: 'https://schema.org/DigitalFormat', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DigitalFormat' } as NamedNode,
	/** Enumerates some common technology platforms, for use with properties such as [[actionPlatform]]. It is not supposed to be comprehensive - when a suitable code is not enumerated here, textual or URL values can be used instead. These codes are at a fairly high level and do not deal with versioning and other nuance. Additional codes can be suggested [in github](https://github.com/schemaorg/schemaorg/issues/3057).  */
	'DigitalPlatformEnumeration': { termType: 'NamedNode', value: 'https://schema.org/DigitalPlatformEnumeration', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DigitalPlatformEnumeration' } as NamedNode,
	/** DisabilitySupport: this is a benefit for disability support. */
	'DisabilitySupport': { termType: 'NamedNode', value: 'https://schema.org/DisabilitySupport', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DisabilitySupport' } as NamedNode,
	/** The act of expressing a difference of opinion with the object. An agent disagrees to/about an object (a proposition, topic or theme) with participants. */
	'DisagreeAction': { termType: 'NamedNode', value: 'https://schema.org/DisagreeAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DisagreeAction' } as NamedNode,
	/** Indicates that the item has been discontinued. */
	'Discontinued': { termType: 'NamedNode', value: 'https://schema.org/Discontinued', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Discontinued' } as NamedNode,
	/** The act of discovering/finding an object. */
	'DiscoverAction': { termType: 'NamedNode', value: 'https://schema.org/DiscoverAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DiscoverAction' } as NamedNode,
	/** A posting to a discussion forum. */
	'DiscussionForumPosting': { termType: 'NamedNode', value: 'https://schema.org/DiscussionForumPosting', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DiscussionForumPosting' } as NamedNode,
	/** The act of expressing a negative sentiment about the object. An agent dislikes an object (a proposition, topic or theme) with participants. */
	'DislikeAction': { termType: 'NamedNode', value: 'https://schema.org/DislikeAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DislikeAction' } as NamedNode,
	/** Properties that take Distances as values are of the form '&lt;Number&gt; &lt;Length unit of measure&gt;'. E.g., '7 ft'. */
	'Distance': { termType: 'NamedNode', value: 'https://schema.org/Distance', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Distance' } as NamedNode,
	/** Represents the distance fee (e.g., price per km or mile) part of the total price for an offered product, for example a car rental. */
	'DistanceFee': { termType: 'NamedNode', value: 'https://schema.org/DistanceFee', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DistanceFee' } as NamedNode,
	/** A distillery. */
	'Distillery': { termType: 'NamedNode', value: 'https://schema.org/Distillery', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Distillery' } as NamedNode,
	/** The act of providing goods, services, or money without compensation, often for philanthropic reasons. */
	'DonateAction': { termType: 'NamedNode', value: 'https://schema.org/DonateAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DonateAction' } as NamedNode,
	/** A specific dosing schedule for a drug or supplement. */
	'DoseSchedule': { termType: 'NamedNode', value: 'https://schema.org/DoseSchedule', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DoseSchedule' } as NamedNode,
	/** A trial design in which neither the researcher nor the patient knows the details of the treatment the patient was randomly assigned to. */
	'DoubleBlindedTrial': { termType: 'NamedNode', value: 'https://schema.org/DoubleBlindedTrial', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DoubleBlindedTrial' } as NamedNode,
	/** The act of downloading an object. */
	'DownloadAction': { termType: 'NamedNode', value: 'https://schema.org/DownloadAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DownloadAction' } as NamedNode,
	/** Represents the downpayment (up-front payment) price component of the total price for an offered product that has additional installment payments. */
	'Downpayment': { termType: 'NamedNode', value: 'https://schema.org/Downpayment', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Downpayment' } as NamedNode,
	/** The act of producing a visual/graphical representation of an object, typically with a pen/pencil and paper as instruments. */
	'DrawAction': { termType: 'NamedNode', value: 'https://schema.org/DrawAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DrawAction' } as NamedNode,
	/** A picture or diagram made with a pencil, pen, or crayon rather than paint. */
	'Drawing': { termType: 'NamedNode', value: 'https://schema.org/Drawing', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Drawing' } as NamedNode,
	/** The act of swallowing liquids. */
	'DrinkAction': { termType: 'NamedNode', value: 'https://schema.org/DrinkAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DrinkAction' } as NamedNode,
	/** A value indicating which roadwheels will receive torque. */
	'DriveWheelConfigurationValue': { termType: 'NamedNode', value: 'https://schema.org/DriveWheelConfigurationValue', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DriveWheelConfigurationValue' } as NamedNode,
	/** Indicates the usage of the vehicle for driving school. */
	'DrivingSchoolVehicleUsage': { termType: 'NamedNode', value: 'https://schema.org/DrivingSchoolVehicleUsage', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DrivingSchoolVehicleUsage' } as NamedNode,
	/** A chemical or biologic substance, used as a medical therapy, that has a physiological effect on an organism. Here the term drug is used interchangeably with the term medicine although clinical knowledge makes a clear difference between them. */
	'Drug': { termType: 'NamedNode', value: 'https://schema.org/Drug', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Drug' } as NamedNode,
	/** A class of medical drugs, e.g., statins. Classes can represent general pharmacological class, common mechanisms of action, common physiological effects, etc. */
	'DrugClass': { termType: 'NamedNode', value: 'https://schema.org/DrugClass', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DrugClass' } as NamedNode,
	/** The cost per unit of a medical drug. Note that this type is not meant to represent the price in an offer of a drug for sale; see the Offer type for that. This type will typically be used to tag wholesale or average retail cost of a drug, or maximum reimbursable cost. Costs of medical drugs vary widely depending on how and where they are paid for, so while this type captures some of the variables, costs should be used with caution by consumers of this schema's markup. */
	'DrugCost': { termType: 'NamedNode', value: 'https://schema.org/DrugCost', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DrugCost' } as NamedNode,
	/** Enumerated categories of medical drug costs. */
	'DrugCostCategory': { termType: 'NamedNode', value: 'https://schema.org/DrugCostCategory', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DrugCostCategory' } as NamedNode,
	/** The legal availability status of a medical drug. */
	'DrugLegalStatus': { termType: 'NamedNode', value: 'https://schema.org/DrugLegalStatus', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DrugLegalStatus' } as NamedNode,
	/** Categories that represent an assessment of the risk of fetal injury due to a drug or pharmaceutical used as directed by the mother during pregnancy. */
	'DrugPregnancyCategory': { termType: 'NamedNode', value: 'https://schema.org/DrugPregnancyCategory', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DrugPregnancyCategory' } as NamedNode,
	/** Indicates whether this drug is available by prescription or over-the-counter. */
	'DrugPrescriptionStatus': { termType: 'NamedNode', value: 'https://schema.org/DrugPrescriptionStatus', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DrugPrescriptionStatus' } as NamedNode,
	/** A specific strength in which a medical drug is available in a specific country. */
	'DrugStrength': { termType: 'NamedNode', value: 'https://schema.org/DrugStrength', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DrugStrength' } as NamedNode,
	/** A dry-cleaning business. */
	'DryCleaningOrLaundry': { termType: 'NamedNode', value: 'https://schema.org/DryCleaningOrLaundry', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/DryCleaningOrLaundry' } as NamedNode,
	/** Quantity: Duration (use [ISO 8601 duration format](http://en.wikipedia.org/wiki/ISO_8601)). */
	'Duration': { termType: 'NamedNode', value: 'https://schema.org/Duration', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Duration' } as NamedNode,
	/** Book format: Ebook. */
	'EBook': { termType: 'NamedNode', value: 'https://schema.org/EBook', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/EBook' } as NamedNode,
	/** EPRelease. */
	'EPRelease': { termType: 'NamedNode', value: 'https://schema.org/EPRelease', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/EPRelease' } as NamedNode,
	/** Represents EU Energy Efficiency Class A as defined in EU energy labeling regulations. */
	'EUEnergyEfficiencyCategoryA': { termType: 'NamedNode', value: 'https://schema.org/EUEnergyEfficiencyCategoryA', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/EUEnergyEfficiencyCategoryA' } as NamedNode,
	/** Represents EU Energy Efficiency Class A+ as defined in EU energy labeling regulations. */
	'EUEnergyEfficiencyCategoryA1Plus': { termType: 'NamedNode', value: 'https://schema.org/EUEnergyEfficiencyCategoryA1Plus', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/EUEnergyEfficiencyCategoryA1Plus' } as NamedNode,
	/** Represents EU Energy Efficiency Class A++ as defined in EU energy labeling regulations. */
	'EUEnergyEfficiencyCategoryA2Plus': { termType: 'NamedNode', value: 'https://schema.org/EUEnergyEfficiencyCategoryA2Plus', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/EUEnergyEfficiencyCategoryA2Plus' } as NamedNode,
	/** Represents EU Energy Efficiency Class A+++ as defined in EU energy labeling regulations. */
	'EUEnergyEfficiencyCategoryA3Plus': { termType: 'NamedNode', value: 'https://schema.org/EUEnergyEfficiencyCategoryA3Plus', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/EUEnergyEfficiencyCategoryA3Plus' } as NamedNode,
	/** Represents EU Energy Efficiency Class B as defined in EU energy labeling regulations. */
	'EUEnergyEfficiencyCategoryB': { termType: 'NamedNode', value: 'https://schema.org/EUEnergyEfficiencyCategoryB', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/EUEnergyEfficiencyCategoryB' } as NamedNode,
	/** Represents EU Energy Efficiency Class C as defined in EU energy labeling regulations. */
	'EUEnergyEfficiencyCategoryC': { termType: 'NamedNode', value: 'https://schema.org/EUEnergyEfficiencyCategoryC', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/EUEnergyEfficiencyCategoryC' } as NamedNode,
	/** Represents EU Energy Efficiency Class D as defined in EU energy labeling regulations. */
	'EUEnergyEfficiencyCategoryD': { termType: 'NamedNode', value: 'https://schema.org/EUEnergyEfficiencyCategoryD', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/EUEnergyEfficiencyCategoryD' } as NamedNode,
	/** Represents EU Energy Efficiency Class E as defined in EU energy labeling regulations. */
	'EUEnergyEfficiencyCategoryE': { termType: 'NamedNode', value: 'https://schema.org/EUEnergyEfficiencyCategoryE', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/EUEnergyEfficiencyCategoryE' } as NamedNode,
	/** Represents EU Energy Efficiency Class F as defined in EU energy labeling regulations. */
	'EUEnergyEfficiencyCategoryF': { termType: 'NamedNode', value: 'https://schema.org/EUEnergyEfficiencyCategoryF', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/EUEnergyEfficiencyCategoryF' } as NamedNode,
	/** Represents EU Energy Efficiency Class G as defined in EU energy labeling regulations. */
	'EUEnergyEfficiencyCategoryG': { termType: 'NamedNode', value: 'https://schema.org/EUEnergyEfficiencyCategoryG', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/EUEnergyEfficiencyCategoryG' } as NamedNode,
	/** Enumerates the EU energy efficiency classes A-G as well as A+, A++, and A+++ as defined in EU directive 2017/1369. */
	'EUEnergyEfficiencyEnumeration': { termType: 'NamedNode', value: 'https://schema.org/EUEnergyEfficiencyEnumeration', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/EUEnergyEfficiencyEnumeration' } as NamedNode,
	/** Ear function assessment with clinical examination. */
	'Ear': { termType: 'NamedNode', value: 'https://schema.org/Ear', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Ear' } as NamedNode,
	/** The act of swallowing solid objects. */
	'EatAction': { termType: 'NamedNode', value: 'https://schema.org/EatAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/EatAction' } as NamedNode,
	/** Content coded 'edited or cropped content' in a [[MediaReview]], considered in the context of how it was published or shared.

For a [[VideoObject]] to be 'edited or cropped content': The video has been edited or rearranged. This category applies to time edits, including editing multiple videos together to alter the story being told or editing out large portions from a video.

For an [[ImageObject]] to be 'edited or cropped content': Presenting a part of an image from a larger whole to mislead the viewer.

For an [[ImageObject]] with embedded text to be 'edited or cropped content': Presenting a part of an image from a larger whole to mislead the viewer.

For an [[AudioObject]] to be 'edited or cropped content': The audio has been edited or rearranged. This category applies to time edits, including editing multiple audio clips together to alter the story being told or editing out large portions from the recording.
 */
	'EditedOrCroppedContent': { termType: 'NamedNode', value: 'https://schema.org/EditedOrCroppedContent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/EditedOrCroppedContent' } as NamedNode,
	/** Event type: Education event. */
	'EducationEvent': { termType: 'NamedNode', value: 'https://schema.org/EducationEvent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/EducationEvent' } as NamedNode,
	/** An EducationalAudience. */
	'EducationalAudience': { termType: 'NamedNode', value: 'https://schema.org/EducationalAudience', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/EducationalAudience' } as NamedNode,
	/** An educational or occupational credential. A diploma, academic degree, certification, qualification, badge, etc., that may be awarded to a person or other entity that meets the requirements defined by the credentialer. */
	'EducationalOccupationalCredential': { termType: 'NamedNode', value: 'https://schema.org/EducationalOccupationalCredential', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/EducationalOccupationalCredential' } as NamedNode,
	/** A program offered by an institution which determines the learning progress to achieve an outcome, usually a credential like a degree or certificate. This would define a discrete set of opportunities (e.g., job, courses) that together constitute a program with a clear start, end, set of requirements, and transition to a new occupational opportunity (e.g., a job), or sometimes a higher educational opportunity (e.g., an advanced degree). */
	'EducationalOccupationalProgram': { termType: 'NamedNode', value: 'https://schema.org/EducationalOccupationalProgram', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/EducationalOccupationalProgram' } as NamedNode,
	/** An educational organization. */
	'EducationalOrganization': { termType: 'NamedNode', value: 'https://schema.org/EducationalOrganization', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/EducationalOrganization' } as NamedNode,
	/** Content about the effectiveness-related aspects of a health topic. */
	'EffectivenessHealthAspect': { termType: 'NamedNode', value: 'https://schema.org/EffectivenessHealthAspect', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/EffectivenessHealthAspect' } as NamedNode,
	/** An electrician. */
	'Electrician': { termType: 'NamedNode', value: 'https://schema.org/Electrician', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Electrician' } as NamedNode,
	/** An electronics store. */
	'ElectronicsStore': { termType: 'NamedNode', value: 'https://schema.org/ElectronicsStore', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ElectronicsStore' } as NamedNode,
	/** An elementary school. */
	'ElementarySchool': { termType: 'NamedNode', value: 'https://schema.org/ElementarySchool', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ElementarySchool' } as NamedNode,
	/** An email message. */
	'EmailMessage': { termType: 'NamedNode', value: 'https://schema.org/EmailMessage', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/EmailMessage' } as NamedNode,
	/** An embassy. */
	'Embassy': { termType: 'NamedNode', value: 'https://schema.org/Embassy', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Embassy' } as NamedNode,
	/** A specific branch of medical science that deals with the evaluation and initial treatment of medical conditions caused by trauma or sudden illness. */
	'Emergency': { termType: 'NamedNode', value: 'https://schema.org/Emergency', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Emergency' } as NamedNode,
	/** An emergency service, such as a fire station or ER. */
	'EmergencyService': { termType: 'NamedNode', value: 'https://schema.org/EmergencyService', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/EmergencyService' } as NamedNode,
	/** A subclass of OrganizationRole used to describe employee relationships. */
	'EmployeeRole': { termType: 'NamedNode', value: 'https://schema.org/EmployeeRole', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/EmployeeRole' } as NamedNode,
	/** An aggregate rating of an Organization related to its role as an employer. */
	'EmployerAggregateRating': { termType: 'NamedNode', value: 'https://schema.org/EmployerAggregateRating', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/EmployerAggregateRating' } as NamedNode,
	/** An [[EmployerReview]] is a review of an [[Organization]] regarding its role as an employer, written by a current or former employee of that organization. */
	'EmployerReview': { termType: 'NamedNode', value: 'https://schema.org/EmployerReview', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/EmployerReview' } as NamedNode,
	/** An employment agency. */
	'EmploymentAgency': { termType: 'NamedNode', value: 'https://schema.org/EmploymentAgency', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/EmploymentAgency' } as NamedNode,
	/** A specific branch of medical science that pertains to diagnosis and treatment of disorders of endocrine glands and their secretions. */
	'Endocrine': { termType: 'NamedNode', value: 'https://schema.org/Endocrine', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Endocrine' } as NamedNode,
	/** An agent approves/certifies/likes/supports/sanctions an object. */
	'EndorseAction': { termType: 'NamedNode', value: 'https://schema.org/EndorseAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/EndorseAction' } as NamedNode,
	/** An EndorsementRating is a rating that expresses some level of endorsement, for example inclusion in a "critic's pick" blog, a
"Like" or "+1" on a social network. It can be considered the [[result]] of an [[EndorseAction]] in which the [[object]] of the action is rated positively by
some [[agent]]. As is common elsewhere in schema.org, it is sometimes more useful to describe the results of such an action without explicitly describing the [[Action]].

An [[EndorsementRating]] may be part of a numeric scale or organized system, but this is not required: having an explicit type for indicating a positive,
endorsement rating is particularly useful in the absence of numeric scales as it helps consumers understand that the rating is broadly positive.
 */
	'EndorsementRating': { termType: 'NamedNode', value: 'https://schema.org/EndorsementRating', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/EndorsementRating' } as NamedNode,
	/** Properties that take Energy as values are of the form '&lt;Number&gt; &lt;Energy unit of measure&gt;'. */
	'Energy': { termType: 'NamedNode', value: 'https://schema.org/Energy', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Energy' } as NamedNode,
	/** EnergyConsumptionDetails represents information related to the energy efficiency of a product that consumes energy. The information that can be provided is based on international regulations such as for example [EU directive 2017/1369](https://eur-lex.europa.eu/eli/reg/2017/1369/oj) for energy labeling and the [Energy labeling rule](https://www.ftc.gov/enforcement/rules/rulemaking-regulatory-reform-proceedings/energy-water-use-labeling-consumer) under the Energy Policy and Conservation Act (EPCA) in the US. */
	'EnergyConsumptionDetails': { termType: 'NamedNode', value: 'https://schema.org/EnergyConsumptionDetails', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/EnergyConsumptionDetails' } as NamedNode,
	/** Enumerates energy efficiency levels (also known as "classes" or "ratings") and certifications that are part of several international energy efficiency standards. */
	'EnergyEfficiencyEnumeration': { termType: 'NamedNode', value: 'https://schema.org/EnergyEfficiencyEnumeration', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/EnergyEfficiencyEnumeration' } as NamedNode,
	/** Represents EnergyStar certification. */
	'EnergyStarCertified': { termType: 'NamedNode', value: 'https://schema.org/EnergyStarCertified', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/EnergyStarCertified' } as NamedNode,
	/** Used to indicate whether a product is EnergyStar certified. */
	'EnergyStarEnergyEfficiencyEnumeration': { termType: 'NamedNode', value: 'https://schema.org/EnergyStarEnergyEfficiencyEnumeration', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/EnergyStarEnergyEfficiencyEnumeration' } as NamedNode,
	/** Information about the engine of the vehicle. A vehicle can have multiple engines represented by multiple engine specification entities. */
	'EngineSpecification': { termType: 'NamedNode', value: 'https://schema.org/EngineSpecification', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/EngineSpecification' } as NamedNode,
	/** Enrolling participants by invitation only. */
	'EnrollingByInvitation': { termType: 'NamedNode', value: 'https://schema.org/EnrollingByInvitation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/EnrollingByInvitation' } as NamedNode,
	/** A business providing entertainment. */
	'EntertainmentBusiness': { termType: 'NamedNode', value: 'https://schema.org/EntertainmentBusiness', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/EntertainmentBusiness' } as NamedNode,
	/** An entry point, within some Web-based protocol. */
	'EntryPoint': { termType: 'NamedNode', value: 'https://schema.org/EntryPoint', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/EntryPoint' } as NamedNode,
	/** Lists or enumerations—for example, a list of cuisines or music genres, etc. */
	'Enumeration': { termType: 'NamedNode', value: 'https://schema.org/Enumeration', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Enumeration' } as NamedNode,
	/** A media episode (e.g. TV, radio, video game) which can be part of a series or season. */
	'Episode': { termType: 'NamedNode', value: 'https://schema.org/Episode', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Episode' } as NamedNode,
	/** An event happening at a certain time and location, such as a concert, lecture, or festival. Ticketing information may be added via the [[offers]] property. Repeated events may be structured as separate Event objects. */
	'Event': { termType: 'NamedNode', value: 'https://schema.org/Event', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Event' } as NamedNode,
	/** An EventAttendanceModeEnumeration value is one of potentially several modes of organising an event, relating to whether it is online or offline. */
	'EventAttendanceModeEnumeration': { termType: 'NamedNode', value: 'https://schema.org/EventAttendanceModeEnumeration', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/EventAttendanceModeEnumeration' } as NamedNode,
	/** The event has been cancelled. If the event has multiple startDate values, all are assumed to be cancelled. Either startDate or previousStartDate may be used to specify the event's cancelled date(s). */
	'EventCancelled': { termType: 'NamedNode', value: 'https://schema.org/EventCancelled', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/EventCancelled' } as NamedNode,
	/** Indicates that the event was changed to allow online participation. See [[eventAttendanceMode]] for specifics of whether it is now fully or partially online. */
	'EventMovedOnline': { termType: 'NamedNode', value: 'https://schema.org/EventMovedOnline', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/EventMovedOnline' } as NamedNode,
	/** The event has been postponed and no new date has been set. The event's previousStartDate should be set. */
	'EventPostponed': { termType: 'NamedNode', value: 'https://schema.org/EventPostponed', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/EventPostponed' } as NamedNode,
	/** The event has been rescheduled. The event's previousStartDate should be set to the old date and the startDate should be set to the event's new date. (If the event has been rescheduled multiple times, the previousStartDate property may be repeated.) */
	'EventRescheduled': { termType: 'NamedNode', value: 'https://schema.org/EventRescheduled', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/EventRescheduled' } as NamedNode,
	/** A reservation for an event like a concert, sporting event, or lecture.\n\nNote: This type is for information about actual reservations, e.g. in confirmation emails or HTML pages with individual confirmations of reservations. For offers of tickets, use [[Offer]]. */
	'EventReservation': { termType: 'NamedNode', value: 'https://schema.org/EventReservation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/EventReservation' } as NamedNode,
	/** The event is taking place or has taken place on the startDate as scheduled. Use of this value is optional, as it is assumed by default. */
	'EventScheduled': { termType: 'NamedNode', value: 'https://schema.org/EventScheduled', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/EventScheduled' } as NamedNode,
	/** A series of [[Event]]s. Included events can relate with the series using the [[superEvent]] property.

An EventSeries is a collection of events that share some unifying characteristic. For example, "The Olympic Games" is a series, which
is repeated regularly. The "2012 London Olympics" can be presented both as an [[Event]] in the series "Olympic Games", and as an
[[EventSeries]] that included a number of sporting competitions as Events.

The nature of the association between the events in an [[EventSeries]] can vary, but typical examples could
include a thematic event series (e.g. topical meetups or classes), or a series of regular events that share a location, attendee group and/or organizers.

EventSeries has been defined as a kind of Event to make it easy for publishers to use it in an Event context without
worrying about which kinds of series are really event-like enough to call an Event. In general an EventSeries
may seem more Event-like when the period of time is compact and when aspects such as location are fixed, but
it may also sometimes prove useful to describe a longer-term series as an Event.
    */
	'EventSeries': { termType: 'NamedNode', value: 'https://schema.org/EventSeries', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/EventSeries' } as NamedNode,
	/** EventStatusType is an enumeration type whose instances represent several states that an Event may be in. */
	'EventStatusType': { termType: 'NamedNode', value: 'https://schema.org/EventStatusType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/EventStatusType' } as NamedNode,
	/** An event venue. */
	'EventVenue': { termType: 'NamedNode', value: 'https://schema.org/EventVenue', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/EventVenue' } as NamedNode,
	/** Data derived from multiple randomized clinical trials or meta-analyses. */
	'EvidenceLevelA': { termType: 'NamedNode', value: 'https://schema.org/EvidenceLevelA', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/EvidenceLevelA' } as NamedNode,
	/** Data derived from a single randomized trial, or nonrandomized studies. */
	'EvidenceLevelB': { termType: 'NamedNode', value: 'https://schema.org/EvidenceLevelB', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/EvidenceLevelB' } as NamedNode,
	/** Only consensus opinion of experts, case studies, or standard-of-care. */
	'EvidenceLevelC': { termType: 'NamedNode', value: 'https://schema.org/EvidenceLevelC', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/EvidenceLevelC' } as NamedNode,
	/** An example [[MeasurementMethodEnum]] (to remove when real enums are added). */
	'ExampleMeasurementMethodEnum': { termType: 'NamedNode', value: 'https://schema.org/ExampleMeasurementMethodEnum', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ExampleMeasurementMethodEnum' } as NamedNode,
	/** A structured value representing exchange rate. */
	'ExchangeRateSpecification': { termType: 'NamedNode', value: 'https://schema.org/ExchangeRateSpecification', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ExchangeRateSpecification' } as NamedNode,
	/** Specifies that a refund can be done as an exchange for the same product. */
	'ExchangeRefund': { termType: 'NamedNode', value: 'https://schema.org/ExchangeRefund', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ExchangeRefund' } as NamedNode,
	/** The act of participating in exertive activity for the purposes of improving health and fitness. */
	'ExerciseAction': { termType: 'NamedNode', value: 'https://schema.org/ExerciseAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ExerciseAction' } as NamedNode,
	/** A gym. */
	'ExerciseGym': { termType: 'NamedNode', value: 'https://schema.org/ExerciseGym', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ExerciseGym' } as NamedNode,
	/** Fitness-related activity designed for a specific health-related purpose, including defined exercise routines as well as activity prescribed by a clinician. */
	'ExercisePlan': { termType: 'NamedNode', value: 'https://schema.org/ExercisePlan', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ExercisePlan' } as NamedNode,
	/** Event type: Exhibition event, e.g. at a museum, library, archive, tradeshow, ... */
	'ExhibitionEvent': { termType: 'NamedNode', value: 'https://schema.org/ExhibitionEvent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ExhibitionEvent' } as NamedNode,
	/** Eye or ophthalmological function assessment with clinical examination. */
	'Eye': { termType: 'NamedNode', value: 'https://schema.org/Eye', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Eye' } as NamedNode,
	/** A [[FAQPage]] is a [[WebPage]] presenting one or more "[Frequently asked questions](https://en.wikipedia.org/wiki/FAQ)" (see also [[QAPage]]). */
	'FAQPage': { termType: 'NamedNode', value: 'https://schema.org/FAQPage', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/FAQPage' } as NamedNode,
	/** A designation by the US FDA signifying that adequate and well-controlled studies have failed to demonstrate a risk to the fetus in the first trimester of pregnancy (and there is no evidence of risk in later trimesters). */
	'FDAcategoryA': { termType: 'NamedNode', value: 'https://schema.org/FDAcategoryA', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/FDAcategoryA' } as NamedNode,
	/** A designation by the US FDA signifying that animal reproduction studies have failed to demonstrate a risk to the fetus and there are no adequate and well-controlled studies in pregnant women. */
	'FDAcategoryB': { termType: 'NamedNode', value: 'https://schema.org/FDAcategoryB', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/FDAcategoryB' } as NamedNode,
	/** A designation by the US FDA signifying that animal reproduction studies have shown an adverse effect on the fetus and there are no adequate and well-controlled studies in humans, but potential benefits may warrant use of the drug in pregnant women despite potential risks. */
	'FDAcategoryC': { termType: 'NamedNode', value: 'https://schema.org/FDAcategoryC', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/FDAcategoryC' } as NamedNode,
	/** A designation by the US FDA signifying that there is positive evidence of human fetal risk based on adverse reaction data from investigational or marketing experience or studies in humans, but potential benefits may warrant use of the drug in pregnant women despite potential risks. */
	'FDAcategoryD': { termType: 'NamedNode', value: 'https://schema.org/FDAcategoryD', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/FDAcategoryD' } as NamedNode,
	/** A designation by the US FDA signifying that studies in animals or humans have demonstrated fetal abnormalities and/or there is positive evidence of human fetal risk based on adverse reaction data from investigational or marketing experience, and the risks involved in use of the drug in pregnant women clearly outweigh potential benefits. */
	'FDAcategoryX': { termType: 'NamedNode', value: 'https://schema.org/FDAcategoryX', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/FDAcategoryX' } as NamedNode,
	/** A designation that the drug in question has not been assigned a pregnancy category designation by the US FDA. */
	'FDAnotEvaluated': { termType: 'NamedNode', value: 'https://schema.org/FDAnotEvaluated', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/FDAnotEvaluated' } as NamedNode,
	/** A radio channel that uses FM. */
	'FMRadioChannel': { termType: 'NamedNode', value: 'https://schema.org/FMRadioChannel', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/FMRadioChannel' } as NamedNode,
	/** An action that failed to complete. The action's error property and the HTTP return code contain more information about the failure. */
	'FailedActionStatus': { termType: 'NamedNode', value: 'https://schema.org/FailedActionStatus', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/FailedActionStatus' } as NamedNode,
	/** The boolean value false. */
	'False': { termType: 'NamedNode', value: 'https://schema.org/False', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/False' } as NamedNode,
	/** A fast-food restaurant. */
	'FastFoodRestaurant': { termType: 'NamedNode', value: 'https://schema.org/FastFoodRestaurant', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/FastFoodRestaurant' } as NamedNode,
	/** The female gender. */
	'Female': { termType: 'NamedNode', value: 'https://schema.org/Female', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Female' } as NamedNode,
	/** Event type: Festival. */
	'Festival': { termType: 'NamedNode', value: 'https://schema.org/Festival', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Festival' } as NamedNode,
	/** The act of capturing sound and moving images on film, video, or digitally. */
	'FilmAction': { termType: 'NamedNode', value: 'https://schema.org/FilmAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/FilmAction' } as NamedNode,
	/** A product provided to consumers and businesses by financial institutions such as banks, insurance companies, brokerage firms, consumer finance companies, and investment companies which comprise the financial services industry. */
	'FinancialProduct': { termType: 'NamedNode', value: 'https://schema.org/FinancialProduct', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/FinancialProduct' } as NamedNode,
	/** Financial services business. */
	'FinancialService': { termType: 'NamedNode', value: 'https://schema.org/FinancialService', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/FinancialService' } as NamedNode,
	/** The act of finding an object.\n\nRelated actions:\n\n* [[SearchAction]]: FindAction is generally lead by a SearchAction, but not necessarily. */
	'FindAction': { termType: 'NamedNode', value: 'https://schema.org/FindAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/FindAction' } as NamedNode,
	/** A fire station. With firemen. */
	'FireStation': { termType: 'NamedNode', value: 'https://schema.org/FireStation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/FireStation' } as NamedNode,
	/** Physical activity that is engaged in to improve joint and muscle flexibility. */
	'Flexibility': { termType: 'NamedNode', value: 'https://schema.org/Flexibility', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Flexibility' } as NamedNode,
	/** An airline flight. */
	'Flight': { termType: 'NamedNode', value: 'https://schema.org/Flight', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Flight' } as NamedNode,
	/** A reservation for air travel.\n\nNote: This type is for information about actual reservations, e.g. in confirmation emails or HTML pages with individual confirmations of reservations. For offers of tickets, use [[Offer]]. */
	'FlightReservation': { termType: 'NamedNode', value: 'https://schema.org/FlightReservation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/FlightReservation' } as NamedNode,
	/** Data type: Floating number. */
	'Float': { termType: 'NamedNode', value: 'https://schema.org/Float', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Float' } as NamedNode,
	/** A FloorPlan is an explicit representation of a collection of similar accommodations, allowing the provision of common information (room counts, sizes, layout diagrams) and offers for rental or sale. In typical use, some [[ApartmentComplex]] has an [[accommodationFloorPlan]] which is a [[FloorPlan]].  A FloorPlan is always in the context of a particular place, either a larger [[ApartmentComplex]] or a single [[Apartment]]. The visual/spatial aspects of a floor plan (i.e. room layout, [see wikipedia](https://en.wikipedia.org/wiki/Floor_plan)) can be indicated using [[image]].  */
	'FloorPlan': { termType: 'NamedNode', value: 'https://schema.org/FloorPlan', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/FloorPlan' } as NamedNode,
	/** A florist. */
	'Florist': { termType: 'NamedNode', value: 'https://schema.org/Florist', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Florist' } as NamedNode,
	/** The act of forming a personal connection with someone/something (object) unidirectionally/asymmetrically to get updates polled from.\n\nRelated actions:\n\n* [[BefriendAction]]: Unlike BefriendAction, FollowAction implies that the connection is *not* necessarily reciprocal.\n* [[SubscribeAction]]: Unlike SubscribeAction, FollowAction implies that the follower acts as an active agent constantly/actively polling for updates.\n* [[RegisterAction]]: Unlike RegisterAction, FollowAction implies that the agent is interested in continuing receiving updates from the object.\n* [[JoinAction]]: Unlike JoinAction, FollowAction implies that the agent is interested in getting updates from the object.\n* [[TrackAction]]: Unlike TrackAction, FollowAction refers to the polling of updates of all aspects of animate objects rather than the location of inanimate objects (e.g. you track a package, but you don't follow it). */
	'FollowAction': { termType: 'NamedNode', value: 'https://schema.org/FollowAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/FollowAction' } as NamedNode,
	/** A food-related business. */
	'FoodEstablishment': { termType: 'NamedNode', value: 'https://schema.org/FoodEstablishment', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/FoodEstablishment' } as NamedNode,
	/** A reservation to dine at a food-related business.\n\nNote: This type is for information about actual reservations, e.g. in confirmation emails or HTML pages with individual confirmations of reservations. */
	'FoodEstablishmentReservation': { termType: 'NamedNode', value: 'https://schema.org/FoodEstablishmentReservation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/FoodEstablishmentReservation' } as NamedNode,
	/** Event type: Food event. */
	'FoodEvent': { termType: 'NamedNode', value: 'https://schema.org/FoodEvent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/FoodEvent' } as NamedNode,
	/** A food service, like breakfast, lunch, or dinner. */
	'FoodService': { termType: 'NamedNode', value: 'https://schema.org/FoodService', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/FoodService' } as NamedNode,
	/** Four-wheel drive is a transmission layout where the engine primarily drives two wheels with a part-time four-wheel drive capability. */
	'FourWheelDriveConfiguration': { termType: 'NamedNode', value: 'https://schema.org/FourWheelDriveConfiguration', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/FourWheelDriveConfiguration' } as NamedNode,
	/** Specifies that product returns are free of charge for the customer. */
	'FreeReturn': { termType: 'NamedNode', value: 'https://schema.org/FreeReturn', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/FreeReturn' } as NamedNode,
	/** The day of the week between Thursday and Saturday. */
	'Friday': { termType: 'NamedNode', value: 'https://schema.org/Friday', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Friday' } as NamedNode,
	/** Front-wheel drive is a transmission layout where the engine drives the front wheels. */
	'FrontWheelDriveConfiguration': { termType: 'NamedNode', value: 'https://schema.org/FrontWheelDriveConfiguration', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/FrontWheelDriveConfiguration' } as NamedNode,
	/** Indicates full game availability. */
	'FullGameAvailability': { termType: 'NamedNode', value: 'https://schema.org/FullGameAvailability', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/FullGameAvailability' } as NamedNode,
	/** Specifies that a refund can be done in the full amount the customer paid for the product. */
	'FullRefund': { termType: 'NamedNode', value: 'https://schema.org/FullRefund', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/FullRefund' } as NamedNode,
	/** A FundingAgency is an organization that implements one or more [[FundingScheme]]s and manages
    the granting process (via [[Grant]]s, typically [[MonetaryGrant]]s).
    A funding agency is not always required for grant funding, e.g. philanthropic giving, corporate sponsorship etc.
    
Examples of funding agencies include ERC, REA, NIH, Bill and Melinda Gates Foundation, ...
     */
	'FundingAgency': { termType: 'NamedNode', value: 'https://schema.org/FundingAgency', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/FundingAgency' } as NamedNode,
	/** A FundingScheme combines organizational, project and policy aspects of grant-based funding
    that sets guidelines, principles and mechanisms to support other kinds of projects and activities.
    Funding is typically organized via [[Grant]] funding. Examples of funding schemes: Swiss Priority Programmes (SPPs); EU Framework 7 (FP7); Horizon 2020; the NIH-R01 Grant Program; Wellcome institutional strategic support fund. For large scale public sector funding, the management and administration of grant awards is often handled by other, dedicated, organizations - [[FundingAgency]]s such as ERC, REA, ... */
	'FundingScheme': { termType: 'NamedNode', value: 'https://schema.org/FundingScheme', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/FundingScheme' } as NamedNode,
	/** Pathogenic fungus. */
	'Fungus': { termType: 'NamedNode', value: 'https://schema.org/Fungus', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Fungus' } as NamedNode,
	/** A furniture store. */
	'FurnitureStore': { termType: 'NamedNode', value: 'https://schema.org/FurnitureStore', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/FurnitureStore' } as NamedNode,
	/** The Game type represents things which are games. These are typically rule-governed recreational activities, e.g. role-playing games in which players assume the role of characters in a fictional setting. */
	'Game': { termType: 'NamedNode', value: 'https://schema.org/Game', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Game' } as NamedNode,
	/** For a [[VideoGame]], such as used with a [[PlayGameAction]], an enumeration of the kind of game availability offered.  */
	'GameAvailabilityEnumeration': { termType: 'NamedNode', value: 'https://schema.org/GameAvailabilityEnumeration', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/GameAvailabilityEnumeration' } as NamedNode,
	/** Indicates whether this game is multi-player, co-op or single-player. */
	'GamePlayMode': { termType: 'NamedNode', value: 'https://schema.org/GamePlayMode', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/GamePlayMode' } as NamedNode,
	/** Server that provides game interaction in a multiplayer game. */
	'GameServer': { termType: 'NamedNode', value: 'https://schema.org/GameServer', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/GameServer' } as NamedNode,
	/** Status of a game server. */
	'GameServerStatus': { termType: 'NamedNode', value: 'https://schema.org/GameServerStatus', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/GameServerStatus' } as NamedNode,
	/** A garden store. */
	'GardenStore': { termType: 'NamedNode', value: 'https://schema.org/GardenStore', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/GardenStore' } as NamedNode,
	/** A gas station. */
	'GasStation': { termType: 'NamedNode', value: 'https://schema.org/GasStation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/GasStation' } as NamedNode,
	/** A specific branch of medical science that pertains to diagnosis and treatment of disorders of digestive system. */
	'Gastroenterologic': { termType: 'NamedNode', value: 'https://schema.org/Gastroenterologic', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Gastroenterologic' } as NamedNode,
	/** Residence type: Gated community. */
	'GatedResidenceCommunity': { termType: 'NamedNode', value: 'https://schema.org/GatedResidenceCommunity', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/GatedResidenceCommunity' } as NamedNode,
	/** An enumeration of genders. */
	'GenderType': { termType: 'NamedNode', value: 'https://schema.org/GenderType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/GenderType' } as NamedNode,
	/** A discrete unit of inheritance which affects one or more biological traits (Source: [https://en.wikipedia.org/wiki/Gene](https://en.wikipedia.org/wiki/Gene)). Examples include FOXP2 (Forkhead box protein P2), SCARNA21 (small Cajal body-specific RNA 21), A- (agouti genotype). */
	'Gene': { termType: 'NamedNode', value: 'https://schema.org/Gene', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Gene' } as NamedNode,
	/** A general contractor. */
	'GeneralContractor': { termType: 'NamedNode', value: 'https://schema.org/GeneralContractor', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/GeneralContractor' } as NamedNode,
	/** Represents the generic notion of the Web Platform. More specific codes include [[MobileWebPlatform]] and [[DesktopWebPlatform]], as an incomplete list.  */
	'GenericWebPlatform': { termType: 'NamedNode', value: 'https://schema.org/GenericWebPlatform', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/GenericWebPlatform' } as NamedNode,
	/** A specific branch of medical science that pertains to hereditary transmission and the variation of inherited characteristics and disorders. */
	'Genetic': { termType: 'NamedNode', value: 'https://schema.org/Genetic', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Genetic' } as NamedNode,
	/** Genitourinary system function assessment with clinical examination. */
	'Genitourinary': { termType: 'NamedNode', value: 'https://schema.org/Genitourinary', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Genitourinary' } as NamedNode,
	/** A GeoCircle is a GeoShape representing a circular geographic area. As it is a GeoShape
          it provides the simple textual property 'circle', but also allows the combination of postalCode alongside geoRadius.
          The center of the circle can be indicated via the 'geoMidpoint' property, or more approximately using 'address', 'postalCode'.
        */
	'GeoCircle': { termType: 'NamedNode', value: 'https://schema.org/GeoCircle', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/GeoCircle' } as NamedNode,
	/** The geographic coordinates of a place or event. */
	'GeoCoordinates': { termType: 'NamedNode', value: 'https://schema.org/GeoCoordinates', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/GeoCoordinates' } as NamedNode,
	/** The geographic shape of a place. A GeoShape can be described using several properties whose values are based on latitude/longitude pairs. Either whitespace or commas can be used to separate latitude and longitude; whitespace should be used when writing a list of several such points. */
	'GeoShape': { termType: 'NamedNode', value: 'https://schema.org/GeoShape', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/GeoShape' } as NamedNode,
	/** (Eventually to be defined as) a supertype of GeoShape designed to accommodate definitions from Geo-Spatial best practices. */
	'GeospatialGeometry': { termType: 'NamedNode', value: 'https://schema.org/GeospatialGeometry', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/GeospatialGeometry' } as NamedNode,
	/** A specific branch of medical science that is concerned with the diagnosis and treatment of diseases, debilities and provision of care to the aged. */
	'Geriatric': { termType: 'NamedNode', value: 'https://schema.org/Geriatric', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Geriatric' } as NamedNode,
	/** Content that discusses practical and policy aspects for getting access to specific kinds of healthcare (e.g. distribution mechanisms for vaccines). */
	'GettingAccessHealthAspect': { termType: 'NamedNode', value: 'https://schema.org/GettingAccessHealthAspect', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/GettingAccessHealthAspect' } as NamedNode,
	/** The act of transferring ownership of an object to a destination. Reciprocal of TakeAction.\n\nRelated actions:\n\n* [[TakeAction]]: Reciprocal of GiveAction.\n* [[SendAction]]: Unlike SendAction, GiveAction implies that ownership is being transferred (e.g. I may send my laptop to you, but that doesn't mean I'm giving it to you). */
	'GiveAction': { termType: 'NamedNode', value: 'https://schema.org/GiveAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/GiveAction' } as NamedNode,
	/** A diet exclusive of gluten. */
	'GlutenFreeDiet': { termType: 'NamedNode', value: 'https://schema.org/GlutenFreeDiet', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/GlutenFreeDiet' } as NamedNode,
	/** A golf course. */
	'GolfCourse': { termType: 'NamedNode', value: 'https://schema.org/GolfCourse', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/GolfCourse' } as NamedNode,
	/** GovernmentBenefitsType enumerates several kinds of government benefits to support the COVID-19 situation. Note that this structure may not capture all benefits offered. */
	'GovernmentBenefitsType': { termType: 'NamedNode', value: 'https://schema.org/GovernmentBenefitsType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/GovernmentBenefitsType' } as NamedNode,
	/** A government building. */
	'GovernmentBuilding': { termType: 'NamedNode', value: 'https://schema.org/GovernmentBuilding', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/GovernmentBuilding' } as NamedNode,
	/** A government office&#x2014;for example, an IRS or DMV office. */
	'GovernmentOffice': { termType: 'NamedNode', value: 'https://schema.org/GovernmentOffice', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/GovernmentOffice' } as NamedNode,
	/** A governmental organization or agency. */
	'GovernmentOrganization': { termType: 'NamedNode', value: 'https://schema.org/GovernmentOrganization', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/GovernmentOrganization' } as NamedNode,
	/** A permit issued by a government agency. */
	'GovernmentPermit': { termType: 'NamedNode', value: 'https://schema.org/GovernmentPermit', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/GovernmentPermit' } as NamedNode,
	/** A service provided by a government organization, e.g. food stamps, veterans benefits, etc. */
	'GovernmentService': { termType: 'NamedNode', value: 'https://schema.org/GovernmentService', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/GovernmentService' } as NamedNode,
	/** A grant, typically financial or otherwise quantifiable, of resources. Typically a [[funder]] sponsors some [[MonetaryAmount]] to an [[Organization]] or [[Person]],
    sometimes not necessarily via a dedicated or long-lived [[Project]], resulting in one or more outputs, or [[fundedItem]]s. For financial sponsorship, indicate the [[funder]] of a [[MonetaryGrant]]. For non-financial support, indicate [[sponsor]] of [[Grant]]s of resources (e.g. office space).

Grants support  activities directed towards some agreed collective goals, often but not always organized as [[Project]]s. Long-lived projects are sometimes sponsored by a variety of grants over time, but it is also common for a project to be associated with a single grant.

The amount of a [[Grant]] is represented using [[amount]] as a [[MonetaryAmount]].
     */
	'Grant': { termType: 'NamedNode', value: 'https://schema.org/Grant', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Grant' } as NamedNode,
	/** Book format: GraphicNovel. May represent a bound collection of ComicIssue instances. */
	'GraphicNovel': { termType: 'NamedNode', value: 'https://schema.org/GraphicNovel', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/GraphicNovel' } as NamedNode,
	/** A grocery store. */
	'GroceryStore': { termType: 'NamedNode', value: 'https://schema.org/GroceryStore', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/GroceryStore' } as NamedNode,
	/** The airline boards by groups based on check-in time, priority, etc. */
	'GroupBoardingPolicy': { termType: 'NamedNode', value: 'https://schema.org/GroupBoardingPolicy', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/GroupBoardingPolicy' } as NamedNode,
	/** [[Guide]] is a page or article that recommends specific products or services, or aspects of a thing for a user to consider. A [[Guide]] may represent a Buying Guide and detail aspects of products or services for a user to consider. A [[Guide]] may represent a Product Guide and recommend specific products or services. A [[Guide]] may represent a Ranked List and recommend specific products or services with ranking. */
	'Guide': { termType: 'NamedNode', value: 'https://schema.org/Guide', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Guide' } as NamedNode,
	/** A specific branch of medical science that pertains to the health care of women, particularly in the diagnosis and treatment of disorders affecting the female reproductive system. */
	'Gynecologic': { termType: 'NamedNode', value: 'https://schema.org/Gynecologic', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Gynecologic' } as NamedNode,
	/** A business that provides Heating, Ventilation and Air Conditioning services. */
	'HVACBusiness': { termType: 'NamedNode', value: 'https://schema.org/HVACBusiness', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/HVACBusiness' } as NamedNode,
	/** A [hackathon](https://en.wikipedia.org/wiki/Hackathon) event. */
	'Hackathon': { termType: 'NamedNode', value: 'https://schema.org/Hackathon', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Hackathon' } as NamedNode,
	/** A hair salon. */
	'HairSalon': { termType: 'NamedNode', value: 'https://schema.org/HairSalon', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/HairSalon' } as NamedNode,
	/** A diet conforming to Islamic dietary practices. */
	'HalalDiet': { termType: 'NamedNode', value: 'https://schema.org/HalalDiet', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/HalalDiet' } as NamedNode,
	/** Book format: Hardcover. */
	'Hardcover': { termType: 'NamedNode', value: 'https://schema.org/Hardcover', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Hardcover' } as NamedNode,
	/** A hardware store. */
	'HardwareStore': { termType: 'NamedNode', value: 'https://schema.org/HardwareStore', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/HardwareStore' } as NamedNode,
	/** Head assessment with clinical examination. */
	'Head': { termType: 'NamedNode', value: 'https://schema.org/Head', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Head' } as NamedNode,
	/** Health and beauty. */
	'HealthAndBeautyBusiness': { termType: 'NamedNode', value: 'https://schema.org/HealthAndBeautyBusiness', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/HealthAndBeautyBusiness' } as NamedNode,
	/** HealthAspectEnumeration enumerates several aspects of health content online, each of which might be described using [[hasHealthAspect]] and [[HealthTopicContent]]. */
	'HealthAspectEnumeration': { termType: 'NamedNode', value: 'https://schema.org/HealthAspectEnumeration', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/HealthAspectEnumeration' } as NamedNode,
	/** HealthCare: this is a benefit for health care. */
	'HealthCare': { termType: 'NamedNode', value: 'https://schema.org/HealthCare', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/HealthCare' } as NamedNode,
	/** A health club. */
	'HealthClub': { termType: 'NamedNode', value: 'https://schema.org/HealthClub', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/HealthClub' } as NamedNode,
	/** A US-style health insurance plan, including PPOs, EPOs, and HMOs.  */
	'HealthInsurancePlan': { termType: 'NamedNode', value: 'https://schema.org/HealthInsurancePlan', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/HealthInsurancePlan' } as NamedNode,
	/** A description of costs to the patient under a given network or formulary. */
	'HealthPlanCostSharingSpecification': { termType: 'NamedNode', value: 'https://schema.org/HealthPlanCostSharingSpecification', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/HealthPlanCostSharingSpecification' } as NamedNode,
	/** For a given health insurance plan, the specification for costs and coverage of prescription drugs.  */
	'HealthPlanFormulary': { termType: 'NamedNode', value: 'https://schema.org/HealthPlanFormulary', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/HealthPlanFormulary' } as NamedNode,
	/** A US-style health insurance plan network.  */
	'HealthPlanNetwork': { termType: 'NamedNode', value: 'https://schema.org/HealthPlanNetwork', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/HealthPlanNetwork' } as NamedNode,
	/** [[HealthTopicContent]] is [[WebContent]] that is about some aspect of a health topic, e.g. a condition, its symptoms or treatments. Such content may be comprised of several parts or sections and use different types of media. Multiple instances of [[WebContent]] (and hence [[HealthTopicContent]]) can be related using [[hasPart]] / [[isPartOf]] where there is some kind of content hierarchy, and their content described with [[about]] and [[mentions]] e.g. building upon the existing [[MedicalCondition]] vocabulary.
   */
	'HealthTopicContent': { termType: 'NamedNode', value: 'https://schema.org/HealthTopicContent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/HealthTopicContent' } as NamedNode,
	/** Item is a pharmaceutical (e.g., a prescription or OTC drug) or a restricted medical device. */
	'HealthcareConsideration': { termType: 'NamedNode', value: 'https://schema.org/HealthcareConsideration', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/HealthcareConsideration' } as NamedNode,
	/** Uses devices to support users with hearing impairments. */
	'HearingImpairedSupported': { termType: 'NamedNode', value: 'https://schema.org/HearingImpairedSupported', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/HearingImpairedSupported' } as NamedNode,
	/** A specific branch of medical science that pertains to diagnosis and treatment of disorders of blood and blood producing organs. */
	'Hematologic': { termType: 'NamedNode', value: 'https://schema.org/Hematologic', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Hematologic' } as NamedNode,
	/** A high school. */
	'HighSchool': { termType: 'NamedNode', value: 'https://schema.org/HighSchool', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/HighSchool' } as NamedNode,
	/** A diet conforming to Hindu dietary practices, in particular, beef-free. */
	'HinduDiet': { termType: 'NamedNode', value: 'https://schema.org/HinduDiet', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/HinduDiet' } as NamedNode,
	/** A Hindu temple. */
	'HinduTemple': { termType: 'NamedNode', value: 'https://schema.org/HinduTemple', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/HinduTemple' } as NamedNode,
	/** A store that sells materials useful or necessary for various hobbies. */
	'HobbyShop': { termType: 'NamedNode', value: 'https://schema.org/HobbyShop', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/HobbyShop' } as NamedNode,
	/** A construction business.\n\nA HomeAndConstructionBusiness is a [[LocalBusiness]] that provides services around homes and buildings.\n\nAs a [[LocalBusiness]] it can be described as a [[provider]] of one or more [[Service]]\(s). */
	'HomeAndConstructionBusiness': { termType: 'NamedNode', value: 'https://schema.org/HomeAndConstructionBusiness', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/HomeAndConstructionBusiness' } as NamedNode,
	/** A home goods store. */
	'HomeGoodsStore': { termType: 'NamedNode', value: 'https://schema.org/HomeGoodsStore', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/HomeGoodsStore' } as NamedNode,
	/** A system of medicine based on the principle that a disease can be cured by a substance that produces similar symptoms in healthy people. */
	'Homeopathic': { termType: 'NamedNode', value: 'https://schema.org/Homeopathic', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Homeopathic' } as NamedNode,
	/** A hospital. */
	'Hospital': { termType: 'NamedNode', value: 'https://schema.org/Hospital', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Hospital' } as NamedNode,
	/** A hostel - cheap accommodation, often in shared dormitories.
<br /><br />
See also the <a href="/docs/hotels.html">dedicated document on the use of schema.org for marking up hotels and other forms of accommodations</a>.
 */
	'Hostel': { termType: 'NamedNode', value: 'https://schema.org/Hostel', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Hostel' } as NamedNode,
	/** A hotel is an establishment that provides lodging paid on a short-term basis (source: Wikipedia, the free encyclopedia, see http://en.wikipedia.org/wiki/Hotel).
<br /><br />
See also the <a href="/docs/hotels.html">dedicated document on the use of schema.org for marking up hotels and other forms of accommodations</a>.
 */
	'Hotel': { termType: 'NamedNode', value: 'https://schema.org/Hotel', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Hotel' } as NamedNode,
	/** A hotel room is a single room in a hotel.
<br /><br />
See also the <a href="/docs/hotels.html">dedicated document on the use of schema.org for marking up hotels and other forms of accommodations</a>.
 */
	'HotelRoom': { termType: 'NamedNode', value: 'https://schema.org/HotelRoom', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/HotelRoom' } as NamedNode,
	/** A house is a building or structure that has the ability to be occupied for habitation by humans or other creatures (source: Wikipedia, the free encyclopedia, see <a href="http://en.wikipedia.org/wiki/House">http://en.wikipedia.org/wiki/House</a>). */
	'House': { termType: 'NamedNode', value: 'https://schema.org/House', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/House' } as NamedNode,
	/** A house painting service. */
	'HousePainter': { termType: 'NamedNode', value: 'https://schema.org/HousePainter', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/HousePainter' } as NamedNode,
	/** Content that discusses and explains how a particular health-related topic works, e.g. in terms of mechanisms and underlying science. */
	'HowItWorksHealthAspect': { termType: 'NamedNode', value: 'https://schema.org/HowItWorksHealthAspect', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/HowItWorksHealthAspect' } as NamedNode,
	/** Information about how or where to find a topic. Also may contain location data that can be used for where to look for help if the topic is observed. */
	'HowOrWhereHealthAspect': { termType: 'NamedNode', value: 'https://schema.org/HowOrWhereHealthAspect', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/HowOrWhereHealthAspect' } as NamedNode,
	/** Instructions that explain how to achieve a result by performing a sequence of steps. */
	'HowTo': { termType: 'NamedNode', value: 'https://schema.org/HowTo', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/HowTo' } as NamedNode,
	/** A direction indicating a single action to do in the instructions for how to achieve a result. */
	'HowToDirection': { termType: 'NamedNode', value: 'https://schema.org/HowToDirection', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/HowToDirection' } as NamedNode,
	/** An item used as either a tool or supply when performing the instructions for how to achieve a result. */
	'HowToItem': { termType: 'NamedNode', value: 'https://schema.org/HowToItem', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/HowToItem' } as NamedNode,
	/** A sub-grouping of steps in the instructions for how to achieve a result (e.g. steps for making a pie crust within a pie recipe). */
	'HowToSection': { termType: 'NamedNode', value: 'https://schema.org/HowToSection', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/HowToSection' } as NamedNode,
	/** A step in the instructions for how to achieve a result. It is an ordered list with HowToDirection and/or HowToTip items. */
	'HowToStep': { termType: 'NamedNode', value: 'https://schema.org/HowToStep', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/HowToStep' } as NamedNode,
	/** A supply consumed when performing the instructions for how to achieve a result. */
	'HowToSupply': { termType: 'NamedNode', value: 'https://schema.org/HowToSupply', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/HowToSupply' } as NamedNode,
	/** An explanation in the instructions for how to achieve a result. It provides supplementary information about a technique, supply, author's preference, etc. It can explain what could be done, or what should not be done, but doesn't specify what should be done (see HowToDirection). */
	'HowToTip': { termType: 'NamedNode', value: 'https://schema.org/HowToTip', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/HowToTip' } as NamedNode,
	/** A tool used (but not consumed) when performing instructions for how to achieve a result. */
	'HowToTool': { termType: 'NamedNode', value: 'https://schema.org/HowToTool', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/HowToTool' } as NamedNode,
	/** A HyperToc represents a hypertext table of contents for complex media objects, such as [[VideoObject]], [[AudioObject]]. Items in the table of contents are indicated using the [[tocEntry]] property, and typed [[HyperTocEntry]]. For cases where the same larger work is split into multiple files, [[associatedMedia]] can be used on individual [[HyperTocEntry]] items. */
	'HyperToc': { termType: 'NamedNode', value: 'https://schema.org/HyperToc', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/HyperToc' } as NamedNode,
	/** A HyperToEntry is an item within a [[HyperToc]], which represents a hypertext table of contents for complex media objects, such as [[VideoObject]], [[AudioObject]]. The media object itself is indicated using [[associatedMedia]]. Each section of interest within that content can be described with a [[HyperTocEntry]], with associated [[startOffset]] and [[endOffset]]. When several entries are all from the same file, [[associatedMedia]] is used on the overarching [[HyperTocEntry]]; if the content has been split into multiple files, they can be referenced using [[associatedMedia]] on each [[HyperTocEntry]]. */
	'HyperTocEntry': { termType: 'NamedNode', value: 'https://schema.org/HyperTocEntry', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/HyperTocEntry' } as NamedNode,
	/** Represents the broad notion of iOS-based operating systems. */
	'IOSPlatform': { termType: 'NamedNode', value: 'https://schema.org/IOSPlatform', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/IOSPlatform' } as NamedNode,
	/** An ice cream shop. */
	'IceCreamShop': { termType: 'NamedNode', value: 'https://schema.org/IceCreamShop', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/IceCreamShop' } as NamedNode,
	/** The act of intentionally disregarding the object. An agent ignores an object. */
	'IgnoreAction': { termType: 'NamedNode', value: 'https://schema.org/IgnoreAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/IgnoreAction' } as NamedNode,
	/** Web page type: Image gallery page. */
	'ImageGallery': { termType: 'NamedNode', value: 'https://schema.org/ImageGallery', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ImageGallery' } as NamedNode,
	/** An image file. */
	'ImageObject': { termType: 'NamedNode', value: 'https://schema.org/ImageObject', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ImageObject' } as NamedNode,
	/** A specific and exact (byte-for-byte) version of an [[ImageObject]]. Two byte-for-byte identical files, for the purposes of this type, considered identical. If they have different embedded metadata (e.g. XMP, EXIF) the files will differ. Different external facts about the files, e.g. creator or dateCreated that aren't represented in their actual content, do not affect this notion of identity. */
	'ImageObjectSnapshot': { termType: 'NamedNode', value: 'https://schema.org/ImageObjectSnapshot', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ImageObjectSnapshot' } as NamedNode,
	/** Any medical imaging modality typically used for diagnostic purposes. */
	'ImagingTest': { termType: 'NamedNode', value: 'https://schema.org/ImagingTest', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ImagingTest' } as NamedNode,
	/** Indicates that a legislation is in force. */
	'InForce': { termType: 'NamedNode', value: 'https://schema.org/InForce', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/InForce' } as NamedNode,
	/** Indicates that the item is in stock. */
	'InStock': { termType: 'NamedNode', value: 'https://schema.org/InStock', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/InStock' } as NamedNode,
	/** Indicates that the item is available only at physical locations. */
	'InStoreOnly': { termType: 'NamedNode', value: 'https://schema.org/InStoreOnly', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/InStoreOnly' } as NamedNode,
	/** A single, identifiable product instance (e.g. a laptop with a particular serial number). */
	'IndividualProduct': { termType: 'NamedNode', value: 'https://schema.org/IndividualProduct', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/IndividualProduct' } as NamedNode,
	/** Something in medical science that pertains to infectious diseases, i.e. caused by bacterial, viral, fungal or parasitic infections. */
	'Infectious': { termType: 'NamedNode', value: 'https://schema.org/Infectious', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Infectious' } as NamedNode,
	/** Classes of agents or pathogens that transmit infectious diseases. Enumerated type. */
	'InfectiousAgentClass': { termType: 'NamedNode', value: 'https://schema.org/InfectiousAgentClass', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/InfectiousAgentClass' } as NamedNode,
	/** An infectious disease is a clinically evident human disease resulting from the presence of pathogenic microbial agents, like pathogenic viruses, pathogenic bacteria, fungi, protozoa, multicellular parasites, and prions. To be considered an infectious disease, such pathogens are known to be able to cause this disease. */
	'InfectiousDisease': { termType: 'NamedNode', value: 'https://schema.org/InfectiousDisease', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/InfectiousDisease' } as NamedNode,
	/** The act of notifying someone of information pertinent to them, with no expectation of a response. */
	'InformAction': { termType: 'NamedNode', value: 'https://schema.org/InformAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/InformAction' } as NamedNode,
	/** Content discussing ingredients-related aspects of a health topic. */
	'IngredientsHealthAspect': { termType: 'NamedNode', value: 'https://schema.org/IngredientsHealthAspect', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/IngredientsHealthAspect' } as NamedNode,
	/** The act of adding at a specific location in an ordered collection. */
	'InsertAction': { termType: 'NamedNode', value: 'https://schema.org/InsertAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/InsertAction' } as NamedNode,
	/** The act of installing an application. */
	'InstallAction': { termType: 'NamedNode', value: 'https://schema.org/InstallAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/InstallAction' } as NamedNode,
	/** Represents the installment pricing component of the total price for an offered product. */
	'Installment': { termType: 'NamedNode', value: 'https://schema.org/Installment', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Installment' } as NamedNode,
	/** An Insurance agency. */
	'InsuranceAgency': { termType: 'NamedNode', value: 'https://schema.org/InsuranceAgency', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/InsuranceAgency' } as NamedNode,
	/** A utility class that serves as the umbrella for a number of 'intangible' things such as quantities, structured values, etc. */
	'Intangible': { termType: 'NamedNode', value: 'https://schema.org/Intangible', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Intangible' } as NamedNode,
	/** Data type: Integer. */
	'Integer': { termType: 'NamedNode', value: 'https://schema.org/Integer', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Integer' } as NamedNode,
	/** The act of interacting with another person or organization. */
	'InteractAction': { termType: 'NamedNode', value: 'https://schema.org/InteractAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/InteractAction' } as NamedNode,
	/** A summary of how users have interacted with this CreativeWork. In most cases, authors will use a subtype to specify the specific type of interaction. */
	'InteractionCounter': { termType: 'NamedNode', value: 'https://schema.org/InteractionCounter', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/InteractionCounter' } as NamedNode,
	/** An international trial. */
	'InternationalTrial': { termType: 'NamedNode', value: 'https://schema.org/InternationalTrial', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/InternationalTrial' } as NamedNode,
	/** An internet cafe. */
	'InternetCafe': { termType: 'NamedNode', value: 'https://schema.org/InternetCafe', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/InternetCafe' } as NamedNode,
	/** A company or fund that gathers capital from a number of investors to create a pool of money that is then re-invested into stocks, bonds and other assets. */
	'InvestmentFund': { termType: 'NamedNode', value: 'https://schema.org/InvestmentFund', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/InvestmentFund' } as NamedNode,
	/** A type of financial product that typically requires the client to transfer funds to a financial service in return for potential beneficial financial return. */
	'InvestmentOrDeposit': { termType: 'NamedNode', value: 'https://schema.org/InvestmentOrDeposit', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/InvestmentOrDeposit' } as NamedNode,
	/** The act of asking someone to attend an event. Reciprocal of RsvpAction. */
	'InviteAction': { termType: 'NamedNode', value: 'https://schema.org/InviteAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/InviteAction' } as NamedNode,
	/** A statement of the money due for goods or services; a bill. */
	'Invoice': { termType: 'NamedNode', value: 'https://schema.org/Invoice', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Invoice' } as NamedNode,
	/** Represents the invoice price of an offered product. */
	'InvoicePrice': { termType: 'NamedNode', value: 'https://schema.org/InvoicePrice', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/InvoicePrice' } as NamedNode,
	/** A list of possible product availability options. */
	'ItemAvailability': { termType: 'NamedNode', value: 'https://schema.org/ItemAvailability', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ItemAvailability' } as NamedNode,
	/** A list of items of any sort&#x2014;for example, Top 10 Movies About Weathermen, or Top 100 Party Songs. Not to be confused with HTML lists, which are often used only for formatting. */
	'ItemList': { termType: 'NamedNode', value: 'https://schema.org/ItemList', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ItemList' } as NamedNode,
	/** An ItemList ordered with lower values listed first. */
	'ItemListOrderAscending': { termType: 'NamedNode', value: 'https://schema.org/ItemListOrderAscending', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ItemListOrderAscending' } as NamedNode,
	/** An ItemList ordered with higher values listed first. */
	'ItemListOrderDescending': { termType: 'NamedNode', value: 'https://schema.org/ItemListOrderDescending', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ItemListOrderDescending' } as NamedNode,
	/** Enumerated for values for itemListOrder for indicating how an ordered ItemList is organized. */
	'ItemListOrderType': { termType: 'NamedNode', value: 'https://schema.org/ItemListOrderType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ItemListOrderType' } as NamedNode,
	/** An ItemList ordered with no explicit order. */
	'ItemListUnordered': { termType: 'NamedNode', value: 'https://schema.org/ItemListUnordered', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ItemListUnordered' } as NamedNode,
	/** A page devoted to a single item, such as a particular product or hotel. */
	'ItemPage': { termType: 'NamedNode', value: 'https://schema.org/ItemPage', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ItemPage' } as NamedNode,
	/** A jewelry store. */
	'JewelryStore': { termType: 'NamedNode', value: 'https://schema.org/JewelryStore', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/JewelryStore' } as NamedNode,
	/** A listing that describes a job opening in a certain organization. */
	'JobPosting': { termType: 'NamedNode', value: 'https://schema.org/JobPosting', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/JobPosting' } as NamedNode,
	/** An agent joins an event/group with participants/friends at a location.\n\nRelated actions:\n\n* [[RegisterAction]]: Unlike RegisterAction, JoinAction refers to joining a group/team of people.\n* [[SubscribeAction]]: Unlike SubscribeAction, JoinAction does not imply that you'll be receiving updates.\n* [[FollowAction]]: Unlike FollowAction, JoinAction does not imply that you'll be polling for updates. */
	'JoinAction': { termType: 'NamedNode', value: 'https://schema.org/JoinAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/JoinAction' } as NamedNode,
	/** The anatomical location at which two or more bones make contact. */
	'Joint': { termType: 'NamedNode', value: 'https://schema.org/Joint', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Joint' } as NamedNode,
	/** A diet conforming to Jewish dietary practices. */
	'KosherDiet': { termType: 'NamedNode', value: 'https://schema.org/KosherDiet', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/KosherDiet' } as NamedNode,
	/** A medical science pertaining to chemical, hematological, immunologic, microscopic, or bacteriological diagnostic analyses or research. */
	'LaboratoryScience': { termType: 'NamedNode', value: 'https://schema.org/LaboratoryScience', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/LaboratoryScience' } as NamedNode,
	/** A lake (for example, Lake Pontrachain). */
	'LakeBodyOfWater': { termType: 'NamedNode', value: 'https://schema.org/LakeBodyOfWater', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/LakeBodyOfWater' } as NamedNode,
	/** A landform or physical feature.  Landform elements include mountains, plains, lakes, rivers, seascape and oceanic waterbody interface features such as bays, peninsulas, seas and so forth, including sub-aqueous terrain features such as submersed mountain ranges, volcanoes, and the great ocean basins. */
	'Landform': { termType: 'NamedNode', value: 'https://schema.org/Landform', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Landform' } as NamedNode,
	/** An historical landmark or building. */
	'LandmarksOrHistoricalBuildings': { termType: 'NamedNode', value: 'https://schema.org/LandmarksOrHistoricalBuildings', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/LandmarksOrHistoricalBuildings' } as NamedNode,
	/** Natural languages such as Spanish, Tamil, Hindi, English, etc. Formal language code tags expressed in [BCP 47](https://en.wikipedia.org/wiki/IETF_language_tag) can be used via the [[alternateName]] property. The Language type previously also covered programming languages such as Scheme and Lisp, which are now best represented using [[ComputerLanguage]]. */
	'Language': { termType: 'NamedNode', value: 'https://schema.org/Language', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Language' } as NamedNode,
	/** LaserDiscFormat. */
	'LaserDiscFormat': { termType: 'NamedNode', value: 'https://schema.org/LaserDiscFormat', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/LaserDiscFormat' } as NamedNode,
	/** The LearningResource type can be used to indicate [[CreativeWork]]s (whether physical or digital) that have a particular and explicit orientation towards learning, education, skill acquisition, and other educational purposes.

[[LearningResource]] is expected to be used as an addition to a primary type such as [[Book]], [[VideoObject]], [[Product]] etc.

[[EducationEvent]] serves a similar purpose for event-like things (e.g. a [[Trip]]). A [[LearningResource]] may be created as a result of an [[EducationEvent]], for example by recording one. */
	'LearningResource': { termType: 'NamedNode', value: 'https://schema.org/LearningResource', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/LearningResource' } as NamedNode,
	/** An agent leaves an event / group with participants/friends at a location.\n\nRelated actions:\n\n* [[JoinAction]]: The antonym of LeaveAction.\n* [[UnRegisterAction]]: Unlike UnRegisterAction, LeaveAction implies leaving a group/team of people rather than a service. */
	'LeaveAction': { termType: 'NamedNode', value: 'https://schema.org/LeaveAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/LeaveAction' } as NamedNode,
	/** The steering position is on the left side of the vehicle (viewed from the main direction of driving). */
	'LeftHandDriving': { termType: 'NamedNode', value: 'https://schema.org/LeftHandDriving', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/LeftHandDriving' } as NamedNode,
	/** A list of possible statuses for the legal force of a legislation. */
	'LegalForceStatus': { termType: 'NamedNode', value: 'https://schema.org/LegalForceStatus', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/LegalForceStatus' } as NamedNode,
	/** A LegalService is a business that provides legally-oriented services, advice and representation, e.g. law firms.\n\nAs a [[LocalBusiness]] it can be described as a [[provider]] of one or more [[Service]]\(s). */
	'LegalService': { termType: 'NamedNode', value: 'https://schema.org/LegalService', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/LegalService' } as NamedNode,
	/** A list of possible levels for the legal validity of a legislation. */
	'LegalValueLevel': { termType: 'NamedNode', value: 'https://schema.org/LegalValueLevel', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/LegalValueLevel' } as NamedNode,
	/** A legal document such as an act, decree, bill, etc. (enforceable or not) or a component of a legal act (like an article). */
	'Legislation': { termType: 'NamedNode', value: 'https://schema.org/Legislation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Legislation' } as NamedNode,
	/** A specific object or file containing a Legislation. Note that the same Legislation can be published in multiple files. For example, a digitally signed PDF, a plain PDF and an HTML version. */
	'LegislationObject': { termType: 'NamedNode', value: 'https://schema.org/LegislationObject', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/LegislationObject' } as NamedNode,
	/** A legislative building&#x2014;for example, the state capitol. */
	'LegislativeBuilding': { termType: 'NamedNode', value: 'https://schema.org/LegislativeBuilding', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/LegislativeBuilding' } as NamedNode,
	/** Any physical activity engaged in for recreational purposes. Examples may include ballroom dancing, roller skating, canoeing, fishing, etc. */
	'LeisureTimeActivity': { termType: 'NamedNode', value: 'https://schema.org/LeisureTimeActivity', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/LeisureTimeActivity' } as NamedNode,
	/** The act of providing an object under an agreement that it will be returned at a later date. Reciprocal of BorrowAction.\n\nRelated actions:\n\n* [[BorrowAction]]: Reciprocal of LendAction. */
	'LendAction': { termType: 'NamedNode', value: 'https://schema.org/LendAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/LendAction' } as NamedNode,
	/** A library. */
	'Library': { termType: 'NamedNode', value: 'https://schema.org/Library', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Library' } as NamedNode,
	/** A [[LibrarySystem]] is a collaborative system amongst several libraries. */
	'LibrarySystem': { termType: 'NamedNode', value: 'https://schema.org/LibrarySystem', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/LibrarySystem' } as NamedNode,
	/** A process of care involving exercise, changes to diet, fitness routines, and other lifestyle changes aimed at improving a health condition. */
	'LifestyleModification': { termType: 'NamedNode', value: 'https://schema.org/LifestyleModification', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/LifestyleModification' } as NamedNode,
	/** A short band of tough, flexible, fibrous connective tissue that functions to connect multiple bones, cartilages, and structurally support joints. */
	'Ligament': { termType: 'NamedNode', value: 'https://schema.org/Ligament', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Ligament' } as NamedNode,
	/** The act of expressing a positive sentiment about the object. An agent likes an object (a proposition, topic or theme) with participants. */
	'LikeAction': { termType: 'NamedNode', value: 'https://schema.org/LikeAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/LikeAction' } as NamedNode,
	/** Indicates that the item has limited availability. */
	'LimitedAvailability': { termType: 'NamedNode', value: 'https://schema.org/LimitedAvailability', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/LimitedAvailability' } as NamedNode,
	/** LimitedByGuaranteeCharity: Non-profit type referring to a charitable company that is limited by guarantee (UK). */
	'LimitedByGuaranteeCharity': { termType: 'NamedNode', value: 'https://schema.org/LimitedByGuaranteeCharity', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/LimitedByGuaranteeCharity' } as NamedNode,
	/** A Role that represents a Web link, e.g. as expressed via the 'url' property. Its linkRelationship property can indicate URL-based and plain textual link types, e.g. those in IANA link registry or others such as 'amphtml'. This structure provides a placeholder where details from HTML's link element can be represented outside of HTML, e.g. in JSON-LD feeds. */
	'LinkRole': { termType: 'NamedNode', value: 'https://schema.org/LinkRole', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/LinkRole' } as NamedNode,
	/** A shop that sells alcoholic drinks such as wine, beer, whisky and other spirits. */
	'LiquorStore': { termType: 'NamedNode', value: 'https://schema.org/LiquorStore', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/LiquorStore' } as NamedNode,
	/** An list item, e.g. a step in a checklist or how-to description. */
	'ListItem': { termType: 'NamedNode', value: 'https://schema.org/ListItem', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ListItem' } as NamedNode,
	/** Represents the list price (the price a product is actually advertised for) of an offered product. */
	'ListPrice': { termType: 'NamedNode', value: 'https://schema.org/ListPrice', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ListPrice' } as NamedNode,
	/** The act of consuming audio content. */
	'ListenAction': { termType: 'NamedNode', value: 'https://schema.org/ListenAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ListenAction' } as NamedNode,
	/** Event type: Literary event. */
	'LiteraryEvent': { termType: 'NamedNode', value: 'https://schema.org/LiteraryEvent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/LiteraryEvent' } as NamedNode,
	/** LiveAlbum. */
	'LiveAlbum': { termType: 'NamedNode', value: 'https://schema.org/LiveAlbum', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/LiveAlbum' } as NamedNode,
	/** A [[LiveBlogPosting]] is a [[BlogPosting]] intended to provide a rolling textual coverage of an ongoing event through continuous updates. */
	'LiveBlogPosting': { termType: 'NamedNode', value: 'https://schema.org/LiveBlogPosting', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/LiveBlogPosting' } as NamedNode,
	/** Information about coping or life related to the topic. */
	'LivingWithHealthAspect': { termType: 'NamedNode', value: 'https://schema.org/LivingWithHealthAspect', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/LivingWithHealthAspect' } as NamedNode,
	/** A financial product for the loaning of an amount of money, or line of credit, under agreed terms and charges. */
	'LoanOrCredit': { termType: 'NamedNode', value: 'https://schema.org/LoanOrCredit', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/LoanOrCredit' } as NamedNode,
	/** A particular physical business or branch of an organization. Examples of LocalBusiness include a restaurant, a particular branch of a restaurant chain, a branch of a bank, a medical practice, a club, a bowling alley, etc. */
	'LocalBusiness': { termType: 'NamedNode', value: 'https://schema.org/LocalBusiness', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/LocalBusiness' } as NamedNode,
	/** Specifies a location feature by providing a structured value representing a feature of an accommodation as a property-value pair of varying degrees of formality. */
	'LocationFeatureSpecification': { termType: 'NamedNode', value: 'https://schema.org/LocationFeatureSpecification', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/LocationFeatureSpecification' } as NamedNode,
	/** A DeliveryMethod in which an item is made available via locker. */
	'LockerDelivery': { termType: 'NamedNode', value: 'https://schema.org/LockerDelivery', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/LockerDelivery' } as NamedNode,
	/** A locksmith. */
	'Locksmith': { termType: 'NamedNode', value: 'https://schema.org/Locksmith', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Locksmith' } as NamedNode,
	/** A lodging business, such as a motel, hotel, or inn. */
	'LodgingBusiness': { termType: 'NamedNode', value: 'https://schema.org/LodgingBusiness', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/LodgingBusiness' } as NamedNode,
	/** A reservation for lodging at a hotel, motel, inn, etc.\n\nNote: This type is for information about actual reservations, e.g. in confirmation emails or HTML pages with individual confirmations of reservations. */
	'LodgingReservation': { termType: 'NamedNode', value: 'https://schema.org/LodgingReservation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/LodgingReservation' } as NamedNode,
	/** Unlike cross-sectional studies, longitudinal studies track the same people, and therefore the differences observed in those people are less likely to be the result of cultural differences across generations. Longitudinal studies are also used in medicine to uncover predictors of certain diseases. */
	'Longitudinal': { termType: 'NamedNode', value: 'https://schema.org/Longitudinal', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Longitudinal' } as NamedNode,
	/** The act of being defeated in a competitive activity. */
	'LoseAction': { termType: 'NamedNode', value: 'https://schema.org/LoseAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/LoseAction' } as NamedNode,
	/** A diet focused on reduced calorie intake. */
	'LowCalorieDiet': { termType: 'NamedNode', value: 'https://schema.org/LowCalorieDiet', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/LowCalorieDiet' } as NamedNode,
	/** A diet focused on reduced fat and cholesterol intake. */
	'LowFatDiet': { termType: 'NamedNode', value: 'https://schema.org/LowFatDiet', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/LowFatDiet' } as NamedNode,
	/** A diet appropriate for people with lactose intolerance. */
	'LowLactoseDiet': { termType: 'NamedNode', value: 'https://schema.org/LowLactoseDiet', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/LowLactoseDiet' } as NamedNode,
	/** A diet focused on reduced sodium intake. */
	'LowSaltDiet': { termType: 'NamedNode', value: 'https://schema.org/LowSaltDiet', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/LowSaltDiet' } as NamedNode,
	/** Lung and respiratory system clinical examination. */
	'Lung': { termType: 'NamedNode', value: 'https://schema.org/Lung', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Lung' } as NamedNode,
	/** A type of blood vessel that specifically carries lymph fluid unidirectionally toward the heart. */
	'LymphaticVessel': { termType: 'NamedNode', value: 'https://schema.org/LymphaticVessel', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/LymphaticVessel' } as NamedNode,
	/** Magnetic resonance imaging. */
	'MRI': { termType: 'NamedNode', value: 'https://schema.org/MRI', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MRI' } as NamedNode,
	/** Represents the manufacturer suggested retail price ("MSRP") of an offered product. */
	'MSRP': { termType: 'NamedNode', value: 'https://schema.org/MSRP', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MSRP' } as NamedNode,
	/** The male gender. */
	'Male': { termType: 'NamedNode', value: 'https://schema.org/Male', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Male' } as NamedNode,
	/** A book, document, or piece of music written by hand rather than typed or printed. */
	'Manuscript': { termType: 'NamedNode', value: 'https://schema.org/Manuscript', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Manuscript' } as NamedNode,
	/** A map. */
	'Map': { termType: 'NamedNode', value: 'https://schema.org/Map', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Map' } as NamedNode,
	/** An enumeration of several kinds of Map. */
	'MapCategoryType': { termType: 'NamedNode', value: 'https://schema.org/MapCategoryType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MapCategoryType' } as NamedNode,
	/** The act of marrying a person. */
	'MarryAction': { termType: 'NamedNode', value: 'https://schema.org/MarryAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MarryAction' } as NamedNode,
	/** Properties that take Mass as values are of the form '&lt;Number&gt; &lt;Mass unit of measure&gt;'. E.g., '7 kg'. */
	'Mass': { termType: 'NamedNode', value: 'https://schema.org/Mass', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Mass' } as NamedNode,
	/** A math solver which is capable of solving a subset of mathematical problems. */
	'MathSolver': { termType: 'NamedNode', value: 'https://schema.org/MathSolver', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MathSolver' } as NamedNode,
	/** The maximum dosing schedule considered safe for a drug or supplement as recommended by an authority or by the drug/supplement's manufacturer. Capture the recommending authority in the recognizingAuthority property of MedicalEntity. */
	'MaximumDoseSchedule': { termType: 'NamedNode', value: 'https://schema.org/MaximumDoseSchedule', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MaximumDoseSchedule' } as NamedNode,
	/** Related topics may be treated by a Topic. */
	'MayTreatHealthAspect': { termType: 'NamedNode', value: 'https://schema.org/MayTreatHealthAspect', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MayTreatHealthAspect' } as NamedNode,
	/** Enumeration(s) for use with [[measurementMethod]]. */
	'MeasurementMethodEnum': { termType: 'NamedNode', value: 'https://schema.org/MeasurementMethodEnum', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MeasurementMethodEnum' } as NamedNode,
	/** Enumeration of common measurement types (or dimensions), for example "chest" for a person, "inseam" for pants, "gauge" for screws, or "wheel" for bicycles. */
	'MeasurementTypeEnumeration': { termType: 'NamedNode', value: 'https://schema.org/MeasurementTypeEnumeration', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MeasurementTypeEnumeration' } as NamedNode,
	/** Web page type: Media gallery page. A mixed-media page that can contain media such as images, videos, and other multimedia. */
	'MediaGallery': { termType: 'NamedNode', value: 'https://schema.org/MediaGallery', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MediaGallery' } as NamedNode,
	/**  Codes for use with the [[mediaAuthenticityCategory]] property, indicating the authenticity of a media object (in the context of how it was published or shared). In general these codes are not mutually exclusive, although some combinations (such as 'original' versus 'transformed', 'edited' and 'staged') would be contradictory if applied in the same [[MediaReview]]. Note that the application of these codes is with regard to a piece of media shared or published in a particular context. */
	'MediaManipulationRatingEnumeration': { termType: 'NamedNode', value: 'https://schema.org/MediaManipulationRatingEnumeration', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MediaManipulationRatingEnumeration' } as NamedNode,
	/** A media object, such as an image, video, audio, or text object embedded in a web page or a downloadable dataset i.e. DataDownload. Note that a creative work may have many media objects associated with it on the same web page. For example, a page about a single song (MusicRecording) may have a music video (VideoObject), and a high and low bandwidth audio stream (2 AudioObject's). */
	'MediaObject': { termType: 'NamedNode', value: 'https://schema.org/MediaObject', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MediaObject' } as NamedNode,
	/** A [[MediaReview]] is a more specialized form of Review dedicated to the evaluation of media content online, typically in the context of fact-checking and misinformation.
    For more general reviews of media in the broader sense, use [[UserReview]], [[CriticReview]] or other [[Review]] types. This definition is
    a work in progress. While the [[MediaManipulationRatingEnumeration]] list reflects significant community review amongst fact-checkers and others working
    to combat misinformation, the specific structures for representing media objects, their versions and publication context, are still evolving. Similarly, best practices for the relationship between [[MediaReview]] and [[ClaimReview]] markup have not yet been finalized. */
	'MediaReview': { termType: 'NamedNode', value: 'https://schema.org/MediaReview', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MediaReview' } as NamedNode,
	/** Represents an item or group of closely related items treated as a unit for the sake of evaluation in a [[MediaReview]]. Authorship etc. apply to the items rather than to the curation/grouping or reviewing party. */
	'MediaReviewItem': { termType: 'NamedNode', value: 'https://schema.org/MediaReviewItem', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MediaReviewItem' } as NamedNode,
	/** A subscription which allows a user to access media including audio, video, books, etc. */
	'MediaSubscription': { termType: 'NamedNode', value: 'https://schema.org/MediaSubscription', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MediaSubscription' } as NamedNode,
	/** Target audiences for medical web pages. */
	'MedicalAudience': { termType: 'NamedNode', value: 'https://schema.org/MedicalAudience', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MedicalAudience' } as NamedNode,
	/** Target audiences types for medical web pages. Enumerated type. */
	'MedicalAudienceType': { termType: 'NamedNode', value: 'https://schema.org/MedicalAudienceType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MedicalAudienceType' } as NamedNode,
	/** A particular physical or virtual business of an organization for medical purposes. Examples of MedicalBusiness include different businesses run by health professionals. */
	'MedicalBusiness': { termType: 'NamedNode', value: 'https://schema.org/MedicalBusiness', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MedicalBusiness' } as NamedNode,
	/** The causative agent(s) that are responsible for the pathophysiologic process that eventually results in a medical condition, symptom or sign. In this schema, unless otherwise specified this is meant to be the proximate cause of the medical condition, symptom or sign. The proximate cause is defined as the causative agent that most directly results in the medical condition, symptom or sign. For example, the HIV virus could be considered a cause of AIDS. Or in a diagnostic context, if a patient fell and sustained a hip fracture and two days later sustained a pulmonary embolism which eventuated in a cardiac arrest, the cause of the cardiac arrest (the proximate cause) would be the pulmonary embolism and not the fall. Medical causes can include cardiovascular, chemical, dermatologic, endocrine, environmental, gastroenterologic, genetic, hematologic, gynecologic, iatrogenic, infectious, musculoskeletal, neurologic, nutritional, obstetric, oncologic, otolaryngologic, pharmacologic, psychiatric, pulmonary, renal, rheumatologic, toxic, traumatic, or urologic causes; medical conditions can be causes as well. */
	'MedicalCause': { termType: 'NamedNode', value: 'https://schema.org/MedicalCause', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MedicalCause' } as NamedNode,
	/** A facility, often associated with a hospital or medical school, that is devoted to the specific diagnosis and/or healthcare. Previously limited to outpatients but with evolution it may be open to inpatients as well. */
	'MedicalClinic': { termType: 'NamedNode', value: 'https://schema.org/MedicalClinic', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MedicalClinic' } as NamedNode,
	/** A code for a medical entity. */
	'MedicalCode': { termType: 'NamedNode', value: 'https://schema.org/MedicalCode', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MedicalCode' } as NamedNode,
	/** Any condition of the human body that affects the normal functioning of a person, whether physically or mentally. Includes diseases, injuries, disabilities, disorders, syndromes, etc. */
	'MedicalCondition': { termType: 'NamedNode', value: 'https://schema.org/MedicalCondition', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MedicalCondition' } as NamedNode,
	/** A stage of a medical condition, such as 'Stage IIIa'. */
	'MedicalConditionStage': { termType: 'NamedNode', value: 'https://schema.org/MedicalConditionStage', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MedicalConditionStage' } as NamedNode,
	/** A condition or factor that serves as a reason to withhold a certain medical therapy. Contraindications can be absolute (there are no reasonable circumstances for undertaking a course of action) or relative (the patient is at higher risk of complications, but these risks may be outweighed by other considerations or mitigated by other measures). */
	'MedicalContraindication': { termType: 'NamedNode', value: 'https://schema.org/MedicalContraindication', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MedicalContraindication' } as NamedNode,
	/** Any object used in a medical capacity, such as to diagnose or treat a patient. */
	'MedicalDevice': { termType: 'NamedNode', value: 'https://schema.org/MedicalDevice', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MedicalDevice' } as NamedNode,
	/** Categories of medical devices, organized by the purpose or intended use of the device. */
	'MedicalDevicePurpose': { termType: 'NamedNode', value: 'https://schema.org/MedicalDevicePurpose', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MedicalDevicePurpose' } as NamedNode,
	/** The most generic type of entity related to health and the practice of medicine. */
	'MedicalEntity': { termType: 'NamedNode', value: 'https://schema.org/MedicalEntity', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MedicalEntity' } as NamedNode,
	/** Enumerations related to health and the practice of medicine: A concept that is used to attribute a quality to another concept, as a qualifier, a collection of items or a listing of all of the elements of a set in medicine practice. */
	'MedicalEnumeration': { termType: 'NamedNode', value: 'https://schema.org/MedicalEnumeration', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MedicalEnumeration' } as NamedNode,
	/** Level of evidence for a medical guideline. Enumerated type. */
	'MedicalEvidenceLevel': { termType: 'NamedNode', value: 'https://schema.org/MedicalEvidenceLevel', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MedicalEvidenceLevel' } as NamedNode,
	/** Any recommendation made by a standard society (e.g. ACC/AHA) or consensus statement that denotes how to diagnose and treat a particular condition. Note: this type should be used to tag the actual guideline recommendation; if the guideline recommendation occurs in a larger scholarly article, use MedicalScholarlyArticle to tag the overall article, not this type. Note also: the organization making the recommendation should be captured in the recognizingAuthority base property of MedicalEntity. */
	'MedicalGuideline': { termType: 'NamedNode', value: 'https://schema.org/MedicalGuideline', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MedicalGuideline' } as NamedNode,
	/** A guideline contraindication that designates a process as harmful and where quality of the data supporting the contraindication is sound. */
	'MedicalGuidelineContraindication': { termType: 'NamedNode', value: 'https://schema.org/MedicalGuidelineContraindication', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MedicalGuidelineContraindication' } as NamedNode,
	/** A guideline recommendation that is regarded as efficacious and where quality of the data supporting the recommendation is sound. */
	'MedicalGuidelineRecommendation': { termType: 'NamedNode', value: 'https://schema.org/MedicalGuidelineRecommendation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MedicalGuidelineRecommendation' } as NamedNode,
	/** Any medical imaging modality typically used for diagnostic purposes. Enumerated type. */
	'MedicalImagingTechnique': { termType: 'NamedNode', value: 'https://schema.org/MedicalImagingTechnique', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MedicalImagingTechnique' } as NamedNode,
	/** A condition or factor that indicates use of a medical therapy, including signs, symptoms, risk factors, anatomical states, etc. */
	'MedicalIndication': { termType: 'NamedNode', value: 'https://schema.org/MedicalIndication', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MedicalIndication' } as NamedNode,
	/** A utility class that serves as the umbrella for a number of 'intangible' things in the medical space. */
	'MedicalIntangible': { termType: 'NamedNode', value: 'https://schema.org/MedicalIntangible', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MedicalIntangible' } as NamedNode,
	/** An observational study is a type of medical study that attempts to infer the possible effect of a treatment through observation of a cohort of subjects over a period of time. In an observational study, the assignment of subjects into treatment groups versus control groups is outside the control of the investigator. This is in contrast with controlled studies, such as the randomized controlled trials represented by MedicalTrial, where each subject is randomly assigned to a treatment group or a control group before the start of the treatment. */
	'MedicalObservationalStudy': { termType: 'NamedNode', value: 'https://schema.org/MedicalObservationalStudy', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MedicalObservationalStudy' } as NamedNode,
	/** Design models for observational medical studies. Enumerated type. */
	'MedicalObservationalStudyDesign': { termType: 'NamedNode', value: 'https://schema.org/MedicalObservationalStudyDesign', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MedicalObservationalStudyDesign' } as NamedNode,
	/** A medical organization (physical or not), such as hospital, institution or clinic. */
	'MedicalOrganization': { termType: 'NamedNode', value: 'https://schema.org/MedicalOrganization', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MedicalOrganization' } as NamedNode,
	/** A process of care used in either a diagnostic, therapeutic, preventive or palliative capacity that relies on invasive (surgical), non-invasive, or other techniques. */
	'MedicalProcedure': { termType: 'NamedNode', value: 'https://schema.org/MedicalProcedure', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MedicalProcedure' } as NamedNode,
	/** An enumeration that describes different types of medical procedures. */
	'MedicalProcedureType': { termType: 'NamedNode', value: 'https://schema.org/MedicalProcedureType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MedicalProcedureType' } as NamedNode,
	/** Medical researchers. */
	'MedicalResearcher': { termType: 'NamedNode', value: 'https://schema.org/MedicalResearcher', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MedicalResearcher' } as NamedNode,
	/** A complex mathematical calculation requiring an online calculator, used to assess prognosis. Note: use the url property of Thing to record any URLs for online calculators. */
	'MedicalRiskCalculator': { termType: 'NamedNode', value: 'https://schema.org/MedicalRiskCalculator', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MedicalRiskCalculator' } as NamedNode,
	/** Any rule set or interactive tool for estimating the risk of developing a complication or condition. */
	'MedicalRiskEstimator': { termType: 'NamedNode', value: 'https://schema.org/MedicalRiskEstimator', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MedicalRiskEstimator' } as NamedNode,
	/** A risk factor is anything that increases a person's likelihood of developing or contracting a disease, medical condition, or complication. */
	'MedicalRiskFactor': { termType: 'NamedNode', value: 'https://schema.org/MedicalRiskFactor', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MedicalRiskFactor' } as NamedNode,
	/** A simple system that adds up the number of risk factors to yield a score that is associated with prognosis, e.g. CHAD score, TIMI risk score. */
	'MedicalRiskScore': { termType: 'NamedNode', value: 'https://schema.org/MedicalRiskScore', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MedicalRiskScore' } as NamedNode,
	/** A scholarly article in the medical domain. */
	'MedicalScholarlyArticle': { termType: 'NamedNode', value: 'https://schema.org/MedicalScholarlyArticle', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MedicalScholarlyArticle' } as NamedNode,
	/** Any physical manifestation of a person's medical condition discoverable by objective diagnostic tests or physical examination. */
	'MedicalSign': { termType: 'NamedNode', value: 'https://schema.org/MedicalSign', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MedicalSign' } as NamedNode,
	/** Any feature associated or not with a medical condition. In medicine a symptom is generally subjective while a sign is objective. */
	'MedicalSignOrSymptom': { termType: 'NamedNode', value: 'https://schema.org/MedicalSignOrSymptom', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MedicalSignOrSymptom' } as NamedNode,
	/** Any specific branch of medical science or practice. Medical specialities include clinical specialties that pertain to particular organ systems and their respective disease states, as well as allied health specialties. Enumerated type. */
	'MedicalSpecialty': { termType: 'NamedNode', value: 'https://schema.org/MedicalSpecialty', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MedicalSpecialty' } as NamedNode,
	/** A medical study is an umbrella type covering all kinds of research studies relating to human medicine or health, including observational studies and interventional trials and registries, randomized, controlled or not. When the specific type of study is known, use one of the extensions of this type, such as MedicalTrial or MedicalObservationalStudy. Also, note that this type should be used to mark up data that describes the study itself; to tag an article that publishes the results of a study, use MedicalScholarlyArticle. Note: use the code property of MedicalEntity to store study IDs, e.g. clinicaltrials.gov ID. */
	'MedicalStudy': { termType: 'NamedNode', value: 'https://schema.org/MedicalStudy', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MedicalStudy' } as NamedNode,
	/** The status of a medical study. Enumerated type. */
	'MedicalStudyStatus': { termType: 'NamedNode', value: 'https://schema.org/MedicalStudyStatus', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MedicalStudyStatus' } as NamedNode,
	/** Any complaint sensed and expressed by the patient (therefore defined as subjective)  like stomachache, lower-back pain, or fatigue. */
	'MedicalSymptom': { termType: 'NamedNode', value: 'https://schema.org/MedicalSymptom', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MedicalSymptom' } as NamedNode,
	/** Any medical test, typically performed for diagnostic purposes. */
	'MedicalTest': { termType: 'NamedNode', value: 'https://schema.org/MedicalTest', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MedicalTest' } as NamedNode,
	/** Any collection of tests commonly ordered together. */
	'MedicalTestPanel': { termType: 'NamedNode', value: 'https://schema.org/MedicalTestPanel', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MedicalTestPanel' } as NamedNode,
	/** Any medical intervention designed to prevent, treat, and cure human diseases and medical conditions, including both curative and palliative therapies. Medical therapies are typically processes of care relying upon pharmacotherapy, behavioral therapy, supportive therapy (with fluid or nutrition for example), or detoxification (e.g. hemodialysis) aimed at improving or preventing a health condition. */
	'MedicalTherapy': { termType: 'NamedNode', value: 'https://schema.org/MedicalTherapy', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MedicalTherapy' } as NamedNode,
	/** A medical trial is a type of medical study that uses a scientific process to compare the safety and efficacy of medical therapies or medical procedures. In general, medical trials are controlled and subjects are allocated at random to the different treatment and/or control groups. */
	'MedicalTrial': { termType: 'NamedNode', value: 'https://schema.org/MedicalTrial', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MedicalTrial' } as NamedNode,
	/** Design models for medical trials. Enumerated type. */
	'MedicalTrialDesign': { termType: 'NamedNode', value: 'https://schema.org/MedicalTrialDesign', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MedicalTrialDesign' } as NamedNode,
	/** A web page that provides medical information. */
	'MedicalWebPage': { termType: 'NamedNode', value: 'https://schema.org/MedicalWebPage', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MedicalWebPage' } as NamedNode,
	/** Systems of medical practice. */
	'MedicineSystem': { termType: 'NamedNode', value: 'https://schema.org/MedicineSystem', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MedicineSystem' } as NamedNode,
	/** A meeting room, conference room, or conference hall is a room provided for singular events such as business conferences and meetings (source: Wikipedia, the free encyclopedia, see <a href="http://en.wikipedia.org/wiki/Conference_hall">http://en.wikipedia.org/wiki/Conference_hall</a>).
<br /><br />
See also the <a href="/docs/hotels.html">dedicated document on the use of schema.org for marking up hotels and other forms of accommodations</a>.
 */
	'MeetingRoom': { termType: 'NamedNode', value: 'https://schema.org/MeetingRoom', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MeetingRoom' } as NamedNode,
	/** A men's clothing store. */
	'MensClothingStore': { termType: 'NamedNode', value: 'https://schema.org/MensClothingStore', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MensClothingStore' } as NamedNode,
	/** A structured representation of food or drink items available from a FoodEstablishment. */
	'Menu': { termType: 'NamedNode', value: 'https://schema.org/Menu', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Menu' } as NamedNode,
	/** A food or drink item listed in a menu or menu section. */
	'MenuItem': { termType: 'NamedNode', value: 'https://schema.org/MenuItem', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MenuItem' } as NamedNode,
	/** A sub-grouping of food or drink items in a menu. E.g. courses (such as 'Dinner', 'Breakfast', etc.), specific type of dishes (such as 'Meat', 'Vegan', 'Drinks', etc.), or some other classification made by the menu provider. */
	'MenuSection': { termType: 'NamedNode', value: 'https://schema.org/MenuSection', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MenuSection' } as NamedNode,
	/** Enumerates several kinds of product return policies. */
	'MerchantReturnEnumeration': { termType: 'NamedNode', value: 'https://schema.org/MerchantReturnEnumeration', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MerchantReturnEnumeration' } as NamedNode,
	/** Specifies that there is a finite window for product returns. */
	'MerchantReturnFiniteReturnWindow': { termType: 'NamedNode', value: 'https://schema.org/MerchantReturnFiniteReturnWindow', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MerchantReturnFiniteReturnWindow' } as NamedNode,
	/** Specifies that product returns are not permitted. */
	'MerchantReturnNotPermitted': { termType: 'NamedNode', value: 'https://schema.org/MerchantReturnNotPermitted', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MerchantReturnNotPermitted' } as NamedNode,
	/** A MerchantReturnPolicy provides information about product return policies associated with an [[Organization]], [[Product]], or [[Offer]]. */
	'MerchantReturnPolicy': { termType: 'NamedNode', value: 'https://schema.org/MerchantReturnPolicy', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MerchantReturnPolicy' } as NamedNode,
	/** A seasonal override of a return policy, for example used for holidays. */
	'MerchantReturnPolicySeasonalOverride': { termType: 'NamedNode', value: 'https://schema.org/MerchantReturnPolicySeasonalOverride', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MerchantReturnPolicySeasonalOverride' } as NamedNode,
	/** Specifies that there is an unlimited window for product returns. */
	'MerchantReturnUnlimitedWindow': { termType: 'NamedNode', value: 'https://schema.org/MerchantReturnUnlimitedWindow', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MerchantReturnUnlimitedWindow' } as NamedNode,
	/** Specifies that a product return policy is not provided. */
	'MerchantReturnUnspecified': { termType: 'NamedNode', value: 'https://schema.org/MerchantReturnUnspecified', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MerchantReturnUnspecified' } as NamedNode,
	/** A single message from a sender to one or more organizations or people. */
	'Message': { termType: 'NamedNode', value: 'https://schema.org/Message', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Message' } as NamedNode,
	/** A middle school (typically for children aged around 11-14, although this varies somewhat). */
	'MiddleSchool': { termType: 'NamedNode', value: 'https://schema.org/MiddleSchool', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MiddleSchool' } as NamedNode,
	/** A nurse-like health profession that deals with pregnancy, childbirth, and the postpartum period (including care of the newborn), besides sexual and reproductive health of women throughout their lives. */
	'Midwifery': { termType: 'NamedNode', value: 'https://schema.org/Midwifery', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Midwifery' } as NamedNode,
	/** Represents the minimum advertised price ("MAP") (as dictated by the manufacturer) of an offered product. */
	'MinimumAdvertisedPrice': { termType: 'NamedNode', value: 'https://schema.org/MinimumAdvertisedPrice', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MinimumAdvertisedPrice' } as NamedNode,
	/** Content about common misconceptions and myths that are related to a topic. */
	'MisconceptionsHealthAspect': { termType: 'NamedNode', value: 'https://schema.org/MisconceptionsHealthAspect', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MisconceptionsHealthAspect' } as NamedNode,
	/** MixedEventAttendanceMode - an event that is conducted as a combination of both offline and online modes. */
	'MixedEventAttendanceMode': { termType: 'NamedNode', value: 'https://schema.org/MixedEventAttendanceMode', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MixedEventAttendanceMode' } as NamedNode,
	/** MixtapeAlbum. */
	'MixtapeAlbum': { termType: 'NamedNode', value: 'https://schema.org/MixtapeAlbum', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MixtapeAlbum' } as NamedNode,
	/** A software application designed specifically to work well on a mobile device such as a telephone. */
	'MobileApplication': { termType: 'NamedNode', value: 'https://schema.org/MobileApplication', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MobileApplication' } as NamedNode,
	/** A store that sells mobile phones and related accessories. */
	'MobilePhoneStore': { termType: 'NamedNode', value: 'https://schema.org/MobilePhoneStore', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MobilePhoneStore' } as NamedNode,
	/** Represents the broad notion of 'mobile' browsers as a Web Platform. */
	'MobileWebPlatform': { termType: 'NamedNode', value: 'https://schema.org/MobileWebPlatform', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MobileWebPlatform' } as NamedNode,
	/** Any constitutionally or isotopically distinct atom, molecule, ion, ion pair, radical, radical ion, complex, conformer etc., identifiable as a separately distinguishable entity. */
	'MolecularEntity': { termType: 'NamedNode', value: 'https://schema.org/MolecularEntity', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MolecularEntity' } as NamedNode,
	/** The day of the week between Sunday and Tuesday. */
	'Monday': { termType: 'NamedNode', value: 'https://schema.org/Monday', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Monday' } as NamedNode,
	/** A monetary value or range. This type can be used to describe an amount of money such as $50 USD, or a range as in describing a bank account being suitable for a balance between £1,000 and £1,000,000 GBP, or the value of a salary, etc. It is recommended to use [[PriceSpecification]] Types to describe the price of an Offer, Invoice, etc. */
	'MonetaryAmount': { termType: 'NamedNode', value: 'https://schema.org/MonetaryAmount', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MonetaryAmount' } as NamedNode,
	/** A statistical distribution of monetary amounts. */
	'MonetaryAmountDistribution': { termType: 'NamedNode', value: 'https://schema.org/MonetaryAmountDistribution', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MonetaryAmountDistribution' } as NamedNode,
	/** A monetary grant. */
	'MonetaryGrant': { termType: 'NamedNode', value: 'https://schema.org/MonetaryGrant', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MonetaryGrant' } as NamedNode,
	/** The act of transferring money from one place to another place. This may occur electronically or physically. */
	'MoneyTransfer': { termType: 'NamedNode', value: 'https://schema.org/MoneyTransfer', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MoneyTransfer' } as NamedNode,
	/** A loan in which property or real estate is used as collateral. (A loan securitized against some real estate.) */
	'MortgageLoan': { termType: 'NamedNode', value: 'https://schema.org/MortgageLoan', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MortgageLoan' } as NamedNode,
	/** A mosque. */
	'Mosque': { termType: 'NamedNode', value: 'https://schema.org/Mosque', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Mosque' } as NamedNode,
	/** A motel.
<br /><br />
See also the <a href="/docs/hotels.html">dedicated document on the use of schema.org for marking up hotels and other forms of accommodations</a>.
 */
	'Motel': { termType: 'NamedNode', value: 'https://schema.org/Motel', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Motel' } as NamedNode,
	/** A motorcycle or motorbike is a single-track, two-wheeled motor vehicle. */
	'Motorcycle': { termType: 'NamedNode', value: 'https://schema.org/Motorcycle', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Motorcycle' } as NamedNode,
	/** A motorcycle dealer. */
	'MotorcycleDealer': { termType: 'NamedNode', value: 'https://schema.org/MotorcycleDealer', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MotorcycleDealer' } as NamedNode,
	/** A motorcycle repair shop. */
	'MotorcycleRepair': { termType: 'NamedNode', value: 'https://schema.org/MotorcycleRepair', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MotorcycleRepair' } as NamedNode,
	/** A motorized bicycle is a bicycle with an attached motor used to power the vehicle, or to assist with pedaling. */
	'MotorizedBicycle': { termType: 'NamedNode', value: 'https://schema.org/MotorizedBicycle', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MotorizedBicycle' } as NamedNode,
	/** A mountain, like Mount Whitney or Mount Everest. */
	'Mountain': { termType: 'NamedNode', value: 'https://schema.org/Mountain', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Mountain' } as NamedNode,
	/** The act of an agent relocating to a place.\n\nRelated actions:\n\n* [[TransferAction]]: Unlike TransferAction, the subject of the move is a living Person or Organization rather than an inanimate object. */
	'MoveAction': { termType: 'NamedNode', value: 'https://schema.org/MoveAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MoveAction' } as NamedNode,
	/** A movie. */
	'Movie': { termType: 'NamedNode', value: 'https://schema.org/Movie', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Movie' } as NamedNode,
	/** A short segment/part of a movie. */
	'MovieClip': { termType: 'NamedNode', value: 'https://schema.org/MovieClip', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MovieClip' } as NamedNode,
	/** A movie rental store. */
	'MovieRentalStore': { termType: 'NamedNode', value: 'https://schema.org/MovieRentalStore', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MovieRentalStore' } as NamedNode,
	/** A series of movies. Included movies can be indicated with the hasPart property. */
	'MovieSeries': { termType: 'NamedNode', value: 'https://schema.org/MovieSeries', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MovieSeries' } as NamedNode,
	/** A movie theater. */
	'MovieTheater': { termType: 'NamedNode', value: 'https://schema.org/MovieTheater', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MovieTheater' } as NamedNode,
	/** A moving company. */
	'MovingCompany': { termType: 'NamedNode', value: 'https://schema.org/MovingCompany', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MovingCompany' } as NamedNode,
	/** A trial that takes place at multiple centers. */
	'MultiCenterTrial': { termType: 'NamedNode', value: 'https://schema.org/MultiCenterTrial', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MultiCenterTrial' } as NamedNode,
	/** Play mode: MultiPlayer. Requiring or allowing multiple human players to play simultaneously. */
	'MultiPlayer': { termType: 'NamedNode', value: 'https://schema.org/MultiPlayer', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MultiPlayer' } as NamedNode,
	/** Multicellular parasite that causes an infection. */
	'MulticellularParasite': { termType: 'NamedNode', value: 'https://schema.org/MulticellularParasite', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MulticellularParasite' } as NamedNode,
	/** A muscle is an anatomical structure consisting of a contractile form of tissue that animals use to effect movement. */
	'Muscle': { termType: 'NamedNode', value: 'https://schema.org/Muscle', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Muscle' } as NamedNode,
	/** A specific branch of medical science that pertains to diagnosis and treatment of disorders of muscles, ligaments and skeletal system. */
	'Musculoskeletal': { termType: 'NamedNode', value: 'https://schema.org/Musculoskeletal', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Musculoskeletal' } as NamedNode,
	/** Musculoskeletal system clinical examination. */
	'MusculoskeletalExam': { termType: 'NamedNode', value: 'https://schema.org/MusculoskeletalExam', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MusculoskeletalExam' } as NamedNode,
	/** A museum. */
	'Museum': { termType: 'NamedNode', value: 'https://schema.org/Museum', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Museum' } as NamedNode,
	/** A collection of music tracks. */
	'MusicAlbum': { termType: 'NamedNode', value: 'https://schema.org/MusicAlbum', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MusicAlbum' } as NamedNode,
	/** Classification of the album by its type of content: soundtrack, live album, studio album, etc. */
	'MusicAlbumProductionType': { termType: 'NamedNode', value: 'https://schema.org/MusicAlbumProductionType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MusicAlbumProductionType' } as NamedNode,
	/** The kind of release which this album is: single, EP or album. */
	'MusicAlbumReleaseType': { termType: 'NamedNode', value: 'https://schema.org/MusicAlbumReleaseType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MusicAlbumReleaseType' } as NamedNode,
	/** A musical composition. */
	'MusicComposition': { termType: 'NamedNode', value: 'https://schema.org/MusicComposition', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MusicComposition' } as NamedNode,
	/** Event type: Music event. */
	'MusicEvent': { termType: 'NamedNode', value: 'https://schema.org/MusicEvent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MusicEvent' } as NamedNode,
	/** A musical group, such as a band, an orchestra, or a choir. Can also be a solo musician. */
	'MusicGroup': { termType: 'NamedNode', value: 'https://schema.org/MusicGroup', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MusicGroup' } as NamedNode,
	/** A collection of music tracks in playlist form. */
	'MusicPlaylist': { termType: 'NamedNode', value: 'https://schema.org/MusicPlaylist', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MusicPlaylist' } as NamedNode,
	/** A music recording (track), usually a single song. */
	'MusicRecording': { termType: 'NamedNode', value: 'https://schema.org/MusicRecording', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MusicRecording' } as NamedNode,
	/** A MusicRelease is a specific release of a music album. */
	'MusicRelease': { termType: 'NamedNode', value: 'https://schema.org/MusicRelease', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MusicRelease' } as NamedNode,
	/** Format of this release (the type of recording media used, i.e. compact disc, digital media, LP, etc.). */
	'MusicReleaseFormatType': { termType: 'NamedNode', value: 'https://schema.org/MusicReleaseFormatType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MusicReleaseFormatType' } as NamedNode,
	/** A music store. */
	'MusicStore': { termType: 'NamedNode', value: 'https://schema.org/MusicStore', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MusicStore' } as NamedNode,
	/** A music venue. */
	'MusicVenue': { termType: 'NamedNode', value: 'https://schema.org/MusicVenue', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MusicVenue' } as NamedNode,
	/** A music video file. */
	'MusicVideoObject': { termType: 'NamedNode', value: 'https://schema.org/MusicVideoObject', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/MusicVideoObject' } as NamedNode,
	/** Organization: Non-governmental Organization. */
	'NGO': { termType: 'NamedNode', value: 'https://schema.org/NGO', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/NGO' } as NamedNode,
	/** NLNonprofitType: Non-profit organization type originating from the Netherlands. */
	'NLNonprofitType': { termType: 'NamedNode', value: 'https://schema.org/NLNonprofitType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/NLNonprofitType' } as NamedNode,
	/** A nail salon. */
	'NailSalon': { termType: 'NamedNode', value: 'https://schema.org/NailSalon', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/NailSalon' } as NamedNode,
	/** Item is a narcotic as defined by the [1961 UN convention](https://www.incb.org/incb/en/narcotic-drugs/Yellowlist/yellow-list.html), for example marijuana or heroin. */
	'NarcoticConsideration': { termType: 'NamedNode', value: 'https://schema.org/NarcoticConsideration', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/NarcoticConsideration' } as NamedNode,
	/** Neck assessment with clinical examination. */
	'Neck': { termType: 'NamedNode', value: 'https://schema.org/Neck', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Neck' } as NamedNode,
	/** A common pathway for the electrochemical nerve impulses that are transmitted along each of the axons. */
	'Nerve': { termType: 'NamedNode', value: 'https://schema.org/Nerve', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Nerve' } as NamedNode,
	/** Neurological system clinical examination. */
	'Neuro': { termType: 'NamedNode', value: 'https://schema.org/Neuro', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Neuro' } as NamedNode,
	/** A specific branch of medical science that studies the nerves and nervous system and its respective disease states. */
	'Neurologic': { termType: 'NamedNode', value: 'https://schema.org/Neurologic', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Neurologic' } as NamedNode,
	/** Indicates that the item is new. */
	'NewCondition': { termType: 'NamedNode', value: 'https://schema.org/NewCondition', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/NewCondition' } as NamedNode,
	/** A NewsArticle is an article whose content reports news, or provides background context and supporting materials for understanding the news.

A more detailed overview of [schema.org News markup](/docs/news.html) is also available.
 */
	'NewsArticle': { termType: 'NamedNode', value: 'https://schema.org/NewsArticle', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/NewsArticle' } as NamedNode,
	/** A News/Media organization such as a newspaper or TV station. */
	'NewsMediaOrganization': { termType: 'NamedNode', value: 'https://schema.org/NewsMediaOrganization', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/NewsMediaOrganization' } as NamedNode,
	/** A publication containing information about varied topics that are pertinent to general information, a geographic area, or a specific subject matter (i.e. business, culture, education). Often published daily. */
	'Newspaper': { termType: 'NamedNode', value: 'https://schema.org/Newspaper', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Newspaper' } as NamedNode,
	/** A nightclub or discotheque. */
	'NightClub': { termType: 'NamedNode', value: 'https://schema.org/NightClub', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/NightClub' } as NamedNode,
	/** A type of medical procedure that involves noninvasive techniques. */
	'NoninvasiveProcedure': { termType: 'NamedNode', value: 'https://schema.org/NoninvasiveProcedure', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/NoninvasiveProcedure' } as NamedNode,
	/** Nonprofit501a: Non-profit type referring to Farmers’ Cooperative Associations. */
	'Nonprofit501a': { termType: 'NamedNode', value: 'https://schema.org/Nonprofit501a', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Nonprofit501a' } as NamedNode,
	/** Nonprofit501c1: Non-profit type referring to Corporations Organized Under Act of Congress, including Federal Credit Unions and National Farm Loan Associations. */
	'Nonprofit501c1': { termType: 'NamedNode', value: 'https://schema.org/Nonprofit501c1', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Nonprofit501c1' } as NamedNode,
	/** Nonprofit501c10: Non-profit type referring to Domestic Fraternal Societies and Associations. */
	'Nonprofit501c10': { termType: 'NamedNode', value: 'https://schema.org/Nonprofit501c10', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Nonprofit501c10' } as NamedNode,
	/** Nonprofit501c11: Non-profit type referring to Teachers' Retirement Fund Associations. */
	'Nonprofit501c11': { termType: 'NamedNode', value: 'https://schema.org/Nonprofit501c11', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Nonprofit501c11' } as NamedNode,
	/** Nonprofit501c12: Non-profit type referring to Benevolent Life Insurance Associations, Mutual Ditch or Irrigation Companies, Mutual or Cooperative Telephone Companies. */
	'Nonprofit501c12': { termType: 'NamedNode', value: 'https://schema.org/Nonprofit501c12', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Nonprofit501c12' } as NamedNode,
	/** Nonprofit501c13: Non-profit type referring to Cemetery Companies. */
	'Nonprofit501c13': { termType: 'NamedNode', value: 'https://schema.org/Nonprofit501c13', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Nonprofit501c13' } as NamedNode,
	/** Nonprofit501c14: Non-profit type referring to State-Chartered Credit Unions, Mutual Reserve Funds. */
	'Nonprofit501c14': { termType: 'NamedNode', value: 'https://schema.org/Nonprofit501c14', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Nonprofit501c14' } as NamedNode,
	/** Nonprofit501c15: Non-profit type referring to Mutual Insurance Companies or Associations. */
	'Nonprofit501c15': { termType: 'NamedNode', value: 'https://schema.org/Nonprofit501c15', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Nonprofit501c15' } as NamedNode,
	/** Nonprofit501c16: Non-profit type referring to Cooperative Organizations to Finance Crop Operations. */
	'Nonprofit501c16': { termType: 'NamedNode', value: 'https://schema.org/Nonprofit501c16', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Nonprofit501c16' } as NamedNode,
	/** Nonprofit501c17: Non-profit type referring to Supplemental Unemployment Benefit Trusts. */
	'Nonprofit501c17': { termType: 'NamedNode', value: 'https://schema.org/Nonprofit501c17', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Nonprofit501c17' } as NamedNode,
	/** Nonprofit501c18: Non-profit type referring to Employee Funded Pension Trust (created before 25 June 1959). */
	'Nonprofit501c18': { termType: 'NamedNode', value: 'https://schema.org/Nonprofit501c18', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Nonprofit501c18' } as NamedNode,
	/** Nonprofit501c19: Non-profit type referring to Post or Organization of Past or Present Members of the Armed Forces. */
	'Nonprofit501c19': { termType: 'NamedNode', value: 'https://schema.org/Nonprofit501c19', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Nonprofit501c19' } as NamedNode,
	/** Nonprofit501c2: Non-profit type referring to Title-holding Corporations for Exempt Organizations. */
	'Nonprofit501c2': { termType: 'NamedNode', value: 'https://schema.org/Nonprofit501c2', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Nonprofit501c2' } as NamedNode,
	/** Nonprofit501c20: Non-profit type referring to Group Legal Services Plan Organizations. */
	'Nonprofit501c20': { termType: 'NamedNode', value: 'https://schema.org/Nonprofit501c20', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Nonprofit501c20' } as NamedNode,
	/** Nonprofit501c21: Non-profit type referring to Black Lung Benefit Trusts. */
	'Nonprofit501c21': { termType: 'NamedNode', value: 'https://schema.org/Nonprofit501c21', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Nonprofit501c21' } as NamedNode,
	/** Nonprofit501c22: Non-profit type referring to Withdrawal Liability Payment Funds. */
	'Nonprofit501c22': { termType: 'NamedNode', value: 'https://schema.org/Nonprofit501c22', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Nonprofit501c22' } as NamedNode,
	/** Nonprofit501c23: Non-profit type referring to Veterans Organizations. */
	'Nonprofit501c23': { termType: 'NamedNode', value: 'https://schema.org/Nonprofit501c23', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Nonprofit501c23' } as NamedNode,
	/** Nonprofit501c24: Non-profit type referring to Section 4049 ERISA Trusts. */
	'Nonprofit501c24': { termType: 'NamedNode', value: 'https://schema.org/Nonprofit501c24', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Nonprofit501c24' } as NamedNode,
	/** Nonprofit501c25: Non-profit type referring to Real Property Title-Holding Corporations or Trusts with Multiple Parents. */
	'Nonprofit501c25': { termType: 'NamedNode', value: 'https://schema.org/Nonprofit501c25', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Nonprofit501c25' } as NamedNode,
	/** Nonprofit501c26: Non-profit type referring to State-Sponsored Organizations Providing Health Coverage for High-Risk Individuals. */
	'Nonprofit501c26': { termType: 'NamedNode', value: 'https://schema.org/Nonprofit501c26', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Nonprofit501c26' } as NamedNode,
	/** Nonprofit501c27: Non-profit type referring to State-Sponsored Workers' Compensation Reinsurance Organizations. */
	'Nonprofit501c27': { termType: 'NamedNode', value: 'https://schema.org/Nonprofit501c27', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Nonprofit501c27' } as NamedNode,
	/** Nonprofit501c28: Non-profit type referring to National Railroad Retirement Investment Trusts. */
	'Nonprofit501c28': { termType: 'NamedNode', value: 'https://schema.org/Nonprofit501c28', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Nonprofit501c28' } as NamedNode,
	/** Nonprofit501c3: Non-profit type referring to Religious, Educational, Charitable, Scientific, Literary, Testing for Public Safety, Fostering National or International Amateur Sports Competition, or Prevention of Cruelty to Children or Animals Organizations. */
	'Nonprofit501c3': { termType: 'NamedNode', value: 'https://schema.org/Nonprofit501c3', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Nonprofit501c3' } as NamedNode,
	/** Nonprofit501c4: Non-profit type referring to Civic Leagues, Social Welfare Organizations, and Local Associations of Employees. */
	'Nonprofit501c4': { termType: 'NamedNode', value: 'https://schema.org/Nonprofit501c4', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Nonprofit501c4' } as NamedNode,
	/** Nonprofit501c5: Non-profit type referring to Labor, Agricultural and Horticultural Organizations. */
	'Nonprofit501c5': { termType: 'NamedNode', value: 'https://schema.org/Nonprofit501c5', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Nonprofit501c5' } as NamedNode,
	/** Nonprofit501c6: Non-profit type referring to Business Leagues, Chambers of Commerce, Real Estate Boards. */
	'Nonprofit501c6': { termType: 'NamedNode', value: 'https://schema.org/Nonprofit501c6', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Nonprofit501c6' } as NamedNode,
	/** Nonprofit501c7: Non-profit type referring to Social and Recreational Clubs. */
	'Nonprofit501c7': { termType: 'NamedNode', value: 'https://schema.org/Nonprofit501c7', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Nonprofit501c7' } as NamedNode,
	/** Nonprofit501c8: Non-profit type referring to Fraternal Beneficiary Societies and Associations. */
	'Nonprofit501c8': { termType: 'NamedNode', value: 'https://schema.org/Nonprofit501c8', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Nonprofit501c8' } as NamedNode,
	/** Nonprofit501c9: Non-profit type referring to Voluntary Employee Beneficiary Associations. */
	'Nonprofit501c9': { termType: 'NamedNode', value: 'https://schema.org/Nonprofit501c9', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Nonprofit501c9' } as NamedNode,
	/** Nonprofit501d: Non-profit type referring to Religious and Apostolic Associations. */
	'Nonprofit501d': { termType: 'NamedNode', value: 'https://schema.org/Nonprofit501d', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Nonprofit501d' } as NamedNode,
	/** Nonprofit501e: Non-profit type referring to Cooperative Hospital Service Organizations. */
	'Nonprofit501e': { termType: 'NamedNode', value: 'https://schema.org/Nonprofit501e', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Nonprofit501e' } as NamedNode,
	/** Nonprofit501f: Non-profit type referring to Cooperative Service Organizations. */
	'Nonprofit501f': { termType: 'NamedNode', value: 'https://schema.org/Nonprofit501f', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Nonprofit501f' } as NamedNode,
	/** Nonprofit501k: Non-profit type referring to Child Care Organizations. */
	'Nonprofit501k': { termType: 'NamedNode', value: 'https://schema.org/Nonprofit501k', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Nonprofit501k' } as NamedNode,
	/** Nonprofit501n: Non-profit type referring to Charitable Risk Pools. */
	'Nonprofit501n': { termType: 'NamedNode', value: 'https://schema.org/Nonprofit501n', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Nonprofit501n' } as NamedNode,
	/** Nonprofit501q: Non-profit type referring to Credit Counseling Organizations. */
	'Nonprofit501q': { termType: 'NamedNode', value: 'https://schema.org/Nonprofit501q', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Nonprofit501q' } as NamedNode,
	/** Nonprofit527: Non-profit type referring to political organizations. */
	'Nonprofit527': { termType: 'NamedNode', value: 'https://schema.org/Nonprofit527', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Nonprofit527' } as NamedNode,
	/** NonprofitANBI: Non-profit type referring to a Public Benefit Organization (NL). */
	'NonprofitANBI': { termType: 'NamedNode', value: 'https://schema.org/NonprofitANBI', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/NonprofitANBI' } as NamedNode,
	/** NonprofitSBBI: Non-profit type referring to a Social Interest Promoting Institution (NL). */
	'NonprofitSBBI': { termType: 'NamedNode', value: 'https://schema.org/NonprofitSBBI', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/NonprofitSBBI' } as NamedNode,
	/** NonprofitType enumerates several kinds of official non-profit types of which a non-profit organization can be. */
	'NonprofitType': { termType: 'NamedNode', value: 'https://schema.org/NonprofitType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/NonprofitType' } as NamedNode,
	/** Nose function assessment with clinical examination. */
	'Nose': { termType: 'NamedNode', value: 'https://schema.org/Nose', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Nose' } as NamedNode,
	/** Indicates that a legislation is currently not in force. */
	'NotInForce': { termType: 'NamedNode', value: 'https://schema.org/NotInForce', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/NotInForce' } as NamedNode,
	/** Not yet recruiting. */
	'NotYetRecruiting': { termType: 'NamedNode', value: 'https://schema.org/NotYetRecruiting', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/NotYetRecruiting' } as NamedNode,
	/** A notary. */
	'Notary': { termType: 'NamedNode', value: 'https://schema.org/Notary', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Notary' } as NamedNode,
	/** A file containing a note, primarily for the author. */
	'NoteDigitalDocument': { termType: 'NamedNode', value: 'https://schema.org/NoteDigitalDocument', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/NoteDigitalDocument' } as NamedNode,
	/** Data type: Number.\n\nUsage guidelines:\n\n* Use values from 0123456789 (Unicode 'DIGIT ZERO' (U+0030) to 'DIGIT NINE' (U+0039)) rather than superficially similar Unicode symbols.\n* Use '.' (Unicode 'FULL STOP' (U+002E)) rather than ',' to indicate a decimal point. Avoid using these symbols as a readability separator. */
	'Number': { termType: 'NamedNode', value: 'https://schema.org/Number', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Number' } as NamedNode,
	/** A health profession of a person formally educated and trained in the care of the sick or infirm person. */
	'Nursing': { termType: 'NamedNode', value: 'https://schema.org/Nursing', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Nursing' } as NamedNode,
	/** Nutritional information about the recipe. */
	'NutritionInformation': { termType: 'NamedNode', value: 'https://schema.org/NutritionInformation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/NutritionInformation' } as NamedNode,
	/** The character of a medical substance, typically a medicine, of being available over the counter or not. */
	'OTC': { termType: 'NamedNode', value: 'https://schema.org/OTC', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/OTC' } as NamedNode,
	/** Instances of the class [[Observation]] are used to specify observations about an entity at a particular time. The principal properties of an [[Observation]] are [[observationAbout]], [[measuredProperty]], [[statType]], [[value] and [[observationDate]]  and [[measuredProperty]]. Some but not all Observations represent a [[QuantitativeValue]]. Quantitative observations can be about a [[StatisticalVariable]], which is an abstract specification about which we can make observations that are grounded at a particular location and time. 
    
Observations can also encode a subset of simple RDF-like statements (its observationAbout, a StatisticalVariable, defining the measuredPoperty; its observationAbout property indicating the entity the statement is about, and [[value]] )

In the context of a quantitative knowledge graph, typical properties could include [[measuredProperty]], [[observationAbout]], [[observationDate]], [[value]], [[unitCode]], [[unitText]], [[measurementMethod]].
     */
	'Observation': { termType: 'NamedNode', value: 'https://schema.org/Observation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Observation' } as NamedNode,
	/** An observational study design. */
	'Observational': { termType: 'NamedNode', value: 'https://schema.org/Observational', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Observational' } as NamedNode,
	/** A specific branch of medical science that specializes in the care of women during the prenatal and postnatal care and with the delivery of the child. */
	'Obstetric': { termType: 'NamedNode', value: 'https://schema.org/Obstetric', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Obstetric' } as NamedNode,
	/** A profession, may involve prolonged training and/or a formal qualification. */
	'Occupation': { termType: 'NamedNode', value: 'https://schema.org/Occupation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Occupation' } as NamedNode,
	/** Any physical activity engaged in for job-related purposes. Examples may include waiting tables, maid service, carrying a mailbag, picking fruits or vegetables, construction work, etc. */
	'OccupationalActivity': { termType: 'NamedNode', value: 'https://schema.org/OccupationalActivity', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/OccupationalActivity' } as NamedNode,
	/** Indicates employment-related experience requirements, e.g. [[monthsOfExperience]]. */
	'OccupationalExperienceRequirements': { termType: 'NamedNode', value: 'https://schema.org/OccupationalExperienceRequirements', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/OccupationalExperienceRequirements' } as NamedNode,
	/** A treatment of people with physical, emotional, or social problems, using purposeful activity to help them overcome or learn to deal with their problems. */
	'OccupationalTherapy': { termType: 'NamedNode', value: 'https://schema.org/OccupationalTherapy', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/OccupationalTherapy' } as NamedNode,
	/** An ocean (for example, the Pacific). */
	'OceanBodyOfWater': { termType: 'NamedNode', value: 'https://schema.org/OceanBodyOfWater', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/OceanBodyOfWater' } as NamedNode,
	/** An offer to transfer some rights to an item or to provide a service — for example, an offer to sell tickets to an event, to rent the DVD of a movie, to stream a TV show over the internet, to repair a motorcycle, or to loan a book.\n\nNote: As the [[businessFunction]] property, which identifies the form of offer (e.g. sell, lease, repair, dispose), defaults to http://purl.org/goodrelations/v1#Sell; an Offer without a defined businessFunction value can be assumed to be an offer to sell.\n\nFor [GTIN](http://www.gs1.org/barcodes/technical/idkeys/gtin)-related fields, see [Check Digit calculator](http://www.gs1.org/barcodes/support/check_digit_calculator) and [validation guide](http://www.gs1us.org/resources/standards/gtin-validation-guide) from [GS1](http://www.gs1.org/). */
	'Offer': { termType: 'NamedNode', value: 'https://schema.org/Offer', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Offer' } as NamedNode,
	/** An OfferCatalog is an ItemList that contains related Offers and/or further OfferCatalogs that are offeredBy the same provider. */
	'OfferCatalog': { termType: 'NamedNode', value: 'https://schema.org/OfferCatalog', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/OfferCatalog' } as NamedNode,
	/** An [[OfferForLease]] in Schema.org represents an [[Offer]] to lease out something, i.e. an [[Offer]] whose
  [[businessFunction]] is [lease out](http://purl.org/goodrelations/v1#LeaseOut.). See [Good Relations](https://en.wikipedia.org/wiki/GoodRelations) for
  background on the underlying concepts.
   */
	'OfferForLease': { termType: 'NamedNode', value: 'https://schema.org/OfferForLease', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/OfferForLease' } as NamedNode,
	/** An [[OfferForPurchase]] in Schema.org represents an [[Offer]] to sell something, i.e. an [[Offer]] whose
  [[businessFunction]] is [sell](http://purl.org/goodrelations/v1#Sell.). See [Good Relations](https://en.wikipedia.org/wiki/GoodRelations) for
  background on the underlying concepts.
   */
	'OfferForPurchase': { termType: 'NamedNode', value: 'https://schema.org/OfferForPurchase', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/OfferForPurchase' } as NamedNode,
	/** A list of possible conditions for the item. */
	'OfferItemCondition': { termType: 'NamedNode', value: 'https://schema.org/OfferItemCondition', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/OfferItemCondition' } as NamedNode,
	/** OfferShippingDetails represents information about shipping destinations.

Multiple of these entities can be used to represent different shipping rates for different destinations:

One entity for Alaska/Hawaii. A different one for continental US. A different one for all France.

Multiple of these entities can be used to represent different shipping costs and delivery times.

Two entities that are identical but differ in rate and time:

E.g. Cheaper and slower: $5 in 5-7 days
or Fast and expensive: $15 in 1-2 days. */
	'OfferShippingDetails': { termType: 'NamedNode', value: 'https://schema.org/OfferShippingDetails', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/OfferShippingDetails' } as NamedNode,
	/** An office equipment store. */
	'OfficeEquipmentStore': { termType: 'NamedNode', value: 'https://schema.org/OfficeEquipmentStore', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/OfficeEquipmentStore' } as NamedNode,
	/** All the documents published by an official publisher should have at least the legal value level "OfficialLegalValue". This indicates that the document was published by an organisation with the public task of making it available (e.g. a consolidated version of an EU directive published by the EU Office of Publications). */
	'OfficialLegalValue': { termType: 'NamedNode', value: 'https://schema.org/OfficialLegalValue', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/OfficialLegalValue' } as NamedNode,
	/** OfflineEventAttendanceMode - an event that is primarily conducted offline.  */
	'OfflineEventAttendanceMode': { termType: 'NamedNode', value: 'https://schema.org/OfflineEventAttendanceMode', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/OfflineEventAttendanceMode' } as NamedNode,
	/** Game server status: OfflinePermanently. Server is offline and not available. */
	'OfflinePermanently': { termType: 'NamedNode', value: 'https://schema.org/OfflinePermanently', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/OfflinePermanently' } as NamedNode,
	/** Game server status: OfflineTemporarily. Server is offline now but it can be online soon. */
	'OfflineTemporarily': { termType: 'NamedNode', value: 'https://schema.org/OfflineTemporarily', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/OfflineTemporarily' } as NamedNode,
	/** A publication event, e.g. catch-up TV or radio podcast, during which a program is available on-demand. */
	'OnDemandEvent': { termType: 'NamedNode', value: 'https://schema.org/OnDemandEvent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/OnDemandEvent' } as NamedNode,
	/** A DeliveryMethod in which an item is collected on site, e.g. in a store or at a box office. */
	'OnSitePickup': { termType: 'NamedNode', value: 'https://schema.org/OnSitePickup', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/OnSitePickup' } as NamedNode,
	/** A specific branch of medical science that deals with benign and malignant tumors, including the study of their development, diagnosis, treatment and prevention. */
	'Oncologic': { termType: 'NamedNode', value: 'https://schema.org/Oncologic', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Oncologic' } as NamedNode,
	/** OneTimePayments: this is a benefit for one-time payments for individuals. */
	'OneTimePayments': { termType: 'NamedNode', value: 'https://schema.org/OneTimePayments', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/OneTimePayments' } as NamedNode,
	/** Game server status: Online. Server is available. */
	'Online': { termType: 'NamedNode', value: 'https://schema.org/Online', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Online' } as NamedNode,
	/** A particular online business, either standalone or the online part of a broader organization. Examples include an eCommerce site, an online travel booking site, an online learning site, an online logistics and shipping provider, an online (virtual) doctor, etc. */
	'OnlineBusiness': { termType: 'NamedNode', value: 'https://schema.org/OnlineBusiness', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/OnlineBusiness' } as NamedNode,
	/** OnlineEventAttendanceMode - an event that is primarily conducted online.  */
	'OnlineEventAttendanceMode': { termType: 'NamedNode', value: 'https://schema.org/OnlineEventAttendanceMode', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/OnlineEventAttendanceMode' } as NamedNode,
	/** Game server status: OnlineFull. Server is online but unavailable. The maximum number of players has reached. */
	'OnlineFull': { termType: 'NamedNode', value: 'https://schema.org/OnlineFull', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/OnlineFull' } as NamedNode,
	/** Indicates that the item is available only online. */
	'OnlineOnly': { termType: 'NamedNode', value: 'https://schema.org/OnlineOnly', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/OnlineOnly' } as NamedNode,
	/** An eCommerce site. */
	'OnlineStore': { termType: 'NamedNode', value: 'https://schema.org/OnlineStore', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/OnlineStore' } as NamedNode,
	/** A trial design in which the researcher knows the full details of the treatment, and so does the patient. */
	'OpenTrial': { termType: 'NamedNode', value: 'https://schema.org/OpenTrial', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/OpenTrial' } as NamedNode,
	/** A structured value providing information about the opening hours of a place or a certain service inside a place.\n\n
The place is __open__ if the [[opens]] property is specified, and __closed__ otherwise.\n\nIf the value for the [[closes]] property is less than the value for the [[opens]] property then the hour range is assumed to span over the next day.
       */
	'OpeningHoursSpecification': { termType: 'NamedNode', value: 'https://schema.org/OpeningHoursSpecification', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/OpeningHoursSpecification' } as NamedNode,
	/** An [[OpinionNewsArticle]] is a [[NewsArticle]] that primarily expresses opinions rather than journalistic reporting of news and events. For example, a [[NewsArticle]] consisting of a column or [[Blog]]/[[BlogPosting]] entry in the Opinions section of a news publication.  */
	'OpinionNewsArticle': { termType: 'NamedNode', value: 'https://schema.org/OpinionNewsArticle', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/OpinionNewsArticle' } as NamedNode,
	/** A store that sells reading glasses and similar devices for improving vision. */
	'Optician': { termType: 'NamedNode', value: 'https://schema.org/Optician', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Optician' } as NamedNode,
	/** The science or practice of testing visual acuity and prescribing corrective lenses. */
	'Optometric': { termType: 'NamedNode', value: 'https://schema.org/Optometric', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Optometric' } as NamedNode,
	/** An order is a confirmation of a transaction (a receipt), which can contain multiple line items, each represented by an Offer that has been accepted by the customer. */
	'Order': { termType: 'NamedNode', value: 'https://schema.org/Order', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Order' } as NamedNode,
	/** An agent orders an object/product/service to be delivered/sent. */
	'OrderAction': { termType: 'NamedNode', value: 'https://schema.org/OrderAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/OrderAction' } as NamedNode,
	/** OrderStatus representing cancellation of an order. */
	'OrderCancelled': { termType: 'NamedNode', value: 'https://schema.org/OrderCancelled', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/OrderCancelled' } as NamedNode,
	/** OrderStatus representing successful delivery of an order. */
	'OrderDelivered': { termType: 'NamedNode', value: 'https://schema.org/OrderDelivered', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/OrderDelivered' } as NamedNode,
	/** OrderStatus representing that an order is in transit. */
	'OrderInTransit': { termType: 'NamedNode', value: 'https://schema.org/OrderInTransit', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/OrderInTransit' } as NamedNode,
	/** An order item is a line of an order. It includes the quantity and shipping details of a bought offer. */
	'OrderItem': { termType: 'NamedNode', value: 'https://schema.org/OrderItem', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/OrderItem' } as NamedNode,
	/** OrderStatus representing that payment is due on an order. */
	'OrderPaymentDue': { termType: 'NamedNode', value: 'https://schema.org/OrderPaymentDue', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/OrderPaymentDue' } as NamedNode,
	/** OrderStatus representing availability of an order for pickup. */
	'OrderPickupAvailable': { termType: 'NamedNode', value: 'https://schema.org/OrderPickupAvailable', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/OrderPickupAvailable' } as NamedNode,
	/** OrderStatus representing that there is a problem with the order. */
	'OrderProblem': { termType: 'NamedNode', value: 'https://schema.org/OrderProblem', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/OrderProblem' } as NamedNode,
	/** OrderStatus representing that an order is being processed. */
	'OrderProcessing': { termType: 'NamedNode', value: 'https://schema.org/OrderProcessing', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/OrderProcessing' } as NamedNode,
	/** OrderStatus representing that an order has been returned. */
	'OrderReturned': { termType: 'NamedNode', value: 'https://schema.org/OrderReturned', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/OrderReturned' } as NamedNode,
	/** Enumerated status values for Order. */
	'OrderStatus': { termType: 'NamedNode', value: 'https://schema.org/OrderStatus', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/OrderStatus' } as NamedNode,
	/** An organization such as a school, NGO, corporation, club, etc. */
	'Organization': { termType: 'NamedNode', value: 'https://schema.org/Organization', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Organization' } as NamedNode,
	/** A subclass of Role used to describe roles within organizations. */
	'OrganizationRole': { termType: 'NamedNode', value: 'https://schema.org/OrganizationRole', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/OrganizationRole' } as NamedNode,
	/** The act of manipulating/administering/supervising/controlling one or more objects. */
	'OrganizeAction': { termType: 'NamedNode', value: 'https://schema.org/OrganizeAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/OrganizeAction' } as NamedNode,
	/** Content coded 'as original media content' in a [[MediaReview]], considered in the context of how it was published or shared.

For a [[VideoObject]] to be 'original': No evidence the footage has been misleadingly altered or manipulated, though it may contain false or misleading claims.

For an [[ImageObject]] to be 'original': No evidence the image has been misleadingly altered or manipulated, though it may still contain false or misleading claims.

For an [[ImageObject]] with embedded text to be 'original': No evidence the image has been misleadingly altered or manipulated, though it may still contain false or misleading claims.

For an [[AudioObject]] to be 'original': No evidence the audio has been misleadingly altered or manipulated, though it may contain false or misleading claims.
 */
	'OriginalMediaContent': { termType: 'NamedNode', value: 'https://schema.org/OriginalMediaContent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/OriginalMediaContent' } as NamedNode,
	/** Specifies that the customer must pay the original shipping costs when returning a product. */
	'OriginalShippingFees': { termType: 'NamedNode', value: 'https://schema.org/OriginalShippingFees', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/OriginalShippingFees' } as NamedNode,
	/** A system of medicine focused on promoting the body's innate ability to heal itself. */
	'Osteopathic': { termType: 'NamedNode', value: 'https://schema.org/Osteopathic', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Osteopathic' } as NamedNode,
	/** A specific branch of medical science that is concerned with the ear, nose and throat and their respective disease states. */
	'Otolaryngologic': { termType: 'NamedNode', value: 'https://schema.org/Otolaryngologic', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Otolaryngologic' } as NamedNode,
	/** Indicates that the item is out of stock. */
	'OutOfStock': { termType: 'NamedNode', value: 'https://schema.org/OutOfStock', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/OutOfStock' } as NamedNode,
	/** An outlet store. */
	'OutletStore': { termType: 'NamedNode', value: 'https://schema.org/OutletStore', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/OutletStore' } as NamedNode,
	/** Overview of the content. Contains a summarized view of the topic with the most relevant information for an introduction. */
	'OverviewHealthAspect': { termType: 'NamedNode', value: 'https://schema.org/OverviewHealthAspect', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/OverviewHealthAspect' } as NamedNode,
	/** A structured value providing information about when a certain organization or person owned a certain product. */
	'OwnershipInfo': { termType: 'NamedNode', value: 'https://schema.org/OwnershipInfo', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/OwnershipInfo' } as NamedNode,
	/** Positron emission tomography imaging. */
	'PET': { termType: 'NamedNode', value: 'https://schema.org/PET', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PET' } as NamedNode,
	/** PaidLeave: this is a benefit for paid leave. */
	'PaidLeave': { termType: 'NamedNode', value: 'https://schema.org/PaidLeave', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PaidLeave' } as NamedNode,
	/** The act of producing a painting, typically with paint and canvas as instruments. */
	'PaintAction': { termType: 'NamedNode', value: 'https://schema.org/PaintAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PaintAction' } as NamedNode,
	/** A painting. */
	'Painting': { termType: 'NamedNode', value: 'https://schema.org/Painting', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Painting' } as NamedNode,
	/** A medical procedure intended primarily for palliative purposes, aimed at relieving the symptoms of an underlying health condition. */
	'PalliativeProcedure': { termType: 'NamedNode', value: 'https://schema.org/PalliativeProcedure', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PalliativeProcedure' } as NamedNode,
	/** Book format: Paperback. */
	'Paperback': { termType: 'NamedNode', value: 'https://schema.org/Paperback', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Paperback' } as NamedNode,
	/** The delivery of a parcel either via the postal service or a commercial service. */
	'ParcelDelivery': { termType: 'NamedNode', value: 'https://schema.org/ParcelDelivery', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ParcelDelivery' } as NamedNode,
	/** A private parcel service as the delivery mode available for a certain offer.\n\nCommonly used values:\n\n* http://purl.org/goodrelations/v1#DHL\n* http://purl.org/goodrelations/v1#FederalExpress\n* http://purl.org/goodrelations/v1#UPS
       */
	'ParcelService': { termType: 'NamedNode', value: 'https://schema.org/ParcelService', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ParcelService' } as NamedNode,
	/** A set of characteristics describing parents, who can be interested in viewing some content. */
	'ParentAudience': { termType: 'NamedNode', value: 'https://schema.org/ParentAudience', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ParentAudience' } as NamedNode,
	/** ParentalSupport: this is a benefit for parental support. */
	'ParentalSupport': { termType: 'NamedNode', value: 'https://schema.org/ParentalSupport', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ParentalSupport' } as NamedNode,
	/** A park. */
	'Park': { termType: 'NamedNode', value: 'https://schema.org/Park', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Park' } as NamedNode,
	/** A parking lot or other parking facility. */
	'ParkingFacility': { termType: 'NamedNode', value: 'https://schema.org/ParkingFacility', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ParkingFacility' } as NamedNode,
	/** A parking map. */
	'ParkingMap': { termType: 'NamedNode', value: 'https://schema.org/ParkingMap', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ParkingMap' } as NamedNode,
	/** Indicates that parts of the legislation are in force, and parts are not. */
	'PartiallyInForce': { termType: 'NamedNode', value: 'https://schema.org/PartiallyInForce', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PartiallyInForce' } as NamedNode,
	/** A specific branch of medical science that is concerned with the study of the cause, origin and nature of a disease state, including its consequences as a result of manifestation of the disease. In clinical care, the term is used to designate a branch of medicine using laboratory tests to diagnose and determine the prognostic significance of illness. */
	'Pathology': { termType: 'NamedNode', value: 'https://schema.org/Pathology', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Pathology' } as NamedNode,
	/** A medical test performed by a laboratory that typically involves examination of a tissue sample by a pathologist. */
	'PathologyTest': { termType: 'NamedNode', value: 'https://schema.org/PathologyTest', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PathologyTest' } as NamedNode,
	/** A patient is any person recipient of health care services. */
	'Patient': { termType: 'NamedNode', value: 'https://schema.org/Patient', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Patient' } as NamedNode,
	/** Content about the real life experience of patients or people that have lived a similar experience about the topic. May be forums, topics, Q-and-A and related material. */
	'PatientExperienceHealthAspect': { termType: 'NamedNode', value: 'https://schema.org/PatientExperienceHealthAspect', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PatientExperienceHealthAspect' } as NamedNode,
	/** A shop that will buy, or lend money against the security of, personal possessions. */
	'PawnShop': { termType: 'NamedNode', value: 'https://schema.org/PawnShop', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PawnShop' } as NamedNode,
	/** An agent pays a price to a participant. */
	'PayAction': { termType: 'NamedNode', value: 'https://schema.org/PayAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PayAction' } as NamedNode,
	/** An automatic payment system is in place and will be used. */
	'PaymentAutomaticallyApplied': { termType: 'NamedNode', value: 'https://schema.org/PaymentAutomaticallyApplied', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PaymentAutomaticallyApplied' } as NamedNode,
	/** A payment method using a credit, debit, store or other card to associate the payment with an account. */
	'PaymentCard': { termType: 'NamedNode', value: 'https://schema.org/PaymentCard', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PaymentCard' } as NamedNode,
	/** The costs of settling the payment using a particular payment method. */
	'PaymentChargeSpecification': { termType: 'NamedNode', value: 'https://schema.org/PaymentChargeSpecification', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PaymentChargeSpecification' } as NamedNode,
	/** The payment has been received and processed. */
	'PaymentComplete': { termType: 'NamedNode', value: 'https://schema.org/PaymentComplete', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PaymentComplete' } as NamedNode,
	/** The payee received the payment, but it was declined for some reason. */
	'PaymentDeclined': { termType: 'NamedNode', value: 'https://schema.org/PaymentDeclined', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PaymentDeclined' } as NamedNode,
	/** The payment is due, but still within an acceptable time to be received. */
	'PaymentDue': { termType: 'NamedNode', value: 'https://schema.org/PaymentDue', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PaymentDue' } as NamedNode,
	/** A payment method is a standardized procedure for transferring the monetary amount for a purchase. Payment methods are characterized by the legal and technical structures used, and by the organization or group carrying out the transaction.\n\nCommonly used values:\n\n* http://purl.org/goodrelations/v1#ByBankTransferInAdvance\n* http://purl.org/goodrelations/v1#ByInvoice\n* http://purl.org/goodrelations/v1#Cash\n* http://purl.org/goodrelations/v1#CheckInAdvance\n* http://purl.org/goodrelations/v1#COD\n* http://purl.org/goodrelations/v1#DirectDebit\n* http://purl.org/goodrelations/v1#GoogleCheckout\n* http://purl.org/goodrelations/v1#PayPal\n* http://purl.org/goodrelations/v1#PaySwarm
         */
	'PaymentMethod': { termType: 'NamedNode', value: 'https://schema.org/PaymentMethod', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PaymentMethod' } as NamedNode,
	/** The payment is due and considered late. */
	'PaymentPastDue': { termType: 'NamedNode', value: 'https://schema.org/PaymentPastDue', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PaymentPastDue' } as NamedNode,
	/** A Service to transfer funds from a person or organization to a beneficiary person or organization. */
	'PaymentService': { termType: 'NamedNode', value: 'https://schema.org/PaymentService', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PaymentService' } as NamedNode,
	/** A specific payment status. For example, PaymentDue, PaymentComplete, etc. */
	'PaymentStatusType': { termType: 'NamedNode', value: 'https://schema.org/PaymentStatusType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PaymentStatusType' } as NamedNode,
	/** A specific branch of medical science that specializes in the care of infants, children and adolescents. */
	'Pediatric': { termType: 'NamedNode', value: 'https://schema.org/Pediatric', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Pediatric' } as NamedNode,
	/** A set of characteristics belonging to people, e.g. who compose an item's target audience. */
	'PeopleAudience': { termType: 'NamedNode', value: 'https://schema.org/PeopleAudience', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PeopleAudience' } as NamedNode,
	/** A type of medical procedure that involves percutaneous techniques, where access to organs or tissue is achieved via needle-puncture of the skin. For example, catheter-based procedures like stent delivery. */
	'PercutaneousProcedure': { termType: 'NamedNode', value: 'https://schema.org/PercutaneousProcedure', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PercutaneousProcedure' } as NamedNode,
	/** The act of participating in performance arts. */
	'PerformAction': { termType: 'NamedNode', value: 'https://schema.org/PerformAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PerformAction' } as NamedNode,
	/** A PerformanceRole is a Role that some entity places with regard to a theatrical performance, e.g. in a Movie, TVSeries etc. */
	'PerformanceRole': { termType: 'NamedNode', value: 'https://schema.org/PerformanceRole', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PerformanceRole' } as NamedNode,
	/** A theater or other performing art center. */
	'PerformingArtsTheater': { termType: 'NamedNode', value: 'https://schema.org/PerformingArtsTheater', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PerformingArtsTheater' } as NamedNode,
	/** A performance group, such as a band, an orchestra, or a circus. */
	'PerformingGroup': { termType: 'NamedNode', value: 'https://schema.org/PerformingGroup', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PerformingGroup' } as NamedNode,
	/** A publication in any medium issued in successive parts bearing numerical or chronological designations and intended to continue indefinitely, such as a magazine, scholarly journal, or newspaper.\n\nSee also [blog post](http://blog.schema.org/2014/09/schemaorg-support-for-bibliographic_2.html). */
	'Periodical': { termType: 'NamedNode', value: 'https://schema.org/Periodical', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Periodical' } as NamedNode,
	/** A permit issued by an organization, e.g. a parking pass. */
	'Permit': { termType: 'NamedNode', value: 'https://schema.org/Permit', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Permit' } as NamedNode,
	/** A person (alive, dead, undead, or fictional). */
	'Person': { termType: 'NamedNode', value: 'https://schema.org/Person', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Person' } as NamedNode,
	/** A pet store. */
	'PetStore': { termType: 'NamedNode', value: 'https://schema.org/PetStore', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PetStore' } as NamedNode,
	/** A pharmacy or drugstore. */
	'Pharmacy': { termType: 'NamedNode', value: 'https://schema.org/Pharmacy', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Pharmacy' } as NamedNode,
	/** The practice or art and science of preparing and dispensing drugs and medicines. */
	'PharmacySpecialty': { termType: 'NamedNode', value: 'https://schema.org/PharmacySpecialty', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PharmacySpecialty' } as NamedNode,
	/** A photograph. */
	'Photograph': { termType: 'NamedNode', value: 'https://schema.org/Photograph', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Photograph' } as NamedNode,
	/** The act of capturing still images of objects using a camera. */
	'PhotographAction': { termType: 'NamedNode', value: 'https://schema.org/PhotographAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PhotographAction' } as NamedNode,
	/** Any bodily activity that enhances or maintains physical fitness and overall health and wellness. Includes activity that is part of daily living and routine, structured exercise, and exercise prescribed as part of a medical treatment or recovery plan. */
	'PhysicalActivity': { termType: 'NamedNode', value: 'https://schema.org/PhysicalActivity', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PhysicalActivity' } as NamedNode,
	/** Categories of physical activity, organized by physiologic classification. */
	'PhysicalActivityCategory': { termType: 'NamedNode', value: 'https://schema.org/PhysicalActivityCategory', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PhysicalActivityCategory' } as NamedNode,
	/** A type of physical examination of a patient performed by a physician.  */
	'PhysicalExam': { termType: 'NamedNode', value: 'https://schema.org/PhysicalExam', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PhysicalExam' } as NamedNode,
	/** A process of progressive physical care and rehabilitation aimed at improving a health condition. */
	'PhysicalTherapy': { termType: 'NamedNode', value: 'https://schema.org/PhysicalTherapy', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PhysicalTherapy' } as NamedNode,
	/** A doctor's office. */
	'Physician': { termType: 'NamedNode', value: 'https://schema.org/Physician', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Physician' } as NamedNode,
	/** The practice of treatment of disease, injury, or deformity by physical methods such as massage, heat treatment, and exercise rather than by drugs or surgery. */
	'Physiotherapy': { termType: 'NamedNode', value: 'https://schema.org/Physiotherapy', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Physiotherapy' } as NamedNode,
	/** Entities that have a somewhat fixed, physical extension. */
	'Place': { termType: 'NamedNode', value: 'https://schema.org/Place', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Place' } as NamedNode,
	/** Place of worship, such as a church, synagogue, or mosque. */
	'PlaceOfWorship': { termType: 'NamedNode', value: 'https://schema.org/PlaceOfWorship', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PlaceOfWorship' } as NamedNode,
	/** A placebo-controlled trial design. */
	'PlaceboControlledTrial': { termType: 'NamedNode', value: 'https://schema.org/PlaceboControlledTrial', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PlaceboControlledTrial' } as NamedNode,
	/** The act of planning the execution of an event/task/action/reservation/plan to a future date. */
	'PlanAction': { termType: 'NamedNode', value: 'https://schema.org/PlanAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PlanAction' } as NamedNode,
	/** A specific branch of medical science that pertains to therapeutic or cosmetic repair or re-formation of missing, injured or malformed tissues or body parts by manual and instrumental means. */
	'PlasticSurgery': { termType: 'NamedNode', value: 'https://schema.org/PlasticSurgery', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PlasticSurgery' } as NamedNode,
	/** A play is a form of literature, usually consisting of dialogue between characters, intended for theatrical performance rather than just reading. Note: A performance of a Play would be a [[TheaterEvent]] or [[BroadcastEvent]] - the *Play* being the [[workPerformed]]. */
	'Play': { termType: 'NamedNode', value: 'https://schema.org/Play', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Play' } as NamedNode,
	/** The act of playing/exercising/training/performing for enjoyment, leisure, recreation, competition or exercise.\n\nRelated actions:\n\n* [[ListenAction]]: Unlike ListenAction (which is under ConsumeAction), PlayAction refers to performing for an audience or at an event, rather than consuming music.\n* [[WatchAction]]: Unlike WatchAction (which is under ConsumeAction), PlayAction refers to showing/displaying for an audience or at an event, rather than consuming visual content. */
	'PlayAction': { termType: 'NamedNode', value: 'https://schema.org/PlayAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PlayAction' } as NamedNode,
	/** The act of playing a video game. */
	'PlayGameAction': { termType: 'NamedNode', value: 'https://schema.org/PlayGameAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PlayGameAction' } as NamedNode,
	/** A playground. */
	'Playground': { termType: 'NamedNode', value: 'https://schema.org/Playground', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Playground' } as NamedNode,
	/** A plumbing service. */
	'Plumber': { termType: 'NamedNode', value: 'https://schema.org/Plumber', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Plumber' } as NamedNode,
	/** A single episode of a podcast series. */
	'PodcastEpisode': { termType: 'NamedNode', value: 'https://schema.org/PodcastEpisode', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PodcastEpisode' } as NamedNode,
	/** A single season of a podcast. Many podcasts do not break down into separate seasons. In that case, PodcastSeries should be used. */
	'PodcastSeason': { termType: 'NamedNode', value: 'https://schema.org/PodcastSeason', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PodcastSeason' } as NamedNode,
	/** A podcast is an episodic series of digital audio or video files which a user can download and listen to. */
	'PodcastSeries': { termType: 'NamedNode', value: 'https://schema.org/PodcastSeries', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PodcastSeries' } as NamedNode,
	/** Podiatry is the care of the human foot, especially the diagnosis and treatment of foot disorders. */
	'Podiatric': { termType: 'NamedNode', value: 'https://schema.org/Podiatric', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Podiatric' } as NamedNode,
	/** A police station. */
	'PoliceStation': { termType: 'NamedNode', value: 'https://schema.org/PoliceStation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PoliceStation' } as NamedNode,
	/** Organization: Political Party. */
	'PoliticalParty': { termType: 'NamedNode', value: 'https://schema.org/PoliticalParty', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PoliticalParty' } as NamedNode,
	/** A pond. */
	'Pond': { termType: 'NamedNode', value: 'https://schema.org/Pond', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Pond' } as NamedNode,
	/** A post office. */
	'PostOffice': { termType: 'NamedNode', value: 'https://schema.org/PostOffice', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PostOffice' } as NamedNode,
	/** The mailing address. */
	'PostalAddress': { termType: 'NamedNode', value: 'https://schema.org/PostalAddress', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PostalAddress' } as NamedNode,
	/** Indicates a range of postal codes, usually defined as the set of valid codes between [[postalCodeBegin]] and [[postalCodeEnd]], inclusively. */
	'PostalCodeRangeSpecification': { termType: 'NamedNode', value: 'https://schema.org/PostalCodeRangeSpecification', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PostalCodeRangeSpecification' } as NamedNode,
	/** A large, usually printed placard, bill, or announcement, often illustrated, that is posted to advertise or publicize something. */
	'Poster': { termType: 'NamedNode', value: 'https://schema.org/Poster', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Poster' } as NamedNode,
	/** A description of an action that is supported. */
	'PotentialActionStatus': { termType: 'NamedNode', value: 'https://schema.org/PotentialActionStatus', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PotentialActionStatus' } as NamedNode,
	/** Indicates that the item is available for pre-order. */
	'PreOrder': { termType: 'NamedNode', value: 'https://schema.org/PreOrder', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PreOrder' } as NamedNode,
	/** An agent orders a (not yet released) object/product/service to be delivered/sent. */
	'PreOrderAction': { termType: 'NamedNode', value: 'https://schema.org/PreOrderAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PreOrderAction' } as NamedNode,
	/** Indicates that the item is available for ordering and delivery before general availability. */
	'PreSale': { termType: 'NamedNode', value: 'https://schema.org/PreSale', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PreSale' } as NamedNode,
	/** Content discussing pregnancy-related aspects of a health topic. */
	'PregnancyHealthAspect': { termType: 'NamedNode', value: 'https://schema.org/PregnancyHealthAspect', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PregnancyHealthAspect' } as NamedNode,
	/** The act of inserting at the beginning if an ordered collection. */
	'PrependAction': { termType: 'NamedNode', value: 'https://schema.org/PrependAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PrependAction' } as NamedNode,
	/** A preschool. */
	'Preschool': { termType: 'NamedNode', value: 'https://schema.org/Preschool', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Preschool' } as NamedNode,
	/** Available by prescription only. */
	'PrescriptionOnly': { termType: 'NamedNode', value: 'https://schema.org/PrescriptionOnly', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PrescriptionOnly' } as NamedNode,
	/** A file containing slides or used for a presentation. */
	'PresentationDigitalDocument': { termType: 'NamedNode', value: 'https://schema.org/PresentationDigitalDocument', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PresentationDigitalDocument' } as NamedNode,
	/** Information about actions or measures that can be taken to avoid getting the topic or reaching a critical situation related to the topic. */
	'PreventionHealthAspect': { termType: 'NamedNode', value: 'https://schema.org/PreventionHealthAspect', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PreventionHealthAspect' } as NamedNode,
	/** An indication for preventing an underlying condition, symptom, etc. */
	'PreventionIndication': { termType: 'NamedNode', value: 'https://schema.org/PreventionIndication', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PreventionIndication' } as NamedNode,
	/** Enumerates different price components that together make up the total price for an offered product. */
	'PriceComponentTypeEnumeration': { termType: 'NamedNode', value: 'https://schema.org/PriceComponentTypeEnumeration', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PriceComponentTypeEnumeration' } as NamedNode,
	/** A structured value representing a price or price range. Typically, only the subclasses of this type are used for markup. It is recommended to use [[MonetaryAmount]] to describe independent amounts of money such as a salary, credit card limits, etc. */
	'PriceSpecification': { termType: 'NamedNode', value: 'https://schema.org/PriceSpecification', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PriceSpecification' } as NamedNode,
	/** Enumerates different price types, for example list price, invoice price, and sale price. */
	'PriceTypeEnumeration': { termType: 'NamedNode', value: 'https://schema.org/PriceTypeEnumeration', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PriceTypeEnumeration' } as NamedNode,
	/** The medical care by a physician, or other health-care professional, who is the patient's first contact with the health-care system and who may recommend a specialist if necessary. */
	'PrimaryCare': { termType: 'NamedNode', value: 'https://schema.org/PrimaryCare', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PrimaryCare' } as NamedNode,
	/** A prion is an infectious agent composed of protein in a misfolded form. */
	'Prion': { termType: 'NamedNode', value: 'https://schema.org/Prion', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Prion' } as NamedNode,
	/** Any offered product or service. For example: a pair of shoes; a concert ticket; the rental of a car; a haircut; or an episode of a TV show streamed online. */
	'Product': { termType: 'NamedNode', value: 'https://schema.org/Product', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Product' } as NamedNode,
	/** A set of products (either [[ProductGroup]]s or specific variants) that are listed together e.g. in an [[Offer]]. */
	'ProductCollection': { termType: 'NamedNode', value: 'https://schema.org/ProductCollection', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ProductCollection' } as NamedNode,
	/** A ProductGroup represents a group of [[Product]]s that vary only in certain well-described ways, such as by [[size]], [[color]], [[material]] etc.

While a ProductGroup itself is not directly offered for sale, the various varying products that it represents can be. The ProductGroup serves as a prototype or template, standing in for all of the products who have an [[isVariantOf]] relationship to it. As such, properties (including additional types) can be applied to the ProductGroup to represent characteristics shared by each of the (possibly very many) variants. Properties that reference a ProductGroup are not included in this mechanism; neither are the following specific properties [[variesBy]], [[hasVariant]], [[url]].  */
	'ProductGroup': { termType: 'NamedNode', value: 'https://schema.org/ProductGroup', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ProductGroup' } as NamedNode,
	/** A datasheet or vendor specification of a product (in the sense of a prototypical description). */
	'ProductModel': { termType: 'NamedNode', value: 'https://schema.org/ProductModel', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ProductModel' } as NamedNode,
	/** Original definition: "provider of professional services."\n\nThe general [[ProfessionalService]] type for local businesses was deprecated due to confusion with [[Service]]. For reference, the types that it included were: [[Dentist]],
        [[AccountingService]], [[Attorney]], [[Notary]], as well as types for several kinds of [[HomeAndConstructionBusiness]]: [[Electrician]], [[GeneralContractor]],
        [[HousePainter]], [[Locksmith]], [[Plumber]], [[RoofingContractor]]. [[LegalService]] was introduced as a more inclusive supertype of [[Attorney]]. */
	'ProfessionalService': { termType: 'NamedNode', value: 'https://schema.org/ProfessionalService', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ProfessionalService' } as NamedNode,
	/** Web page type: Profile page. */
	'ProfilePage': { termType: 'NamedNode', value: 'https://schema.org/ProfilePage', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ProfilePage' } as NamedNode,
	/** Typical progression and happenings of life course of the topic. */
	'PrognosisHealthAspect': { termType: 'NamedNode', value: 'https://schema.org/PrognosisHealthAspect', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PrognosisHealthAspect' } as NamedNode,
	/** Used to describe membership in a loyalty programs (e.g. "StarAliance"), traveler clubs (e.g. "AAA"), purchase clubs ("Safeway Club"), etc. */
	'ProgramMembership': { termType: 'NamedNode', value: 'https://schema.org/ProgramMembership', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ProgramMembership' } as NamedNode,
	/** An enterprise (potentially individual but typically collaborative), planned to achieve a particular aim.
Use properties from [[Organization]], [[subOrganization]]/[[parentOrganization]] to indicate project sub-structures. 
    */
	'Project': { termType: 'NamedNode', value: 'https://schema.org/Project', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Project' } as NamedNode,
	/** Data type: PronounceableText. */
	'PronounceableText': { termType: 'NamedNode', value: 'https://schema.org/PronounceableText', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PronounceableText' } as NamedNode,
	/** A property, used to indicate attributes and relationships of some Thing; equivalent to rdf:Property. */
	'Property': { termType: 'NamedNode', value: 'https://schema.org/Property', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Property' } as NamedNode,
	/** A property-value pair, e.g. representing a feature of a product or place. Use the 'name' property for the name of the property. If there is an additional human-readable version of the value, put that into the 'description' property.\n\n Always use specific schema.org properties when a) they exist and b) you can populate them. Using PropertyValue as a substitute will typically not trigger the same effect as using the original, specific property.
     */
	'PropertyValue': { termType: 'NamedNode', value: 'https://schema.org/PropertyValue', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PropertyValue' } as NamedNode,
	/** A Property value specification. */
	'PropertyValueSpecification': { termType: 'NamedNode', value: 'https://schema.org/PropertyValueSpecification', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PropertyValueSpecification' } as NamedNode,
	/** Protein is here used in its widest possible definition, as classes of amino acid based molecules. Amyloid-beta Protein in human (UniProt P05067), eukaryota (e.g. an OrthoDB group) or even a single molecule that one can point to are all of type :Protein. A protein can thus be a subclass of another protein, e.g. :Protein as a UniProt record can have multiple isoforms inside it which would also be :Protein. They can be imagined, synthetic, hypothetical or naturally occurring. */
	'Protein': { termType: 'NamedNode', value: 'https://schema.org/Protein', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Protein' } as NamedNode,
	/** Single-celled organism that causes an infection. */
	'Protozoa': { termType: 'NamedNode', value: 'https://schema.org/Protozoa', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Protozoa' } as NamedNode,
	/** A specific branch of medical science that is concerned with the study, treatment, and prevention of mental illness, using both medical and psychological therapies. */
	'Psychiatric': { termType: 'NamedNode', value: 'https://schema.org/Psychiatric', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Psychiatric' } as NamedNode,
	/** A process of care relying upon counseling, dialogue and communication  aimed at improving a mental health condition without use of drugs. */
	'PsychologicalTreatment': { termType: 'NamedNode', value: 'https://schema.org/PsychologicalTreatment', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PsychologicalTreatment' } as NamedNode,
	/** Branch of medicine that pertains to the health services to improve and protect community health, especially epidemiology, sanitation, immunization, and preventive medicine. */
	'PublicHealth': { termType: 'NamedNode', value: 'https://schema.org/PublicHealth', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PublicHealth' } as NamedNode,
	/** This stands for any day that is a public holiday; it is a placeholder for all official public holidays in some particular location. While not technically a "day of the week", it can be used with [[OpeningHoursSpecification]]. In the context of an opening hours specification it can be used to indicate opening hours on public holidays, overriding general opening hours for the day of the week on which a public holiday occurs. */
	'PublicHolidays': { termType: 'NamedNode', value: 'https://schema.org/PublicHolidays', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PublicHolidays' } as NamedNode,
	/** A public swimming pool. */
	'PublicSwimmingPool': { termType: 'NamedNode', value: 'https://schema.org/PublicSwimmingPool', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PublicSwimmingPool' } as NamedNode,
	/** A public toilet is a room or small building containing one or more toilets (and possibly also urinals) which is available for use by the general public, or by customers or employees of certain businesses. */
	'PublicToilet': { termType: 'NamedNode', value: 'https://schema.org/PublicToilet', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PublicToilet' } as NamedNode,
	/** A PublicationEvent corresponds indifferently to the event of publication for a CreativeWork of any type, e.g. a broadcast event, an on-demand event, a book/journal publication via a variety of delivery media. */
	'PublicationEvent': { termType: 'NamedNode', value: 'https://schema.org/PublicationEvent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PublicationEvent' } as NamedNode,
	/** A part of a successively published publication such as a periodical or publication volume, often numbered, usually containing a grouping of works such as articles.\n\nSee also [blog post](http://blog.schema.org/2014/09/schemaorg-support-for-bibliographic_2.html). */
	'PublicationIssue': { termType: 'NamedNode', value: 'https://schema.org/PublicationIssue', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PublicationIssue' } as NamedNode,
	/** A part of a successively published publication such as a periodical or multi-volume work, often numbered. It may represent a time span, such as a year.\n\nSee also [blog post](http://blog.schema.org/2014/09/schemaorg-support-for-bibliographic_2.html). */
	'PublicationVolume': { termType: 'NamedNode', value: 'https://schema.org/PublicationVolume', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/PublicationVolume' } as NamedNode,
	/** A specific branch of medical science that pertains to the study of the respiratory system and its respective disease states. */
	'Pulmonary': { termType: 'NamedNode', value: 'https://schema.org/Pulmonary', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Pulmonary' } as NamedNode,
	/** A QAPage is a WebPage focussed on a specific Question and its Answer(s), e.g. in a question answering site or documenting Frequently Asked Questions (FAQs). */
	'QAPage': { termType: 'NamedNode', value: 'https://schema.org/QAPage', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/QAPage' } as NamedNode,
	/** A predefined value for a product characteristic, e.g. the power cord plug type 'US' or the garment sizes 'S', 'M', 'L', and 'XL'. */
	'QualitativeValue': { termType: 'NamedNode', value: 'https://schema.org/QualitativeValue', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/QualitativeValue' } as NamedNode,
	/**  A point value or interval for product characteristics and other purposes. */
	'QuantitativeValue': { termType: 'NamedNode', value: 'https://schema.org/QuantitativeValue', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/QuantitativeValue' } as NamedNode,
	/** A statistical distribution of values. */
	'QuantitativeValueDistribution': { termType: 'NamedNode', value: 'https://schema.org/QuantitativeValueDistribution', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/QuantitativeValueDistribution' } as NamedNode,
	/** Quantities such as distance, time, mass, weight, etc. Particular instances of say Mass are entities like '3 kg' or '4 milligrams'. */
	'Quantity': { termType: 'NamedNode', value: 'https://schema.org/Quantity', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Quantity' } as NamedNode,
	/** A specific question - e.g. from a user seeking answers online, or collected in a Frequently Asked Questions (FAQ) document. */
	'Question': { termType: 'NamedNode', value: 'https://schema.org/Question', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Question' } as NamedNode,
	/** Quiz: A test of knowledge, skills and abilities. */
	'Quiz': { termType: 'NamedNode', value: 'https://schema.org/Quiz', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Quiz' } as NamedNode,
	/** A quotation. Often but not necessarily from some written work, attributable to a real world author and - if associated with a fictional character - to any fictional Person. Use [[isBasedOn]] to link to source/origin. The [[recordedIn]] property can be used to reference a Quotation from an [[Event]]. */
	'Quotation': { termType: 'NamedNode', value: 'https://schema.org/Quotation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Quotation' } as NamedNode,
	/** An agent quotes/estimates/appraises an object/product/service with a price at a location/store. */
	'QuoteAction': { termType: 'NamedNode', value: 'https://schema.org/QuoteAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/QuoteAction' } as NamedNode,
	/** A place offering space for "Recreational Vehicles", Caravans, mobile homes and the like. */
	'RVPark': { termType: 'NamedNode', value: 'https://schema.org/RVPark', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/RVPark' } as NamedNode,
	/** A process of care using radiation aimed at improving a health condition. */
	'RadiationTherapy': { termType: 'NamedNode', value: 'https://schema.org/RadiationTherapy', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/RadiationTherapy' } as NamedNode,
	/** A delivery service through which radio content is provided via broadcast over the air or online. */
	'RadioBroadcastService': { termType: 'NamedNode', value: 'https://schema.org/RadioBroadcastService', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/RadioBroadcastService' } as NamedNode,
	/** A unique instance of a radio BroadcastService on a CableOrSatelliteService lineup. */
	'RadioChannel': { termType: 'NamedNode', value: 'https://schema.org/RadioChannel', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/RadioChannel' } as NamedNode,
	/** A short radio program or a segment/part of a radio program. */
	'RadioClip': { termType: 'NamedNode', value: 'https://schema.org/RadioClip', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/RadioClip' } as NamedNode,
	/** A radio episode which can be part of a series or season. */
	'RadioEpisode': { termType: 'NamedNode', value: 'https://schema.org/RadioEpisode', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/RadioEpisode' } as NamedNode,
	/** Season dedicated to radio broadcast and associated online delivery. */
	'RadioSeason': { termType: 'NamedNode', value: 'https://schema.org/RadioSeason', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/RadioSeason' } as NamedNode,
	/** CreativeWorkSeries dedicated to radio broadcast and associated online delivery. */
	'RadioSeries': { termType: 'NamedNode', value: 'https://schema.org/RadioSeries', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/RadioSeries' } as NamedNode,
	/** A radio station. */
	'RadioStation': { termType: 'NamedNode', value: 'https://schema.org/RadioStation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/RadioStation' } as NamedNode,
	/** Radiography is an imaging technique that uses electromagnetic radiation other than visible light, especially X-rays, to view the internal structure of a non-uniformly composed and opaque object such as the human body. */
	'Radiography': { termType: 'NamedNode', value: 'https://schema.org/Radiography', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Radiography' } as NamedNode,
	/** A randomized trial design. */
	'RandomizedTrial': { termType: 'NamedNode', value: 'https://schema.org/RandomizedTrial', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/RandomizedTrial' } as NamedNode,
	/** A rating is an evaluation on a numeric scale, such as 1 to 5 stars. */
	'Rating': { termType: 'NamedNode', value: 'https://schema.org/Rating', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Rating' } as NamedNode,
	/** The act of responding instinctively and emotionally to an object, expressing a sentiment. */
	'ReactAction': { termType: 'NamedNode', value: 'https://schema.org/ReactAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ReactAction' } as NamedNode,
	/** The act of consuming written content. */
	'ReadAction': { termType: 'NamedNode', value: 'https://schema.org/ReadAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ReadAction' } as NamedNode,
	/** Permission to read or view the document. */
	'ReadPermission': { termType: 'NamedNode', value: 'https://schema.org/ReadPermission', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ReadPermission' } as NamedNode,
	/** A real-estate agent. */
	'RealEstateAgent': { termType: 'NamedNode', value: 'https://schema.org/RealEstateAgent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/RealEstateAgent' } as NamedNode,
	/** A [[RealEstateListing]] is a listing that describes one or more real-estate [[Offer]]s (whose [[businessFunction]] is typically to lease out, or to sell).
  The [[RealEstateListing]] type itself represents the overall listing, as manifested in some [[WebPage]].
   */
	'RealEstateListing': { termType: 'NamedNode', value: 'https://schema.org/RealEstateListing', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/RealEstateListing' } as NamedNode,
	/** Real-wheel drive is a transmission layout where the engine drives the rear wheels. */
	'RearWheelDriveConfiguration': { termType: 'NamedNode', value: 'https://schema.org/RearWheelDriveConfiguration', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/RearWheelDriveConfiguration' } as NamedNode,
	/** The act of physically/electronically taking delivery of an object that has been transferred from an origin to a destination. Reciprocal of SendAction.\n\nRelated actions:\n\n* [[SendAction]]: The reciprocal of ReceiveAction.\n* [[TakeAction]]: Unlike TakeAction, ReceiveAction does not imply that the ownership has been transferred (e.g. I can receive a package, but it does not mean the package is now mine). */
	'ReceiveAction': { termType: 'NamedNode', value: 'https://schema.org/ReceiveAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ReceiveAction' } as NamedNode,
	/** A recipe. For dietary restrictions covered by the recipe, a few common restrictions are enumerated via [[suitableForDiet]]. The [[keywords]] property can also be used to add more detail. */
	'Recipe': { termType: 'NamedNode', value: 'https://schema.org/Recipe', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Recipe' } as NamedNode,
	/** [[Recommendation]] is a type of [[Review]] that suggests or proposes something as the best option or best course of action. Recommendations may be for products or services, or other concrete things, as in the case of a ranked list or product guide. A [[Guide]] may list multiple recommendations for different categories. For example, in a [[Guide]] about which TVs to buy, the author may have several [[Recommendation]]s. */
	'Recommendation': { termType: 'NamedNode', value: 'https://schema.org/Recommendation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Recommendation' } as NamedNode,
	/** A recommended dosing schedule for a drug or supplement as prescribed or recommended by an authority or by the drug/supplement's manufacturer. Capture the recommending authority in the recognizingAuthority property of MedicalEntity. */
	'RecommendedDoseSchedule': { termType: 'NamedNode', value: 'https://schema.org/RecommendedDoseSchedule', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/RecommendedDoseSchedule' } as NamedNode,
	/** Recruiting participants. */
	'Recruiting': { termType: 'NamedNode', value: 'https://schema.org/Recruiting', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Recruiting' } as NamedNode,
	/** A recycling center. */
	'RecyclingCenter': { termType: 'NamedNode', value: 'https://schema.org/RecyclingCenter', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/RecyclingCenter' } as NamedNode,
	/** A general code for cases where relevance to children is reduced, e.g. adult education, mortgages, retirement-related products, etc. */
	'ReducedRelevanceForChildrenConsideration': { termType: 'NamedNode', value: 'https://schema.org/ReducedRelevanceForChildrenConsideration', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ReducedRelevanceForChildrenConsideration' } as NamedNode,
	/** Enumerates several kinds of product return refund types. */
	'RefundTypeEnumeration': { termType: 'NamedNode', value: 'https://schema.org/RefundTypeEnumeration', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/RefundTypeEnumeration' } as NamedNode,
	/** Indicates that the item is refurbished. */
	'RefurbishedCondition': { termType: 'NamedNode', value: 'https://schema.org/RefurbishedCondition', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/RefurbishedCondition' } as NamedNode,
	/** The act of registering to be a user of a service, product or web page.\n\nRelated actions:\n\n* [[JoinAction]]: Unlike JoinAction, RegisterAction implies you are registering to be a user of a service, *not* a group/team of people.\n* [[FollowAction]]: Unlike FollowAction, RegisterAction doesn't imply that the agent is expecting to poll for updates from the object.\n* [[SubscribeAction]]: Unlike SubscribeAction, RegisterAction doesn't imply that the agent is expecting updates from the object. */
	'RegisterAction': { termType: 'NamedNode', value: 'https://schema.org/RegisterAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/RegisterAction' } as NamedNode,
	/** A registry-based study design. */
	'Registry': { termType: 'NamedNode', value: 'https://schema.org/Registry', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Registry' } as NamedNode,
	/** The drug's cost represents the maximum reimbursement paid by an insurer for the drug. */
	'ReimbursementCap': { termType: 'NamedNode', value: 'https://schema.org/ReimbursementCap', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ReimbursementCap' } as NamedNode,
	/** The act of rejecting to/adopting an object.\n\nRelated actions:\n\n* [[AcceptAction]]: The antonym of RejectAction. */
	'RejectAction': { termType: 'NamedNode', value: 'https://schema.org/RejectAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/RejectAction' } as NamedNode,
	/** Other prominent or relevant topics tied to the main topic. */
	'RelatedTopicsHealthAspect': { termType: 'NamedNode', value: 'https://schema.org/RelatedTopicsHealthAspect', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/RelatedTopicsHealthAspect' } as NamedNode,
	/** RemixAlbum. */
	'RemixAlbum': { termType: 'NamedNode', value: 'https://schema.org/RemixAlbum', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/RemixAlbum' } as NamedNode,
	/** A specific branch of medical science that pertains to the study of the kidneys and its respective disease states. */
	'Renal': { termType: 'NamedNode', value: 'https://schema.org/Renal', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Renal' } as NamedNode,
	/** The act of giving money in return for temporary use, but not ownership, of an object such as a vehicle or property. For example, an agent rents a property from a landlord in exchange for a periodic payment. */
	'RentAction': { termType: 'NamedNode', value: 'https://schema.org/RentAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/RentAction' } as NamedNode,
	/** A reservation for a rental car.\n\nNote: This type is for information about actual reservations, e.g. in confirmation emails or HTML pages with individual confirmations of reservations. */
	'RentalCarReservation': { termType: 'NamedNode', value: 'https://schema.org/RentalCarReservation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/RentalCarReservation' } as NamedNode,
	/** Indicates the usage of the vehicle as a rental car. */
	'RentalVehicleUsage': { termType: 'NamedNode', value: 'https://schema.org/RentalVehicleUsage', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/RentalVehicleUsage' } as NamedNode,
	/** A structured value representing repayment. */
	'RepaymentSpecification': { termType: 'NamedNode', value: 'https://schema.org/RepaymentSpecification', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/RepaymentSpecification' } as NamedNode,
	/** The act of editing a recipient by replacing an old object with a new object. */
	'ReplaceAction': { termType: 'NamedNode', value: 'https://schema.org/ReplaceAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ReplaceAction' } as NamedNode,
	/** The act of responding to a question/message asked/sent by the object. Related to [[AskAction]].\n\nRelated actions:\n\n* [[AskAction]]: Appears generally as an origin of a ReplyAction. */
	'ReplyAction': { termType: 'NamedNode', value: 'https://schema.org/ReplyAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ReplyAction' } as NamedNode,
	/** A Report generated by governmental or non-governmental organization. */
	'Report': { termType: 'NamedNode', value: 'https://schema.org/Report', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Report' } as NamedNode,
	/** The [[ReportageNewsArticle]] type is a subtype of [[NewsArticle]] representing
 news articles which are the result of journalistic news reporting conventions.

In practice many news publishers produce a wide variety of article types, many of which might be considered a [[NewsArticle]] but not a [[ReportageNewsArticle]]. For example, opinion pieces, reviews, analysis, sponsored or satirical articles, or articles that combine several of these elements.

The [[ReportageNewsArticle]] type is based on a stricter ideal for "news" as a work of journalism, with articles based on factual information either observed or verified by the author, or reported and verified from knowledgeable sources.  This often includes perspectives from multiple viewpoints on a particular issue (distinguishing news reports from public relations or propaganda).  News reports in the [[ReportageNewsArticle]] sense de-emphasize the opinion of the author, with commentary and value judgements typically expressed elsewhere.

A [[ReportageNewsArticle]] which goes deeper into analysis can also be marked with an additional type of [[AnalysisNewsArticle]].
 */
	'ReportageNewsArticle': { termType: 'NamedNode', value: 'https://schema.org/ReportageNewsArticle', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ReportageNewsArticle' } as NamedNode,
	/** A patient-reported or observed dosing schedule for a drug or supplement. */
	'ReportedDoseSchedule': { termType: 'NamedNode', value: 'https://schema.org/ReportedDoseSchedule', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ReportedDoseSchedule' } as NamedNode,
	/** A Research Organization (e.g. scientific institute, research company). */
	'ResearchOrganization': { termType: 'NamedNode', value: 'https://schema.org/ResearchOrganization', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ResearchOrganization' } as NamedNode,
	/** A Research project. */
	'ResearchProject': { termType: 'NamedNode', value: 'https://schema.org/ResearchProject', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ResearchProject' } as NamedNode,
	/** Researchers. */
	'Researcher': { termType: 'NamedNode', value: 'https://schema.org/Researcher', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Researcher' } as NamedNode,
	/** Describes a reservation for travel, dining or an event. Some reservations require tickets. \n\nNote: This type is for information about actual reservations, e.g. in confirmation emails or HTML pages with individual confirmations of reservations. For offers of tickets, restaurant reservations, flights, or rental cars, use [[Offer]]. */
	'Reservation': { termType: 'NamedNode', value: 'https://schema.org/Reservation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Reservation' } as NamedNode,
	/** The status for a previously confirmed reservation that is now cancelled. */
	'ReservationCancelled': { termType: 'NamedNode', value: 'https://schema.org/ReservationCancelled', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ReservationCancelled' } as NamedNode,
	/** The status of a confirmed reservation. */
	'ReservationConfirmed': { termType: 'NamedNode', value: 'https://schema.org/ReservationConfirmed', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ReservationConfirmed' } as NamedNode,
	/** The status of a reservation on hold pending an update like credit card number or flight changes. */
	'ReservationHold': { termType: 'NamedNode', value: 'https://schema.org/ReservationHold', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ReservationHold' } as NamedNode,
	/** A group of multiple reservations with common values for all sub-reservations. */
	'ReservationPackage': { termType: 'NamedNode', value: 'https://schema.org/ReservationPackage', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ReservationPackage' } as NamedNode,
	/** The status of a reservation when a request has been sent, but not confirmed. */
	'ReservationPending': { termType: 'NamedNode', value: 'https://schema.org/ReservationPending', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ReservationPending' } as NamedNode,
	/** Enumerated status values for Reservation. */
	'ReservationStatusType': { termType: 'NamedNode', value: 'https://schema.org/ReservationStatusType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ReservationStatusType' } as NamedNode,
	/** Reserving a concrete object.\n\nRelated actions:\n\n* [[ScheduleAction]]: Unlike ScheduleAction, ReserveAction reserves concrete objects (e.g. a table, a hotel) towards a time slot / spatial allocation. */
	'ReserveAction': { termType: 'NamedNode', value: 'https://schema.org/ReserveAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ReserveAction' } as NamedNode,
	/** A reservoir of water, typically an artificially created lake, like the Lake Kariba reservoir. */
	'Reservoir': { termType: 'NamedNode', value: 'https://schema.org/Reservoir', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Reservoir' } as NamedNode,
	/** The place where a person lives. */
	'Residence': { termType: 'NamedNode', value: 'https://schema.org/Residence', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Residence' } as NamedNode,
	/** A resort is a place used for relaxation or recreation, attracting visitors for holidays or vacations. Resorts are places, towns or sometimes commercial establishments operated by a single company (source: Wikipedia, the free encyclopedia, see <a href="http://en.wikipedia.org/wiki/Resort">http://en.wikipedia.org/wiki/Resort</a>).
<br /><br />
See also the <a href="/docs/hotels.html">dedicated document on the use of schema.org for marking up hotels and other forms of accommodations</a>.
     */
	'Resort': { termType: 'NamedNode', value: 'https://schema.org/Resort', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Resort' } as NamedNode,
	/** The therapy that is concerned with the maintenance or improvement of respiratory function (as in patients with pulmonary disease). */
	'RespiratoryTherapy': { termType: 'NamedNode', value: 'https://schema.org/RespiratoryTherapy', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/RespiratoryTherapy' } as NamedNode,
	/** A restaurant. */
	'Restaurant': { termType: 'NamedNode', value: 'https://schema.org/Restaurant', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Restaurant' } as NamedNode,
	/** Specifies that the customer must pay a restocking fee when returning a product. */
	'RestockingFees': { termType: 'NamedNode', value: 'https://schema.org/RestockingFees', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/RestockingFees' } as NamedNode,
	/** A diet restricted to certain foods or preparations for cultural, religious, health or lifestyle reasons.  */
	'RestrictedDiet': { termType: 'NamedNode', value: 'https://schema.org/RestrictedDiet', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/RestrictedDiet' } as NamedNode,
	/** Results are available. */
	'ResultsAvailable': { termType: 'NamedNode', value: 'https://schema.org/ResultsAvailable', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ResultsAvailable' } as NamedNode,
	/** Results are not available. */
	'ResultsNotAvailable': { termType: 'NamedNode', value: 'https://schema.org/ResultsNotAvailable', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ResultsNotAvailable' } as NamedNode,
	/** The act of resuming a device or application which was formerly paused (e.g. resume music playback or resume a timer). */
	'ResumeAction': { termType: 'NamedNode', value: 'https://schema.org/ResumeAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ResumeAction' } as NamedNode,
	/** The drug's cost represents the retail cost of the drug. */
	'Retail': { termType: 'NamedNode', value: 'https://schema.org/Retail', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Retail' } as NamedNode,
	/** The act of returning to the origin that which was previously received (concrete objects) or taken (ownership). */
	'ReturnAction': { termType: 'NamedNode', value: 'https://schema.org/ReturnAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ReturnAction' } as NamedNode,
	/** Specifies that product returns must be made at a kiosk. */
	'ReturnAtKiosk': { termType: 'NamedNode', value: 'https://schema.org/ReturnAtKiosk', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ReturnAtKiosk' } as NamedNode,
	/** Specifies that product returns must be done by mail. */
	'ReturnByMail': { termType: 'NamedNode', value: 'https://schema.org/ReturnByMail', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ReturnByMail' } as NamedNode,
	/** Specifies that product returns must be paid for, and are the responsibility of, the customer. */
	'ReturnFeesCustomerResponsibility': { termType: 'NamedNode', value: 'https://schema.org/ReturnFeesCustomerResponsibility', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ReturnFeesCustomerResponsibility' } as NamedNode,
	/** Enumerates several kinds of policies for product return fees. */
	'ReturnFeesEnumeration': { termType: 'NamedNode', value: 'https://schema.org/ReturnFeesEnumeration', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ReturnFeesEnumeration' } as NamedNode,
	/** Specifies that product returns must be made in a store. */
	'ReturnInStore': { termType: 'NamedNode', value: 'https://schema.org/ReturnInStore', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ReturnInStore' } as NamedNode,
	/** Indicated that creating a return label is the responsibility of the customer. */
	'ReturnLabelCustomerResponsibility': { termType: 'NamedNode', value: 'https://schema.org/ReturnLabelCustomerResponsibility', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ReturnLabelCustomerResponsibility' } as NamedNode,
	/** Indicated that a return label must be downloaded and printed by the customer. */
	'ReturnLabelDownloadAndPrint': { termType: 'NamedNode', value: 'https://schema.org/ReturnLabelDownloadAndPrint', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ReturnLabelDownloadAndPrint' } as NamedNode,
	/** Specifies that a return label will be provided by the seller in the shipping box. */
	'ReturnLabelInBox': { termType: 'NamedNode', value: 'https://schema.org/ReturnLabelInBox', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ReturnLabelInBox' } as NamedNode,
	/** Enumerates several types of return labels for product returns. */
	'ReturnLabelSourceEnumeration': { termType: 'NamedNode', value: 'https://schema.org/ReturnLabelSourceEnumeration', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ReturnLabelSourceEnumeration' } as NamedNode,
	/** Enumerates several types of product return methods. */
	'ReturnMethodEnumeration': { termType: 'NamedNode', value: 'https://schema.org/ReturnMethodEnumeration', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ReturnMethodEnumeration' } as NamedNode,
	/** Specifies that the customer must pay the return shipping costs when returning a product. */
	'ReturnShippingFees': { termType: 'NamedNode', value: 'https://schema.org/ReturnShippingFees', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ReturnShippingFees' } as NamedNode,
	/** A review of an item - for example, of a restaurant, movie, or store. */
	'Review': { termType: 'NamedNode', value: 'https://schema.org/Review', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Review' } as NamedNode,
	/** The act of producing a balanced opinion about the object for an audience. An agent reviews an object with participants resulting in a review. */
	'ReviewAction': { termType: 'NamedNode', value: 'https://schema.org/ReviewAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ReviewAction' } as NamedNode,
	/** A [[NewsArticle]] and [[CriticReview]] providing a professional critic's assessment of a service, product, performance, or artistic or literary work. */
	'ReviewNewsArticle': { termType: 'NamedNode', value: 'https://schema.org/ReviewNewsArticle', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ReviewNewsArticle' } as NamedNode,
	/** A specific branch of medical science that deals with the study and treatment of rheumatic, autoimmune or joint diseases. */
	'Rheumatologic': { termType: 'NamedNode', value: 'https://schema.org/Rheumatologic', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Rheumatologic' } as NamedNode,
	/** The steering position is on the right side of the vehicle (viewed from the main direction of driving). */
	'RightHandDriving': { termType: 'NamedNode', value: 'https://schema.org/RightHandDriving', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/RightHandDriving' } as NamedNode,
	/** Information about the risk factors and possible complications that may follow a topic. */
	'RisksOrComplicationsHealthAspect': { termType: 'NamedNode', value: 'https://schema.org/RisksOrComplicationsHealthAspect', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/RisksOrComplicationsHealthAspect' } as NamedNode,
	/** A river (for example, the broad majestic Shannon). */
	'RiverBodyOfWater': { termType: 'NamedNode', value: 'https://schema.org/RiverBodyOfWater', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/RiverBodyOfWater' } as NamedNode,
	/** Represents additional information about a relationship or property. For example a Role can be used to say that a 'member' role linking some SportsTeam to a player occurred during a particular time period. Or that a Person's 'actor' role in a Movie was for some particular characterName. Such properties can be attached to a Role entity, which is then associated with the main entities using ordinary properties like 'member' or 'actor'.\n\nSee also [blog post](http://blog.schema.org/2014/06/introducing-role.html). */
	'Role': { termType: 'NamedNode', value: 'https://schema.org/Role', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Role' } as NamedNode,
	/** A roofing contractor. */
	'RoofingContractor': { termType: 'NamedNode', value: 'https://schema.org/RoofingContractor', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/RoofingContractor' } as NamedNode,
	/** A room is a distinguishable space within a structure, usually separated from other spaces by interior walls (source: Wikipedia, the free encyclopedia, see <a href="http://en.wikipedia.org/wiki/Room">http://en.wikipedia.org/wiki/Room</a>).
<br /><br />
See also the <a href="/docs/hotels.html">dedicated document on the use of schema.org for marking up hotels and other forms of accommodations</a>.
 */
	'Room': { termType: 'NamedNode', value: 'https://schema.org/Room', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Room' } as NamedNode,
	/** The act of notifying an event organizer as to whether you expect to attend the event. */
	'RsvpAction': { termType: 'NamedNode', value: 'https://schema.org/RsvpAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/RsvpAction' } as NamedNode,
	/** The invitee may or may not attend. */
	'RsvpResponseMaybe': { termType: 'NamedNode', value: 'https://schema.org/RsvpResponseMaybe', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/RsvpResponseMaybe' } as NamedNode,
	/** The invitee will not attend. */
	'RsvpResponseNo': { termType: 'NamedNode', value: 'https://schema.org/RsvpResponseNo', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/RsvpResponseNo' } as NamedNode,
	/** RsvpResponseType is an enumeration type whose instances represent responding to an RSVP request. */
	'RsvpResponseType': { termType: 'NamedNode', value: 'https://schema.org/RsvpResponseType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/RsvpResponseType' } as NamedNode,
	/** The invitee will attend. */
	'RsvpResponseYes': { termType: 'NamedNode', value: 'https://schema.org/RsvpResponseYes', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/RsvpResponseYes' } as NamedNode,
	/** Represents the suggested retail price ("SRP") of an offered product. */
	'SRP': { termType: 'NamedNode', value: 'https://schema.org/SRP', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SRP' } as NamedNode,
	/** Content about the safety-related aspects of a health topic. */
	'SafetyHealthAspect': { termType: 'NamedNode', value: 'https://schema.org/SafetyHealthAspect', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SafetyHealthAspect' } as NamedNode,
	/** Event type: Sales event. */
	'SaleEvent': { termType: 'NamedNode', value: 'https://schema.org/SaleEvent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SaleEvent' } as NamedNode,
	/** Represents a sale price (usually active for a limited period) of an offered product. */
	'SalePrice': { termType: 'NamedNode', value: 'https://schema.org/SalePrice', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SalePrice' } as NamedNode,
	/** Content coded 'satire or parody content' in a [[MediaReview]], considered in the context of how it was published or shared.

For a [[VideoObject]] to be 'satire or parody content': A video that was created as political or humorous commentary and is presented in that context. (Reshares of satire/parody content that do not include relevant context are more likely to fall under the “missing context” rating.)

For an [[ImageObject]] to be 'satire or parody content': An image that was created as political or humorous commentary and is presented in that context. (Reshares of satire/parody content that do not include relevant context are more likely to fall under the “missing context” rating.)

For an [[ImageObject]] with embedded text to be 'satire or parody content': An image that was created as political or humorous commentary and is presented in that context. (Reshares of satire/parody content that do not include relevant context are more likely to fall under the “missing context” rating.)

For an [[AudioObject]] to be 'satire or parody content': Audio that was created as political or humorous commentary and is presented in that context. (Reshares of satire/parody content that do not include relevant context are more likely to fall under the “missing context” rating.)
 */
	'SatireOrParodyContent': { termType: 'NamedNode', value: 'https://schema.org/SatireOrParodyContent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SatireOrParodyContent' } as NamedNode,
	/** An [[Article]] whose content is primarily [[satirical]](https://en.wikipedia.org/wiki/Satire) in nature, i.e. unlikely to be literally true. A satirical article is sometimes but not necessarily also a [[NewsArticle]]. [[ScholarlyArticle]]s are also sometimes satirized. */
	'SatiricalArticle': { termType: 'NamedNode', value: 'https://schema.org/SatiricalArticle', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SatiricalArticle' } as NamedNode,
	/** The day of the week between Friday and Sunday. */
	'Saturday': { termType: 'NamedNode', value: 'https://schema.org/Saturday', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Saturday' } as NamedNode,
	/** A schedule defines a repeating time period used to describe a regularly occurring [[Event]]. At a minimum a schedule will specify [[repeatFrequency]] which describes the interval between occurrences of the event. Additional information can be provided to specify the schedule more precisely.
      This includes identifying the day(s) of the week or month when the recurring event will take place, in addition to its start and end time. Schedules may also
      have start and end dates to indicate when they are active, e.g. to define a limited calendar of events. */
	'Schedule': { termType: 'NamedNode', value: 'https://schema.org/Schedule', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Schedule' } as NamedNode,
	/** Scheduling future actions, events, or tasks.\n\nRelated actions:\n\n* [[ReserveAction]]: Unlike ReserveAction, ScheduleAction allocates future actions (e.g. an event, a task, etc) towards a time slot / spatial allocation. */
	'ScheduleAction': { termType: 'NamedNode', value: 'https://schema.org/ScheduleAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ScheduleAction' } as NamedNode,
	/** A scholarly article. */
	'ScholarlyArticle': { termType: 'NamedNode', value: 'https://schema.org/ScholarlyArticle', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ScholarlyArticle' } as NamedNode,
	/** A school. */
	'School': { termType: 'NamedNode', value: 'https://schema.org/School', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/School' } as NamedNode,
	/** A School District is an administrative area for the administration of schools. */
	'SchoolDistrict': { termType: 'NamedNode', value: 'https://schema.org/SchoolDistrict', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SchoolDistrict' } as NamedNode,
	/** A screening of a movie or other video. */
	'ScreeningEvent': { termType: 'NamedNode', value: 'https://schema.org/ScreeningEvent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ScreeningEvent' } as NamedNode,
	/** Content about how to screen or further filter a topic. */
	'ScreeningHealthAspect': { termType: 'NamedNode', value: 'https://schema.org/ScreeningHealthAspect', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ScreeningHealthAspect' } as NamedNode,
	/** A piece of sculpture. */
	'Sculpture': { termType: 'NamedNode', value: 'https://schema.org/Sculpture', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Sculpture' } as NamedNode,
	/** A sea (for example, the Caspian sea). */
	'SeaBodyOfWater': { termType: 'NamedNode', value: 'https://schema.org/SeaBodyOfWater', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SeaBodyOfWater' } as NamedNode,
	/** The act of searching for an object.\n\nRelated actions:\n\n* [[FindAction]]: SearchAction generally leads to a FindAction, but not necessarily. */
	'SearchAction': { termType: 'NamedNode', value: 'https://schema.org/SearchAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SearchAction' } as NamedNode,
	/** A Search and Rescue organization of some kind. */
	'SearchRescueOrganization': { termType: 'NamedNode', value: 'https://schema.org/SearchRescueOrganization', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SearchRescueOrganization' } as NamedNode,
	/** Web page type: Search results page. */
	'SearchResultsPage': { termType: 'NamedNode', value: 'https://schema.org/SearchResultsPage', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SearchResultsPage' } as NamedNode,
	/** A media season, e.g. TV, radio, video game etc. */
	'Season': { termType: 'NamedNode', value: 'https://schema.org/Season', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Season' } as NamedNode,
	/** Used to describe a seat, such as a reserved seat in an event reservation. */
	'Seat': { termType: 'NamedNode', value: 'https://schema.org/Seat', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Seat' } as NamedNode,
	/** A seating map. */
	'SeatingMap': { termType: 'NamedNode', value: 'https://schema.org/SeatingMap', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SeatingMap' } as NamedNode,
	/** Information about questions that may be asked, when to see a professional, measures before seeing a doctor or content about the first consultation. */
	'SeeDoctorHealthAspect': { termType: 'NamedNode', value: 'https://schema.org/SeeDoctorHealthAspect', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SeeDoctorHealthAspect' } as NamedNode,
	/** This is the [[Action]] of navigating to a specific [[startOffset]] timestamp within a [[VideoObject]], typically represented with a URL template structure. */
	'SeekToAction': { termType: 'NamedNode', value: 'https://schema.org/SeekToAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SeekToAction' } as NamedNode,
	/** Self care actions or measures that can be taken to sooth, health or avoid a topic. This may be carried at home and can be carried/managed by the person itself. */
	'SelfCareHealthAspect': { termType: 'NamedNode', value: 'https://schema.org/SelfCareHealthAspect', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SelfCareHealthAspect' } as NamedNode,
	/** A self-storage facility. */
	'SelfStorage': { termType: 'NamedNode', value: 'https://schema.org/SelfStorage', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SelfStorage' } as NamedNode,
	/** The act of taking money from a buyer in exchange for goods or services rendered. An agent sells an object, product, or service to a buyer for a price. Reciprocal of BuyAction. */
	'SellAction': { termType: 'NamedNode', value: 'https://schema.org/SellAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SellAction' } as NamedNode,
	/** The act of physically/electronically dispatching an object for transfer from an origin to a destination. Related actions:\n\n* [[ReceiveAction]]: The reciprocal of SendAction.\n* [[GiveAction]]: Unlike GiveAction, SendAction does not imply the transfer of ownership (e.g. I can send you my laptop, but I'm not necessarily giving it to you). */
	'SendAction': { termType: 'NamedNode', value: 'https://schema.org/SendAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SendAction' } as NamedNode,
	/** A Series in schema.org is a group of related items, typically but not necessarily of the same kind. See also [[CreativeWorkSeries]], [[EventSeries]]. */
	'Series': { termType: 'NamedNode', value: 'https://schema.org/Series', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Series' } as NamedNode,
	/** A service provided by an organization, e.g. delivery service, print services, etc. */
	'Service': { termType: 'NamedNode', value: 'https://schema.org/Service', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Service' } as NamedNode,
	/** A means for accessing a service, e.g. a government office location, web site, or phone number. */
	'ServiceChannel': { termType: 'NamedNode', value: 'https://schema.org/ServiceChannel', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ServiceChannel' } as NamedNode,
	/** The item contains sexually oriented content such as nudity, suggestive or explicit material, or related online services, or is intended to enhance sexual activity. Examples: Erotic videos or magazine, sexual enhancement devices, sex toys. */
	'SexualContentConsideration': { termType: 'NamedNode', value: 'https://schema.org/SexualContentConsideration', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SexualContentConsideration' } as NamedNode,
	/** The act of distributing content to people for their amusement or edification. */
	'ShareAction': { termType: 'NamedNode', value: 'https://schema.org/ShareAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ShareAction' } as NamedNode,
	/** Printed music, as opposed to performed or recorded music. */
	'SheetMusic': { termType: 'NamedNode', value: 'https://schema.org/SheetMusic', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SheetMusic' } as NamedNode,
	/** ShippingDeliveryTime provides various pieces of information about delivery times for shipping. */
	'ShippingDeliveryTime': { termType: 'NamedNode', value: 'https://schema.org/ShippingDeliveryTime', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ShippingDeliveryTime' } as NamedNode,
	/** A ShippingRateSettings represents re-usable pieces of shipping information. It is designed for publication on an URL that may be referenced via the [[shippingSettingsLink]] property of an [[OfferShippingDetails]]. Several occurrences can be published, distinguished and matched (i.e. identified/referenced) by their different values for [[shippingLabel]]. */
	'ShippingRateSettings': { termType: 'NamedNode', value: 'https://schema.org/ShippingRateSettings', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ShippingRateSettings' } as NamedNode,
	/** A shoe store. */
	'ShoeStore': { termType: 'NamedNode', value: 'https://schema.org/ShoeStore', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ShoeStore' } as NamedNode,
	/** A shopping center or mall. */
	'ShoppingCenter': { termType: 'NamedNode', value: 'https://schema.org/ShoppingCenter', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ShoppingCenter' } as NamedNode,
	/** Short story or tale. A brief work of literature, usually written in narrative prose. */
	'ShortStory': { termType: 'NamedNode', value: 'https://schema.org/ShortStory', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ShortStory' } as NamedNode,
	/** Side effects that can be observed from the usage of the topic. */
	'SideEffectsHealthAspect': { termType: 'NamedNode', value: 'https://schema.org/SideEffectsHealthAspect', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SideEffectsHealthAspect' } as NamedNode,
	/** A trial design in which the researcher knows which treatment the patient was randomly assigned to but the patient does not. */
	'SingleBlindedTrial': { termType: 'NamedNode', value: 'https://schema.org/SingleBlindedTrial', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SingleBlindedTrial' } as NamedNode,
	/** A trial that takes place at a single center. */
	'SingleCenterTrial': { termType: 'NamedNode', value: 'https://schema.org/SingleCenterTrial', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SingleCenterTrial' } as NamedNode,
	/** Residence type: Single-family home. */
	'SingleFamilyResidence': { termType: 'NamedNode', value: 'https://schema.org/SingleFamilyResidence', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SingleFamilyResidence' } as NamedNode,
	/** Play mode: SinglePlayer. Which is played by a lone player. */
	'SinglePlayer': { termType: 'NamedNode', value: 'https://schema.org/SinglePlayer', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SinglePlayer' } as NamedNode,
	/** SingleRelease. */
	'SingleRelease': { termType: 'NamedNode', value: 'https://schema.org/SingleRelease', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SingleRelease' } as NamedNode,
	/** A navigation element of the page. */
	'SiteNavigationElement': { termType: 'NamedNode', value: 'https://schema.org/SiteNavigationElement', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SiteNavigationElement' } as NamedNode,
	/** Enumerates common size groups for various product categories. */
	'SizeGroupEnumeration': { termType: 'NamedNode', value: 'https://schema.org/SizeGroupEnumeration', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SizeGroupEnumeration' } as NamedNode,
	/** Size related properties of a product, typically a size code ([[name]]) and optionally a [[sizeSystem]], [[sizeGroup]], and product measurements ([[hasMeasurement]]). In addition, the intended audience can be defined through [[suggestedAge]], [[suggestedGender]], and suggested body measurements ([[suggestedMeasurement]]). */
	'SizeSpecification': { termType: 'NamedNode', value: 'https://schema.org/SizeSpecification', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SizeSpecification' } as NamedNode,
	/** Enumerates common size systems for different categories of products, for example "EN-13402" or "UK" for wearables or "Imperial" for screws. */
	'SizeSystemEnumeration': { termType: 'NamedNode', value: 'https://schema.org/SizeSystemEnumeration', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SizeSystemEnumeration' } as NamedNode,
	/** Imperial size system. */
	'SizeSystemImperial': { termType: 'NamedNode', value: 'https://schema.org/SizeSystemImperial', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SizeSystemImperial' } as NamedNode,
	/** Metric size system. */
	'SizeSystemMetric': { termType: 'NamedNode', value: 'https://schema.org/SizeSystemMetric', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SizeSystemMetric' } as NamedNode,
	/** A ski resort. */
	'SkiResort': { termType: 'NamedNode', value: 'https://schema.org/SkiResort', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SkiResort' } as NamedNode,
	/** Skin assessment with clinical examination. */
	'Skin': { termType: 'NamedNode', value: 'https://schema.org/Skin', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Skin' } as NamedNode,
	/** Event type: Social event. */
	'SocialEvent': { termType: 'NamedNode', value: 'https://schema.org/SocialEvent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SocialEvent' } as NamedNode,
	/** A post to a social media platform, including blog posts, tweets, Facebook posts, etc. */
	'SocialMediaPosting': { termType: 'NamedNode', value: 'https://schema.org/SocialMediaPosting', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SocialMediaPosting' } as NamedNode,
	/** A software application. */
	'SoftwareApplication': { termType: 'NamedNode', value: 'https://schema.org/SoftwareApplication', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SoftwareApplication' } as NamedNode,
	/** Computer programming source code. Example: Full (compile ready) solutions, code snippet samples, scripts, templates. */
	'SoftwareSourceCode': { termType: 'NamedNode', value: 'https://schema.org/SoftwareSourceCode', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SoftwareSourceCode' } as NamedNode,
	/** Indicates that the item has sold out. */
	'SoldOut': { termType: 'NamedNode', value: 'https://schema.org/SoldOut', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SoldOut' } as NamedNode,
	/** The action that takes in a math expression and directs users to a page potentially capable of solving/simplifying that expression. */
	'SolveMathAction': { termType: 'NamedNode', value: 'https://schema.org/SolveMathAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SolveMathAction' } as NamedNode,
	/** A placeholder for multiple similar products of the same kind. */
	'SomeProducts': { termType: 'NamedNode', value: 'https://schema.org/SomeProducts', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SomeProducts' } as NamedNode,
	/** SoundtrackAlbum. */
	'SoundtrackAlbum': { termType: 'NamedNode', value: 'https://schema.org/SoundtrackAlbum', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SoundtrackAlbum' } as NamedNode,
	/** A SpeakableSpecification indicates (typically via [[xpath]] or [[cssSelector]]) sections of a document that are highlighted as particularly [[speakable]]. Instances of this type are expected to be used primarily as values of the [[speakable]] property. */
	'SpeakableSpecification': { termType: 'NamedNode', value: 'https://schema.org/SpeakableSpecification', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SpeakableSpecification' } as NamedNode,
	/** A SpecialAnnouncement combines a simple date-stamped textual information update
      with contextualized Web links and other structured data.  It represents an information update made by a
      locally-oriented organization, for example schools, pharmacies, healthcare providers,  community groups, police,
      local government.

For work in progress guidelines on Coronavirus-related markup see [this doc](https://docs.google.com/document/d/14ikaGCKxo50rRM7nvKSlbUpjyIk2WMQd3IkB1lItlrM/edit#).

The motivating scenario for SpecialAnnouncement is the [Coronavirus pandemic](https://en.wikipedia.org/wiki/2019%E2%80%9320_coronavirus_pandemic), and the initial vocabulary is oriented to this urgent situation. Schema.org
expect to improve the markup iteratively as it is deployed and as feedback emerges from use. In addition to our
usual [Github entry](https://github.com/schemaorg/schemaorg/issues/2490), feedback comments can also be provided in [this document](https://docs.google.com/document/d/1fpdFFxk8s87CWwACs53SGkYv3aafSxz_DTtOQxMrBJQ/edit#).


While this schema is designed to communicate urgent crisis-related information, it is not the same as an emergency warning technology like [CAP](https://en.wikipedia.org/wiki/Common_Alerting_Protocol), although there may be overlaps. The intent is to cover
the kinds of everyday practical information being posted to existing websites during an emergency situation.

Several kinds of information can be provided:

We encourage the provision of "name", "text", "datePosted", "expires" (if appropriate), "category" and
"url" as a simple baseline. It is important to provide a value for "category" where possible, most ideally as a well known
URL from Wikipedia or Wikidata. In the case of the 2019-2020 Coronavirus pandemic, this should be "https://en.wikipedia.org/w/index.php?title=2019-20\_coronavirus\_pandemic" or "https://www.wikidata.org/wiki/Q81068910".

For many of the possible properties, values can either be simple links or an inline description, depending on whether a summary is available. For a link, provide just the URL of the appropriate page as the property's value. For an inline description, use a [[WebContent]] type, and provide the url as a property of that, alongside at least a simple "[[text]]" summary of the page. It is
unlikely that a single SpecialAnnouncement will need all of the possible properties simultaneously.

We expect that in many cases the page referenced might contain more specialized structured data, e.g. contact info, [[openingHours]], [[Event]], [[FAQPage]] etc. By linking to those pages from a [[SpecialAnnouncement]] you can help make it clearer that the events are related to the situation (e.g. Coronavirus) indicated by the [[category]] property of the [[SpecialAnnouncement]].

Many [[SpecialAnnouncement]]s will relate to particular regions and to identifiable local organizations. Use [[spatialCoverage]] for the region, and [[announcementLocation]] to indicate specific [[LocalBusiness]]es and [[CivicStructure]]s. If the announcement affects both a particular region and a specific location (for example, a library closure that serves an entire region), use both [[spatialCoverage]] and [[announcementLocation]].

The [[about]] property can be used to indicate entities that are the focus of the announcement. We now recommend using [[about]] only
for representing non-location entities (e.g. a [[Course]] or a [[RadioStation]]). For places, use [[announcementLocation]] and [[spatialCoverage]]. Consumers of this markup should be aware that the initial design encouraged the use of [[about]] for locations too.

The basic content of [[SpecialAnnouncement]] is similar to that of an [RSS](https://en.wikipedia.org/wiki/RSS) or [Atom](https://en.wikipedia.org/wiki/Atom_(Web_standard)) feed. For publishers without such feeds, basic feed-like information can be shared by posting
[[SpecialAnnouncement]] updates in a page, e.g. using JSON-LD. For sites with Atom/RSS functionality, you can point to a feed
with the [[webFeed]] property. This can be a simple URL, or an inline [[DataFeed]] object, with [[encodingFormat]] providing
media type information, e.g. "application/rss+xml" or "application/atom+xml".
 */
	'SpecialAnnouncement': { termType: 'NamedNode', value: 'https://schema.org/SpecialAnnouncement', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SpecialAnnouncement' } as NamedNode,
	/** Any branch of a field in which people typically develop specific expertise, usually after significant study, time, and effort. */
	'Specialty': { termType: 'NamedNode', value: 'https://schema.org/Specialty', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Specialty' } as NamedNode,
	/** The scientific study and treatment of defects, disorders, and malfunctions of speech and voice, as stuttering, lisping, or lalling, and of language disturbances, as aphasia or delayed language acquisition. */
	'SpeechPathology': { termType: 'NamedNode', value: 'https://schema.org/SpeechPathology', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SpeechPathology' } as NamedNode,
	/** SpokenWordAlbum. */
	'SpokenWordAlbum': { termType: 'NamedNode', value: 'https://schema.org/SpokenWordAlbum', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SpokenWordAlbum' } as NamedNode,
	/** A sporting goods store. */
	'SportingGoodsStore': { termType: 'NamedNode', value: 'https://schema.org/SportingGoodsStore', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SportingGoodsStore' } as NamedNode,
	/** A sports location, such as a playing field. */
	'SportsActivityLocation': { termType: 'NamedNode', value: 'https://schema.org/SportsActivityLocation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SportsActivityLocation' } as NamedNode,
	/** A sports club. */
	'SportsClub': { termType: 'NamedNode', value: 'https://schema.org/SportsClub', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SportsClub' } as NamedNode,
	/** Event type: Sports event. */
	'SportsEvent': { termType: 'NamedNode', value: 'https://schema.org/SportsEvent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SportsEvent' } as NamedNode,
	/** Represents the collection of all sports organizations, including sports teams, governing bodies, and sports associations. */
	'SportsOrganization': { termType: 'NamedNode', value: 'https://schema.org/SportsOrganization', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SportsOrganization' } as NamedNode,
	/** Organization: Sports team. */
	'SportsTeam': { termType: 'NamedNode', value: 'https://schema.org/SportsTeam', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SportsTeam' } as NamedNode,
	/** A spreadsheet file. */
	'SpreadsheetDigitalDocument': { termType: 'NamedNode', value: 'https://schema.org/SpreadsheetDigitalDocument', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SpreadsheetDigitalDocument' } as NamedNode,
	/** A stadium. */
	'StadiumOrArena': { termType: 'NamedNode', value: 'https://schema.org/StadiumOrArena', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/StadiumOrArena' } as NamedNode,
	/** Content coded 'staged content' in a [[MediaReview]], considered in the context of how it was published or shared.

For a [[VideoObject]] to be 'staged content': A video that has been created using actors or similarly contrived.

For an [[ImageObject]] to be 'staged content': An image that was created using actors or similarly contrived, such as a screenshot of a fake tweet.

For an [[ImageObject]] with embedded text to be 'staged content': An image that was created using actors or similarly contrived, such as a screenshot of a fake tweet.

For an [[AudioObject]] to be 'staged content': Audio that has been created using actors or similarly contrived.
 */
	'StagedContent': { termType: 'NamedNode', value: 'https://schema.org/StagedContent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/StagedContent' } as NamedNode,
	/** Stages that can be observed from a topic. */
	'StagesHealthAspect': { termType: 'NamedNode', value: 'https://schema.org/StagesHealthAspect', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/StagesHealthAspect' } as NamedNode,
	/** A state or province of a country. */
	'State': { termType: 'NamedNode', value: 'https://schema.org/State', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/State' } as NamedNode,
	/** A statement about something, for example a fun or interesting fact. If known, the main entity this statement is about can be indicated using mainEntity. For more formal claims (e.g. in Fact Checking), consider using [[Claim]] instead. Use the [[text]] property to capture the text of the statement. */
	'Statement': { termType: 'NamedNode', value: 'https://schema.org/Statement', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Statement' } as NamedNode,
	/** A StatisticalPopulation is a set of instances of a certain given type that satisfy some set of constraints. The property [[populationType]] is used to specify the type. Any property that can be used on instances of that type can appear on the statistical population. For example, a [[StatisticalPopulation]] representing all [[Person]]s with a [[homeLocation]] of East Podunk California would be described by applying the appropriate [[homeLocation]] and [[populationType]] properties to a [[StatisticalPopulation]] item that stands for that set of people.
The properties [[numConstraints]] and [[constraintProperty]] are used to specify which of the populations properties are used to specify the population. Note that the sense of "population" used here is the general sense of a statistical
population, and does not imply that the population consists of people. For example, a [[populationType]] of [[Event]] or [[NewsArticle]] could be used. See also [[Observation]], where a [[populationType]] such as [[Person]] or [[Event]] can be indicated directly. In most cases it may be better to use [[StatisticalVariable]] instead of [[StatisticalPopulation]]. */
	'StatisticalPopulation': { termType: 'NamedNode', value: 'https://schema.org/StatisticalPopulation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/StatisticalPopulation' } as NamedNode,
	/** [[StatisticalVariable]] represents any type of statistical metric that can be measured at a place and time. The usage pattern for [[StatisticalVariable]] is typically expressed using [[Observation]] with an explicit [[populationType]], which is a type, typically drawn from Schema.org. Each [[StatisticalVariable]] is marked as a [[ConstraintNode]], meaning that some properties (those listed using [[constraintProperty]]) serve in this setting solely to define the statistical variable rather than literally describe a specific person, place or thing. For example, a [[StatisticalVariable]] Median_Height_Person_Female representing the median height of women, could be written as follows: the population type is [[Person]]; the measuredProperty [[height]]; the [[statType]] [[median]]; the [[gender]] [[Female]]. It is important to note that there are many kinds of scientific quantitative observation which are not fully, perfectly or unambiguously described following this pattern, or with solely Schema.org terminology. The approach taken here is designed to allow partial, incremental or minimal description of [[StatisticalVariable]]s, and the use of detailed sets of entity and property IDs from external repositories. The [[measurementMethod]], [[unitCode]] and [[unitText]] properties can also be used to clarify the specific nature and notation of an observed measurement.  */
	'StatisticalVariable': { termType: 'NamedNode', value: 'https://schema.org/StatisticalVariable', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/StatisticalVariable' } as NamedNode,
	/** Lists or enumerations dealing with status types. */
	'StatusEnumeration': { termType: 'NamedNode', value: 'https://schema.org/StatusEnumeration', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/StatusEnumeration' } as NamedNode,
	/** A value indicating a steering position. */
	'SteeringPositionValue': { termType: 'NamedNode', value: 'https://schema.org/SteeringPositionValue', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SteeringPositionValue' } as NamedNode,
	/** A retail good store. */
	'Store': { termType: 'NamedNode', value: 'https://schema.org/Store', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Store' } as NamedNode,
	/** Specifies that the customer receives a store credit as refund when returning a product. */
	'StoreCreditRefund': { termType: 'NamedNode', value: 'https://schema.org/StoreCreditRefund', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/StoreCreditRefund' } as NamedNode,
	/** Physical activity that is engaged in to improve muscle and bone strength. Also referred to as resistance training. */
	'StrengthTraining': { termType: 'NamedNode', value: 'https://schema.org/StrengthTraining', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/StrengthTraining' } as NamedNode,
	/** Structured values are used when the value of a property has a more complex structure than simply being a textual value or a reference to another thing. */
	'StructuredValue': { termType: 'NamedNode', value: 'https://schema.org/StructuredValue', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/StructuredValue' } as NamedNode,
	/** StudioAlbum. */
	'StudioAlbum': { termType: 'NamedNode', value: 'https://schema.org/StudioAlbum', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/StudioAlbum' } as NamedNode,
	/** The act of forming a personal connection with someone/something (object) unidirectionally/asymmetrically to get updates pushed to.\n\nRelated actions:\n\n* [[FollowAction]]: Unlike FollowAction, SubscribeAction implies that the subscriber acts as a passive agent being constantly/actively pushed for updates.\n* [[RegisterAction]]: Unlike RegisterAction, SubscribeAction implies that the agent is interested in continuing receiving updates from the object.\n* [[JoinAction]]: Unlike JoinAction, SubscribeAction implies that the agent is interested in continuing receiving updates from the object. */
	'SubscribeAction': { termType: 'NamedNode', value: 'https://schema.org/SubscribeAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SubscribeAction' } as NamedNode,
	/** Represents the subscription pricing component of the total price for an offered product. */
	'Subscription': { termType: 'NamedNode', value: 'https://schema.org/Subscription', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Subscription' } as NamedNode,
	/** Any matter of defined composition that has discrete existence, whose origin may be biological, mineral or chemical. */
	'Substance': { termType: 'NamedNode', value: 'https://schema.org/Substance', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Substance' } as NamedNode,
	/** A subway station. */
	'SubwayStation': { termType: 'NamedNode', value: 'https://schema.org/SubwayStation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SubwayStation' } as NamedNode,
	/** A suite in a hotel or other public accommodation, denotes a class of luxury accommodations, the key feature of which is multiple rooms (source: Wikipedia, the free encyclopedia, see <a href="http://en.wikipedia.org/wiki/Suite_(hotel)">http://en.wikipedia.org/wiki/Suite_(hotel)</a>).
<br /><br />
See also the <a href="/docs/hotels.html">dedicated document on the use of schema.org for marking up hotels and other forms of accommodations</a>.
 */
	'Suite': { termType: 'NamedNode', value: 'https://schema.org/Suite', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Suite' } as NamedNode,
	/** The day of the week between Saturday and Monday. */
	'Sunday': { termType: 'NamedNode', value: 'https://schema.org/Sunday', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Sunday' } as NamedNode,
	/** Anatomical features that can be observed by sight (without dissection), including the form and proportions of the human body as well as surface landmarks that correspond to deeper subcutaneous structures. Superficial anatomy plays an important role in sports medicine, phlebotomy, and other medical specialties as underlying anatomical structures can be identified through surface palpation. For example, during back surgery, superficial anatomy can be used to palpate and count vertebrae to find the site of incision. Or in phlebotomy, superficial anatomy can be used to locate an underlying vein; for example, the median cubital vein can be located by palpating the borders of the cubital fossa (such as the epicondyles of the humerus) and then looking for the superficial signs of the vein, such as size, prominence, ability to refill after depression, and feel of surrounding tissue support. As another example, in a subluxation (dislocation) of the glenohumeral joint, the bony structure becomes pronounced with the deltoid muscle failing to cover the glenohumeral joint allowing the edges of the scapula to be superficially visible. Here, the superficial anatomy is the visible edges of the scapula, implying the underlying dislocation of the joint (the related anatomical structure). */
	'SuperficialAnatomy': { termType: 'NamedNode', value: 'https://schema.org/SuperficialAnatomy', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SuperficialAnatomy' } as NamedNode,
	/** A specific branch of medical science that pertains to treating diseases, injuries and deformities by manual and instrumental means. */
	'Surgical': { termType: 'NamedNode', value: 'https://schema.org/Surgical', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Surgical' } as NamedNode,
	/** A medical procedure involving an incision with instruments; performed for diagnose, or therapeutic purposes. */
	'SurgicalProcedure': { termType: 'NamedNode', value: 'https://schema.org/SurgicalProcedure', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SurgicalProcedure' } as NamedNode,
	/** The act of momentarily pausing a device or application (e.g. pause music playback or pause a timer). */
	'SuspendAction': { termType: 'NamedNode', value: 'https://schema.org/SuspendAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SuspendAction' } as NamedNode,
	/** Suspended. */
	'Suspended': { termType: 'NamedNode', value: 'https://schema.org/Suspended', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Suspended' } as NamedNode,
	/** A syllabus that describes the material covered in a course, often with several such sections per [[Course]] so that a distinct [[timeRequired]] can be provided for that section of the [[Course]]. */
	'Syllabus': { termType: 'NamedNode', value: 'https://schema.org/Syllabus', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Syllabus' } as NamedNode,
	/** Symptoms or related symptoms of a Topic. */
	'SymptomsHealthAspect': { termType: 'NamedNode', value: 'https://schema.org/SymptomsHealthAspect', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/SymptomsHealthAspect' } as NamedNode,
	/** A synagogue. */
	'Synagogue': { termType: 'NamedNode', value: 'https://schema.org/Synagogue', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Synagogue' } as NamedNode,
	/** A short TV program or a segment/part of a TV program. */
	'TVClip': { termType: 'NamedNode', value: 'https://schema.org/TVClip', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/TVClip' } as NamedNode,
	/** A TV episode which can be part of a series or season. */
	'TVEpisode': { termType: 'NamedNode', value: 'https://schema.org/TVEpisode', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/TVEpisode' } as NamedNode,
	/** Season dedicated to TV broadcast and associated online delivery. */
	'TVSeason': { termType: 'NamedNode', value: 'https://schema.org/TVSeason', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/TVSeason' } as NamedNode,
	/** CreativeWorkSeries dedicated to TV broadcast and associated online delivery. */
	'TVSeries': { termType: 'NamedNode', value: 'https://schema.org/TVSeries', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/TVSeries' } as NamedNode,
	/** A table on a Web page. */
	'Table': { termType: 'NamedNode', value: 'https://schema.org/Table', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Table' } as NamedNode,
	/** The act of gaining ownership of an object from an origin. Reciprocal of GiveAction.\n\nRelated actions:\n\n* [[GiveAction]]: The reciprocal of TakeAction.\n* [[ReceiveAction]]: Unlike ReceiveAction, TakeAction implies that ownership has been transferred. */
	'TakeAction': { termType: 'NamedNode', value: 'https://schema.org/TakeAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/TakeAction' } as NamedNode,
	/** A tattoo parlor. */
	'TattooParlor': { termType: 'NamedNode', value: 'https://schema.org/TattooParlor', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/TattooParlor' } as NamedNode,
	/** A taxi. */
	'Taxi': { termType: 'NamedNode', value: 'https://schema.org/Taxi', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Taxi' } as NamedNode,
	/** A reservation for a taxi.\n\nNote: This type is for information about actual reservations, e.g. in confirmation emails or HTML pages with individual confirmations of reservations. For offers of tickets, use [[Offer]]. */
	'TaxiReservation': { termType: 'NamedNode', value: 'https://schema.org/TaxiReservation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/TaxiReservation' } as NamedNode,
	/** A service for a vehicle for hire with a driver for local travel. Fares are usually calculated based on distance traveled. */
	'TaxiService': { termType: 'NamedNode', value: 'https://schema.org/TaxiService', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/TaxiService' } as NamedNode,
	/** A taxi stand. */
	'TaxiStand': { termType: 'NamedNode', value: 'https://schema.org/TaxiStand', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/TaxiStand' } as NamedNode,
	/** Indicates the usage of the car as a taxi. */
	'TaxiVehicleUsage': { termType: 'NamedNode', value: 'https://schema.org/TaxiVehicleUsage', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/TaxiVehicleUsage' } as NamedNode,
	/** A set of organisms asserted to represent a natural cohesive biological unit. */
	'Taxon': { termType: 'NamedNode', value: 'https://schema.org/Taxon', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Taxon' } as NamedNode,
	/** A technical article - Example: How-to (task) topics, step-by-step, procedural troubleshooting, specifications, etc. */
	'TechArticle': { termType: 'NamedNode', value: 'https://schema.org/TechArticle', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/TechArticle' } as NamedNode,
	/** A unique instance of a television BroadcastService on a CableOrSatelliteService lineup. */
	'TelevisionChannel': { termType: 'NamedNode', value: 'https://schema.org/TelevisionChannel', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/TelevisionChannel' } as NamedNode,
	/** A television station. */
	'TelevisionStation': { termType: 'NamedNode', value: 'https://schema.org/TelevisionStation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/TelevisionStation' } as NamedNode,
	/** A tennis complex. */
	'TennisComplex': { termType: 'NamedNode', value: 'https://schema.org/TennisComplex', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/TennisComplex' } as NamedNode,
	/** Terminated. */
	'Terminated': { termType: 'NamedNode', value: 'https://schema.org/Terminated', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Terminated' } as NamedNode,
	/** Data type: Text. */
	'Text': { termType: 'NamedNode', value: 'https://schema.org/Text', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Text' } as NamedNode,
	/** A file composed primarily of text. */
	'TextDigitalDocument': { termType: 'NamedNode', value: 'https://schema.org/TextDigitalDocument', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/TextDigitalDocument' } as NamedNode,
	/** A text file. The text can be unformatted or contain markup, html, etc. */
	'TextObject': { termType: 'NamedNode', value: 'https://schema.org/TextObject', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/TextObject' } as NamedNode,
	/** Event type: Theater performance. */
	'TheaterEvent': { termType: 'NamedNode', value: 'https://schema.org/TheaterEvent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/TheaterEvent' } as NamedNode,
	/** A theater group or company, for example, the Royal Shakespeare Company or Druid Theatre. */
	'TheaterGroup': { termType: 'NamedNode', value: 'https://schema.org/TheaterGroup', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/TheaterGroup' } as NamedNode,
	/** A medical device used for therapeutic purposes. */
	'Therapeutic': { termType: 'NamedNode', value: 'https://schema.org/Therapeutic', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Therapeutic' } as NamedNode,
	/** A medical procedure intended primarily for therapeutic purposes, aimed at improving a health condition. */
	'TherapeuticProcedure': { termType: 'NamedNode', value: 'https://schema.org/TherapeuticProcedure', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/TherapeuticProcedure' } as NamedNode,
	/** A thesis or dissertation document submitted in support of candidature for an academic degree or professional qualification. */
	'Thesis': { termType: 'NamedNode', value: 'https://schema.org/Thesis', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Thesis' } as NamedNode,
	/** The most generic type of item. */
	'Thing': { termType: 'NamedNode', value: 'https://schema.org/Thing', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Thing' } as NamedNode,
	/** Throat assessment with  clinical examination. */
	'Throat': { termType: 'NamedNode', value: 'https://schema.org/Throat', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Throat' } as NamedNode,
	/** The day of the week between Wednesday and Friday. */
	'Thursday': { termType: 'NamedNode', value: 'https://schema.org/Thursday', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Thursday' } as NamedNode,
	/** Used to describe a ticket to an event, a flight, a bus ride, etc. */
	'Ticket': { termType: 'NamedNode', value: 'https://schema.org/Ticket', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Ticket' } as NamedNode,
	/** The act of reaching a draw in a competitive activity. */
	'TieAction': { termType: 'NamedNode', value: 'https://schema.org/TieAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/TieAction' } as NamedNode,
	/** A point in time recurring on multiple days in the form hh:mm:ss[Z|(+|-)hh:mm] (see [XML schema for details](http://www.w3.org/TR/xmlschema-2/#time)). */
	'Time': { termType: 'NamedNode', value: 'https://schema.org/Time', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Time' } as NamedNode,
	/** The act of giving money voluntarily to a beneficiary in recognition of services rendered. */
	'TipAction': { termType: 'NamedNode', value: 'https://schema.org/TipAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/TipAction' } as NamedNode,
	/** A tire shop. */
	'TireShop': { termType: 'NamedNode', value: 'https://schema.org/TireShop', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/TireShop' } as NamedNode,
	/** Item contains tobacco and/or nicotine, for example cigars, cigarettes, chewing tobacco, e-cigarettes, or hookahs. */
	'TobaccoNicotineConsideration': { termType: 'NamedNode', value: 'https://schema.org/TobaccoNicotineConsideration', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/TobaccoNicotineConsideration' } as NamedNode,
	/** The associated telephone number is toll free. */
	'TollFree': { termType: 'NamedNode', value: 'https://schema.org/TollFree', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/TollFree' } as NamedNode,
	/** A tourist attraction.  In principle any Thing can be a [[TouristAttraction]], from a [[Mountain]] and [[LandmarksOrHistoricalBuildings]] to a [[LocalBusiness]].  This Type can be used on its own to describe a general [[TouristAttraction]], or be used as an [[additionalType]] to add tourist attraction properties to any other type.  (See examples below) */
	'TouristAttraction': { termType: 'NamedNode', value: 'https://schema.org/TouristAttraction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/TouristAttraction' } as NamedNode,
	/** A tourist destination. In principle any [[Place]] can be a [[TouristDestination]] from a [[City]], Region or [[Country]] to an [[AmusementPark]] or [[Hotel]]. This Type can be used on its own to describe a general [[TouristDestination]], or be used as an [[additionalType]] to add tourist relevant properties to any other [[Place]].  A [[TouristDestination]] is defined as a [[Place]] that contains, or is colocated with, one or more [[TouristAttraction]]s, often linked by a similar theme or interest to a particular [[touristType]]. The [UNWTO](http://www2.unwto.org/) defines Destination (main destination of a tourism trip) as the place visited that is central to the decision to take the trip.
  (See examples below.) */
	'TouristDestination': { termType: 'NamedNode', value: 'https://schema.org/TouristDestination', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/TouristDestination' } as NamedNode,
	/** A tourist information center. */
	'TouristInformationCenter': { termType: 'NamedNode', value: 'https://schema.org/TouristInformationCenter', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/TouristInformationCenter' } as NamedNode,
	/** A tourist trip. A created itinerary of visits to one or more places of interest ([[TouristAttraction]]/[[TouristDestination]]) often linked by a similar theme, geographic area, or interest to a particular [[touristType]]. The [UNWTO](http://www2.unwto.org/) defines tourism trip as the Trip taken by visitors.
  (See examples below.) */
	'TouristTrip': { termType: 'NamedNode', value: 'https://schema.org/TouristTrip', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/TouristTrip' } as NamedNode,
	/** A specific branch of medical science that is concerned with poisons, their nature, effects and detection and involved in the treatment of poisoning. */
	'Toxicologic': { termType: 'NamedNode', value: 'https://schema.org/Toxicologic', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Toxicologic' } as NamedNode,
	/** A toy store. */
	'ToyStore': { termType: 'NamedNode', value: 'https://schema.org/ToyStore', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ToyStore' } as NamedNode,
	/** An agent tracks an object for updates.\n\nRelated actions:\n\n* [[FollowAction]]: Unlike FollowAction, TrackAction refers to the interest on the location of innanimates objects.\n* [[SubscribeAction]]: Unlike SubscribeAction, TrackAction refers to  the interest on the location of innanimate objects. */
	'TrackAction': { termType: 'NamedNode', value: 'https://schema.org/TrackAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/TrackAction' } as NamedNode,
	/** The act of participating in an exchange of goods and services for monetary compensation. An agent trades an object, product or service with a participant in exchange for a one time or periodic payment. */
	'TradeAction': { termType: 'NamedNode', value: 'https://schema.org/TradeAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/TradeAction' } as NamedNode,
	/** A system of medicine based on common theoretical concepts that originated in China and evolved over thousands of years, that uses herbs, acupuncture, exercise, massage, dietary therapy, and other methods to treat a wide range of conditions. */
	'TraditionalChinese': { termType: 'NamedNode', value: 'https://schema.org/TraditionalChinese', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/TraditionalChinese' } as NamedNode,
	/** A reservation for train travel.\n\nNote: This type is for information about actual reservations, e.g. in confirmation emails or HTML pages with individual confirmations of reservations. For offers of tickets, use [[Offer]]. */
	'TrainReservation': { termType: 'NamedNode', value: 'https://schema.org/TrainReservation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/TrainReservation' } as NamedNode,
	/** A train station. */
	'TrainStation': { termType: 'NamedNode', value: 'https://schema.org/TrainStation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/TrainStation' } as NamedNode,
	/** A trip on a commercial train line. */
	'TrainTrip': { termType: 'NamedNode', value: 'https://schema.org/TrainTrip', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/TrainTrip' } as NamedNode,
	/** The act of transferring/moving (abstract or concrete) animate or inanimate objects from one place to another. */
	'TransferAction': { termType: 'NamedNode', value: 'https://schema.org/TransferAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/TransferAction' } as NamedNode,
	/** Content coded 'transformed content' in a [[MediaReview]], considered in the context of how it was published or shared.

For a [[VideoObject]] to be 'transformed content':  or all of the video has been manipulated to transform the footage itself. This category includes using tools like the Adobe Suite to change the speed of the video, add or remove visual elements or dub audio. Deepfakes are also a subset of transformation.

For an [[ImageObject]] to be 'transformed content': Adding or deleting visual elements to give the image a different meaning with the intention to mislead.

For an [[ImageObject]] with embedded text to be 'transformed content': Adding or deleting visual elements to give the image a different meaning with the intention to mislead.

For an [[AudioObject]] to be 'transformed content': Part or all of the audio has been manipulated to alter the words or sounds, or the audio has been synthetically generated, such as to create a sound-alike voice.
 */
	'TransformedContent': { termType: 'NamedNode', value: 'https://schema.org/TransformedContent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/TransformedContent' } as NamedNode,
	/** A transit map. */
	'TransitMap': { termType: 'NamedNode', value: 'https://schema.org/TransitMap', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/TransitMap' } as NamedNode,
	/** The act of traveling from a fromLocation to a destination by a specified mode of transport, optionally with participants. */
	'TravelAction': { termType: 'NamedNode', value: 'https://schema.org/TravelAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/TravelAction' } as NamedNode,
	/** A travel agency. */
	'TravelAgency': { termType: 'NamedNode', value: 'https://schema.org/TravelAgency', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/TravelAgency' } as NamedNode,
	/** An indication for treating an underlying condition, symptom, etc. */
	'TreatmentIndication': { termType: 'NamedNode', value: 'https://schema.org/TreatmentIndication', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/TreatmentIndication' } as NamedNode,
	/** Treatments or related therapies for a Topic. */
	'TreatmentsHealthAspect': { termType: 'NamedNode', value: 'https://schema.org/TreatmentsHealthAspect', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/TreatmentsHealthAspect' } as NamedNode,
	/** A trip or journey. An itinerary of visits to one or more places. */
	'Trip': { termType: 'NamedNode', value: 'https://schema.org/Trip', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Trip' } as NamedNode,
	/** A trial design in which neither the researcher, the person administering the therapy nor the patient knows the details of the treatment the patient was randomly assigned to. */
	'TripleBlindedTrial': { termType: 'NamedNode', value: 'https://schema.org/TripleBlindedTrial', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/TripleBlindedTrial' } as NamedNode,
	/** The boolean value true. */
	'True': { termType: 'NamedNode', value: 'https://schema.org/True', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/True' } as NamedNode,
	/** The day of the week between Monday and Wednesday. */
	'Tuesday': { termType: 'NamedNode', value: 'https://schema.org/Tuesday', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Tuesday' } as NamedNode,
	/** A structured value indicating the quantity, unit of measurement, and business function of goods included in a bundle offer. */
	'TypeAndQuantityNode': { termType: 'NamedNode', value: 'https://schema.org/TypeAndQuantityNode', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/TypeAndQuantityNode' } as NamedNode,
	/** Categorization and other types related to a topic. */
	'TypesHealthAspect': { termType: 'NamedNode', value: 'https://schema.org/TypesHealthAspect', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/TypesHealthAspect' } as NamedNode,
	/** UKNonprofitType: Non-profit organization type originating from the United Kingdom. */
	'UKNonprofitType': { termType: 'NamedNode', value: 'https://schema.org/UKNonprofitType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/UKNonprofitType' } as NamedNode,
	/** UKTrust: Non-profit type referring to a UK trust. */
	'UKTrust': { termType: 'NamedNode', value: 'https://schema.org/UKTrust', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/UKTrust' } as NamedNode,
	/** Data type: URL. */
	'URL': { termType: 'NamedNode', value: 'https://schema.org/URL', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/URL' } as NamedNode,
	/** USNonprofitType: Non-profit organization type originating from the United States. */
	'USNonprofitType': { termType: 'NamedNode', value: 'https://schema.org/USNonprofitType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/USNonprofitType' } as NamedNode,
	/** Ultrasound imaging. */
	'Ultrasound': { termType: 'NamedNode', value: 'https://schema.org/Ultrasound', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Ultrasound' } as NamedNode,
	/** The act of un-registering from a service.\n\nRelated actions:\n\n* [[RegisterAction]]: antonym of UnRegisterAction.\n* [[LeaveAction]]: Unlike LeaveAction, UnRegisterAction implies that you are unregistering from a service you were previously registered, rather than leaving a team/group of people. */
	'UnRegisterAction': { termType: 'NamedNode', value: 'https://schema.org/UnRegisterAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/UnRegisterAction' } as NamedNode,
	/** The item is suitable only for adults, without indicating why. Due to widespread use of "adult" as a euphemism for "sexual", many such items are likely suited also for the SexualContentConsideration code. */
	'UnclassifiedAdultConsideration': { termType: 'NamedNode', value: 'https://schema.org/UnclassifiedAdultConsideration', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/UnclassifiedAdultConsideration' } as NamedNode,
	/** UnemploymentSupport: this is a benefit for unemployment support. */
	'UnemploymentSupport': { termType: 'NamedNode', value: 'https://schema.org/UnemploymentSupport', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/UnemploymentSupport' } as NamedNode,
	/** UnincorporatedAssociationCharity: Non-profit type referring to a charitable company that is not incorporated (UK). */
	'UnincorporatedAssociationCharity': { termType: 'NamedNode', value: 'https://schema.org/UnincorporatedAssociationCharity', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/UnincorporatedAssociationCharity' } as NamedNode,
	/** The price asked for a given offer by the respective organization or person. */
	'UnitPriceSpecification': { termType: 'NamedNode', value: 'https://schema.org/UnitPriceSpecification', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/UnitPriceSpecification' } as NamedNode,
	/** Indicates that a document has no particular or special standing (e.g. a republication of a law by a private publisher). */
	'UnofficialLegalValue': { termType: 'NamedNode', value: 'https://schema.org/UnofficialLegalValue', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/UnofficialLegalValue' } as NamedNode,
	/** The act of managing by changing/editing the state of the object. */
	'UpdateAction': { termType: 'NamedNode', value: 'https://schema.org/UpdateAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/UpdateAction' } as NamedNode,
	/** A specific branch of medical science that is concerned with the diagnosis and treatment of diseases pertaining to the urinary tract and the urogenital system. */
	'Urologic': { termType: 'NamedNode', value: 'https://schema.org/Urologic', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Urologic' } as NamedNode,
	/** Content about how, when, frequency and dosage of a topic. */
	'UsageOrScheduleHealthAspect': { termType: 'NamedNode', value: 'https://schema.org/UsageOrScheduleHealthAspect', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/UsageOrScheduleHealthAspect' } as NamedNode,
	/** The act of applying an object to its intended purpose. */
	'UseAction': { termType: 'NamedNode', value: 'https://schema.org/UseAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/UseAction' } as NamedNode,
	/** Indicates that the item is used. */
	'UsedCondition': { termType: 'NamedNode', value: 'https://schema.org/UsedCondition', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/UsedCondition' } as NamedNode,
	/** UserInteraction and its subtypes is an old way of talking about users interacting with pages. It is generally better to use [[Action]]-based vocabulary, alongside types such as [[Comment]]. */
	'UserBlocks': { termType: 'NamedNode', value: 'https://schema.org/UserBlocks', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/UserBlocks' } as NamedNode,
	/** UserInteraction and its subtypes is an old way of talking about users interacting with pages. It is generally better to use [[Action]]-based vocabulary, alongside types such as [[Comment]]. */
	'UserCheckins': { termType: 'NamedNode', value: 'https://schema.org/UserCheckins', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/UserCheckins' } as NamedNode,
	/** UserInteraction and its subtypes is an old way of talking about users interacting with pages. It is generally better to use [[Action]]-based vocabulary, alongside types such as [[Comment]]. */
	'UserComments': { termType: 'NamedNode', value: 'https://schema.org/UserComments', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/UserComments' } as NamedNode,
	/** UserInteraction and its subtypes is an old way of talking about users interacting with pages. It is generally better to use [[Action]]-based vocabulary, alongside types such as [[Comment]]. */
	'UserDownloads': { termType: 'NamedNode', value: 'https://schema.org/UserDownloads', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/UserDownloads' } as NamedNode,
	/** UserInteraction and its subtypes is an old way of talking about users interacting with pages. It is generally better to use [[Action]]-based vocabulary, alongside types such as [[Comment]]. */
	'UserInteraction': { termType: 'NamedNode', value: 'https://schema.org/UserInteraction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/UserInteraction' } as NamedNode,
	/** UserInteraction and its subtypes is an old way of talking about users interacting with pages. It is generally better to use [[Action]]-based vocabulary, alongside types such as [[Comment]]. */
	'UserLikes': { termType: 'NamedNode', value: 'https://schema.org/UserLikes', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/UserLikes' } as NamedNode,
	/** UserInteraction and its subtypes is an old way of talking about users interacting with pages. It is generally better to use [[Action]]-based vocabulary, alongside types such as [[Comment]]. */
	'UserPageVisits': { termType: 'NamedNode', value: 'https://schema.org/UserPageVisits', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/UserPageVisits' } as NamedNode,
	/** UserInteraction and its subtypes is an old way of talking about users interacting with pages. It is generally better to use [[Action]]-based vocabulary, alongside types such as [[Comment]]. */
	'UserPlays': { termType: 'NamedNode', value: 'https://schema.org/UserPlays', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/UserPlays' } as NamedNode,
	/** UserInteraction and its subtypes is an old way of talking about users interacting with pages. It is generally better to use [[Action]]-based vocabulary, alongside types such as [[Comment]]. */
	'UserPlusOnes': { termType: 'NamedNode', value: 'https://schema.org/UserPlusOnes', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/UserPlusOnes' } as NamedNode,
	/** A review created by an end-user (e.g. consumer, purchaser, attendee etc.), in contrast with [[CriticReview]]. */
	'UserReview': { termType: 'NamedNode', value: 'https://schema.org/UserReview', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/UserReview' } as NamedNode,
	/** UserInteraction and its subtypes is an old way of talking about users interacting with pages. It is generally better to use [[Action]]-based vocabulary, alongside types such as [[Comment]]. */
	'UserTweets': { termType: 'NamedNode', value: 'https://schema.org/UserTweets', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/UserTweets' } as NamedNode,
	/** A kind of lodging business that focuses on renting single properties for limited time. */
	'VacationRental': { termType: 'NamedNode', value: 'https://schema.org/VacationRental', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/VacationRental' } as NamedNode,
	/** A diet exclusive of all animal products. */
	'VeganDiet': { termType: 'NamedNode', value: 'https://schema.org/VeganDiet', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/VeganDiet' } as NamedNode,
	/** A diet exclusive of animal meat. */
	'VegetarianDiet': { termType: 'NamedNode', value: 'https://schema.org/VegetarianDiet', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/VegetarianDiet' } as NamedNode,
	/** A vehicle is a device that is designed or used to transport people or cargo over land, water, air, or through space. */
	'Vehicle': { termType: 'NamedNode', value: 'https://schema.org/Vehicle', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Vehicle' } as NamedNode,
	/** A type of blood vessel that specifically carries blood to the heart. */
	'Vein': { termType: 'NamedNode', value: 'https://schema.org/Vein', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Vein' } as NamedNode,
	/** A venue map (e.g. for malls, auditoriums, museums, etc.). */
	'VenueMap': { termType: 'NamedNode', value: 'https://schema.org/VenueMap', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/VenueMap' } as NamedNode,
	/** A component of the human body circulatory system comprised of an intricate network of hollow tubes that transport blood throughout the entire body. */
	'Vessel': { termType: 'NamedNode', value: 'https://schema.org/Vessel', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Vessel' } as NamedNode,
	/** A vet's office. */
	'VeterinaryCare': { termType: 'NamedNode', value: 'https://schema.org/VeterinaryCare', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/VeterinaryCare' } as NamedNode,
	/** Web page type: Video gallery page. */
	'VideoGallery': { termType: 'NamedNode', value: 'https://schema.org/VideoGallery', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/VideoGallery' } as NamedNode,
	/** A video game is an electronic game that involves human interaction with a user interface to generate visual feedback on a video device. */
	'VideoGame': { termType: 'NamedNode', value: 'https://schema.org/VideoGame', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/VideoGame' } as NamedNode,
	/** A short segment/part of a video game. */
	'VideoGameClip': { termType: 'NamedNode', value: 'https://schema.org/VideoGameClip', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/VideoGameClip' } as NamedNode,
	/** A video game series. */
	'VideoGameSeries': { termType: 'NamedNode', value: 'https://schema.org/VideoGameSeries', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/VideoGameSeries' } as NamedNode,
	/** A video file. */
	'VideoObject': { termType: 'NamedNode', value: 'https://schema.org/VideoObject', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/VideoObject' } as NamedNode,
	/** A specific and exact (byte-for-byte) version of a [[VideoObject]]. Two byte-for-byte identical files, for the purposes of this type, considered identical. If they have different embedded metadata the files will differ. Different external facts about the files, e.g. creator or dateCreated that aren't represented in their actual content, do not affect this notion of identity. */
	'VideoObjectSnapshot': { termType: 'NamedNode', value: 'https://schema.org/VideoObjectSnapshot', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/VideoObjectSnapshot' } as NamedNode,
	/** The act of consuming static visual content. */
	'ViewAction': { termType: 'NamedNode', value: 'https://schema.org/ViewAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ViewAction' } as NamedNode,
	/** VinylFormat. */
	'VinylFormat': { termType: 'NamedNode', value: 'https://schema.org/VinylFormat', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/VinylFormat' } as NamedNode,
	/** Item shows or promotes violence. */
	'ViolenceConsideration': { termType: 'NamedNode', value: 'https://schema.org/ViolenceConsideration', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ViolenceConsideration' } as NamedNode,
	/** An online or virtual location for attending events. For example, one may attend an online seminar or educational event. While a virtual location may be used as the location of an event, virtual locations should not be confused with physical locations in the real world. */
	'VirtualLocation': { termType: 'NamedNode', value: 'https://schema.org/VirtualLocation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/VirtualLocation' } as NamedNode,
	/** Pathogenic virus that causes viral infection. */
	'Virus': { termType: 'NamedNode', value: 'https://schema.org/Virus', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Virus' } as NamedNode,
	/** Event type: Visual arts event. */
	'VisualArtsEvent': { termType: 'NamedNode', value: 'https://schema.org/VisualArtsEvent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/VisualArtsEvent' } as NamedNode,
	/** A work of art that is primarily visual in character. */
	'VisualArtwork': { termType: 'NamedNode', value: 'https://schema.org/VisualArtwork', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/VisualArtwork' } as NamedNode,
	/** Vital signs are measures of various physiological functions in order to assess the most basic body functions. */
	'VitalSign': { termType: 'NamedNode', value: 'https://schema.org/VitalSign', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/VitalSign' } as NamedNode,
	/** A volcano, like Fujisan. */
	'Volcano': { termType: 'NamedNode', value: 'https://schema.org/Volcano', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Volcano' } as NamedNode,
	/** The act of expressing a preference from a fixed/finite/structured set of choices/options. */
	'VoteAction': { termType: 'NamedNode', value: 'https://schema.org/VoteAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/VoteAction' } as NamedNode,
	/** An advertising section of the page. */
	'WPAdBlock': { termType: 'NamedNode', value: 'https://schema.org/WPAdBlock', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WPAdBlock' } as NamedNode,
	/** The footer section of the page. */
	'WPFooter': { termType: 'NamedNode', value: 'https://schema.org/WPFooter', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WPFooter' } as NamedNode,
	/** The header section of the page. */
	'WPHeader': { termType: 'NamedNode', value: 'https://schema.org/WPHeader', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WPHeader' } as NamedNode,
	/** A sidebar section of the page. */
	'WPSideBar': { termType: 'NamedNode', value: 'https://schema.org/WPSideBar', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WPSideBar' } as NamedNode,
	/** The act of expressing a desire about the object. An agent wants an object. */
	'WantAction': { termType: 'NamedNode', value: 'https://schema.org/WantAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WantAction' } as NamedNode,
	/** A structured value representing the duration and scope of services that will be provided to a customer free of charge in case of a defect or malfunction of a product. */
	'WarrantyPromise': { termType: 'NamedNode', value: 'https://schema.org/WarrantyPromise', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WarrantyPromise' } as NamedNode,
	/** A range of services that will be provided to a customer free of charge in case of a defect or malfunction of a product.\n\nCommonly used values:\n\n* http://purl.org/goodrelations/v1#Labor-BringIn\n* http://purl.org/goodrelations/v1#PartsAndLabor-BringIn\n* http://purl.org/goodrelations/v1#PartsAndLabor-PickUp
       */
	'WarrantyScope': { termType: 'NamedNode', value: 'https://schema.org/WarrantyScope', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WarrantyScope' } as NamedNode,
	/** The act of consuming dynamic/moving visual content. */
	'WatchAction': { termType: 'NamedNode', value: 'https://schema.org/WatchAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WatchAction' } as NamedNode,
	/** A waterfall, like Niagara. */
	'Waterfall': { termType: 'NamedNode', value: 'https://schema.org/Waterfall', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Waterfall' } as NamedNode,
	/** The item is intended to induce bodily harm, for example guns, mace, combat knives, brass knuckles, nail or other bombs, and spears. */
	'WeaponConsideration': { termType: 'NamedNode', value: 'https://schema.org/WeaponConsideration', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WeaponConsideration' } as NamedNode,
	/** The act of dressing oneself in clothing. */
	'WearAction': { termType: 'NamedNode', value: 'https://schema.org/WearAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WearAction' } as NamedNode,
	/** Measurement of the back section, for example of a jacket */
	'WearableMeasurementBack': { termType: 'NamedNode', value: 'https://schema.org/WearableMeasurementBack', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WearableMeasurementBack' } as NamedNode,
	/** Measurement of the chest/bust section, for example of a suit */
	'WearableMeasurementChestOrBust': { termType: 'NamedNode', value: 'https://schema.org/WearableMeasurementChestOrBust', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WearableMeasurementChestOrBust' } as NamedNode,
	/** Measurement of the collar, for example of a shirt */
	'WearableMeasurementCollar': { termType: 'NamedNode', value: 'https://schema.org/WearableMeasurementCollar', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WearableMeasurementCollar' } as NamedNode,
	/** Measurement of the cup, for example of a bra */
	'WearableMeasurementCup': { termType: 'NamedNode', value: 'https://schema.org/WearableMeasurementCup', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WearableMeasurementCup' } as NamedNode,
	/** Measurement of the height, for example the heel height of a shoe */
	'WearableMeasurementHeight': { termType: 'NamedNode', value: 'https://schema.org/WearableMeasurementHeight', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WearableMeasurementHeight' } as NamedNode,
	/** Measurement of the hip section, for example of a skirt */
	'WearableMeasurementHips': { termType: 'NamedNode', value: 'https://schema.org/WearableMeasurementHips', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WearableMeasurementHips' } as NamedNode,
	/** Measurement of the inseam, for example of pants */
	'WearableMeasurementInseam': { termType: 'NamedNode', value: 'https://schema.org/WearableMeasurementInseam', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WearableMeasurementInseam' } as NamedNode,
	/** Represents the length, for example of a dress */
	'WearableMeasurementLength': { termType: 'NamedNode', value: 'https://schema.org/WearableMeasurementLength', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WearableMeasurementLength' } as NamedNode,
	/** Measurement of the outside leg, for example of pants */
	'WearableMeasurementOutsideLeg': { termType: 'NamedNode', value: 'https://schema.org/WearableMeasurementOutsideLeg', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WearableMeasurementOutsideLeg' } as NamedNode,
	/** Measurement of the sleeve length, for example of a shirt */
	'WearableMeasurementSleeve': { termType: 'NamedNode', value: 'https://schema.org/WearableMeasurementSleeve', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WearableMeasurementSleeve' } as NamedNode,
	/** Enumerates common types of measurement for wearables products. */
	'WearableMeasurementTypeEnumeration': { termType: 'NamedNode', value: 'https://schema.org/WearableMeasurementTypeEnumeration', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WearableMeasurementTypeEnumeration' } as NamedNode,
	/** Measurement of the waist section, for example of pants */
	'WearableMeasurementWaist': { termType: 'NamedNode', value: 'https://schema.org/WearableMeasurementWaist', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WearableMeasurementWaist' } as NamedNode,
	/** Measurement of the width, for example of shoes */
	'WearableMeasurementWidth': { termType: 'NamedNode', value: 'https://schema.org/WearableMeasurementWidth', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WearableMeasurementWidth' } as NamedNode,
	/** Size group "Big" for wearables. */
	'WearableSizeGroupBig': { termType: 'NamedNode', value: 'https://schema.org/WearableSizeGroupBig', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WearableSizeGroupBig' } as NamedNode,
	/** Size group "Boys" for wearables. */
	'WearableSizeGroupBoys': { termType: 'NamedNode', value: 'https://schema.org/WearableSizeGroupBoys', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WearableSizeGroupBoys' } as NamedNode,
	/** Enumerates common size groups (also known as "size types") for wearable products. */
	'WearableSizeGroupEnumeration': { termType: 'NamedNode', value: 'https://schema.org/WearableSizeGroupEnumeration', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WearableSizeGroupEnumeration' } as NamedNode,
	/** Size group "Extra Short" for wearables. */
	'WearableSizeGroupExtraShort': { termType: 'NamedNode', value: 'https://schema.org/WearableSizeGroupExtraShort', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WearableSizeGroupExtraShort' } as NamedNode,
	/** Size group "Extra Tall" for wearables. */
	'WearableSizeGroupExtraTall': { termType: 'NamedNode', value: 'https://schema.org/WearableSizeGroupExtraTall', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WearableSizeGroupExtraTall' } as NamedNode,
	/** Size group "Girls" for wearables. */
	'WearableSizeGroupGirls': { termType: 'NamedNode', value: 'https://schema.org/WearableSizeGroupGirls', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WearableSizeGroupGirls' } as NamedNode,
	/** Size group "Husky" (or "Stocky") for wearables. */
	'WearableSizeGroupHusky': { termType: 'NamedNode', value: 'https://schema.org/WearableSizeGroupHusky', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WearableSizeGroupHusky' } as NamedNode,
	/** Size group "Infants" for wearables. */
	'WearableSizeGroupInfants': { termType: 'NamedNode', value: 'https://schema.org/WearableSizeGroupInfants', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WearableSizeGroupInfants' } as NamedNode,
	/** Size group "Juniors" for wearables. */
	'WearableSizeGroupJuniors': { termType: 'NamedNode', value: 'https://schema.org/WearableSizeGroupJuniors', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WearableSizeGroupJuniors' } as NamedNode,
	/** Size group "Maternity" for wearables. */
	'WearableSizeGroupMaternity': { termType: 'NamedNode', value: 'https://schema.org/WearableSizeGroupMaternity', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WearableSizeGroupMaternity' } as NamedNode,
	/** Size group "Mens" for wearables. */
	'WearableSizeGroupMens': { termType: 'NamedNode', value: 'https://schema.org/WearableSizeGroupMens', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WearableSizeGroupMens' } as NamedNode,
	/** Size group "Misses" (also known as "Missy") for wearables. */
	'WearableSizeGroupMisses': { termType: 'NamedNode', value: 'https://schema.org/WearableSizeGroupMisses', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WearableSizeGroupMisses' } as NamedNode,
	/** Size group "Petite" for wearables. */
	'WearableSizeGroupPetite': { termType: 'NamedNode', value: 'https://schema.org/WearableSizeGroupPetite', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WearableSizeGroupPetite' } as NamedNode,
	/** Size group "Plus" for wearables. */
	'WearableSizeGroupPlus': { termType: 'NamedNode', value: 'https://schema.org/WearableSizeGroupPlus', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WearableSizeGroupPlus' } as NamedNode,
	/** Size group "Regular" for wearables. */
	'WearableSizeGroupRegular': { termType: 'NamedNode', value: 'https://schema.org/WearableSizeGroupRegular', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WearableSizeGroupRegular' } as NamedNode,
	/** Size group "Short" for wearables. */
	'WearableSizeGroupShort': { termType: 'NamedNode', value: 'https://schema.org/WearableSizeGroupShort', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WearableSizeGroupShort' } as NamedNode,
	/** Size group "Tall" for wearables. */
	'WearableSizeGroupTall': { termType: 'NamedNode', value: 'https://schema.org/WearableSizeGroupTall', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WearableSizeGroupTall' } as NamedNode,
	/** Size group "Womens" for wearables. */
	'WearableSizeGroupWomens': { termType: 'NamedNode', value: 'https://schema.org/WearableSizeGroupWomens', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WearableSizeGroupWomens' } as NamedNode,
	/** Australian size system for wearables. */
	'WearableSizeSystemAU': { termType: 'NamedNode', value: 'https://schema.org/WearableSizeSystemAU', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WearableSizeSystemAU' } as NamedNode,
	/** Brazilian size system for wearables. */
	'WearableSizeSystemBR': { termType: 'NamedNode', value: 'https://schema.org/WearableSizeSystemBR', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WearableSizeSystemBR' } as NamedNode,
	/** Chinese size system for wearables. */
	'WearableSizeSystemCN': { termType: 'NamedNode', value: 'https://schema.org/WearableSizeSystemCN', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WearableSizeSystemCN' } as NamedNode,
	/** Continental size system for wearables. */
	'WearableSizeSystemContinental': { termType: 'NamedNode', value: 'https://schema.org/WearableSizeSystemContinental', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WearableSizeSystemContinental' } as NamedNode,
	/** German size system for wearables. */
	'WearableSizeSystemDE': { termType: 'NamedNode', value: 'https://schema.org/WearableSizeSystemDE', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WearableSizeSystemDE' } as NamedNode,
	/** EN 13402 (joint European standard for size labelling of clothes). */
	'WearableSizeSystemEN13402': { termType: 'NamedNode', value: 'https://schema.org/WearableSizeSystemEN13402', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WearableSizeSystemEN13402' } as NamedNode,
	/** Enumerates common size systems specific for wearable products */
	'WearableSizeSystemEnumeration': { termType: 'NamedNode', value: 'https://schema.org/WearableSizeSystemEnumeration', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WearableSizeSystemEnumeration' } as NamedNode,
	/** European size system for wearables. */
	'WearableSizeSystemEurope': { termType: 'NamedNode', value: 'https://schema.org/WearableSizeSystemEurope', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WearableSizeSystemEurope' } as NamedNode,
	/** French size system for wearables. */
	'WearableSizeSystemFR': { termType: 'NamedNode', value: 'https://schema.org/WearableSizeSystemFR', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WearableSizeSystemFR' } as NamedNode,
	/** GS1 (formerly NRF) size system for wearables. */
	'WearableSizeSystemGS1': { termType: 'NamedNode', value: 'https://schema.org/WearableSizeSystemGS1', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WearableSizeSystemGS1' } as NamedNode,
	/** Italian size system for wearables. */
	'WearableSizeSystemIT': { termType: 'NamedNode', value: 'https://schema.org/WearableSizeSystemIT', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WearableSizeSystemIT' } as NamedNode,
	/** Japanese size system for wearables. */
	'WearableSizeSystemJP': { termType: 'NamedNode', value: 'https://schema.org/WearableSizeSystemJP', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WearableSizeSystemJP' } as NamedNode,
	/** Mexican size system for wearables. */
	'WearableSizeSystemMX': { termType: 'NamedNode', value: 'https://schema.org/WearableSizeSystemMX', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WearableSizeSystemMX' } as NamedNode,
	/** United Kingdom size system for wearables. */
	'WearableSizeSystemUK': { termType: 'NamedNode', value: 'https://schema.org/WearableSizeSystemUK', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WearableSizeSystemUK' } as NamedNode,
	/** United States size system for wearables. */
	'WearableSizeSystemUS': { termType: 'NamedNode', value: 'https://schema.org/WearableSizeSystemUS', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WearableSizeSystemUS' } as NamedNode,
	/** An application programming interface accessible over Web/Internet technologies. */
	'WebAPI': { termType: 'NamedNode', value: 'https://schema.org/WebAPI', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WebAPI' } as NamedNode,
	/** Web applications. */
	'WebApplication': { termType: 'NamedNode', value: 'https://schema.org/WebApplication', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WebApplication' } as NamedNode,
	/** WebContent is a type representing all [[WebPage]], [[WebSite]] and [[WebPageElement]] content. It is sometimes the case that detailed distinctions between Web pages, sites and their parts are not always important or obvious. The  [[WebContent]] type makes it easier to describe Web-addressable content without requiring such distinctions to always be stated. (The intent is that the existing types [[WebPage]], [[WebSite]] and [[WebPageElement]] will eventually be declared as subtypes of [[WebContent]].) */
	'WebContent': { termType: 'NamedNode', value: 'https://schema.org/WebContent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WebContent' } as NamedNode,
	/** A web page. Every web page is implicitly assumed to be declared to be of type WebPage, so the various properties about that webpage, such as <code>breadcrumb</code> may be used. We recommend explicit declaration if these properties are specified, but if they are found outside of an itemscope, they will be assumed to be about the page. */
	'WebPage': { termType: 'NamedNode', value: 'https://schema.org/WebPage', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WebPage' } as NamedNode,
	/** A web page element, like a table or an image. */
	'WebPageElement': { termType: 'NamedNode', value: 'https://schema.org/WebPageElement', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WebPageElement' } as NamedNode,
	/** A WebSite is a set of related web pages and other items typically served from a single web domain and accessible via URLs. */
	'WebSite': { termType: 'NamedNode', value: 'https://schema.org/WebSite', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WebSite' } as NamedNode,
	/** The day of the week between Tuesday and Thursday. */
	'Wednesday': { termType: 'NamedNode', value: 'https://schema.org/Wednesday', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Wednesday' } as NamedNode,
	/** The conventional Western system of medicine, that aims to apply the best available evidence gained from the scientific method to clinical decision making. Also known as conventional or Western medicine. */
	'WesternConventional': { termType: 'NamedNode', value: 'https://schema.org/WesternConventional', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WesternConventional' } as NamedNode,
	/** The drug's cost represents the wholesale acquisition cost of the drug. */
	'Wholesale': { termType: 'NamedNode', value: 'https://schema.org/Wholesale', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Wholesale' } as NamedNode,
	/** A wholesale store. */
	'WholesaleStore': { termType: 'NamedNode', value: 'https://schema.org/WholesaleStore', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WholesaleStore' } as NamedNode,
	/** The act of achieving victory in a competitive activity. */
	'WinAction': { termType: 'NamedNode', value: 'https://schema.org/WinAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WinAction' } as NamedNode,
	/** A winery. */
	'Winery': { termType: 'NamedNode', value: 'https://schema.org/Winery', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Winery' } as NamedNode,
	/** Withdrawn. */
	'Withdrawn': { termType: 'NamedNode', value: 'https://schema.org/Withdrawn', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Withdrawn' } as NamedNode,
	/** A program with both an educational and employment component. Typically based at a workplace and structured around work-based learning, with the aim of instilling competencies related to an occupation. WorkBasedProgram is used to distinguish programs such as apprenticeships from school, college or other classroom based educational programs. */
	'WorkBasedProgram': { termType: 'NamedNode', value: 'https://schema.org/WorkBasedProgram', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WorkBasedProgram' } as NamedNode,
	/** A Workers Union (also known as a Labor Union, Labour Union, or Trade Union) is an organization that promotes the interests of its worker members by collectively bargaining with management, organizing, and political lobbying. */
	'WorkersUnion': { termType: 'NamedNode', value: 'https://schema.org/WorkersUnion', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WorkersUnion' } as NamedNode,
	/** The act of authoring written creative content. */
	'WriteAction': { termType: 'NamedNode', value: 'https://schema.org/WriteAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WriteAction' } as NamedNode,
	/** Permission to write or edit the document. */
	'WritePermission': { termType: 'NamedNode', value: 'https://schema.org/WritePermission', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/WritePermission' } as NamedNode,
	/** Text representing an XPath (typically but not necessarily version 1.0). */
	'XPathType': { termType: 'NamedNode', value: 'https://schema.org/XPathType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/XPathType' } as NamedNode,
	/** X-ray imaging. */
	'XRay': { termType: 'NamedNode', value: 'https://schema.org/XRay', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/XRay' } as NamedNode,
	/** The airline boards by zones of the plane. */
	'ZoneBoardingPolicy': { termType: 'NamedNode', value: 'https://schema.org/ZoneBoardingPolicy', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ZoneBoardingPolicy' } as NamedNode,
	/** A zoo. */
	'Zoo': { termType: 'NamedNode', value: 'https://schema.org/Zoo', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/Zoo' } as NamedNode,
	/** The subject matter of the content. */
	'about': { termType: 'NamedNode', value: 'https://schema.org/about', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/about' } as NamedNode,
	/** Indicates whether the book is an abridged edition. */
	'abridged': { termType: 'NamedNode', value: 'https://schema.org/abridged', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/abridged' } as NamedNode,
	/** An abstract is a short description that summarizes a [[CreativeWork]]. */
	'abstract': { termType: 'NamedNode', value: 'https://schema.org/abstract', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/abstract' } as NamedNode,
	/** The time needed to accelerate the vehicle from a given start velocity to a given target velocity.\n\nTypical unit code(s): SEC for seconds\n\n* Note: There are unfortunately no standard unit codes for seconds/0..100 km/h or seconds/0..60 mph. Simply use "SEC" for seconds and indicate the velocities in the [[name]] of the [[QuantitativeValue]], or use [[valueReference]] with a [[QuantitativeValue]] of 0..60 mph or 0..100 km/h to specify the reference speeds. */
	'accelerationTime': { termType: 'NamedNode', value: 'https://schema.org/accelerationTime', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/accelerationTime' } as NamedNode,
	/** The answer(s) that has been accepted as best, typically on a Question/Answer site. Sites vary in their selection mechanisms, e.g. drawing on community opinion and/or the view of the Question author. */
	'acceptedAnswer': { termType: 'NamedNode', value: 'https://schema.org/acceptedAnswer', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/acceptedAnswer' } as NamedNode,
	/** The offer(s) -- e.g., product, quantity and price combinations -- included in the order. */
	'acceptedOffer': { termType: 'NamedNode', value: 'https://schema.org/acceptedOffer', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/acceptedOffer' } as NamedNode,
	/** The payment method(s) accepted by seller for this offer. */
	'acceptedPaymentMethod': { termType: 'NamedNode', value: 'https://schema.org/acceptedPaymentMethod', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/acceptedPaymentMethod' } as NamedNode,
	/** Indicates whether a FoodEstablishment accepts reservations. Values can be Boolean, an URL at which reservations can be made or (for backwards compatibility) the strings ```Yes``` or ```No```. */
	'acceptsReservations': { termType: 'NamedNode', value: 'https://schema.org/acceptsReservations', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/acceptsReservations' } as NamedNode,
	/** Password, PIN, or access code needed for delivery (e.g. from a locker). */
	'accessCode': { termType: 'NamedNode', value: 'https://schema.org/accessCode', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/accessCode' } as NamedNode,
	/** The human sensory perceptual system or cognitive faculty through which a person may process or perceive information. Values should be drawn from the [approved vocabulary](https://www.w3.org/2021/a11y-discov-vocab/latest/#accessMode-vocabulary). */
	'accessMode': { termType: 'NamedNode', value: 'https://schema.org/accessMode', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/accessMode' } as NamedNode,
	/** A list of single or combined accessModes that are sufficient to understand all the intellectual content of a resource. Values should be drawn from the [approved vocabulary](https://www.w3.org/2021/a11y-discov-vocab/latest/#accessModeSufficient-vocabulary). */
	'accessModeSufficient': { termType: 'NamedNode', value: 'https://schema.org/accessModeSufficient', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/accessModeSufficient' } as NamedNode,
	/** Indicates that the resource is compatible with the referenced accessibility API. Values should be drawn from the [approved vocabulary](https://www.w3.org/2021/a11y-discov-vocab/latest/#accessibilityAPI-vocabulary). */
	'accessibilityAPI': { termType: 'NamedNode', value: 'https://schema.org/accessibilityAPI', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/accessibilityAPI' } as NamedNode,
	/** Identifies input methods that are sufficient to fully control the described resource. Values should be drawn from the [approved vocabulary](https://www.w3.org/2021/a11y-discov-vocab/latest/#accessibilityControl-vocabulary). */
	'accessibilityControl': { termType: 'NamedNode', value: 'https://schema.org/accessibilityControl', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/accessibilityControl' } as NamedNode,
	/** Content features of the resource, such as accessible media, alternatives and supported enhancements for accessibility. Values should be drawn from the [approved vocabulary](https://www.w3.org/2021/a11y-discov-vocab/latest/#accessibilityFeature-vocabulary). */
	'accessibilityFeature': { termType: 'NamedNode', value: 'https://schema.org/accessibilityFeature', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/accessibilityFeature' } as NamedNode,
	/** A characteristic of the described resource that is physiologically dangerous to some users. Related to WCAG 2.0 guideline 2.3. Values should be drawn from the [approved vocabulary](https://www.w3.org/2021/a11y-discov-vocab/latest/#accessibilityHazard-vocabulary). */
	'accessibilityHazard': { termType: 'NamedNode', value: 'https://schema.org/accessibilityHazard', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/accessibilityHazard' } as NamedNode,
	/** A human-readable summary of specific accessibility features or deficiencies, consistent with the other accessibility metadata but expressing subtleties such as "short descriptions are present but long descriptions will be needed for non-visual users" or "short descriptions are present and no long descriptions are needed." */
	'accessibilitySummary': { termType: 'NamedNode', value: 'https://schema.org/accessibilitySummary', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/accessibilitySummary' } as NamedNode,
	/** Category of an [[Accommodation]], following real estate conventions, e.g. RESO (see [PropertySubType](https://ddwiki.reso.org/display/DDW17/PropertySubType+Field), and [PropertyType](https://ddwiki.reso.org/display/DDW17/PropertyType+Field) fields  for suggested values). */
	'accommodationCategory': { termType: 'NamedNode', value: 'https://schema.org/accommodationCategory', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/accommodationCategory' } as NamedNode,
	/** A floorplan of some [[Accommodation]]. */
	'accommodationFloorPlan': { termType: 'NamedNode', value: 'https://schema.org/accommodationFloorPlan', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/accommodationFloorPlan' } as NamedNode,
	/** The identifier for the account the payment will be applied to. */
	'accountId': { termType: 'NamedNode', value: 'https://schema.org/accountId', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/accountId' } as NamedNode,
	/** A minimum amount that has to be paid in every month. */
	'accountMinimumInflow': { termType: 'NamedNode', value: 'https://schema.org/accountMinimumInflow', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/accountMinimumInflow' } as NamedNode,
	/** An overdraft is an extension of credit from a lending institution when an account reaches zero. An overdraft allows the individual to continue withdrawing money even if the account has no funds in it. Basically the bank allows people to borrow a set amount of money. */
	'accountOverdraftLimit': { termType: 'NamedNode', value: 'https://schema.org/accountOverdraftLimit', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/accountOverdraftLimit' } as NamedNode,
	/** Specifies the Person that is legally accountable for the CreativeWork. */
	'accountablePerson': { termType: 'NamedNode', value: 'https://schema.org/accountablePerson', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/accountablePerson' } as NamedNode,
	/** Indicates a page documenting how licenses can be purchased or otherwise acquired, for the current item. */
	'acquireLicensePage': { termType: 'NamedNode', value: 'https://schema.org/acquireLicensePage', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/acquireLicensePage' } as NamedNode,
	/** The organization or person from which the product was acquired. */
	'acquiredFrom': { termType: 'NamedNode', value: 'https://schema.org/acquiredFrom', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/acquiredFrom' } as NamedNode,
	/** The ACRISS Car Classification Code is a code used by many car rental companies, for classifying vehicles. ACRISS stands for Association of Car Rental Industry Systems and Standards. */
	'acrissCode': { termType: 'NamedNode', value: 'https://schema.org/acrissCode', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/acrissCode' } as NamedNode,
	/** A set of requirements that must be fulfilled in order to perform an Action. If more than one value is specified, fulfilling one set of requirements will allow the Action to be performed. */
	'actionAccessibilityRequirement': { termType: 'NamedNode', value: 'https://schema.org/actionAccessibilityRequirement', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/actionAccessibilityRequirement' } as NamedNode,
	/** An application that can complete the request. */
	'actionApplication': { termType: 'NamedNode', value: 'https://schema.org/actionApplication', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/actionApplication' } as NamedNode,
	/** A sub property of object. The options subject to this action. */
	'actionOption': { termType: 'NamedNode', value: 'https://schema.org/actionOption', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/actionOption' } as NamedNode,
	/** The high level platform(s) where the Action can be performed for the given URL. To specify a specific application or operating system instance, use actionApplication. */
	'actionPlatform': { termType: 'NamedNode', value: 'https://schema.org/actionPlatform', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/actionPlatform' } as NamedNode,
	/** Indicates the current disposition of the Action. */
	'actionStatus': { termType: 'NamedNode', value: 'https://schema.org/actionStatus', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/actionStatus' } as NamedNode,
	/** For a [[NewsMediaOrganization]] or other news-related [[Organization]], a statement about public engagement activities (for news media, the newsroom’s), including involving the public - digitally or otherwise -- in coverage decisions, reporting and activities after publication. */
	'actionableFeedbackPolicy': { termType: 'NamedNode', value: 'https://schema.org/actionableFeedbackPolicy', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/actionableFeedbackPolicy' } as NamedNode,
	/** An active ingredient, typically chemical compounds and/or biologic substances. */
	'activeIngredient': { termType: 'NamedNode', value: 'https://schema.org/activeIngredient', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/activeIngredient' } as NamedNode,
	/** Length of time to engage in the activity. */
	'activityDuration': { termType: 'NamedNode', value: 'https://schema.org/activityDuration', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/activityDuration' } as NamedNode,
	/** How often one should engage in the activity. */
	'activityFrequency': { termType: 'NamedNode', value: 'https://schema.org/activityFrequency', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/activityFrequency' } as NamedNode,
	/** An actor, e.g. in TV, radio, movie, video games etc., or in an event. Actors can be associated with individual items or with a series, episode, clip. */
	'actor': { termType: 'NamedNode', value: 'https://schema.org/actor', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/actor' } as NamedNode,
	/** An actor, e.g. in TV, radio, movie, video games etc. Actors can be associated with individual items or with a series, episode, clip. */
	'actors': { termType: 'NamedNode', value: 'https://schema.org/actors', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/actors' } as NamedNode,
	/** An additional offer that can only be obtained in combination with the first base offer (e.g. supplements and extensions that are available for a surcharge). */
	'addOn': { termType: 'NamedNode', value: 'https://schema.org/addOn', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/addOn' } as NamedNode,
	/** An additional name for a Person, can be used for a middle name. */
	'additionalName': { termType: 'NamedNode', value: 'https://schema.org/additionalName', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/additionalName' } as NamedNode,
	/** If responding yes, the number of guests who will attend in addition to the invitee. */
	'additionalNumberOfGuests': { termType: 'NamedNode', value: 'https://schema.org/additionalNumberOfGuests', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/additionalNumberOfGuests' } as NamedNode,
	/** A property-value pair representing an additional characteristic of the entity, e.g. a product feature or another characteristic for which there is no matching property in schema.org.\n\nNote: Publishers should be aware that applications designed to use specific schema.org properties (e.g. https://schema.org/width, https://schema.org/color, https://schema.org/gtin13, ...) will typically expect such data to be provided using those properties, rather than using the generic property/value mechanism.
 */
	'additionalProperty': { termType: 'NamedNode', value: 'https://schema.org/additionalProperty', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/additionalProperty' } as NamedNode,
	/** An additional type for the item, typically used for adding more specific types from external vocabularies in microdata syntax. This is a relationship between something and a class that the thing is in. Typically the value is a URI-identified RDF class, and in this case corresponds to the 
    use of rdf:type in RDF. Text values can be used sparingly, for cases where useful information can be added without their being an appropriate schema to reference. In the case of text values, the class label should follow the schema.org <a href="https://schema.org/docs/styleguide.html">style guide</a> */
	'additionalType': { termType: 'NamedNode', value: 'https://schema.org/additionalType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/additionalType' } as NamedNode,
	/** Any additional component of the exercise prescription that may need to be articulated to the patient. This may include the order of exercises, the number of repetitions of movement, quantitative distance, progressions over time, etc. */
	'additionalVariable': { termType: 'NamedNode', value: 'https://schema.org/additionalVariable', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/additionalVariable' } as NamedNode,
	/** Physical address of the item. */
	'address': { termType: 'NamedNode', value: 'https://schema.org/address', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/address' } as NamedNode,
	/** The country. For example, USA. You can also provide the two-letter [ISO 3166-1 alpha-2 country code](http://en.wikipedia.org/wiki/ISO_3166-1). */
	'addressCountry': { termType: 'NamedNode', value: 'https://schema.org/addressCountry', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/addressCountry' } as NamedNode,
	/** The locality in which the street address is, and which is in the region. For example, Mountain View. */
	'addressLocality': { termType: 'NamedNode', value: 'https://schema.org/addressLocality', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/addressLocality' } as NamedNode,
	/** The region in which the locality is, and which is in the country. For example, California or another appropriate first-level [Administrative division](https://en.wikipedia.org/wiki/List_of_administrative_divisions_by_country). */
	'addressRegion': { termType: 'NamedNode', value: 'https://schema.org/addressRegion', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/addressRegion' } as NamedNode,
	/** A route by which this drug may be administered, e.g. 'oral'. */
	'administrationRoute': { termType: 'NamedNode', value: 'https://schema.org/administrationRoute', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/administrationRoute' } as NamedNode,
	/** The amount of time that is required between accepting the offer and the actual usage of the resource or service. */
	'advanceBookingRequirement': { termType: 'NamedNode', value: 'https://schema.org/advanceBookingRequirement', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/advanceBookingRequirement' } as NamedNode,
	/** A possible complication and/or side effect of this therapy. If it is known that an adverse outcome is serious (resulting in death, disability, or permanent damage; requiring hospitalization; or otherwise life-threatening or requiring immediate medical attention), tag it as a seriousAdverseOutcome instead. */
	'adverseOutcome': { termType: 'NamedNode', value: 'https://schema.org/adverseOutcome', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/adverseOutcome' } as NamedNode,
	/** Drugs that affect the test's results. */
	'affectedBy': { termType: 'NamedNode', value: 'https://schema.org/affectedBy', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/affectedBy' } as NamedNode,
	/** An organization that this person is affiliated with. For example, a school/university, a club, or a team. */
	'affiliation': { termType: 'NamedNode', value: 'https://schema.org/affiliation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/affiliation' } as NamedNode,
	/** A media object representing the circumstances after performing this direction. */
	'afterMedia': { termType: 'NamedNode', value: 'https://schema.org/afterMedia', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/afterMedia' } as NamedNode,
	/** The direct performer or driver of the action (animate or inanimate). E.g. *John* wrote a book. */
	'agent': { termType: 'NamedNode', value: 'https://schema.org/agent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/agent' } as NamedNode,
	/** The overall rating, based on a collection of reviews or ratings, of the item. */
	'aggregateRating': { termType: 'NamedNode', value: 'https://schema.org/aggregateRating', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/aggregateRating' } as NamedNode,
	/** The kind of aircraft (e.g., "Boeing 747"). */
	'aircraft': { termType: 'NamedNode', value: 'https://schema.org/aircraft', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/aircraft' } as NamedNode,
	/** A music album. */
	'album': { termType: 'NamedNode', value: 'https://schema.org/album', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/album' } as NamedNode,
	/** Classification of the album by its type of content: soundtrack, live album, studio album, etc. */
	'albumProductionType': { termType: 'NamedNode', value: 'https://schema.org/albumProductionType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/albumProductionType' } as NamedNode,
	/** A release of this album. */
	'albumRelease': { termType: 'NamedNode', value: 'https://schema.org/albumRelease', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/albumRelease' } as NamedNode,
	/** The kind of release which this album is: single, EP or album. */
	'albumReleaseType': { termType: 'NamedNode', value: 'https://schema.org/albumReleaseType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/albumReleaseType' } as NamedNode,
	/** A collection of music albums. */
	'albums': { termType: 'NamedNode', value: 'https://schema.org/albums', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/albums' } as NamedNode,
	/** Any precaution, guidance, contraindication, etc. related to consumption of alcohol while taking this drug. */
	'alcoholWarning': { termType: 'NamedNode', value: 'https://schema.org/alcoholWarning', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/alcoholWarning' } as NamedNode,
	/** The algorithm or rules to follow to compute the score. */
	'algorithm': { termType: 'NamedNode', value: 'https://schema.org/algorithm', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/algorithm' } as NamedNode,
	/** A category of alignment between the learning resource and the framework node. Recommended values include: 'requires', 'textComplexity', 'readingLevel', and 'educationalSubject'. */
	'alignmentType': { termType: 'NamedNode', value: 'https://schema.org/alignmentType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/alignmentType' } as NamedNode,
	/** An alias for the item. */
	'alternateName': { termType: 'NamedNode', value: 'https://schema.org/alternateName', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/alternateName' } as NamedNode,
	/** A secondary title of the CreativeWork. */
	'alternativeHeadline': { termType: 'NamedNode', value: 'https://schema.org/alternativeHeadline', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/alternativeHeadline' } as NamedNode,
	/** Another gene which is a variation of this one. */
	'alternativeOf': { termType: 'NamedNode', value: 'https://schema.org/alternativeOf', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/alternativeOf' } as NamedNode,
	/** Alumni of an organization. */
	'alumni': { termType: 'NamedNode', value: 'https://schema.org/alumni', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/alumni' } as NamedNode,
	/** An organization that the person is an alumni of. */
	'alumniOf': { termType: 'NamedNode', value: 'https://schema.org/alumniOf', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/alumniOf' } as NamedNode,
	/** An amenity feature (e.g. a characteristic or service) of the Accommodation. This generic property does not make a statement about whether the feature is included in an offer for the main accommodation or available at extra costs. */
	'amenityFeature': { termType: 'NamedNode', value: 'https://schema.org/amenityFeature', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/amenityFeature' } as NamedNode,
	/** The amount of money. */
	'amount': { termType: 'NamedNode', value: 'https://schema.org/amount', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/amount' } as NamedNode,
	/** The quantity of the goods included in the offer. */
	'amountOfThisGood': { termType: 'NamedNode', value: 'https://schema.org/amountOfThisGood', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/amountOfThisGood' } as NamedNode,
	/** Indicates a specific [[CivicStructure]] or [[LocalBusiness]] associated with the SpecialAnnouncement. For example, a specific testing facility or business with special opening hours. For a larger geographic region like a quarantine of an entire region, use [[spatialCoverage]]. */
	'announcementLocation': { termType: 'NamedNode', value: 'https://schema.org/announcementLocation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/announcementLocation' } as NamedNode,
	/** The annual rate that is charged for borrowing (or made by investing), expressed as a single percentage number that represents the actual yearly cost of funds over the term of a loan. This includes any fees or additional costs associated with the transaction. */
	'annualPercentageRate': { termType: 'NamedNode', value: 'https://schema.org/annualPercentageRate', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/annualPercentageRate' } as NamedNode,
	/** The number of answers this question has received. */
	'answerCount': { termType: 'NamedNode', value: 'https://schema.org/answerCount', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/answerCount' } as NamedNode,
	/** A step-by-step or full explanation about Answer. Can outline how this Answer was achieved or contain more broad clarification or statement about it.  */
	'answerExplanation': { termType: 'NamedNode', value: 'https://schema.org/answerExplanation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/answerExplanation' } as NamedNode,
	/** The muscle whose action counteracts the specified muscle. */
	'antagonist': { termType: 'NamedNode', value: 'https://schema.org/antagonist', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/antagonist' } as NamedNode,
	/** Indicates an occurrence of a [[Claim]] in some [[CreativeWork]]. */
	'appearance': { termType: 'NamedNode', value: 'https://schema.org/appearance', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/appearance' } as NamedNode,
	/** A country where a particular merchant return policy applies to, for example the two-letter ISO 3166-1 alpha-2 country code. */
	'applicableCountry': { termType: 'NamedNode', value: 'https://schema.org/applicableCountry', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/applicableCountry' } as NamedNode,
	/** The location in which the status applies. */
	'applicableLocation': { termType: 'NamedNode', value: 'https://schema.org/applicableLocation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/applicableLocation' } as NamedNode,
	/** The location(s) applicants can apply from. This is usually used for telecommuting jobs where the applicant does not need to be in a physical office. Note: This should not be used for citizenship or work visa requirements. */
	'applicantLocationRequirements': { termType: 'NamedNode', value: 'https://schema.org/applicantLocationRequirements', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/applicantLocationRequirements' } as NamedNode,
	/** An application that can complete the request. */
	'application': { termType: 'NamedNode', value: 'https://schema.org/application', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/application' } as NamedNode,
	/** Type of software application, e.g. 'Game, Multimedia'. */
	'applicationCategory': { termType: 'NamedNode', value: 'https://schema.org/applicationCategory', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/applicationCategory' } as NamedNode,
	/** Contact details for further information relevant to this job posting. */
	'applicationContact': { termType: 'NamedNode', value: 'https://schema.org/applicationContact', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/applicationContact' } as NamedNode,
	/** The date at which the program stops collecting applications for the next enrollment cycle. */
	'applicationDeadline': { termType: 'NamedNode', value: 'https://schema.org/applicationDeadline', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/applicationDeadline' } as NamedNode,
	/** The date at which the program begins collecting applications for the next enrollment cycle. */
	'applicationStartDate': { termType: 'NamedNode', value: 'https://schema.org/applicationStartDate', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/applicationStartDate' } as NamedNode,
	/** Subcategory of the application, e.g. 'Arcade Game'. */
	'applicationSubCategory': { termType: 'NamedNode', value: 'https://schema.org/applicationSubCategory', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/applicationSubCategory' } as NamedNode,
	/** The name of the application suite to which the application belongs (e.g. Excel belongs to Office). */
	'applicationSuite': { termType: 'NamedNode', value: 'https://schema.org/applicationSuite', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/applicationSuite' } as NamedNode,
	/** The delivery method(s) to which the delivery charge or payment charge specification applies. */
	'appliesToDeliveryMethod': { termType: 'NamedNode', value: 'https://schema.org/appliesToDeliveryMethod', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/appliesToDeliveryMethod' } as NamedNode,
	/** The payment method(s) to which the payment charge specification applies. */
	'appliesToPaymentMethod': { termType: 'NamedNode', value: 'https://schema.org/appliesToPaymentMethod', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/appliesToPaymentMethod' } as NamedNode,
	/** Collection, [fonds](https://en.wikipedia.org/wiki/Fonds), or item held, kept or maintained by an [[ArchiveOrganization]]. */
	'archiveHeld': { termType: 'NamedNode', value: 'https://schema.org/archiveHeld', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/archiveHeld' } as NamedNode,
	/** Indicates a page or other link involved in archival of a [[CreativeWork]]. In the case of [[MediaReview]], the items in a [[MediaReviewItem]] may often become inaccessible, but be archived by archival, journalistic, activist, or law enforcement organizations. In such cases, the referenced page may not directly publish the content. */
	'archivedAt': { termType: 'NamedNode', value: 'https://schema.org/archivedAt', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/archivedAt' } as NamedNode,
	/** The area within which users can expect to reach the broadcast service. */
	'area': { termType: 'NamedNode', value: 'https://schema.org/area', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/area' } as NamedNode,
	/** The geographic area where a service or offered item is provided. */
	'areaServed': { termType: 'NamedNode', value: 'https://schema.org/areaServed', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/areaServed' } as NamedNode,
	/** The airport where the flight terminates. */
	'arrivalAirport': { termType: 'NamedNode', value: 'https://schema.org/arrivalAirport', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/arrivalAirport' } as NamedNode,
	/** The terminal or port from which the boat arrives. */
	'arrivalBoatTerminal': { termType: 'NamedNode', value: 'https://schema.org/arrivalBoatTerminal', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/arrivalBoatTerminal' } as NamedNode,
	/** The stop or station from which the bus arrives. */
	'arrivalBusStop': { termType: 'NamedNode', value: 'https://schema.org/arrivalBusStop', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/arrivalBusStop' } as NamedNode,
	/** Identifier of the flight's arrival gate. */
	'arrivalGate': { termType: 'NamedNode', value: 'https://schema.org/arrivalGate', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/arrivalGate' } as NamedNode,
	/** The platform where the train arrives. */
	'arrivalPlatform': { termType: 'NamedNode', value: 'https://schema.org/arrivalPlatform', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/arrivalPlatform' } as NamedNode,
	/** The station where the train trip ends. */
	'arrivalStation': { termType: 'NamedNode', value: 'https://schema.org/arrivalStation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/arrivalStation' } as NamedNode,
	/** Identifier of the flight's arrival terminal. */
	'arrivalTerminal': { termType: 'NamedNode', value: 'https://schema.org/arrivalTerminal', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/arrivalTerminal' } as NamedNode,
	/** The expected arrival time. */
	'arrivalTime': { termType: 'NamedNode', value: 'https://schema.org/arrivalTime', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/arrivalTime' } as NamedNode,
	/** The number of copies when multiple copies of a piece of artwork are produced - e.g. for a limited edition of 20 prints, 'artEdition' refers to the total number of copies (in this example "20"). */
	'artEdition': { termType: 'NamedNode', value: 'https://schema.org/artEdition', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/artEdition' } as NamedNode,
	/** The material used. (E.g. Oil, Watercolour, Acrylic, Linoprint, Marble, Cyanotype, Digital, Lithograph, DryPoint, Intaglio, Pastel, Woodcut, Pencil, Mixed Media, etc.) */
	'artMedium': { termType: 'NamedNode', value: 'https://schema.org/artMedium', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/artMedium' } as NamedNode,
	/** The branches that comprise the arterial structure. */
	'arterialBranch': { termType: 'NamedNode', value: 'https://schema.org/arterialBranch', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/arterialBranch' } as NamedNode,
	/** e.g. Painting, Drawing, Sculpture, Print, Photograph, Assemblage, Collage, etc. */
	'artform': { termType: 'NamedNode', value: 'https://schema.org/artform', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/artform' } as NamedNode,
	/** The actual body of the article. */
	'articleBody': { termType: 'NamedNode', value: 'https://schema.org/articleBody', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/articleBody' } as NamedNode,
	/** Articles may belong to one or more 'sections' in a magazine or newspaper, such as Sports, Lifestyle, etc. */
	'articleSection': { termType: 'NamedNode', value: 'https://schema.org/articleSection', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/articleSection' } as NamedNode,
	/** The primary artist for a work
    	in a medium other than pencils or digital line art--for example, if the
    	primary artwork is done in watercolors or digital paints. */
	'artist': { termType: 'NamedNode', value: 'https://schema.org/artist', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/artist' } as NamedNode,
	/** The supporting materials for the artwork, e.g. Canvas, Paper, Wood, Board, etc. */
	'artworkSurface': { termType: 'NamedNode', value: 'https://schema.org/artworkSurface', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/artworkSurface' } as NamedNode,
	/** An Amazon Standard Identification Number (ASIN) is a 10-character alphanumeric unique identifier assigned by Amazon.com and its partners for product identification within the Amazon organization (summary from [Wikipedia](https://en.wikipedia.org/wiki/Amazon_Standard_Identification_Number)'s article).

Note also that this is a definition for how to include ASINs in Schema.org data, and not a definition of ASINs in general - see documentation from Amazon for authoritative details.
ASINs are most commonly encoded as text strings, but the [asin] property supports URL/URI as potential values too. */
	'asin': { termType: 'NamedNode', value: 'https://schema.org/asin', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/asin' } as NamedNode,
	/** An aspect of medical practice that is considered on the page, such as 'diagnosis', 'treatment', 'causes', 'prognosis', 'etiology', 'epidemiology', etc. */
	'aspect': { termType: 'NamedNode', value: 'https://schema.org/aspect', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/aspect' } as NamedNode,
	/** Library file name, e.g., mscorlib.dll, system.web.dll. */
	'assembly': { termType: 'NamedNode', value: 'https://schema.org/assembly', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/assembly' } as NamedNode,
	/** Associated product/technology version. E.g., .NET Framework 4.5. */
	'assemblyVersion': { termType: 'NamedNode', value: 'https://schema.org/assemblyVersion', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/assemblyVersion' } as NamedNode,
	/** The item being described is intended to assess the competency or learning outcome defined by the referenced term. */
	'assesses': { termType: 'NamedNode', value: 'https://schema.org/assesses', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/assesses' } as NamedNode,
	/** The anatomy of the underlying organ system or structures associated with this entity. */
	'associatedAnatomy': { termType: 'NamedNode', value: 'https://schema.org/associatedAnatomy', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/associatedAnatomy' } as NamedNode,
	/** A NewsArticle associated with the Media Object. */
	'associatedArticle': { termType: 'NamedNode', value: 'https://schema.org/associatedArticle', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/associatedArticle' } as NamedNode,
	/** An associated [[ClaimReview]], related by specific common content, topic or claim. The expectation is that this property would be most typically used in cases where a single activity is conducting both claim reviews and media reviews, in which case [[relatedMediaReview]] would commonly be used on a [[ClaimReview]], while [[relatedClaimReview]] would be used on [[MediaReview]]. */
	'associatedClaimReview': { termType: 'NamedNode', value: 'https://schema.org/associatedClaimReview', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/associatedClaimReview' } as NamedNode,
	/** Disease associated to this BioChemEntity. Such disease can be a MedicalCondition or a URL. If you want to add an evidence supporting the association, please use PropertyValue. */
	'associatedDisease': { termType: 'NamedNode', value: 'https://schema.org/associatedDisease', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/associatedDisease' } as NamedNode,
	/** A media object that encodes this CreativeWork. This property is a synonym for encoding. */
	'associatedMedia': { termType: 'NamedNode', value: 'https://schema.org/associatedMedia', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/associatedMedia' } as NamedNode,
	/** An associated [[MediaReview]], related by specific common content, topic or claim. The expectation is that this property would be most typically used in cases where a single activity is conducting both claim reviews and media reviews, in which case [[relatedMediaReview]] would commonly be used on a [[ClaimReview]], while [[relatedClaimReview]] would be used on [[MediaReview]]. */
	'associatedMediaReview': { termType: 'NamedNode', value: 'https://schema.org/associatedMediaReview', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/associatedMediaReview' } as NamedNode,
	/** If applicable, a description of the pathophysiology associated with the anatomical system, including potential abnormal changes in the mechanical, physical, and biochemical functions of the system. */
	'associatedPathophysiology': { termType: 'NamedNode', value: 'https://schema.org/associatedPathophysiology', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/associatedPathophysiology' } as NamedNode,
	/** An associated [[Review]]. */
	'associatedReview': { termType: 'NamedNode', value: 'https://schema.org/associatedReview', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/associatedReview' } as NamedNode,
	/** A person that acts as performing member of a sports team; a player as opposed to a coach. */
	'athlete': { termType: 'NamedNode', value: 'https://schema.org/athlete', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/athlete' } as NamedNode,
	/** A person or organization attending the event. */
	'attendee': { termType: 'NamedNode', value: 'https://schema.org/attendee', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/attendee' } as NamedNode,
	/** A person attending the event. */
	'attendees': { termType: 'NamedNode', value: 'https://schema.org/attendees', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/attendees' } as NamedNode,
	/** An intended audience, i.e. a group for whom something was created. */
	'audience': { termType: 'NamedNode', value: 'https://schema.org/audience', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/audience' } as NamedNode,
	/** The target group associated with a given audience (e.g. veterans, car owners, musicians, etc.). */
	'audienceType': { termType: 'NamedNode', value: 'https://schema.org/audienceType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/audienceType' } as NamedNode,
	/** An embedded audio object. */
	'audio': { termType: 'NamedNode', value: 'https://schema.org/audio', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/audio' } as NamedNode,
	/** The Organization responsible for authenticating the user's subscription. For example, many media apps require a cable/satellite provider to authenticate your subscription before playing media. */
	'authenticator': { termType: 'NamedNode', value: 'https://schema.org/authenticator', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/authenticator' } as NamedNode,
	/** The author of this content or rating. Please note that author is special in that HTML 5 provides a special mechanism for indicating authorship via the rel tag. That is equivalent to this and may be used interchangeably. */
	'author': { termType: 'NamedNode', value: 'https://schema.org/author', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/author' } as NamedNode,
	/** The availability of this item&#x2014;for example In stock, Out of stock, Pre-order, etc. */
	'availability': { termType: 'NamedNode', value: 'https://schema.org/availability', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/availability' } as NamedNode,
	/** The end of the availability of the product or service included in the offer. */
	'availabilityEnds': { termType: 'NamedNode', value: 'https://schema.org/availabilityEnds', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/availabilityEnds' } as NamedNode,
	/** The beginning of the availability of the product or service included in the offer. */
	'availabilityStarts': { termType: 'NamedNode', value: 'https://schema.org/availabilityStarts', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/availabilityStarts' } as NamedNode,
	/** The place(s) from which the offer can be obtained (e.g. store locations). */
	'availableAtOrFrom': { termType: 'NamedNode', value: 'https://schema.org/availableAtOrFrom', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/availableAtOrFrom' } as NamedNode,
	/** A means of accessing the service (e.g. a phone bank, a web site, a location, etc.). */
	'availableChannel': { termType: 'NamedNode', value: 'https://schema.org/availableChannel', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/availableChannel' } as NamedNode,
	/** The delivery method(s) available for this offer. */
	'availableDeliveryMethod': { termType: 'NamedNode', value: 'https://schema.org/availableDeliveryMethod', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/availableDeliveryMethod' } as NamedNode,
	/** When the item is available for pickup from the store, locker, etc. */
	'availableFrom': { termType: 'NamedNode', value: 'https://schema.org/availableFrom', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/availableFrom' } as NamedNode,
	/** The location in which the strength is available. */
	'availableIn': { termType: 'NamedNode', value: 'https://schema.org/availableIn', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/availableIn' } as NamedNode,
	/** A language someone may use with or at the item, service or place. Please use one of the language codes from the [IETF BCP 47 standard](http://tools.ietf.org/html/bcp47). See also [[inLanguage]]. */
	'availableLanguage': { termType: 'NamedNode', value: 'https://schema.org/availableLanguage', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/availableLanguage' } as NamedNode,
	/** Device required to run the application. Used in cases where a specific make/model is required to run the application. */
	'availableOnDevice': { termType: 'NamedNode', value: 'https://schema.org/availableOnDevice', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/availableOnDevice' } as NamedNode,
	/** A medical service available from this provider. */
	'availableService': { termType: 'NamedNode', value: 'https://schema.org/availableService', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/availableService' } as NamedNode,
	/** An available dosage strength for the drug. */
	'availableStrength': { termType: 'NamedNode', value: 'https://schema.org/availableStrength', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/availableStrength' } as NamedNode,
	/** A diagnostic test or procedure offered by this lab. */
	'availableTest': { termType: 'NamedNode', value: 'https://schema.org/availableTest', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/availableTest' } as NamedNode,
	/** After this date, the item will no longer be available for pickup. */
	'availableThrough': { termType: 'NamedNode', value: 'https://schema.org/availableThrough', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/availableThrough' } as NamedNode,
	/** An award won by or for this item. */
	'award': { termType: 'NamedNode', value: 'https://schema.org/award', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/award' } as NamedNode,
	/** Awards won by or for this item. */
	'awards': { termType: 'NamedNode', value: 'https://schema.org/awards', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/awards' } as NamedNode,
	/** The away team in a sports event. */
	'awayTeam': { termType: 'NamedNode', value: 'https://schema.org/awayTeam', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/awayTeam' } as NamedNode,
	/** For an [[Article]], typically a [[NewsArticle]], the backstory property provides a textual summary giving a brief explanation of why and how an article was created. In a journalistic setting this could include information about reporting process, methods, interviews, data sources, etc. */
	'backstory': { termType: 'NamedNode', value: 'https://schema.org/backstory', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/backstory' } as NamedNode,
	/** The type of a bank account. */
	'bankAccountType': { termType: 'NamedNode', value: 'https://schema.org/bankAccountType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/bankAccountType' } as NamedNode,
	/** The base salary of the job or of an employee in an EmployeeRole. */
	'baseSalary': { termType: 'NamedNode', value: 'https://schema.org/baseSalary', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/baseSalary' } as NamedNode,
	/** A sub property of recipient. The recipient blind copied on a message. */
	'bccRecipient': { termType: 'NamedNode', value: 'https://schema.org/bccRecipient', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/bccRecipient' } as NamedNode,
	/** The type of bed or beds included in the accommodation. For the single case of just one bed of a certain type, you use bed directly with a text.
      If you want to indicate the quantity of a certain kind of bed, use an instance of BedDetails. For more detailed information, use the amenityFeature property. */
	'bed': { termType: 'NamedNode', value: 'https://schema.org/bed', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/bed' } as NamedNode,
	/** A media object representing the circumstances before performing this direction. */
	'beforeMedia': { termType: 'NamedNode', value: 'https://schema.org/beforeMedia', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/beforeMedia' } as NamedNode,
	/** A bank or bank’s branch, financial institution or international financial institution operating the beneficiary’s bank account or releasing funds for the beneficiary. */
	'beneficiaryBank': { termType: 'NamedNode', value: 'https://schema.org/beneficiaryBank', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/beneficiaryBank' } as NamedNode,
	/** Description of benefits associated with the job. */
	'benefits': { termType: 'NamedNode', value: 'https://schema.org/benefits', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/benefits' } as NamedNode,
	/** The URL that goes directly to the summary of benefits and coverage for the specific standard plan or plan variation. */
	'benefitsSummaryUrl': { termType: 'NamedNode', value: 'https://schema.org/benefitsSummaryUrl', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/benefitsSummaryUrl' } as NamedNode,
	/** The highest value allowed in this rating system. If bestRating is omitted, 5 is assumed. */
	'bestRating': { termType: 'NamedNode', value: 'https://schema.org/bestRating', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/bestRating' } as NamedNode,
	/** The billing address for the order. */
	'billingAddress': { termType: 'NamedNode', value: 'https://schema.org/billingAddress', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/billingAddress' } as NamedNode,
	/** Specifies for how long this price (or price component) will be billed. Can be used, for example, to model the contractual duration of a subscription or payment plan. Type can be either a Duration or a Number (in which case the unit of measurement, for example month, is specified by the unitCode property). */
	'billingDuration': { termType: 'NamedNode', value: 'https://schema.org/billingDuration', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/billingDuration' } as NamedNode,
	/** This property specifies the minimal quantity and rounding increment that will be the basis for the billing. The unit of measurement is specified by the unitCode property. */
	'billingIncrement': { termType: 'NamedNode', value: 'https://schema.org/billingIncrement', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/billingIncrement' } as NamedNode,
	/** The time interval used to compute the invoice. */
	'billingPeriod': { termType: 'NamedNode', value: 'https://schema.org/billingPeriod', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/billingPeriod' } as NamedNode,
	/** Specifies after how much time this price (or price component) becomes valid and billing starts. Can be used, for example, to model a price increase after the first year of a subscription. The unit of measurement is specified by the unitCode property. */
	'billingStart': { termType: 'NamedNode', value: 'https://schema.org/billingStart', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/billingStart' } as NamedNode,
	/** A BioChemEntity that is known to interact with this item. */
	'bioChemInteraction': { termType: 'NamedNode', value: 'https://schema.org/bioChemInteraction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/bioChemInteraction' } as NamedNode,
	/** A similar BioChemEntity, e.g., obtained by fingerprint similarity algorithms. */
	'bioChemSimilarity': { termType: 'NamedNode', value: 'https://schema.org/bioChemSimilarity', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/bioChemSimilarity' } as NamedNode,
	/** A role played by the BioChemEntity within a biological context. */
	'biologicalRole': { termType: 'NamedNode', value: 'https://schema.org/biologicalRole', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/biologicalRole' } as NamedNode,
	/** The biomechanical properties of the bone. */
	'biomechnicalClass': { termType: 'NamedNode', value: 'https://schema.org/biomechnicalClass', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/biomechnicalClass' } as NamedNode,
	/** Date of birth. */
	'birthDate': { termType: 'NamedNode', value: 'https://schema.org/birthDate', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/birthDate' } as NamedNode,
	/** The place where the person was born. */
	'birthPlace': { termType: 'NamedNode', value: 'https://schema.org/birthPlace', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/birthPlace' } as NamedNode,
	/** The bitrate of the media object. */
	'bitrate': { termType: 'NamedNode', value: 'https://schema.org/bitrate', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/bitrate' } as NamedNode,
	/** A posting that is part of this blog. */
	'blogPost': { termType: 'NamedNode', value: 'https://schema.org/blogPost', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/blogPost' } as NamedNode,
	/** Indicates a post that is part of a [[Blog]]. Note that historically, what we term a "Blog" was once known as a "weblog", and that what we term a "BlogPosting" is now often colloquially referred to as a "blog". */
	'blogPosts': { termType: 'NamedNode', value: 'https://schema.org/blogPosts', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/blogPosts' } as NamedNode,
	/** The blood vessel that carries blood from the heart to the muscle. */
	'bloodSupply': { termType: 'NamedNode', value: 'https://schema.org/bloodSupply', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/bloodSupply' } as NamedNode,
	/** The airline-specific indicator of boarding order / preference. */
	'boardingGroup': { termType: 'NamedNode', value: 'https://schema.org/boardingGroup', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/boardingGroup' } as NamedNode,
	/** The type of boarding policy used by the airline (e.g. zone-based or group-based). */
	'boardingPolicy': { termType: 'NamedNode', value: 'https://schema.org/boardingPolicy', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/boardingPolicy' } as NamedNode,
	/** Location in the body of the anatomical structure. */
	'bodyLocation': { termType: 'NamedNode', value: 'https://schema.org/bodyLocation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/bodyLocation' } as NamedNode,
	/** Indicates the design and body style of the vehicle (e.g. station wagon, hatchback, etc.). */
	'bodyType': { termType: 'NamedNode', value: 'https://schema.org/bodyType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/bodyType' } as NamedNode,
	/** The edition of the book. */
	'bookEdition': { termType: 'NamedNode', value: 'https://schema.org/bookEdition', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/bookEdition' } as NamedNode,
	/** The format of the book. */
	'bookFormat': { termType: 'NamedNode', value: 'https://schema.org/bookFormat', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/bookFormat' } as NamedNode,
	/** 'bookingAgent' is an out-dated term indicating a 'broker' that serves as a booking agent. */
	'bookingAgent': { termType: 'NamedNode', value: 'https://schema.org/bookingAgent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/bookingAgent' } as NamedNode,
	/** The date and time the reservation was booked. */
	'bookingTime': { termType: 'NamedNode', value: 'https://schema.org/bookingTime', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/bookingTime' } as NamedNode,
	/** A sub property of participant. The person that borrows the object being lent. */
	'borrower': { termType: 'NamedNode', value: 'https://schema.org/borrower', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/borrower' } as NamedNode,
	/** A box is the area enclosed by the rectangle formed by two points. The first point is the lower corner, the second point is the upper corner. A box is expressed as two points separated by a space character. */
	'box': { termType: 'NamedNode', value: 'https://schema.org/box', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/box' } as NamedNode,
	/** The branches that delineate from the nerve bundle. Not to be confused with [[branchOf]]. */
	'branch': { termType: 'NamedNode', value: 'https://schema.org/branch', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/branch' } as NamedNode,
	/** A short textual code (also called "store code") that uniquely identifies a place of business. The code is typically assigned by the parentOrganization and used in structured URLs.\n\nFor example, in the URL http://www.starbucks.co.uk/store-locator/etc/detail/3047 the code "3047" is a branchCode for a particular branch.
       */
	'branchCode': { termType: 'NamedNode', value: 'https://schema.org/branchCode', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/branchCode' } as NamedNode,
	/** The larger organization that this local business is a branch of, if any. Not to be confused with (anatomical) [[branch]]. */
	'branchOf': { termType: 'NamedNode', value: 'https://schema.org/branchOf', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/branchOf' } as NamedNode,
	/** The brand(s) associated with a product or service, or the brand(s) maintained by an organization or business person. */
	'brand': { termType: 'NamedNode', value: 'https://schema.org/brand', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/brand' } as NamedNode,
	/** A set of links that can help a user understand and navigate a website hierarchy. */
	'breadcrumb': { termType: 'NamedNode', value: 'https://schema.org/breadcrumb', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/breadcrumb' } as NamedNode,
	/** Any precaution, guidance, contraindication, etc. related to this drug's use by breastfeeding mothers. */
	'breastfeedingWarning': { termType: 'NamedNode', value: 'https://schema.org/breastfeedingWarning', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/breastfeedingWarning' } as NamedNode,
	/** The media network(s) whose content is broadcast on this station. */
	'broadcastAffiliateOf': { termType: 'NamedNode', value: 'https://schema.org/broadcastAffiliateOf', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/broadcastAffiliateOf' } as NamedNode,
	/** The unique address by which the BroadcastService can be identified in a provider lineup. In US, this is typically a number. */
	'broadcastChannelId': { termType: 'NamedNode', value: 'https://schema.org/broadcastChannelId', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/broadcastChannelId' } as NamedNode,
	/** The name displayed in the channel guide. For many US affiliates, it is the network name. */
	'broadcastDisplayName': { termType: 'NamedNode', value: 'https://schema.org/broadcastDisplayName', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/broadcastDisplayName' } as NamedNode,
	/** The frequency used for over-the-air broadcasts. Numeric values or simple ranges, e.g. 87-99. In addition a shortcut idiom is supported for frequences of AM and FM radio channels, e.g. "87 FM". */
	'broadcastFrequency': { termType: 'NamedNode', value: 'https://schema.org/broadcastFrequency', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/broadcastFrequency' } as NamedNode,
	/** The frequency in MHz for a particular broadcast. */
	'broadcastFrequencyValue': { termType: 'NamedNode', value: 'https://schema.org/broadcastFrequencyValue', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/broadcastFrequencyValue' } as NamedNode,
	/** The event being broadcast such as a sporting event or awards ceremony. */
	'broadcastOfEvent': { termType: 'NamedNode', value: 'https://schema.org/broadcastOfEvent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/broadcastOfEvent' } as NamedNode,
	/** The type of service required to have access to the channel (e.g. Standard or Premium). */
	'broadcastServiceTier': { termType: 'NamedNode', value: 'https://schema.org/broadcastServiceTier', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/broadcastServiceTier' } as NamedNode,
	/** The modulation (e.g. FM, AM, etc) used by a particular broadcast service. */
	'broadcastSignalModulation': { termType: 'NamedNode', value: 'https://schema.org/broadcastSignalModulation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/broadcastSignalModulation' } as NamedNode,
	/** The subchannel used for the broadcast. */
	'broadcastSubChannel': { termType: 'NamedNode', value: 'https://schema.org/broadcastSubChannel', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/broadcastSubChannel' } as NamedNode,
	/** The timezone in [ISO 8601 format](http://en.wikipedia.org/wiki/ISO_8601) for which the service bases its broadcasts. */
	'broadcastTimezone': { termType: 'NamedNode', value: 'https://schema.org/broadcastTimezone', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/broadcastTimezone' } as NamedNode,
	/** The organization owning or operating the broadcast service. */
	'broadcaster': { termType: 'NamedNode', value: 'https://schema.org/broadcaster', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/broadcaster' } as NamedNode,
	/** An entity that arranges for an exchange between a buyer and a seller.  In most cases a broker never acquires or releases ownership of a product or service involved in an exchange.  If it is not clear whether an entity is a broker, seller, or buyer, the latter two terms are preferred. */
	'broker': { termType: 'NamedNode', value: 'https://schema.org/broker', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/broker' } as NamedNode,
	/** Specifies browser requirements in human-readable text. For example, 'requires HTML5 support'. */
	'browserRequirements': { termType: 'NamedNode', value: 'https://schema.org/browserRequirements', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/browserRequirements' } as NamedNode,
	/** The name of the bus (e.g. Bolt Express). */
	'busName': { termType: 'NamedNode', value: 'https://schema.org/busName', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/busName' } as NamedNode,
	/** The unique identifier for the bus. */
	'busNumber': { termType: 'NamedNode', value: 'https://schema.org/busNumber', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/busNumber' } as NamedNode,
	/** Days of the week when the merchant typically operates, indicated via opening hours markup. */
	'businessDays': { termType: 'NamedNode', value: 'https://schema.org/businessDays', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/businessDays' } as NamedNode,
	/** The business function (e.g. sell, lease, repair, dispose) of the offer or component of a bundle (TypeAndQuantityNode). The default is http://purl.org/goodrelations/v1#Sell. */
	'businessFunction': { termType: 'NamedNode', value: 'https://schema.org/businessFunction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/businessFunction' } as NamedNode,
	/** A sub property of participant. The participant/person/organization that bought the object. */
	'buyer': { termType: 'NamedNode', value: 'https://schema.org/buyer', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/buyer' } as NamedNode,
	/** The artist that performed this album or recording. */
	'byArtist': { termType: 'NamedNode', value: 'https://schema.org/byArtist', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/byArtist' } as NamedNode,
	/** Defines the day(s) of the week on which a recurring [[Event]] takes place. May be specified using either [[DayOfWeek]], or alternatively [[Text]] conforming to iCal's syntax for byDay recurrence rules. */
	'byDay': { termType: 'NamedNode', value: 'https://schema.org/byDay', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/byDay' } as NamedNode,
	/** Defines the month(s) of the year on which a recurring [[Event]] takes place. Specified as an [[Integer]] between 1-12. January is 1. */
	'byMonth': { termType: 'NamedNode', value: 'https://schema.org/byMonth', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/byMonth' } as NamedNode,
	/** Defines the day(s) of the month on which a recurring [[Event]] takes place. Specified as an [[Integer]] between 1-31. */
	'byMonthDay': { termType: 'NamedNode', value: 'https://schema.org/byMonthDay', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/byMonthDay' } as NamedNode,
	/** Defines the week(s) of the month on which a recurring Event takes place. Specified as an Integer between 1-5. For clarity, byMonthWeek is best used in conjunction with byDay to indicate concepts like the first and third Mondays of a month. */
	'byMonthWeek': { termType: 'NamedNode', value: 'https://schema.org/byMonthWeek', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/byMonthWeek' } as NamedNode,
	/** A [callsign](https://en.wikipedia.org/wiki/Call_sign), as used in broadcasting and radio communications to identify people, radio and TV stations, or vehicles. */
	'callSign': { termType: 'NamedNode', value: 'https://schema.org/callSign', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/callSign' } as NamedNode,
	/** The number of calories. */
	'calories': { termType: 'NamedNode', value: 'https://schema.org/calories', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/calories' } as NamedNode,
	/** A sub property of object. The candidate subject of this action. */
	'candidate': { termType: 'NamedNode', value: 'https://schema.org/candidate', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/candidate' } as NamedNode,
	/** The caption for this object. For downloadable machine formats (closed caption, subtitles etc.) use MediaObject and indicate the [[encodingFormat]]. */
	'caption': { termType: 'NamedNode', value: 'https://schema.org/caption', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/caption' } as NamedNode,
	/** The number of grams of carbohydrates. */
	'carbohydrateContent': { termType: 'NamedNode', value: 'https://schema.org/carbohydrateContent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/carbohydrateContent' } as NamedNode,
	/** The available volume for cargo or luggage. For automobiles, this is usually the trunk volume.\n\nTypical unit code(s): LTR for liters, FTQ for cubic foot/feet\n\nNote: You can use [[minValue]] and [[maxValue]] to indicate ranges. */
	'cargoVolume': { termType: 'NamedNode', value: 'https://schema.org/cargoVolume', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/cargoVolume' } as NamedNode,
	/** 'carrier' is an out-dated term indicating the 'provider' for parcel delivery and flights. */
	'carrier': { termType: 'NamedNode', value: 'https://schema.org/carrier', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/carrier' } as NamedNode,
	/** Specifies specific carrier(s) requirements for the application (e.g. an application may only work on a specific carrier network). */
	'carrierRequirements': { termType: 'NamedNode', value: 'https://schema.org/carrierRequirements', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/carrierRequirements' } as NamedNode,
	/** A cardholder benefit that pays the cardholder a small percentage of their net expenditures. */
	'cashBack': { termType: 'NamedNode', value: 'https://schema.org/cashBack', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/cashBack' } as NamedNode,
	/** A data catalog which contains this dataset. */
	'catalog': { termType: 'NamedNode', value: 'https://schema.org/catalog', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/catalog' } as NamedNode,
	/** The catalog number for the release. */
	'catalogNumber': { termType: 'NamedNode', value: 'https://schema.org/catalogNumber', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/catalogNumber' } as NamedNode,
	/** A category for the item. Greater signs or slashes can be used to informally indicate a category hierarchy. */
	'category': { termType: 'NamedNode', value: 'https://schema.org/category', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/category' } as NamedNode,
	/** The condition, complication, symptom, sign, etc. caused. */
	'causeOf': { termType: 'NamedNode', value: 'https://schema.org/causeOf', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/causeOf' } as NamedNode,
	/** A sub property of recipient. The recipient copied on a message. */
	'ccRecipient': { termType: 'NamedNode', value: 'https://schema.org/ccRecipient', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ccRecipient' } as NamedNode,
	/** Fictional person connected with a creative work. */
	'character': { termType: 'NamedNode', value: 'https://schema.org/character', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/character' } as NamedNode,
	/** A piece of data that represents a particular aspect of a fictional character (skill, power, character points, advantage, disadvantage). */
	'characterAttribute': { termType: 'NamedNode', value: 'https://schema.org/characterAttribute', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/characterAttribute' } as NamedNode,
	/** The name of a character played in some acting or performing role, i.e. in a PerformanceRole. */
	'characterName': { termType: 'NamedNode', value: 'https://schema.org/characterName', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/characterName' } as NamedNode,
	/** Cheat codes to the game. */
	'cheatCode': { termType: 'NamedNode', value: 'https://schema.org/cheatCode', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/cheatCode' } as NamedNode,
	/** The earliest someone may check into a lodging establishment. */
	'checkinTime': { termType: 'NamedNode', value: 'https://schema.org/checkinTime', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/checkinTime' } as NamedNode,
	/** A URL template (RFC 6570) for a checkout page for an offer. This approach allows merchants to specify a URL for online checkout of the offered product, by interpolating parameters such as the logged in user ID, product ID, quantity, discount code etc. Parameter naming and standardization are not specified here. */
	'checkoutPageURLTemplate': { termType: 'NamedNode', value: 'https://schema.org/checkoutPageURLTemplate', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/checkoutPageURLTemplate' } as NamedNode,
	/** The latest someone may check out of a lodging establishment. */
	'checkoutTime': { termType: 'NamedNode', value: 'https://schema.org/checkoutTime', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/checkoutTime' } as NamedNode,
	/** The chemical composition describes the identity and relative ratio of the chemical elements that make up the substance. */
	'chemicalComposition': { termType: 'NamedNode', value: 'https://schema.org/chemicalComposition', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/chemicalComposition' } as NamedNode,
	/** A role played by the BioChemEntity within a chemical context. */
	'chemicalRole': { termType: 'NamedNode', value: 'https://schema.org/chemicalRole', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/chemicalRole' } as NamedNode,
	/** Maximal age of the child. */
	'childMaxAge': { termType: 'NamedNode', value: 'https://schema.org/childMaxAge', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/childMaxAge' } as NamedNode,
	/** Minimal age of the child. */
	'childMinAge': { termType: 'NamedNode', value: 'https://schema.org/childMinAge', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/childMinAge' } as NamedNode,
	/** Closest child taxa of the taxon in question. */
	'childTaxon': { termType: 'NamedNode', value: 'https://schema.org/childTaxon', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/childTaxon' } as NamedNode,
	/** A child of the person. */
	'children': { termType: 'NamedNode', value: 'https://schema.org/children', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/children' } as NamedNode,
	/** The number of milligrams of cholesterol. */
	'cholesterolContent': { termType: 'NamedNode', value: 'https://schema.org/cholesterolContent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/cholesterolContent' } as NamedNode,
	/** A circle is the circular region of a specified radius centered at a specified latitude and longitude. A circle is expressed as a pair followed by a radius in meters. */
	'circle': { termType: 'NamedNode', value: 'https://schema.org/circle', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/circle' } as NamedNode,
	/** A citation or reference to another creative work, such as another publication, web page, scholarly article, etc. */
	'citation': { termType: 'NamedNode', value: 'https://schema.org/citation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/citation' } as NamedNode,
	/** For a [[Claim]] interpreted from [[MediaObject]] content
    sed to indicate a claim contained, implied or refined from the content of a [[MediaObject]]. */
	'claimInterpreter': { termType: 'NamedNode', value: 'https://schema.org/claimInterpreter', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/claimInterpreter' } as NamedNode,
	/** A short summary of the specific claims reviewed in a ClaimReview. */
	'claimReviewed': { termType: 'NamedNode', value: 'https://schema.org/claimReviewed', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/claimReviewed' } as NamedNode,
	/** Description of the absorption and elimination of drugs, including their concentration (pharmacokinetics, pK) and biological effects (pharmacodynamics, pD). */
	'clincalPharmacology': { termType: 'NamedNode', value: 'https://schema.org/clincalPharmacology', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/clincalPharmacology' } as NamedNode,
	/** Description of the absorption and elimination of drugs, including their concentration (pharmacokinetics, pK) and biological effects (pharmacodynamics, pD). */
	'clinicalPharmacology': { termType: 'NamedNode', value: 'https://schema.org/clinicalPharmacology', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/clinicalPharmacology' } as NamedNode,
	/** Position of the clip within an ordered group of clips. */
	'clipNumber': { termType: 'NamedNode', value: 'https://schema.org/clipNumber', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/clipNumber' } as NamedNode,
	/** The closing hour of the place or service on the given day(s) of the week. */
	'closes': { termType: 'NamedNode', value: 'https://schema.org/closes', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/closes' } as NamedNode,
	/** A person that acts in a coaching role for a sports team. */
	'coach': { termType: 'NamedNode', value: 'https://schema.org/coach', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/coach' } as NamedNode,
	/** A medical code for the entity, taken from a controlled vocabulary or ontology such as ICD-9, DiseasesDB, MeSH, SNOMED-CT, RxNorm, etc. */
	'code': { termType: 'NamedNode', value: 'https://schema.org/code', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/code' } as NamedNode,
	/** Link to the repository where the un-compiled, human readable code and related code is located (SVN, GitHub, CodePlex). */
	'codeRepository': { termType: 'NamedNode', value: 'https://schema.org/codeRepository', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/codeRepository' } as NamedNode,
	/** What type of code sample: full (compile ready) solution, code snippet, inline code, scripts, template. */
	'codeSampleType': { termType: 'NamedNode', value: 'https://schema.org/codeSampleType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/codeSampleType' } as NamedNode,
	/** A short textual code that uniquely identifies the value. */
	'codeValue': { termType: 'NamedNode', value: 'https://schema.org/codeValue', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/codeValue' } as NamedNode,
	/** The coding system, e.g. 'ICD-10'. */
	'codingSystem': { termType: 'NamedNode', value: 'https://schema.org/codingSystem', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/codingSystem' } as NamedNode,
	/** A colleague of the person. */
	'colleague': { termType: 'NamedNode', value: 'https://schema.org/colleague', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/colleague' } as NamedNode,
	/** A colleague of the person. */
	'colleagues': { termType: 'NamedNode', value: 'https://schema.org/colleagues', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/colleagues' } as NamedNode,
	/** A sub property of object. The collection target of the action. */
	'collection': { termType: 'NamedNode', value: 'https://schema.org/collection', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/collection' } as NamedNode,
	/** The number of items in the [[Collection]]. */
	'collectionSize': { termType: 'NamedNode', value: 'https://schema.org/collectionSize', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/collectionSize' } as NamedNode,
	/** The color of the product. */
	'color': { termType: 'NamedNode', value: 'https://schema.org/color', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/color' } as NamedNode,
	/** The individual who adds color to inked drawings. */
	'colorist': { termType: 'NamedNode', value: 'https://schema.org/colorist', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/colorist' } as NamedNode,
	/** Comments, typically from users. */
	'comment': { termType: 'NamedNode', value: 'https://schema.org/comment', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/comment' } as NamedNode,
	/** The number of comments this CreativeWork (e.g. Article, Question or Answer) has received. This is most applicable to works published in Web sites with commenting system; additional comments may exist elsewhere. */
	'commentCount': { termType: 'NamedNode', value: 'https://schema.org/commentCount', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/commentCount' } as NamedNode,
	/** The text of the UserComment. */
	'commentText': { termType: 'NamedNode', value: 'https://schema.org/commentText', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/commentText' } as NamedNode,
	/** The time at which the UserComment was made. */
	'commentTime': { termType: 'NamedNode', value: 'https://schema.org/commentTime', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/commentTime' } as NamedNode,
	/** Knowledge, skill, ability or personal attribute that must be demonstrated by a person or other entity in order to do something such as earn an Educational Occupational Credential or understand a LearningResource. */
	'competencyRequired': { termType: 'NamedNode', value: 'https://schema.org/competencyRequired', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/competencyRequired' } as NamedNode,
	/** A competitor in a sports event. */
	'competitor': { termType: 'NamedNode', value: 'https://schema.org/competitor', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/competitor' } as NamedNode,
	/** The person or organization who wrote a composition, or who is the composer of a work performed at some event. */
	'composer': { termType: 'NamedNode', value: 'https://schema.org/composer', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/composer' } as NamedNode,
	/** Specifying something physically contained by something else. Typically used here for the underlying anatomical structures, such as organs, that comprise the anatomical system. */
	'comprisedOf': { termType: 'NamedNode', value: 'https://schema.org/comprisedOf', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/comprisedOf' } as NamedNode,
	/** Conditions that affect the availability of, or method(s) of access to, an item. Typically used for real world items such as an [[ArchiveComponent]] held by an [[ArchiveOrganization]]. This property is not suitable for use as a general Web access control mechanism. It is expressed only in natural language.\n\nFor example "Available by appointment from the Reading Room" or "Accessible only from logged-in accounts ".  */
	'conditionsOfAccess': { termType: 'NamedNode', value: 'https://schema.org/conditionsOfAccess', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/conditionsOfAccess' } as NamedNode,
	/** A number that confirms the given order or payment has been received. */
	'confirmationNumber': { termType: 'NamedNode', value: 'https://schema.org/confirmationNumber', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/confirmationNumber' } as NamedNode,
	/** Other anatomical structures to which this structure is connected. */
	'connectedTo': { termType: 'NamedNode', value: 'https://schema.org/connectedTo', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/connectedTo' } as NamedNode,
	/** Indicates a property used as a constraint. For example, in the definition of a [[StatisticalVariable]]. The value is a property, either from within Schema.org or from other compatible (e.g. RDF) systems such as DataCommons.org or Wikidata.org.  */
	'constraintProperty': { termType: 'NamedNode', value: 'https://schema.org/constraintProperty', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/constraintProperty' } as NamedNode,
	/** An option available on this contact point (e.g. a toll-free number or support for hearing-impaired callers). */
	'contactOption': { termType: 'NamedNode', value: 'https://schema.org/contactOption', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/contactOption' } as NamedNode,
	/** A contact point for a person or organization. */
	'contactPoint': { termType: 'NamedNode', value: 'https://schema.org/contactPoint', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/contactPoint' } as NamedNode,
	/** A contact point for a person or organization. */
	'contactPoints': { termType: 'NamedNode', value: 'https://schema.org/contactPoints', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/contactPoints' } as NamedNode,
	/** A person or organization can have different contact points, for different purposes. For example, a sales contact point, a PR contact point and so on. This property is used to specify the kind of contact point. */
	'contactType': { termType: 'NamedNode', value: 'https://schema.org/contactType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/contactType' } as NamedNode,
	/** A secure method for consumers to purchase products or services via debit, credit or smartcards by using RFID or NFC technology. */
	'contactlessPayment': { termType: 'NamedNode', value: 'https://schema.org/contactlessPayment', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/contactlessPayment' } as NamedNode,
	/** The basic containment relation between a place and one that contains it. */
	'containedIn': { termType: 'NamedNode', value: 'https://schema.org/containedIn', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/containedIn' } as NamedNode,
	/** The basic containment relation between a place and one that contains it. */
	'containedInPlace': { termType: 'NamedNode', value: 'https://schema.org/containedInPlace', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/containedInPlace' } as NamedNode,
	/** The basic containment relation between a place and another that it contains. */
	'containsPlace': { termType: 'NamedNode', value: 'https://schema.org/containsPlace', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/containsPlace' } as NamedNode,
	/** A season that is part of the media series. */
	'containsSeason': { termType: 'NamedNode', value: 'https://schema.org/containsSeason', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/containsSeason' } as NamedNode,
	/** The location depicted or described in the content. For example, the location in a photograph or painting. */
	'contentLocation': { termType: 'NamedNode', value: 'https://schema.org/contentLocation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/contentLocation' } as NamedNode,
	/** Official rating of a piece of content&#x2014;for example, 'MPAA PG-13'. */
	'contentRating': { termType: 'NamedNode', value: 'https://schema.org/contentRating', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/contentRating' } as NamedNode,
	/** The specific time described by a creative work, for works (e.g. articles, video objects etc.) that emphasise a particular moment within an Event. */
	'contentReferenceTime': { termType: 'NamedNode', value: 'https://schema.org/contentReferenceTime', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/contentReferenceTime' } as NamedNode,
	/** File size in (mega/kilo)bytes. */
	'contentSize': { termType: 'NamedNode', value: 'https://schema.org/contentSize', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/contentSize' } as NamedNode,
	/** The supported content type(s) for an EntryPoint response. */
	'contentType': { termType: 'NamedNode', value: 'https://schema.org/contentType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/contentType' } as NamedNode,
	/** Actual bytes of the media object, for example the image file or video file. */
	'contentUrl': { termType: 'NamedNode', value: 'https://schema.org/contentUrl', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/contentUrl' } as NamedNode,
	/** A contraindication for this therapy. */
	'contraindication': { termType: 'NamedNode', value: 'https://schema.org/contraindication', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/contraindication' } as NamedNode,
	/** A secondary contributor to the CreativeWork or Event. */
	'contributor': { termType: 'NamedNode', value: 'https://schema.org/contributor', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/contributor' } as NamedNode,
	/** The time it takes to actually cook the dish, in [ISO 8601 duration format](http://en.wikipedia.org/wiki/ISO_8601). */
	'cookTime': { termType: 'NamedNode', value: 'https://schema.org/cookTime', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/cookTime' } as NamedNode,
	/** The method of cooking, such as Frying, Steaming, ... */
	'cookingMethod': { termType: 'NamedNode', value: 'https://schema.org/cookingMethod', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/cookingMethod' } as NamedNode,
	/** The party holding the legal copyright to the CreativeWork. */
	'copyrightHolder': { termType: 'NamedNode', value: 'https://schema.org/copyrightHolder', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/copyrightHolder' } as NamedNode,
	/** Text of a notice appropriate for describing the copyright aspects of this Creative Work, ideally indicating the owner of the copyright for the Work. */
	'copyrightNotice': { termType: 'NamedNode', value: 'https://schema.org/copyrightNotice', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/copyrightNotice' } as NamedNode,
	/** The year during which the claimed copyright for the CreativeWork was first asserted. */
	'copyrightYear': { termType: 'NamedNode', value: 'https://schema.org/copyrightYear', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/copyrightYear' } as NamedNode,
	/** Indicates a correction to a [[CreativeWork]], either via a [[CorrectionComment]], textually or in another document. */
	'correction': { termType: 'NamedNode', value: 'https://schema.org/correction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/correction' } as NamedNode,
	/** For an [[Organization]] (e.g. [[NewsMediaOrganization]]), a statement describing (in news media, the newsroom’s) disclosure and correction policy for errors. */
	'correctionsPolicy': { termType: 'NamedNode', value: 'https://schema.org/correctionsPolicy', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/correctionsPolicy' } as NamedNode,
	/** The category of cost, such as wholesale, retail, reimbursement cap, etc. */
	'costCategory': { termType: 'NamedNode', value: 'https://schema.org/costCategory', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/costCategory' } as NamedNode,
	/** The currency (in 3-letter) of the drug cost. See: http://en.wikipedia.org/wiki/ISO_4217.  */
	'costCurrency': { termType: 'NamedNode', value: 'https://schema.org/costCurrency', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/costCurrency' } as NamedNode,
	/** Additional details to capture the origin of the cost data. For example, 'Medicare Part B'. */
	'costOrigin': { termType: 'NamedNode', value: 'https://schema.org/costOrigin', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/costOrigin' } as NamedNode,
	/** The cost per unit of the drug. */
	'costPerUnit': { termType: 'NamedNode', value: 'https://schema.org/costPerUnit', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/costPerUnit' } as NamedNode,
	/** Countries for which the application is not supported. You can also provide the two-letter ISO 3166-1 alpha-2 country code. */
	'countriesNotSupported': { termType: 'NamedNode', value: 'https://schema.org/countriesNotSupported', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/countriesNotSupported' } as NamedNode,
	/** Countries for which the application is supported. You can also provide the two-letter ISO 3166-1 alpha-2 country code. */
	'countriesSupported': { termType: 'NamedNode', value: 'https://schema.org/countriesSupported', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/countriesSupported' } as NamedNode,
	/** The place where the product was assembled. */
	'countryOfAssembly': { termType: 'NamedNode', value: 'https://schema.org/countryOfAssembly', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/countryOfAssembly' } as NamedNode,
	/** The place where the item (typically [[Product]]) was last processed and tested before importation. */
	'countryOfLastProcessing': { termType: 'NamedNode', value: 'https://schema.org/countryOfLastProcessing', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/countryOfLastProcessing' } as NamedNode,
	/** The country of origin of something, including products as well as creative  works such as movie and TV content.

In the case of TV and movie, this would be the country of the principle offices of the production company or individual responsible for the movie. For other kinds of [[CreativeWork]] it is difficult to provide fully general guidance, and properties such as [[contentLocation]] and [[locationCreated]] may be more applicable.

In the case of products, the country of origin of the product. The exact interpretation of this may vary by context and product type, and cannot be fully enumerated here. */
	'countryOfOrigin': { termType: 'NamedNode', value: 'https://schema.org/countryOfOrigin', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/countryOfOrigin' } as NamedNode,
	/** A sub property of location. The course where this action was taken. */
	'course': { termType: 'NamedNode', value: 'https://schema.org/course', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/course' } as NamedNode,
	/** The identifier for the [[Course]] used by the course [[provider]] (e.g. CS101 or 6.001). */
	'courseCode': { termType: 'NamedNode', value: 'https://schema.org/courseCode', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/courseCode' } as NamedNode,
	/** The medium or means of delivery of the course instance or the mode of study, either as a text label (e.g. "online", "onsite" or "blended"; "synchronous" or "asynchronous"; "full-time" or "part-time") or as a URL reference to a term from a controlled vocabulary (e.g. https://ceds.ed.gov/element/001311#Asynchronous). */
	'courseMode': { termType: 'NamedNode', value: 'https://schema.org/courseMode', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/courseMode' } as NamedNode,
	/** Requirements for taking the Course. May be completion of another [[Course]] or a textual description like "permission of instructor". Requirements may be a pre-requisite competency, referenced using [[AlignmentObject]]. */
	'coursePrerequisites': { termType: 'NamedNode', value: 'https://schema.org/coursePrerequisites', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/coursePrerequisites' } as NamedNode,
	/** Represents the length and pace of a course, expressed as a [[Schedule]]. */
	'courseSchedule': { termType: 'NamedNode', value: 'https://schema.org/courseSchedule', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/courseSchedule' } as NamedNode,
	/** The amount of work expected of students taking the course, often provided as a figure per week or per month, and may be broken down by type. For example, "2 hours of lectures, 1 hour of lab work and 3 hours of independent study per week". */
	'courseWorkload': { termType: 'NamedNode', value: 'https://schema.org/courseWorkload', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/courseWorkload' } as NamedNode,
	/** The time when the live blog will stop covering the Event. Note that coverage may continue after the Event concludes. */
	'coverageEndTime': { termType: 'NamedNode', value: 'https://schema.org/coverageEndTime', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/coverageEndTime' } as NamedNode,
	/** The time when the live blog will begin covering the Event. Note that coverage may begin before the Event's start time. The LiveBlogPosting may also be created before coverage begins. */
	'coverageStartTime': { termType: 'NamedNode', value: 'https://schema.org/coverageStartTime', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/coverageStartTime' } as NamedNode,
	/** The status of a creative work in terms of its stage in a lifecycle. Example terms include Incomplete, Draft, Published, Obsolete. Some organizations define a set of terms for the stages of their publication lifecycle. */
	'creativeWorkStatus': { termType: 'NamedNode', value: 'https://schema.org/creativeWorkStatus', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/creativeWorkStatus' } as NamedNode,
	/** The creator/author of this CreativeWork. This is the same as the Author property for CreativeWork. */
	'creator': { termType: 'NamedNode', value: 'https://schema.org/creator', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/creator' } as NamedNode,
	/** The category or type of credential being described, for example "degree”, “certificate”, “badge”, or more specific term. */
	'credentialCategory': { termType: 'NamedNode', value: 'https://schema.org/credentialCategory', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/credentialCategory' } as NamedNode,
	/** Text that can be used to credit person(s) and/or organization(s) associated with a published Creative Work. */
	'creditText': { termType: 'NamedNode', value: 'https://schema.org/creditText', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/creditText' } as NamedNode,
	/** The group the release is credited to if different than the byArtist. For example, Red and Blue is credited to "Stefani Germanotta Band", but by Lady Gaga. */
	'creditedTo': { termType: 'NamedNode', value: 'https://schema.org/creditedTo', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/creditedTo' } as NamedNode,
	/** A CSS selector, e.g. of a [[SpeakableSpecification]] or [[WebPageElement]]. In the latter case, multiple matches within a page can constitute a single conceptual "Web page element". */
	'cssSelector': { termType: 'NamedNode', value: 'https://schema.org/cssSelector', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/cssSelector' } as NamedNode,
	/** The currency accepted.\n\nUse standard formats: [ISO 4217 currency format](http://en.wikipedia.org/wiki/ISO_4217), e.g. "USD"; [Ticker symbol](https://en.wikipedia.org/wiki/List_of_cryptocurrencies) for cryptocurrencies, e.g. "BTC"; well known names for [Local Exchange Trading Systems](https://en.wikipedia.org/wiki/Local_exchange_trading_system) (LETS) and other currency types, e.g. "Ithaca HOUR". */
	'currenciesAccepted': { termType: 'NamedNode', value: 'https://schema.org/currenciesAccepted', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/currenciesAccepted' } as NamedNode,
	/** The currency in which the monetary amount is expressed.\n\nUse standard formats: [ISO 4217 currency format](http://en.wikipedia.org/wiki/ISO_4217), e.g. "USD"; [Ticker symbol](https://en.wikipedia.org/wiki/List_of_cryptocurrencies) for cryptocurrencies, e.g. "BTC"; well known names for [Local Exchange Trading Systems](https://en.wikipedia.org/wiki/Local_exchange_trading_system) (LETS) and other currency types, e.g. "Ithaca HOUR". */
	'currency': { termType: 'NamedNode', value: 'https://schema.org/currency', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/currency' } as NamedNode,
	/** The current price of a currency. */
	'currentExchangeRate': { termType: 'NamedNode', value: 'https://schema.org/currentExchangeRate', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/currentExchangeRate' } as NamedNode,
	/** Party placing the order or paying the invoice. */
	'customer': { termType: 'NamedNode', value: 'https://schema.org/customer', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/customer' } as NamedNode,
	/** The type of return fees if the product is returned due to customer remorse. */
	'customerRemorseReturnFees': { termType: 'NamedNode', value: 'https://schema.org/customerRemorseReturnFees', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/customerRemorseReturnFees' } as NamedNode,
	/** The method (from an enumeration) by which the customer obtains a return shipping label for a product returned due to customer remorse. */
	'customerRemorseReturnLabelSource': { termType: 'NamedNode', value: 'https://schema.org/customerRemorseReturnLabelSource', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/customerRemorseReturnLabelSource' } as NamedNode,
	/** The amount of shipping costs if a product is returned due to customer remorse. Applicable when property [[customerRemorseReturnFees]] equals [[ReturnShippingFees]]. */
	'customerRemorseReturnShippingFeesAmount': { termType: 'NamedNode', value: 'https://schema.org/customerRemorseReturnShippingFeesAmount', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/customerRemorseReturnShippingFeesAmount' } as NamedNode,
	/** Order cutoff time allows merchants to describe the time after which they will no longer process orders received on that day. For orders processed after cutoff time, one day gets added to the delivery time estimate. This property is expected to be most typically used via the [[ShippingRateSettings]] publication pattern. The time is indicated using the ISO-8601 Time format, e.g. "23:30:00-05:00" would represent 6:30 pm Eastern Standard Time (EST) which is 5 hours behind Coordinated Universal Time (UTC). */
	'cutoffTime': { termType: 'NamedNode', value: 'https://schema.org/cutoffTime', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/cutoffTime' } as NamedNode,
	/** collectiondate - Date for which patient counts are reported. */
	'cvdCollectionDate': { termType: 'NamedNode', value: 'https://schema.org/cvdCollectionDate', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/cvdCollectionDate' } as NamedNode,
	/** Name of the County of the NHSN facility that this data record applies to. Use [[cvdFacilityId]] to identify the facility. To provide other details, [[healthcareReportingData]] can be used on a [[Hospital]] entry. */
	'cvdFacilityCounty': { termType: 'NamedNode', value: 'https://schema.org/cvdFacilityCounty', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/cvdFacilityCounty' } as NamedNode,
	/** Identifier of the NHSN facility that this data record applies to. Use [[cvdFacilityCounty]] to indicate the county. To provide other details, [[healthcareReportingData]] can be used on a [[Hospital]] entry. */
	'cvdFacilityId': { termType: 'NamedNode', value: 'https://schema.org/cvdFacilityId', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/cvdFacilityId' } as NamedNode,
	/** numbeds - HOSPITAL INPATIENT BEDS: Inpatient beds, including all staffed, licensed, and overflow (surge) beds used for inpatients. */
	'cvdNumBeds': { termType: 'NamedNode', value: 'https://schema.org/cvdNumBeds', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/cvdNumBeds' } as NamedNode,
	/** numbedsocc - HOSPITAL INPATIENT BED OCCUPANCY: Total number of staffed inpatient beds that are occupied. */
	'cvdNumBedsOcc': { termType: 'NamedNode', value: 'https://schema.org/cvdNumBedsOcc', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/cvdNumBedsOcc' } as NamedNode,
	/** numc19died - DEATHS: Patients with suspected or confirmed COVID-19 who died in the hospital, ED, or any overflow location. */
	'cvdNumC19Died': { termType: 'NamedNode', value: 'https://schema.org/cvdNumC19Died', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/cvdNumC19Died' } as NamedNode,
	/** numc19hopats - HOSPITAL ONSET: Patients hospitalized in an NHSN inpatient care location with onset of suspected or confirmed COVID-19 14 or more days after hospitalization. */
	'cvdNumC19HOPats': { termType: 'NamedNode', value: 'https://schema.org/cvdNumC19HOPats', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/cvdNumC19HOPats' } as NamedNode,
	/** numc19hosppats - HOSPITALIZED: Patients currently hospitalized in an inpatient care location who have suspected or confirmed COVID-19. */
	'cvdNumC19HospPats': { termType: 'NamedNode', value: 'https://schema.org/cvdNumC19HospPats', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/cvdNumC19HospPats' } as NamedNode,
	/** numc19mechventpats - HOSPITALIZED and VENTILATED: Patients hospitalized in an NHSN inpatient care location who have suspected or confirmed COVID-19 and are on a mechanical ventilator. */
	'cvdNumC19MechVentPats': { termType: 'NamedNode', value: 'https://schema.org/cvdNumC19MechVentPats', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/cvdNumC19MechVentPats' } as NamedNode,
	/** numc19ofmechventpats - ED/OVERFLOW and VENTILATED: Patients with suspected or confirmed COVID-19 who are in the ED or any overflow location awaiting an inpatient bed and on a mechanical ventilator. */
	'cvdNumC19OFMechVentPats': { termType: 'NamedNode', value: 'https://schema.org/cvdNumC19OFMechVentPats', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/cvdNumC19OFMechVentPats' } as NamedNode,
	/** numc19overflowpats - ED/OVERFLOW: Patients with suspected or confirmed COVID-19 who are in the ED or any overflow location awaiting an inpatient bed. */
	'cvdNumC19OverflowPats': { termType: 'NamedNode', value: 'https://schema.org/cvdNumC19OverflowPats', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/cvdNumC19OverflowPats' } as NamedNode,
	/** numicubeds - ICU BEDS: Total number of staffed inpatient intensive care unit (ICU) beds. */
	'cvdNumICUBeds': { termType: 'NamedNode', value: 'https://schema.org/cvdNumICUBeds', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/cvdNumICUBeds' } as NamedNode,
	/** numicubedsocc - ICU BED OCCUPANCY: Total number of staffed inpatient ICU beds that are occupied. */
	'cvdNumICUBedsOcc': { termType: 'NamedNode', value: 'https://schema.org/cvdNumICUBedsOcc', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/cvdNumICUBedsOcc' } as NamedNode,
	/** numtotbeds - ALL HOSPITAL BEDS: Total number of all inpatient and outpatient beds, including all staffed, ICU, licensed, and overflow (surge) beds used for inpatients or outpatients. */
	'cvdNumTotBeds': { termType: 'NamedNode', value: 'https://schema.org/cvdNumTotBeds', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/cvdNumTotBeds' } as NamedNode,
	/** numvent - MECHANICAL VENTILATORS: Total number of ventilators available. */
	'cvdNumVent': { termType: 'NamedNode', value: 'https://schema.org/cvdNumVent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/cvdNumVent' } as NamedNode,
	/** numventuse - MECHANICAL VENTILATORS IN USE: Total number of ventilators in use. */
	'cvdNumVentUse': { termType: 'NamedNode', value: 'https://schema.org/cvdNumVentUse', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/cvdNumVentUse' } as NamedNode,
	/** An item within a data feed. Data feeds may have many elements. */
	'dataFeedElement': { termType: 'NamedNode', value: 'https://schema.org/dataFeedElement', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/dataFeedElement' } as NamedNode,
	/** A dataset contained in this catalog. */
	'dataset': { termType: 'NamedNode', value: 'https://schema.org/dataset', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/dataset' } as NamedNode,
	/** The range of temporal applicability of a dataset, e.g. for a 2011 census dataset, the year 2011 (in ISO 8601 time interval format). */
	'datasetTimeInterval': { termType: 'NamedNode', value: 'https://schema.org/datasetTimeInterval', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/datasetTimeInterval' } as NamedNode,
	/** The date on which the CreativeWork was created or the item was added to a DataFeed. */
	'dateCreated': { termType: 'NamedNode', value: 'https://schema.org/dateCreated', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/dateCreated' } as NamedNode,
	/** The datetime the item was removed from the DataFeed. */
	'dateDeleted': { termType: 'NamedNode', value: 'https://schema.org/dateDeleted', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/dateDeleted' } as NamedNode,
	/** The date the ticket was issued. */
	'dateIssued': { termType: 'NamedNode', value: 'https://schema.org/dateIssued', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/dateIssued' } as NamedNode,
	/** The date on which the CreativeWork was most recently modified or when the item's entry was modified within a DataFeed. */
	'dateModified': { termType: 'NamedNode', value: 'https://schema.org/dateModified', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/dateModified' } as NamedNode,
	/** Publication date of an online listing. */
	'datePosted': { termType: 'NamedNode', value: 'https://schema.org/datePosted', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/datePosted' } as NamedNode,
	/** Date of first broadcast/publication. */
	'datePublished': { termType: 'NamedNode', value: 'https://schema.org/datePublished', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/datePublished' } as NamedNode,
	/** The date/time at which the message has been read by the recipient if a single recipient exists. */
	'dateRead': { termType: 'NamedNode', value: 'https://schema.org/dateRead', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/dateRead' } as NamedNode,
	/** The date/time the message was received if a single recipient exists. */
	'dateReceived': { termType: 'NamedNode', value: 'https://schema.org/dateReceived', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/dateReceived' } as NamedNode,
	/** The date/time at which the message was sent. */
	'dateSent': { termType: 'NamedNode', value: 'https://schema.org/dateSent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/dateSent' } as NamedNode,
	/** The date of the first registration of the vehicle with the respective public authorities. */
	'dateVehicleFirstRegistered': { termType: 'NamedNode', value: 'https://schema.org/dateVehicleFirstRegistered', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/dateVehicleFirstRegistered' } as NamedNode,
	/** A [dateline](https://en.wikipedia.org/wiki/Dateline) is a brief piece of text included in news articles that describes where and when the story was written or filed though the date is often omitted. Sometimes only a placename is provided.

Structured representations of dateline-related information can also be expressed more explicitly using [[locationCreated]] (which represents where a work was created, e.g. where a news report was written).  For location depicted or described in the content, use [[contentLocation]].

Dateline summaries are oriented more towards human readers than towards automated processing, and can vary substantially. Some examples: "BEIRUT, Lebanon, June 2.", "Paris, France", "December 19, 2017 11:43AM Reporting from Washington", "Beijing/Moscow", "QUEZON CITY, Philippines".
       */
	'dateline': { termType: 'NamedNode', value: 'https://schema.org/dateline', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/dateline' } as NamedNode,
	/** The day of the week for which these opening hours are valid. */
	'dayOfWeek': { termType: 'NamedNode', value: 'https://schema.org/dayOfWeek', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/dayOfWeek' } as NamedNode,
	/** Date of death. */
	'deathDate': { termType: 'NamedNode', value: 'https://schema.org/deathDate', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/deathDate' } as NamedNode,
	/** The place where the person died. */
	'deathPlace': { termType: 'NamedNode', value: 'https://schema.org/deathPlace', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/deathPlace' } as NamedNode,
	/** The default value of the input.  For properties that expect a literal, the default is a literal value, for properties that expect an object, it's an ID reference to one of the current values. */
	'defaultValue': { termType: 'NamedNode', value: 'https://schema.org/defaultValue', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/defaultValue' } as NamedNode,
	/** Destination address. */
	'deliveryAddress': { termType: 'NamedNode', value: 'https://schema.org/deliveryAddress', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/deliveryAddress' } as NamedNode,
	/** The typical delay between the receipt of the order and the goods either leaving the warehouse or being prepared for pickup, in case the delivery method is on site pickup. */
	'deliveryLeadTime': { termType: 'NamedNode', value: 'https://schema.org/deliveryLeadTime', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/deliveryLeadTime' } as NamedNode,
	/** A sub property of instrument. The method of delivery. */
	'deliveryMethod': { termType: 'NamedNode', value: 'https://schema.org/deliveryMethod', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/deliveryMethod' } as NamedNode,
	/** New entry added as the package passes through each leg of its journey (from shipment to final delivery). */
	'deliveryStatus': { termType: 'NamedNode', value: 'https://schema.org/deliveryStatus', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/deliveryStatus' } as NamedNode,
	/** The total delay between the receipt of the order and the goods reaching the final customer. */
	'deliveryTime': { termType: 'NamedNode', value: 'https://schema.org/deliveryTime', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/deliveryTime' } as NamedNode,
	/** A relationship between an organization and a department of that organization, also described as an organization (allowing different urls, logos, opening hours). For example: a store with a pharmacy, or a bakery with a cafe. */
	'department': { termType: 'NamedNode', value: 'https://schema.org/department', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/department' } as NamedNode,
	/** The airport where the flight originates. */
	'departureAirport': { termType: 'NamedNode', value: 'https://schema.org/departureAirport', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/departureAirport' } as NamedNode,
	/** The terminal or port from which the boat departs. */
	'departureBoatTerminal': { termType: 'NamedNode', value: 'https://schema.org/departureBoatTerminal', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/departureBoatTerminal' } as NamedNode,
	/** The stop or station from which the bus departs. */
	'departureBusStop': { termType: 'NamedNode', value: 'https://schema.org/departureBusStop', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/departureBusStop' } as NamedNode,
	/** Identifier of the flight's departure gate. */
	'departureGate': { termType: 'NamedNode', value: 'https://schema.org/departureGate', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/departureGate' } as NamedNode,
	/** The platform from which the train departs. */
	'departurePlatform': { termType: 'NamedNode', value: 'https://schema.org/departurePlatform', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/departurePlatform' } as NamedNode,
	/** The station from which the train departs. */
	'departureStation': { termType: 'NamedNode', value: 'https://schema.org/departureStation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/departureStation' } as NamedNode,
	/** Identifier of the flight's departure terminal. */
	'departureTerminal': { termType: 'NamedNode', value: 'https://schema.org/departureTerminal', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/departureTerminal' } as NamedNode,
	/** The expected departure time. */
	'departureTime': { termType: 'NamedNode', value: 'https://schema.org/departureTime', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/departureTime' } as NamedNode,
	/** Prerequisites needed to fulfill steps in article. */
	'dependencies': { termType: 'NamedNode', value: 'https://schema.org/dependencies', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/dependencies' } as NamedNode,
	/** The depth of the item. */
	'depth': { termType: 'NamedNode', value: 'https://schema.org/depth', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/depth' } as NamedNode,
	/** A description of the item. */
	'description': { termType: 'NamedNode', value: 'https://schema.org/description', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/description' } as NamedNode,
	/** Device required to run the application. Used in cases where a specific make/model is required to run the application. */
	'device': { termType: 'NamedNode', value: 'https://schema.org/device', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/device' } as NamedNode,
	/** One or more alternative conditions considered in the differential diagnosis process as output of a diagnosis process. */
	'diagnosis': { termType: 'NamedNode', value: 'https://schema.org/diagnosis', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/diagnosis' } as NamedNode,
	/** An image containing a diagram that illustrates the structure and/or its component substructures and/or connections with other structures. */
	'diagram': { termType: 'NamedNode', value: 'https://schema.org/diagram', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/diagram' } as NamedNode,
	/** A sub property of instrument. The diet used in this action. */
	'diet': { termType: 'NamedNode', value: 'https://schema.org/diet', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/diet' } as NamedNode,
	/** Nutritional information specific to the dietary plan. May include dietary recommendations on what foods to avoid, what foods to consume, and specific alterations/deviations from the USDA or other regulatory body's approved dietary guidelines. */
	'dietFeatures': { termType: 'NamedNode', value: 'https://schema.org/dietFeatures', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/dietFeatures' } as NamedNode,
	/** One of a set of differential diagnoses for the condition. Specifically, a closely-related or competing diagnosis typically considered later in the cognitive process whereby this medical condition is distinguished from others most likely responsible for a similar collection of signs and symptoms to reach the most parsimonious diagnosis or diagnoses in a patient. */
	'differentialDiagnosis': { termType: 'NamedNode', value: 'https://schema.org/differentialDiagnosis', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/differentialDiagnosis' } as NamedNode,
	/** Indicates whether an [[url]] that is associated with a [[JobPosting]] enables direct application for the job, via the posting website. A job posting is considered to have directApply of [[True]] if an application process for the specified job can be directly initiated via the url(s) given (noting that e.g. multiple internet domains might nevertheless be involved at an implementation level). A value of [[False]] is appropriate if there is no clear path to applying directly online for the specified job, navigating directly from the JobPosting url(s) supplied. */
	'directApply': { termType: 'NamedNode', value: 'https://schema.org/directApply', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/directApply' } as NamedNode,
	/** A director of e.g. TV, radio, movie, video gaming etc. content, or of an event. Directors can be associated with individual items or with a series, episode, clip. */
	'director': { termType: 'NamedNode', value: 'https://schema.org/director', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/director' } as NamedNode,
	/** A director of e.g. TV, radio, movie, video games etc. content. Directors can be associated with individual items or with a series, episode, clip. */
	'directors': { termType: 'NamedNode', value: 'https://schema.org/directors', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/directors' } as NamedNode,
	/** A sub property of description. A short description of the item used to disambiguate from other, similar items. Information from other properties (in particular, name) may be necessary for the description to be useful for disambiguation. */
	'disambiguatingDescription': { termType: 'NamedNode', value: 'https://schema.org/disambiguatingDescription', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/disambiguatingDescription' } as NamedNode,
	/** Any discount applied (to an Order). */
	'discount': { termType: 'NamedNode', value: 'https://schema.org/discount', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/discount' } as NamedNode,
	/** Code used to redeem a discount. */
	'discountCode': { termType: 'NamedNode', value: 'https://schema.org/discountCode', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/discountCode' } as NamedNode,
	/** The currency of the discount.\n\nUse standard formats: [ISO 4217 currency format](http://en.wikipedia.org/wiki/ISO_4217), e.g. "USD"; [Ticker symbol](https://en.wikipedia.org/wiki/List_of_cryptocurrencies) for cryptocurrencies, e.g. "BTC"; well known names for [Local Exchange Trading Systems](https://en.wikipedia.org/wiki/Local_exchange_trading_system) (LETS) and other currency types, e.g. "Ithaca HOUR". */
	'discountCurrency': { termType: 'NamedNode', value: 'https://schema.org/discountCurrency', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/discountCurrency' } as NamedNode,
	/** Specifies the CreativeWork associated with the UserComment. */
	'discusses': { termType: 'NamedNode', value: 'https://schema.org/discusses', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/discusses' } as NamedNode,
	/** A link to the page containing the comments of the CreativeWork. */
	'discussionUrl': { termType: 'NamedNode', value: 'https://schema.org/discussionUrl', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/discussionUrl' } as NamedNode,
	/** Information about disease prevention. */
	'diseasePreventionInfo': { termType: 'NamedNode', value: 'https://schema.org/diseasePreventionInfo', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/diseasePreventionInfo' } as NamedNode,
	/** Statistical information about the spread of a disease, either as [[WebContent]], or
  described directly as a [[Dataset]], or the specific [[Observation]]s in the dataset. When a [[WebContent]] URL is
  provided, the page indicated might also contain more such markup. */
	'diseaseSpreadStatistics': { termType: 'NamedNode', value: 'https://schema.org/diseaseSpreadStatistics', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/diseaseSpreadStatistics' } as NamedNode,
	/** The date that this organization was dissolved. */
	'dissolutionDate': { termType: 'NamedNode', value: 'https://schema.org/dissolutionDate', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/dissolutionDate' } as NamedNode,
	/** The distance travelled, e.g. exercising or travelling. */
	'distance': { termType: 'NamedNode', value: 'https://schema.org/distance', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/distance' } as NamedNode,
	/** One of a set of signs and symptoms that can be used to distinguish this diagnosis from others in the differential diagnosis. */
	'distinguishingSign': { termType: 'NamedNode', value: 'https://schema.org/distinguishingSign', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/distinguishingSign' } as NamedNode,
	/** A downloadable form of this dataset, at a specific location, in a specific format. This property can be repeated if different variations are available. There is no expectation that different downloadable distributions must contain exactly equivalent information (see also [DCAT](https://www.w3.org/TR/vocab-dcat-3/#Class:Distribution) on this point). Different distributions might include or exclude different subsets of the entire dataset, for example. */
	'distribution': { termType: 'NamedNode', value: 'https://schema.org/distribution', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/distribution' } as NamedNode,
	/** Statement on diversity policy by an [[Organization]] e.g. a [[NewsMediaOrganization]]. For a [[NewsMediaOrganization]], a statement describing the newsroom’s diversity policy on both staffing and sources, typically providing staffing data. */
	'diversityPolicy': { termType: 'NamedNode', value: 'https://schema.org/diversityPolicy', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/diversityPolicy' } as NamedNode,
	/** For an [[Organization]] (often but not necessarily a [[NewsMediaOrganization]]), a report on staffing diversity issues. In a news context this might be for example ASNE or RTDNA (US) reports, or self-reported. */
	'diversityStaffingReport': { termType: 'NamedNode', value: 'https://schema.org/diversityStaffingReport', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/diversityStaffingReport' } as NamedNode,
	/** Further documentation describing the Web API in more detail. */
	'documentation': { termType: 'NamedNode', value: 'https://schema.org/documentation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/documentation' } as NamedNode,
	/** Indicates when shipping to a particular [[shippingDestination]] is not available. */
	'doesNotShip': { termType: 'NamedNode', value: 'https://schema.org/doesNotShip', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/doesNotShip' } as NamedNode,
	/** Relates a property to a class that is (one of) the type(s) the property is expected to be used on. */
	'domainIncludes': { termType: 'NamedNode', value: 'https://schema.org/domainIncludes', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/domainIncludes' } as NamedNode,
	/** Whether borrower is a resident of the jurisdiction where the property is located. */
	'domiciledMortgage': { termType: 'NamedNode', value: 'https://schema.org/domiciledMortgage', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/domiciledMortgage' } as NamedNode,
	/** The time admission will commence. */
	'doorTime': { termType: 'NamedNode', value: 'https://schema.org/doorTime', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/doorTime' } as NamedNode,
	/** A dosage form in which this drug/supplement is available, e.g. 'tablet', 'suspension', 'injection'. */
	'dosageForm': { termType: 'NamedNode', value: 'https://schema.org/dosageForm', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/dosageForm' } as NamedNode,
	/** A dosing schedule for the drug for a given population, either observed, recommended, or maximum dose based on the type used. */
	'doseSchedule': { termType: 'NamedNode', value: 'https://schema.org/doseSchedule', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/doseSchedule' } as NamedNode,
	/** The unit of the dose, e.g. 'mg'. */
	'doseUnit': { termType: 'NamedNode', value: 'https://schema.org/doseUnit', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/doseUnit' } as NamedNode,
	/** The value of the dose, e.g. 500. */
	'doseValue': { termType: 'NamedNode', value: 'https://schema.org/doseValue', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/doseValue' } as NamedNode,
	/** a type of payment made in cash during the onset of the purchase of an expensive good/service. The payment typically represents only a percentage of the full purchase price. */
	'downPayment': { termType: 'NamedNode', value: 'https://schema.org/downPayment', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/downPayment' } as NamedNode,
	/** If the file can be downloaded, URL to download the binary. */
	'downloadUrl': { termType: 'NamedNode', value: 'https://schema.org/downloadUrl', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/downloadUrl' } as NamedNode,
	/** The number of downvotes this question, answer or comment has received from the community. */
	'downvoteCount': { termType: 'NamedNode', value: 'https://schema.org/downvoteCount', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/downvoteCount' } as NamedNode,
	/** The vasculature that the vein drains into. */
	'drainsTo': { termType: 'NamedNode', value: 'https://schema.org/drainsTo', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/drainsTo' } as NamedNode,
	/** The drive wheel configuration, i.e. which roadwheels will receive torque from the vehicle's engine via the drivetrain. */
	'driveWheelConfiguration': { termType: 'NamedNode', value: 'https://schema.org/driveWheelConfiguration', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/driveWheelConfiguration' } as NamedNode,
	/** Where a rental car can be dropped off. */
	'dropoffLocation': { termType: 'NamedNode', value: 'https://schema.org/dropoffLocation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/dropoffLocation' } as NamedNode,
	/** When a rental car can be dropped off. */
	'dropoffTime': { termType: 'NamedNode', value: 'https://schema.org/dropoffTime', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/dropoffTime' } as NamedNode,
	/** Specifying a drug or medicine used in a medication procedure. */
	'drug': { termType: 'NamedNode', value: 'https://schema.org/drug', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/drug' } as NamedNode,
	/** The class of drug this belongs to (e.g., statins). */
	'drugClass': { termType: 'NamedNode', value: 'https://schema.org/drugClass', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/drugClass' } as NamedNode,
	/** The unit in which the drug is measured, e.g. '5 mg tablet'. */
	'drugUnit': { termType: 'NamedNode', value: 'https://schema.org/drugUnit', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/drugUnit' } as NamedNode,
	/** The Dun & Bradstreet DUNS number for identifying an organization or business person. */
	'duns': { termType: 'NamedNode', value: 'https://schema.org/duns', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/duns' } as NamedNode,
	/** A therapy that duplicates or overlaps this one. */
	'duplicateTherapy': { termType: 'NamedNode', value: 'https://schema.org/duplicateTherapy', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/duplicateTherapy' } as NamedNode,
	/** The duration of the item (movie, audio recording, event, etc.) in [ISO 8601 date format](http://en.wikipedia.org/wiki/ISO_8601). */
	'duration': { termType: 'NamedNode', value: 'https://schema.org/duration', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/duration' } as NamedNode,
	/** The duration of the warranty promise. Common unitCode values are ANN for year, MON for months, or DAY for days. */
	'durationOfWarranty': { termType: 'NamedNode', value: 'https://schema.org/durationOfWarranty', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/durationOfWarranty' } as NamedNode,
	/** A media object representing the circumstances while performing this direction. */
	'duringMedia': { termType: 'NamedNode', value: 'https://schema.org/duringMedia', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/duringMedia' } as NamedNode,
	/** The amount to be paid as a penalty in the event of early payment of the loan. */
	'earlyPrepaymentPenalty': { termType: 'NamedNode', value: 'https://schema.org/earlyPrepaymentPenalty', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/earlyPrepaymentPenalty' } as NamedNode,
	/** An [EIDR](https://eidr.org/) (Entertainment Identifier Registry) [[identifier]] representing a specific edit / edition for a work of film or television.

For example, the motion picture known as "Ghostbusters" whose [[titleEIDR]] is "10.5240/7EC7-228A-510A-053E-CBB8-J" has several edits, e.g. "10.5240/1F2A-E1C5-680A-14C6-E76B-I" and "10.5240/8A35-3BEE-6497-5D12-9E4F-3".

Since schema.org types like [[Movie]] and [[TVEpisode]] can be used for both works and their multiple expressions, it is possible to use [[titleEIDR]] alone (for a general description), or alongside [[editEIDR]] for a more edit-specific description.
 */
	'editEIDR': { termType: 'NamedNode', value: 'https://schema.org/editEIDR', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/editEIDR' } as NamedNode,
	/** Specifies the Person who edited the CreativeWork. */
	'editor': { termType: 'NamedNode', value: 'https://schema.org/editor', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/editor' } as NamedNode,
	/** For questions that are part of learning resources (e.g. Quiz), eduQuestionType indicates the format of question being given. Example: "Multiple choice", "Open ended", "Flashcard". */
	'eduQuestionType': { termType: 'NamedNode', value: 'https://schema.org/eduQuestionType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/eduQuestionType' } as NamedNode,
	/** Educational background needed for the position or Occupation. */
	'educationRequirements': { termType: 'NamedNode', value: 'https://schema.org/educationRequirements', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/educationRequirements' } as NamedNode,
	/** An alignment to an established educational framework.

This property should not be used where the nature of the alignment can be described using a simple property, for example to express that a resource [[teaches]] or [[assesses]] a competency. */
	'educationalAlignment': { termType: 'NamedNode', value: 'https://schema.org/educationalAlignment', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/educationalAlignment' } as NamedNode,
	/** A description of the qualification, award, certificate, diploma or other educational credential awarded as a consequence of successful completion of this course or program. */
	'educationalCredentialAwarded': { termType: 'NamedNode', value: 'https://schema.org/educationalCredentialAwarded', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/educationalCredentialAwarded' } as NamedNode,
	/** The framework to which the resource being described is aligned. */
	'educationalFramework': { termType: 'NamedNode', value: 'https://schema.org/educationalFramework', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/educationalFramework' } as NamedNode,
	/** The level in terms of progression through an educational or training context. Examples of educational levels include 'beginner', 'intermediate' or 'advanced', and formal sets of level indicators. */
	'educationalLevel': { termType: 'NamedNode', value: 'https://schema.org/educationalLevel', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/educationalLevel' } as NamedNode,
	/** Similar to courseMode, the medium or means of delivery of the program as a whole. The value may either be a text label (e.g. "online", "onsite" or "blended"; "synchronous" or "asynchronous"; "full-time" or "part-time") or a URL reference to a term from a controlled vocabulary (e.g. https://ceds.ed.gov/element/001311#Asynchronous ). */
	'educationalProgramMode': { termType: 'NamedNode', value: 'https://schema.org/educationalProgramMode', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/educationalProgramMode' } as NamedNode,
	/** An educationalRole of an EducationalAudience. */
	'educationalRole': { termType: 'NamedNode', value: 'https://schema.org/educationalRole', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/educationalRole' } as NamedNode,
	/** The purpose of a work in the context of education; for example, 'assignment', 'group work'. */
	'educationalUse': { termType: 'NamedNode', value: 'https://schema.org/educationalUse', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/educationalUse' } as NamedNode,
	/** The elevation of a location ([WGS 84](https://en.wikipedia.org/wiki/World_Geodetic_System)). Values may be of the form 'NUMBER UNIT\_OF\_MEASUREMENT' (e.g., '1,000 m', '3,200 ft') while numbers alone should be assumed to be a value in meters. */
	'elevation': { termType: 'NamedNode', value: 'https://schema.org/elevation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/elevation' } as NamedNode,
	/** The legal requirements such as citizenship, visa and other documentation required for an applicant to this job. */
	'eligibilityToWorkRequirement': { termType: 'NamedNode', value: 'https://schema.org/eligibilityToWorkRequirement', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/eligibilityToWorkRequirement' } as NamedNode,
	/** The type(s) of customers for which the given offer is valid. */
	'eligibleCustomerType': { termType: 'NamedNode', value: 'https://schema.org/eligibleCustomerType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/eligibleCustomerType' } as NamedNode,
	/** The duration for which the given offer is valid. */
	'eligibleDuration': { termType: 'NamedNode', value: 'https://schema.org/eligibleDuration', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/eligibleDuration' } as NamedNode,
	/** The interval and unit of measurement of ordering quantities for which the offer or price specification is valid. This allows e.g. specifying that a certain freight charge is valid only for a certain quantity. */
	'eligibleQuantity': { termType: 'NamedNode', value: 'https://schema.org/eligibleQuantity', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/eligibleQuantity' } as NamedNode,
	/** The ISO 3166-1 (ISO 3166-1 alpha-2) or ISO 3166-2 code, the place, or the GeoShape for the geo-political region(s) for which the offer or delivery charge specification is valid.\n\nSee also [[ineligibleRegion]].
     */
	'eligibleRegion': { termType: 'NamedNode', value: 'https://schema.org/eligibleRegion', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/eligibleRegion' } as NamedNode,
	/** The transaction volume, in a monetary unit, for which the offer or price specification is valid, e.g. for indicating a minimal purchasing volume, to express free shipping above a certain order volume, or to limit the acceptance of credit cards to purchases to a certain minimal amount. */
	'eligibleTransactionVolume': { termType: 'NamedNode', value: 'https://schema.org/eligibleTransactionVolume', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/eligibleTransactionVolume' } as NamedNode,
	/** Email address. */
	'email': { termType: 'NamedNode', value: 'https://schema.org/email', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/email' } as NamedNode,
	/** A URL pointing to a player for a specific video. In general, this is the information in the ```src``` element of an ```embed``` tag and should not be the same as the content of the ```loc``` tag. */
	'embedUrl': { termType: 'NamedNode', value: 'https://schema.org/embedUrl', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/embedUrl' } as NamedNode,
	/** Represents textual captioning from a [[MediaObject]], e.g. text of a 'meme'. */
	'embeddedTextCaption': { termType: 'NamedNode', value: 'https://schema.org/embeddedTextCaption', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/embeddedTextCaption' } as NamedNode,
	/** The CO2 emissions in g/km. When used in combination with a QuantitativeValue, put "g/km" into the unitText property of that value, since there is no UN/CEFACT Common Code for "g/km". */
	'emissionsCO2': { termType: 'NamedNode', value: 'https://schema.org/emissionsCO2', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/emissionsCO2' } as NamedNode,
	/** Someone working for this organization. */
	'employee': { termType: 'NamedNode', value: 'https://schema.org/employee', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/employee' } as NamedNode,
	/** People working for this organization. */
	'employees': { termType: 'NamedNode', value: 'https://schema.org/employees', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/employees' } as NamedNode,
	/** A description of the employer, career opportunities and work environment for this position. */
	'employerOverview': { termType: 'NamedNode', value: 'https://schema.org/employerOverview', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/employerOverview' } as NamedNode,
	/** Type of employment (e.g. full-time, part-time, contract, temporary, seasonal, internship). */
	'employmentType': { termType: 'NamedNode', value: 'https://schema.org/employmentType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/employmentType' } as NamedNode,
	/** Indicates the department, unit and/or facility where the employee reports and/or in which the job is to be performed. */
	'employmentUnit': { termType: 'NamedNode', value: 'https://schema.org/employmentUnit', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/employmentUnit' } as NamedNode,
	/** Another BioChemEntity encoded by this one.  */
	'encodesBioChemEntity': { termType: 'NamedNode', value: 'https://schema.org/encodesBioChemEntity', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/encodesBioChemEntity' } as NamedNode,
	/** The CreativeWork encoded by this media object. */
	'encodesCreativeWork': { termType: 'NamedNode', value: 'https://schema.org/encodesCreativeWork', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/encodesCreativeWork' } as NamedNode,
	/** A media object that encodes this CreativeWork. This property is a synonym for associatedMedia. */
	'encoding': { termType: 'NamedNode', value: 'https://schema.org/encoding', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/encoding' } as NamedNode,
	/** Media type typically expressed using a MIME format (see [IANA site](http://www.iana.org/assignments/media-types/media-types.xhtml) and [MDN reference](https://developer.mozilla.org/en-US/docs/Web/HTTP/Basics_of_HTTP/MIME_types)), e.g. application/zip for a SoftwareApplication binary, audio/mpeg for .mp3 etc.

In cases where a [[CreativeWork]] has several media type representations, [[encoding]] can be used to indicate each [[MediaObject]] alongside particular [[encodingFormat]] information.

Unregistered or niche encoding and file formats can be indicated instead via the most appropriate URL, e.g. defining Web page or a Wikipedia/Wikidata entry. */
	'encodingFormat': { termType: 'NamedNode', value: 'https://schema.org/encodingFormat', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/encodingFormat' } as NamedNode,
	/** The supported encoding type(s) for an EntryPoint request. */
	'encodingType': { termType: 'NamedNode', value: 'https://schema.org/encodingType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/encodingType' } as NamedNode,
	/** A media object that encodes this CreativeWork. */
	'encodings': { termType: 'NamedNode', value: 'https://schema.org/encodings', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/encodings' } as NamedNode,
	/** The end date and time of the item (in [ISO 8601 date format](http://en.wikipedia.org/wiki/ISO_8601)). */
	'endDate': { termType: 'NamedNode', value: 'https://schema.org/endDate', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/endDate' } as NamedNode,
	/** The end time of the clip expressed as the number of seconds from the beginning of the work. */
	'endOffset': { termType: 'NamedNode', value: 'https://schema.org/endOffset', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/endOffset' } as NamedNode,
	/** The endTime of something. For a reserved event or service (e.g. FoodEstablishmentReservation), the time that it is expected to end. For actions that span a period of time, when the action was performed. E.g. John wrote a book from January to *December*. For media, including audio and video, it's the time offset of the end of a clip within a larger file.\n\nNote that Event uses startDate/endDate instead of startTime/endTime, even when describing dates with times. This situation may be clarified in future revisions. */
	'endTime': { termType: 'NamedNode', value: 'https://schema.org/endTime', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/endTime' } as NamedNode,
	/** A sub property of participant. The person/organization being supported. */
	'endorsee': { termType: 'NamedNode', value: 'https://schema.org/endorsee', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/endorsee' } as NamedNode,
	/** People or organizations that endorse the plan. */
	'endorsers': { termType: 'NamedNode', value: 'https://schema.org/endorsers', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/endorsers' } as NamedNode,
	/** Specifies the most energy efficient class on the regulated EU energy consumption scale for the product category a product belongs to. For example, energy consumption for televisions placed on the market after January 1, 2020 is scaled from D to A+++. */
	'energyEfficiencyScaleMax': { termType: 'NamedNode', value: 'https://schema.org/energyEfficiencyScaleMax', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/energyEfficiencyScaleMax' } as NamedNode,
	/** Specifies the least energy efficient class on the regulated EU energy consumption scale for the product category a product belongs to. For example, energy consumption for televisions placed on the market after January 1, 2020 is scaled from D to A+++. */
	'energyEfficiencyScaleMin': { termType: 'NamedNode', value: 'https://schema.org/energyEfficiencyScaleMin', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/energyEfficiencyScaleMin' } as NamedNode,
	/** The volume swept by all of the pistons inside the cylinders of an internal combustion engine in a single movement. \n\nTypical unit code(s): CMQ for cubic centimeter, LTR for liters, INQ for cubic inches\n* Note 1: You can link to information about how the given value has been determined using the [[valueReference]] property.\n* Note 2: You can use [[minValue]] and [[maxValue]] to indicate ranges. */
	'engineDisplacement': { termType: 'NamedNode', value: 'https://schema.org/engineDisplacement', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/engineDisplacement' } as NamedNode,
	/** The power of the vehicle's engine.
    Typical unit code(s): KWT for kilowatt, BHP for brake horsepower, N12 for metric horsepower (PS, with 1 PS = 735,49875 W)\n\n* Note 1: There are many different ways of measuring an engine's power. For an overview, see  [http://en.wikipedia.org/wiki/Horsepower#Engine\_power\_test\_codes](http://en.wikipedia.org/wiki/Horsepower#Engine_power_test_codes).\n* Note 2: You can link to information about how the given value has been determined using the [[valueReference]] property.\n* Note 3: You can use [[minValue]] and [[maxValue]] to indicate ranges. */
	'enginePower': { termType: 'NamedNode', value: 'https://schema.org/enginePower', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/enginePower' } as NamedNode,
	/** The type of engine or engines powering the vehicle. */
	'engineType': { termType: 'NamedNode', value: 'https://schema.org/engineType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/engineType' } as NamedNode,
	/** A sub property of location. The entertainment business where the action occurred. */
	'entertainmentBusiness': { termType: 'NamedNode', value: 'https://schema.org/entertainmentBusiness', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/entertainmentBusiness' } as NamedNode,
	/** The characteristics of associated patients, such as age, gender, race etc. */
	'epidemiology': { termType: 'NamedNode', value: 'https://schema.org/epidemiology', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/epidemiology' } as NamedNode,
	/** An episode of a TV, radio or game media within a series or season. */
	'episode': { termType: 'NamedNode', value: 'https://schema.org/episode', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/episode' } as NamedNode,
	/** Position of the episode within an ordered group of episodes. */
	'episodeNumber': { termType: 'NamedNode', value: 'https://schema.org/episodeNumber', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/episodeNumber' } as NamedNode,
	/** An episode of a TV/radio series or season. */
	'episodes': { termType: 'NamedNode', value: 'https://schema.org/episodes', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/episodes' } as NamedNode,
	/** This ordering relation for qualitative values indicates that the subject is equal to the object. */
	'equal': { termType: 'NamedNode', value: 'https://schema.org/equal', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/equal' } as NamedNode,
	/** For failed actions, more information on the cause of the failure. */
	'error': { termType: 'NamedNode', value: 'https://schema.org/error', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/error' } as NamedNode,
	/** The estimated cost of the supply or supplies consumed when performing instructions. */
	'estimatedCost': { termType: 'NamedNode', value: 'https://schema.org/estimatedCost', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/estimatedCost' } as NamedNode,
	/** The estimated time the flight will take. */
	'estimatedFlightDuration': { termType: 'NamedNode', value: 'https://schema.org/estimatedFlightDuration', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/estimatedFlightDuration' } as NamedNode,
	/** An estimated salary for a job posting or occupation, based on a variety of variables including, but not limited to industry, job title, and location. Estimated salaries  are often computed by outside organizations rather than the hiring organization, who may not have committed to the estimated value. */
	'estimatedSalary': { termType: 'NamedNode', value: 'https://schema.org/estimatedSalary', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/estimatedSalary' } as NamedNode,
	/** The condition, complication, or symptom whose risk is being estimated. */
	'estimatesRiskOf': { termType: 'NamedNode', value: 'https://schema.org/estimatesRiskOf', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/estimatesRiskOf' } as NamedNode,
	/** Statement about ethics policy, e.g. of a [[NewsMediaOrganization]] regarding journalistic and publishing practices, or of a [[Restaurant]], a page describing food source policies. In the case of a [[NewsMediaOrganization]], an ethicsPolicy is typically a statement describing the personal, organizational, and corporate standards of behavior expected by the organization. */
	'ethicsPolicy': { termType: 'NamedNode', value: 'https://schema.org/ethicsPolicy', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ethicsPolicy' } as NamedNode,
	/** Upcoming or past event associated with this place, organization, or action. */
	'event': { termType: 'NamedNode', value: 'https://schema.org/event', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/event' } as NamedNode,
	/** The eventAttendanceMode of an event indicates whether it occurs online, offline, or a mix. */
	'eventAttendanceMode': { termType: 'NamedNode', value: 'https://schema.org/eventAttendanceMode', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/eventAttendanceMode' } as NamedNode,
	/** Associates an [[Event]] with a [[Schedule]]. There are circumstances where it is preferable to share a schedule for a series of
      repeating events rather than data on the individual events themselves. For example, a website or application might prefer to publish a schedule for a weekly
      gym class rather than provide data on every event. A schedule could be processed by applications to add forthcoming events to a calendar. An [[Event]] that
      is associated with a [[Schedule]] using this property should not have [[startDate]] or [[endDate]] properties. These are instead defined within the associated
      [[Schedule]], this avoids any ambiguity for clients using the data. The property might have repeated values to specify different schedules, e.g. for different months
      or seasons. */
	'eventSchedule': { termType: 'NamedNode', value: 'https://schema.org/eventSchedule', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/eventSchedule' } as NamedNode,
	/** An eventStatus of an event represents its status; particularly useful when an event is cancelled or rescheduled. */
	'eventStatus': { termType: 'NamedNode', value: 'https://schema.org/eventStatus', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/eventStatus' } as NamedNode,
	/** Upcoming or past events associated with this place or organization. */
	'events': { termType: 'NamedNode', value: 'https://schema.org/events', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/events' } as NamedNode,
	/** Strength of evidence of the data used to formulate the guideline (enumerated). */
	'evidenceLevel': { termType: 'NamedNode', value: 'https://schema.org/evidenceLevel', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/evidenceLevel' } as NamedNode,
	/** Source of the data used to formulate the guidance, e.g. RCT, consensus opinion, etc. */
	'evidenceOrigin': { termType: 'NamedNode', value: 'https://schema.org/evidenceOrigin', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/evidenceOrigin' } as NamedNode,
	/** A creative work that this work is an example/instance/realization/derivation of. */
	'exampleOfWork': { termType: 'NamedNode', value: 'https://schema.org/exampleOfWork', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/exampleOfWork' } as NamedNode,
	/** Defines a [[Date]] or [[DateTime]] during which a scheduled [[Event]] will not take place. The property allows exceptions to
      a [[Schedule]] to be specified. If an exception is specified as a [[DateTime]] then only the event that would have started at that specific date and time
      should be excluded from the schedule. If an exception is specified as a [[Date]] then any event that is scheduled for that 24 hour period should be
      excluded from the schedule. This allows a whole day to be excluded from the schedule without having to itemise every scheduled event. */
	'exceptDate': { termType: 'NamedNode', value: 'https://schema.org/exceptDate', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/exceptDate' } as NamedNode,
	/** The difference between the price at which a broker or other intermediary buys and sells foreign currency. */
	'exchangeRateSpread': { termType: 'NamedNode', value: 'https://schema.org/exchangeRateSpread', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/exchangeRateSpread' } as NamedNode,
	/** Library file name, e.g., mscorlib.dll, system.web.dll. */
	'executableLibraryName': { termType: 'NamedNode', value: 'https://schema.org/executableLibraryName', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/executableLibraryName' } as NamedNode,
	/** A sub property of location. The course where this action was taken. */
	'exerciseCourse': { termType: 'NamedNode', value: 'https://schema.org/exerciseCourse', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/exerciseCourse' } as NamedNode,
	/** A sub property of instrument. The exercise plan used on this action. */
	'exercisePlan': { termType: 'NamedNode', value: 'https://schema.org/exercisePlan', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/exercisePlan' } as NamedNode,
	/** A sub property of instrument. The diet used in this action. */
	'exerciseRelatedDiet': { termType: 'NamedNode', value: 'https://schema.org/exerciseRelatedDiet', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/exerciseRelatedDiet' } as NamedNode,
	/** Type(s) of exercise or activity, such as strength training, flexibility training, aerobics, cardiac rehabilitation, etc. */
	'exerciseType': { termType: 'NamedNode', value: 'https://schema.org/exerciseType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/exerciseType' } as NamedNode,
	/** exif data for this object. */
	'exifData': { termType: 'NamedNode', value: 'https://schema.org/exifData', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/exifData' } as NamedNode,
	/** The earliest date the package may arrive. */
	'expectedArrivalFrom': { termType: 'NamedNode', value: 'https://schema.org/expectedArrivalFrom', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/expectedArrivalFrom' } as NamedNode,
	/** The latest date the package may arrive. */
	'expectedArrivalUntil': { termType: 'NamedNode', value: 'https://schema.org/expectedArrivalUntil', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/expectedArrivalUntil' } as NamedNode,
	/** The likely outcome in either the short term or long term of the medical condition. */
	'expectedPrognosis': { termType: 'NamedNode', value: 'https://schema.org/expectedPrognosis', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/expectedPrognosis' } as NamedNode,
	/** An Offer which must be accepted before the user can perform the Action. For example, the user may need to buy a movie before being able to watch it. */
	'expectsAcceptanceOf': { termType: 'NamedNode', value: 'https://schema.org/expectsAcceptanceOf', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/expectsAcceptanceOf' } as NamedNode,
	/** Indicates whether a [[JobPosting]] will accept experience (as indicated by [[OccupationalExperienceRequirements]]) in place of its formal educational qualifications (as indicated by [[educationRequirements]]). If true, indicates that satisfying one of these requirements is sufficient. */
	'experienceInPlaceOfEducation': { termType: 'NamedNode', value: 'https://schema.org/experienceInPlaceOfEducation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/experienceInPlaceOfEducation' } as NamedNode,
	/** Description of skills and experience needed for the position or Occupation. */
	'experienceRequirements': { termType: 'NamedNode', value: 'https://schema.org/experienceRequirements', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/experienceRequirements' } as NamedNode,
	/** Medical expert advice related to the plan. */
	'expertConsiderations': { termType: 'NamedNode', value: 'https://schema.org/expertConsiderations', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/expertConsiderations' } as NamedNode,
	/** Date the content expires and is no longer useful or available. For example a [[VideoObject]] or [[NewsArticle]] whose availability or relevance is time-limited, or a [[ClaimReview]] fact check whose publisher wants to indicate that it may no longer be relevant (or helpful to highlight) after some date. */
	'expires': { termType: 'NamedNode', value: 'https://schema.org/expires', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/expires' } as NamedNode,
	/** Tissue, organ, biological sample, etc in which activity of this gene has been observed experimentally. For example brain, digestive system. */
	'expressedIn': { termType: 'NamedNode', value: 'https://schema.org/expressedIn', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/expressedIn' } as NamedNode,
	/** Family name. In the U.S., the last name of a Person. */
	'familyName': { termType: 'NamedNode', value: 'https://schema.org/familyName', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/familyName' } as NamedNode,
	/** The number of grams of fat. */
	'fatContent': { termType: 'NamedNode', value: 'https://schema.org/fatContent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/fatContent' } as NamedNode,
	/** The fax number. */
	'faxNumber': { termType: 'NamedNode', value: 'https://schema.org/faxNumber', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/faxNumber' } as NamedNode,
	/** Features or modules provided by this application (and possibly required by other applications). */
	'featureList': { termType: 'NamedNode', value: 'https://schema.org/featureList', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/featureList' } as NamedNode,
	/** Description of fees, commissions, and other terms applied either to a class of financial product, or by a financial service organization. */
	'feesAndCommissionsSpecification': { termType: 'NamedNode', value: 'https://schema.org/feesAndCommissionsSpecification', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/feesAndCommissionsSpecification' } as NamedNode,
	/** The number of grams of fiber. */
	'fiberContent': { termType: 'NamedNode', value: 'https://schema.org/fiberContent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/fiberContent' } as NamedNode,
	/** Media type, typically MIME format (see [IANA site](http://www.iana.org/assignments/media-types/media-types.xhtml)) of the content, e.g. application/zip of a SoftwareApplication binary. In cases where a CreativeWork has several media type representations, 'encoding' can be used to indicate each MediaObject alongside particular fileFormat information. Unregistered or niche file formats can be indicated instead via the most appropriate URL, e.g. defining Web page or a Wikipedia entry. */
	'fileFormat': { termType: 'NamedNode', value: 'https://schema.org/fileFormat', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/fileFormat' } as NamedNode,
	/** Size of the application / package (e.g. 18MB). In the absence of a unit (MB, KB etc.), KB will be assumed. */
	'fileSize': { termType: 'NamedNode', value: 'https://schema.org/fileSize', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/fileSize' } as NamedNode,
	/** A financial aid type or program which students may use to pay for tuition or fees associated with the program. */
	'financialAidEligible': { termType: 'NamedNode', value: 'https://schema.org/financialAidEligible', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/financialAidEligible' } as NamedNode,
	/** Indicates the first known occurrence of a [[Claim]] in some [[CreativeWork]]. */
	'firstAppearance': { termType: 'NamedNode', value: 'https://schema.org/firstAppearance', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/firstAppearance' } as NamedNode,
	/** The date and place the work was first performed. */
	'firstPerformance': { termType: 'NamedNode', value: 'https://schema.org/firstPerformance', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/firstPerformance' } as NamedNode,
	/** The distance of the flight. */
	'flightDistance': { termType: 'NamedNode', value: 'https://schema.org/flightDistance', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/flightDistance' } as NamedNode,
	/** The unique identifier for a flight including the airline IATA code. For example, if describing United flight 110, where the IATA code for United is 'UA', the flightNumber is 'UA110'. */
	'flightNumber': { termType: 'NamedNode', value: 'https://schema.org/flightNumber', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/flightNumber' } as NamedNode,
	/** The floor level for an [[Accommodation]] in a multi-storey building. Since counting
  systems [vary internationally](https://en.wikipedia.org/wiki/Storey#Consecutive_number_floor_designations), the local system should be used where possible. */
	'floorLevel': { termType: 'NamedNode', value: 'https://schema.org/floorLevel', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/floorLevel' } as NamedNode,
	/** A floor limit is the amount of money above which credit card transactions must be authorized. */
	'floorLimit': { termType: 'NamedNode', value: 'https://schema.org/floorLimit', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/floorLimit' } as NamedNode,
	/** The size of the accommodation, e.g. in square meter or squarefoot.
Typical unit code(s): MTK for square meter, FTK for square foot, or YDK for square yard  */
	'floorSize': { termType: 'NamedNode', value: 'https://schema.org/floorSize', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/floorSize' } as NamedNode,
	/** A sub property of object. The person or organization being followed. */
	'followee': { termType: 'NamedNode', value: 'https://schema.org/followee', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/followee' } as NamedNode,
	/** The most generic uni-directional social relation. */
	'follows': { termType: 'NamedNode', value: 'https://schema.org/follows', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/follows' } as NamedNode,
	/** Typical or recommended followup care after the procedure is performed. */
	'followup': { termType: 'NamedNode', value: 'https://schema.org/followup', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/followup' } as NamedNode,
	/** A sub property of location. The specific food establishment where the action occurred. */
	'foodEstablishment': { termType: 'NamedNode', value: 'https://schema.org/foodEstablishment', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/foodEstablishment' } as NamedNode,
	/** A sub property of location. The specific food event where the action occurred. */
	'foodEvent': { termType: 'NamedNode', value: 'https://schema.org/foodEvent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/foodEvent' } as NamedNode,
	/** Any precaution, guidance, contraindication, etc. related to consumption of specific foods while taking this drug. */
	'foodWarning': { termType: 'NamedNode', value: 'https://schema.org/foodWarning', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/foodWarning' } as NamedNode,
	/** A person who founded this organization. */
	'founder': { termType: 'NamedNode', value: 'https://schema.org/founder', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/founder' } as NamedNode,
	/** A person who founded this organization. */
	'founders': { termType: 'NamedNode', value: 'https://schema.org/founders', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/founders' } as NamedNode,
	/** The date that this organization was founded. */
	'foundingDate': { termType: 'NamedNode', value: 'https://schema.org/foundingDate', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/foundingDate' } as NamedNode,
	/** The place where the Organization was founded. */
	'foundingLocation': { termType: 'NamedNode', value: 'https://schema.org/foundingLocation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/foundingLocation' } as NamedNode,
	/** A flag to signal that the item, event, or place is accessible for free. */
	'free': { termType: 'NamedNode', value: 'https://schema.org/free', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/free' } as NamedNode,
	/** A monetary value above (or at) which the shipping rate becomes free. Intended to be used via an [[OfferShippingDetails]] with [[shippingSettingsLink]] matching this [[ShippingRateSettings]]. */
	'freeShippingThreshold': { termType: 'NamedNode', value: 'https://schema.org/freeShippingThreshold', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/freeShippingThreshold' } as NamedNode,
	/** How often the dose is taken, e.g. 'daily'. */
	'frequency': { termType: 'NamedNode', value: 'https://schema.org/frequency', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/frequency' } as NamedNode,
	/** A sub property of location. The original location of the object or the agent before the action. */
	'fromLocation': { termType: 'NamedNode', value: 'https://schema.org/fromLocation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/fromLocation' } as NamedNode,
	/** The capacity of the fuel tank or in the case of electric cars, the battery. If there are multiple components for storage, this should indicate the total of all storage of the same type.\n\nTypical unit code(s): LTR for liters, GLL of US gallons, GLI for UK / imperial gallons, AMH for ampere-hours (for electrical vehicles). */
	'fuelCapacity': { termType: 'NamedNode', value: 'https://schema.org/fuelCapacity', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/fuelCapacity' } as NamedNode,
	/** The amount of fuel consumed for traveling a particular distance or temporal duration with the given vehicle (e.g. liters per 100 km).\n\n* Note 1: There are unfortunately no standard unit codes for liters per 100 km.  Use [[unitText]] to indicate the unit of measurement, e.g. L/100 km.\n* Note 2: There are two ways of indicating the fuel consumption, [[fuelConsumption]] (e.g. 8 liters per 100 km) and [[fuelEfficiency]] (e.g. 30 miles per gallon). They are reciprocal.\n* Note 3: Often, the absolute value is useful only when related to driving speed ("at 80 km/h") or usage pattern ("city traffic"). You can use [[valueReference]] to link the value for the fuel consumption to another value. */
	'fuelConsumption': { termType: 'NamedNode', value: 'https://schema.org/fuelConsumption', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/fuelConsumption' } as NamedNode,
	/** The distance traveled per unit of fuel used; most commonly miles per gallon (mpg) or kilometers per liter (km/L).\n\n* Note 1: There are unfortunately no standard unit codes for miles per gallon or kilometers per liter. Use [[unitText]] to indicate the unit of measurement, e.g. mpg or km/L.\n* Note 2: There are two ways of indicating the fuel consumption, [[fuelConsumption]] (e.g. 8 liters per 100 km) and [[fuelEfficiency]] (e.g. 30 miles per gallon). They are reciprocal.\n* Note 3: Often, the absolute value is useful only when related to driving speed ("at 80 km/h") or usage pattern ("city traffic"). You can use [[valueReference]] to link the value for the fuel economy to another value. */
	'fuelEfficiency': { termType: 'NamedNode', value: 'https://schema.org/fuelEfficiency', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/fuelEfficiency' } as NamedNode,
	/** The type of fuel suitable for the engine or engines of the vehicle. If the vehicle has only one engine, this property can be attached directly to the vehicle. */
	'fuelType': { termType: 'NamedNode', value: 'https://schema.org/fuelType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/fuelType' } as NamedNode,
	/** The degree of mobility the joint allows. */
	'functionalClass': { termType: 'NamedNode', value: 'https://schema.org/functionalClass', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/functionalClass' } as NamedNode,
	/** Indicates something directly or indirectly funded or sponsored through a [[Grant]]. See also [[ownershipFundingInfo]]. */
	'fundedItem': { termType: 'NamedNode', value: 'https://schema.org/fundedItem', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/fundedItem' } as NamedNode,
	/** A person or organization that supports (sponsors) something through some kind of financial contribution. */
	'funder': { termType: 'NamedNode', value: 'https://schema.org/funder', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/funder' } as NamedNode,
	/** A [[Grant]] that directly or indirectly provide funding or sponsorship for this item. See also [[ownershipFundingInfo]]. */
	'funding': { termType: 'NamedNode', value: 'https://schema.org/funding', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/funding' } as NamedNode,
	/** Video game which is played on this server. */
	'game': { termType: 'NamedNode', value: 'https://schema.org/game', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/game' } as NamedNode,
	/** Indicates the availability type of the game content associated with this action, such as whether it is a full version or a demo. */
	'gameAvailabilityType': { termType: 'NamedNode', value: 'https://schema.org/gameAvailabilityType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/gameAvailabilityType' } as NamedNode,
	/** The edition of a video game. */
	'gameEdition': { termType: 'NamedNode', value: 'https://schema.org/gameEdition', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/gameEdition' } as NamedNode,
	/** An item is an object within the game world that can be collected by a player or, occasionally, a non-player character. */
	'gameItem': { termType: 'NamedNode', value: 'https://schema.org/gameItem', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/gameItem' } as NamedNode,
	/** Real or fictional location of the game (or part of game). */
	'gameLocation': { termType: 'NamedNode', value: 'https://schema.org/gameLocation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/gameLocation' } as NamedNode,
	/** The electronic systems used to play <a href="http://en.wikipedia.org/wiki/Category:Video_game_platforms">video games</a>. */
	'gamePlatform': { termType: 'NamedNode', value: 'https://schema.org/gamePlatform', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/gamePlatform' } as NamedNode,
	/** The server on which  it is possible to play the game. */
	'gameServer': { termType: 'NamedNode', value: 'https://schema.org/gameServer', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/gameServer' } as NamedNode,
	/** Links to tips, tactics, etc. */
	'gameTip': { termType: 'NamedNode', value: 'https://schema.org/gameTip', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/gameTip' } as NamedNode,
	/** Gender of something, typically a [[Person]], but possibly also fictional characters, animals, etc. While https://schema.org/Male and https://schema.org/Female may be used, text strings are also acceptable for people who do not identify as a binary gender. The [[gender]] property can also be used in an extended sense to cover e.g. the gender of sports teams. As with the gender of individuals, we do not try to enumerate all possibilities. A mixed-gender [[SportsTeam]] can be indicated with a text value of "Mixed". */
	'gender': { termType: 'NamedNode', value: 'https://schema.org/gender', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/gender' } as NamedNode,
	/** Genre of the creative work, broadcast channel or group. */
	'genre': { termType: 'NamedNode', value: 'https://schema.org/genre', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/genre' } as NamedNode,
	/** The geo coordinates of the place. */
	'geo': { termType: 'NamedNode', value: 'https://schema.org/geo', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/geo' } as NamedNode,
	/** Represents a relationship between two geometries (or the places they represent), relating a containing geometry to a contained geometry. "a contains b iff no points of b lie in the exterior of a, and at least one point of the interior of b lies in the interior of a". As defined in [DE-9IM](https://en.wikipedia.org/wiki/DE-9IM). */
	'geoContains': { termType: 'NamedNode', value: 'https://schema.org/geoContains', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/geoContains' } as NamedNode,
	/** Represents a relationship between two geometries (or the places they represent), relating a geometry to another that covers it. As defined in [DE-9IM](https://en.wikipedia.org/wiki/DE-9IM). */
	'geoCoveredBy': { termType: 'NamedNode', value: 'https://schema.org/geoCoveredBy', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/geoCoveredBy' } as NamedNode,
	/** Represents a relationship between two geometries (or the places they represent), relating a covering geometry to a covered geometry. "Every point of b is a point of (the interior or boundary of) a". As defined in [DE-9IM](https://en.wikipedia.org/wiki/DE-9IM). */
	'geoCovers': { termType: 'NamedNode', value: 'https://schema.org/geoCovers', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/geoCovers' } as NamedNode,
	/** Represents a relationship between two geometries (or the places they represent), relating a geometry to another that crosses it: "a crosses b: they have some but not all interior points in common, and the dimension of the intersection is less than that of at least one of them". As defined in [DE-9IM](https://en.wikipedia.org/wiki/DE-9IM). */
	'geoCrosses': { termType: 'NamedNode', value: 'https://schema.org/geoCrosses', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/geoCrosses' } as NamedNode,
	/** Represents spatial relations in which two geometries (or the places they represent) are topologically disjoint: "they have no point in common. They form a set of disconnected geometries." (A symmetric relationship, as defined in [DE-9IM](https://en.wikipedia.org/wiki/DE-9IM).) */
	'geoDisjoint': { termType: 'NamedNode', value: 'https://schema.org/geoDisjoint', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/geoDisjoint' } as NamedNode,
	/** Represents spatial relations in which two geometries (or the places they represent) are topologically equal, as defined in [DE-9IM](https://en.wikipedia.org/wiki/DE-9IM). "Two geometries are topologically equal if their interiors intersect and no part of the interior or boundary of one geometry intersects the exterior of the other" (a symmetric relationship). */
	'geoEquals': { termType: 'NamedNode', value: 'https://schema.org/geoEquals', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/geoEquals' } as NamedNode,
	/** Represents spatial relations in which two geometries (or the places they represent) have at least one point in common. As defined in [DE-9IM](https://en.wikipedia.org/wiki/DE-9IM). */
	'geoIntersects': { termType: 'NamedNode', value: 'https://schema.org/geoIntersects', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/geoIntersects' } as NamedNode,
	/** Indicates the GeoCoordinates at the centre of a GeoShape, e.g. GeoCircle. */
	'geoMidpoint': { termType: 'NamedNode', value: 'https://schema.org/geoMidpoint', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/geoMidpoint' } as NamedNode,
	/** Represents a relationship between two geometries (or the places they represent), relating a geometry to another that geospatially overlaps it, i.e. they have some but not all points in common. As defined in [DE-9IM](https://en.wikipedia.org/wiki/DE-9IM). */
	'geoOverlaps': { termType: 'NamedNode', value: 'https://schema.org/geoOverlaps', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/geoOverlaps' } as NamedNode,
	/** Indicates the approximate radius of a GeoCircle (metres unless indicated otherwise via Distance notation). */
	'geoRadius': { termType: 'NamedNode', value: 'https://schema.org/geoRadius', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/geoRadius' } as NamedNode,
	/** Represents spatial relations in which two geometries (or the places they represent) touch: "they have at least one boundary point in common, but no interior points." (A symmetric relationship, as defined in [DE-9IM](https://en.wikipedia.org/wiki/DE-9IM).) */
	'geoTouches': { termType: 'NamedNode', value: 'https://schema.org/geoTouches', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/geoTouches' } as NamedNode,
	/** Represents a relationship between two geometries (or the places they represent), relating a geometry to one that contains it, i.e. it is inside (i.e. within) its interior. As defined in [DE-9IM](https://en.wikipedia.org/wiki/DE-9IM). */
	'geoWithin': { termType: 'NamedNode', value: 'https://schema.org/geoWithin', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/geoWithin' } as NamedNode,
	/** The geographic area associated with the audience. */
	'geographicArea': { termType: 'NamedNode', value: 'https://schema.org/geographicArea', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/geographicArea' } as NamedNode,
	/** Information about getting tested (for a [[MedicalCondition]]), e.g. in the context of a pandemic. */
	'gettingTestedInfo': { termType: 'NamedNode', value: 'https://schema.org/gettingTestedInfo', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/gettingTestedInfo' } as NamedNode,
	/** Given name. In the U.S., the first name of a Person. */
	'givenName': { termType: 'NamedNode', value: 'https://schema.org/givenName', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/givenName' } as NamedNode,
	/** The [Global Location Number](http://www.gs1.org/gln) (GLN, sometimes also referred to as International Location Number or ILN) of the respective organization, person, or place. The GLN is a 13-digit number used to identify parties and physical locations. */
	'globalLocationNumber': { termType: 'NamedNode', value: 'https://schema.org/globalLocationNumber', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/globalLocationNumber' } as NamedNode,
	/** governmentBenefitsInfo provides information about government benefits associated with a SpecialAnnouncement. */
	'governmentBenefitsInfo': { termType: 'NamedNode', value: 'https://schema.org/governmentBenefitsInfo', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/governmentBenefitsInfo' } as NamedNode,
	/** The period of time after any due date that the borrower has to fulfil its obligations before a default (failure to pay) is deemed to have occurred. */
	'gracePeriod': { termType: 'NamedNode', value: 'https://schema.org/gracePeriod', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/gracePeriod' } as NamedNode,
	/** The person, organization, contact point, or audience that has been granted this permission. */
	'grantee': { termType: 'NamedNode', value: 'https://schema.org/grantee', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/grantee' } as NamedNode,
	/** This ordering relation for qualitative values indicates that the subject is greater than the object. */
	'greater': { termType: 'NamedNode', value: 'https://schema.org/greater', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/greater' } as NamedNode,
	/** This ordering relation for qualitative values indicates that the subject is greater than or equal to the object. */
	'greaterOrEqual': { termType: 'NamedNode', value: 'https://schema.org/greaterOrEqual', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/greaterOrEqual' } as NamedNode,
	/** A Global Trade Item Number ([GTIN](https://www.gs1.org/standards/id-keys/gtin)). GTINs identify trade items, including products and services, using numeric identification codes.

The GS1 [digital link specifications](https://www.gs1.org/standards/Digital-Link/) express GTINs as URLs (URIs, IRIs, etc.). Details including regular expression examples can be found in, Section 6 of the GS1 URI Syntax specification; see also [schema.org tracking issue](https://github.com/schemaorg/schemaorg/issues/3156#issuecomment-1209522809) for schema.org-specific discussion. A correct [[gtin]] value should be a valid GTIN, which means that it should be an all-numeric string of either 8, 12, 13 or 14 digits, or a "GS1 Digital Link" URL based on such a string. The numeric component should also have a [valid GS1 check digit](https://www.gs1.org/services/check-digit-calculator) and meet the other rules for valid GTINs. See also [GS1's GTIN Summary](http://www.gs1.org/barcodes/technical/idkeys/gtin) and [Wikipedia](https://en.wikipedia.org/wiki/Global_Trade_Item_Number) for more details. Left-padding of the gtin values is not required or encouraged. The [[gtin]] property generalizes the earlier [[gtin8]], [[gtin12]], [[gtin13]], and [[gtin14]] properties.

Note also that this is a definition for how to include GTINs in Schema.org data, and not a definition of GTINs in general - see the GS1 documentation for authoritative details. */
	'gtin': { termType: 'NamedNode', value: 'https://schema.org/gtin', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/gtin' } as NamedNode,
	/** The GTIN-12 code of the product, or the product to which the offer refers. The GTIN-12 is the 12-digit GS1 Identification Key composed of a U.P.C. Company Prefix, Item Reference, and Check Digit used to identify trade items. See [GS1 GTIN Summary](http://www.gs1.org/barcodes/technical/idkeys/gtin) for more details. */
	'gtin12': { termType: 'NamedNode', value: 'https://schema.org/gtin12', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/gtin12' } as NamedNode,
	/** The GTIN-13 code of the product, or the product to which the offer refers. This is equivalent to 13-digit ISBN codes and EAN UCC-13. Former 12-digit UPC codes can be converted into a GTIN-13 code by simply adding a preceding zero. See [GS1 GTIN Summary](http://www.gs1.org/barcodes/technical/idkeys/gtin) for more details. */
	'gtin13': { termType: 'NamedNode', value: 'https://schema.org/gtin13', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/gtin13' } as NamedNode,
	/** The GTIN-14 code of the product, or the product to which the offer refers. See [GS1 GTIN Summary](http://www.gs1.org/barcodes/technical/idkeys/gtin) for more details. */
	'gtin14': { termType: 'NamedNode', value: 'https://schema.org/gtin14', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/gtin14' } as NamedNode,
	/** The GTIN-8 code of the product, or the product to which the offer refers. This code is also known as EAN/UCC-8 or 8-digit EAN. See [GS1 GTIN Summary](http://www.gs1.org/barcodes/technical/idkeys/gtin) for more details. */
	'gtin8': { termType: 'NamedNode', value: 'https://schema.org/gtin8', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/gtin8' } as NamedNode,
	/** A medical guideline related to this entity. */
	'guideline': { termType: 'NamedNode', value: 'https://schema.org/guideline', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/guideline' } as NamedNode,
	/** Date on which this guideline's recommendation was made. */
	'guidelineDate': { termType: 'NamedNode', value: 'https://schema.org/guidelineDate', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/guidelineDate' } as NamedNode,
	/** The medical conditions, treatments, etc. that are the subject of the guideline. */
	'guidelineSubject': { termType: 'NamedNode', value: 'https://schema.org/guidelineSubject', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/guidelineSubject' } as NamedNode,
	/** The typical delay between the receipt of the order and the goods either leaving the warehouse or being prepared for pickup, in case the delivery method is on site pickup. Typical properties: minValue, maxValue, unitCode (d for DAY).  This is by common convention assumed to mean business days (if a unitCode is used, coded as "d"), i.e. only counting days when the business normally operates. */
	'handlingTime': { termType: 'NamedNode', value: 'https://schema.org/handlingTime', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/handlingTime' } as NamedNode,
	/** Used to tag an item to be intended or suitable for consumption or use by adults only. */
	'hasAdultConsideration': { termType: 'NamedNode', value: 'https://schema.org/hasAdultConsideration', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/hasAdultConsideration' } as NamedNode,
	/** Indicates a BioChemEntity that (in some sense) has this BioChemEntity as a part.  */
	'hasBioChemEntityPart': { termType: 'NamedNode', value: 'https://schema.org/hasBioChemEntityPart', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/hasBioChemEntityPart' } as NamedNode,
	/** A symbolic representation of a BioChemEntity. For example, a nucleotide sequence of a Gene or an amino acid sequence of a Protein. */
	'hasBioPolymerSequence': { termType: 'NamedNode', value: 'https://schema.org/hasBioPolymerSequence', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/hasBioPolymerSequence' } as NamedNode,
	/** A broadcast channel of a broadcast service. */
	'hasBroadcastChannel': { termType: 'NamedNode', value: 'https://schema.org/hasBroadcastChannel', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/hasBroadcastChannel' } as NamedNode,
	/** A Category code contained in this code set. */
	'hasCategoryCode': { termType: 'NamedNode', value: 'https://schema.org/hasCategoryCode', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/hasCategoryCode' } as NamedNode,
	/** A course or class that is one of the learning opportunities that constitute an educational / occupational program. No information is implied about whether the course is mandatory or optional; no guarantee is implied about whether the course will be available to everyone on the program. */
	'hasCourse': { termType: 'NamedNode', value: 'https://schema.org/hasCourse', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/hasCourse' } as NamedNode,
	/** An offering of the course at a specific time and place or through specific media or mode of study or to a specific section of students. */
	'hasCourseInstance': { termType: 'NamedNode', value: 'https://schema.org/hasCourseInstance', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/hasCourseInstance' } as NamedNode,
	/** A credential awarded to the Person or Organization. */
	'hasCredential': { termType: 'NamedNode', value: 'https://schema.org/hasCredential', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/hasCredential' } as NamedNode,
	/** A Defined Term contained in this term set. */
	'hasDefinedTerm': { termType: 'NamedNode', value: 'https://schema.org/hasDefinedTerm', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/hasDefinedTerm' } as NamedNode,
	/** Method used for delivery or shipping. */
	'hasDeliveryMethod': { termType: 'NamedNode', value: 'https://schema.org/hasDeliveryMethod', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/hasDeliveryMethod' } as NamedNode,
	/** A permission related to the access to this document (e.g. permission to read or write an electronic document). For a public document, specify a grantee with an Audience with audienceType equal to "public". */
	'hasDigitalDocumentPermission': { termType: 'NamedNode', value: 'https://schema.org/hasDigitalDocumentPermission', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/hasDigitalDocumentPermission' } as NamedNode,
	/** Indicates whether some facility (e.g. [[FoodEstablishment]], [[CovidTestingFacility]]) offers a service that can be used by driving through in a car. In the case of [[CovidTestingFacility]] such facilities could potentially help with social distancing from other potentially-infected users. */
	'hasDriveThroughService': { termType: 'NamedNode', value: 'https://schema.org/hasDriveThroughService', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/hasDriveThroughService' } as NamedNode,
	/** Defines the energy efficiency Category (also known as "class" or "rating") for a product according to an international energy efficiency standard. */
	'hasEnergyConsumptionDetails': { termType: 'NamedNode', value: 'https://schema.org/hasEnergyConsumptionDetails', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/hasEnergyConsumptionDetails' } as NamedNode,
	/** Defines the energy efficiency Category (which could be either a rating out of range of values or a yes/no certification) for a product according to an international energy efficiency standard. */
	'hasEnergyEfficiencyCategory': { termType: 'NamedNode', value: 'https://schema.org/hasEnergyEfficiencyCategory', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/hasEnergyEfficiencyCategory' } as NamedNode,
	/** Indicates the aspect or aspects specifically addressed in some [[HealthTopicContent]]. For example, that the content is an overview, or that it talks about treatment, self-care, treatments or their side-effects. */
	'hasHealthAspect': { termType: 'NamedNode', value: 'https://schema.org/hasHealthAspect', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/hasHealthAspect' } as NamedNode,
	/** A URL to a map of the place. */
	'hasMap': { termType: 'NamedNode', value: 'https://schema.org/hasMap', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/hasMap' } as NamedNode,
	/** A product measurement, for example the inseam of pants, the wheel size of a bicycle, or the gauge of a screw. Usually an exact measurement, but can also be a range of measurements for adjustable products, for example belts and ski bindings. */
	'hasMeasurement': { termType: 'NamedNode', value: 'https://schema.org/hasMeasurement', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/hasMeasurement' } as NamedNode,
	/** Either the actual menu as a structured representation, as text, or a URL of the menu. */
	'hasMenu': { termType: 'NamedNode', value: 'https://schema.org/hasMenu', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/hasMenu' } as NamedNode,
	/** A food or drink item contained in a menu or menu section. */
	'hasMenuItem': { termType: 'NamedNode', value: 'https://schema.org/hasMenuItem', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/hasMenuItem' } as NamedNode,
	/** A subgrouping of the menu (by dishes, course, serving time period, etc.). */
	'hasMenuSection': { termType: 'NamedNode', value: 'https://schema.org/hasMenuSection', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/hasMenuSection' } as NamedNode,
	/** Specifies a MerchantReturnPolicy that may be applicable. */
	'hasMerchantReturnPolicy': { termType: 'NamedNode', value: 'https://schema.org/hasMerchantReturnPolicy', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/hasMerchantReturnPolicy' } as NamedNode,
	/** Molecular function performed by this BioChemEntity; please use PropertyValue if you want to include any evidence. */
	'hasMolecularFunction': { termType: 'NamedNode', value: 'https://schema.org/hasMolecularFunction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/hasMolecularFunction' } as NamedNode,
	/** The Person's occupation. For past professions, use Role for expressing dates. */
	'hasOccupation': { termType: 'NamedNode', value: 'https://schema.org/hasOccupation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/hasOccupation' } as NamedNode,
	/** Indicates an OfferCatalog listing for this Organization, Person, or Service. */
	'hasOfferCatalog': { termType: 'NamedNode', value: 'https://schema.org/hasOfferCatalog', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/hasOfferCatalog' } as NamedNode,
	/** Points-of-Sales operated by the organization or person. */
	'hasPOS': { termType: 'NamedNode', value: 'https://schema.org/hasPOS', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/hasPOS' } as NamedNode,
	/** Indicates an item or CreativeWork that is part of this item, or CreativeWork (in some sense). */
	'hasPart': { termType: 'NamedNode', value: 'https://schema.org/hasPart', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/hasPart' } as NamedNode,
	/** A common representation such as a protein sequence or chemical structure for this entity. For images use schema.org/image. */
	'hasRepresentation': { termType: 'NamedNode', value: 'https://schema.org/hasRepresentation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/hasRepresentation' } as NamedNode,
	/** Indicates a [[Product]] that is a member of this [[ProductGroup]] (or [[ProductModel]]). */
	'hasVariant': { termType: 'NamedNode', value: 'https://schema.org/hasVariant', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/hasVariant' } as NamedNode,
	/** Headline of the article. */
	'headline': { termType: 'NamedNode', value: 'https://schema.org/headline', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/headline' } as NamedNode,
	/** Specifying the health condition(s) of a patient, medical study, or other target audience. */
	'healthCondition': { termType: 'NamedNode', value: 'https://schema.org/healthCondition', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/healthCondition' } as NamedNode,
	/** Whether the coinsurance applies before or after deductible, etc. TODO: Is this a closed set? */
	'healthPlanCoinsuranceOption': { termType: 'NamedNode', value: 'https://schema.org/healthPlanCoinsuranceOption', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/healthPlanCoinsuranceOption' } as NamedNode,
	/** The rate of coinsurance expressed as a number between 0.0 and 1.0. */
	'healthPlanCoinsuranceRate': { termType: 'NamedNode', value: 'https://schema.org/healthPlanCoinsuranceRate', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/healthPlanCoinsuranceRate' } as NamedNode,
	/** The copay amount. */
	'healthPlanCopay': { termType: 'NamedNode', value: 'https://schema.org/healthPlanCopay', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/healthPlanCopay' } as NamedNode,
	/** Whether the copay is before or after deductible, etc. TODO: Is this a closed set? */
	'healthPlanCopayOption': { termType: 'NamedNode', value: 'https://schema.org/healthPlanCopayOption', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/healthPlanCopayOption' } as NamedNode,
	/** The costs to the patient for services under this network or formulary. */
	'healthPlanCostSharing': { termType: 'NamedNode', value: 'https://schema.org/healthPlanCostSharing', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/healthPlanCostSharing' } as NamedNode,
	/** TODO. */
	'healthPlanDrugOption': { termType: 'NamedNode', value: 'https://schema.org/healthPlanDrugOption', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/healthPlanDrugOption' } as NamedNode,
	/** The tier(s) of drugs offered by this formulary or insurance plan. */
	'healthPlanDrugTier': { termType: 'NamedNode', value: 'https://schema.org/healthPlanDrugTier', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/healthPlanDrugTier' } as NamedNode,
	/** The 14-character, HIOS-generated Plan ID number. (Plan IDs must be unique, even across different markets.) */
	'healthPlanId': { termType: 'NamedNode', value: 'https://schema.org/healthPlanId', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/healthPlanId' } as NamedNode,
	/** The URL that goes directly to the plan brochure for the specific standard plan or plan variation. */
	'healthPlanMarketingUrl': { termType: 'NamedNode', value: 'https://schema.org/healthPlanMarketingUrl', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/healthPlanMarketingUrl' } as NamedNode,
	/** Name or unique ID of network. (Networks are often reused across different insurance plans.) */
	'healthPlanNetworkId': { termType: 'NamedNode', value: 'https://schema.org/healthPlanNetworkId', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/healthPlanNetworkId' } as NamedNode,
	/** The tier(s) for this network. */
	'healthPlanNetworkTier': { termType: 'NamedNode', value: 'https://schema.org/healthPlanNetworkTier', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/healthPlanNetworkTier' } as NamedNode,
	/** The category or type of pharmacy associated with this cost sharing. */
	'healthPlanPharmacyCategory': { termType: 'NamedNode', value: 'https://schema.org/healthPlanPharmacyCategory', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/healthPlanPharmacyCategory' } as NamedNode,
	/** Indicates data describing a hospital, e.g. a CDC [[CDCPMDRecord]] or as some kind of [[Dataset]]. */
	'healthcareReportingData': { termType: 'NamedNode', value: 'https://schema.org/healthcareReportingData', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/healthcareReportingData' } as NamedNode,
	/** The height of the item. */
	'height': { termType: 'NamedNode', value: 'https://schema.org/height', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/height' } as NamedNode,
	/** The highest price of all offers available.\n\nUsage guidelines:\n\n* Use values from 0123456789 (Unicode 'DIGIT ZERO' (U+0030) to 'DIGIT NINE' (U+0039)) rather than superficially similar Unicode symbols.\n* Use '.' (Unicode 'FULL STOP' (U+002E)) rather than ',' to indicate a decimal point. Avoid using these symbols as a readability separator. */
	'highPrice': { termType: 'NamedNode', value: 'https://schema.org/highPrice', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/highPrice' } as NamedNode,
	/** Organization or Person offering the job position. */
	'hiringOrganization': { termType: 'NamedNode', value: 'https://schema.org/hiringOrganization', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/hiringOrganization' } as NamedNode,
	/** [[ArchiveOrganization]] that holds, keeps or maintains the [[ArchiveComponent]]. */
	'holdingArchive': { termType: 'NamedNode', value: 'https://schema.org/holdingArchive', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/holdingArchive' } as NamedNode,
	/** A contact location for a person's residence. */
	'homeLocation': { termType: 'NamedNode', value: 'https://schema.org/homeLocation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/homeLocation' } as NamedNode,
	/** The home team in a sports event. */
	'homeTeam': { termType: 'NamedNode', value: 'https://schema.org/homeTeam', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/homeTeam' } as NamedNode,
	/** An honorific prefix preceding a Person's name such as Dr/Mrs/Mr. */
	'honorificPrefix': { termType: 'NamedNode', value: 'https://schema.org/honorificPrefix', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/honorificPrefix' } as NamedNode,
	/** An honorific suffix following a Person's name such as M.D./PhD/MSCSW. */
	'honorificSuffix': { termType: 'NamedNode', value: 'https://schema.org/honorificSuffix', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/honorificSuffix' } as NamedNode,
	/** A hospital with which the physician or office is affiliated. */
	'hospitalAffiliation': { termType: 'NamedNode', value: 'https://schema.org/hospitalAffiliation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/hospitalAffiliation' } as NamedNode,
	/** The organization (airline, travelers' club, etc.) the membership is made with. */
	'hostingOrganization': { termType: 'NamedNode', value: 'https://schema.org/hostingOrganization', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/hostingOrganization' } as NamedNode,
	/** The hours during which this service or contact is available. */
	'hoursAvailable': { termType: 'NamedNode', value: 'https://schema.org/hoursAvailable', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/hoursAvailable' } as NamedNode,
	/** How the procedure is performed. */
	'howPerformed': { termType: 'NamedNode', value: 'https://schema.org/howPerformed', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/howPerformed' } as NamedNode,
	/** An HTTP method that specifies the appropriate HTTP method for a request to an HTTP EntryPoint. Values are capitalized strings as used in HTTP. */
	'httpMethod': { termType: 'NamedNode', value: 'https://schema.org/httpMethod', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/httpMethod' } as NamedNode,
	/** IATA identifier for an airline or airport. */
	'iataCode': { termType: 'NamedNode', value: 'https://schema.org/iataCode', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/iataCode' } as NamedNode,
	/** ICAO identifier for an airport. */
	'icaoCode': { termType: 'NamedNode', value: 'https://schema.org/icaoCode', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/icaoCode' } as NamedNode,
	/** The identifier property represents any kind of identifier for any kind of [[Thing]], such as ISBNs, GTIN codes, UUIDs etc. Schema.org provides dedicated properties for representing many of these, either as textual strings or as URL (URI) links. See [background notes](/docs/datamodel.html#identifierBg) for more details.
         */
	'identifier': { termType: 'NamedNode', value: 'https://schema.org/identifier', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/identifier' } as NamedNode,
	/** A physical examination that can identify this sign. */
	'identifyingExam': { termType: 'NamedNode', value: 'https://schema.org/identifyingExam', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/identifyingExam' } as NamedNode,
	/** A diagnostic test that can identify this sign. */
	'identifyingTest': { termType: 'NamedNode', value: 'https://schema.org/identifyingTest', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/identifyingTest' } as NamedNode,
	/** The illustrator of the book. */
	'illustrator': { termType: 'NamedNode', value: 'https://schema.org/illustrator', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/illustrator' } as NamedNode,
	/** An image of the item. This can be a [[URL]] or a fully described [[ImageObject]]. */
	'image': { termType: 'NamedNode', value: 'https://schema.org/image', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/image' } as NamedNode,
	/** Imaging technique used. */
	'imagingTechnique': { termType: 'NamedNode', value: 'https://schema.org/imagingTechnique', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/imagingTechnique' } as NamedNode,
	/** The album to which this recording belongs. */
	'inAlbum': { termType: 'NamedNode', value: 'https://schema.org/inAlbum', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/inAlbum' } as NamedNode,
	/** The CableOrSatelliteService offering the channel. */
	'inBroadcastLineup': { termType: 'NamedNode', value: 'https://schema.org/inBroadcastLineup', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/inBroadcastLineup' } as NamedNode,
	/** Non-proprietary identifier for molecular entity that can be used in printed and electronic data sources thus enabling easier linking of diverse data compilations. */
	'inChI': { termType: 'NamedNode', value: 'https://schema.org/inChI', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/inChI' } as NamedNode,
	/** InChIKey is a hashed version of the full InChI (using the SHA-256 algorithm). */
	'inChIKey': { termType: 'NamedNode', value: 'https://schema.org/inChIKey', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/inChIKey' } as NamedNode,
	/** A [[CategoryCodeSet]] that contains this category code. */
	'inCodeSet': { termType: 'NamedNode', value: 'https://schema.org/inCodeSet', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/inCodeSet' } as NamedNode,
	/** A [[DefinedTermSet]] that contains this term. */
	'inDefinedTermSet': { termType: 'NamedNode', value: 'https://schema.org/inDefinedTermSet', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/inDefinedTermSet' } as NamedNode,
	/** The language of the content or performance or used in an action. Please use one of the language codes from the [IETF BCP 47 standard](http://tools.ietf.org/html/bcp47). See also [[availableLanguage]]. */
	'inLanguage': { termType: 'NamedNode', value: 'https://schema.org/inLanguage', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/inLanguage' } as NamedNode,
	/** The playlist to which this recording belongs. */
	'inPlaylist': { termType: 'NamedNode', value: 'https://schema.org/inPlaylist', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/inPlaylist' } as NamedNode,
	/** Indicates the [[productGroupID]] for a [[ProductGroup]] that this product [[isVariantOf]].  */
	'inProductGroupWithID': { termType: 'NamedNode', value: 'https://schema.org/inProductGroupWithID', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/inProductGroupWithID' } as NamedNode,
	/** Are in-store returns offered? (For more advanced return methods use the [[returnMethod]] property.) */
	'inStoreReturnsOffered': { termType: 'NamedNode', value: 'https://schema.org/inStoreReturnsOffered', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/inStoreReturnsOffered' } as NamedNode,
	/** Qualification, candidature, degree, application that Thesis supports. */
	'inSupportOf': { termType: 'NamedNode', value: 'https://schema.org/inSupportOf', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/inSupportOf' } as NamedNode,
	/** Description of bonus and commission compensation aspects of the job. */
	'incentiveCompensation': { termType: 'NamedNode', value: 'https://schema.org/incentiveCompensation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/incentiveCompensation' } as NamedNode,
	/** Description of bonus and commission compensation aspects of the job. */
	'incentives': { termType: 'NamedNode', value: 'https://schema.org/incentives', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/incentives' } as NamedNode,
	/** Smaller compositions included in this work (e.g. a movement in a symphony). */
	'includedComposition': { termType: 'NamedNode', value: 'https://schema.org/includedComposition', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/includedComposition' } as NamedNode,
	/** A data catalog which contains this dataset (this property was previously 'catalog', preferred name is now 'includedInDataCatalog'). */
	'includedDataCatalog': { termType: 'NamedNode', value: 'https://schema.org/includedDataCatalog', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/includedDataCatalog' } as NamedNode,
	/** A data catalog which contains this dataset. */
	'includedInDataCatalog': { termType: 'NamedNode', value: 'https://schema.org/includedInDataCatalog', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/includedInDataCatalog' } as NamedNode,
	/** The insurance plans that cover this drug. */
	'includedInHealthInsurancePlan': { termType: 'NamedNode', value: 'https://schema.org/includedInHealthInsurancePlan', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/includedInHealthInsurancePlan' } as NamedNode,
	/** A modifiable or non-modifiable risk factor included in the calculation, e.g. age, coexisting condition. */
	'includedRiskFactor': { termType: 'NamedNode', value: 'https://schema.org/includedRiskFactor', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/includedRiskFactor' } as NamedNode,
	/** Attraction located at destination. */
	'includesAttraction': { termType: 'NamedNode', value: 'https://schema.org/includesAttraction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/includesAttraction' } as NamedNode,
	/** Formularies covered by this plan. */
	'includesHealthPlanFormulary': { termType: 'NamedNode', value: 'https://schema.org/includesHealthPlanFormulary', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/includesHealthPlanFormulary' } as NamedNode,
	/** Networks covered by this plan. */
	'includesHealthPlanNetwork': { termType: 'NamedNode', value: 'https://schema.org/includesHealthPlanNetwork', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/includesHealthPlanNetwork' } as NamedNode,
	/** This links to a node or nodes indicating the exact quantity of the products included in  an [[Offer]] or [[ProductCollection]]. */
	'includesObject': { termType: 'NamedNode', value: 'https://schema.org/includesObject', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/includesObject' } as NamedNode,
	/** The condition, complication, etc. influenced by this factor. */
	'increasesRiskOf': { termType: 'NamedNode', value: 'https://schema.org/increasesRiskOf', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/increasesRiskOf' } as NamedNode,
	/** The industry associated with the job position. */
	'industry': { termType: 'NamedNode', value: 'https://schema.org/industry', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/industry' } as NamedNode,
	/** The ISO 3166-1 (ISO 3166-1 alpha-2) or ISO 3166-2 code, the place, or the GeoShape for the geo-political region(s) for which the offer or delivery charge specification is not valid, e.g. a region where the transaction is not allowed.\n\nSee also [[eligibleRegion]].
       */
	'ineligibleRegion': { termType: 'NamedNode', value: 'https://schema.org/ineligibleRegion', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ineligibleRegion' } as NamedNode,
	/** The actual infectious agent, such as a specific bacterium. */
	'infectiousAgent': { termType: 'NamedNode', value: 'https://schema.org/infectiousAgent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/infectiousAgent' } as NamedNode,
	/** The class of infectious agent (bacteria, prion, etc.) that causes the disease. */
	'infectiousAgentClass': { termType: 'NamedNode', value: 'https://schema.org/infectiousAgentClass', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/infectiousAgentClass' } as NamedNode,
	/** A single ingredient used in the recipe, e.g. sugar, flour or garlic. */
	'ingredients': { termType: 'NamedNode', value: 'https://schema.org/ingredients', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ingredients' } as NamedNode,
	/** The individual who traces over the pencil drawings in ink after pencils are complete. */
	'inker': { termType: 'NamedNode', value: 'https://schema.org/inker', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/inker' } as NamedNode,
	/** The place of attachment of a muscle, or what the muscle moves. */
	'insertion': { termType: 'NamedNode', value: 'https://schema.org/insertion', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/insertion' } as NamedNode,
	/** URL at which the app may be installed, if different from the URL of the item. */
	'installUrl': { termType: 'NamedNode', value: 'https://schema.org/installUrl', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/installUrl' } as NamedNode,
	/** A person assigned to instruct or provide instructional assistance for the [[CourseInstance]]. */
	'instructor': { termType: 'NamedNode', value: 'https://schema.org/instructor', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/instructor' } as NamedNode,
	/** The object that helped the agent perform the action. E.g. John wrote a book with *a pen*. */
	'instrument': { termType: 'NamedNode', value: 'https://schema.org/instrument', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/instrument' } as NamedNode,
	/** Quantitative measure gauging the degree of force involved in the exercise, for example, heartbeats per minute. May include the velocity of the movement. */
	'intensity': { termType: 'NamedNode', value: 'https://schema.org/intensity', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/intensity' } as NamedNode,
	/** Another drug that is known to interact with this drug in a way that impacts the effect of this drug or causes a risk to the patient. Note: disease interactions are typically captured as contraindications. */
	'interactingDrug': { termType: 'NamedNode', value: 'https://schema.org/interactingDrug', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/interactingDrug' } as NamedNode,
	/** This property is deprecated, alongside the UserInteraction types on which it depended. */
	'interactionCount': { termType: 'NamedNode', value: 'https://schema.org/interactionCount', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/interactionCount' } as NamedNode,
	/** The WebSite or SoftwareApplication where the interactions took place. */
	'interactionService': { termType: 'NamedNode', value: 'https://schema.org/interactionService', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/interactionService' } as NamedNode,
	/** The number of interactions for the CreativeWork using the WebSite or SoftwareApplication. The most specific child type of InteractionCounter should be used. */
	'interactionStatistic': { termType: 'NamedNode', value: 'https://schema.org/interactionStatistic', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/interactionStatistic' } as NamedNode,
	/** The Action representing the type of interaction. For up votes, +1s, etc. use [[LikeAction]]. For down votes use [[DislikeAction]]. Otherwise, use the most specific Action. */
	'interactionType': { termType: 'NamedNode', value: 'https://schema.org/interactionType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/interactionType' } as NamedNode,
	/** The predominant mode of learning supported by the learning resource. Acceptable values are 'active', 'expositive', or 'mixed'. */
	'interactivityType': { termType: 'NamedNode', value: 'https://schema.org/interactivityType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/interactivityType' } as NamedNode,
	/** The interest rate, charged or paid, applicable to the financial product. Note: This is different from the calculated annualPercentageRate. */
	'interestRate': { termType: 'NamedNode', value: 'https://schema.org/interestRate', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/interestRate' } as NamedNode,
	/** Used to indicate a specific claim contained, implied, translated or refined from the content of a [[MediaObject]] or other [[CreativeWork]]. The interpreting party can be indicated using [[claimInterpreter]]. */
	'interpretedAsClaim': { termType: 'NamedNode', value: 'https://schema.org/interpretedAsClaim', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/interpretedAsClaim' } as NamedNode,
	/** The current approximate inventory level for the item or items. */
	'inventoryLevel': { termType: 'NamedNode', value: 'https://schema.org/inventoryLevel', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/inventoryLevel' } as NamedNode,
	/** Relates a property to a property that is its inverse. Inverse properties relate the same pairs of items to each other, but in reversed direction. For example, the 'alumni' and 'alumniOf' properties are inverseOf each other. Some properties don't have explicit inverses; in these situations RDFa and JSON-LD syntax for reverse properties can be used. */
	'inverseOf': { termType: 'NamedNode', value: 'https://schema.org/inverseOf', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/inverseOf' } as NamedNode,
	/** Whether the provider is accepting new patients. */
	'isAcceptingNewPatients': { termType: 'NamedNode', value: 'https://schema.org/isAcceptingNewPatients', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/isAcceptingNewPatients' } as NamedNode,
	/** A flag to signal that the item, event, or place is accessible for free. */
	'isAccessibleForFree': { termType: 'NamedNode', value: 'https://schema.org/isAccessibleForFree', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/isAccessibleForFree' } as NamedNode,
	/** A pointer to another product (or multiple products) for which this product is an accessory or spare part. */
	'isAccessoryOrSparePartFor': { termType: 'NamedNode', value: 'https://schema.org/isAccessoryOrSparePartFor', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/isAccessoryOrSparePartFor' } as NamedNode,
	/** True if the drug is available in a generic form (regardless of name). */
	'isAvailableGenerically': { termType: 'NamedNode', value: 'https://schema.org/isAvailableGenerically', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/isAvailableGenerically' } as NamedNode,
	/** A resource from which this work is derived or from which it is a modification or adaptation. */
	'isBasedOn': { termType: 'NamedNode', value: 'https://schema.org/isBasedOn', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/isBasedOn' } as NamedNode,
	/** A resource that was used in the creation of this resource. This term can be repeated for multiple sources. For example, http://example.com/great-multiplication-intro.html. */
	'isBasedOnUrl': { termType: 'NamedNode', value: 'https://schema.org/isBasedOnUrl', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/isBasedOnUrl' } as NamedNode,
	/** A pointer to another product (or multiple products) for which this product is a consumable. */
	'isConsumableFor': { termType: 'NamedNode', value: 'https://schema.org/isConsumableFor', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/isConsumableFor' } as NamedNode,
	/** Another BioChemEntity encoding by this one. */
	'isEncodedByBioChemEntity': { termType: 'NamedNode', value: 'https://schema.org/isEncodedByBioChemEntity', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/isEncodedByBioChemEntity' } as NamedNode,
	/** Indicates whether this content is family friendly. */
	'isFamilyFriendly': { termType: 'NamedNode', value: 'https://schema.org/isFamilyFriendly', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/isFamilyFriendly' } as NamedNode,
	/** Indicates whether the offer was accepted as a gift for someone other than the buyer. */
	'isGift': { termType: 'NamedNode', value: 'https://schema.org/isGift', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/isGift' } as NamedNode,
	/** Biological process this BioChemEntity is involved in; please use PropertyValue if you want to include any evidence. */
	'isInvolvedInBiologicalProcess': { termType: 'NamedNode', value: 'https://schema.org/isInvolvedInBiologicalProcess', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/isInvolvedInBiologicalProcess' } as NamedNode,
	/** True if the broadcast is of a live event. */
	'isLiveBroadcast': { termType: 'NamedNode', value: 'https://schema.org/isLiveBroadcast', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/isLiveBroadcast' } as NamedNode,
	/** Subcellular location where this BioChemEntity is located; please use PropertyValue if you want to include any evidence. */
	'isLocatedInSubcellularLocation': { termType: 'NamedNode', value: 'https://schema.org/isLocatedInSubcellularLocation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/isLocatedInSubcellularLocation' } as NamedNode,
	/** Indicates an item or CreativeWork that this item, or CreativeWork (in some sense), is part of. */
	'isPartOf': { termType: 'NamedNode', value: 'https://schema.org/isPartOf', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/isPartOf' } as NamedNode,
	/** Indicates a BioChemEntity that is (in some sense) a part of this BioChemEntity.  */
	'isPartOfBioChemEntity': { termType: 'NamedNode', value: 'https://schema.org/isPartOfBioChemEntity', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/isPartOfBioChemEntity' } as NamedNode,
	/** Indicates some accommodation that this floor plan describes. */
	'isPlanForApartment': { termType: 'NamedNode', value: 'https://schema.org/isPlanForApartment', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/isPlanForApartment' } as NamedNode,
	/** True if this item's name is a proprietary/brand name (vs. generic name). */
	'isProprietary': { termType: 'NamedNode', value: 'https://schema.org/isProprietary', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/isProprietary' } as NamedNode,
	/** A pointer to another, somehow related product (or multiple products). */
	'isRelatedTo': { termType: 'NamedNode', value: 'https://schema.org/isRelatedTo', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/isRelatedTo' } as NamedNode,
	/** Whether the 3DModel allows resizing. For example, room layout applications often do not allow 3DModel elements to be resized to reflect reality. */
	'isResizable': { termType: 'NamedNode', value: 'https://schema.org/isResizable', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/isResizable' } as NamedNode,
	/** A pointer to another, functionally similar product (or multiple products). */
	'isSimilarTo': { termType: 'NamedNode', value: 'https://schema.org/isSimilarTo', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/isSimilarTo' } as NamedNode,
	/** This can be marked 'true' to indicate that some published [[DeliveryTimeSettings]] or [[ShippingRateSettings]] are intended to apply to all [[OfferShippingDetails]] published by the same merchant, when referenced by a [[shippingSettingsLink]] in those settings. It is not meaningful to use a 'true' value for this property alongside a transitTimeLabel (for [[DeliveryTimeSettings]]) or shippingLabel (for [[ShippingRateSettings]]), since this property is for use with unlabelled settings. */
	'isUnlabelledFallback': { termType: 'NamedNode', value: 'https://schema.org/isUnlabelledFallback', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/isUnlabelledFallback' } as NamedNode,
	/** Indicates the kind of product that this is a variant of. In the case of [[ProductModel]], this is a pointer (from a ProductModel) to a base product from which this product is a variant. It is safe to infer that the variant inherits all product features from the base model, unless defined locally. This is not transitive. In the case of a [[ProductGroup]], the group description also serves as a template, representing a set of Products that vary on explicitly defined, specific dimensions only (so it defines both a set of variants, as well as which values distinguish amongst those variants). When used with [[ProductGroup]], this property can apply to any [[Product]] included in the group. */
	'isVariantOf': { termType: 'NamedNode', value: 'https://schema.org/isVariantOf', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/isVariantOf' } as NamedNode,
	/** The ISBN of the book. */
	'isbn': { termType: 'NamedNode', value: 'https://schema.org/isbn', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/isbn' } as NamedNode,
	/** The International Standard of Industrial Classification of All Economic Activities (ISIC), Revision 4 code for a particular organization, business person, or place. */
	'isicV4': { termType: 'NamedNode', value: 'https://schema.org/isicV4', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/isicV4' } as NamedNode,
	/** An organization identifier as defined in ISO 6523(-1). Note that many existing organization identifiers such as [leiCode](https://schema.org/leiCode), [duns](https://schema.org/duns) and [vatID](https://schema.org/vatID) can be expressed as an ISO 6523 identifier by setting the ICD part of the ISO 6523 identifier accordingly.  */
	'iso6523Code': { termType: 'NamedNode', value: 'https://schema.org/iso6523Code', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/iso6523Code' } as NamedNode,
	/** The International Standard Recording Code for the recording. */
	'isrcCode': { termType: 'NamedNode', value: 'https://schema.org/isrcCode', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/isrcCode' } as NamedNode,
	/** The International Standard Serial Number (ISSN) that identifies this serial publication. You can repeat this property to identify different formats of, or the linking ISSN (ISSN-L) for, this serial publication. */
	'issn': { termType: 'NamedNode', value: 'https://schema.org/issn', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/issn' } as NamedNode,
	/** Identifies the issue of publication; for example, "iii" or "2". */
	'issueNumber': { termType: 'NamedNode', value: 'https://schema.org/issueNumber', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/issueNumber' } as NamedNode,
	/** The organization issuing the ticket or permit. */
	'issuedBy': { termType: 'NamedNode', value: 'https://schema.org/issuedBy', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/issuedBy' } as NamedNode,
	/** The service through which the permit was granted. */
	'issuedThrough': { termType: 'NamedNode', value: 'https://schema.org/issuedThrough', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/issuedThrough' } as NamedNode,
	/** The International Standard Musical Work Code for the composition. */
	'iswcCode': { termType: 'NamedNode', value: 'https://schema.org/iswcCode', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/iswcCode' } as NamedNode,
	/** An entity represented by an entry in a list or data feed (e.g. an 'artist' in a list of 'artists'). */
	'item': { termType: 'NamedNode', value: 'https://schema.org/item', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/item' } as NamedNode,
	/** A predefined value from OfferItemCondition specifying the condition of the product or service, or the products or services included in the offer. Also used for product return policies to specify the condition of products accepted for returns. */
	'itemCondition': { termType: 'NamedNode', value: 'https://schema.org/itemCondition', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/itemCondition' } as NamedNode,
	/** The type of return fees for returns of defect products. */
	'itemDefectReturnFees': { termType: 'NamedNode', value: 'https://schema.org/itemDefectReturnFees', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/itemDefectReturnFees' } as NamedNode,
	/** The method (from an enumeration) by which the customer obtains a return shipping label for a defect product. */
	'itemDefectReturnLabelSource': { termType: 'NamedNode', value: 'https://schema.org/itemDefectReturnLabelSource', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/itemDefectReturnLabelSource' } as NamedNode,
	/** Amount of shipping costs for defect product returns. Applicable when property [[itemDefectReturnFees]] equals [[ReturnShippingFees]]. */
	'itemDefectReturnShippingFeesAmount': { termType: 'NamedNode', value: 'https://schema.org/itemDefectReturnShippingFeesAmount', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/itemDefectReturnShippingFeesAmount' } as NamedNode,
	/** For itemListElement values, you can use simple strings (e.g. "Peter", "Paul", "Mary"), existing entities, or use ListItem.\n\nText values are best if the elements in the list are plain strings. Existing entities are best for a simple, unordered list of existing things in your data. ListItem is used with ordered lists when you want to provide additional context about the element in that list or when the same item might be in different places in different lists.\n\nNote: The order of elements in your mark-up is not sufficient for indicating the order or elements.  Use ListItem with a 'position' property in such cases. */
	'itemListElement': { termType: 'NamedNode', value: 'https://schema.org/itemListElement', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/itemListElement' } as NamedNode,
	/** Type of ordering (e.g. Ascending, Descending, Unordered). */
	'itemListOrder': { termType: 'NamedNode', value: 'https://schema.org/itemListOrder', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/itemListOrder' } as NamedNode,
	/** Current location of the item. */
	'itemLocation': { termType: 'NamedNode', value: 'https://schema.org/itemLocation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/itemLocation' } as NamedNode,
	/** An item being offered (or demanded). The transactional nature of the offer or demand is documented using [[businessFunction]], e.g. sell, lease etc. While several common expected types are listed explicitly in this definition, others can be used. Using a second type, such as Product or a subtype of Product, can clarify the nature of the offer. */
	'itemOffered': { termType: 'NamedNode', value: 'https://schema.org/itemOffered', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/itemOffered' } as NamedNode,
	/** The item that is being reviewed/rated. */
	'itemReviewed': { termType: 'NamedNode', value: 'https://schema.org/itemReviewed', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/itemReviewed' } as NamedNode,
	/** Item(s) being shipped. */
	'itemShipped': { termType: 'NamedNode', value: 'https://schema.org/itemShipped', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/itemShipped' } as NamedNode,
	/** Destination(s) ( [[Place]] ) that make up a trip. For a trip where destination order is important use [[ItemList]] to specify that order (see examples). */
	'itinerary': { termType: 'NamedNode', value: 'https://schema.org/itinerary', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/itinerary' } as NamedNode,
	/** Systematic method of naming chemical compounds as recommended by the International Union of Pure and Applied Chemistry (IUPAC). */
	'iupacName': { termType: 'NamedNode', value: 'https://schema.org/iupacName', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/iupacName' } as NamedNode,
	/** Description of benefits associated with the job. */
	'jobBenefits': { termType: 'NamedNode', value: 'https://schema.org/jobBenefits', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/jobBenefits' } as NamedNode,
	/** An indicator as to whether a position is available for an immediate start. */
	'jobImmediateStart': { termType: 'NamedNode', value: 'https://schema.org/jobImmediateStart', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/jobImmediateStart' } as NamedNode,
	/** A (typically single) geographic location associated with the job position. */
	'jobLocation': { termType: 'NamedNode', value: 'https://schema.org/jobLocation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/jobLocation' } as NamedNode,
	/** A description of the job location (e.g. TELECOMMUTE for telecommute jobs). */
	'jobLocationType': { termType: 'NamedNode', value: 'https://schema.org/jobLocationType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/jobLocationType' } as NamedNode,
	/** The date on which a successful applicant for this job would be expected to start work. Choose a specific date in the future or use the jobImmediateStart property to indicate the position is to be filled as soon as possible. */
	'jobStartDate': { termType: 'NamedNode', value: 'https://schema.org/jobStartDate', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/jobStartDate' } as NamedNode,
	/** The job title of the person (for example, Financial Manager). */
	'jobTitle': { termType: 'NamedNode', value: 'https://schema.org/jobTitle', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/jobTitle' } as NamedNode,
	/** Indicates a legal jurisdiction, e.g. of some legislation, or where some government service is based. */
	'jurisdiction': { termType: 'NamedNode', value: 'https://schema.org/jurisdiction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/jurisdiction' } as NamedNode,
	/** Keywords or tags used to describe some item. Multiple textual entries in a keywords list are typically delimited by commas, or by repeating the property. */
	'keywords': { termType: 'NamedNode', value: 'https://schema.org/keywords', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/keywords' } as NamedNode,
	/** A textual description of known damages, both repaired and unrepaired. */
	'knownVehicleDamages': { termType: 'NamedNode', value: 'https://schema.org/knownVehicleDamages', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/knownVehicleDamages' } as NamedNode,
	/** The most generic bi-directional social/work relation. */
	'knows': { termType: 'NamedNode', value: 'https://schema.org/knows', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/knows' } as NamedNode,
	/** Of a [[Person]], and less typically of an [[Organization]], to indicate a topic that is known about - suggesting possible expertise but not implying it. We do not distinguish skill levels here, or relate this to educational content, events, objectives or [[JobPosting]] descriptions. */
	'knowsAbout': { termType: 'NamedNode', value: 'https://schema.org/knowsAbout', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/knowsAbout' } as NamedNode,
	/** Of a [[Person]], and less typically of an [[Organization]], to indicate a known language. We do not distinguish skill levels or reading/writing/speaking/signing here. Use language codes from the [IETF BCP 47 standard](http://tools.ietf.org/html/bcp47). */
	'knowsLanguage': { termType: 'NamedNode', value: 'https://schema.org/knowsLanguage', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/knowsLanguage' } as NamedNode,
	/** Link to the drug's label details. */
	'labelDetails': { termType: 'NamedNode', value: 'https://schema.org/labelDetails', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/labelDetails' } as NamedNode,
	/** A sub property of participant. The owner of the real estate property. */
	'landlord': { termType: 'NamedNode', value: 'https://schema.org/landlord', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/landlord' } as NamedNode,
	/** A sub property of instrument. The language used on this action. */
	'language': { termType: 'NamedNode', value: 'https://schema.org/language', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/language' } as NamedNode,
	/** Date on which the content on this web page was last reviewed for accuracy and/or completeness. */
	'lastReviewed': { termType: 'NamedNode', value: 'https://schema.org/lastReviewed', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/lastReviewed' } as NamedNode,
	/** The latitude of a location. For example ```37.42242``` ([WGS 84](https://en.wikipedia.org/wiki/World_Geodetic_System)). */
	'latitude': { termType: 'NamedNode', value: 'https://schema.org/latitude', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/latitude' } as NamedNode,
	/** A schematic image showing the floorplan layout. */
	'layoutImage': { termType: 'NamedNode', value: 'https://schema.org/layoutImage', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/layoutImage' } as NamedNode,
	/** The predominant type or kind characterizing the learning resource. For example, 'presentation', 'handout'. */
	'learningResourceType': { termType: 'NamedNode', value: 'https://schema.org/learningResourceType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/learningResourceType' } as NamedNode,
	/** Length of the lease for some [[Accommodation]], either particular to some [[Offer]] or in some cases intrinsic to the property. */
	'leaseLength': { termType: 'NamedNode', value: 'https://schema.org/leaseLength', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/leaseLength' } as NamedNode,
	/** The official name of the organization, e.g. the registered company name. */
	'legalName': { termType: 'NamedNode', value: 'https://schema.org/legalName', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/legalName' } as NamedNode,
	/** The drug or supplement's legal status, including any controlled substance schedules that apply. */
	'legalStatus': { termType: 'NamedNode', value: 'https://schema.org/legalStatus', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/legalStatus' } as NamedNode,
	/** Indicates that this legislation (or part of a legislation) somehow transfers another legislation in a different legislative context. This is an informative link, and it has no legal value. For legally-binding links of transposition, use the <a href="/legislationTransposes">legislationTransposes</a> property. For example an informative consolidated law of a European Union's member state "applies" the consolidated version of the European Directive implemented in it. */
	'legislationApplies': { termType: 'NamedNode', value: 'https://schema.org/legislationApplies', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/legislationApplies' } as NamedNode,
	/** Another legislation that this legislation changes. This encompasses the notions of amendment, replacement, correction, repeal, or other types of change. This may be a direct change (textual or non-textual amendment) or a consequential or indirect change. The property is to be used to express the existence of a change relationship between two acts rather than the existence of a consolidated version of the text that shows the result of the change. For consolidation relationships, use the <a href="/legislationConsolidates">legislationConsolidates</a> property. */
	'legislationChanges': { termType: 'NamedNode', value: 'https://schema.org/legislationChanges', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/legislationChanges' } as NamedNode,
	/** Indicates another legislation taken into account in this consolidated legislation (which is usually the product of an editorial process that revises the legislation). This property should be used multiple times to refer to both the original version or the previous consolidated version, and to the legislations making the change. */
	'legislationConsolidates': { termType: 'NamedNode', value: 'https://schema.org/legislationConsolidates', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/legislationConsolidates' } as NamedNode,
	/** The date of adoption or signature of the legislation. This is the date at which the text is officially aknowledged to be a legislation, even though it might not even be published or in force. */
	'legislationDate': { termType: 'NamedNode', value: 'https://schema.org/legislationDate', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/legislationDate' } as NamedNode,
	/** The point-in-time at which the provided description of the legislation is valid (e.g.: when looking at the law on the 2016-04-07 (= dateVersion), I get the consolidation of 2015-04-12 of the "National Insurance Contributions Act 2015") */
	'legislationDateVersion': { termType: 'NamedNode', value: 'https://schema.org/legislationDateVersion', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/legislationDateVersion' } as NamedNode,
	/** An identifier for the legislation. This can be either a string-based identifier, like the CELEX at EU level or the NOR in France, or a web-based, URL/URI identifier, like an ELI (European Legislation Identifier) or an URN-Lex. */
	'legislationIdentifier': { termType: 'NamedNode', value: 'https://schema.org/legislationIdentifier', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/legislationIdentifier' } as NamedNode,
	/** The jurisdiction from which the legislation originates. */
	'legislationJurisdiction': { termType: 'NamedNode', value: 'https://schema.org/legislationJurisdiction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/legislationJurisdiction' } as NamedNode,
	/** Whether the legislation is currently in force, not in force, or partially in force. */
	'legislationLegalForce': { termType: 'NamedNode', value: 'https://schema.org/legislationLegalForce', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/legislationLegalForce' } as NamedNode,
	/** The legal value of this legislation file. The same legislation can be written in multiple files with different legal values. Typically a digitally signed PDF have a "stronger" legal value than the HTML file of the same act. */
	'legislationLegalValue': { termType: 'NamedNode', value: 'https://schema.org/legislationLegalValue', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/legislationLegalValue' } as NamedNode,
	/** The person or organization that originally passed or made the law: typically parliament (for primary legislation) or government (for secondary legislation). This indicates the "legal author" of the law, as opposed to its physical author. */
	'legislationPassedBy': { termType: 'NamedNode', value: 'https://schema.org/legislationPassedBy', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/legislationPassedBy' } as NamedNode,
	/** An individual or organization that has some kind of responsibility for the legislation. Typically the ministry who is/was in charge of elaborating the legislation, or the adressee for potential questions about the legislation once it is published. */
	'legislationResponsible': { termType: 'NamedNode', value: 'https://schema.org/legislationResponsible', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/legislationResponsible' } as NamedNode,
	/** Indicates that this legislation (or part of legislation) fulfills the objectives set by another legislation, by passing appropriate implementation measures. Typically, some legislations of European Union's member states or regions transpose European Directives. This indicates a legally binding link between the 2 legislations. */
	'legislationTransposes': { termType: 'NamedNode', value: 'https://schema.org/legislationTransposes', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/legislationTransposes' } as NamedNode,
	/** The type of the legislation. Examples of values are "law", "act", "directive", "decree", "regulation", "statutory instrument", "loi organique", "règlement grand-ducal", etc., depending on the country. */
	'legislationType': { termType: 'NamedNode', value: 'https://schema.org/legislationType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/legislationType' } as NamedNode,
	/** An organization identifier that uniquely identifies a legal entity as defined in ISO 17442. */
	'leiCode': { termType: 'NamedNode', value: 'https://schema.org/leiCode', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/leiCode' } as NamedNode,
	/** A sub property of participant. The person that lends the object being borrowed. */
	'lender': { termType: 'NamedNode', value: 'https://schema.org/lender', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/lender' } as NamedNode,
	/** This ordering relation for qualitative values indicates that the subject is lesser than the object. */
	'lesser': { termType: 'NamedNode', value: 'https://schema.org/lesser', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/lesser' } as NamedNode,
	/** This ordering relation for qualitative values indicates that the subject is lesser than or equal to the object. */
	'lesserOrEqual': { termType: 'NamedNode', value: 'https://schema.org/lesserOrEqual', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/lesserOrEqual' } as NamedNode,
	/** The individual who adds lettering, including speech balloons and sound effects, to artwork. */
	'letterer': { termType: 'NamedNode', value: 'https://schema.org/letterer', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/letterer' } as NamedNode,
	/** A license document that applies to this content, typically indicated by URL. */
	'license': { termType: 'NamedNode', value: 'https://schema.org/license', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/license' } as NamedNode,
	/** A line is a point-to-point path consisting of two or more points. A line is expressed as a series of two or more point objects separated by space. */
	'line': { termType: 'NamedNode', value: 'https://schema.org/line', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/line' } as NamedNode,
	/** Indicates the relationship type of a Web link.  */
	'linkRelationship': { termType: 'NamedNode', value: 'https://schema.org/linkRelationship', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/linkRelationship' } as NamedNode,
	/** An update to the LiveBlog. */
	'liveBlogUpdate': { termType: 'NamedNode', value: 'https://schema.org/liveBlogUpdate', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/liveBlogUpdate' } as NamedNode,
	/** Amount of mortgage mandate that can be converted into a proper mortgage at a later stage. */
	'loanMortgageMandateAmount': { termType: 'NamedNode', value: 'https://schema.org/loanMortgageMandateAmount', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/loanMortgageMandateAmount' } as NamedNode,
	/** The amount of money to pay in a single payment. */
	'loanPaymentAmount': { termType: 'NamedNode', value: 'https://schema.org/loanPaymentAmount', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/loanPaymentAmount' } as NamedNode,
	/** Frequency of payments due, i.e. number of months between payments. This is defined as a frequency, i.e. the reciprocal of a period of time. */
	'loanPaymentFrequency': { termType: 'NamedNode', value: 'https://schema.org/loanPaymentFrequency', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/loanPaymentFrequency' } as NamedNode,
	/** A form of paying back money previously borrowed from a lender. Repayment usually takes the form of periodic payments that normally include part principal plus interest in each payment. */
	'loanRepaymentForm': { termType: 'NamedNode', value: 'https://schema.org/loanRepaymentForm', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/loanRepaymentForm' } as NamedNode,
	/** The duration of the loan or credit agreement. */
	'loanTerm': { termType: 'NamedNode', value: 'https://schema.org/loanTerm', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/loanTerm' } as NamedNode,
	/** The type of a loan or credit. */
	'loanType': { termType: 'NamedNode', value: 'https://schema.org/loanType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/loanType' } as NamedNode,
	/** The location of, for example, where an event is happening, where an organization is located, or where an action takes place. */
	'location': { termType: 'NamedNode', value: 'https://schema.org/location', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/location' } as NamedNode,
	/** The location where the CreativeWork was created, which may not be the same as the location depicted in the CreativeWork. */
	'locationCreated': { termType: 'NamedNode', value: 'https://schema.org/locationCreated', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/locationCreated' } as NamedNode,
	/** A full description of the lodging unit. */
	'lodgingUnitDescription': { termType: 'NamedNode', value: 'https://schema.org/lodgingUnitDescription', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/lodgingUnitDescription' } as NamedNode,
	/** Textual description of the unit type (including suite vs. room, size of bed, etc.). */
	'lodgingUnitType': { termType: 'NamedNode', value: 'https://schema.org/lodgingUnitType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/lodgingUnitType' } as NamedNode,
	/** An associated logo. */
	'logo': { termType: 'NamedNode', value: 'https://schema.org/logo', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/logo' } as NamedNode,
	/** The longitude of a location. For example ```-122.08585``` ([WGS 84](https://en.wikipedia.org/wiki/World_Geodetic_System)). */
	'longitude': { termType: 'NamedNode', value: 'https://schema.org/longitude', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/longitude' } as NamedNode,
	/** A sub property of participant. The loser of the action. */
	'loser': { termType: 'NamedNode', value: 'https://schema.org/loser', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/loser' } as NamedNode,
	/** The lowest price of all offers available.\n\nUsage guidelines:\n\n* Use values from 0123456789 (Unicode 'DIGIT ZERO' (U+0030) to 'DIGIT NINE' (U+0039)) rather than superficially similar Unicode symbols.\n* Use '.' (Unicode 'FULL STOP' (U+002E)) rather than ',' to indicate a decimal point. Avoid using these symbols as a readability separator. */
	'lowPrice': { termType: 'NamedNode', value: 'https://schema.org/lowPrice', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/lowPrice' } as NamedNode,
	/** The person who wrote the words. */
	'lyricist': { termType: 'NamedNode', value: 'https://schema.org/lyricist', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/lyricist' } as NamedNode,
	/** The words in the song. */
	'lyrics': { termType: 'NamedNode', value: 'https://schema.org/lyrics', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/lyrics' } as NamedNode,
	/** Indicates if this web page element is the main subject of the page. */
	'mainContentOfPage': { termType: 'NamedNode', value: 'https://schema.org/mainContentOfPage', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/mainContentOfPage' } as NamedNode,
	/** Indicates the primary entity described in some page or other CreativeWork. */
	'mainEntity': { termType: 'NamedNode', value: 'https://schema.org/mainEntity', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/mainEntity' } as NamedNode,
	/** Indicates a page (or other CreativeWork) for which this thing is the main entity being described. See [background notes](/docs/datamodel.html#mainEntityBackground) for details. */
	'mainEntityOfPage': { termType: 'NamedNode', value: 'https://schema.org/mainEntityOfPage', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/mainEntityOfPage' } as NamedNode,
	/** A maintainer of a [[Dataset]], software package ([[SoftwareApplication]]), or other [[Project]]. A maintainer is a [[Person]] or [[Organization]] that manages contributions to, and/or publication of, some (typically complex) artifact. It is common for distributions of software and data to be based on "upstream" sources. When [[maintainer]] is applied to a specific version of something e.g. a particular version or packaging of a [[Dataset]], it is always  possible that the upstream source has a different maintainer. The [[isBasedOn]] property can be used to indicate such relationships between datasets to make the different maintenance roles clear. Similarly in the case of software, a package may have dedicated maintainers working on integration into software distributions such as Ubuntu, as well as upstream maintainers of the underlying work.
       */
	'maintainer': { termType: 'NamedNode', value: 'https://schema.org/maintainer', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/maintainer' } as NamedNode,
	/** A pointer to products or services offered by the organization or person. */
	'makesOffer': { termType: 'NamedNode', value: 'https://schema.org/makesOffer', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/makesOffer' } as NamedNode,
	/** The manufacturer of the product. */
	'manufacturer': { termType: 'NamedNode', value: 'https://schema.org/manufacturer', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/manufacturer' } as NamedNode,
	/** A URL to a map of the place. */
	'map': { termType: 'NamedNode', value: 'https://schema.org/map', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/map' } as NamedNode,
	/** Indicates the kind of Map, from the MapCategoryType Enumeration. */
	'mapType': { termType: 'NamedNode', value: 'https://schema.org/mapType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/mapType' } as NamedNode,
	/** A URL to a map of the place. */
	'maps': { termType: 'NamedNode', value: 'https://schema.org/maps', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/maps' } as NamedNode,
	/** A [[marginOfError]] for an [[Observation]]. */
	'marginOfError': { termType: 'NamedNode', value: 'https://schema.org/marginOfError', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/marginOfError' } as NamedNode,
	/** For a [[NewsMediaOrganization]], a link to the masthead page or a page listing top editorial management. */
	'masthead': { termType: 'NamedNode', value: 'https://schema.org/masthead', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/masthead' } as NamedNode,
	/** A material that something is made from, e.g. leather, wool, cotton, paper. */
	'material': { termType: 'NamedNode', value: 'https://schema.org/material', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/material' } as NamedNode,
	/** The quantity of the materials being described or an expression of the physical space they occupy. */
	'materialExtent': { termType: 'NamedNode', value: 'https://schema.org/materialExtent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/materialExtent' } as NamedNode,
	/** A mathematical expression (e.g. 'x^2-3x=0') that may be solved for a specific variable, simplified, or transformed. This can take many formats, e.g. LaTeX, Ascii-Math, or math as you would write with a keyboard. */
	'mathExpression': { termType: 'NamedNode', value: 'https://schema.org/mathExpression', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/mathExpression' } as NamedNode,
	/** The highest price if the price is a range. */
	'maxPrice': { termType: 'NamedNode', value: 'https://schema.org/maxPrice', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/maxPrice' } as NamedNode,
	/** The upper value of some characteristic or property. */
	'maxValue': { termType: 'NamedNode', value: 'https://schema.org/maxValue', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/maxValue' } as NamedNode,
	/** The total number of individuals that may attend an event or venue. */
	'maximumAttendeeCapacity': { termType: 'NamedNode', value: 'https://schema.org/maximumAttendeeCapacity', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/maximumAttendeeCapacity' } as NamedNode,
	/** The maximum number of students who may be enrolled in the program. */
	'maximumEnrollment': { termType: 'NamedNode', value: 'https://schema.org/maximumEnrollment', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/maximumEnrollment' } as NamedNode,
	/** Recommended intake of this supplement for a given population as defined by a specific recommending authority. */
	'maximumIntake': { termType: 'NamedNode', value: 'https://schema.org/maximumIntake', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/maximumIntake' } as NamedNode,
	/** The maximum physical attendee capacity of an [[Event]] whose [[eventAttendanceMode]] is [[OfflineEventAttendanceMode]] (or the offline aspects, in the case of a [[MixedEventAttendanceMode]]).  */
	'maximumPhysicalAttendeeCapacity': { termType: 'NamedNode', value: 'https://schema.org/maximumPhysicalAttendeeCapacity', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/maximumPhysicalAttendeeCapacity' } as NamedNode,
	/** The maximum virtual attendee capacity of an [[Event]] whose [[eventAttendanceMode]] is [[OnlineEventAttendanceMode]] (or the online aspects, in the case of a [[MixedEventAttendanceMode]]).  */
	'maximumVirtualAttendeeCapacity': { termType: 'NamedNode', value: 'https://schema.org/maximumVirtualAttendeeCapacity', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/maximumVirtualAttendeeCapacity' } as NamedNode,
	/** Description of the meals that will be provided or available for purchase. */
	'mealService': { termType: 'NamedNode', value: 'https://schema.org/mealService', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/mealService' } as NamedNode,
	/** The measuredProperty of an [[Observation]], typically via its [[StatisticalVariable]]. There are various kinds of applicable [[Property]]: a schema.org property, a property from other RDF-compatible systems, e.g. W3C RDF Data Cube, Data Commons, Wikidata, or schema.org extensions such as [GS1's](https://www.gs1.org/voc/?show=properties). */
	'measuredProperty': { termType: 'NamedNode', value: 'https://schema.org/measuredProperty', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/measuredProperty' } as NamedNode,
	/** Identifies the denominator variable when an observation represents a ratio or percentage. */
	'measurementDenominator': { termType: 'NamedNode', value: 'https://schema.org/measurementDenominator', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/measurementDenominator' } as NamedNode,
	/** A subproperty of [[measurementTechnique]] that can be used for specifying specific methods, in particular via [[MeasurementMethodEnum]]. */
	'measurementMethod': { termType: 'NamedNode', value: 'https://schema.org/measurementMethod', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/measurementMethod' } as NamedNode,
	/** Provides additional qualification to an observation. For example, a GDP observation measures the Nominal value. */
	'measurementQualifier': { termType: 'NamedNode', value: 'https://schema.org/measurementQualifier', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/measurementQualifier' } as NamedNode,
	/** A technique, method or technology used in an [[Observation]], [[StatisticalVariable]] or [[Dataset]] (or [[DataDownload]], [[DataCatalog]]), corresponding to the method used for measuring the corresponding variable(s) (for datasets, described using [[variableMeasured]]; for [[Observation]], a [[StatisticalVariable]]). Often but not necessarily each [[variableMeasured]] will have an explicit representation as (or mapping to) an property such as those defined in Schema.org, or other RDF vocabularies and "knowledge graphs". In that case the subproperty of [[variableMeasured]] called [[measuredProperty]] is applicable.
    
The [[measurementTechnique]] property helps when extra clarification is needed about how a [[measuredProperty]] was measured. This is oriented towards scientific and scholarly dataset publication but may have broader applicability; it is not intended as a full representation of measurement, but can often serve as a high level summary for dataset discovery. 

For example, if [[variableMeasured]] is: molecule concentration, [[measurementTechnique]] could be: "mass spectrometry" or "nmr spectroscopy" or "colorimetry" or "immunofluorescence". If the [[variableMeasured]] is "depression rating", the [[measurementTechnique]] could be "Zung Scale" or "HAM-D" or "Beck Depression Inventory". 

If there are several [[variableMeasured]] properties recorded for some given data object, use a [[PropertyValue]] for each [[variableMeasured]] and attach the corresponding [[measurementTechnique]]. The value can also be from an enumeration, organized as a [[MeasurementMetholdEnumeration]]. */
	'measurementTechnique': { termType: 'NamedNode', value: 'https://schema.org/measurementTechnique', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/measurementTechnique' } as NamedNode,
	/** The specific biochemical interaction through which this drug or supplement produces its pharmacological effect. */
	'mechanismOfAction': { termType: 'NamedNode', value: 'https://schema.org/mechanismOfAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/mechanismOfAction' } as NamedNode,
	/** Indicates a MediaManipulationRatingEnumeration classification of a media object (in the context of how it was published or shared). */
	'mediaAuthenticityCategory': { termType: 'NamedNode', value: 'https://schema.org/mediaAuthenticityCategory', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/mediaAuthenticityCategory' } as NamedNode,
	/** In the context of a [[MediaReview]], indicates specific media item(s) that are grouped using a [[MediaReviewItem]]. */
	'mediaItemAppearance': { termType: 'NamedNode', value: 'https://schema.org/mediaItemAppearance', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/mediaItemAppearance' } as NamedNode,
	/** The median value. */
	'median': { termType: 'NamedNode', value: 'https://schema.org/median', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/median' } as NamedNode,
	/** Medical audience for page. */
	'medicalAudience': { termType: 'NamedNode', value: 'https://schema.org/medicalAudience', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/medicalAudience' } as NamedNode,
	/** A medical specialty of the provider. */
	'medicalSpecialty': { termType: 'NamedNode', value: 'https://schema.org/medicalSpecialty', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/medicalSpecialty' } as NamedNode,
	/** The system of medicine that includes this MedicalEntity, for example 'evidence-based', 'homeopathic', 'chiropractic', etc. */
	'medicineSystem': { termType: 'NamedNode', value: 'https://schema.org/medicineSystem', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/medicineSystem' } as NamedNode,
	/** Indicates that the vehicle meets the respective emission standard. */
	'meetsEmissionStandard': { termType: 'NamedNode', value: 'https://schema.org/meetsEmissionStandard', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/meetsEmissionStandard' } as NamedNode,
	/** A member of an Organization or a ProgramMembership. Organizations can be members of organizations; ProgramMembership is typically for individuals. */
	'member': { termType: 'NamedNode', value: 'https://schema.org/member', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/member' } as NamedNode,
	/** An Organization (or ProgramMembership) to which this Person or Organization belongs. */
	'memberOf': { termType: 'NamedNode', value: 'https://schema.org/memberOf', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/memberOf' } as NamedNode,
	/** A member of this organization. */
	'members': { termType: 'NamedNode', value: 'https://schema.org/members', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/members' } as NamedNode,
	/** A unique identifier for the membership. */
	'membershipNumber': { termType: 'NamedNode', value: 'https://schema.org/membershipNumber', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/membershipNumber' } as NamedNode,
	/** The number of membership points earned by the member. If necessary, the unitText can be used to express the units the points are issued in. (E.g. stars, miles, etc.) */
	'membershipPointsEarned': { termType: 'NamedNode', value: 'https://schema.org/membershipPointsEarned', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/membershipPointsEarned' } as NamedNode,
	/** Minimum memory requirements. */
	'memoryRequirements': { termType: 'NamedNode', value: 'https://schema.org/memoryRequirements', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/memoryRequirements' } as NamedNode,
	/** Indicates that the CreativeWork contains a reference to, but is not necessarily about a concept. */
	'mentions': { termType: 'NamedNode', value: 'https://schema.org/mentions', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/mentions' } as NamedNode,
	/** Either the actual menu as a structured representation, as text, or a URL of the menu. */
	'menu': { termType: 'NamedNode', value: 'https://schema.org/menu', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/menu' } as NamedNode,
	/** Additional menu item(s) such as a side dish of salad or side order of fries that can be added to this menu item. Additionally it can be a menu section containing allowed add-on menu items for this menu item. */
	'menuAddOn': { termType: 'NamedNode', value: 'https://schema.org/menuAddOn', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/menuAddOn' } as NamedNode,
	/** 'merchant' is an out-dated term for 'seller'. */
	'merchant': { termType: 'NamedNode', value: 'https://schema.org/merchant', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/merchant' } as NamedNode,
	/** Specifies either a fixed return date or the number of days (from the delivery date) that a product can be returned. Used when the [[returnPolicyCategory]] property is specified as [[MerchantReturnFiniteReturnWindow]]. */
	'merchantReturnDays': { termType: 'NamedNode', value: 'https://schema.org/merchantReturnDays', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/merchantReturnDays' } as NamedNode,
	/** Specifies a Web page or service by URL, for product returns. */
	'merchantReturnLink': { termType: 'NamedNode', value: 'https://schema.org/merchantReturnLink', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/merchantReturnLink' } as NamedNode,
	/** A CreativeWork attached to the message. */
	'messageAttachment': { termType: 'NamedNode', value: 'https://schema.org/messageAttachment', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/messageAttachment' } as NamedNode,
	/** The total distance travelled by the particular vehicle since its initial production, as read from its odometer.\n\nTypical unit code(s): KMT for kilometers, SMI for statute miles */
	'mileageFromOdometer': { termType: 'NamedNode', value: 'https://schema.org/mileageFromOdometer', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/mileageFromOdometer' } as NamedNode,
	/** The lowest price if the price is a range. */
	'minPrice': { termType: 'NamedNode', value: 'https://schema.org/minPrice', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/minPrice' } as NamedNode,
	/** The lower value of some characteristic or property. */
	'minValue': { termType: 'NamedNode', value: 'https://schema.org/minValue', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/minValue' } as NamedNode,
	/** The minimum payment required at this time. */
	'minimumPaymentDue': { termType: 'NamedNode', value: 'https://schema.org/minimumPaymentDue', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/minimumPaymentDue' } as NamedNode,
	/** For a [[NewsMediaOrganization]], a statement on coverage priorities, including any public agenda or stance on issues. */
	'missionCoveragePrioritiesPolicy': { termType: 'NamedNode', value: 'https://schema.org/missionCoveragePrioritiesPolicy', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/missionCoveragePrioritiesPolicy' } as NamedNode,
	/** The [[mobileUrl]] property is provided for specific situations in which data consumers need to determine whether one of several provided URLs is a dedicated 'mobile site'.

To discourage over-use, and reflecting intial usecases, the property is expected only on [[Product]] and [[Offer]], rather than [[Thing]]. The general trend in web technology is towards [responsive design](https://en.wikipedia.org/wiki/Responsive_web_design) in which content can be flexibly adapted to a wide range of browsing environments. Pages and sites referenced with the long-established [[url]] property should ideally also be usable on a wide variety of devices, including mobile phones. In most cases, it would be pointless and counter productive to attempt to update all [[url]] markup to use [[mobileUrl]] for more mobile-oriented pages. The property is intended for the case when items (primarily [[Product]] and [[Offer]]) have extra URLs hosted on an additional "mobile site" alongside the main one. It should not be taken as an endorsement of this publication style.
     */
	'mobileUrl': { termType: 'NamedNode', value: 'https://schema.org/mobileUrl', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/mobileUrl' } as NamedNode,
	/** The model of the product. Use with the URL of a ProductModel or a textual representation of the model identifier. The URL of the ProductModel can be from an external source. It is recommended to additionally provide strong product identifiers via the gtin8/gtin13/gtin14 and mpn properties. */
	'model': { termType: 'NamedNode', value: 'https://schema.org/model', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/model' } as NamedNode,
	/** The release date of a vehicle model (often used to differentiate versions of the same make and model). */
	'modelDate': { termType: 'NamedNode', value: 'https://schema.org/modelDate', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/modelDate' } as NamedNode,
	/** The date and time the reservation was modified. */
	'modifiedTime': { termType: 'NamedNode', value: 'https://schema.org/modifiedTime', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/modifiedTime' } as NamedNode,
	/** The empirical formula is the simplest whole number ratio of all the atoms in a molecule. */
	'molecularFormula': { termType: 'NamedNode', value: 'https://schema.org/molecularFormula', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/molecularFormula' } as NamedNode,
	/** This is the molecular weight of the entity being described, not of the parent. Units should be included in the form '&lt;Number&gt; &lt;unit&gt;', for example '12 amu' or as '&lt;QuantitativeValue&gt;. */
	'molecularWeight': { termType: 'NamedNode', value: 'https://schema.org/molecularWeight', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/molecularWeight' } as NamedNode,
	/** The monoisotopic mass is the sum of the masses of the atoms in a molecule using the unbound, ground-state, rest mass of the principal (most abundant) isotope for each element instead of the isotopic average mass. Please include the units in the form '&lt;Number&gt; &lt;unit&gt;', for example '770.230488 g/mol' or as '&lt;QuantitativeValue&gt;. */
	'monoisotopicMolecularWeight': { termType: 'NamedNode', value: 'https://schema.org/monoisotopicMolecularWeight', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/monoisotopicMolecularWeight' } as NamedNode,
	/** The minimum payment is the lowest amount of money that one is required to pay on a credit card statement each month. */
	'monthlyMinimumRepaymentAmount': { termType: 'NamedNode', value: 'https://schema.org/monthlyMinimumRepaymentAmount', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/monthlyMinimumRepaymentAmount' } as NamedNode,
	/** Indicates the minimal number of months of experience required for a position. */
	'monthsOfExperience': { termType: 'NamedNode', value: 'https://schema.org/monthsOfExperience', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/monthsOfExperience' } as NamedNode,
	/** The Manufacturer Part Number (MPN) of the product, or the product to which the offer refers. */
	'mpn': { termType: 'NamedNode', value: 'https://schema.org/mpn', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/mpn' } as NamedNode,
	/** Whether multiple values are allowed for the property.  Default is false. */
	'multipleValues': { termType: 'NamedNode', value: 'https://schema.org/multipleValues', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/multipleValues' } as NamedNode,
	/** The movement the muscle generates. */
	'muscleAction': { termType: 'NamedNode', value: 'https://schema.org/muscleAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/muscleAction' } as NamedNode,
	/** An arrangement derived from the composition. */
	'musicArrangement': { termType: 'NamedNode', value: 'https://schema.org/musicArrangement', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/musicArrangement' } as NamedNode,
	/** The composer of the soundtrack. */
	'musicBy': { termType: 'NamedNode', value: 'https://schema.org/musicBy', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/musicBy' } as NamedNode,
	/** The type of composition (e.g. overture, sonata, symphony, etc.). */
	'musicCompositionForm': { termType: 'NamedNode', value: 'https://schema.org/musicCompositionForm', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/musicCompositionForm' } as NamedNode,
	/** A member of a music group&#x2014;for example, John, Paul, George, or Ringo. */
	'musicGroupMember': { termType: 'NamedNode', value: 'https://schema.org/musicGroupMember', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/musicGroupMember' } as NamedNode,
	/** Format of this release (the type of recording media used, i.e. compact disc, digital media, LP, etc.). */
	'musicReleaseFormat': { termType: 'NamedNode', value: 'https://schema.org/musicReleaseFormat', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/musicReleaseFormat' } as NamedNode,
	/** The key, mode, or scale this composition uses. */
	'musicalKey': { termType: 'NamedNode', value: 'https://schema.org/musicalKey', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/musicalKey' } as NamedNode,
	/** The North American Industry Classification System (NAICS) code for a particular organization or business person. */
	'naics': { termType: 'NamedNode', value: 'https://schema.org/naics', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/naics' } as NamedNode,
	/** The name of the item. */
	'name': { termType: 'NamedNode', value: 'https://schema.org/name', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/name' } as NamedNode,
	/** A position played, performed or filled by a person or organization, as part of an organization. For example, an athlete in a SportsTeam might play in the position named 'Quarterback'. */
	'namedPosition': { termType: 'NamedNode', value: 'https://schema.org/namedPosition', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/namedPosition' } as NamedNode,
	/** Nationality of the person. */
	'nationality': { termType: 'NamedNode', value: 'https://schema.org/nationality', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/nationality' } as NamedNode,
	/** The expected progression of the condition if it is not treated and allowed to progress naturally. */
	'naturalProgression': { termType: 'NamedNode', value: 'https://schema.org/naturalProgression', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/naturalProgression' } as NamedNode,
	/** Provides negative considerations regarding something, most typically in pro/con lists for reviews (alongside [[positiveNotes]]). For symmetry 

In the case of a [[Review]], the property describes the [[itemReviewed]] from the perspective of the review; in the case of a [[Product]], the product itself is being described. Since product descriptions 
tend to emphasise positive claims, it may be relatively unusual to find [[negativeNotes]] used in this way. Nevertheless for the sake of symmetry, [[negativeNotes]] can be used on [[Product]].

The property values can be expressed either as unstructured text (repeated as necessary), or if ordered, as a list (in which case the most negative is at the beginning of the list). */
	'negativeNotes': { termType: 'NamedNode', value: 'https://schema.org/negativeNotes', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/negativeNotes' } as NamedNode,
	/** The underlying innervation associated with the muscle. */
	'nerve': { termType: 'NamedNode', value: 'https://schema.org/nerve', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/nerve' } as NamedNode,
	/** The neurological pathway extension that involves muscle control. */
	'nerveMotor': { termType: 'NamedNode', value: 'https://schema.org/nerveMotor', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/nerveMotor' } as NamedNode,
	/** The total financial value of the person as calculated by subtracting assets from liabilities. */
	'netWorth': { termType: 'NamedNode', value: 'https://schema.org/netWorth', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/netWorth' } as NamedNode,
	/** Indicates a page with news updates and guidelines. This could often be (but is not required to be) the main page containing [[SpecialAnnouncement]] markup on a site. */
	'newsUpdatesAndGuidelines': { termType: 'NamedNode', value: 'https://schema.org/newsUpdatesAndGuidelines', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/newsUpdatesAndGuidelines' } as NamedNode,
	/** A link to the ListItem that follows the current one. */
	'nextItem': { termType: 'NamedNode', value: 'https://schema.org/nextItem', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/nextItem' } as NamedNode,
	/** For a [[NewsMediaOrganization]] or other news-related [[Organization]], a statement explaining when authors of articles are not named in bylines. */
	'noBylinesPolicy': { termType: 'NamedNode', value: 'https://schema.org/noBylinesPolicy', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/noBylinesPolicy' } as NamedNode,
	/** This ordering relation for qualitative values indicates that the subject is not equal to the object. */
	'nonEqual': { termType: 'NamedNode', value: 'https://schema.org/nonEqual', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/nonEqual' } as NamedNode,
	/** The generic name of this drug or supplement. */
	'nonProprietaryName': { termType: 'NamedNode', value: 'https://schema.org/nonProprietaryName', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/nonProprietaryName' } as NamedNode,
	/** nonprofitStatus indicates the legal status of a non-profit organization in its primary place of business. */
	'nonprofitStatus': { termType: 'NamedNode', value: 'https://schema.org/nonprofitStatus', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/nonprofitStatus' } as NamedNode,
	/** Range of acceptable values for a typical patient, when applicable. */
	'normalRange': { termType: 'NamedNode', value: 'https://schema.org/normalRange', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/normalRange' } as NamedNode,
	/** Indicates the [NATO stock number](https://en.wikipedia.org/wiki/NATO_Stock_Number) (nsn) of a [[Product]].  */
	'nsn': { termType: 'NamedNode', value: 'https://schema.org/nsn', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/nsn' } as NamedNode,
	/** The number of adults staying in the unit. */
	'numAdults': { termType: 'NamedNode', value: 'https://schema.org/numAdults', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/numAdults' } as NamedNode,
	/** The number of children staying in the unit. */
	'numChildren': { termType: 'NamedNode', value: 'https://schema.org/numChildren', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/numChildren' } as NamedNode,
	/** Indicates the number of constraints property values defined for a particular [[ConstraintNode]] such as [[StatisticalVariable]]. This helps applications understand if they have access to a sufficiently complete description of a [[StatisticalVariable]] or other construct that is defined using properties on template-style nodes. */
	'numConstraints': { termType: 'NamedNode', value: 'https://schema.org/numConstraints', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/numConstraints' } as NamedNode,
	/** The number of tracks in this album or playlist. */
	'numTracks': { termType: 'NamedNode', value: 'https://schema.org/numTracks', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/numTracks' } as NamedNode,
	/** Indicates the total (available plus unavailable) number of accommodation units in an [[ApartmentComplex]], or the number of accommodation units for a specific [[FloorPlan]] (within its specific [[ApartmentComplex]]). See also [[numberOfAvailableAccommodationUnits]]. */
	'numberOfAccommodationUnits': { termType: 'NamedNode', value: 'https://schema.org/numberOfAccommodationUnits', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/numberOfAccommodationUnits' } as NamedNode,
	/** The number or type of airbags in the vehicle. */
	'numberOfAirbags': { termType: 'NamedNode', value: 'https://schema.org/numberOfAirbags', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/numberOfAirbags' } as NamedNode,
	/** Indicates the number of available accommodation units in an [[ApartmentComplex]], or the number of accommodation units for a specific [[FloorPlan]] (within its specific [[ApartmentComplex]]). See also [[numberOfAccommodationUnits]]. */
	'numberOfAvailableAccommodationUnits': { termType: 'NamedNode', value: 'https://schema.org/numberOfAvailableAccommodationUnits', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/numberOfAvailableAccommodationUnits' } as NamedNode,
	/** The number of axles.\n\nTypical unit code(s): C62 */
	'numberOfAxles': { termType: 'NamedNode', value: 'https://schema.org/numberOfAxles', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/numberOfAxles' } as NamedNode,
	/** The total integer number of bathrooms in some [[Accommodation]], following real estate conventions as [documented in RESO](https://ddwiki.reso.org/display/DDW17/BathroomsTotalInteger+Field): "The simple sum of the number of bathrooms. For example for a property with two Full Bathrooms and one Half Bathroom, the Bathrooms Total Integer will be 3.". See also [[numberOfRooms]]. */
	'numberOfBathroomsTotal': { termType: 'NamedNode', value: 'https://schema.org/numberOfBathroomsTotal', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/numberOfBathroomsTotal' } as NamedNode,
	/** The total integer number of bedrooms in a some [[Accommodation]], [[ApartmentComplex]] or [[FloorPlan]]. */
	'numberOfBedrooms': { termType: 'NamedNode', value: 'https://schema.org/numberOfBedrooms', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/numberOfBedrooms' } as NamedNode,
	/** The quantity of the given bed type available in the HotelRoom, Suite, House, or Apartment. */
	'numberOfBeds': { termType: 'NamedNode', value: 'https://schema.org/numberOfBeds', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/numberOfBeds' } as NamedNode,
	/** The number of credits or units awarded by a Course or required to complete an EducationalOccupationalProgram. */
	'numberOfCredits': { termType: 'NamedNode', value: 'https://schema.org/numberOfCredits', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/numberOfCredits' } as NamedNode,
	/** The number of doors.\n\nTypical unit code(s): C62 */
	'numberOfDoors': { termType: 'NamedNode', value: 'https://schema.org/numberOfDoors', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/numberOfDoors' } as NamedNode,
	/** The number of employees in an organization, e.g. business. */
	'numberOfEmployees': { termType: 'NamedNode', value: 'https://schema.org/numberOfEmployees', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/numberOfEmployees' } as NamedNode,
	/** The number of episodes in this season or series. */
	'numberOfEpisodes': { termType: 'NamedNode', value: 'https://schema.org/numberOfEpisodes', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/numberOfEpisodes' } as NamedNode,
	/** The total number of forward gears available for the transmission system of the vehicle.\n\nTypical unit code(s): C62 */
	'numberOfForwardGears': { termType: 'NamedNode', value: 'https://schema.org/numberOfForwardGears', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/numberOfForwardGears' } as NamedNode,
	/** Number of full bathrooms - The total number of full and ¾ bathrooms in an [[Accommodation]]. This corresponds to the [BathroomsFull field in RESO](https://ddwiki.reso.org/display/DDW17/BathroomsFull+Field). */
	'numberOfFullBathrooms': { termType: 'NamedNode', value: 'https://schema.org/numberOfFullBathrooms', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/numberOfFullBathrooms' } as NamedNode,
	/** The number of items in an ItemList. Note that some descriptions might not fully describe all items in a list (e.g., multi-page pagination); in such cases, the numberOfItems would be for the entire list. */
	'numberOfItems': { termType: 'NamedNode', value: 'https://schema.org/numberOfItems', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/numberOfItems' } as NamedNode,
	/** The number of payments contractually required at origination to repay the loan. For monthly paying loans this is the number of months from the contractual first payment date to the maturity date. */
	'numberOfLoanPayments': { termType: 'NamedNode', value: 'https://schema.org/numberOfLoanPayments', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/numberOfLoanPayments' } as NamedNode,
	/** The number of pages in the book. */
	'numberOfPages': { termType: 'NamedNode', value: 'https://schema.org/numberOfPages', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/numberOfPages' } as NamedNode,
	/** Number of partial bathrooms - The total number of half and ¼ bathrooms in an [[Accommodation]]. This corresponds to the [BathroomsPartial field in RESO](https://ddwiki.reso.org/display/DDW17/BathroomsPartial+Field).  */
	'numberOfPartialBathrooms': { termType: 'NamedNode', value: 'https://schema.org/numberOfPartialBathrooms', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/numberOfPartialBathrooms' } as NamedNode,
	/** Indicate how many people can play this game (minimum, maximum, or range). */
	'numberOfPlayers': { termType: 'NamedNode', value: 'https://schema.org/numberOfPlayers', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/numberOfPlayers' } as NamedNode,
	/** The number of owners of the vehicle, including the current one.\n\nTypical unit code(s): C62 */
	'numberOfPreviousOwners': { termType: 'NamedNode', value: 'https://schema.org/numberOfPreviousOwners', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/numberOfPreviousOwners' } as NamedNode,
	/** The number of rooms (excluding bathrooms and closets) of the accommodation or lodging business.
Typical unit code(s): ROM for room or C62 for no unit. The type of room can be put in the unitText property of the QuantitativeValue. */
	'numberOfRooms': { termType: 'NamedNode', value: 'https://schema.org/numberOfRooms', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/numberOfRooms' } as NamedNode,
	/** The number of seasons in this series. */
	'numberOfSeasons': { termType: 'NamedNode', value: 'https://schema.org/numberOfSeasons', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/numberOfSeasons' } as NamedNode,
	/** A number associated with a role in an organization, for example, the number on an athlete's jersey. */
	'numberedPosition': { termType: 'NamedNode', value: 'https://schema.org/numberedPosition', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/numberedPosition' } as NamedNode,
	/** Nutrition information about the recipe or menu item. */
	'nutrition': { termType: 'NamedNode', value: 'https://schema.org/nutrition', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/nutrition' } as NamedNode,
	/** The object upon which the action is carried out, whose state is kept intact or changed. Also known as the semantic roles patient, affected or undergoer (which change their state) or theme (which doesn't). E.g. John read *a book*. */
	'object': { termType: 'NamedNode', value: 'https://schema.org/object', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/object' } as NamedNode,
	/** The [[observationAbout]] property identifies an entity, often a [[Place]], associated with an [[Observation]]. */
	'observationAbout': { termType: 'NamedNode', value: 'https://schema.org/observationAbout', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/observationAbout' } as NamedNode,
	/** The observationDate of an [[Observation]]. */
	'observationDate': { termType: 'NamedNode', value: 'https://schema.org/observationDate', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/observationDate' } as NamedNode,
	/** The length of time an Observation took place over. The format follows `P[0-9]*[Y|M|D|h|m|s]`. For example, P1Y is Period 1 Year, P3M is Period 3 Months, P3h is Period 3 hours. */
	'observationPeriod': { termType: 'NamedNode', value: 'https://schema.org/observationPeriod', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/observationPeriod' } as NamedNode,
	/** The allowed total occupancy for the accommodation in persons (including infants etc). For individual accommodations, this is not necessarily the legal maximum but defines the permitted usage as per the contractual agreement (e.g. a double room used by a single person).
Typical unit code(s): C62 for person */
	'occupancy': { termType: 'NamedNode', value: 'https://schema.org/occupancy', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/occupancy' } as NamedNode,
	/**  The region/country for which this occupational description is appropriate. Note that educational requirements and qualifications can vary between jurisdictions. */
	'occupationLocation': { termType: 'NamedNode', value: 'https://schema.org/occupationLocation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/occupationLocation' } as NamedNode,
	/** A category describing the job, preferably using a term from a taxonomy such as [BLS O*NET-SOC](http://www.onetcenter.org/taxonomy.html), [ISCO-08](https://www.ilo.org/public/english/bureau/stat/isco/isco08/) or similar, with the property repeated for each applicable value. Ideally the taxonomy should be identified, and both the textual label and formal code for the category should be provided.\n
Note: for historical reasons, any textual label and formal code provided as a literal may be assumed to be from O*NET-SOC. */
	'occupationalCategory': { termType: 'NamedNode', value: 'https://schema.org/occupationalCategory', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/occupationalCategory' } as NamedNode,
	/** A description of the qualification, award, certificate, diploma or other occupational credential awarded as a consequence of successful completion of this course or program. */
	'occupationalCredentialAwarded': { termType: 'NamedNode', value: 'https://schema.org/occupationalCredentialAwarded', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/occupationalCredentialAwarded' } as NamedNode,
	/** The number of offers for the product. */
	'offerCount': { termType: 'NamedNode', value: 'https://schema.org/offerCount', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/offerCount' } as NamedNode,
	/** A pointer to the organization or person making the offer. */
	'offeredBy': { termType: 'NamedNode', value: 'https://schema.org/offeredBy', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/offeredBy' } as NamedNode,
	/** An offer to provide this item&#x2014;for example, an offer to sell a product, rent the DVD of a movie, perform a service, or give away tickets to an event. Use [[businessFunction]] to indicate the kind of transaction offered, i.e. sell, lease, etc. This property can also be used to describe a [[Demand]]. While this property is listed as expected on a number of common types, it can be used in others. In that case, using a second type, such as Product or a subtype of Product, can clarify the nature of the offer.
       */
	'offers': { termType: 'NamedNode', value: 'https://schema.org/offers', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/offers' } as NamedNode,
	/** Whether prescriptions can be delivered by mail. */
	'offersPrescriptionByMail': { termType: 'NamedNode', value: 'https://schema.org/offersPrescriptionByMail', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/offersPrescriptionByMail' } as NamedNode,
	/** The general opening hours for a business. Opening hours can be specified as a weekly time range, starting with days, then times per day. Multiple days can be listed with commas ',' separating each day. Day or time ranges are specified using a hyphen '-'.\n\n* Days are specified using the following two-letter combinations: ```Mo```, ```Tu```, ```We```, ```Th```, ```Fr```, ```Sa```, ```Su```.\n* Times are specified using 24:00 format. For example, 3pm is specified as ```15:00```, 10am as ```10:00```. \n* Here is an example: <code>&lt;time itemprop="openingHours" datetime=&quot;Tu,Th 16:00-20:00&quot;&gt;Tuesdays and Thursdays 4-8pm&lt;/time&gt;</code>.\n* If a business is open 7 days a week, then it can be specified as <code>&lt;time itemprop=&quot;openingHours&quot; datetime=&quot;Mo-Su&quot;&gt;Monday through Sunday, all day&lt;/time&gt;</code>. */
	'openingHours': { termType: 'NamedNode', value: 'https://schema.org/openingHours', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/openingHours' } as NamedNode,
	/** The opening hours of a certain place. */
	'openingHoursSpecification': { termType: 'NamedNode', value: 'https://schema.org/openingHoursSpecification', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/openingHoursSpecification' } as NamedNode,
	/** The opening hour of the place or service on the given day(s) of the week. */
	'opens': { termType: 'NamedNode', value: 'https://schema.org/opens', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/opens' } as NamedNode,
	/** Operating systems supported (Windows 7, OS X 10.6, Android 1.6). */
	'operatingSystem': { termType: 'NamedNode', value: 'https://schema.org/operatingSystem', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/operatingSystem' } as NamedNode,
	/** A sub property of participant. The opponent on this action. */
	'opponent': { termType: 'NamedNode', value: 'https://schema.org/opponent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/opponent' } as NamedNode,
	/** A sub property of object. The options subject to this action. */
	'option': { termType: 'NamedNode', value: 'https://schema.org/option', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/option' } as NamedNode,
	/** Date order was placed. */
	'orderDate': { termType: 'NamedNode', value: 'https://schema.org/orderDate', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/orderDate' } as NamedNode,
	/** The delivery of the parcel related to this order or order item. */
	'orderDelivery': { termType: 'NamedNode', value: 'https://schema.org/orderDelivery', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/orderDelivery' } as NamedNode,
	/** The identifier of the order item. */
	'orderItemNumber': { termType: 'NamedNode', value: 'https://schema.org/orderItemNumber', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/orderItemNumber' } as NamedNode,
	/** The current status of the order item. */
	'orderItemStatus': { termType: 'NamedNode', value: 'https://schema.org/orderItemStatus', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/orderItemStatus' } as NamedNode,
	/** The identifier of the transaction. */
	'orderNumber': { termType: 'NamedNode', value: 'https://schema.org/orderNumber', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/orderNumber' } as NamedNode,
	/** The number of the item ordered. If the property is not set, assume the quantity is one. */
	'orderQuantity': { termType: 'NamedNode', value: 'https://schema.org/orderQuantity', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/orderQuantity' } as NamedNode,
	/** The current status of the order. */
	'orderStatus': { termType: 'NamedNode', value: 'https://schema.org/orderStatus', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/orderStatus' } as NamedNode,
	/** The item ordered. */
	'orderedItem': { termType: 'NamedNode', value: 'https://schema.org/orderedItem', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/orderedItem' } as NamedNode,
	/** An organizer of an Event. */
	'organizer': { termType: 'NamedNode', value: 'https://schema.org/organizer', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/organizer' } as NamedNode,
	/** Shipper's address. */
	'originAddress': { termType: 'NamedNode', value: 'https://schema.org/originAddress', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/originAddress' } as NamedNode,
	/** Describes, in a [[MediaReview]] when dealing with [[DecontextualizedContent]], background information that can contribute to better interpretation of the [[MediaObject]]. */
	'originalMediaContextDescription': { termType: 'NamedNode', value: 'https://schema.org/originalMediaContextDescription', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/originalMediaContextDescription' } as NamedNode,
	/** Link to the page containing an original version of the content, or directly to an online copy of the original [[MediaObject]] content, e.g. video file. */
	'originalMediaLink': { termType: 'NamedNode', value: 'https://schema.org/originalMediaLink', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/originalMediaLink' } as NamedNode,
	/** The vasculature the lymphatic structure originates, or afferents, from. */
	'originatesFrom': { termType: 'NamedNode', value: 'https://schema.org/originatesFrom', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/originatesFrom' } as NamedNode,
	/** Any information related to overdose on a drug, including signs or symptoms, treatments, contact information for emergency response. */
	'overdosage': { termType: 'NamedNode', value: 'https://schema.org/overdosage', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/overdosage' } as NamedNode,
	/** The date and time of obtaining the product. */
	'ownedFrom': { termType: 'NamedNode', value: 'https://schema.org/ownedFrom', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ownedFrom' } as NamedNode,
	/** The date and time of giving up ownership on the product. */
	'ownedThrough': { termType: 'NamedNode', value: 'https://schema.org/ownedThrough', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ownedThrough' } as NamedNode,
	/** For an [[Organization]] (often but not necessarily a [[NewsMediaOrganization]]), a description of organizational ownership structure; funding and grants. In a news/media setting, this is with particular reference to editorial independence.   Note that the [[funder]] is also available and can be used to make basic funder information machine-readable. */
	'ownershipFundingInfo': { termType: 'NamedNode', value: 'https://schema.org/ownershipFundingInfo', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ownershipFundingInfo' } as NamedNode,
	/** Products owned by the organization or person. */
	'owns': { termType: 'NamedNode', value: 'https://schema.org/owns', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/owns' } as NamedNode,
	/** The page on which the work ends; for example "138" or "xvi". */
	'pageEnd': { termType: 'NamedNode', value: 'https://schema.org/pageEnd', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/pageEnd' } as NamedNode,
	/** The page on which the work starts; for example "135" or "xiii". */
	'pageStart': { termType: 'NamedNode', value: 'https://schema.org/pageStart', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/pageStart' } as NamedNode,
	/** Any description of pages that is not separated into pageStart and pageEnd; for example, "1-6, 9, 55" or "10-12, 46-49". */
	'pagination': { termType: 'NamedNode', value: 'https://schema.org/pagination', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/pagination' } as NamedNode,
	/** A parent of this person. */
	'parent': { termType: 'NamedNode', value: 'https://schema.org/parent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/parent' } as NamedNode,
	/** The parent of a question, answer or item in general. */
	'parentItem': { termType: 'NamedNode', value: 'https://schema.org/parentItem', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/parentItem' } as NamedNode,
	/** The larger organization that this organization is a [[subOrganization]] of, if any. */
	'parentOrganization': { termType: 'NamedNode', value: 'https://schema.org/parentOrganization', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/parentOrganization' } as NamedNode,
	/** A broadcast service to which the broadcast service may belong to such as regional variations of a national channel. */
	'parentService': { termType: 'NamedNode', value: 'https://schema.org/parentService', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/parentService' } as NamedNode,
	/** Closest parent taxon of the taxon in question. */
	'parentTaxon': { termType: 'NamedNode', value: 'https://schema.org/parentTaxon', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/parentTaxon' } as NamedNode,
	/** A parents of the person. */
	'parents': { termType: 'NamedNode', value: 'https://schema.org/parents', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/parents' } as NamedNode,
	/** The episode to which this clip belongs. */
	'partOfEpisode': { termType: 'NamedNode', value: 'https://schema.org/partOfEpisode', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/partOfEpisode' } as NamedNode,
	/** The order is being paid as part of the referenced Invoice. */
	'partOfInvoice': { termType: 'NamedNode', value: 'https://schema.org/partOfInvoice', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/partOfInvoice' } as NamedNode,
	/** The overall order the items in this delivery were included in. */
	'partOfOrder': { termType: 'NamedNode', value: 'https://schema.org/partOfOrder', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/partOfOrder' } as NamedNode,
	/** The season to which this episode belongs. */
	'partOfSeason': { termType: 'NamedNode', value: 'https://schema.org/partOfSeason', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/partOfSeason' } as NamedNode,
	/** The series to which this episode or season belongs. */
	'partOfSeries': { termType: 'NamedNode', value: 'https://schema.org/partOfSeries', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/partOfSeries' } as NamedNode,
	/** The anatomical or organ system that this structure is part of. */
	'partOfSystem': { termType: 'NamedNode', value: 'https://schema.org/partOfSystem', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/partOfSystem' } as NamedNode,
	/** The TV series to which this episode or season belongs. */
	'partOfTVSeries': { termType: 'NamedNode', value: 'https://schema.org/partOfTVSeries', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/partOfTVSeries' } as NamedNode,
	/** Identifies that this [[Trip]] is a subTrip of another Trip.  For example Day 1, Day 2, etc. of a multi-day trip. */
	'partOfTrip': { termType: 'NamedNode', value: 'https://schema.org/partOfTrip', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/partOfTrip' } as NamedNode,
	/** Other co-agents that participated in the action indirectly. E.g. John wrote a book with *Steve*. */
	'participant': { termType: 'NamedNode', value: 'https://schema.org/participant', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/participant' } as NamedNode,
	/** Number of people the reservation should accommodate. */
	'partySize': { termType: 'NamedNode', value: 'https://schema.org/partySize', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/partySize' } as NamedNode,
	/** The priority status assigned to a passenger for security or boarding (e.g. FastTrack or Priority). */
	'passengerPriorityStatus': { termType: 'NamedNode', value: 'https://schema.org/passengerPriorityStatus', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/passengerPriorityStatus' } as NamedNode,
	/** The passenger's sequence number as assigned by the airline. */
	'passengerSequenceNumber': { termType: 'NamedNode', value: 'https://schema.org/passengerSequenceNumber', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/passengerSequenceNumber' } as NamedNode,
	/** Changes in the normal mechanical, physical, and biochemical functions that are associated with this activity or condition. */
	'pathophysiology': { termType: 'NamedNode', value: 'https://schema.org/pathophysiology', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/pathophysiology' } as NamedNode,
	/** A pattern that something has, for example 'polka dot', 'striped', 'Canadian flag'. Values are typically expressed as text, although links to controlled value schemes are also supported. */
	'pattern': { termType: 'NamedNode', value: 'https://schema.org/pattern', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/pattern' } as NamedNode,
	/** The permitted weight of passengers and cargo, EXCLUDING the weight of the empty vehicle.\n\nTypical unit code(s): KGM for kilogram, LBR for pound\n\n* Note 1: Many databases specify the permitted TOTAL weight instead, which is the sum of [[weight]] and [[payload]]\n* Note 2: You can indicate additional information in the [[name]] of the [[QuantitativeValue]] node.\n* Note 3: You may also link to a [[QualitativeValue]] node that provides additional information using [[valueReference]].\n* Note 4: Note that you can use [[minValue]] and [[maxValue]] to indicate ranges. */
	'payload': { termType: 'NamedNode', value: 'https://schema.org/payload', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/payload' } as NamedNode,
	/** Cash, Credit Card, Cryptocurrency, Local Exchange Tradings System, etc. */
	'paymentAccepted': { termType: 'NamedNode', value: 'https://schema.org/paymentAccepted', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/paymentAccepted' } as NamedNode,
	/** The date that payment is due. */
	'paymentDue': { termType: 'NamedNode', value: 'https://schema.org/paymentDue', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/paymentDue' } as NamedNode,
	/** The date that payment is due. */
	'paymentDueDate': { termType: 'NamedNode', value: 'https://schema.org/paymentDueDate', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/paymentDueDate' } as NamedNode,
	/** The name of the credit card or other method of payment for the order. */
	'paymentMethod': { termType: 'NamedNode', value: 'https://schema.org/paymentMethod', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/paymentMethod' } as NamedNode,
	/** An identifier for the method of payment used (e.g. the last 4 digits of the credit card). */
	'paymentMethodId': { termType: 'NamedNode', value: 'https://schema.org/paymentMethodId', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/paymentMethodId' } as NamedNode,
	/** The status of payment; whether the invoice has been paid or not. */
	'paymentStatus': { termType: 'NamedNode', value: 'https://schema.org/paymentStatus', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/paymentStatus' } as NamedNode,
	/** The URL for sending a payment. */
	'paymentUrl': { termType: 'NamedNode', value: 'https://schema.org/paymentUrl', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/paymentUrl' } as NamedNode,
	/** The individual who draws the primary narrative artwork. */
	'penciler': { termType: 'NamedNode', value: 'https://schema.org/penciler', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/penciler' } as NamedNode,
	/** The 10th percentile value. */
	'percentile10': { termType: 'NamedNode', value: 'https://schema.org/percentile10', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/percentile10' } as NamedNode,
	/** The 25th percentile value. */
	'percentile25': { termType: 'NamedNode', value: 'https://schema.org/percentile25', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/percentile25' } as NamedNode,
	/** The 75th percentile value. */
	'percentile75': { termType: 'NamedNode', value: 'https://schema.org/percentile75', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/percentile75' } as NamedNode,
	/** The 90th percentile value. */
	'percentile90': { termType: 'NamedNode', value: 'https://schema.org/percentile90', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/percentile90' } as NamedNode,
	/** The length of time it takes to perform instructions or a direction (not including time to prepare the supplies), in [ISO 8601 duration format](http://en.wikipedia.org/wiki/ISO_8601). */
	'performTime': { termType: 'NamedNode', value: 'https://schema.org/performTime', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/performTime' } as NamedNode,
	/** A performer at the event&#x2014;for example, a presenter, musician, musical group or actor. */
	'performer': { termType: 'NamedNode', value: 'https://schema.org/performer', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/performer' } as NamedNode,
	/** Event that this person is a performer or participant in. */
	'performerIn': { termType: 'NamedNode', value: 'https://schema.org/performerIn', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/performerIn' } as NamedNode,
	/** The main performer or performers of the event&#x2014;for example, a presenter, musician, or actor. */
	'performers': { termType: 'NamedNode', value: 'https://schema.org/performers', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/performers' } as NamedNode,
	/** The type of permission granted the person, organization, or audience. */
	'permissionType': { termType: 'NamedNode', value: 'https://schema.org/permissionType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/permissionType' } as NamedNode,
	/** Permission(s) required to run the app (for example, a mobile app may require full internet access or may run only on wifi). */
	'permissions': { termType: 'NamedNode', value: 'https://schema.org/permissions', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/permissions' } as NamedNode,
	/** The target audience for this permit. */
	'permitAudience': { termType: 'NamedNode', value: 'https://schema.org/permitAudience', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/permitAudience' } as NamedNode,
	/** Indications regarding the permitted usage of the accommodation. */
	'permittedUsage': { termType: 'NamedNode', value: 'https://schema.org/permittedUsage', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/permittedUsage' } as NamedNode,
	/** Indicates whether pets are allowed to enter the accommodation or lodging business. More detailed information can be put in a text value. */
	'petsAllowed': { termType: 'NamedNode', value: 'https://schema.org/petsAllowed', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/petsAllowed' } as NamedNode,
	/** Representation of a text [[textValue]] using the specified [[speechToTextMarkup]]. For example the city name of Houston in IPA: /ˈhjuːstən/. */
	'phoneticText': { termType: 'NamedNode', value: 'https://schema.org/phoneticText', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/phoneticText' } as NamedNode,
	/** A photograph of this place. */
	'photo': { termType: 'NamedNode', value: 'https://schema.org/photo', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/photo' } as NamedNode,
	/** Photographs of this place. */
	'photos': { termType: 'NamedNode', value: 'https://schema.org/photos', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/photos' } as NamedNode,
	/** A description of the types of physical activity associated with the job. Defined terms such as those in O*net may be used, but note that there is no way to specify the level of ability as well as its nature when using a defined term. */
	'physicalRequirement': { termType: 'NamedNode', value: 'https://schema.org/physicalRequirement', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/physicalRequirement' } as NamedNode,
	/** Specific physiologic benefits associated to the plan. */
	'physiologicalBenefits': { termType: 'NamedNode', value: 'https://schema.org/physiologicalBenefits', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/physiologicalBenefits' } as NamedNode,
	/** Where a taxi will pick up a passenger or a rental car can be picked up. */
	'pickupLocation': { termType: 'NamedNode', value: 'https://schema.org/pickupLocation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/pickupLocation' } as NamedNode,
	/** When a taxi will pick up a passenger or a rental car can be picked up. */
	'pickupTime': { termType: 'NamedNode', value: 'https://schema.org/pickupTime', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/pickupTime' } as NamedNode,
	/** Indicates whether this game is multi-player, co-op or single-player.  The game can be marked as multi-player, co-op and single-player at the same time. */
	'playMode': { termType: 'NamedNode', value: 'https://schema.org/playMode', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/playMode' } as NamedNode,
	/** Player type required&#x2014;for example, Flash or Silverlight. */
	'playerType': { termType: 'NamedNode', value: 'https://schema.org/playerType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/playerType' } as NamedNode,
	/** Number of players on the server. */
	'playersOnline': { termType: 'NamedNode', value: 'https://schema.org/playersOnline', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/playersOnline' } as NamedNode,
	/** A polygon is the area enclosed by a point-to-point path for which the starting and ending points are the same. A polygon is expressed as a series of four or more space delimited points where the first and final points are identical. */
	'polygon': { termType: 'NamedNode', value: 'https://schema.org/polygon', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/polygon' } as NamedNode,
	/** Indicates the populationType common to all members of a [[StatisticalPopulation]] or all cases within the scope of a [[StatisticalVariable]]. */
	'populationType': { termType: 'NamedNode', value: 'https://schema.org/populationType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/populationType' } as NamedNode,
	/** The position of an item in a series or sequence of items. */
	'position': { termType: 'NamedNode', value: 'https://schema.org/position', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/position' } as NamedNode,
	/** Provides positive considerations regarding something, for example product highlights or (alongside [[negativeNotes]]) pro/con lists for reviews.

In the case of a [[Review]], the property describes the [[itemReviewed]] from the perspective of the review; in the case of a [[Product]], the product itself is being described.

The property values can be expressed either as unstructured text (repeated as necessary), or if ordered, as a list (in which case the most positive is at the beginning of the list). */
	'positiveNotes': { termType: 'NamedNode', value: 'https://schema.org/positiveNotes', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/positiveNotes' } as NamedNode,
	/** A possible unexpected and unfavorable evolution of a medical condition. Complications may include worsening of the signs or symptoms of the disease, extension of the condition to other organ systems, etc. */
	'possibleComplication': { termType: 'NamedNode', value: 'https://schema.org/possibleComplication', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/possibleComplication' } as NamedNode,
	/** A possible treatment to address this condition, sign or symptom. */
	'possibleTreatment': { termType: 'NamedNode', value: 'https://schema.org/possibleTreatment', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/possibleTreatment' } as NamedNode,
	/** The post office box number for PO box addresses. */
	'postOfficeBoxNumber': { termType: 'NamedNode', value: 'https://schema.org/postOfficeBoxNumber', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/postOfficeBoxNumber' } as NamedNode,
	/** A description of the postoperative procedures, care, and/or followups for this device. */
	'postOp': { termType: 'NamedNode', value: 'https://schema.org/postOp', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/postOp' } as NamedNode,
	/** The postal code. For example, 94043. */
	'postalCode': { termType: 'NamedNode', value: 'https://schema.org/postalCode', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/postalCode' } as NamedNode,
	/** First postal code in a range (included). */
	'postalCodeBegin': { termType: 'NamedNode', value: 'https://schema.org/postalCodeBegin', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/postalCodeBegin' } as NamedNode,
	/** Last postal code in the range (included). Needs to be after [[postalCodeBegin]]. */
	'postalCodeEnd': { termType: 'NamedNode', value: 'https://schema.org/postalCodeEnd', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/postalCodeEnd' } as NamedNode,
	/** A defined range of postal codes indicated by a common textual prefix. Used for non-numeric systems such as UK. */
	'postalCodePrefix': { termType: 'NamedNode', value: 'https://schema.org/postalCodePrefix', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/postalCodePrefix' } as NamedNode,
	/** A defined range of postal codes. */
	'postalCodeRange': { termType: 'NamedNode', value: 'https://schema.org/postalCodeRange', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/postalCodeRange' } as NamedNode,
	/** Indicates a potential Action, which describes an idealized action in which this thing would play an 'object' role. */
	'potentialAction': { termType: 'NamedNode', value: 'https://schema.org/potentialAction', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/potentialAction' } as NamedNode,
	/** Intended use of the BioChemEntity by humans. */
	'potentialUse': { termType: 'NamedNode', value: 'https://schema.org/potentialUse', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/potentialUse' } as NamedNode,
	/** A description of the workup, testing, and other preparations required before implanting this device. */
	'preOp': { termType: 'NamedNode', value: 'https://schema.org/preOp', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/preOp' } as NamedNode,
	/** A pointer from a previous, often discontinued variant of the product to its newer variant. */
	'predecessorOf': { termType: 'NamedNode', value: 'https://schema.org/predecessorOf', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/predecessorOf' } as NamedNode,
	/** Pregnancy category of this drug. */
	'pregnancyCategory': { termType: 'NamedNode', value: 'https://schema.org/pregnancyCategory', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/pregnancyCategory' } as NamedNode,
	/** Any precaution, guidance, contraindication, etc. related to this drug's use during pregnancy. */
	'pregnancyWarning': { termType: 'NamedNode', value: 'https://schema.org/pregnancyWarning', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/pregnancyWarning' } as NamedNode,
	/** The length of time it takes to prepare the items to be used in instructions or a direction, in [ISO 8601 duration format](http://en.wikipedia.org/wiki/ISO_8601). */
	'prepTime': { termType: 'NamedNode', value: 'https://schema.org/prepTime', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/prepTime' } as NamedNode,
	/** Typical preparation that a patient must undergo before having the procedure performed. */
	'preparation': { termType: 'NamedNode', value: 'https://schema.org/preparation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/preparation' } as NamedNode,
	/** Link to prescribing information for the drug. */
	'prescribingInfo': { termType: 'NamedNode', value: 'https://schema.org/prescribingInfo', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/prescribingInfo' } as NamedNode,
	/** Indicates the status of drug prescription, e.g. local catalogs classifications or whether the drug is available by prescription or over-the-counter, etc. */
	'prescriptionStatus': { termType: 'NamedNode', value: 'https://schema.org/prescriptionStatus', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/prescriptionStatus' } as NamedNode,
	/** A link to the ListItem that precedes the current one. */
	'previousItem': { termType: 'NamedNode', value: 'https://schema.org/previousItem', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/previousItem' } as NamedNode,
	/** Used in conjunction with eventStatus for rescheduled or cancelled events. This property contains the previously scheduled start date. For rescheduled events, the startDate property should be used for the newly scheduled start date. In the (rare) case of an event that has been postponed and rescheduled multiple times, this field may be repeated. */
	'previousStartDate': { termType: 'NamedNode', value: 'https://schema.org/previousStartDate', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/previousStartDate' } as NamedNode,
	/** The offer price of a product, or of a price component when attached to PriceSpecification and its subtypes.\n\nUsage guidelines:\n\n* Use the [[priceCurrency]] property (with standard formats: [ISO 4217 currency format](http://en.wikipedia.org/wiki/ISO_4217), e.g. "USD"; [Ticker symbol](https://en.wikipedia.org/wiki/List_of_cryptocurrencies) for cryptocurrencies, e.g. "BTC"; well known names for [Local Exchange Trading Systems](https://en.wikipedia.org/wiki/Local_exchange_trading_system) (LETS) and other currency types, e.g. "Ithaca HOUR") instead of including [ambiguous symbols](http://en.wikipedia.org/wiki/Dollar_sign#Currencies_that_use_the_dollar_or_peso_sign) such as '$' in the value.\n* Use '.' (Unicode 'FULL STOP' (U+002E)) rather than ',' to indicate a decimal point. Avoid using these symbols as a readability separator.\n* Note that both [RDFa](http://www.w3.org/TR/xhtml-rdfa-primer/#using-the-content-attribute) and Microdata syntax allow the use of a "content=" attribute for publishing simple machine-readable values alongside more human-friendly formatting.\n* Use values from 0123456789 (Unicode 'DIGIT ZERO' (U+0030) to 'DIGIT NINE' (U+0039)) rather than superficially similar Unicode symbols.
       */
	'price': { termType: 'NamedNode', value: 'https://schema.org/price', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/price' } as NamedNode,
	/** This property links to all [[UnitPriceSpecification]] nodes that apply in parallel for the [[CompoundPriceSpecification]] node. */
	'priceComponent': { termType: 'NamedNode', value: 'https://schema.org/priceComponent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/priceComponent' } as NamedNode,
	/** Identifies a price component (for example, a line item on an invoice), part of the total price for an offer. */
	'priceComponentType': { termType: 'NamedNode', value: 'https://schema.org/priceComponentType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/priceComponentType' } as NamedNode,
	/** The currency of the price, or a price component when attached to [[PriceSpecification]] and its subtypes.\n\nUse standard formats: [ISO 4217 currency format](http://en.wikipedia.org/wiki/ISO_4217), e.g. "USD"; [Ticker symbol](https://en.wikipedia.org/wiki/List_of_cryptocurrencies) for cryptocurrencies, e.g. "BTC"; well known names for [Local Exchange Trading Systems](https://en.wikipedia.org/wiki/Local_exchange_trading_system) (LETS) and other currency types, e.g. "Ithaca HOUR". */
	'priceCurrency': { termType: 'NamedNode', value: 'https://schema.org/priceCurrency', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/priceCurrency' } as NamedNode,
	/** The price range of the business, for example ```$$$```. */
	'priceRange': { termType: 'NamedNode', value: 'https://schema.org/priceRange', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/priceRange' } as NamedNode,
	/** One or more detailed price specifications, indicating the unit price and delivery or payment charges. */
	'priceSpecification': { termType: 'NamedNode', value: 'https://schema.org/priceSpecification', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/priceSpecification' } as NamedNode,
	/** Defines the type of a price specified for an offered product, for example a list price, a (temporary) sale price or a manufacturer suggested retail price. If multiple prices are specified for an offer the [[priceType]] property can be used to identify the type of each such specified price. The value of priceType can be specified as a value from enumeration PriceTypeEnumeration or as a free form text string for price types that are not already predefined in PriceTypeEnumeration. */
	'priceType': { termType: 'NamedNode', value: 'https://schema.org/priceType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/priceType' } as NamedNode,
	/** The date after which the price is no longer available. */
	'priceValidUntil': { termType: 'NamedNode', value: 'https://schema.org/priceValidUntil', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/priceValidUntil' } as NamedNode,
	/** Indicates the main image on the page. */
	'primaryImageOfPage': { termType: 'NamedNode', value: 'https://schema.org/primaryImageOfPage', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/primaryImageOfPage' } as NamedNode,
	/** A preventative therapy used to prevent an initial occurrence of the medical condition, such as vaccination. */
	'primaryPrevention': { termType: 'NamedNode', value: 'https://schema.org/primaryPrevention', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/primaryPrevention' } as NamedNode,
	/** The number of the column in which the NewsArticle appears in the print edition. */
	'printColumn': { termType: 'NamedNode', value: 'https://schema.org/printColumn', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/printColumn' } as NamedNode,
	/** The edition of the print product in which the NewsArticle appears. */
	'printEdition': { termType: 'NamedNode', value: 'https://schema.org/printEdition', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/printEdition' } as NamedNode,
	/** If this NewsArticle appears in print, this field indicates the name of the page on which the article is found. Please note that this field is intended for the exact page name (e.g. A5, B18). */
	'printPage': { termType: 'NamedNode', value: 'https://schema.org/printPage', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/printPage' } as NamedNode,
	/** If this NewsArticle appears in print, this field indicates the print section in which the article appeared. */
	'printSection': { termType: 'NamedNode', value: 'https://schema.org/printSection', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/printSection' } as NamedNode,
	/** A description of the procedure involved in setting up, using, and/or installing the device. */
	'procedure': { termType: 'NamedNode', value: 'https://schema.org/procedure', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/procedure' } as NamedNode,
	/** The type of procedure, for example Surgical, Noninvasive, or Percutaneous. */
	'procedureType': { termType: 'NamedNode', value: 'https://schema.org/procedureType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/procedureType' } as NamedNode,
	/** Estimated processing time for the service using this channel. */
	'processingTime': { termType: 'NamedNode', value: 'https://schema.org/processingTime', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/processingTime' } as NamedNode,
	/** Processor architecture required to run the application (e.g. IA64). */
	'processorRequirements': { termType: 'NamedNode', value: 'https://schema.org/processorRequirements', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/processorRequirements' } as NamedNode,
	/** The person or organization who produced the work (e.g. music album, movie, TV/radio series etc.). */
	'producer': { termType: 'NamedNode', value: 'https://schema.org/producer', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/producer' } as NamedNode,
	/** The tangible thing generated by the service, e.g. a passport, permit, etc. */
	'produces': { termType: 'NamedNode', value: 'https://schema.org/produces', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/produces' } as NamedNode,
	/** Indicates a textual identifier for a ProductGroup. */
	'productGroupID': { termType: 'NamedNode', value: 'https://schema.org/productGroupID', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/productGroupID' } as NamedNode,
	/** The product identifier, such as ISBN. For example: ``` meta itemprop="productID" content="isbn:123-456-789" ```. */
	'productID': { termType: 'NamedNode', value: 'https://schema.org/productID', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/productID' } as NamedNode,
	/** The product or service this support contact point is related to (such as product support for a particular product line). This can be a specific product or product line (e.g. "iPhone") or a general category of products or services (e.g. "smartphones"). */
	'productSupported': { termType: 'NamedNode', value: 'https://schema.org/productSupported', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/productSupported' } as NamedNode,
	/** The production company or studio responsible for the item, e.g. series, video game, episode etc. */
	'productionCompany': { termType: 'NamedNode', value: 'https://schema.org/productionCompany', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/productionCompany' } as NamedNode,
	/** The date of production of the item, e.g. vehicle. */
	'productionDate': { termType: 'NamedNode', value: 'https://schema.org/productionDate', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/productionDate' } as NamedNode,
	/** Proficiency needed for this content; expected values: 'Beginner', 'Expert'. */
	'proficiencyLevel': { termType: 'NamedNode', value: 'https://schema.org/proficiencyLevel', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/proficiencyLevel' } as NamedNode,
	/** Any membership in a frequent flyer, hotel loyalty program, etc. being applied to the reservation. */
	'programMembershipUsed': { termType: 'NamedNode', value: 'https://schema.org/programMembershipUsed', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/programMembershipUsed' } as NamedNode,
	/** The program providing the membership. */
	'programName': { termType: 'NamedNode', value: 'https://schema.org/programName', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/programName' } as NamedNode,
	/** Prerequisites for enrolling in the program. */
	'programPrerequisites': { termType: 'NamedNode', value: 'https://schema.org/programPrerequisites', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/programPrerequisites' } as NamedNode,
	/** The type of educational or occupational program. For example, classroom, internship, alternance, etc. */
	'programType': { termType: 'NamedNode', value: 'https://schema.org/programType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/programType' } as NamedNode,
	/** The computer programming language. */
	'programmingLanguage': { termType: 'NamedNode', value: 'https://schema.org/programmingLanguage', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/programmingLanguage' } as NamedNode,
	/** Indicates whether API is managed or unmanaged. */
	'programmingModel': { termType: 'NamedNode', value: 'https://schema.org/programmingModel', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/programmingModel' } as NamedNode,
	/** A commonly used identifier for the characteristic represented by the property, e.g. a manufacturer or a standard code for a property. propertyID can be
(1) a prefixed string, mainly meant to be used with standards for product properties; (2) a site-specific, non-prefixed string (e.g. the primary key of the property or the vendor-specific ID of the property), or (3)
a URL indicating the type of the property, either pointing to an external vocabulary, or a Web resource that describes the property (e.g. a glossary entry).
Standards bodies should promote a standard prefix for the identifiers of properties from their standards. */
	'propertyID': { termType: 'NamedNode', value: 'https://schema.org/propertyID', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/propertyID' } as NamedNode,
	/** Proprietary name given to the diet plan, typically by its originator or creator. */
	'proprietaryName': { termType: 'NamedNode', value: 'https://schema.org/proprietaryName', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/proprietaryName' } as NamedNode,
	/** The number of grams of protein. */
	'proteinContent': { termType: 'NamedNode', value: 'https://schema.org/proteinContent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/proteinContent' } as NamedNode,
	/** The service provider, service operator, or service performer; the goods producer. Another party (a seller) may offer those services or goods on behalf of the provider. A provider may also serve as the seller. */
	'provider': { termType: 'NamedNode', value: 'https://schema.org/provider', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/provider' } as NamedNode,
	/** Indicates the mobility of a provided service (e.g. 'static', 'dynamic'). */
	'providerMobility': { termType: 'NamedNode', value: 'https://schema.org/providerMobility', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/providerMobility' } as NamedNode,
	/** The BroadcastService offered on this channel. */
	'providesBroadcastService': { termType: 'NamedNode', value: 'https://schema.org/providesBroadcastService', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/providesBroadcastService' } as NamedNode,
	/** The service provided by this channel. */
	'providesService': { termType: 'NamedNode', value: 'https://schema.org/providesService', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/providesService' } as NamedNode,
	/** A flag to signal that the [[Place]] is open to public visitors.  If this property is omitted there is no assumed default boolean value */
	'publicAccess': { termType: 'NamedNode', value: 'https://schema.org/publicAccess', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/publicAccess' } as NamedNode,
	/** Information about public transport closures. */
	'publicTransportClosuresInfo': { termType: 'NamedNode', value: 'https://schema.org/publicTransportClosuresInfo', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/publicTransportClosuresInfo' } as NamedNode,
	/** A publication event associated with the item. */
	'publication': { termType: 'NamedNode', value: 'https://schema.org/publication', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/publication' } as NamedNode,
	/** The type of the medical article, taken from the US NLM MeSH publication type catalog. See also [MeSH documentation](http://www.nlm.nih.gov/mesh/pubtypes.html). */
	'publicationType': { termType: 'NamedNode', value: 'https://schema.org/publicationType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/publicationType' } as NamedNode,
	/** An agent associated with the publication event. */
	'publishedBy': { termType: 'NamedNode', value: 'https://schema.org/publishedBy', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/publishedBy' } as NamedNode,
	/** A broadcast service associated with the publication event. */
	'publishedOn': { termType: 'NamedNode', value: 'https://schema.org/publishedOn', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/publishedOn' } as NamedNode,
	/** The publisher of the creative work. */
	'publisher': { termType: 'NamedNode', value: 'https://schema.org/publisher', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/publisher' } as NamedNode,
	/** The publishing division which published the comic. */
	'publisherImprint': { termType: 'NamedNode', value: 'https://schema.org/publisherImprint', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/publisherImprint' } as NamedNode,
	/** The publishingPrinciples property indicates (typically via [[URL]]) a document describing the editorial principles of an [[Organization]] (or individual, e.g. a [[Person]] writing a blog) that relate to their activities as a publisher, e.g. ethics or diversity policies. When applied to a [[CreativeWork]] (e.g. [[NewsArticle]]) the principles are those of the party primarily responsible for the creation of the [[CreativeWork]].

While such policies are most typically expressed in natural language, sometimes related information (e.g. indicating a [[funder]]) can be expressed using schema.org terminology.
 */
	'publishingPrinciples': { termType: 'NamedNode', value: 'https://schema.org/publishingPrinciples', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/publishingPrinciples' } as NamedNode,
	/** The date the item, e.g. vehicle, was purchased by the current owner. */
	'purchaseDate': { termType: 'NamedNode', value: 'https://schema.org/purchaseDate', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/purchaseDate' } as NamedNode,
	/** Specific qualifications required for this role or Occupation. */
	'qualifications': { termType: 'NamedNode', value: 'https://schema.org/qualifications', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/qualifications' } as NamedNode,
	/** Guidelines about quarantine rules, e.g. in the context of a pandemic. */
	'quarantineGuidelines': { termType: 'NamedNode', value: 'https://schema.org/quarantineGuidelines', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/quarantineGuidelines' } as NamedNode,
	/** A sub property of instrument. The query used on this action. */
	'query': { termType: 'NamedNode', value: 'https://schema.org/query', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/query' } as NamedNode,
	/** The task that a player-controlled character, or group of characters may complete in order to gain a reward. */
	'quest': { termType: 'NamedNode', value: 'https://schema.org/quest', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/quest' } as NamedNode,
	/** A sub property of object. A question. */
	'question': { termType: 'NamedNode', value: 'https://schema.org/question', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/question' } as NamedNode,
	/** Relates a property to a class that constitutes (one of) the expected type(s) for values of the property. */
	'rangeIncludes': { termType: 'NamedNode', value: 'https://schema.org/rangeIncludes', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/rangeIncludes' } as NamedNode,
	/** The count of total number of ratings. */
	'ratingCount': { termType: 'NamedNode', value: 'https://schema.org/ratingCount', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ratingCount' } as NamedNode,
	/** A short explanation (e.g. one to two sentences) providing background context and other information that led to the conclusion expressed in the rating. This is particularly applicable to ratings associated with "fact check" markup using [[ClaimReview]]. */
	'ratingExplanation': { termType: 'NamedNode', value: 'https://schema.org/ratingExplanation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ratingExplanation' } as NamedNode,
	/** The rating for the content.\n\nUsage guidelines:\n\n* Use values from 0123456789 (Unicode 'DIGIT ZERO' (U+0030) to 'DIGIT NINE' (U+0039)) rather than superficially similar Unicode symbols.\n* Use '.' (Unicode 'FULL STOP' (U+002E)) rather than ',' to indicate a decimal point. Avoid using these symbols as a readability separator. */
	'ratingValue': { termType: 'NamedNode', value: 'https://schema.org/ratingValue', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ratingValue' } as NamedNode,
	/** A person who reads (performs) the audiobook. */
	'readBy': { termType: 'NamedNode', value: 'https://schema.org/readBy', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/readBy' } as NamedNode,
	/** Whether or not a property is mutable.  Default is false. Specifying this for a property that also has a value makes it act similar to a "hidden" input in an HTML form. */
	'readonlyValue': { termType: 'NamedNode', value: 'https://schema.org/readonlyValue', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/readonlyValue' } as NamedNode,
	/** A sub property of participant. The real estate agent involved in the action. */
	'realEstateAgent': { termType: 'NamedNode', value: 'https://schema.org/realEstateAgent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/realEstateAgent' } as NamedNode,
	/** A sub property of instrument. The recipe/instructions used to perform the action. */
	'recipe': { termType: 'NamedNode', value: 'https://schema.org/recipe', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/recipe' } as NamedNode,
	/** The category of the recipe—for example, appetizer, entree, etc. */
	'recipeCategory': { termType: 'NamedNode', value: 'https://schema.org/recipeCategory', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/recipeCategory' } as NamedNode,
	/** The cuisine of the recipe (for example, French or Ethiopian). */
	'recipeCuisine': { termType: 'NamedNode', value: 'https://schema.org/recipeCuisine', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/recipeCuisine' } as NamedNode,
	/** A single ingredient used in the recipe, e.g. sugar, flour or garlic. */
	'recipeIngredient': { termType: 'NamedNode', value: 'https://schema.org/recipeIngredient', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/recipeIngredient' } as NamedNode,
	/** A step in making the recipe, in the form of a single item (document, video, etc.) or an ordered list with HowToStep and/or HowToSection items. */
	'recipeInstructions': { termType: 'NamedNode', value: 'https://schema.org/recipeInstructions', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/recipeInstructions' } as NamedNode,
	/** The quantity produced by the recipe (for example, number of people served, number of servings, etc). */
	'recipeYield': { termType: 'NamedNode', value: 'https://schema.org/recipeYield', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/recipeYield' } as NamedNode,
	/** A sub property of participant. The participant who is at the receiving end of the action. */
	'recipient': { termType: 'NamedNode', value: 'https://schema.org/recipient', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/recipient' } as NamedNode,
	/** An organization that acknowledges the validity, value or utility of a credential. Note: recognition may include a process of quality assurance or accreditation. */
	'recognizedBy': { termType: 'NamedNode', value: 'https://schema.org/recognizedBy', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/recognizedBy' } as NamedNode,
	/** If applicable, the organization that officially recognizes this entity as part of its endorsed system of medicine. */
	'recognizingAuthority': { termType: 'NamedNode', value: 'https://schema.org/recognizingAuthority', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/recognizingAuthority' } as NamedNode,
	/** Strength of the guideline's recommendation (e.g. 'class I'). */
	'recommendationStrength': { termType: 'NamedNode', value: 'https://schema.org/recommendationStrength', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/recommendationStrength' } as NamedNode,
	/** Recommended intake of this supplement for a given population as defined by a specific recommending authority. */
	'recommendedIntake': { termType: 'NamedNode', value: 'https://schema.org/recommendedIntake', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/recommendedIntake' } as NamedNode,
	/** The label that issued the release. */
	'recordLabel': { termType: 'NamedNode', value: 'https://schema.org/recordLabel', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/recordLabel' } as NamedNode,
	/** An audio recording of the work. */
	'recordedAs': { termType: 'NamedNode', value: 'https://schema.org/recordedAs', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/recordedAs' } as NamedNode,
	/** The Event where the CreativeWork was recorded. The CreativeWork may capture all or part of the event. */
	'recordedAt': { termType: 'NamedNode', value: 'https://schema.org/recordedAt', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/recordedAt' } as NamedNode,
	/** The CreativeWork that captured all or part of this Event. */
	'recordedIn': { termType: 'NamedNode', value: 'https://schema.org/recordedIn', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/recordedIn' } as NamedNode,
	/** The composition this track is a recording of. */
	'recordingOf': { termType: 'NamedNode', value: 'https://schema.org/recordingOf', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/recordingOf' } as NamedNode,
	/** The only way you get the money back in the event of default is the security. Recourse is where you still have the opportunity to go back to the borrower for the rest of the money. */
	'recourseLoan': { termType: 'NamedNode', value: 'https://schema.org/recourseLoan', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/recourseLoan' } as NamedNode,
	/** The reference quantity for which a certain price applies, e.g. 1 EUR per 4 kWh of electricity. This property is a replacement for unitOfMeasurement for the advanced cases where the price does not relate to a standard unit. */
	'referenceQuantity': { termType: 'NamedNode', value: 'https://schema.org/referenceQuantity', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/referenceQuantity' } as NamedNode,
	/** The Order(s) related to this Invoice. One or more Orders may be combined into a single Invoice. */
	'referencesOrder': { termType: 'NamedNode', value: 'https://schema.org/referencesOrder', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/referencesOrder' } as NamedNode,
	/** A refund type, from an enumerated list. */
	'refundType': { termType: 'NamedNode', value: 'https://schema.org/refundType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/refundType' } as NamedNode,
	/** The anatomical or organ system drained by this vessel; generally refers to a specific part of an organ. */
	'regionDrained': { termType: 'NamedNode', value: 'https://schema.org/regionDrained', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/regionDrained' } as NamedNode,
	/** The regions where the media is allowed. If not specified, then it's assumed to be allowed everywhere. Specify the countries in [ISO 3166 format](http://en.wikipedia.org/wiki/ISO_3166). */
	'regionsAllowed': { termType: 'NamedNode', value: 'https://schema.org/regionsAllowed', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/regionsAllowed' } as NamedNode,
	/** Anatomical systems or structures that relate to the superficial anatomy. */
	'relatedAnatomy': { termType: 'NamedNode', value: 'https://schema.org/relatedAnatomy', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/relatedAnatomy' } as NamedNode,
	/** A medical condition associated with this anatomy. */
	'relatedCondition': { termType: 'NamedNode', value: 'https://schema.org/relatedCondition', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/relatedCondition' } as NamedNode,
	/** Any other drug related to this one, for example commonly-prescribed alternatives. */
	'relatedDrug': { termType: 'NamedNode', value: 'https://schema.org/relatedDrug', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/relatedDrug' } as NamedNode,
	/** A link related to this web page, for example to other related web pages. */
	'relatedLink': { termType: 'NamedNode', value: 'https://schema.org/relatedLink', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/relatedLink' } as NamedNode,
	/** Related anatomical structure(s) that are not part of the system but relate or connect to it, such as vascular bundles associated with an organ system. */
	'relatedStructure': { termType: 'NamedNode', value: 'https://schema.org/relatedStructure', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/relatedStructure' } as NamedNode,
	/** A medical therapy related to this anatomy. */
	'relatedTherapy': { termType: 'NamedNode', value: 'https://schema.org/relatedTherapy', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/relatedTherapy' } as NamedNode,
	/** The most generic familial relation. */
	'relatedTo': { termType: 'NamedNode', value: 'https://schema.org/relatedTo', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/relatedTo' } as NamedNode,
	/** The release date of a product or product model. This can be used to distinguish the exact variant of a product. */
	'releaseDate': { termType: 'NamedNode', value: 'https://schema.org/releaseDate', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/releaseDate' } as NamedNode,
	/** Description of what changed in this version. */
	'releaseNotes': { termType: 'NamedNode', value: 'https://schema.org/releaseNotes', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/releaseNotes' } as NamedNode,
	/** The album this is a release of. */
	'releaseOf': { termType: 'NamedNode', value: 'https://schema.org/releaseOf', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/releaseOf' } as NamedNode,
	/** The place and time the release was issued, expressed as a PublicationEvent. */
	'releasedEvent': { termType: 'NamedNode', value: 'https://schema.org/releasedEvent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/releasedEvent' } as NamedNode,
	/** The Occupation for the JobPosting. */
	'relevantOccupation': { termType: 'NamedNode', value: 'https://schema.org/relevantOccupation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/relevantOccupation' } as NamedNode,
	/** If applicable, a medical specialty in which this entity is relevant. */
	'relevantSpecialty': { termType: 'NamedNode', value: 'https://schema.org/relevantSpecialty', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/relevantSpecialty' } as NamedNode,
	/** The number of attendee places for an event that remain unallocated. */
	'remainingAttendeeCapacity': { termType: 'NamedNode', value: 'https://schema.org/remainingAttendeeCapacity', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/remainingAttendeeCapacity' } as NamedNode,
	/** Whether the terms for payment of interest can be renegotiated during the life of the loan. */
	'renegotiableLoan': { termType: 'NamedNode', value: 'https://schema.org/renegotiableLoan', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/renegotiableLoan' } as NamedNode,
	/** Defines the number of times a recurring [[Event]] will take place. */
	'repeatCount': { termType: 'NamedNode', value: 'https://schema.org/repeatCount', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/repeatCount' } as NamedNode,
	/** Defines the frequency at which [[Event]]s will occur according to a schedule [[Schedule]]. The intervals between
      events should be defined as a [[Duration]] of time. */
	'repeatFrequency': { termType: 'NamedNode', value: 'https://schema.org/repeatFrequency', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/repeatFrequency' } as NamedNode,
	/** Number of times one should repeat the activity. */
	'repetitions': { termType: 'NamedNode', value: 'https://schema.org/repetitions', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/repetitions' } as NamedNode,
	/** A sub property of object. The object that is being replaced. */
	'replacee': { termType: 'NamedNode', value: 'https://schema.org/replacee', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/replacee' } as NamedNode,
	/** A sub property of object. The object that replaces. */
	'replacer': { termType: 'NamedNode', value: 'https://schema.org/replacer', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/replacer' } as NamedNode,
	/** The URL at which a reply may be posted to the specified UserComment. */
	'replyToUrl': { termType: 'NamedNode', value: 'https://schema.org/replyToUrl', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/replyToUrl' } as NamedNode,
	/** The number or other unique designator assigned to a Report by the publishing organization. */
	'reportNumber': { termType: 'NamedNode', value: 'https://schema.org/reportNumber', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/reportNumber' } as NamedNode,
	/** Indicates whether this image is representative of the content of the page. */
	'representativeOfPage': { termType: 'NamedNode', value: 'https://schema.org/representativeOfPage', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/representativeOfPage' } as NamedNode,
	/** Assets required to secure loan or credit repayments. It may take form of third party pledge, goods, financial instruments (cash, securities, etc.) */
	'requiredCollateral': { termType: 'NamedNode', value: 'https://schema.org/requiredCollateral', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/requiredCollateral' } as NamedNode,
	/** Audiences defined by a person's gender. */
	'requiredGender': { termType: 'NamedNode', value: 'https://schema.org/requiredGender', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/requiredGender' } as NamedNode,
	/** Audiences defined by a person's maximum age. */
	'requiredMaxAge': { termType: 'NamedNode', value: 'https://schema.org/requiredMaxAge', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/requiredMaxAge' } as NamedNode,
	/** Audiences defined by a person's minimum age. */
	'requiredMinAge': { termType: 'NamedNode', value: 'https://schema.org/requiredMinAge', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/requiredMinAge' } as NamedNode,
	/** The required quantity of the item(s). */
	'requiredQuantity': { termType: 'NamedNode', value: 'https://schema.org/requiredQuantity', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/requiredQuantity' } as NamedNode,
	/** Component dependency requirements for application. This includes runtime environments and shared libraries that are not included in the application distribution package, but required to run the application (examples: DirectX, Java or .NET runtime). */
	'requirements': { termType: 'NamedNode', value: 'https://schema.org/requirements', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/requirements' } as NamedNode,
	/** Indicates if use of the media require a subscription  (either paid or free). Allowed values are ```true``` or ```false``` (note that an earlier version had 'yes', 'no'). */
	'requiresSubscription': { termType: 'NamedNode', value: 'https://schema.org/requiresSubscription', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/requiresSubscription' } as NamedNode,
	/** The thing -- flight, event, restaurant, etc. being reserved. */
	'reservationFor': { termType: 'NamedNode', value: 'https://schema.org/reservationFor', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/reservationFor' } as NamedNode,
	/** A unique identifier for the reservation. */
	'reservationId': { termType: 'NamedNode', value: 'https://schema.org/reservationId', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/reservationId' } as NamedNode,
	/** The current status of the reservation. */
	'reservationStatus': { termType: 'NamedNode', value: 'https://schema.org/reservationStatus', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/reservationStatus' } as NamedNode,
	/** A ticket associated with the reservation. */
	'reservedTicket': { termType: 'NamedNode', value: 'https://schema.org/reservedTicket', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/reservedTicket' } as NamedNode,
	/** Responsibilities associated with this role or Occupation. */
	'responsibilities': { termType: 'NamedNode', value: 'https://schema.org/responsibilities', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/responsibilities' } as NamedNode,
	/** How often one should break from the activity. */
	'restPeriods': { termType: 'NamedNode', value: 'https://schema.org/restPeriods', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/restPeriods' } as NamedNode,
	/** Use [[MonetaryAmount]] to specify a fixed restocking fee for product returns, or use [[Number]] to specify a percentage of the product price paid by the customer. */
	'restockingFee': { termType: 'NamedNode', value: 'https://schema.org/restockingFee', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/restockingFee' } as NamedNode,
	/** The result produced in the action. E.g. John wrote *a book*. */
	'result': { termType: 'NamedNode', value: 'https://schema.org/result', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/result' } as NamedNode,
	/** A sub property of result. The Comment created or sent as a result of this action. */
	'resultComment': { termType: 'NamedNode', value: 'https://schema.org/resultComment', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/resultComment' } as NamedNode,
	/** A sub property of result. The review that resulted in the performing of the action. */
	'resultReview': { termType: 'NamedNode', value: 'https://schema.org/resultReview', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/resultReview' } as NamedNode,
	/** The type of return fees for purchased products (for any return reason). */
	'returnFees': { termType: 'NamedNode', value: 'https://schema.org/returnFees', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/returnFees' } as NamedNode,
	/** The method (from an enumeration) by which the customer obtains a return shipping label for a product returned for any reason. */
	'returnLabelSource': { termType: 'NamedNode', value: 'https://schema.org/returnLabelSource', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/returnLabelSource' } as NamedNode,
	/** The type of return method offered, specified from an enumeration. */
	'returnMethod': { termType: 'NamedNode', value: 'https://schema.org/returnMethod', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/returnMethod' } as NamedNode,
	/** Specifies an applicable return policy (from an enumeration). */
	'returnPolicyCategory': { termType: 'NamedNode', value: 'https://schema.org/returnPolicyCategory', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/returnPolicyCategory' } as NamedNode,
	/** The country where the product has to be sent to for returns, for example "Ireland" using the [[name]] property of [[Country]]. You can also provide the two-letter [ISO 3166-1 alpha-2 country code](http://en.wikipedia.org/wiki/ISO_3166-1). Note that this can be different from the country where the product was originally shipped from or sent to. */
	'returnPolicyCountry': { termType: 'NamedNode', value: 'https://schema.org/returnPolicyCountry', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/returnPolicyCountry' } as NamedNode,
	/** Seasonal override of a return policy. */
	'returnPolicySeasonalOverride': { termType: 'NamedNode', value: 'https://schema.org/returnPolicySeasonalOverride', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/returnPolicySeasonalOverride' } as NamedNode,
	/** Amount of shipping costs for product returns (for any reason). Applicable when property [[returnFees]] equals [[ReturnShippingFees]]. */
	'returnShippingFeesAmount': { termType: 'NamedNode', value: 'https://schema.org/returnShippingFeesAmount', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/returnShippingFeesAmount' } as NamedNode,
	/** A review of the item. */
	'review': { termType: 'NamedNode', value: 'https://schema.org/review', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/review' } as NamedNode,
	/** This Review or Rating is relevant to this part or facet of the itemReviewed. */
	'reviewAspect': { termType: 'NamedNode', value: 'https://schema.org/reviewAspect', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/reviewAspect' } as NamedNode,
	/** The actual body of the review. */
	'reviewBody': { termType: 'NamedNode', value: 'https://schema.org/reviewBody', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/reviewBody' } as NamedNode,
	/** The count of total number of reviews. */
	'reviewCount': { termType: 'NamedNode', value: 'https://schema.org/reviewCount', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/reviewCount' } as NamedNode,
	/** The rating given in this review. Note that reviews can themselves be rated. The ```reviewRating``` applies to rating given by the review. The [[aggregateRating]] property applies to the review itself, as a creative work. */
	'reviewRating': { termType: 'NamedNode', value: 'https://schema.org/reviewRating', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/reviewRating' } as NamedNode,
	/** People or organizations that have reviewed the content on this web page for accuracy and/or completeness. */
	'reviewedBy': { termType: 'NamedNode', value: 'https://schema.org/reviewedBy', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/reviewedBy' } as NamedNode,
	/** Review of the item. */
	'reviews': { termType: 'NamedNode', value: 'https://schema.org/reviews', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/reviews' } as NamedNode,
	/** A modifiable or non-modifiable factor that increases the risk of a patient contracting this condition, e.g. age,  coexisting condition. */
	'riskFactor': { termType: 'NamedNode', value: 'https://schema.org/riskFactor', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/riskFactor' } as NamedNode,
	/** Specific physiologic risks associated to the diet plan. */
	'risks': { termType: 'NamedNode', value: 'https://schema.org/risks', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/risks' } as NamedNode,
	/** A role played, performed or filled by a person or organization. For example, the team of creators for a comic book might fill the roles named 'inker', 'penciller', and 'letterer'; or an athlete in a SportsTeam might play in the position named 'Quarterback'. */
	'roleName': { termType: 'NamedNode', value: 'https://schema.org/roleName', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/roleName' } as NamedNode,
	/** The permitted total weight of cargo and installations (e.g. a roof rack) on top of the vehicle.\n\nTypical unit code(s): KGM for kilogram, LBR for pound\n\n* Note 1: You can indicate additional information in the [[name]] of the [[QuantitativeValue]] node.\n* Note 2: You may also link to a [[QualitativeValue]] node that provides additional information using [[valueReference]]\n* Note 3: Note that you can use [[minValue]] and [[maxValue]] to indicate ranges. */
	'roofLoad': { termType: 'NamedNode', value: 'https://schema.org/roofLoad', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/roofLoad' } as NamedNode,
	/** The response (yes, no, maybe) to the RSVP. */
	'rsvpResponse': { termType: 'NamedNode', value: 'https://schema.org/rsvpResponse', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/rsvpResponse' } as NamedNode,
	/** The vasculature the lymphatic structure runs, or efferents, to. */
	'runsTo': { termType: 'NamedNode', value: 'https://schema.org/runsTo', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/runsTo' } as NamedNode,
	/** Runtime platform or script interpreter dependencies (example: Java v1, Python 2.3, .NET Framework 3.0). */
	'runtime': { termType: 'NamedNode', value: 'https://schema.org/runtime', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/runtime' } as NamedNode,
	/** Runtime platform or script interpreter dependencies (example: Java v1, Python 2.3, .NET Framework 3.0). */
	'runtimePlatform': { termType: 'NamedNode', value: 'https://schema.org/runtimePlatform', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/runtimePlatform' } as NamedNode,
	/** The RxCUI drug identifier from RXNORM. */
	'rxcui': { termType: 'NamedNode', value: 'https://schema.org/rxcui', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/rxcui' } as NamedNode,
	/** Any potential safety concern associated with the supplement. May include interactions with other drugs and foods, pregnancy, breastfeeding, known adverse reactions, and documented efficacy of the supplement. */
	'safetyConsideration': { termType: 'NamedNode', value: 'https://schema.org/safetyConsideration', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/safetyConsideration' } as NamedNode,
	/** The currency (coded using [ISO 4217](http://en.wikipedia.org/wiki/ISO_4217)) used for the main salary information in this job posting or for this employee. */
	'salaryCurrency': { termType: 'NamedNode', value: 'https://schema.org/salaryCurrency', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/salaryCurrency' } as NamedNode,
	/** The expected salary upon completing the training. */
	'salaryUponCompletion': { termType: 'NamedNode', value: 'https://schema.org/salaryUponCompletion', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/salaryUponCompletion' } as NamedNode,
	/** URL of a reference Web page that unambiguously indicates the item's identity. E.g. the URL of the item's Wikipedia page, Wikidata entry, or official website. */
	'sameAs': { termType: 'NamedNode', value: 'https://schema.org/sameAs', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/sameAs' } as NamedNode,
	/** What type of code sample: full (compile ready) solution, code snippet, inline code, scripts, template. */
	'sampleType': { termType: 'NamedNode', value: 'https://schema.org/sampleType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/sampleType' } as NamedNode,
	/** The number of grams of saturated fat. */
	'saturatedFatContent': { termType: 'NamedNode', value: 'https://schema.org/saturatedFatContent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/saturatedFatContent' } as NamedNode,
	/** Indicates the timezone for which the time(s) indicated in the [[Schedule]] are given. The value provided should be among those listed in the IANA Time Zone Database. */
	'scheduleTimezone': { termType: 'NamedNode', value: 'https://schema.org/scheduleTimezone', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/scheduleTimezone' } as NamedNode,
	/** The date the invoice is scheduled to be paid. */
	'scheduledPaymentDate': { termType: 'NamedNode', value: 'https://schema.org/scheduledPaymentDate', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/scheduledPaymentDate' } as NamedNode,
	/** The time the object is scheduled to. */
	'scheduledTime': { termType: 'NamedNode', value: 'https://schema.org/scheduledTime', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/scheduledTime' } as NamedNode,
	/** Indicates (by URL or string) a particular version of a schema used in some CreativeWork. This property was created primarily to
    indicate the use of a specific schema.org release, e.g. ```10.0``` as a simple string, or more explicitly via URL, ```https://schema.org/docs/releases.html#v10.0```. There may be situations in which other schemas might usefully be referenced this way, e.g. ```http://dublincore.org/specifications/dublin-core/dces/1999-07-02/``` but this has not been carefully explored in the community. */
	'schemaVersion': { termType: 'NamedNode', value: 'https://schema.org/schemaVersion', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/schemaVersion' } as NamedNode,
	/** Information about school closures. */
	'schoolClosuresInfo': { termType: 'NamedNode', value: 'https://schema.org/schoolClosuresInfo', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/schoolClosuresInfo' } as NamedNode,
	/** The number of screens in the movie theater. */
	'screenCount': { termType: 'NamedNode', value: 'https://schema.org/screenCount', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/screenCount' } as NamedNode,
	/** A link to a screenshot image of the app. */
	'screenshot': { termType: 'NamedNode', value: 'https://schema.org/screenshot', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/screenshot' } as NamedNode,
	/** Indicates the date on which the current structured data was generated / published. Typically used alongside [[sdPublisher]] */
	'sdDatePublished': { termType: 'NamedNode', value: 'https://schema.org/sdDatePublished', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/sdDatePublished' } as NamedNode,
	/** A license document that applies to this structured data, typically indicated by URL. */
	'sdLicense': { termType: 'NamedNode', value: 'https://schema.org/sdLicense', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/sdLicense' } as NamedNode,
	/** Indicates the party responsible for generating and publishing the current structured data markup, typically in cases where the structured data is derived automatically from existing published content but published on a different site. For example, student projects and open data initiatives often re-publish existing content with more explicitly structured metadata. The
[[sdPublisher]] property helps make such practices more explicit. */
	'sdPublisher': { termType: 'NamedNode', value: 'https://schema.org/sdPublisher', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/sdPublisher' } as NamedNode,
	/** A season in a media series. */
	'season': { termType: 'NamedNode', value: 'https://schema.org/season', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/season' } as NamedNode,
	/** Position of the season within an ordered group of seasons. */
	'seasonNumber': { termType: 'NamedNode', value: 'https://schema.org/seasonNumber', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/seasonNumber' } as NamedNode,
	/** A season in a media series. */
	'seasons': { termType: 'NamedNode', value: 'https://schema.org/seasons', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/seasons' } as NamedNode,
	/** The location of the reserved seat (e.g., 27). */
	'seatNumber': { termType: 'NamedNode', value: 'https://schema.org/seatNumber', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/seatNumber' } as NamedNode,
	/** The row location of the reserved seat (e.g., B). */
	'seatRow': { termType: 'NamedNode', value: 'https://schema.org/seatRow', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/seatRow' } as NamedNode,
	/** The section location of the reserved seat (e.g. Orchestra). */
	'seatSection': { termType: 'NamedNode', value: 'https://schema.org/seatSection', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/seatSection' } as NamedNode,
	/** The number of persons that can be seated (e.g. in a vehicle), both in terms of the physical space available, and in terms of limitations set by law.\n\nTypical unit code(s): C62 for persons  */
	'seatingCapacity': { termType: 'NamedNode', value: 'https://schema.org/seatingCapacity', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/seatingCapacity' } as NamedNode,
	/** The type/class of the seat. */
	'seatingType': { termType: 'NamedNode', value: 'https://schema.org/seatingType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/seatingType' } as NamedNode,
	/** A preventative therapy used to prevent reoccurrence of the medical condition after an initial episode of the condition. */
	'secondaryPrevention': { termType: 'NamedNode', value: 'https://schema.org/secondaryPrevention', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/secondaryPrevention' } as NamedNode,
	/** A description of any security clearance requirements of the job. */
	'securityClearanceRequirement': { termType: 'NamedNode', value: 'https://schema.org/securityClearanceRequirement', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/securityClearanceRequirement' } as NamedNode,
	/** The type of security screening the passenger is subject to. */
	'securityScreening': { termType: 'NamedNode', value: 'https://schema.org/securityScreening', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/securityScreening' } as NamedNode,
	/** A pointer to products or services sought by the organization or person (demand). */
	'seeks': { termType: 'NamedNode', value: 'https://schema.org/seeks', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/seeks' } as NamedNode,
	/** An entity which offers (sells / leases / lends / loans) the services / goods.  A seller may also be a provider. */
	'seller': { termType: 'NamedNode', value: 'https://schema.org/seller', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/seller' } as NamedNode,
	/** A sub property of participant. The participant who is at the sending end of the action. */
	'sender': { termType: 'NamedNode', value: 'https://schema.org/sender', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/sender' } as NamedNode,
	/** A description of any sensory requirements and levels necessary to function on the job, including hearing and vision. Defined terms such as those in O*net may be used, but note that there is no way to specify the level of ability as well as its nature when using a defined term. */
	'sensoryRequirement': { termType: 'NamedNode', value: 'https://schema.org/sensoryRequirement', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/sensoryRequirement' } as NamedNode,
	/** The neurological pathway extension that inputs and sends information to the brain or spinal cord. */
	'sensoryUnit': { termType: 'NamedNode', value: 'https://schema.org/sensoryUnit', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/sensoryUnit' } as NamedNode,
	/** The serial number or any alphanumeric identifier of a particular product. When attached to an offer, it is a shortcut for the serial number of the product included in the offer. */
	'serialNumber': { termType: 'NamedNode', value: 'https://schema.org/serialNumber', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/serialNumber' } as NamedNode,
	/** A possible serious complication and/or serious side effect of this therapy. Serious adverse outcomes include those that are life-threatening; result in death, disability, or permanent damage; require hospitalization or prolong existing hospitalization; cause congenital anomalies or birth defects; or jeopardize the patient and may require medical or surgical intervention to prevent one of the outcomes in this definition. */
	'seriousAdverseOutcome': { termType: 'NamedNode', value: 'https://schema.org/seriousAdverseOutcome', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/seriousAdverseOutcome' } as NamedNode,
	/** Status of a game server. */
	'serverStatus': { termType: 'NamedNode', value: 'https://schema.org/serverStatus', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/serverStatus' } as NamedNode,
	/** The cuisine of the restaurant. */
	'servesCuisine': { termType: 'NamedNode', value: 'https://schema.org/servesCuisine', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/servesCuisine' } as NamedNode,
	/** The geographic area where the service is provided. */
	'serviceArea': { termType: 'NamedNode', value: 'https://schema.org/serviceArea', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/serviceArea' } as NamedNode,
	/** The audience eligible for this service. */
	'serviceAudience': { termType: 'NamedNode', value: 'https://schema.org/serviceAudience', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/serviceAudience' } as NamedNode,
	/** The location (e.g. civic structure, local business, etc.) where a person can go to access the service. */
	'serviceLocation': { termType: 'NamedNode', value: 'https://schema.org/serviceLocation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/serviceLocation' } as NamedNode,
	/** The operating organization, if different from the provider.  This enables the representation of services that are provided by an organization, but operated by another organization like a subcontractor. */
	'serviceOperator': { termType: 'NamedNode', value: 'https://schema.org/serviceOperator', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/serviceOperator' } as NamedNode,
	/** The tangible thing generated by the service, e.g. a passport, permit, etc. */
	'serviceOutput': { termType: 'NamedNode', value: 'https://schema.org/serviceOutput', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/serviceOutput' } as NamedNode,
	/** The phone number to use to access the service. */
	'servicePhone': { termType: 'NamedNode', value: 'https://schema.org/servicePhone', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/servicePhone' } as NamedNode,
	/** The address for accessing the service by mail. */
	'servicePostalAddress': { termType: 'NamedNode', value: 'https://schema.org/servicePostalAddress', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/servicePostalAddress' } as NamedNode,
	/** The number to access the service by text message. */
	'serviceSmsNumber': { termType: 'NamedNode', value: 'https://schema.org/serviceSmsNumber', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/serviceSmsNumber' } as NamedNode,
	/** The type of service being offered, e.g. veterans' benefits, emergency relief, etc. */
	'serviceType': { termType: 'NamedNode', value: 'https://schema.org/serviceType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/serviceType' } as NamedNode,
	/** The website to access the service. */
	'serviceUrl': { termType: 'NamedNode', value: 'https://schema.org/serviceUrl', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/serviceUrl' } as NamedNode,
	/** The serving size, in terms of the number of volume or mass. */
	'servingSize': { termType: 'NamedNode', value: 'https://schema.org/servingSize', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/servingSize' } as NamedNode,
	/** The [SHA-2](https://en.wikipedia.org/wiki/SHA-2) SHA256 hash of the content of the item. For example, a zero-length input has value 'e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855' */
	'sha256': { termType: 'NamedNode', value: 'https://schema.org/sha256', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/sha256' } as NamedNode,
	/** A CreativeWork such as an image, video, or audio clip shared as part of this posting. */
	'sharedContent': { termType: 'NamedNode', value: 'https://schema.org/sharedContent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/sharedContent' } as NamedNode,
	/** indicates (possibly multiple) shipping destinations. These can be defined in several ways, e.g. postalCode ranges. */
	'shippingDestination': { termType: 'NamedNode', value: 'https://schema.org/shippingDestination', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/shippingDestination' } as NamedNode,
	/** Indicates information about the shipping policies and options associated with an [[Offer]]. */
	'shippingDetails': { termType: 'NamedNode', value: 'https://schema.org/shippingDetails', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/shippingDetails' } as NamedNode,
	/** Label to match an [[OfferShippingDetails]] with a [[ShippingRateSettings]] (within the context of a [[shippingSettingsLink]] cross-reference). */
	'shippingLabel': { termType: 'NamedNode', value: 'https://schema.org/shippingLabel', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/shippingLabel' } as NamedNode,
	/** Indicates the origin of a shipment, i.e. where it should be coming from. */
	'shippingOrigin': { termType: 'NamedNode', value: 'https://schema.org/shippingOrigin', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/shippingOrigin' } as NamedNode,
	/** The shipping rate is the cost of shipping to the specified destination. Typically, the maxValue and currency values (of the [[MonetaryAmount]]) are most appropriate. */
	'shippingRate': { termType: 'NamedNode', value: 'https://schema.org/shippingRate', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/shippingRate' } as NamedNode,
	/** Link to a page containing [[ShippingRateSettings]] and [[DeliveryTimeSettings]] details. */
	'shippingSettingsLink': { termType: 'NamedNode', value: 'https://schema.org/shippingSettingsLink', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/shippingSettingsLink' } as NamedNode,
	/** A sibling of the person. */
	'sibling': { termType: 'NamedNode', value: 'https://schema.org/sibling', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/sibling' } as NamedNode,
	/** A sibling of the person. */
	'siblings': { termType: 'NamedNode', value: 'https://schema.org/siblings', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/siblings' } as NamedNode,
	/** A sign detected by the test. */
	'signDetected': { termType: 'NamedNode', value: 'https://schema.org/signDetected', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/signDetected' } as NamedNode,
	/** A sign or symptom of this condition. Signs are objective or physically observable manifestations of the medical condition while symptoms are the subjective experience of the medical condition. */
	'signOrSymptom': { termType: 'NamedNode', value: 'https://schema.org/signOrSymptom', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/signOrSymptom' } as NamedNode,
	/** The significance associated with the superficial anatomy; as an example, how characteristics of the superficial anatomy can suggest underlying medical conditions or courses of treatment. */
	'significance': { termType: 'NamedNode', value: 'https://schema.org/significance', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/significance' } as NamedNode,
	/** One of the more significant URLs on the page. Typically, these are the non-navigation links that are clicked on the most. */
	'significantLink': { termType: 'NamedNode', value: 'https://schema.org/significantLink', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/significantLink' } as NamedNode,
	/** The most significant URLs on the page. Typically, these are the non-navigation links that are clicked on the most. */
	'significantLinks': { termType: 'NamedNode', value: 'https://schema.org/significantLinks', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/significantLinks' } as NamedNode,
	/** A standardized size of a product or creative work, specified either through a simple textual string (for example 'XL', '32Wx34L'), a  QuantitativeValue with a unitCode, or a comprehensive and structured [[SizeSpecification]]; in other cases, the [[width]], [[height]], [[depth]] and [[weight]] properties may be more applicable.  */
	'size': { termType: 'NamedNode', value: 'https://schema.org/size', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/size' } as NamedNode,
	/** The size group (also known as "size type") for a product's size. Size groups are common in the fashion industry to define size segments and suggested audiences for wearable products. Multiple values can be combined, for example "men's big and tall", "petite maternity" or "regular" */
	'sizeGroup': { termType: 'NamedNode', value: 'https://schema.org/sizeGroup', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/sizeGroup' } as NamedNode,
	/** The size system used to identify a product's size. Typically either a standard (for example, "GS1" or "ISO-EN13402"), country code (for example "US" or "JP"), or a measuring system (for example "Metric" or "Imperial"). */
	'sizeSystem': { termType: 'NamedNode', value: 'https://schema.org/sizeSystem', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/sizeSystem' } as NamedNode,
	/** A statement of knowledge, skill, ability, task or any other assertion expressing a competency that is desired or required to fulfill this role or to work in this occupation. */
	'skills': { termType: 'NamedNode', value: 'https://schema.org/skills', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/skills' } as NamedNode,
	/** The Stock Keeping Unit (SKU), i.e. a merchant-specific identifier for a product or service, or the product to which the offer refers. */
	'sku': { termType: 'NamedNode', value: 'https://schema.org/sku', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/sku' } as NamedNode,
	/** A slogan or motto associated with the item. */
	'slogan': { termType: 'NamedNode', value: 'https://schema.org/slogan', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/slogan' } as NamedNode,
	/** A specification in form of a line notation for describing the structure of chemical species using short ASCII strings.  Double bond stereochemistry \ indicators may need to be escaped in the string in formats where the backslash is an escape character. */
	'smiles': { termType: 'NamedNode', value: 'https://schema.org/smiles', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/smiles' } as NamedNode,
	/** Indicates whether it is allowed to smoke in the place, e.g. in the restaurant, hotel or hotel room. */
	'smokingAllowed': { termType: 'NamedNode', value: 'https://schema.org/smokingAllowed', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/smokingAllowed' } as NamedNode,
	/** The number of milligrams of sodium. */
	'sodiumContent': { termType: 'NamedNode', value: 'https://schema.org/sodiumContent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/sodiumContent' } as NamedNode,
	/** Additional content for a software application. */
	'softwareAddOn': { termType: 'NamedNode', value: 'https://schema.org/softwareAddOn', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/softwareAddOn' } as NamedNode,
	/** Software application help. */
	'softwareHelp': { termType: 'NamedNode', value: 'https://schema.org/softwareHelp', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/softwareHelp' } as NamedNode,
	/** Component dependency requirements for application. This includes runtime environments and shared libraries that are not included in the application distribution package, but required to run the application (examples: DirectX, Java or .NET runtime). */
	'softwareRequirements': { termType: 'NamedNode', value: 'https://schema.org/softwareRequirements', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/softwareRequirements' } as NamedNode,
	/** Version of the software instance. */
	'softwareVersion': { termType: 'NamedNode', value: 'https://schema.org/softwareVersion', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/softwareVersion' } as NamedNode,
	/** The Organization on whose behalf the creator was working. */
	'sourceOrganization': { termType: 'NamedNode', value: 'https://schema.org/sourceOrganization', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/sourceOrganization' } as NamedNode,
	/** The neurological pathway that originates the neurons. */
	'sourcedFrom': { termType: 'NamedNode', value: 'https://schema.org/sourcedFrom', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/sourcedFrom' } as NamedNode,
	/** The "spatial" property can be used in cases when more specific properties
(e.g. [[locationCreated]], [[spatialCoverage]], [[contentLocation]]) are not known to be appropriate. */
	'spatial': { termType: 'NamedNode', value: 'https://schema.org/spatial', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/spatial' } as NamedNode,
	/** The spatialCoverage of a CreativeWork indicates the place(s) which are the focus of the content. It is a subproperty of
      contentLocation intended primarily for more technical and detailed materials. For example with a Dataset, it indicates
      areas that the dataset describes: a dataset of New York weather would have spatialCoverage which was the place: the state of New York. */
	'spatialCoverage': { termType: 'NamedNode', value: 'https://schema.org/spatialCoverage', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/spatialCoverage' } as NamedNode,
	/** Indicates sections of a Web page that are particularly 'speakable' in the sense of being highlighted as being especially appropriate for text-to-speech conversion. Other sections of a page may also be usefully spoken in particular circumstances; the 'speakable' property serves to indicate the parts most likely to be generally useful for speech.

The *speakable* property can be repeated an arbitrary number of times, with three kinds of possible 'content-locator' values:

1.) *id-value* URL references - uses *id-value* of an element in the page being annotated. The simplest use of *speakable* has (potentially relative) URL values, referencing identified sections of the document concerned.

2.) CSS Selectors - addresses content in the annotated page, e.g. via class attribute. Use the [[cssSelector]] property.

3.)  XPaths - addresses content via XPaths (assuming an XML view of the content). Use the [[xpath]] property.


For more sophisticated markup of speakable sections beyond simple ID references, either CSS selectors or XPath expressions to pick out document section(s) as speakable. For this
we define a supporting type, [[SpeakableSpecification]]  which is defined to be a possible value of the *speakable* property.
          */
	'speakable': { termType: 'NamedNode', value: 'https://schema.org/speakable', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/speakable' } as NamedNode,
	/** Any special commitments associated with this job posting. Valid entries include VeteranCommit, MilitarySpouseCommit, etc. */
	'specialCommitments': { termType: 'NamedNode', value: 'https://schema.org/specialCommitments', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/specialCommitments' } as NamedNode,
	/** The special opening hours of a certain place.\n\nUse this to explicitly override general opening hours brought in scope by [[openingHoursSpecification]] or [[openingHours]].
       */
	'specialOpeningHoursSpecification': { termType: 'NamedNode', value: 'https://schema.org/specialOpeningHoursSpecification', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/specialOpeningHoursSpecification' } as NamedNode,
	/** One of the domain specialities to which this web page's content applies. */
	'specialty': { termType: 'NamedNode', value: 'https://schema.org/specialty', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/specialty' } as NamedNode,
	/** Form of markup used. eg. [SSML](https://www.w3.org/TR/speech-synthesis11) or [IPA](https://www.wikidata.org/wiki/Property:P898). */
	'speechToTextMarkup': { termType: 'NamedNode', value: 'https://schema.org/speechToTextMarkup', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/speechToTextMarkup' } as NamedNode,
	/** The speed range of the vehicle. If the vehicle is powered by an engine, the upper limit of the speed range (indicated by [[maxValue]]) should be the maximum speed achievable under regular conditions.\n\nTypical unit code(s): KMH for km/h, HM for mile per hour (0.447 04 m/s), KNT for knot\n\n*Note 1: Use [[minValue]] and [[maxValue]] to indicate the range. Typically, the minimal value is zero.\n* Note 2: There are many different ways of measuring the speed range. You can link to information about how the given value has been determined using the [[valueReference]] property. */
	'speed': { termType: 'NamedNode', value: 'https://schema.org/speed', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/speed' } as NamedNode,
	/** The (e.g. fictional) character, Person or Organization to whom the quotation is attributed within the containing CreativeWork. */
	'spokenByCharacter': { termType: 'NamedNode', value: 'https://schema.org/spokenByCharacter', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/spokenByCharacter' } as NamedNode,
	/** A person or organization that supports a thing through a pledge, promise, or financial contribution. E.g. a sponsor of a Medical Study or a corporate sponsor of an event. */
	'sponsor': { termType: 'NamedNode', value: 'https://schema.org/sponsor', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/sponsor' } as NamedNode,
	/** A type of sport (e.g. Baseball). */
	'sport': { termType: 'NamedNode', value: 'https://schema.org/sport', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/sport' } as NamedNode,
	/** A sub property of location. The sports activity location where this action occurred. */
	'sportsActivityLocation': { termType: 'NamedNode', value: 'https://schema.org/sportsActivityLocation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/sportsActivityLocation' } as NamedNode,
	/** A sub property of location. The sports event where this action occurred. */
	'sportsEvent': { termType: 'NamedNode', value: 'https://schema.org/sportsEvent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/sportsEvent' } as NamedNode,
	/** A sub property of participant. The sports team that participated on this action. */
	'sportsTeam': { termType: 'NamedNode', value: 'https://schema.org/sportsTeam', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/sportsTeam' } as NamedNode,
	/** The person's spouse. */
	'spouse': { termType: 'NamedNode', value: 'https://schema.org/spouse', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/spouse' } as NamedNode,
	/** The stage of the condition, if applicable. */
	'stage': { termType: 'NamedNode', value: 'https://schema.org/stage', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/stage' } as NamedNode,
	/** The stage represented as a number, e.g. 3. */
	'stageAsNumber': { termType: 'NamedNode', value: 'https://schema.org/stageAsNumber', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/stageAsNumber' } as NamedNode,
	/** An official rating for a lodging business or food establishment, e.g. from national associations or standards bodies. Use the author property to indicate the rating organization, e.g. as an Organization with name such as (e.g. HOTREC, DEHOGA, WHR, or Hotelstars). */
	'starRating': { termType: 'NamedNode', value: 'https://schema.org/starRating', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/starRating' } as NamedNode,
	/** The start date and time of the item (in [ISO 8601 date format](http://en.wikipedia.org/wiki/ISO_8601)). */
	'startDate': { termType: 'NamedNode', value: 'https://schema.org/startDate', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/startDate' } as NamedNode,
	/** The start time of the clip expressed as the number of seconds from the beginning of the work. */
	'startOffset': { termType: 'NamedNode', value: 'https://schema.org/startOffset', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/startOffset' } as NamedNode,
	/** The startTime of something. For a reserved event or service (e.g. FoodEstablishmentReservation), the time that it is expected to start. For actions that span a period of time, when the action was performed. E.g. John wrote a book from *January* to December. For media, including audio and video, it's the time offset of the start of a clip within a larger file.\n\nNote that Event uses startDate/endDate instead of startTime/endTime, even when describing dates with times. This situation may be clarified in future revisions. */
	'startTime': { termType: 'NamedNode', value: 'https://schema.org/startTime', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/startTime' } as NamedNode,
	/** Indicates the kind of statistic represented by a [[StatisticalVariable]], e.g. mean, count etc. The value of statType is a property, either from within Schema.org (e.g. [[count]], [[median]], [[marginOfError]], [[maxValue]], [[minValue]]) or from other compatible (e.g. RDF) systems such as DataCommons.org or Wikidata.org.  */
	'statType': { termType: 'NamedNode', value: 'https://schema.org/statType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/statType' } as NamedNode,
	/** The status of the study (enumerated). */
	'status': { termType: 'NamedNode', value: 'https://schema.org/status', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/status' } as NamedNode,
	/** The position of the steering wheel or similar device (mostly for cars). */
	'steeringPosition': { termType: 'NamedNode', value: 'https://schema.org/steeringPosition', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/steeringPosition' } as NamedNode,
	/** A single step item (as HowToStep, text, document, video, etc.) or a HowToSection. */
	'step': { termType: 'NamedNode', value: 'https://schema.org/step', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/step' } as NamedNode,
	/** The stepValue attribute indicates the granularity that is expected (and required) of the value in a PropertyValueSpecification. */
	'stepValue': { termType: 'NamedNode', value: 'https://schema.org/stepValue', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/stepValue' } as NamedNode,
	/** A single step item (as HowToStep, text, document, video, etc.) or a HowToSection (originally misnamed 'steps'; 'step' is preferred). */
	'steps': { termType: 'NamedNode', value: 'https://schema.org/steps', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/steps' } as NamedNode,
	/** Storage requirements (free space required). */
	'storageRequirements': { termType: 'NamedNode', value: 'https://schema.org/storageRequirements', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/storageRequirements' } as NamedNode,
	/** The street address. For example, 1600 Amphitheatre Pkwy. */
	'streetAddress': { termType: 'NamedNode', value: 'https://schema.org/streetAddress', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/streetAddress' } as NamedNode,
	/** The units of an active ingredient's strength, e.g. mg. */
	'strengthUnit': { termType: 'NamedNode', value: 'https://schema.org/strengthUnit', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/strengthUnit' } as NamedNode,
	/** The value of an active ingredient's strength, e.g. 325. */
	'strengthValue': { termType: 'NamedNode', value: 'https://schema.org/strengthValue', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/strengthValue' } as NamedNode,
	/** The name given to how bone physically connects to each other. */
	'structuralClass': { termType: 'NamedNode', value: 'https://schema.org/structuralClass', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/structuralClass' } as NamedNode,
	/** A medical study or trial related to this entity. */
	'study': { termType: 'NamedNode', value: 'https://schema.org/study', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/study' } as NamedNode,
	/** Specifics about the observational study design (enumerated). */
	'studyDesign': { termType: 'NamedNode', value: 'https://schema.org/studyDesign', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/studyDesign' } as NamedNode,
	/** The location in which the study is taking/took place. */
	'studyLocation': { termType: 'NamedNode', value: 'https://schema.org/studyLocation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/studyLocation' } as NamedNode,
	/** A subject of the study, i.e. one of the medical conditions, therapies, devices, drugs, etc. investigated by the study. */
	'studySubject': { termType: 'NamedNode', value: 'https://schema.org/studySubject', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/studySubject' } as NamedNode,
	/** An Event that is part of this event. For example, a conference event includes many presentations, each of which is a subEvent of the conference. */
	'subEvent': { termType: 'NamedNode', value: 'https://schema.org/subEvent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/subEvent' } as NamedNode,
	/** Events that are a part of this event. For example, a conference event includes many presentations, each subEvents of the conference. */
	'subEvents': { termType: 'NamedNode', value: 'https://schema.org/subEvents', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/subEvents' } as NamedNode,
	/** A relationship between two organizations where the first includes the second, e.g., as a subsidiary. See also: the more specific 'department' property. */
	'subOrganization': { termType: 'NamedNode', value: 'https://schema.org/subOrganization', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/subOrganization' } as NamedNode,
	/** The individual reservations included in the package. Typically a repeated property. */
	'subReservation': { termType: 'NamedNode', value: 'https://schema.org/subReservation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/subReservation' } as NamedNode,
	/** The substage, e.g. 'a' for Stage IIIa. */
	'subStageSuffix': { termType: 'NamedNode', value: 'https://schema.org/subStageSuffix', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/subStageSuffix' } as NamedNode,
	/** Component (sub-)structure(s) that comprise this anatomical structure. */
	'subStructure': { termType: 'NamedNode', value: 'https://schema.org/subStructure', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/subStructure' } as NamedNode,
	/** A component test of the panel. */
	'subTest': { termType: 'NamedNode', value: 'https://schema.org/subTest', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/subTest' } as NamedNode,
	/** Identifies a [[Trip]] that is a subTrip of this Trip.  For example Day 1, Day 2, etc. of a multi-day trip. */
	'subTrip': { termType: 'NamedNode', value: 'https://schema.org/subTrip', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/subTrip' } as NamedNode,
	/** A CreativeWork or Event about this Thing. */
	'subjectOf': { termType: 'NamedNode', value: 'https://schema.org/subjectOf', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/subjectOf' } as NamedNode,
	/** Languages in which subtitles/captions are available, in [IETF BCP 47 standard format](http://tools.ietf.org/html/bcp47). */
	'subtitleLanguage': { termType: 'NamedNode', value: 'https://schema.org/subtitleLanguage', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/subtitleLanguage' } as NamedNode,
	/** A pointer from a newer variant of a product  to its previous, often discontinued predecessor. */
	'successorOf': { termType: 'NamedNode', value: 'https://schema.org/successorOf', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/successorOf' } as NamedNode,
	/** The number of grams of sugar. */
	'sugarContent': { termType: 'NamedNode', value: 'https://schema.org/sugarContent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/sugarContent' } as NamedNode,
	/** The age or age range for the intended audience or person, for example 3-12 months for infants, 1-5 years for toddlers. */
	'suggestedAge': { termType: 'NamedNode', value: 'https://schema.org/suggestedAge', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/suggestedAge' } as NamedNode,
	/** An answer (possibly one of several, possibly incorrect) to a Question, e.g. on a Question/Answer site. */
	'suggestedAnswer': { termType: 'NamedNode', value: 'https://schema.org/suggestedAnswer', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/suggestedAnswer' } as NamedNode,
	/** The suggested gender of the intended person or audience, for example "male", "female", or "unisex". */
	'suggestedGender': { termType: 'NamedNode', value: 'https://schema.org/suggestedGender', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/suggestedGender' } as NamedNode,
	/** Maximum recommended age in years for the audience or user. */
	'suggestedMaxAge': { termType: 'NamedNode', value: 'https://schema.org/suggestedMaxAge', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/suggestedMaxAge' } as NamedNode,
	/** A suggested range of body measurements for the intended audience or person, for example inseam between 32 and 34 inches or height between 170 and 190 cm. Typically found on a size chart for wearable products. */
	'suggestedMeasurement': { termType: 'NamedNode', value: 'https://schema.org/suggestedMeasurement', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/suggestedMeasurement' } as NamedNode,
	/** Minimum recommended age in years for the audience or user. */
	'suggestedMinAge': { termType: 'NamedNode', value: 'https://schema.org/suggestedMinAge', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/suggestedMinAge' } as NamedNode,
	/** Indicates a dietary restriction or guideline for which this recipe or menu item is suitable, e.g. diabetic, halal etc. */
	'suitableForDiet': { termType: 'NamedNode', value: 'https://schema.org/suitableForDiet', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/suitableForDiet' } as NamedNode,
	/** An event that this event is a part of. For example, a collection of individual music performances might each have a music festival as their superEvent. */
	'superEvent': { termType: 'NamedNode', value: 'https://schema.org/superEvent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/superEvent' } as NamedNode,
	/** Relates a term (i.e. a property, class or enumeration) to one that supersedes it. */
	'supersededBy': { termType: 'NamedNode', value: 'https://schema.org/supersededBy', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/supersededBy' } as NamedNode,
	/** A sub-property of instrument. A supply consumed when performing instructions or a direction. */
	'supply': { termType: 'NamedNode', value: 'https://schema.org/supply', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/supply' } as NamedNode,
	/** The area to which the artery supplies blood. */
	'supplyTo': { termType: 'NamedNode', value: 'https://schema.org/supplyTo', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/supplyTo' } as NamedNode,
	/** Supporting data for a SoftwareApplication. */
	'supportingData': { termType: 'NamedNode', value: 'https://schema.org/supportingData', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/supportingData' } as NamedNode,
	/** A material used as a surface in some artwork, e.g. Canvas, Paper, Wood, Board, etc. */
	'surface': { termType: 'NamedNode', value: 'https://schema.org/surface', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/surface' } as NamedNode,
	/** Indicates (typically several) Syllabus entities that lay out what each section of the overall course will cover. */
	'syllabusSections': { termType: 'NamedNode', value: 'https://schema.org/syllabusSections', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/syllabusSections' } as NamedNode,
	/** Indicates a target EntryPoint, or url, for an Action. */
	'target': { termType: 'NamedNode', value: 'https://schema.org/target', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/target' } as NamedNode,
	/** A sub property of object. The collection target of the action. */
	'targetCollection': { termType: 'NamedNode', value: 'https://schema.org/targetCollection', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/targetCollection' } as NamedNode,
	/** The description of a node in an established educational framework. */
	'targetDescription': { termType: 'NamedNode', value: 'https://schema.org/targetDescription', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/targetDescription' } as NamedNode,
	/** The name of a node in an established educational framework. */
	'targetName': { termType: 'NamedNode', value: 'https://schema.org/targetName', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/targetName' } as NamedNode,
	/** Type of app development: phone, Metro style, desktop, XBox, etc. */
	'targetPlatform': { termType: 'NamedNode', value: 'https://schema.org/targetPlatform', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/targetPlatform' } as NamedNode,
	/** Characteristics of the population for which this is intended, or which typically uses it, e.g. 'adults'. */
	'targetPopulation': { termType: 'NamedNode', value: 'https://schema.org/targetPopulation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/targetPopulation' } as NamedNode,
	/** Target Operating System / Product to which the code applies.  If applies to several versions, just the product name can be used. */
	'targetProduct': { termType: 'NamedNode', value: 'https://schema.org/targetProduct', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/targetProduct' } as NamedNode,
	/** The URL of a node in an established educational framework. */
	'targetUrl': { termType: 'NamedNode', value: 'https://schema.org/targetUrl', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/targetUrl' } as NamedNode,
	/** The Tax / Fiscal ID of the organization or person, e.g. the TIN in the US or the CIF/NIF in Spain. */
	'taxID': { termType: 'NamedNode', value: 'https://schema.org/taxID', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/taxID' } as NamedNode,
	/** The taxonomic rank of this taxon given preferably as a URI from a controlled vocabulary – typically the ranks from TDWG TaxonRank ontology or equivalent Wikidata URIs. */
	'taxonRank': { termType: 'NamedNode', value: 'https://schema.org/taxonRank', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/taxonRank' } as NamedNode,
	/** The taxonomic grouping of the organism that expresses, encodes, or in some way related to the BioChemEntity. */
	'taxonomicRange': { termType: 'NamedNode', value: 'https://schema.org/taxonomicRange', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/taxonomicRange' } as NamedNode,
	/** The item being described is intended to help a person learn the competency or learning outcome defined by the referenced term. */
	'teaches': { termType: 'NamedNode', value: 'https://schema.org/teaches', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/teaches' } as NamedNode,
	/** The telephone number. */
	'telephone': { termType: 'NamedNode', value: 'https://schema.org/telephone', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/telephone' } as NamedNode,
	/** The "temporal" property can be used in cases where more specific properties
(e.g. [[temporalCoverage]], [[dateCreated]], [[dateModified]], [[datePublished]]) are not known to be appropriate. */
	'temporal': { termType: 'NamedNode', value: 'https://schema.org/temporal', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/temporal' } as NamedNode,
	/** The temporalCoverage of a CreativeWork indicates the period that the content applies to, i.e. that it describes, either as a DateTime or as a textual string indicating a time period in [ISO 8601 time interval format](https://en.wikipedia.org/wiki/ISO_8601#Time_intervals). In
      the case of a Dataset it will typically indicate the relevant time period in a precise notation (e.g. for a 2011 census dataset, the year 2011 would be written "2011/2012"). Other forms of content, e.g. ScholarlyArticle, Book, TVSeries or TVEpisode, may indicate their temporalCoverage in broader terms - textually or via well-known URL.
      Written works such as books may sometimes have precise temporal coverage too, e.g. a work set in 1939 - 1945 can be indicated in ISO 8601 interval format format via "1939/1945".

Open-ended date ranges can be written with ".." in place of the end date. For example, "2015-11/.." indicates a range beginning in November 2015 and with no specified final date. This is tentative and might be updated in future when ISO 8601 is officially updated. */
	'temporalCoverage': { termType: 'NamedNode', value: 'https://schema.org/temporalCoverage', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/temporalCoverage' } as NamedNode,
	/** A code that identifies this [[DefinedTerm]] within a [[DefinedTermSet]] */
	'termCode': { termType: 'NamedNode', value: 'https://schema.org/termCode', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/termCode' } as NamedNode,
	/** The amount of time in a term as defined by the institution. A term is a length of time where students take one or more classes. Semesters and quarters are common units for term. */
	'termDuration': { termType: 'NamedNode', value: 'https://schema.org/termDuration', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/termDuration' } as NamedNode,
	/** Human-readable terms of service documentation. */
	'termsOfService': { termType: 'NamedNode', value: 'https://schema.org/termsOfService', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/termsOfService' } as NamedNode,
	/** The number of times terms of study are offered per year. Semesters and quarters are common units for term. For example, if the student can only take 2 semesters for the program in one year, then termsPerYear should be 2. */
	'termsPerYear': { termType: 'NamedNode', value: 'https://schema.org/termsPerYear', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/termsPerYear' } as NamedNode,
	/** The textual content of this CreativeWork. */
	'text': { termType: 'NamedNode', value: 'https://schema.org/text', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/text' } as NamedNode,
	/** Text value being annotated. */
	'textValue': { termType: 'NamedNode', value: 'https://schema.org/textValue', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/textValue' } as NamedNode,
	/** Thumbnail image for an image or video. */
	'thumbnail': { termType: 'NamedNode', value: 'https://schema.org/thumbnail', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/thumbnail' } as NamedNode,
	/** A thumbnail image relevant to the Thing. */
	'thumbnailUrl': { termType: 'NamedNode', value: 'https://schema.org/thumbnailUrl', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/thumbnailUrl' } as NamedNode,
	/** The exchange traded instrument associated with a Corporation object. The tickerSymbol is expressed as an exchange and an instrument name separated by a space character. For the exchange component of the tickerSymbol attribute, we recommend using the controlled vocabulary of Market Identifier Codes (MIC) specified in ISO 15022. */
	'tickerSymbol': { termType: 'NamedNode', value: 'https://schema.org/tickerSymbol', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/tickerSymbol' } as NamedNode,
	/** The unique identifier for the ticket. */
	'ticketNumber': { termType: 'NamedNode', value: 'https://schema.org/ticketNumber', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ticketNumber' } as NamedNode,
	/** Reference to an asset (e.g., Barcode, QR code image or PDF) usable for entrance. */
	'ticketToken': { termType: 'NamedNode', value: 'https://schema.org/ticketToken', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ticketToken' } as NamedNode,
	/** The seat associated with the ticket. */
	'ticketedSeat': { termType: 'NamedNode', value: 'https://schema.org/ticketedSeat', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/ticketedSeat' } as NamedNode,
	/** The time of day the program normally runs. For example, "evenings". */
	'timeOfDay': { termType: 'NamedNode', value: 'https://schema.org/timeOfDay', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/timeOfDay' } as NamedNode,
	/** Approximate or typical time it usually takes to work with or through the content of this work for the typical or target audience. */
	'timeRequired': { termType: 'NamedNode', value: 'https://schema.org/timeRequired', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/timeRequired' } as NamedNode,
	/** The expected length of time to complete the program if attending full-time. */
	'timeToComplete': { termType: 'NamedNode', value: 'https://schema.org/timeToComplete', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/timeToComplete' } as NamedNode,
	/** The type of tissue sample required for the test. */
	'tissueSample': { termType: 'NamedNode', value: 'https://schema.org/tissueSample', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/tissueSample' } as NamedNode,
	/** The title of the job. */
	'title': { termType: 'NamedNode', value: 'https://schema.org/title', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/title' } as NamedNode,
	/** An [EIDR](https://eidr.org/) (Entertainment Identifier Registry) [[identifier]] representing at the most general/abstract level, a work of film or television.

For example, the motion picture known as "Ghostbusters" has a titleEIDR of  "10.5240/7EC7-228A-510A-053E-CBB8-J". This title (or work) may have several variants, which EIDR calls "edits". See [[editEIDR]].

Since schema.org types like [[Movie]], [[TVEpisode]], [[TVSeason]], and [[TVSeries]] can be used for both works and their multiple expressions, it is possible to use [[titleEIDR]] alone (for a general description), or alongside [[editEIDR]] for a more edit-specific description.
 */
	'titleEIDR': { termType: 'NamedNode', value: 'https://schema.org/titleEIDR', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/titleEIDR' } as NamedNode,
	/** A sub property of location. The final location of the object or the agent after the action. */
	'toLocation': { termType: 'NamedNode', value: 'https://schema.org/toLocation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/toLocation' } as NamedNode,
	/** A sub property of recipient. The recipient who was directly sent the message. */
	'toRecipient': { termType: 'NamedNode', value: 'https://schema.org/toRecipient', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/toRecipient' } as NamedNode,
	/** A [[HyperTocEntry]] can have a [[tocContinuation]] indicated, which is another [[HyperTocEntry]] that would be the default next item to play or render. */
	'tocContinuation': { termType: 'NamedNode', value: 'https://schema.org/tocContinuation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/tocContinuation' } as NamedNode,
	/** Indicates a [[HyperTocEntry]] in a [[HyperToc]]. */
	'tocEntry': { termType: 'NamedNode', value: 'https://schema.org/tocEntry', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/tocEntry' } as NamedNode,
	/** The permitted vertical load (TWR) of a trailer attached to the vehicle. Also referred to as Tongue Load Rating (TLR) or Vertical Load Rating (VLR).\n\nTypical unit code(s): KGM for kilogram, LBR for pound\n\n* Note 1: You can indicate additional information in the [[name]] of the [[QuantitativeValue]] node.\n* Note 2: You may also link to a [[QualitativeValue]] node that provides additional information using [[valueReference]].\n* Note 3: Note that you can use [[minValue]] and [[maxValue]] to indicate ranges. */
	'tongueWeight': { termType: 'NamedNode', value: 'https://schema.org/tongueWeight', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/tongueWeight' } as NamedNode,
	/** A sub property of instrument. An object used (but not consumed) when performing instructions or a direction. */
	'tool': { termType: 'NamedNode', value: 'https://schema.org/tool', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/tool' } as NamedNode,
	/** The torque (turning force) of the vehicle's engine.\n\nTypical unit code(s): NU for newton metre (N m), F17 for pound-force per foot, or F48 for pound-force per inch\n\n* Note 1: You can link to information about how the given value has been determined (e.g. reference RPM) using the [[valueReference]] property.\n* Note 2: You can use [[minValue]] and [[maxValue]] to indicate ranges. */
	'torque': { termType: 'NamedNode', value: 'https://schema.org/torque', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/torque' } as NamedNode,
	/** The total number of students that have enrolled in the history of the course. */
	'totalHistoricalEnrollment': { termType: 'NamedNode', value: 'https://schema.org/totalHistoricalEnrollment', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/totalHistoricalEnrollment' } as NamedNode,
	/** The number of positions open for this job posting. Use a positive integer. Do not use if the number of positions is unclear or not known. */
	'totalJobOpenings': { termType: 'NamedNode', value: 'https://schema.org/totalJobOpenings', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/totalJobOpenings' } as NamedNode,
	/** The total amount due. */
	'totalPaymentDue': { termType: 'NamedNode', value: 'https://schema.org/totalPaymentDue', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/totalPaymentDue' } as NamedNode,
	/** The total price for the reservation or ticket, including applicable taxes, shipping, etc.\n\nUsage guidelines:\n\n* Use values from 0123456789 (Unicode 'DIGIT ZERO' (U+0030) to 'DIGIT NINE' (U+0039)) rather than superficially similar Unicode symbols.\n* Use '.' (Unicode 'FULL STOP' (U+002E)) rather than ',' to indicate a decimal point. Avoid using these symbols as a readability separator. */
	'totalPrice': { termType: 'NamedNode', value: 'https://schema.org/totalPrice', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/totalPrice' } as NamedNode,
	/** The total time required to perform instructions or a direction (including time to prepare the supplies), in [ISO 8601 duration format](http://en.wikipedia.org/wiki/ISO_8601). */
	'totalTime': { termType: 'NamedNode', value: 'https://schema.org/totalTime', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/totalTime' } as NamedNode,
	/** A page providing information on how to book a tour of some [[Place]], such as an [[Accommodation]] or [[ApartmentComplex]] in a real estate setting, as well as other kinds of tours as appropriate. */
	'tourBookingPage': { termType: 'NamedNode', value: 'https://schema.org/tourBookingPage', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/tourBookingPage' } as NamedNode,
	/** Attraction suitable for type(s) of tourist. E.g. children, visitors from a particular country, etc.  */
	'touristType': { termType: 'NamedNode', value: 'https://schema.org/touristType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/touristType' } as NamedNode,
	/** A music recording (track)&#x2014;usually a single song. If an ItemList is given, the list should contain items of type MusicRecording. */
	'track': { termType: 'NamedNode', value: 'https://schema.org/track', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/track' } as NamedNode,
	/** Shipper tracking number. */
	'trackingNumber': { termType: 'NamedNode', value: 'https://schema.org/trackingNumber', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/trackingNumber' } as NamedNode,
	/** Tracking url for the parcel delivery. */
	'trackingUrl': { termType: 'NamedNode', value: 'https://schema.org/trackingUrl', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/trackingUrl' } as NamedNode,
	/** A music recording (track)&#x2014;usually a single song. */
	'tracks': { termType: 'NamedNode', value: 'https://schema.org/tracks', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/tracks' } as NamedNode,
	/** The trailer of a movie or TV/radio series, season, episode, etc. */
	'trailer': { termType: 'NamedNode', value: 'https://schema.org/trailer', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/trailer' } as NamedNode,
	/** The permitted weight of a trailer attached to the vehicle.\n\nTypical unit code(s): KGM for kilogram, LBR for pound\n* Note 1: You can indicate additional information in the [[name]] of the [[QuantitativeValue]] node.\n* Note 2: You may also link to a [[QualitativeValue]] node that provides additional information using [[valueReference]].\n* Note 3: Note that you can use [[minValue]] and [[maxValue]] to indicate ranges. */
	'trailerWeight': { termType: 'NamedNode', value: 'https://schema.org/trailerWeight', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/trailerWeight' } as NamedNode,
	/** The name of the train (e.g. The Orient Express). */
	'trainName': { termType: 'NamedNode', value: 'https://schema.org/trainName', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/trainName' } as NamedNode,
	/** The unique identifier for the train. */
	'trainNumber': { termType: 'NamedNode', value: 'https://schema.org/trainNumber', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/trainNumber' } as NamedNode,
	/** The estimated salary earned while in the program. */
	'trainingSalary': { termType: 'NamedNode', value: 'https://schema.org/trainingSalary', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/trainingSalary' } as NamedNode,
	/** The number of grams of trans fat. */
	'transFatContent': { termType: 'NamedNode', value: 'https://schema.org/transFatContent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/transFatContent' } as NamedNode,
	/** If this MediaObject is an AudioObject or VideoObject, the transcript of that object. */
	'transcript': { termType: 'NamedNode', value: 'https://schema.org/transcript', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/transcript' } as NamedNode,
	/** The typical delay the order has been sent for delivery and the goods reach the final customer. Typical properties: minValue, maxValue, unitCode (d for DAY). */
	'transitTime': { termType: 'NamedNode', value: 'https://schema.org/transitTime', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/transitTime' } as NamedNode,
	/** Label to match an [[OfferShippingDetails]] with a [[DeliveryTimeSettings]] (within the context of a [[shippingSettingsLink]] cross-reference). */
	'transitTimeLabel': { termType: 'NamedNode', value: 'https://schema.org/transitTimeLabel', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/transitTimeLabel' } as NamedNode,
	/** The work that this work has been translated from. E.g. 物种起源 is a translationOf “On the Origin of Species”. */
	'translationOfWork': { termType: 'NamedNode', value: 'https://schema.org/translationOfWork', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/translationOfWork' } as NamedNode,
	/** Organization or person who adapts a creative work to different languages, regional differences and technical requirements of a target market, or that translates during some event. */
	'translator': { termType: 'NamedNode', value: 'https://schema.org/translator', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/translator' } as NamedNode,
	/** How the disease spreads, either as a route or vector, for example 'direct contact', 'Aedes aegypti', etc. */
	'transmissionMethod': { termType: 'NamedNode', value: 'https://schema.org/transmissionMethod', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/transmissionMethod' } as NamedNode,
	/** Information about travel bans, e.g. in the context of a pandemic. */
	'travelBans': { termType: 'NamedNode', value: 'https://schema.org/travelBans', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/travelBans' } as NamedNode,
	/** Specifics about the trial design (enumerated). */
	'trialDesign': { termType: 'NamedNode', value: 'https://schema.org/trialDesign', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/trialDesign' } as NamedNode,
	/** The anatomical or organ system that the vein flows into; a larger structure that the vein connects to. */
	'tributary': { termType: 'NamedNode', value: 'https://schema.org/tributary', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/tributary' } as NamedNode,
	/** The location of origin of the trip, prior to any destination(s). */
	'tripOrigin': { termType: 'NamedNode', value: 'https://schema.org/tripOrigin', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/tripOrigin' } as NamedNode,
	/** The type of bed to which the BedDetail refers, i.e. the type of bed available in the quantity indicated by quantity. */
	'typeOfBed': { termType: 'NamedNode', value: 'https://schema.org/typeOfBed', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/typeOfBed' } as NamedNode,
	/** The product that this structured value is referring to. */
	'typeOfGood': { termType: 'NamedNode', value: 'https://schema.org/typeOfGood', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/typeOfGood' } as NamedNode,
	/** The typical expected age range, e.g. '7-9', '11-'. */
	'typicalAgeRange': { termType: 'NamedNode', value: 'https://schema.org/typicalAgeRange', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/typicalAgeRange' } as NamedNode,
	/** The number of credits or units a full-time student would be expected to take in 1 term however 'term' is defined by the institution. */
	'typicalCreditsPerTerm': { termType: 'NamedNode', value: 'https://schema.org/typicalCreditsPerTerm', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/typicalCreditsPerTerm' } as NamedNode,
	/** A medical test typically performed given this condition. */
	'typicalTest': { termType: 'NamedNode', value: 'https://schema.org/typicalTest', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/typicalTest' } as NamedNode,
	/** The person or organization the reservation or ticket is for. */
	'underName': { termType: 'NamedNode', value: 'https://schema.org/underName', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/underName' } as NamedNode,
	/** The unit of measurement given using the UN/CEFACT Common Code (3 characters) or a URL. Other codes than the UN/CEFACT Common Code may be used with a prefix followed by a colon. */
	'unitCode': { termType: 'NamedNode', value: 'https://schema.org/unitCode', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/unitCode' } as NamedNode,
	/** A string or text indicating the unit of measurement. Useful if you cannot provide a standard unit code for
<a href='unitCode'>unitCode</a>. */
	'unitText': { termType: 'NamedNode', value: 'https://schema.org/unitText', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/unitText' } as NamedNode,
	/** For an [[Organization]] (typically a [[NewsMediaOrganization]]), a statement about policy on use of unnamed sources and the decision process required. */
	'unnamedSourcesPolicy': { termType: 'NamedNode', value: 'https://schema.org/unnamedSourcesPolicy', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/unnamedSourcesPolicy' } as NamedNode,
	/** The number of grams of unsaturated fat. */
	'unsaturatedFatContent': { termType: 'NamedNode', value: 'https://schema.org/unsaturatedFatContent', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/unsaturatedFatContent' } as NamedNode,
	/** Date when this media object was uploaded to this site. */
	'uploadDate': { termType: 'NamedNode', value: 'https://schema.org/uploadDate', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/uploadDate' } as NamedNode,
	/** The number of upvotes this question, answer or comment has received from the community. */
	'upvoteCount': { termType: 'NamedNode', value: 'https://schema.org/upvoteCount', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/upvoteCount' } as NamedNode,
	/** URL of the item. */
	'url': { termType: 'NamedNode', value: 'https://schema.org/url', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/url' } as NamedNode,
	/** An url template (RFC6570) that will be used to construct the target of the execution of the action. */
	'urlTemplate': { termType: 'NamedNode', value: 'https://schema.org/urlTemplate', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/urlTemplate' } as NamedNode,
	/** The schema.org [[usageInfo]] property indicates further information about a [[CreativeWork]]. This property is applicable both to works that are freely available and to those that require payment or other transactions. It can reference additional information, e.g. community expectations on preferred linking and citation conventions, as well as purchasing details. For something that can be commercially licensed, usageInfo can provide detailed, resource-specific information about licensing options.

This property can be used alongside the license property which indicates license(s) applicable to some piece of content. The usageInfo property can provide information about other licensing options, e.g. acquiring commercial usage rights for an image that is also available under non-commercial creative commons licenses. */
	'usageInfo': { termType: 'NamedNode', value: 'https://schema.org/usageInfo', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/usageInfo' } as NamedNode,
	/** A condition the test is used to diagnose. */
	'usedToDiagnose': { termType: 'NamedNode', value: 'https://schema.org/usedToDiagnose', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/usedToDiagnose' } as NamedNode,
	/** The number of interactions for the CreativeWork using the WebSite or SoftwareApplication. */
	'userInteractionCount': { termType: 'NamedNode', value: 'https://schema.org/userInteractionCount', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/userInteractionCount' } as NamedNode,
	/** Device used to perform the test. */
	'usesDevice': { termType: 'NamedNode', value: 'https://schema.org/usesDevice', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/usesDevice' } as NamedNode,
	/** The standard for interpreting the Plan ID. The preferred is "HIOS". See the Centers for Medicare & Medicaid Services for more details. */
	'usesHealthPlanIdStandard': { termType: 'NamedNode', value: 'https://schema.org/usesHealthPlanIdStandard', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/usesHealthPlanIdStandard' } as NamedNode,
	/** Text of an utterances (spoken words, lyrics etc.) that occurs at a certain section of a media object, represented as a [[HyperTocEntry]]. */
	'utterances': { termType: 'NamedNode', value: 'https://schema.org/utterances', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/utterances' } as NamedNode,
	/** The duration of validity of a permit or similar thing. */
	'validFor': { termType: 'NamedNode', value: 'https://schema.org/validFor', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/validFor' } as NamedNode,
	/** The date when the item becomes valid. */
	'validFrom': { termType: 'NamedNode', value: 'https://schema.org/validFrom', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/validFrom' } as NamedNode,
	/** The geographic area where a permit or similar thing is valid. */
	'validIn': { termType: 'NamedNode', value: 'https://schema.org/validIn', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/validIn' } as NamedNode,
	/** The date after when the item is not valid. For example the end of an offer, salary period, or a period of opening hours. */
	'validThrough': { termType: 'NamedNode', value: 'https://schema.org/validThrough', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/validThrough' } as NamedNode,
	/** The date when the item is no longer valid. */
	'validUntil': { termType: 'NamedNode', value: 'https://schema.org/validUntil', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/validUntil' } as NamedNode,
	/** The value of a [[QuantitativeValue]] (including [[Observation]]) or property value node.\n\n* For [[QuantitativeValue]] and [[MonetaryAmount]], the recommended type for values is 'Number'.\n* For [[PropertyValue]], it can be 'Text', 'Number', 'Boolean', or 'StructuredValue'.\n* Use values from 0123456789 (Unicode 'DIGIT ZERO' (U+0030) to 'DIGIT NINE' (U+0039)) rather than superficially similar Unicode symbols.\n* Use '.' (Unicode 'FULL STOP' (U+002E)) rather than ',' to indicate a decimal point. Avoid using these symbols as a readability separator. */
	'value': { termType: 'NamedNode', value: 'https://schema.org/value', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/value' } as NamedNode,
	/** Specifies whether the applicable value-added tax (VAT) is included in the price specification or not. */
	'valueAddedTaxIncluded': { termType: 'NamedNode', value: 'https://schema.org/valueAddedTaxIncluded', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/valueAddedTaxIncluded' } as NamedNode,
	/** Specifies the allowed range for number of characters in a literal value. */
	'valueMaxLength': { termType: 'NamedNode', value: 'https://schema.org/valueMaxLength', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/valueMaxLength' } as NamedNode,
	/** Specifies the minimum allowed range for number of characters in a literal value. */
	'valueMinLength': { termType: 'NamedNode', value: 'https://schema.org/valueMinLength', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/valueMinLength' } as NamedNode,
	/** Indicates the name of the PropertyValueSpecification to be used in URL templates and form encoding in a manner analogous to HTML's input@name. */
	'valueName': { termType: 'NamedNode', value: 'https://schema.org/valueName', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/valueName' } as NamedNode,
	/** Specifies a regular expression for testing literal values according to the HTML spec. */
	'valuePattern': { termType: 'NamedNode', value: 'https://schema.org/valuePattern', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/valuePattern' } as NamedNode,
	/** A secondary value that provides additional information on the original value, e.g. a reference temperature or a type of measurement. */
	'valueReference': { termType: 'NamedNode', value: 'https://schema.org/valueReference', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/valueReference' } as NamedNode,
	/** Whether the property must be filled in to complete the action.  Default is false. */
	'valueRequired': { termType: 'NamedNode', value: 'https://schema.org/valueRequired', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/valueRequired' } as NamedNode,
	/** The variableMeasured property can indicate (repeated as necessary) the  variables that are measured in some dataset, either described as text or as pairs of identifier and description using PropertyValue, or more explicitly as a [[StatisticalVariable]]. */
	'variableMeasured': { termType: 'NamedNode', value: 'https://schema.org/variableMeasured', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/variableMeasured' } as NamedNode,
	/** A description of the variant cover
    	for the issue, if the issue is a variant printing. For example, "Bryan Hitch
    	Variant Cover" or "2nd Printing Variant". */
	'variantCover': { termType: 'NamedNode', value: 'https://schema.org/variantCover', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/variantCover' } as NamedNode,
	/** Indicates the property or properties by which the variants in a [[ProductGroup]] vary, e.g. their size, color etc. Schema.org properties can be referenced by their short name e.g. "color"; terms defined elsewhere can be referenced with their URIs. */
	'variesBy': { termType: 'NamedNode', value: 'https://schema.org/variesBy', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/variesBy' } as NamedNode,
	/** The Value-added Tax ID of the organization or person. */
	'vatID': { termType: 'NamedNode', value: 'https://schema.org/vatID', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/vatID' } as NamedNode,
	/** A short text indicating the configuration of the vehicle, e.g. '5dr hatchback ST 2.5 MT 225 hp' or 'limited edition'. */
	'vehicleConfiguration': { termType: 'NamedNode', value: 'https://schema.org/vehicleConfiguration', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/vehicleConfiguration' } as NamedNode,
	/** Information about the engine or engines of the vehicle. */
	'vehicleEngine': { termType: 'NamedNode', value: 'https://schema.org/vehicleEngine', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/vehicleEngine' } as NamedNode,
	/** The Vehicle Identification Number (VIN) is a unique serial number used by the automotive industry to identify individual motor vehicles. */
	'vehicleIdentificationNumber': { termType: 'NamedNode', value: 'https://schema.org/vehicleIdentificationNumber', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/vehicleIdentificationNumber' } as NamedNode,
	/** The color or color combination of the interior of the vehicle. */
	'vehicleInteriorColor': { termType: 'NamedNode', value: 'https://schema.org/vehicleInteriorColor', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/vehicleInteriorColor' } as NamedNode,
	/** The type or material of the interior of the vehicle (e.g. synthetic fabric, leather, wood, etc.). While most interior types are characterized by the material used, an interior type can also be based on vehicle usage or target audience. */
	'vehicleInteriorType': { termType: 'NamedNode', value: 'https://schema.org/vehicleInteriorType', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/vehicleInteriorType' } as NamedNode,
	/** The release date of a vehicle model (often used to differentiate versions of the same make and model). */
	'vehicleModelDate': { termType: 'NamedNode', value: 'https://schema.org/vehicleModelDate', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/vehicleModelDate' } as NamedNode,
	/** The number of passengers that can be seated in the vehicle, both in terms of the physical space available, and in terms of limitations set by law.\n\nTypical unit code(s): C62 for persons. */
	'vehicleSeatingCapacity': { termType: 'NamedNode', value: 'https://schema.org/vehicleSeatingCapacity', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/vehicleSeatingCapacity' } as NamedNode,
	/** Indicates whether the vehicle has been used for special purposes, like commercial rental, driving school, or as a taxi. The legislation in many countries requires this information to be revealed when offering a car for sale. */
	'vehicleSpecialUsage': { termType: 'NamedNode', value: 'https://schema.org/vehicleSpecialUsage', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/vehicleSpecialUsage' } as NamedNode,
	/** The type of component used for transmitting the power from a rotating power source to the wheels or other relevant component(s) ("gearbox" for cars). */
	'vehicleTransmission': { termType: 'NamedNode', value: 'https://schema.org/vehicleTransmission', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/vehicleTransmission' } as NamedNode,
	/** 'vendor' is an earlier term for 'seller'. */
	'vendor': { termType: 'NamedNode', value: 'https://schema.org/vendor', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/vendor' } as NamedNode,
	/** Disclosure about verification and fact-checking processes for a [[NewsMediaOrganization]] or other fact-checking [[Organization]]. */
	'verificationFactCheckingPolicy': { termType: 'NamedNode', value: 'https://schema.org/verificationFactCheckingPolicy', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/verificationFactCheckingPolicy' } as NamedNode,
	/** The version of the CreativeWork embodied by a specified resource. */
	'version': { termType: 'NamedNode', value: 'https://schema.org/version', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/version' } as NamedNode,
	/** An embedded video object. */
	'video': { termType: 'NamedNode', value: 'https://schema.org/video', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/video' } as NamedNode,
	/** The type of screening or video broadcast used (e.g. IMAX, 3D, SD, HD, etc.). */
	'videoFormat': { termType: 'NamedNode', value: 'https://schema.org/videoFormat', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/videoFormat' } as NamedNode,
	/** The frame size of the video. */
	'videoFrameSize': { termType: 'NamedNode', value: 'https://schema.org/videoFrameSize', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/videoFrameSize' } as NamedNode,
	/** The quality of the video. */
	'videoQuality': { termType: 'NamedNode', value: 'https://schema.org/videoQuality', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/videoQuality' } as NamedNode,
	/** Identifies the volume of publication or multi-part work; for example, "iii" or "2". */
	'volumeNumber': { termType: 'NamedNode', value: 'https://schema.org/volumeNumber', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/volumeNumber' } as NamedNode,
	/** Any FDA or other warnings about the drug (text or URL). */
	'warning': { termType: 'NamedNode', value: 'https://schema.org/warning', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/warning' } as NamedNode,
	/** The warranty promise(s) included in the offer. */
	'warranty': { termType: 'NamedNode', value: 'https://schema.org/warranty', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/warranty' } as NamedNode,
	/** The warranty promise(s) included in the offer. */
	'warrantyPromise': { termType: 'NamedNode', value: 'https://schema.org/warrantyPromise', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/warrantyPromise' } as NamedNode,
	/** The scope of the warranty promise. */
	'warrantyScope': { termType: 'NamedNode', value: 'https://schema.org/warrantyScope', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/warrantyScope' } as NamedNode,
	/** The time when a passenger can check into the flight online. */
	'webCheckinTime': { termType: 'NamedNode', value: 'https://schema.org/webCheckinTime', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/webCheckinTime' } as NamedNode,
	/** The URL for a feed, e.g. associated with a podcast series, blog, or series of date-stamped updates. This is usually RSS or Atom. */
	'webFeed': { termType: 'NamedNode', value: 'https://schema.org/webFeed', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/webFeed' } as NamedNode,
	/** The weight of the product or person. */
	'weight': { termType: 'NamedNode', value: 'https://schema.org/weight', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/weight' } as NamedNode,
	/** The permitted total weight of the loaded vehicle, including passengers and cargo and the weight of the empty vehicle.\n\nTypical unit code(s): KGM for kilogram, LBR for pound\n\n* Note 1: You can indicate additional information in the [[name]] of the [[QuantitativeValue]] node.\n* Note 2: You may also link to a [[QualitativeValue]] node that provides additional information using [[valueReference]].\n* Note 3: Note that you can use [[minValue]] and [[maxValue]] to indicate ranges. */
	'weightTotal': { termType: 'NamedNode', value: 'https://schema.org/weightTotal', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/weightTotal' } as NamedNode,
	/** The distance between the centers of the front and rear wheels.\n\nTypical unit code(s): CMT for centimeters, MTR for meters, INH for inches, FOT for foot/feet */
	'wheelbase': { termType: 'NamedNode', value: 'https://schema.org/wheelbase', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/wheelbase' } as NamedNode,
	/** The width of the item. */
	'width': { termType: 'NamedNode', value: 'https://schema.org/width', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/width' } as NamedNode,
	/** A sub property of participant. The winner of the action. */
	'winner': { termType: 'NamedNode', value: 'https://schema.org/winner', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/winner' } as NamedNode,
	/** The number of words in the text of the Article. */
	'wordCount': { termType: 'NamedNode', value: 'https://schema.org/wordCount', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/wordCount' } as NamedNode,
	/** Example/instance/realization/derivation of the concept of this creative work. E.g. the paperback edition, first edition, or e-book. */
	'workExample': { termType: 'NamedNode', value: 'https://schema.org/workExample', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/workExample' } as NamedNode,
	/** A work featured in some event, e.g. exhibited in an ExhibitionEvent.
       Specific subproperties are available for workPerformed (e.g. a play), or a workPresented (a Movie at a ScreeningEvent). */
	'workFeatured': { termType: 'NamedNode', value: 'https://schema.org/workFeatured', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/workFeatured' } as NamedNode,
	/** The typical working hours for this job (e.g. 1st shift, night shift, 8am-5pm). */
	'workHours': { termType: 'NamedNode', value: 'https://schema.org/workHours', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/workHours' } as NamedNode,
	/** A contact location for a person's place of work. */
	'workLocation': { termType: 'NamedNode', value: 'https://schema.org/workLocation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/workLocation' } as NamedNode,
	/** A work performed in some event, for example a play performed in a TheaterEvent. */
	'workPerformed': { termType: 'NamedNode', value: 'https://schema.org/workPerformed', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/workPerformed' } as NamedNode,
	/** The movie presented during this event. */
	'workPresented': { termType: 'NamedNode', value: 'https://schema.org/workPresented', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/workPresented' } as NamedNode,
	/** A work that is a translation of the content of this work. E.g. 西遊記 has an English workTranslation “Journey to the West”, a German workTranslation “Monkeys Pilgerfahrt” and a Vietnamese  translation Tây du ký bình khảo. */
	'workTranslation': { termType: 'NamedNode', value: 'https://schema.org/workTranslation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/workTranslation' } as NamedNode,
	/** Quantitative measure of the physiologic output of the exercise; also referred to as energy expenditure. */
	'workload': { termType: 'NamedNode', value: 'https://schema.org/workload', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/workload' } as NamedNode,
	/** Organizations that the person works for. */
	'worksFor': { termType: 'NamedNode', value: 'https://schema.org/worksFor', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/worksFor' } as NamedNode,
	/** The lowest value allowed in this rating system. If worstRating is omitted, 1 is assumed. */
	'worstRating': { termType: 'NamedNode', value: 'https://schema.org/worstRating', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/worstRating' } as NamedNode,
	/** An XPath, e.g. of a [[SpeakableSpecification]] or [[WebPageElement]]. In the latter case, multiple matches within a page can constitute a single conceptual "Web page element". */
	'xpath': { termType: 'NamedNode', value: 'https://schema.org/xpath', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/xpath' } as NamedNode,
	/** The year an [[Accommodation]] was constructed. This corresponds to the [YearBuilt field in RESO](https://ddwiki.reso.org/display/DDW17/YearBuilt+Field).  */
	'yearBuilt': { termType: 'NamedNode', value: 'https://schema.org/yearBuilt', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/yearBuilt' } as NamedNode,
	/** The size of the business in annual revenue. */
	'yearlyRevenue': { termType: 'NamedNode', value: 'https://schema.org/yearlyRevenue', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/yearlyRevenue' } as NamedNode,
	/** The age of the business. */
	'yearsInOperation': { termType: 'NamedNode', value: 'https://schema.org/yearsInOperation', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/yearsInOperation' } as NamedNode,
	/** The quantity that results by performing instructions. For example, a paper airplane, 10 personalized candles. */
	'yield': { termType: 'NamedNode', value: 'https://schema.org/yield', equals: (other: any) => other && (other.termType === 'NamedNode' || other.type === 'NamedNode') && other.value === 'https://schema.org/yield' } as NamedNode,
}